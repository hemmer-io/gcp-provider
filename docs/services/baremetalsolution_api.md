# Baremetalsolution_api Service



**Resources**: 15

---

## Overview

The baremetalsolution_api service provides access to 15 resource types:

- [Operation](#operation) [CRD]
- [Lun](#lun) [CR]
- [Os_image](#os_image) [R]
- [Provisioning_quota](#provisioning_quota) [R]
- [Nfs_share](#nfs_share) [CRUD]
- [Provisioning_config](#provisioning_config) [CRU]
- [Location](#location) [R]
- [Snapshot](#snapshot) [CRD]
- [Operation](#operation) [R]
- [Instance](#instance) [CRU]
- [Ssh_key](#ssh_key) [CRD]
- [Volume](#volume) [CRU]
- [Network](#network) [CRU]
- [Location](#location) [C]
- [Provisioning_quota](#provisioning_quota) [R]

---

## Resources


### Operation

Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns `google.rpc.Code.UNIMPLEMENTED`. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to `Code.CANCELLED`.

**Operations**: ✅ Create ✅ Read ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `name` | String | ✅ | The name of the operation resource to be cancelled. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `name` | String | The server-assigned name, which is only unique within the same service that originally returns it. If you use the default HTTP mapping, the `name` should be a resource name ending with `operations/{unique_id}`. |
| `error` | String | The error result of the operation in case of failure or cancellation. |
| `metadata` | HashMap<String, String> | Service-specific metadata associated with the operation. It typically contains progress information and common metadata such as create time. Some services might not provide such metadata. Any method that returns a long-running operation should document the metadata type, if any. |
| `response` | HashMap<String, String> | The normal response of the operation in case of success. If the original method returns no data on success, such as `Delete`, the response is `google.protobuf.Empty`. If the original method is standard `Get`/`Create`/`Update`, the response should be the resource. For other methods, the response should have the type `XxxResponse`, where `Xxx` is the original method name. For example, if the original method name is `TakeSnapshot()`, the inferred response type is `TakeSnapshotResponse`. |
| `done` | bool | If the value is `false`, it means the operation is still in progress. If `true`, the operation is completed, and either `error` or `response` is available. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create operation
operation = provider.baremetalsolution_api.Operation {
    name = "value"  # The name of the operation resource to be cancelled.
}

# Access operation outputs
operation_id = operation.id
operation_name = operation.name
operation_error = operation.error
operation_metadata = operation.metadata
operation_response = operation.response
operation_done = operation.done
```

---


### Lun

Skips lun's cooloff and deletes it now. Lun must be in cooloff state.

**Operations**: ✅ Create ✅ Read

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `name` | String | ✅ | Required. The name of the lun. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `size_gb` | String | The size of this LUN, in GiB. |
| `expire_time` | String | Output only. Time after which LUN will be fully deleted. It is filled only for LUNs in COOL_OFF state. |
| `storage_volume` | String | Display the storage volume for this LUN. |
| `wwid` | String | The WWID for this LUN. |
| `multiprotocol_type` | String | The LUN multiprotocol type ensures the characteristics of the LUN are optimized for each operating system. |
| `state` | String | The state of this storage volume. |
| `instances` | Vec<String> | Output only. Instances this Lun is attached to. |
| `name` | String | Output only. The name of the LUN. |
| `id` | String | An identifier for the LUN, generated by the backend. |
| `storage_type` | String | The storage type for this LUN. |
| `shareable` | bool | Display if this LUN can be shared between multiple physical servers. |
| `boot_lun` | bool | Display if this LUN is a boot LUN. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create lun
lun = provider.baremetalsolution_api.Lun {
    name = "value"  # Required. The name of the lun.
}

# Access lun outputs
lun_id = lun.id
lun_size_gb = lun.size_gb
lun_expire_time = lun.expire_time
lun_storage_volume = lun.storage_volume
lun_wwid = lun.wwid
lun_multiprotocol_type = lun.multiprotocol_type
lun_state = lun.state
lun_instances = lun.instances
lun_name = lun.name
lun_id = lun.id
lun_storage_type = lun.storage_type
lun_shareable = lun.shareable
lun_boot_lun = lun.boot_lun
```

---


### Os_image

Get details of a single OS image.

**Operations**: ✅ Read

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `code` | String | OS Image code. |
| `name` | String | Output only. OS Image's unique name. |
| `applicable_instance_types` | Vec<String> | Instance types this image is applicable to. [Available types](https://cloud.google.com/bare-metal/docs/bms-planning#server_configurations) |
| `description` | String | OS Image description. |
| `supported_network_templates` | Vec<String> | Network templates that can be used with this OS Image. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Access os_image outputs
os_image_id = os_image.id
os_image_code = os_image.code
os_image_name = os_image.name
os_image_applicable_instance_types = os_image.applicable_instance_types
os_image_description = os_image.description
os_image_supported_network_templates = os_image.supported_network_templates
```

---


### Provisioning_quota

List the budget details to provision resources on a given project.

**Operations**: ✅ Read

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `provisioning_quotas` | Vec<String> | The provisioning quotas registered in this project. |
| `next_page_token` | String | Token to retrieve the next page of results, or empty if there are no more results in the list. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Access provisioning_quota outputs
provisioning_quota_id = provisioning_quota.id
provisioning_quota_provisioning_quotas = provisioning_quota.provisioning_quotas
provisioning_quota_next_page_token = provisioning_quota.next_page_token
```

---


### Nfs_share

Create an NFS share.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `storage_type` | String |  | Immutable. The storage type of the underlying volume. |
| `volume` | String |  | Output only. The underlying volume of the share. Created automatically during provisioning. |
| `requested_size_gib` | String |  | The requested size, in GiB. |
| `allowed_clients` | Vec<String> |  | List of allowed access points. |
| `nfs_share_id` | String |  | Output only. An identifier for the NFS share, generated by the backend. This field will be deprecated in the future, use `id` instead. |
| `state` | String |  | Output only. The state of the NFS share. |
| `name` | String |  | Immutable. The name of the NFS share. |
| `id` | String |  | Output only. An identifier for the NFS share, generated by the backend. This is the same value as nfs_share_id and will replace it in the future. |
| `labels` | HashMap<String, String> |  | Labels as key value pairs. |
| `pod` | String |  | Immutable. Pod name. Pod is an independent part of infrastructure. NFSShare can only be connected to the assets (networks, instances) allocated in the same pod. |
| `parent` | String | ✅ | Required. The parent project and location. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `storage_type` | String | Immutable. The storage type of the underlying volume. |
| `volume` | String | Output only. The underlying volume of the share. Created automatically during provisioning. |
| `requested_size_gib` | String | The requested size, in GiB. |
| `allowed_clients` | Vec<String> | List of allowed access points. |
| `nfs_share_id` | String | Output only. An identifier for the NFS share, generated by the backend. This field will be deprecated in the future, use `id` instead. |
| `state` | String | Output only. The state of the NFS share. |
| `name` | String | Immutable. The name of the NFS share. |
| `id` | String | Output only. An identifier for the NFS share, generated by the backend. This is the same value as nfs_share_id and will replace it in the future. |
| `labels` | HashMap<String, String> | Labels as key value pairs. |
| `pod` | String | Immutable. Pod name. Pod is an independent part of infrastructure. NFSShare can only be connected to the assets (networks, instances) allocated in the same pod. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create nfs_share
nfs_share = provider.baremetalsolution_api.Nfs_share {
    parent = "value"  # Required. The parent project and location.
}

# Access nfs_share outputs
nfs_share_id = nfs_share.id
nfs_share_storage_type = nfs_share.storage_type
nfs_share_volume = nfs_share.volume
nfs_share_requested_size_gib = nfs_share.requested_size_gib
nfs_share_allowed_clients = nfs_share.allowed_clients
nfs_share_nfs_share_id = nfs_share.nfs_share_id
nfs_share_state = nfs_share.state
nfs_share_name = nfs_share.name
nfs_share_id = nfs_share.id
nfs_share_labels = nfs_share.labels
nfs_share_pod = nfs_share.pod
```

---


### Provisioning_config

Create new ProvisioningConfig.

**Operations**: ✅ Create ✅ Read ✅ Update

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `location` | String |  | Optional. Location name of this ProvisioningConfig. It is optional only for Intake UI transition period. |
| `cloud_console_uri` | String |  | Output only. URI to Cloud Console UI view of this provisioning config. |
| `ticket_id` | String |  | A generated ticket id to track provisioning request. |
| `handover_service_account` | String |  | A service account to enable customers to access instance credentials upon handover. |
| `status_message` | String |  | Optional status messages associated with the FAILED state. |
| `custom_id` | String |  | Optional. The user-defined identifier of the provisioning config. |
| `state` | String |  | Output only. State of ProvisioningConfig. |
| `vpc_sc_enabled` | bool |  | If true, VPC SC is enabled for the cluster. |
| `networks` | Vec<String> |  | Networks to be created. |
| `email` | String |  | Email provided to send a confirmation with provisioning config to. Deprecated in favour of email field in request messages. |
| `instances` | Vec<String> |  | Instances to be created. |
| `volumes` | Vec<String> |  | Volumes to be created. |
| `pod` | String |  | Optional. Pod name. Pod is an independent part of infrastructure. Instance can be connected to the assets (networks, volumes, nfsshares) allocated in the same pod only. |
| `update_time` | String |  | Output only. Last update timestamp. |
| `name` | String |  | Output only. The system-generated name of the provisioning config. This follows the UUID format. |
| `parent` | String | ✅ | Required. The parent project and location containing the ProvisioningConfig. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `location` | String | Optional. Location name of this ProvisioningConfig. It is optional only for Intake UI transition period. |
| `cloud_console_uri` | String | Output only. URI to Cloud Console UI view of this provisioning config. |
| `ticket_id` | String | A generated ticket id to track provisioning request. |
| `handover_service_account` | String | A service account to enable customers to access instance credentials upon handover. |
| `status_message` | String | Optional status messages associated with the FAILED state. |
| `custom_id` | String | Optional. The user-defined identifier of the provisioning config. |
| `state` | String | Output only. State of ProvisioningConfig. |
| `vpc_sc_enabled` | bool | If true, VPC SC is enabled for the cluster. |
| `networks` | Vec<String> | Networks to be created. |
| `email` | String | Email provided to send a confirmation with provisioning config to. Deprecated in favour of email field in request messages. |
| `instances` | Vec<String> | Instances to be created. |
| `volumes` | Vec<String> | Volumes to be created. |
| `pod` | String | Optional. Pod name. Pod is an independent part of infrastructure. Instance can be connected to the assets (networks, volumes, nfsshares) allocated in the same pod only. |
| `update_time` | String | Output only. Last update timestamp. |
| `name` | String | Output only. The system-generated name of the provisioning config. This follows the UUID format. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create provisioning_config
provisioning_config = provider.baremetalsolution_api.Provisioning_config {
    parent = "value"  # Required. The parent project and location containing the ProvisioningConfig.
}

# Access provisioning_config outputs
provisioning_config_id = provisioning_config.id
provisioning_config_location = provisioning_config.location
provisioning_config_cloud_console_uri = provisioning_config.cloud_console_uri
provisioning_config_ticket_id = provisioning_config.ticket_id
provisioning_config_handover_service_account = provisioning_config.handover_service_account
provisioning_config_status_message = provisioning_config.status_message
provisioning_config_custom_id = provisioning_config.custom_id
provisioning_config_state = provisioning_config.state
provisioning_config_vpc_sc_enabled = provisioning_config.vpc_sc_enabled
provisioning_config_networks = provisioning_config.networks
provisioning_config_email = provisioning_config.email
provisioning_config_instances = provisioning_config.instances
provisioning_config_volumes = provisioning_config.volumes
provisioning_config_pod = provisioning_config.pod
provisioning_config_update_time = provisioning_config.update_time
provisioning_config_name = provisioning_config.name
```

---


### Location

Gets information about a location.

**Operations**: ✅ Read

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `location_id` | String | The canonical id for this location. For example: `"us-east1"`. |
| `name` | String | Resource name for the location, which may vary between implementations. For example: `"projects/example-project/locations/us-east1"` |
| `labels` | HashMap<String, String> | Cross-service attributes for the location. For example {"cloud.googleapis.com/region": "us-east1"} |
| `metadata` | HashMap<String, String> | Service-specific metadata. For example the available capacity at the given location. |
| `display_name` | String | The friendly name for this location, typically a nearby city name. For example, "Tokyo". |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Access location outputs
location_id = location.id
location_location_id = location.location_id
location_name = location.name
location_labels = location.labels
location_metadata = location.metadata
location_display_name = location.display_name
```

---


### Snapshot

Takes a snapshot of a boot volume. Returns INVALID_ARGUMENT if called for a non-boot volume.

**Operations**: ✅ Create ✅ Read ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `id` | String |  | Output only. An identifier for the snapshot, generated by the backend. |
| `storage_volume` | String |  | Output only. The name of the volume which this snapshot belongs to. |
| `description` | String |  | The description of the snapshot. |
| `type` | String |  | Output only. The type of the snapshot which indicates whether it was scheduled or manual/ad-hoc. |
| `create_time` | String |  | Output only. The creation time of the snapshot. |
| `name` | String |  | The name of the snapshot. |
| `parent` | String | ✅ | Required. The volume to snapshot. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `id` | String | Output only. An identifier for the snapshot, generated by the backend. |
| `storage_volume` | String | Output only. The name of the volume which this snapshot belongs to. |
| `description` | String | The description of the snapshot. |
| `type` | String | Output only. The type of the snapshot which indicates whether it was scheduled or manual/ad-hoc. |
| `create_time` | String | Output only. The creation time of the snapshot. |
| `name` | String | The name of the snapshot. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create snapshot
snapshot = provider.baremetalsolution_api.Snapshot {
    parent = "value"  # Required. The volume to snapshot.
}

# Access snapshot outputs
snapshot_id = snapshot.id
snapshot_id = snapshot.id
snapshot_storage_volume = snapshot.storage_volume
snapshot_description = snapshot.description
snapshot_type = snapshot.type
snapshot_create_time = snapshot.create_time
snapshot_name = snapshot.name
```

---


### Operation

Get details about an operation.

**Operations**: ✅ Read

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `done` | bool | If the value is `false`, it means the operation is still in progress. If `true`, the operation is completed, and either `error` or `response` is available. |
| `response` | HashMap<String, String> | The normal, successful response of the operation. If the original method returns no data on success, such as `Delete`, the response is `google.protobuf.Empty`. If the original method is standard `Get`/`Create`/`Update`, the response should be the resource. For other methods, the response should have the type `XxxResponse`, where `Xxx` is the original method name. For example, if the original method name is `TakeSnapshot()`, the inferred response type is `TakeSnapshotResponse`. |
| `metadata` | HashMap<String, String> | Service-specific metadata associated with the operation. It typically contains progress information and common metadata such as create time. Some services might not provide such metadata. Any method that returns a long-running operation should document the metadata type, if any. |
| `error` | String | The error result of the operation in case of failure or cancellation. |
| `name` | String | The server-assigned name, which is only unique within the same service that originally returns it. If you use the default HTTP mapping, the `name` should be a resource name ending with `operations/{unique_id}`. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Access operation outputs
operation_id = operation.id
operation_done = operation.done
operation_response = operation.response
operation_metadata = operation.metadata
operation_error = operation.error
operation_name = operation.name
```

---


### Instance

Detach LUN from Instance.

**Operations**: ✅ Create ✅ Read ✅ Update

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `lun` | String |  | Required. Name of the Lun to detach. |
| `skip_reboot` | bool |  | If true, performs lun unmapping without instance reboot. |
| `instance` | String | ✅ | Required. Name of the instance. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `luns` | Vec<String> | Immutable. List of LUNs associated with this server. |
| `machine_type` | String | Immutable. The server type. [Available server types](https://cloud.google.com/bare-metal/docs/bms-planning#server_configurations) |
| `update_time` | String | Output only. Update a time stamp. |
| `interactive_serial_console_enabled` | bool | Output only. True if the interactive serial console feature is enabled for the instance, false otherwise. The default value is false. |
| `volumes` | Vec<String> | Input only. List of Volumes to attach to this Instance on creation. This field won't be populated in Get/List responses. |
| `create_time` | String | Output only. Create a time stamp. |
| `hyperthreading_enabled` | bool | True if you enable hyperthreading for the server, otherwise false. The default value is false. |
| `network_template` | String | Instance network template name. For eg, bondaa-bondaa, bondab-nic, etc. Generally, the template name follows the syntax of "bond" or "nic". |
| `networks` | Vec<String> | Output only. List of networks associated with this server. |
| `login_info` | String | Output only. Text field about info for logging in. |
| `kms_key_version` | String | Optional. Name of the KMS crypto key version used to encrypt the initial passwords. The key has to have ASYMMETRIC_DECRYPT purpose. Format is `projects/{project}/locations/{location}/keyRings/{keyring}/cryptoKeys/{key}/cryptoKeyVersions/{version}`. |
| `name` | String | Immutable. The resource name of this `Instance`. Resource names are schemeless URIs that follow the conventions in https://cloud.google.com/apis/design/resource_names. Format: `projects/{project}/locations/{location}/instances/{instance}` |
| `firmware_version` | String | Output only. The firmware version for the instance. |
| `pod` | String | Immutable. Pod name. Pod is an independent part of infrastructure. Instance can only be connected to the assets (networks, volumes) allocated in the same pod. |
| `logical_interfaces` | Vec<String> | List of logical interfaces for the instance. The number of logical interfaces will be the same as number of hardware bond/nic on the chosen network template. For the non-multivlan configurations (for eg, existing servers) that use existing default network template (bondaa-bondaa), both the Instance.networks field and the Instance.logical_interfaces fields will be filled to ensure backward compatibility. For the others, only Instance.logical_interfaces will be filled. |
| `labels` | HashMap<String, String> | Labels as key value pairs. |
| `os_image` | String | The OS image currently installed on the server. |
| `id` | String | Output only. An identifier for the `Instance`, generated by the backend. |
| `ssh_keys` | Vec<String> | Optional. List of SSH Keys used during instance provisioning. |
| `state` | String | Output only. The state of the server. |
| `workload_profile` | String | The workload profile for the instance. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create instance
instance = provider.baremetalsolution_api.Instance {
    instance = "value"  # Required. Name of the instance.
}

# Access instance outputs
instance_id = instance.id
instance_luns = instance.luns
instance_machine_type = instance.machine_type
instance_update_time = instance.update_time
instance_interactive_serial_console_enabled = instance.interactive_serial_console_enabled
instance_volumes = instance.volumes
instance_create_time = instance.create_time
instance_hyperthreading_enabled = instance.hyperthreading_enabled
instance_network_template = instance.network_template
instance_networks = instance.networks
instance_login_info = instance.login_info
instance_kms_key_version = instance.kms_key_version
instance_name = instance.name
instance_firmware_version = instance.firmware_version
instance_pod = instance.pod
instance_logical_interfaces = instance.logical_interfaces
instance_labels = instance.labels
instance_os_image = instance.os_image
instance_id = instance.id
instance_ssh_keys = instance.ssh_keys
instance_state = instance.state
instance_workload_profile = instance.workload_profile
```

---


### Ssh_key

Register a public SSH key in the specified project for use with the interactive serial console feature.

**Operations**: ✅ Create ✅ Read ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `public_key` | String |  | The public SSH key. This must be in OpenSSH .authorized_keys format. |
| `name` | String |  | Output only. The name of this SSH key. Currently, the only valid value for the location is "global". |
| `parent` | String | ✅ | Required. The parent containing the SSH keys. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `ssh_keys` | Vec<String> | The SSH keys registered in the project. |
| `next_page_token` | String | Token to retrieve the next page of results, or empty if there are no more results in the list. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create ssh_key
ssh_key = provider.baremetalsolution_api.Ssh_key {
    parent = "value"  # Required. The parent containing the SSH keys.
}

# Access ssh_key outputs
ssh_key_id = ssh_key.id
ssh_key_ssh_keys = ssh_key.ssh_keys
ssh_key_next_page_token = ssh_key.next_page_token
```

---


### Volume

Emergency Volume resize.

**Operations**: ✅ Create ✅ Read ✅ Update

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `size_gib` | String |  | New Volume size, in GiB. |
| `volume` | String | ✅ | Required. Volume to resize. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `storage_type` | String | The storage type for this volume. |
| `snapshot_reservation_detail` | String | Details about snapshot space reservation and usage on the storage volume. |
| `auto_grown_size_gib` | String | The size, in GiB, that this storage volume has expanded as a result of an auto grow policy. In the absence of auto-grow, the value is 0. |
| `current_size_gib` | String | The current size of this storage volume, in GiB, including space reserved for snapshots. This size might be different than the requested size if the storage volume has been configured with auto grow or auto shrink. |
| `max_size_gib` | String | Maximum size volume can be expanded to in case of evergency, in GiB. |
| `originally_requested_size_gib` | String | Originally requested size, in GiB. |
| `expire_time` | String | Output only. Time after which volume will be fully deleted. It is filled only for volumes in COOLOFF state. |
| `protocol` | String | Output only. Storage protocol for the Volume. |
| `requested_size_gib` | String | The requested size of this storage volume, in GiB. |
| `state` | String | The state of this storage volume. |
| `instances` | Vec<String> | Output only. Instances this Volume is attached to. This field is set only in Get requests. |
| `notes` | String | Input only. User-specified notes for new Volume. Used to provision Volumes that require manual intervention. |
| `workload_profile` | String | The workload profile for the volume. |
| `pod` | String | Immutable. Pod name. Pod is an independent part of infrastructure. Volume can only be connected to the instances allocated in the same pod. |
| `name` | String | Output only. The resource name of this `Volume`. Resource names are schemeless URIs that follow the conventions in https://cloud.google.com/apis/design/resource_names. Format: `projects/{project}/locations/{location}/volumes/{volume}` |
| `performance_tier` | String | Immutable. Performance tier of the Volume. Default is SHARED. |
| `attached` | bool | Output only. Is the Volume attached at at least one instance. This field is a lightweight counterpart of `instances` field. It is filled in List responses as well. |
| `boot_volume` | bool | Output only. Whether this volume is a boot volume. A boot volume is one which contains a boot LUN. |
| `snapshot_enabled` | bool | Whether snapshots are enabled. |
| `id` | String | An identifier for the `Volume`, generated by the backend. |
| `labels` | HashMap<String, String> | Labels as key value pairs. |
| `remaining_space_gib` | String | The space remaining in the storage volume for new LUNs, in GiB, excluding space reserved for snapshots. |
| `snapshot_auto_delete_behavior` | String | The behavior to use when snapshot reserved space is full. |
| `emergency_size_gib` | String | Additional emergency size that was requested for this Volume, in GiB. current_size_gib includes this value. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create volume
volume = provider.baremetalsolution_api.Volume {
    volume = "value"  # Required. Volume to resize.
}

# Access volume outputs
volume_id = volume.id
volume_storage_type = volume.storage_type
volume_snapshot_reservation_detail = volume.snapshot_reservation_detail
volume_auto_grown_size_gib = volume.auto_grown_size_gib
volume_current_size_gib = volume.current_size_gib
volume_max_size_gib = volume.max_size_gib
volume_originally_requested_size_gib = volume.originally_requested_size_gib
volume_expire_time = volume.expire_time
volume_protocol = volume.protocol
volume_requested_size_gib = volume.requested_size_gib
volume_state = volume.state
volume_instances = volume.instances
volume_notes = volume.notes
volume_workload_profile = volume.workload_profile
volume_pod = volume.pod
volume_name = volume.name
volume_performance_tier = volume.performance_tier
volume_attached = volume.attached
volume_boot_volume = volume.boot_volume
volume_snapshot_enabled = volume.snapshot_enabled
volume_id = volume.id
volume_labels = volume.labels
volume_remaining_space_gib = volume.remaining_space_gib
volume_snapshot_auto_delete_behavior = volume.snapshot_auto_delete_behavior
volume_emergency_size_gib = volume.emergency_size_gib
```

---


### Network

RenameNetwork sets a new name for a network. Use with caution, previous names become immediately invalidated.

**Operations**: ✅ Create ✅ Read ✅ Update

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `new_network_id` | String |  | Required. The new `id` of the network. |
| `name` | String | ✅ | Required. The `name` field is used to identify the network. Format: projects/{project}/locations/{location}/networks/{network} |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `pod` | String | Immutable. Pod name. Pod is an independent part of infrastructure. Network can only be connected to the assets (instances, nfsshares) allocated in the same pod. |
| `cidr` | String | The cidr of the Network. |
| `vrf` | String | The Vrf for the Network. Use this only if a new Vrf needs to be created. |
| `type` | String | The type of this network. |
| `gateway_ip` | String | Output only. Gateway ip address. |
| `vrf_attachment` | String | Optional. The name of a pre-existing Vrf that the network should be attached to. Format is `vrfs/{vrf}`. |
| `jumbo_frames_enabled` | bool | Whether network uses standard frames or jumbo ones. |
| `id` | String | An identifier for the `Network`, generated by the backend. |
| `ip_address` | String | IP address configured. |
| `name` | String | Output only. The resource name of this `Network`. Resource names are schemeless URIs that follow the conventions in https://cloud.google.com/apis/design/resource_names. Format: `projects/{project}/locations/{location}/networks/{network}` |
| `mac_address` | Vec<String> | List of physical interfaces. |
| `vlan_id` | String | The vlan id of the Network. |
| `services_cidr` | String | IP range for reserved for services (e.g. NFS). |
| `state` | String | The Network state. |
| `labels` | HashMap<String, String> | Labels as key value pairs. |
| `mount_points` | Vec<String> | Input only. List of mount points to attach the network to. |
| `reservations` | Vec<String> | List of IP address reservations in this network. When updating this field, an error will be generated if a reservation conflicts with an IP address already allocated to a physical server. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create network
network = provider.baremetalsolution_api.Network {
    name = "value"  # Required. The `name` field is used to identify the network. Format: projects/{project}/locations/{location}/networks/{network}
}

# Access network outputs
network_id = network.id
network_pod = network.pod
network_cidr = network.cidr
network_vrf = network.vrf
network_type = network.type
network_gateway_ip = network.gateway_ip
network_vrf_attachment = network.vrf_attachment
network_jumbo_frames_enabled = network.jumbo_frames_enabled
network_id = network.id
network_ip_address = network.ip_address
network_name = network.name
network_mac_address = network.mac_address
network_vlan_id = network.vlan_id
network_services_cidr = network.services_cidr
network_state = network.state
network_labels = network.labels
network_mount_points = network.mount_points
network_reservations = network.reservations
```

---


### Location

Submit a provisiong configuration for a given project.

**Operations**: ✅ Create

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `email` | String |  | Optional. Email provided to send a confirmation with provisioning config to. |
| `provisioning_config` | String |  | Required. The ProvisioningConfig to submit. |
| `location` | String | ✅ | Required. The target location of the provisioning request. |
| `project` | String | ✅ | Required. The target project of the provisioning request. |



#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create location
location = provider.baremetalsolution_api.Location {
    location = "value"  # Required. The target location of the provisioning request.
    project = "value"  # Required. The target project of the provisioning request.
}

```

---


### Provisioning_quota

List the budget details to provision resources on a given project.

**Operations**: ✅ Read

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `next_page_token` | String | Token to retrieve the next page of results, or empty if there are no more results in the list. |
| `provisioning_quotas` | Vec<String> | The provisioning quotas registered in this project. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Access provisioning_quota outputs
provisioning_quota_id = provisioning_quota.id
provisioning_quota_next_page_token = provisioning_quota.next_page_token
provisioning_quota_provisioning_quotas = provisioning_quota.provisioning_quotas
```

---



## Common Operations

### Creating Multiple Resources

```kcl
import gcp

provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create multiple operation resources
operation_0 = provider.baremetalsolution_api.Operation {
    name = "value-0"
}
operation_1 = provider.baremetalsolution_api.Operation {
    name = "value-1"
}
operation_2 = provider.baremetalsolution_api.Operation {
    name = "value-2"
}
```

### Conditional Creation

```kcl
# Only create in production
if environment == "production":
    operation = provider.baremetalsolution_api.Operation {
        name = "production-value"
    }
```

---

## Related Documentation

- [GCP Baremetalsolution_api Documentation](https://cloud.google.com/baremetalsolution_api/docs)
- [Getting Started Guide](../getting-started.md)
- [Installation Guide](../installation.md)
- ⬅️ [Back to README](../../README.md)
