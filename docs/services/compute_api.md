# Compute_api Service



**Resources**: 371

---

## Overview

The compute_api service provides access to 371 resource types:

- [Instant_snapshot](#instant_snapshot) [CRD]
- [Region_ssl_policie](#region_ssl_policie) [CRUD]
- [Region_instance_template](#region_instance_template) [CRD]
- [Vpn_tunnel](#vpn_tunnel) [CRD]
- [Region_ssl_certificate](#region_ssl_certificate) [CRD]
- [Wire_group](#wire_group) [CRUD]
- [Target_grpc_proxie](#target_grpc_proxie) [CRUD]
- [Network_attachment](#network_attachment) [CRUD]
- [Region_disk_type](#region_disk_type) [R]
- [Interconnect_attachment](#interconnect_attachment) [CRUD]
- [Region_network_endpoint_group](#region_network_endpoint_group) [CRD]
- [Ssl_certificate](#ssl_certificate) [CRD]
- [Subnetwork](#subnetwork) [CRUD]
- [Interconnect](#interconnect) [CRUD]
- [Interconnect_attachment_group](#interconnect_attachment_group) [CRUD]
- [Target_vpn_gateway](#target_vpn_gateway) [CRD]
- [Forwarding_rule](#forwarding_rule) [CRUD]
- [Https_health_check](#https_health_check) [CRUD]
- [Region_target_tcp_proxie](#region_target_tcp_proxie) [CRD]
- [Vpn_gateway](#vpn_gateway) [CRD]
- [Region_url_map](#region_url_map) [CRUD]
- [Region_health_check_service](#region_health_check_service) [CRUD]
- [Interconnect_group](#interconnect_group) [CRUD]
- [External_vpn_gateway](#external_vpn_gateway) [CRD]
- [Instance_group_manager_resize_request](#instance_group_manager_resize_request) [CRD]
- [Instance_group_manager](#instance_group_manager) [CRUD]
- [License_code](#license_code) [CR]
- [Security_policie](#security_policie) [CRUD]
- [Reservation](#reservation) [CRUD]
- [Service_attachment](#service_attachment) [CRUD]
- [Node_type](#node_type) [R]
- [Interconnect_location](#interconnect_location) [R]
- [Autoscaler](#autoscaler) [CRUD]
- [Image_family_view](#image_family_view) [R]
- [Snapshot_setting](#snapshot_setting) [RU]
- [Backend_bucket](#backend_bucket) [CRUD]
- [Storage_pool](#storage_pool) [CRUD]
- [Packet_mirroring](#packet_mirroring) [CRUD]
- [Node_group](#node_group) [CRUD]
- [Url_map](#url_map) [CRUD]
- [Disk](#disk) [CRUD]
- [Target_pool](#target_pool) [CRD]
- [Region_health_check](#region_health_check) [CRUD]
- [Global_operation](#global_operation) [CRD]
- [Public_advertised_prefixe](#public_advertised_prefixe) [CRUD]
- [Region_instance_group_manager](#region_instance_group_manager) [CRUD]
- [Http_health_check](#http_health_check) [CRUD]
- [Network_edge_security_service](#network_edge_security_service) [CRUD]
- [Target_instance](#target_instance) [CRD]
- [Region_security_policie](#region_security_policie) [CRUD]
- [Global_network_endpoint_group](#global_network_endpoint_group) [CRD]
- [Resource_policie](#resource_policie) [CRUD]
- [Region_instant_snapshot](#region_instant_snapshot) [CRD]
- [License](#license) [CRUD]
- [Ssl_policie](#ssl_policie) [CRUD]
- [Global_forwarding_rule](#global_forwarding_rule) [CRUD]
- [Zone](#zone) [R]
- [Zone_operation](#zone_operation) [CRD]
- [Region_operation](#region_operation) [CRD]
- [Reservation_sub_block](#reservation_sub_block) [CR]
- [Reservation_block](#reservation_block) [CR]
- [Region_instance](#region_instance) [C]
- [Cross_site_network](#cross_site_network) [CRUD]
- [Region_backend_service](#region_backend_service) [CRUD]
- [Disk_type](#disk_type) [R]
- [Machine_image](#machine_image) [CRD]
- [Target_tcp_proxie](#target_tcp_proxie) [CRD]
- [Target_https_proxie](#target_https_proxie) [CRUD]
- [Addresse](#addresse) [CRD]
- [Organization_security_policie](#organization_security_policie) [CRUD]
- [Network_profile](#network_profile) [R]
- [Global_addresse](#global_addresse) [CRD]
- [Preview_feature](#preview_feature) [RU]
- [Accelerator_type](#accelerator_type) [R]
- [Region_target_http_proxie](#region_target_http_proxie) [CRD]
- [Firewall_policie](#firewall_policie) [CRUD]
- [Instance_group](#instance_group) [CRD]
- [Firewall](#firewall) [CRUD]
- [Global_organization_operation](#global_organization_operation) [RD]
- [Region_target_https_proxie](#region_target_https_proxie) [CRUD]
- [Region_commitment](#region_commitment) [CRU]
- [Route](#route) [CRD]
- [Node_template](#node_template) [CRD]
- [Interconnect_remote_location](#interconnect_remote_location) [R]
- [Region_disk](#region_disk) [CRUD]
- [Project](#project) [CR]
- [Region_instance_group](#region_instance_group) [CR]
- [Router](#router) [CRUD]
- [Network_firewall_policie](#network_firewall_policie) [CRUD]
- [Storage_pool_type](#storage_pool_type) [R]
- [Image](#image) [CRUD]
- [Region_network_firewall_policie](#region_network_firewall_policie) [CRUD]
- [Backend_service](#backend_service) [CRUD]
- [Health_check](#health_check) [CRUD]
- [Region_zone](#region_zone) [R]
- [Global_public_delegated_prefixe](#global_public_delegated_prefixe) [CRUD]
- [Instance_setting](#instance_setting) [RU]
- [Future_reservation](#future_reservation) [CRUD]
- [Target_http_proxie](#target_http_proxie) [CRUD]
- [Instance](#instance) [CRUD]
- [Snapshot](#snapshot) [CRD]
- [Instance_template](#instance_template) [CRD]
- [Region_notification_endpoint](#region_notification_endpoint) [CRD]
- [Network_endpoint_group](#network_endpoint_group) [CRD]
- [Region_autoscaler](#region_autoscaler) [CRUD]
- [Target_ssl_proxie](#target_ssl_proxie) [CRD]
- [Region](#region) [R]
- [Network](#network) [CRUD]
- [Machine_type](#machine_type) [R]
- [Public_delegated_prefixe](#public_delegated_prefixe) [CRUD]
- [Network_attachment](#network_attachment) [CRUD]
- [Region_autoscaler](#region_autoscaler) [CRUD]
- [Vpn_gateway](#vpn_gateway) [CRD]
- [Region_notification_endpoint](#region_notification_endpoint) [CRD]
- [Preview_feature](#preview_feature) [RU]
- [License_code](#license_code) [CR]
- [Region_instance_template](#region_instance_template) [CRD]
- [Machine_type](#machine_type) [R]
- [Reservation_sub_block](#reservation_sub_block) [CR]
- [Target_pool](#target_pool) [CRD]
- [Packet_mirroring](#packet_mirroring) [CRUD]
- [Interconnect_location](#interconnect_location) [R]
- [Security_policie](#security_policie) [CRUD]
- [Zone](#zone) [R]
- [Health_check](#health_check) [CRUD]
- [Global_forwarding_rule](#global_forwarding_rule) [CRUD]
- [Interconnect_group](#interconnect_group) [CRUD]
- [Region_backend_bucket](#region_backend_bucket) [CRUD]
- [Region_health_aggregation_policie](#region_health_aggregation_policie) [CRUD]
- [External_vpn_gateway](#external_vpn_gateway) [CRD]
- [Region_backend_service](#region_backend_service) [CRUD]
- [Global_vm_extension_policie](#global_vm_extension_policie) [CRUD]
- [Node_group](#node_group) [CRUD]
- [Region_ssl_policie](#region_ssl_policie) [CRUD]
- [Snapshot_setting](#snapshot_setting) [RU]
- [Instance_group_manager_resize_request](#instance_group_manager_resize_request) [CRD]
- [Disk_type](#disk_type) [R]
- [Subnetwork](#subnetwork) [CRUD]
- [Region_zone](#region_zone) [R]
- [Instance](#instance) [CRUD]
- [Region_target_http_proxie](#region_target_http_proxie) [CRD]
- [Disk_setting](#disk_setting) [RU]
- [Target_ssl_proxie](#target_ssl_proxie) [CRD]
- [Reservation](#reservation) [CRUD]
- [Addresse](#addresse) [CRD]
- [Region](#region) [R]
- [Network](#network) [CRUD]
- [Instance_group_manager](#instance_group_manager) [CRUD]
- [Snapshot](#snapshot) [CRD]
- [Region_security_policie](#region_security_policie) [CRUD]
- [Http_health_check](#http_health_check) [CRUD]
- [License](#license) [CRUD]
- [Region_instance_group_manager_resize_request](#region_instance_group_manager_resize_request) [CRD]
- [Zone_folder_operation](#zone_folder_operation) [R]
- [Global_operation](#global_operation) [CRD]
- [Route](#route) [CRD]
- [Global_folder_operation](#global_folder_operation) [R]
- [Machine_image](#machine_image) [CRD]
- [Zone_vm_extension_policie](#zone_vm_extension_policie) [CRUD]
- [Instant_snapshot_group](#instant_snapshot_group) [CRD]
- [Target_grpc_proxie](#target_grpc_proxie) [CRUD]
- [Zone_queued_resource](#zone_queued_resource) [CRD]
- [Region_commitment](#region_commitment) [CRU]
- [Reservation_block](#reservation_block) [CR]
- [Region_multi_mig_member](#region_multi_mig_member) [R]
- [Vpn_tunnel](#vpn_tunnel) [CRD]
- [Network_edge_security_service](#network_edge_security_service) [CRUD]
- [Region_instance_group_manager](#region_instance_group_manager) [CRUD]
- [Snapshot_group](#snapshot_group) [CRD]
- [Node_template](#node_template) [CRD]
- [Zone_organization_operation](#zone_organization_operation) [R]
- [Interconnect_attachment_group](#interconnect_attachment_group) [CRUD]
- [Advice](#advice) [C]
- [Region_composite_health_check](#region_composite_health_check) [CRUD]
- [Target_https_proxie](#target_https_proxie) [CRUD]
- [Region_instant_snapshot](#region_instant_snapshot) [CRD]
- [Region_snapshot_setting](#region_snapshot_setting) [RU]
- [Instance_template](#instance_template) [CRD]
- [Instant_snapshot](#instant_snapshot) [CRD]
- [Storage_pool_type](#storage_pool_type) [R]
- [Global_organization_operation](#global_organization_operation) [RD]
- [Zone_operation](#zone_operation) [CRD]
- [Target_instance](#target_instance) [CRD]
- [Network_profile](#network_profile) [R]
- [Service_attachment](#service_attachment) [CRUD]
- [Region_ssl_certificate](#region_ssl_certificate) [CRD]
- [Global_addresse](#global_addresse) [CRD]
- [Global_network_endpoint_group](#global_network_endpoint_group) [CRD]
- [Interconnect_attachment](#interconnect_attachment) [CRUD]
- [Project](#project) [CR]
- [Region_url_map](#region_url_map) [CRUD]
- [Region_target_tcp_proxie](#region_target_tcp_proxie) [CRD]
- [Https_health_check](#https_health_check) [CRUD]
- [Cross_site_network](#cross_site_network) [CRUD]
- [Resource_policie](#resource_policie) [CRUD]
- [Image](#image) [CRUD]
- [Backend_bucket](#backend_bucket) [CRUD]
- [Autoscaler](#autoscaler) [CRUD]
- [Region_disk_setting](#region_disk_setting) [RU]
- [Rollout_plan](#rollout_plan) [CRD]
- [Region_instance_group](#region_instance_group) [CR]
- [Rollout](#rollout) [RUD]
- [Router](#router) [CRUD]
- [Recoverable_snapshot](#recoverable_snapshot) [CRD]
- [Firewall_policie](#firewall_policie) [CRUD]
- [Region_disk](#region_disk) [CRUD]
- [Target_tcp_proxie](#target_tcp_proxie) [CRD]
- [Region_multi_mig](#region_multi_mig) [CRD]
- [Instance_setting](#instance_setting) [RU]
- [Url_map](#url_map) [CRUD]
- [Node_type](#node_type) [R]
- [Interconnect_remote_location](#interconnect_remote_location) [R]
- [Ssl_policie](#ssl_policie) [CRUD]
- [Region_health_check_service](#region_health_check_service) [CRUD]
- [Region_instant_snapshot_group](#region_instant_snapshot_group) [CRD]
- [Region_disk_type](#region_disk_type) [R]
- [Region_health_check](#region_health_check) [CRUD]
- [Wire_group](#wire_group) [CRUD]
- [Target_http_proxie](#target_http_proxie) [CRUD]
- [Forwarding_rule](#forwarding_rule) [CRUD]
- [Public_advertised_prefixe](#public_advertised_prefixe) [CRUD]
- [Ha_controller](#ha_controller) [CRUD]
- [Region_target_https_proxie](#region_target_https_proxie) [CRUD]
- [Backend_service](#backend_service) [CRUD]
- [Disk](#disk) [CRUD]
- [Reliability_risk](#reliability_risk) [R]
- [Instance_group](#instance_group) [CRD]
- [Target_vpn_gateway](#target_vpn_gateway) [CRD]
- [Future_reservation](#future_reservation) [CRUD]
- [Region_network_policie](#region_network_policie) [CRUD]
- [Network_endpoint_group](#network_endpoint_group) [CRD]
- [Region_network_firewall_policie](#region_network_firewall_policie) [CRUD]
- [Interconnect](#interconnect) [CRUD]
- [Region_instance](#region_instance) [C]
- [Global_public_delegated_prefixe](#global_public_delegated_prefixe) [CRUD]
- [Public_delegated_prefixe](#public_delegated_prefixe) [CRUD]
- [Region_snapshot](#region_snapshot) [CRD]
- [Region_network_endpoint_group](#region_network_endpoint_group) [CRD]
- [Organization_security_policie](#organization_security_policie) [CRUD]
- [Accelerator_type](#accelerator_type) [R]
- [Region_health_source](#region_health_source) [CRUD]
- [Region_operation](#region_operation) [CRD]
- [Storage_pool](#storage_pool) [CRUD]
- [Ssl_certificate](#ssl_certificate) [CRD]
- [Firewall](#firewall) [CRUD]
- [Image_family_view](#image_family_view) [R]
- [Network_firewall_policie](#network_firewall_policie) [CRUD]
- [Interconnect_location](#interconnect_location) [R]
- [Cross_site_network](#cross_site_network) [CRUD]
- [Region_ssl_certificate](#region_ssl_certificate) [CRD]
- [Zone_operation](#zone_operation) [CRD]
- [Region_composite_health_check](#region_composite_health_check) [CRUD]
- [Region_disk_setting](#region_disk_setting) [RU]
- [Global_forwarding_rule](#global_forwarding_rule) [CRUD]
- [Public_delegated_prefixe](#public_delegated_prefixe) [CRUD]
- [External_vpn_gateway](#external_vpn_gateway) [CRD]
- [Region_security_policie](#region_security_policie) [CRUD]
- [Interconnect_attachment](#interconnect_attachment) [CRUD]
- [Global_network_endpoint_group](#global_network_endpoint_group) [CRD]
- [Global_public_delegated_prefixe](#global_public_delegated_prefixe) [CRUD]
- [Target_vpn_gateway](#target_vpn_gateway) [CRD]
- [Router](#router) [CRUD]
- [License_code](#license_code) [CR]
- [Security_policie](#security_policie) [CRUD]
- [Region_backend_bucket](#region_backend_bucket) [CRUD]
- [Http_health_check](#http_health_check) [CRUD]
- [Global_operation](#global_operation) [CRD]
- [Region_target_http_proxie](#region_target_http_proxie) [CRD]
- [Region_instance](#region_instance) [C]
- [Region_instance_group_manager](#region_instance_group_manager) [CRUD]
- [Instance_group_manager](#instance_group_manager) [CRUD]
- [Health_check](#health_check) [CRUD]
- [Snapshot_setting](#snapshot_setting) [RU]
- [Snapshot](#snapshot) [CRD]
- [Region_url_map](#region_url_map) [CRUD]
- [Network](#network) [CRUD]
- [Subnetwork](#subnetwork) [CRUD]
- [Machine_type](#machine_type) [R]
- [Interconnect_remote_location](#interconnect_remote_location) [R]
- [Instant_snapshot](#instant_snapshot) [CRD]
- [Ssl_policie](#ssl_policie) [CRUD]
- [Instance_setting](#instance_setting) [RU]
- [Ssl_certificate](#ssl_certificate) [CRD]
- [Disk_type](#disk_type) [R]
- [Region_network_firewall_policie](#region_network_firewall_policie) [CRUD]
- [Region_target_https_proxie](#region_target_https_proxie) [CRUD]
- [Advice](#advice) [C]
- [Region_notification_endpoint](#region_notification_endpoint) [CRD]
- [Future_reservation](#future_reservation) [CRUD]
- [Node_template](#node_template) [CRD]
- [Route](#route) [CRD]
- [Target_instance](#target_instance) [CRD]
- [Region_snapshot_setting](#region_snapshot_setting) [RU]
- [Region_snapshot](#region_snapshot) [CRD]
- [Region](#region) [R]
- [Global_addresse](#global_addresse) [CRD]
- [Disk_setting](#disk_setting) [RU]
- [Resource_policie](#resource_policie) [CRUD]
- [Storage_pool](#storage_pool) [CRUD]
- [Target_pool](#target_pool) [CRD]
- [Forwarding_rule](#forwarding_rule) [CRUD]
- [Node_type](#node_type) [R]
- [Packet_mirroring](#packet_mirroring) [CRUD]
- [Region_health_check](#region_health_check) [CRUD]
- [Firewall_policie](#firewall_policie) [CRUD]
- [Region_instance_template](#region_instance_template) [CRD]
- [Backend_service](#backend_service) [CRUD]
- [License](#license) [CRUD]
- [Addresse](#addresse) [CRD]
- [Interconnect_group](#interconnect_group) [CRUD]
- [Public_advertised_prefixe](#public_advertised_prefixe) [CRUD]
- [Autoscaler](#autoscaler) [CRUD]
- [Instance_group](#instance_group) [CRD]
- [Interconnect_attachment_group](#interconnect_attachment_group) [CRUD]
- [Accelerator_type](#accelerator_type) [R]
- [Vpn_tunnel](#vpn_tunnel) [CRD]
- [Network_edge_security_service](#network_edge_security_service) [CRUD]
- [Region_zone](#region_zone) [R]
- [Reservation_block](#reservation_block) [CR]
- [Instance_template](#instance_template) [CRD]
- [Region_instance_group](#region_instance_group) [CR]
- [Target_http_proxie](#target_http_proxie) [CRUD]
- [Instance](#instance) [CRUD]
- [Service_attachment](#service_attachment) [CRUD]
- [Preview_feature](#preview_feature) [RU]
- [Region_disk](#region_disk) [CRUD]
- [Target_grpc_proxie](#target_grpc_proxie) [CRUD]
- [Backend_bucket](#backend_bucket) [CRUD]
- [Interconnect](#interconnect) [CRUD]
- [Url_map](#url_map) [CRUD]
- [Project](#project) [CR]
- [Region_target_tcp_proxie](#region_target_tcp_proxie) [CRD]
- [Region_network_endpoint_group](#region_network_endpoint_group) [CRD]
- [Region_network_policie](#region_network_policie) [CRUD]
- [Region_health_source](#region_health_source) [CRUD]
- [Target_tcp_proxie](#target_tcp_proxie) [CRD]
- [Global_organization_operation](#global_organization_operation) [RD]
- [Disk](#disk) [CRUD]
- [Region_instance_group_manager_resize_request](#region_instance_group_manager_resize_request) [CRD]
- [Region_health_check_service](#region_health_check_service) [CRUD]
- [Storage_pool_type](#storage_pool_type) [R]
- [Reservation](#reservation) [CRUD]
- [Region_backend_service](#region_backend_service) [CRUD]
- [Network_endpoint_group](#network_endpoint_group) [CRD]
- [Wire_group](#wire_group) [CRUD]
- [Target_ssl_proxie](#target_ssl_proxie) [CRD]
- [Zone_vm_extension_policie](#zone_vm_extension_policie) [CRUD]
- [Region_operation](#region_operation) [CRD]
- [Node_group](#node_group) [CRUD]
- [Region_multi_mig](#region_multi_mig) [CRD]
- [Machine_image](#machine_image) [CRD]
- [Image](#image) [CRUD]
- [Https_health_check](#https_health_check) [CRUD]
- [Region_health_aggregation_policie](#region_health_aggregation_policie) [CRUD]
- [Region_disk_type](#region_disk_type) [R]
- [Firewall](#firewall) [CRUD]
- [Reservation_sub_block](#reservation_sub_block) [CR]
- [Network_attachment](#network_attachment) [CRUD]
- [Region_instant_snapshot](#region_instant_snapshot) [CRD]
- [Region_autoscaler](#region_autoscaler) [CRUD]
- [Region_ssl_policie](#region_ssl_policie) [CRUD]
- [Global_vm_extension_policie](#global_vm_extension_policie) [R]
- [Instance_group_manager_resize_request](#instance_group_manager_resize_request) [CRD]
- [Target_https_proxie](#target_https_proxie) [CRUD]
- [Vpn_gateway](#vpn_gateway) [CRD]
- [Network_profile](#network_profile) [R]
- [Region_commitment](#region_commitment) [CRU]
- [Zone](#zone) [R]
- [Network_firewall_policie](#network_firewall_policie) [CRUD]
- [Organization_security_policie](#organization_security_policie) [CRUD]
- [Image_family_view](#image_family_view) [R]

---

## Resources


### Instant_snapshot

Creates an instant snapshot in the specified zone.

**Operations**: ✅ Create ✅ Read ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `label_fingerprint` | String |  | A fingerprint for the labels being applied to this InstantSnapshot, which
is essentially a hash of the labels set used for optimistic locking. The
fingerprint is initially generated by Compute Engine and changes after
every request to modify or update labels. You must always provide an
up-to-date fingerprint hash in order to update or change labels,
otherwise the request will fail with error412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve a InstantSnapshot. |
| `region` | String |  | [Output Only] URL of the region where the instant snapshot resides.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `self_link_with_id` | String |  | [Output Only] Server-defined URL for this resource's resource id. |
| `architecture` | String |  | [Output Only] The architecture of the instant snapshot. Valid values are
ARM64 or X86_64. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `source_disk` | String |  | URL of the source disk used to create this instant snapshot.
Note that the source disk must be in the same zone/region as the
instant snapshot to be created. This can be a full or valid partial URL.
For example, the following are valid values:
   
   
     - 
       https://www.googleapis.com/compute/v1/projects/project/zones/zone/disks/disk
     
     - 
       https://www.googleapis.com/compute/v1/projects/project/regions/region/disks/disk
     
     - 
       projects/project/zones/zone/disks/disk
     
     - 
       projects/project/regions/region/disks/disk
     
     - 
       zones/zone/disks/disk
     
     - 
       regions/region/disks/disk |
| `name` | String |  | Name of the resource; provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must be
a dash, lowercase letter, or digit, except the last character, which cannot
be a dash. |
| `status` | String |  | [Output Only] The status of the instantSnapshot. This can beCREATING, DELETING, FAILED, orREADY. |
| `disk_size_gb` | String |  | [Output Only] Size of the source disk, specified in GB. |
| `satisfies_pzi` | bool |  | Output only. Reserved for future use. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `kind` | String |  | [Output Only] Type of the resource. Alwayscompute#instantSnapshot for InstantSnapshot resources. |
| `source_disk_id` | String |  | [Output Only] The ID value of the disk used to create this InstantSnapshot.
This value may be used to determine whether the InstantSnapshot
was taken from the current or a previous instance of a given disk name. |
| `zone` | String |  | [Output Only] URL of the zone where the instant snapshot resides.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `resource_status` | String |  | [Output Only] Status information for the instant snapshot resource. |
| `labels` | HashMap<String, String> |  | Labels to apply to this InstantSnapshot. These can be later modified by
the setLabels method.
Label values may be empty. |
| `satisfies_pzs` | bool |  | [Output Only] Reserved for future use. |
| `project` | String | ✅ | Project ID for this request. |
| `zone` | String | ✅ | Name of the zone for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `label_fingerprint` | String | A fingerprint for the labels being applied to this InstantSnapshot, which
is essentially a hash of the labels set used for optimistic locking. The
fingerprint is initially generated by Compute Engine and changes after
every request to modify or update labels. You must always provide an
up-to-date fingerprint hash in order to update or change labels,
otherwise the request will fail with error412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve a InstantSnapshot. |
| `region` | String | [Output Only] URL of the region where the instant snapshot resides.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `self_link_with_id` | String | [Output Only] Server-defined URL for this resource's resource id. |
| `architecture` | String | [Output Only] The architecture of the instant snapshot. Valid values are
ARM64 or X86_64. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `source_disk` | String | URL of the source disk used to create this instant snapshot.
Note that the source disk must be in the same zone/region as the
instant snapshot to be created. This can be a full or valid partial URL.
For example, the following are valid values:
   
   
     - 
       https://www.googleapis.com/compute/v1/projects/project/zones/zone/disks/disk
     
     - 
       https://www.googleapis.com/compute/v1/projects/project/regions/region/disks/disk
     
     - 
       projects/project/zones/zone/disks/disk
     
     - 
       projects/project/regions/region/disks/disk
     
     - 
       zones/zone/disks/disk
     
     - 
       regions/region/disks/disk |
| `name` | String | Name of the resource; provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must be
a dash, lowercase letter, or digit, except the last character, which cannot
be a dash. |
| `status` | String | [Output Only] The status of the instantSnapshot. This can beCREATING, DELETING, FAILED, orREADY. |
| `disk_size_gb` | String | [Output Only] Size of the source disk, specified in GB. |
| `satisfies_pzi` | bool | Output only. Reserved for future use. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `kind` | String | [Output Only] Type of the resource. Alwayscompute#instantSnapshot for InstantSnapshot resources. |
| `source_disk_id` | String | [Output Only] The ID value of the disk used to create this InstantSnapshot.
This value may be used to determine whether the InstantSnapshot
was taken from the current or a previous instance of a given disk name. |
| `zone` | String | [Output Only] URL of the zone where the instant snapshot resides.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `resource_status` | String | [Output Only] Status information for the instant snapshot resource. |
| `labels` | HashMap<String, String> | Labels to apply to this InstantSnapshot. These can be later modified by
the setLabels method.
Label values may be empty. |
| `satisfies_pzs` | bool | [Output Only] Reserved for future use. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create instant_snapshot
instant_snapshot = provider.compute_api.Instant_snapshot {
    project = "value"  # Project ID for this request.
    zone = "value"  # Name of the zone for this request.
}

# Access instant_snapshot outputs
instant_snapshot_id = instant_snapshot.id
instant_snapshot_description = instant_snapshot.description
instant_snapshot_label_fingerprint = instant_snapshot.label_fingerprint
instant_snapshot_region = instant_snapshot.region
instant_snapshot_self_link = instant_snapshot.self_link
instant_snapshot_self_link_with_id = instant_snapshot.self_link_with_id
instant_snapshot_architecture = instant_snapshot.architecture
instant_snapshot_creation_timestamp = instant_snapshot.creation_timestamp
instant_snapshot_source_disk = instant_snapshot.source_disk
instant_snapshot_name = instant_snapshot.name
instant_snapshot_status = instant_snapshot.status
instant_snapshot_disk_size_gb = instant_snapshot.disk_size_gb
instant_snapshot_satisfies_pzi = instant_snapshot.satisfies_pzi
instant_snapshot_id = instant_snapshot.id
instant_snapshot_kind = instant_snapshot.kind
instant_snapshot_source_disk_id = instant_snapshot.source_disk_id
instant_snapshot_zone = instant_snapshot.zone
instant_snapshot_resource_status = instant_snapshot.resource_status
instant_snapshot_labels = instant_snapshot.labels
instant_snapshot_satisfies_pzs = instant_snapshot.satisfies_pzs
```

---


### Region_ssl_policie

Creates a new policy in the specified project and region using the data
included in the request.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `warnings` | Vec<String> |  | [Output Only] If potential misconfigurations are detected for this
SSL policy, this field will be populated with warning messages. |
| `enabled_features` | Vec<String> |  | [Output Only] The list of features enabled in the SSL policy. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `name` | String |  | Name of the resource. The name must be 1-63 characters long, and comply
with RFC1035. Specifically, the name must be 1-63 characters
long and match the regular expression
`[a-z]([-a-z0-9]*[a-z0-9])?` which means the first character
must be a lowercase letter, and all following characters must be a dash,
lowercase letter, or digit, except the last character, which cannot be a
dash. |
| `profile` | String |  | Profile specifies the set of SSL features that can be used by the load
balancer when negotiating SSL with clients. This can be one ofCOMPATIBLE, MODERN, RESTRICTED, orCUSTOM. If using CUSTOM, the set of SSL features
to enable must be specified in the customFeatures field. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `custom_features` | Vec<String> |  | A list of features enabled when the selected profile is CUSTOM. The
 method returns the set of features that can be
specified in this list. This field must be empty if the profile is notCUSTOM. |
| `kind` | String |  | [Output only] Type of the resource. Alwayscompute#sslPolicyfor SSL policies. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `fingerprint` | String |  | Fingerprint of this resource. A hash of the contents stored in this object.
This field is used in optimistic locking. This field will be ignored when
inserting a SslPolicy. An up-to-date fingerprint must be
provided in order to update the SslPolicy, otherwise the
request will fail with error 412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve an SslPolicy. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `min_tls_version` | String |  | The minimum version of SSL protocol that can be used by the clients to
establish a connection with the load balancer. This can be one ofTLS_1_0, TLS_1_1, TLS_1_2,TLS_1_3. When set to TLS_1_3, the profile field
must be set to RESTRICTED. |
| `region` | String |  | [Output Only] URL of the region where the regional SSL policy
resides. This field is not applicable to global SSL policies. |
| `project` | String | ✅ | Project ID for this request. |
| `region` | String | ✅ | Name of the region scoping this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `warnings` | Vec<String> | [Output Only] If potential misconfigurations are detected for this
SSL policy, this field will be populated with warning messages. |
| `enabled_features` | Vec<String> | [Output Only] The list of features enabled in the SSL policy. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `name` | String | Name of the resource. The name must be 1-63 characters long, and comply
with RFC1035. Specifically, the name must be 1-63 characters
long and match the regular expression
`[a-z]([-a-z0-9]*[a-z0-9])?` which means the first character
must be a lowercase letter, and all following characters must be a dash,
lowercase letter, or digit, except the last character, which cannot be a
dash. |
| `profile` | String | Profile specifies the set of SSL features that can be used by the load
balancer when negotiating SSL with clients. This can be one ofCOMPATIBLE, MODERN, RESTRICTED, orCUSTOM. If using CUSTOM, the set of SSL features
to enable must be specified in the customFeatures field. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `custom_features` | Vec<String> | A list of features enabled when the selected profile is CUSTOM. The
 method returns the set of features that can be
specified in this list. This field must be empty if the profile is notCUSTOM. |
| `kind` | String | [Output only] Type of the resource. Alwayscompute#sslPolicyfor SSL policies. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `fingerprint` | String | Fingerprint of this resource. A hash of the contents stored in this object.
This field is used in optimistic locking. This field will be ignored when
inserting a SslPolicy. An up-to-date fingerprint must be
provided in order to update the SslPolicy, otherwise the
request will fail with error 412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve an SslPolicy. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `min_tls_version` | String | The minimum version of SSL protocol that can be used by the clients to
establish a connection with the load balancer. This can be one ofTLS_1_0, TLS_1_1, TLS_1_2,TLS_1_3. When set to TLS_1_3, the profile field
must be set to RESTRICTED. |
| `region` | String | [Output Only] URL of the region where the regional SSL policy
resides. This field is not applicable to global SSL policies. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create region_ssl_policie
region_ssl_policie = provider.compute_api.Region_ssl_policie {
    project = "value"  # Project ID for this request.
    region = "value"  # Name of the region scoping this request.
}

# Access region_ssl_policie outputs
region_ssl_policie_id = region_ssl_policie.id
region_ssl_policie_warnings = region_ssl_policie.warnings
region_ssl_policie_enabled_features = region_ssl_policie.enabled_features
region_ssl_policie_description = region_ssl_policie.description
region_ssl_policie_name = region_ssl_policie.name
region_ssl_policie_profile = region_ssl_policie.profile
region_ssl_policie_self_link = region_ssl_policie.self_link
region_ssl_policie_custom_features = region_ssl_policie.custom_features
region_ssl_policie_kind = region_ssl_policie.kind
region_ssl_policie_creation_timestamp = region_ssl_policie.creation_timestamp
region_ssl_policie_fingerprint = region_ssl_policie.fingerprint
region_ssl_policie_id = region_ssl_policie.id
region_ssl_policie_min_tls_version = region_ssl_policie.min_tls_version
region_ssl_policie_region = region_ssl_policie.region
```

---


### Region_instance_template

Creates an instance template in the specified project and region using the
global instance template whose URL is included in the request.

**Operations**: ✅ Create ✅ Read ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `name` | String |  | Name of the resource; provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must be
a dash, lowercase letter, or digit, except the last character, which cannot
be a dash. |
| `source_instance_params` | String |  | The source instance params to use to create this instance template. |
| `creation_timestamp` | String |  | [Output Only] The creation timestamp for this instance template inRFC3339
text format. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `kind` | String |  | [Output Only] The resource type, which is alwayscompute#instanceTemplate for instance templates. |
| `self_link` | String |  | [Output Only] The URL for this instance template. The server defines this
URL. |
| `region` | String |  | [Output Only] URL of the region where the instance template resides. Only
applicable for regional resources. |
| `source_instance` | String |  | The source instance used to create the template. You can provide this as a
partial or full URL to the resource. For example, the following are valid
values:
   
   
     - https://www.googleapis.com/compute/v1/projects/project/zones/zone/instances/instance 
   - projects/project/zones/zone/instances/instance |
| `id` | String |  | [Output Only] A unique identifier for this instance template. The server
defines this identifier. |
| `properties` | String |  | The instance properties for this instance template. |
| `region` | String | ✅ | The name of the region for this request. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `name` | String | Name of the resource; provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must be
a dash, lowercase letter, or digit, except the last character, which cannot
be a dash. |
| `source_instance_params` | String | The source instance params to use to create this instance template. |
| `creation_timestamp` | String | [Output Only] The creation timestamp for this instance template inRFC3339
text format. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `kind` | String | [Output Only] The resource type, which is alwayscompute#instanceTemplate for instance templates. |
| `self_link` | String | [Output Only] The URL for this instance template. The server defines this
URL. |
| `region` | String | [Output Only] URL of the region where the instance template resides. Only
applicable for regional resources. |
| `source_instance` | String | The source instance used to create the template. You can provide this as a
partial or full URL to the resource. For example, the following are valid
values:
   
   
     - https://www.googleapis.com/compute/v1/projects/project/zones/zone/instances/instance 
   - projects/project/zones/zone/instances/instance |
| `id` | String | [Output Only] A unique identifier for this instance template. The server
defines this identifier. |
| `properties` | String | The instance properties for this instance template. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create region_instance_template
region_instance_template = provider.compute_api.Region_instance_template {
    region = "value"  # The name of the region for this request.
    project = "value"  # Project ID for this request.
}

# Access region_instance_template outputs
region_instance_template_id = region_instance_template.id
region_instance_template_name = region_instance_template.name
region_instance_template_source_instance_params = region_instance_template.source_instance_params
region_instance_template_creation_timestamp = region_instance_template.creation_timestamp
region_instance_template_description = region_instance_template.description
region_instance_template_kind = region_instance_template.kind
region_instance_template_self_link = region_instance_template.self_link
region_instance_template_region = region_instance_template.region
region_instance_template_source_instance = region_instance_template.source_instance
region_instance_template_id = region_instance_template.id
region_instance_template_properties = region_instance_template.properties
```

---


### Vpn_tunnel

Creates a VpnTunnel resource in the specified project and region using
the data included in the request.

**Operations**: ✅ Create ✅ Read ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `cipher_suite` | String |  | User specified list of ciphers to use for the phase 1 and phase 2 of the
IKE protocol. |
| `vpn_gateway_interface` | i64 |  | The interface ID of the VPN gateway with which this VPN tunnel is
associated.
Possible values are: `0`, `1`. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `name` | String |  | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `detailed_status` | String |  | [Output Only] Detailed status message for the VPN tunnel. |
| `local_traffic_selector` | Vec<String> |  | Local traffic selector to use when establishing the VPN tunnel with the
peer VPN gateway. The value should be a CIDR formatted string, for
example: 192.168.0.0/16. The ranges must be disjoint.
Only IPv4 is supported for Classic VPN tunnels. This field is output only
for HA VPN tunnels. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `ike_version` | i64 |  | IKE protocol version to use when establishing the VPN tunnel with the peer
VPN gateway. Acceptable IKE versions are 1 or 2.
The default version is 2. |
| `status` | String |  | [Output Only] The status of the VPN tunnel, which can be one of the
following:
   
   - PROVISIONING: Resource is being allocated for the VPN tunnel.
   - WAITING_FOR_FULL_CONFIG: Waiting to receive all VPN-related configs
   from
     the user. Network, TargetVpnGateway, VpnTunnel, ForwardingRule, and Route
     resources are needed to setup the VPN tunnel.
   - FIRST_HANDSHAKE: Successful first handshake with the peer VPN.
   - ESTABLISHED: Secure session is successfully established with the peer
   VPN. 
   - NETWORK_ERROR: Deprecated, replaced by
   NO_INCOMING_PACKETS 
   - AUTHORIZATION_ERROR: Auth error (for example,
   bad shared secret). 
   - NEGOTIATION_FAILURE: Handshake failed.
   - DEPROVISIONING: Resources are being deallocated for the VPN
   tunnel. 
   - FAILED: Tunnel creation has failed and the tunnel is not
   ready to be used. 
   - NO_INCOMING_PACKETS: No incoming packets from
   peer. 
   - REJECTED: Tunnel configuration was rejected, can be result
   of being denied access. 
   - ALLOCATING_RESOURCES: Cloud VPN is in the
   process of allocating all required resources.
   - STOPPED: Tunnel is stopped due to its Forwarding Rules being deleted
   for Classic VPN tunnels or the project is in frozen state.
   - PEER_IDENTITY_MISMATCH: Peer identity does not match peer IP,
   probably behind NAT. 
   - TS_NARROWING_NOT_ALLOWED: Traffic selector
   narrowing not allowed for an HA-VPN tunnel. |
| `region` | String |  | [Output Only] URL of the region where the VPN tunnel resides.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `remote_traffic_selector` | Vec<String> |  | Remote traffic selectors to use when establishing the VPN tunnel with
the peer VPN gateway. The value should be a CIDR formatted string,
for example: 192.168.0.0/16. The ranges should be disjoint. Only IPv4 is
supported for Classic VPN tunnels. This field is output only for HA VPN
tunnels. |
| `label_fingerprint` | String |  | A fingerprint for the labels being applied to this VpnTunnel, which is
essentially a hash of the labels set used for optimistic locking. The
fingerprint is initially generated by Compute Engine and changes after
every request to modify or update labels. You must always provide an
up-to-date fingerprint hash in order to update or change labels,
otherwise the request will fail with error412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve a VpnTunnel. |
| `shared_secret_hash` | String |  | Hash of the shared secret. |
| `target_vpn_gateway` | String |  | URL of the Target VPN gateway with which this VPN tunnel is associated.
Provided by the client when the VPN tunnel is created.
This field can be set only for Classic VPN tunnels. |
| `labels` | HashMap<String, String> |  | Labels for this resource. These can only be added or modified by thesetLabels method. Each label key/value pair must comply withRFC1035.
Label values may be empty. |
| `peer_external_gateway` | String |  | URL of the peer side external VPN gateway to which this VPN tunnel is
connected.
Provided by the client when the VPN tunnel is created.
This field is exclusive with the field peerGcpGateway. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `peer_gcp_gateway` | String |  | URL of the peer side HA VPN gateway to which this VPN tunnel
is connected. Provided by the client when the VPN tunnel is created.
This field can be used when creating highly available VPN from VPC network
to VPC network, the field is exclusive with the field peerExternalGateway.
If provided, the VPN tunnel will automatically use the same
vpnGatewayInterface ID in the peer Google Cloud VPN gateway. |
| `peer_ip` | String |  | IP address of the peer VPN gateway. Only IPv4 is supported. This field can
be set only for Classic VPN tunnels. |
| `router` | String |  | URL of the router resource to be used for dynamic routing. |
| `kind` | String |  | [Output Only] Type of resource. Always compute#vpnTunnel for
VPN tunnels. |
| `peer_external_gateway_interface` | i64 |  | The interface ID of the external VPN gateway to which this VPN tunnel is
connected. Provided by the client when the VPN tunnel is created.
Possible values are: `0`, `1`, `2`, `3`. The number of IDs in use
depends on the external VPN gateway redundancy type. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `shared_secret` | String |  | Shared secret used to set the secure session between the Cloud VPN gateway
and the peer VPN gateway. |
| `vpn_gateway` | String |  | URL of the VPN gateway with which this VPN tunnel is associated.
Provided by the client when the VPN tunnel is created. This must be
used (instead of target_vpn_gateway) if a High Availability VPN gateway
resource is created. |
| `region` | String | ✅ | Name of the region for this request. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `cipher_suite` | String | User specified list of ciphers to use for the phase 1 and phase 2 of the
IKE protocol. |
| `vpn_gateway_interface` | i64 | The interface ID of the VPN gateway with which this VPN tunnel is
associated.
Possible values are: `0`, `1`. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `name` | String | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `detailed_status` | String | [Output Only] Detailed status message for the VPN tunnel. |
| `local_traffic_selector` | Vec<String> | Local traffic selector to use when establishing the VPN tunnel with the
peer VPN gateway. The value should be a CIDR formatted string, for
example: 192.168.0.0/16. The ranges must be disjoint.
Only IPv4 is supported for Classic VPN tunnels. This field is output only
for HA VPN tunnels. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `ike_version` | i64 | IKE protocol version to use when establishing the VPN tunnel with the peer
VPN gateway. Acceptable IKE versions are 1 or 2.
The default version is 2. |
| `status` | String | [Output Only] The status of the VPN tunnel, which can be one of the
following:
   
   - PROVISIONING: Resource is being allocated for the VPN tunnel.
   - WAITING_FOR_FULL_CONFIG: Waiting to receive all VPN-related configs
   from
     the user. Network, TargetVpnGateway, VpnTunnel, ForwardingRule, and Route
     resources are needed to setup the VPN tunnel.
   - FIRST_HANDSHAKE: Successful first handshake with the peer VPN.
   - ESTABLISHED: Secure session is successfully established with the peer
   VPN. 
   - NETWORK_ERROR: Deprecated, replaced by
   NO_INCOMING_PACKETS 
   - AUTHORIZATION_ERROR: Auth error (for example,
   bad shared secret). 
   - NEGOTIATION_FAILURE: Handshake failed.
   - DEPROVISIONING: Resources are being deallocated for the VPN
   tunnel. 
   - FAILED: Tunnel creation has failed and the tunnel is not
   ready to be used. 
   - NO_INCOMING_PACKETS: No incoming packets from
   peer. 
   - REJECTED: Tunnel configuration was rejected, can be result
   of being denied access. 
   - ALLOCATING_RESOURCES: Cloud VPN is in the
   process of allocating all required resources.
   - STOPPED: Tunnel is stopped due to its Forwarding Rules being deleted
   for Classic VPN tunnels or the project is in frozen state.
   - PEER_IDENTITY_MISMATCH: Peer identity does not match peer IP,
   probably behind NAT. 
   - TS_NARROWING_NOT_ALLOWED: Traffic selector
   narrowing not allowed for an HA-VPN tunnel. |
| `region` | String | [Output Only] URL of the region where the VPN tunnel resides.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `remote_traffic_selector` | Vec<String> | Remote traffic selectors to use when establishing the VPN tunnel with
the peer VPN gateway. The value should be a CIDR formatted string,
for example: 192.168.0.0/16. The ranges should be disjoint. Only IPv4 is
supported for Classic VPN tunnels. This field is output only for HA VPN
tunnels. |
| `label_fingerprint` | String | A fingerprint for the labels being applied to this VpnTunnel, which is
essentially a hash of the labels set used for optimistic locking. The
fingerprint is initially generated by Compute Engine and changes after
every request to modify or update labels. You must always provide an
up-to-date fingerprint hash in order to update or change labels,
otherwise the request will fail with error412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve a VpnTunnel. |
| `shared_secret_hash` | String | Hash of the shared secret. |
| `target_vpn_gateway` | String | URL of the Target VPN gateway with which this VPN tunnel is associated.
Provided by the client when the VPN tunnel is created.
This field can be set only for Classic VPN tunnels. |
| `labels` | HashMap<String, String> | Labels for this resource. These can only be added or modified by thesetLabels method. Each label key/value pair must comply withRFC1035.
Label values may be empty. |
| `peer_external_gateway` | String | URL of the peer side external VPN gateway to which this VPN tunnel is
connected.
Provided by the client when the VPN tunnel is created.
This field is exclusive with the field peerGcpGateway. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `peer_gcp_gateway` | String | URL of the peer side HA VPN gateway to which this VPN tunnel
is connected. Provided by the client when the VPN tunnel is created.
This field can be used when creating highly available VPN from VPC network
to VPC network, the field is exclusive with the field peerExternalGateway.
If provided, the VPN tunnel will automatically use the same
vpnGatewayInterface ID in the peer Google Cloud VPN gateway. |
| `peer_ip` | String | IP address of the peer VPN gateway. Only IPv4 is supported. This field can
be set only for Classic VPN tunnels. |
| `router` | String | URL of the router resource to be used for dynamic routing. |
| `kind` | String | [Output Only] Type of resource. Always compute#vpnTunnel for
VPN tunnels. |
| `peer_external_gateway_interface` | i64 | The interface ID of the external VPN gateway to which this VPN tunnel is
connected. Provided by the client when the VPN tunnel is created.
Possible values are: `0`, `1`, `2`, `3`. The number of IDs in use
depends on the external VPN gateway redundancy type. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `shared_secret` | String | Shared secret used to set the secure session between the Cloud VPN gateway
and the peer VPN gateway. |
| `vpn_gateway` | String | URL of the VPN gateway with which this VPN tunnel is associated.
Provided by the client when the VPN tunnel is created. This must be
used (instead of target_vpn_gateway) if a High Availability VPN gateway
resource is created. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create vpn_tunnel
vpn_tunnel = provider.compute_api.Vpn_tunnel {
    region = "value"  # Name of the region for this request.
    project = "value"  # Project ID for this request.
}

# Access vpn_tunnel outputs
vpn_tunnel_id = vpn_tunnel.id
vpn_tunnel_cipher_suite = vpn_tunnel.cipher_suite
vpn_tunnel_vpn_gateway_interface = vpn_tunnel.vpn_gateway_interface
vpn_tunnel_creation_timestamp = vpn_tunnel.creation_timestamp
vpn_tunnel_name = vpn_tunnel.name
vpn_tunnel_detailed_status = vpn_tunnel.detailed_status
vpn_tunnel_local_traffic_selector = vpn_tunnel.local_traffic_selector
vpn_tunnel_description = vpn_tunnel.description
vpn_tunnel_ike_version = vpn_tunnel.ike_version
vpn_tunnel_status = vpn_tunnel.status
vpn_tunnel_region = vpn_tunnel.region
vpn_tunnel_remote_traffic_selector = vpn_tunnel.remote_traffic_selector
vpn_tunnel_label_fingerprint = vpn_tunnel.label_fingerprint
vpn_tunnel_shared_secret_hash = vpn_tunnel.shared_secret_hash
vpn_tunnel_target_vpn_gateway = vpn_tunnel.target_vpn_gateway
vpn_tunnel_labels = vpn_tunnel.labels
vpn_tunnel_peer_external_gateway = vpn_tunnel.peer_external_gateway
vpn_tunnel_id = vpn_tunnel.id
vpn_tunnel_peer_gcp_gateway = vpn_tunnel.peer_gcp_gateway
vpn_tunnel_peer_ip = vpn_tunnel.peer_ip
vpn_tunnel_router = vpn_tunnel.router
vpn_tunnel_kind = vpn_tunnel.kind
vpn_tunnel_peer_external_gateway_interface = vpn_tunnel.peer_external_gateway_interface
vpn_tunnel_self_link = vpn_tunnel.self_link
vpn_tunnel_shared_secret = vpn_tunnel.shared_secret
vpn_tunnel_vpn_gateway = vpn_tunnel.vpn_gateway
```

---


### Region_ssl_certificate

Creates a SslCertificate resource in the specified project and region using
the data included in the request

**Operations**: ✅ Create ✅ Read ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `expire_time` | String |  | [Output Only] Expire time of the certificate. RFC3339 |
| `kind` | String |  | [Output Only] Type of the resource. Alwayscompute#sslCertificate for SSL certificates. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `private_key` | String |  | A value read into memory from a write-only private key file. The private
key file must be in PEM format. For security, only insert
requests include this field. |
| `region` | String |  | [Output Only] URL of the region where the regional SSL Certificate
resides. This field is not applicable to global SSL Certificate. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `self_link` | String |  | [Output only] Server-defined URL for the resource. |
| `self_managed` | String |  | Configuration and status of a self-managed SSL certificate. |
| `certificate` | String |  | A value read into memory from a certificate file. The certificate file must
be in PEM format. The certificate chain must be no greater than 5 certs
long. The chain must include at least one intermediate cert. |
| `type` | String |  | (Optional) Specifies the type of SSL certificate, either "SELF_MANAGED" or
"MANAGED". If not specified, the certificate is self-managed and the fieldscertificate and private_key are used. |
| `managed` | String |  | Configuration and status of a managed SSL certificate. |
| `name` | String |  | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `subject_alternative_names` | Vec<String> |  | [Output Only] Domains associated with the certificate via Subject
Alternative Name. |
| `project` | String | ✅ | Project ID for this request. |
| `region` | String | ✅ | Name of the region scoping this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `expire_time` | String | [Output Only] Expire time of the certificate. RFC3339 |
| `kind` | String | [Output Only] Type of the resource. Alwayscompute#sslCertificate for SSL certificates. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `private_key` | String | A value read into memory from a write-only private key file. The private
key file must be in PEM format. For security, only insert
requests include this field. |
| `region` | String | [Output Only] URL of the region where the regional SSL Certificate
resides. This field is not applicable to global SSL Certificate. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `self_link` | String | [Output only] Server-defined URL for the resource. |
| `self_managed` | String | Configuration and status of a self-managed SSL certificate. |
| `certificate` | String | A value read into memory from a certificate file. The certificate file must
be in PEM format. The certificate chain must be no greater than 5 certs
long. The chain must include at least one intermediate cert. |
| `type` | String | (Optional) Specifies the type of SSL certificate, either "SELF_MANAGED" or
"MANAGED". If not specified, the certificate is self-managed and the fieldscertificate and private_key are used. |
| `managed` | String | Configuration and status of a managed SSL certificate. |
| `name` | String | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `subject_alternative_names` | Vec<String> | [Output Only] Domains associated with the certificate via Subject
Alternative Name. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create region_ssl_certificate
region_ssl_certificate = provider.compute_api.Region_ssl_certificate {
    project = "value"  # Project ID for this request.
    region = "value"  # Name of the region scoping this request.
}

# Access region_ssl_certificate outputs
region_ssl_certificate_id = region_ssl_certificate.id
region_ssl_certificate_expire_time = region_ssl_certificate.expire_time
region_ssl_certificate_kind = region_ssl_certificate.kind
region_ssl_certificate_description = region_ssl_certificate.description
region_ssl_certificate_id = region_ssl_certificate.id
region_ssl_certificate_private_key = region_ssl_certificate.private_key
region_ssl_certificate_region = region_ssl_certificate.region
region_ssl_certificate_creation_timestamp = region_ssl_certificate.creation_timestamp
region_ssl_certificate_self_link = region_ssl_certificate.self_link
region_ssl_certificate_self_managed = region_ssl_certificate.self_managed
region_ssl_certificate_certificate = region_ssl_certificate.certificate
region_ssl_certificate_type = region_ssl_certificate.type
region_ssl_certificate_managed = region_ssl_certificate.managed
region_ssl_certificate_name = region_ssl_certificate.name
region_ssl_certificate_subject_alternative_names = region_ssl_certificate.subject_alternative_names
```

---


### Wire_group

Creates a wire group in the specified project in the given scope
using the parameters that are included in the request.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `name` | String |  | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `description` | String |  | An optional description of the wire group. |
| `endpoints` | HashMap<String, String> |  | A map that contains the logical endpoints of the wire group. Specify
key-value pairs for the map as follows:
   
   - Key: an RFC1035 user-specified label.
   - Value: an Endpoint object. |
| `reconciling` | bool |  | [Output Only] Indicates whether there are wire changes yet to be processed. |
| `topology` | String |  | Topology details for the wire group configuration. |
| `wire_properties` | String |  | Properties for all wires in the wire group. |
| `admin_enabled` | bool |  | Indicates whether the wires in the wire group are enabled. When false, the
wires in the wire group are disabled. When true and when
there is simultaneously no wire-specific override of `adminEnabled` to
false, a given wire is enabled. Defaults to true. |
| `id` | String |  | [Output Only] The unique identifier for the resource type. The server
generates this identifier. |
| `wires` | Vec<String> |  | The single/redundant wire(s) managed by the wire group. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `kind` | String |  | [Output Only] Type of the resource. Alwayscompute#wireGroups for wire groups. |
| `cross_site_network` | String | ✅ |  |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `name` | String | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `description` | String | An optional description of the wire group. |
| `endpoints` | HashMap<String, String> | A map that contains the logical endpoints of the wire group. Specify
key-value pairs for the map as follows:
   
   - Key: an RFC1035 user-specified label.
   - Value: an Endpoint object. |
| `reconciling` | bool | [Output Only] Indicates whether there are wire changes yet to be processed. |
| `topology` | String | Topology details for the wire group configuration. |
| `wire_properties` | String | Properties for all wires in the wire group. |
| `admin_enabled` | bool | Indicates whether the wires in the wire group are enabled. When false, the
wires in the wire group are disabled. When true and when
there is simultaneously no wire-specific override of `adminEnabled` to
false, a given wire is enabled. Defaults to true. |
| `id` | String | [Output Only] The unique identifier for the resource type. The server
generates this identifier. |
| `wires` | Vec<String> | The single/redundant wire(s) managed by the wire group. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `kind` | String | [Output Only] Type of the resource. Alwayscompute#wireGroups for wire groups. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create wire_group
wire_group = provider.compute_api.Wire_group {
    cross_site_network = "value"  # Required field
    project = "value"  # Project ID for this request.
}

# Access wire_group outputs
wire_group_id = wire_group.id
wire_group_name = wire_group.name
wire_group_description = wire_group.description
wire_group_endpoints = wire_group.endpoints
wire_group_reconciling = wire_group.reconciling
wire_group_topology = wire_group.topology
wire_group_wire_properties = wire_group.wire_properties
wire_group_admin_enabled = wire_group.admin_enabled
wire_group_id = wire_group.id
wire_group_wires = wire_group.wires
wire_group_creation_timestamp = wire_group.creation_timestamp
wire_group_self_link = wire_group.self_link
wire_group_kind = wire_group.kind
```

---


### Target_grpc_proxie

Creates a TargetGrpcProxy in the specified project in the given scope
using the parameters that are included in the request.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `validate_for_proxyless` | bool |  | If true, indicates that the BackendServices referenced by the urlMap may be
accessed by gRPC applications without using a sidecar proxy. This will
enable configuration checks on urlMap and its referenced BackendServices to
not allow unsupported features. A gRPC application must use
"xds:///" scheme in the target URI of the service it is
connecting to.
If false, indicates that the BackendServices referenced by the urlMap will
be accessed by gRPC applications via a sidecar proxy. In this case, a gRPC
application must not use "xds:///" scheme in the target URI of
the service it is connecting to |
| `name` | String |  | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `kind` | String |  | [Output Only] Type of the resource. Alwayscompute#targetGrpcProxy for target grpc proxies. |
| `fingerprint` | String |  | Fingerprint of this resource. A hash of the contents stored in this object.
This field is used in optimistic locking. This field will be ignored when
inserting a TargetGrpcProxy. An up-to-date fingerprint must
be provided in order to patch/update the TargetGrpcProxy; otherwise, the
request will fail with error 412 conditionNotMet. To see the
latest fingerprint, make a get() request to retrieve the
TargetGrpcProxy. |
| `id` | String |  | [Output Only] The unique identifier for the resource type. The server
generates this identifier. |
| `self_link_with_id` | String |  | [Output Only] Server-defined URL with id for the resource. |
| `url_map` | String |  | URL to the UrlMap resource that defines the mapping from URL to the
BackendService. The protocol field in the BackendService must be set to
GRPC. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `validate_for_proxyless` | bool | If true, indicates that the BackendServices referenced by the urlMap may be
accessed by gRPC applications without using a sidecar proxy. This will
enable configuration checks on urlMap and its referenced BackendServices to
not allow unsupported features. A gRPC application must use
"xds:///" scheme in the target URI of the service it is
connecting to.
If false, indicates that the BackendServices referenced by the urlMap will
be accessed by gRPC applications via a sidecar proxy. In this case, a gRPC
application must not use "xds:///" scheme in the target URI of
the service it is connecting to |
| `name` | String | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `kind` | String | [Output Only] Type of the resource. Alwayscompute#targetGrpcProxy for target grpc proxies. |
| `fingerprint` | String | Fingerprint of this resource. A hash of the contents stored in this object.
This field is used in optimistic locking. This field will be ignored when
inserting a TargetGrpcProxy. An up-to-date fingerprint must
be provided in order to patch/update the TargetGrpcProxy; otherwise, the
request will fail with error 412 conditionNotMet. To see the
latest fingerprint, make a get() request to retrieve the
TargetGrpcProxy. |
| `id` | String | [Output Only] The unique identifier for the resource type. The server
generates this identifier. |
| `self_link_with_id` | String | [Output Only] Server-defined URL with id for the resource. |
| `url_map` | String | URL to the UrlMap resource that defines the mapping from URL to the
BackendService. The protocol field in the BackendService must be set to
GRPC. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create target_grpc_proxie
target_grpc_proxie = provider.compute_api.Target_grpc_proxie {
    project = "value"  # Project ID for this request.
}

# Access target_grpc_proxie outputs
target_grpc_proxie_id = target_grpc_proxie.id
target_grpc_proxie_self_link = target_grpc_proxie.self_link
target_grpc_proxie_validate_for_proxyless = target_grpc_proxie.validate_for_proxyless
target_grpc_proxie_name = target_grpc_proxie.name
target_grpc_proxie_kind = target_grpc_proxie.kind
target_grpc_proxie_fingerprint = target_grpc_proxie.fingerprint
target_grpc_proxie_id = target_grpc_proxie.id
target_grpc_proxie_self_link_with_id = target_grpc_proxie.self_link_with_id
target_grpc_proxie_url_map = target_grpc_proxie.url_map
target_grpc_proxie_creation_timestamp = target_grpc_proxie.creation_timestamp
target_grpc_proxie_description = target_grpc_proxie.description
```

---


### Network_attachment

Creates a NetworkAttachment in the specified project in the given scope
using the parameters that are included in the request.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `subnetworks` | Vec<String> |  | An array of URLs where each entry is the URL of a subnet
provided by the service consumer to use for
endpoints in the producers that connect to this network attachment. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `connection_preference` | String |  |  |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `self_link_with_id` | String |  | [Output Only] Server-defined URL for this resource's resource id. |
| `id` | String |  | [Output Only] The unique identifier for the resource type. The server
generates this identifier. |
| `kind` | String |  | [Output Only] Type of the resource. |
| `producer_accept_lists` | Vec<String> |  | Projects that are allowed to connect to this network attachment.
The project can be specified using its id or number. |
| `fingerprint` | String |  | Fingerprint of this resource. A hash of the contents stored
in this object. This field is used in optimistic locking. An up-to-date
fingerprint must be provided in order to patch. |
| `connection_endpoints` | Vec<String> |  | [Output Only] An array of connections for all the producers connected
to this network attachment. |
| `producer_reject_lists` | Vec<String> |  | Projects that are not allowed to connect to this network attachment.
The project can be specified using its id or number. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `name` | String |  | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `network` | String |  | [Output Only] The URL of the network which the Network Attachment
belongs to. Practically it is inferred by fetching the network of the first
subnetwork associated. Because it is required that all the subnetworks must
be from the same network, it is assured that the Network Attachment belongs
to the same network as all the subnetworks. |
| `region` | String |  | [Output Only] URL of the region where the network attachment resides.
This field applies only to the region resource. You must specify this
field as part of the HTTP request URL. It is not settable as a field in
the request body. |
| `region` | String | ✅ | Name of the region of this request. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `subnetworks` | Vec<String> | An array of URLs where each entry is the URL of a subnet
provided by the service consumer to use for
endpoints in the producers that connect to this network attachment. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `connection_preference` | String |  |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `self_link_with_id` | String | [Output Only] Server-defined URL for this resource's resource id. |
| `id` | String | [Output Only] The unique identifier for the resource type. The server
generates this identifier. |
| `kind` | String | [Output Only] Type of the resource. |
| `producer_accept_lists` | Vec<String> | Projects that are allowed to connect to this network attachment.
The project can be specified using its id or number. |
| `fingerprint` | String | Fingerprint of this resource. A hash of the contents stored
in this object. This field is used in optimistic locking. An up-to-date
fingerprint must be provided in order to patch. |
| `connection_endpoints` | Vec<String> | [Output Only] An array of connections for all the producers connected
to this network attachment. |
| `producer_reject_lists` | Vec<String> | Projects that are not allowed to connect to this network attachment.
The project can be specified using its id or number. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `name` | String | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `network` | String | [Output Only] The URL of the network which the Network Attachment
belongs to. Practically it is inferred by fetching the network of the first
subnetwork associated. Because it is required that all the subnetworks must
be from the same network, it is assured that the Network Attachment belongs
to the same network as all the subnetworks. |
| `region` | String | [Output Only] URL of the region where the network attachment resides.
This field applies only to the region resource. You must specify this
field as part of the HTTP request URL. It is not settable as a field in
the request body. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create network_attachment
network_attachment = provider.compute_api.Network_attachment {
    region = "value"  # Name of the region of this request.
    project = "value"  # Project ID for this request.
}

# Access network_attachment outputs
network_attachment_id = network_attachment.id
network_attachment_subnetworks = network_attachment.subnetworks
network_attachment_creation_timestamp = network_attachment.creation_timestamp
network_attachment_connection_preference = network_attachment.connection_preference
network_attachment_description = network_attachment.description
network_attachment_self_link_with_id = network_attachment.self_link_with_id
network_attachment_id = network_attachment.id
network_attachment_kind = network_attachment.kind
network_attachment_producer_accept_lists = network_attachment.producer_accept_lists
network_attachment_fingerprint = network_attachment.fingerprint
network_attachment_connection_endpoints = network_attachment.connection_endpoints
network_attachment_producer_reject_lists = network_attachment.producer_reject_lists
network_attachment_self_link = network_attachment.self_link
network_attachment_name = network_attachment.name
network_attachment_network = network_attachment.network
network_attachment_region = network_attachment.region
```

---


### Region_disk_type

Returns the specified regional disk type.

**Operations**: ✅ Read

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `deprecated` | String | [Output Only] The deprecation status associated with this disk type. |
| `kind` | String | [Output Only] Type of the resource. Always compute#diskType
for disk types. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `default_disk_size_gb` | String | [Output Only] Server-defined default disk size in GB. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `name` | String | [Output Only] Name of the resource. |
| `description` | String | [Output Only] An optional description of this resource. |
| `region` | String | [Output Only] URL of the region where the disk type resides. Only
applicable for regional resources.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `valid_disk_size` | String | [Output Only] An optional textual description of the valid disk size,
such as "10GB-10TB". |
| `zone` | String | [Output Only] URL of the zone where the disk type resides.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Access region_disk_type outputs
region_disk_type_id = region_disk_type.id
region_disk_type_deprecated = region_disk_type.deprecated
region_disk_type_kind = region_disk_type.kind
region_disk_type_creation_timestamp = region_disk_type.creation_timestamp
region_disk_type_default_disk_size_gb = region_disk_type.default_disk_size_gb
region_disk_type_self_link = region_disk_type.self_link
region_disk_type_name = region_disk_type.name
region_disk_type_description = region_disk_type.description
region_disk_type_region = region_disk_type.region
region_disk_type_valid_disk_size = region_disk_type.valid_disk_size
region_disk_type_zone = region_disk_type.zone
region_disk_type_id = region_disk_type.id
```

---


### Interconnect_attachment

Creates an InterconnectAttachment in the specified project using the data
included in the request.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `configuration_constraints` | String |  | [Output Only] Constraints for this attachment, if any. The attachment does
not work if these constraints are not met. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `cloud_router_ipv6_address` | String |  | [Output Only] IPv6 address + prefix length to be configured on Cloud
Router Interface for this interconnect attachment. |
| `ipsec_internal_addresses` | Vec<String> |  | A list of URLs of addresses that have been reserved for the VLAN
attachment. Used only for the VLAN attachment that has the encryption
option as IPSEC. The addresses must be regional internal IP address ranges.
When creating an HA VPN gateway over the VLAN attachment, if the attachment
is configured to use a regional internal IP address, then the VPN gateway's
IP address is allocated from the IP address range specified here. For
example, if the HA VPN gateway's interface 0 is paired to this VLAN
attachment, then a regional internal IP address for the VPN gateway
interface 0 will be allocated from the IP address specified for this
VLAN attachment.
If this field is not specified when creating the VLAN attachment, then
later on when creating an HA VPN gateway on this VLAN attachment, the HA
VPN gateway's IP address is allocated from the regional external IP address
pool. |
| `cloud_router_ip_address` | String |  | [Output Only] IPv4 address + prefix length to be configured on Cloud Router
Interface for this interconnect attachment. |
| `operational_status` | String |  | [Output Only] The current status of whether or not this interconnect
attachment is functional, which can take one of the following values:
   
   - OS_ACTIVE: The attachment has been turned up and is ready to
   use. 
   - OS_UNPROVISIONED: The attachment is not ready to use yet,
   because turnup is not complete. |
| `google_reference_id` | String |  | [Output Only] Google reference ID, to be used when raising support tickets
with Google or otherwise to debug backend connectivity issues.
[Deprecated] This field is not used. |
| `pairing_key` | String |  | [Output only for type PARTNER. Input only for PARTNER_PROVIDER. Not
present for DEDICATED].
The opaque identifier of a PARTNER attachment used to initiate
provisioning with a selected partner.
Of the form "XXXXX/region/domain" |
| `region` | String |  | [Output Only] URL of the region where the regional interconnect attachment
resides.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `remote_service` | String |  | [Output Only]
If the attachment is on a Cross-Cloud Interconnect connection, this field
contains the interconnect's remote location service provider. Example
values: "Amazon Web Services" "Microsoft Azure".

The field is set only for attachments on Cross-Cloud Interconnect
connections. Its value is copied from the InterconnectRemoteLocation
remoteService field. |
| `l2_forwarding` | String |  | L2 Interconnect Attachment related config. This field is required if the
type is L2_DEDICATED.

The configuration specifies how VLAN tags (like dot1q, qinq, or dot1ad)
within L2 packets are mapped to the destination appliances IP addresses.
The packet is then encapsulated with the appliance IP address and sent to
the edge appliance. |
| `labels` | HashMap<String, String> |  | Labels for this resource. These can only be added or modified by thesetLabels method. Each label key/value pair must comply withRFC1035.
Label values may be empty. |
| `attachment_group` | String |  | [Output Only] URL of the AttachmentGroup that includes this Attachment. |
| `candidate_subnets` | Vec<String> |  | Input only. Up to 16 candidate prefixes that can be used to restrict the allocation
of cloudRouterIpAddress and customerRouterIpAddress for this attachment.
All prefixes must be within link-local address space (169.254.0.0/16) and
must be /29 or shorter (/28, /27, etc). Google will attempt to select an
unused /29 from the supplied candidate prefix(es). The request will fail if
all possible /29s are in use on Google's edge. If not supplied, Google will
randomly select an unused /29 from all of link-local space. |
| `satisfies_pzs` | bool |  | [Output Only] Reserved for future use. |
| `partner_metadata` | String |  | Informational metadata about Partner attachments from Partners to display
to customers.
Output only for PARTNER type, mutable for PARTNER_PROVIDER, not
available for DEDICATED. |
| `kind` | String |  | [Output Only] Type of the resource. Alwayscompute#interconnectAttachment for interconnect attachments. |
| `stack_type` | String |  | The stack type for this interconnect attachment to identify whether the
IPv6 feature is enabled or not. If not specified, IPV4_ONLY
will be used.

This field can be both set at interconnect attachments creation and
update interconnect attachment operations. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `candidate_ipv6_subnets` | Vec<String> |  | This field is not available. |
| `interconnect` | String |  | URL of the underlying Interconnect object that this attachment's traffic
will traverse through. |
| `partner_asn` | String |  | Optional BGP ASN for the router supplied by a Layer 3 Partner if they
configured BGP on behalf of the customer.
Output only for PARTNER type, input only for PARTNER_PROVIDER, not
available for DEDICATED. |
| `bandwidth` | String |  | Provisioned bandwidth capacity for the interconnect attachment. For
attachments of type DEDICATED, the user can set the bandwidth.
For attachments of type PARTNER, the Google Partner that is operating
the interconnect must set the bandwidth.
Output only for PARTNER type, mutable for PARTNER_PROVIDER and DEDICATED,
and can take one of the following values:
   
   - BPS_50M: 50 Mbit/s
   - BPS_100M: 100 Mbit/s
   - BPS_200M: 200 Mbit/s
   - BPS_300M: 300 Mbit/s
   - BPS_400M: 400 Mbit/s
   - BPS_500M: 500 Mbit/s
   - BPS_1G: 1 Gbit/s
   - BPS_2G: 2 Gbit/s
   - BPS_5G: 5 Gbit/s
   - BPS_10G: 10 Gbit/s
   - BPS_20G: 20 Gbit/s
   - BPS_50G: 50 Gbit/s
   - BPS_100G: 100 Gbit/s |
| `admin_enabled` | bool |  | Determines whether this Attachment will carry packets.
Not present for PARTNER_PROVIDER. |
| `subnet_length` | i64 |  | Input only. Length of the IPv4 subnet mask.
Allowed values:
   
   
    - 29 (default)
    - 30

The default value is 29, except for Cross-Cloud Interconnect
connections that use an InterconnectRemoteLocation with a
constraints.subnetLengthRange.min equal to 30. For example,
connections that use an Azure remote location fall into this
category. In these cases, the default value is 30, and requesting
29 returns an error.

Where both 29 and 30 are allowed, 29 is preferred, because it gives
Google Cloud Support more debugging visibility. |
| `encryption` | String |  | Indicates the user-supplied encryption option of this VLAN attachment
(interconnectAttachment). Can only be specified at attachment creation
for PARTNER or DEDICATED attachments.
Possible values are:
   
   - NONE - This is the default value, which means that the
   VLAN attachment carries unencrypted traffic. VMs are able to send
   traffic to, or receive traffic from, such a VLAN attachment.
   - IPSEC - The VLAN attachment carries only encrypted
   traffic that is encrypted by an IPsec device, such as an HA VPN gateway or
   third-party IPsec VPN. VMs cannot directly send traffic to, or receive
   traffic from, such a VLAN attachment. To use *HA VPN over Cloud
   Interconnect*, the VLAN attachment must be created with this
   option. |
| `dataplane_version` | i64 |  | [Output Only] Dataplane version for this InterconnectAttachment. This
field is only present for Dataplane version 2 and higher. Absence of this
field in the API output indicates that the Dataplane is version 1. |
| `cloud_router_ipv6_interface_id` | String |  | This field is not available. |
| `customer_router_ipv6_interface_id` | String |  | This field is not available. |
| `type` | String |  | The type of interconnect attachment this is, which can take one of the
following values:
   
   - DEDICATED: an attachment to a Dedicated Interconnect.
   - PARTNER: an attachment to a Partner Interconnect, created by the
   customer.
   - PARTNER_PROVIDER: an attachment to a Partner Interconnect, created by
   the partner.

- L2_DEDICATED: a L2 attachment to a Dedicated Interconnect. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `router` | String |  | URL of the Cloud Router to be used for dynamic routing. This router must be
in the same region as this InterconnectAttachment. The
InterconnectAttachment will automatically connect the Interconnect to the
network & region within which the Cloud Router is configured. |
| `name` | String |  | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `description` | String |  | An optional description of this resource. |
| `mtu` | i64 |  | Maximum Transmission Unit (MTU), in bytes, of packets passing through this
interconnect attachment.
Valid values are 1440, 1460, 1500, and 8896. If not specified,
the value will default to 1440. |
| `label_fingerprint` | String |  | A fingerprint for the labels being applied to this InterconnectAttachment,
which is essentially a hash of the labels set used for optimistic locking.
The fingerprint is initially generated by Compute Engine and changes after
every request to modify or update labels. You must always provide an
up-to-date fingerprint hash in order to update or change labels,
otherwise the request will fail with error412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve an InterconnectAttachment. |
| `private_interconnect_info` | String |  | [Output Only] Information specific to an InterconnectAttachment.
This property is populated if the interconnect that
this is attached to is of type DEDICATED. |
| `edge_availability_domain` | String |  | Input only. Desired availability domain for the attachment. Only available for type
PARTNER, at creation time, and can take one of the following values:
   
   - AVAILABILITY_DOMAIN_ANY
   - AVAILABILITY_DOMAIN_1
   - AVAILABILITY_DOMAIN_2


For improved reliability, customers should configure a pair of attachments,
one per availability domain. The selected availability domain will be
provided to the Partner via the pairing key, so that the provisioned
circuit will lie in the specified domain. If not specified, the value will
default to AVAILABILITY_DOMAIN_ANY. |
| `state` | String |  | [Output Only] The current state of this attachment's functionality.
Enum values ACTIVE and UNPROVISIONED are shared by DEDICATED/PRIVATE,
PARTNER, and PARTNER_PROVIDER interconnect attachments, while enum values
PENDING_PARTNER, PARTNER_REQUEST_RECEIVED, and PENDING_CUSTOMER are used
for only PARTNER and PARTNER_PROVIDER interconnect attachments.
This state can take one of the following values:
   
   - ACTIVE: The attachment has been turned up and is ready to use.
   - UNPROVISIONED: The attachment is not ready to use yet, because turnup
   is not complete.
   - PENDING_PARTNER: A newly-created PARTNER attachment that has not yet
   been configured on the Partner side.
   - PARTNER_REQUEST_RECEIVED: A PARTNER attachment is in the process of
   provisioning after a PARTNER_PROVIDER attachment was created that
   references it. 
   - PENDING_CUSTOMER: A PARTNER or PARTNER_PROVIDER
   attachment that is waiting for a customer to activate it. 
   - DEFUNCT:
   The attachment was deleted externally and is no longer functional. This
   could be because the associated Interconnect was removed, or because the
   other side of a Partner attachment was deleted. |
| `customer_router_ipv6_address` | String |  | [Output Only] IPv6 address + prefix length to be configured on the
customer router subinterface for this interconnect attachment. |
| `customer_router_ip_address` | String |  | [Output Only] IPv4 address + prefix length to be configured on the customer
router subinterface for this interconnect attachment. |
| `vlan_tag8021q` | i64 |  | The IEEE 802.1Q VLAN tag for this attachment, in the range 2-4093.
Only specified at creation time. |
| `params` | String |  | Input only. [Input Only] Additional params passed with the request, but not persisted
as part of resource payload. |
| `project` | String | ✅ | Project ID for this request. |
| `region` | String | ✅ | Name of the region for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `configuration_constraints` | String | [Output Only] Constraints for this attachment, if any. The attachment does
not work if these constraints are not met. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `cloud_router_ipv6_address` | String | [Output Only] IPv6 address + prefix length to be configured on Cloud
Router Interface for this interconnect attachment. |
| `ipsec_internal_addresses` | Vec<String> | A list of URLs of addresses that have been reserved for the VLAN
attachment. Used only for the VLAN attachment that has the encryption
option as IPSEC. The addresses must be regional internal IP address ranges.
When creating an HA VPN gateway over the VLAN attachment, if the attachment
is configured to use a regional internal IP address, then the VPN gateway's
IP address is allocated from the IP address range specified here. For
example, if the HA VPN gateway's interface 0 is paired to this VLAN
attachment, then a regional internal IP address for the VPN gateway
interface 0 will be allocated from the IP address specified for this
VLAN attachment.
If this field is not specified when creating the VLAN attachment, then
later on when creating an HA VPN gateway on this VLAN attachment, the HA
VPN gateway's IP address is allocated from the regional external IP address
pool. |
| `cloud_router_ip_address` | String | [Output Only] IPv4 address + prefix length to be configured on Cloud Router
Interface for this interconnect attachment. |
| `operational_status` | String | [Output Only] The current status of whether or not this interconnect
attachment is functional, which can take one of the following values:
   
   - OS_ACTIVE: The attachment has been turned up and is ready to
   use. 
   - OS_UNPROVISIONED: The attachment is not ready to use yet,
   because turnup is not complete. |
| `google_reference_id` | String | [Output Only] Google reference ID, to be used when raising support tickets
with Google or otherwise to debug backend connectivity issues.
[Deprecated] This field is not used. |
| `pairing_key` | String | [Output only for type PARTNER. Input only for PARTNER_PROVIDER. Not
present for DEDICATED].
The opaque identifier of a PARTNER attachment used to initiate
provisioning with a selected partner.
Of the form "XXXXX/region/domain" |
| `region` | String | [Output Only] URL of the region where the regional interconnect attachment
resides.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `remote_service` | String | [Output Only]
If the attachment is on a Cross-Cloud Interconnect connection, this field
contains the interconnect's remote location service provider. Example
values: "Amazon Web Services" "Microsoft Azure".

The field is set only for attachments on Cross-Cloud Interconnect
connections. Its value is copied from the InterconnectRemoteLocation
remoteService field. |
| `l2_forwarding` | String | L2 Interconnect Attachment related config. This field is required if the
type is L2_DEDICATED.

The configuration specifies how VLAN tags (like dot1q, qinq, or dot1ad)
within L2 packets are mapped to the destination appliances IP addresses.
The packet is then encapsulated with the appliance IP address and sent to
the edge appliance. |
| `labels` | HashMap<String, String> | Labels for this resource. These can only be added or modified by thesetLabels method. Each label key/value pair must comply withRFC1035.
Label values may be empty. |
| `attachment_group` | String | [Output Only] URL of the AttachmentGroup that includes this Attachment. |
| `candidate_subnets` | Vec<String> | Input only. Up to 16 candidate prefixes that can be used to restrict the allocation
of cloudRouterIpAddress and customerRouterIpAddress for this attachment.
All prefixes must be within link-local address space (169.254.0.0/16) and
must be /29 or shorter (/28, /27, etc). Google will attempt to select an
unused /29 from the supplied candidate prefix(es). The request will fail if
all possible /29s are in use on Google's edge. If not supplied, Google will
randomly select an unused /29 from all of link-local space. |
| `satisfies_pzs` | bool | [Output Only] Reserved for future use. |
| `partner_metadata` | String | Informational metadata about Partner attachments from Partners to display
to customers.
Output only for PARTNER type, mutable for PARTNER_PROVIDER, not
available for DEDICATED. |
| `kind` | String | [Output Only] Type of the resource. Alwayscompute#interconnectAttachment for interconnect attachments. |
| `stack_type` | String | The stack type for this interconnect attachment to identify whether the
IPv6 feature is enabled or not. If not specified, IPV4_ONLY
will be used.

This field can be both set at interconnect attachments creation and
update interconnect attachment operations. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `candidate_ipv6_subnets` | Vec<String> | This field is not available. |
| `interconnect` | String | URL of the underlying Interconnect object that this attachment's traffic
will traverse through. |
| `partner_asn` | String | Optional BGP ASN for the router supplied by a Layer 3 Partner if they
configured BGP on behalf of the customer.
Output only for PARTNER type, input only for PARTNER_PROVIDER, not
available for DEDICATED. |
| `bandwidth` | String | Provisioned bandwidth capacity for the interconnect attachment. For
attachments of type DEDICATED, the user can set the bandwidth.
For attachments of type PARTNER, the Google Partner that is operating
the interconnect must set the bandwidth.
Output only for PARTNER type, mutable for PARTNER_PROVIDER and DEDICATED,
and can take one of the following values:
   
   - BPS_50M: 50 Mbit/s
   - BPS_100M: 100 Mbit/s
   - BPS_200M: 200 Mbit/s
   - BPS_300M: 300 Mbit/s
   - BPS_400M: 400 Mbit/s
   - BPS_500M: 500 Mbit/s
   - BPS_1G: 1 Gbit/s
   - BPS_2G: 2 Gbit/s
   - BPS_5G: 5 Gbit/s
   - BPS_10G: 10 Gbit/s
   - BPS_20G: 20 Gbit/s
   - BPS_50G: 50 Gbit/s
   - BPS_100G: 100 Gbit/s |
| `admin_enabled` | bool | Determines whether this Attachment will carry packets.
Not present for PARTNER_PROVIDER. |
| `subnet_length` | i64 | Input only. Length of the IPv4 subnet mask.
Allowed values:
   
   
    - 29 (default)
    - 30

The default value is 29, except for Cross-Cloud Interconnect
connections that use an InterconnectRemoteLocation with a
constraints.subnetLengthRange.min equal to 30. For example,
connections that use an Azure remote location fall into this
category. In these cases, the default value is 30, and requesting
29 returns an error.

Where both 29 and 30 are allowed, 29 is preferred, because it gives
Google Cloud Support more debugging visibility. |
| `encryption` | String | Indicates the user-supplied encryption option of this VLAN attachment
(interconnectAttachment). Can only be specified at attachment creation
for PARTNER or DEDICATED attachments.
Possible values are:
   
   - NONE - This is the default value, which means that the
   VLAN attachment carries unencrypted traffic. VMs are able to send
   traffic to, or receive traffic from, such a VLAN attachment.
   - IPSEC - The VLAN attachment carries only encrypted
   traffic that is encrypted by an IPsec device, such as an HA VPN gateway or
   third-party IPsec VPN. VMs cannot directly send traffic to, or receive
   traffic from, such a VLAN attachment. To use *HA VPN over Cloud
   Interconnect*, the VLAN attachment must be created with this
   option. |
| `dataplane_version` | i64 | [Output Only] Dataplane version for this InterconnectAttachment. This
field is only present for Dataplane version 2 and higher. Absence of this
field in the API output indicates that the Dataplane is version 1. |
| `cloud_router_ipv6_interface_id` | String | This field is not available. |
| `customer_router_ipv6_interface_id` | String | This field is not available. |
| `type` | String | The type of interconnect attachment this is, which can take one of the
following values:
   
   - DEDICATED: an attachment to a Dedicated Interconnect.
   - PARTNER: an attachment to a Partner Interconnect, created by the
   customer.
   - PARTNER_PROVIDER: an attachment to a Partner Interconnect, created by
   the partner.

- L2_DEDICATED: a L2 attachment to a Dedicated Interconnect. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `router` | String | URL of the Cloud Router to be used for dynamic routing. This router must be
in the same region as this InterconnectAttachment. The
InterconnectAttachment will automatically connect the Interconnect to the
network & region within which the Cloud Router is configured. |
| `name` | String | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `description` | String | An optional description of this resource. |
| `mtu` | i64 | Maximum Transmission Unit (MTU), in bytes, of packets passing through this
interconnect attachment.
Valid values are 1440, 1460, 1500, and 8896. If not specified,
the value will default to 1440. |
| `label_fingerprint` | String | A fingerprint for the labels being applied to this InterconnectAttachment,
which is essentially a hash of the labels set used for optimistic locking.
The fingerprint is initially generated by Compute Engine and changes after
every request to modify or update labels. You must always provide an
up-to-date fingerprint hash in order to update or change labels,
otherwise the request will fail with error412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve an InterconnectAttachment. |
| `private_interconnect_info` | String | [Output Only] Information specific to an InterconnectAttachment.
This property is populated if the interconnect that
this is attached to is of type DEDICATED. |
| `edge_availability_domain` | String | Input only. Desired availability domain for the attachment. Only available for type
PARTNER, at creation time, and can take one of the following values:
   
   - AVAILABILITY_DOMAIN_ANY
   - AVAILABILITY_DOMAIN_1
   - AVAILABILITY_DOMAIN_2


For improved reliability, customers should configure a pair of attachments,
one per availability domain. The selected availability domain will be
provided to the Partner via the pairing key, so that the provisioned
circuit will lie in the specified domain. If not specified, the value will
default to AVAILABILITY_DOMAIN_ANY. |
| `state` | String | [Output Only] The current state of this attachment's functionality.
Enum values ACTIVE and UNPROVISIONED are shared by DEDICATED/PRIVATE,
PARTNER, and PARTNER_PROVIDER interconnect attachments, while enum values
PENDING_PARTNER, PARTNER_REQUEST_RECEIVED, and PENDING_CUSTOMER are used
for only PARTNER and PARTNER_PROVIDER interconnect attachments.
This state can take one of the following values:
   
   - ACTIVE: The attachment has been turned up and is ready to use.
   - UNPROVISIONED: The attachment is not ready to use yet, because turnup
   is not complete.
   - PENDING_PARTNER: A newly-created PARTNER attachment that has not yet
   been configured on the Partner side.
   - PARTNER_REQUEST_RECEIVED: A PARTNER attachment is in the process of
   provisioning after a PARTNER_PROVIDER attachment was created that
   references it. 
   - PENDING_CUSTOMER: A PARTNER or PARTNER_PROVIDER
   attachment that is waiting for a customer to activate it. 
   - DEFUNCT:
   The attachment was deleted externally and is no longer functional. This
   could be because the associated Interconnect was removed, or because the
   other side of a Partner attachment was deleted. |
| `customer_router_ipv6_address` | String | [Output Only] IPv6 address + prefix length to be configured on the
customer router subinterface for this interconnect attachment. |
| `customer_router_ip_address` | String | [Output Only] IPv4 address + prefix length to be configured on the customer
router subinterface for this interconnect attachment. |
| `vlan_tag8021q` | i64 | The IEEE 802.1Q VLAN tag for this attachment, in the range 2-4093.
Only specified at creation time. |
| `params` | String | Input only. [Input Only] Additional params passed with the request, but not persisted
as part of resource payload. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create interconnect_attachment
interconnect_attachment = provider.compute_api.Interconnect_attachment {
    project = "value"  # Project ID for this request.
    region = "value"  # Name of the region for this request.
}

# Access interconnect_attachment outputs
interconnect_attachment_id = interconnect_attachment.id
interconnect_attachment_configuration_constraints = interconnect_attachment.configuration_constraints
interconnect_attachment_creation_timestamp = interconnect_attachment.creation_timestamp
interconnect_attachment_cloud_router_ipv6_address = interconnect_attachment.cloud_router_ipv6_address
interconnect_attachment_ipsec_internal_addresses = interconnect_attachment.ipsec_internal_addresses
interconnect_attachment_cloud_router_ip_address = interconnect_attachment.cloud_router_ip_address
interconnect_attachment_operational_status = interconnect_attachment.operational_status
interconnect_attachment_google_reference_id = interconnect_attachment.google_reference_id
interconnect_attachment_pairing_key = interconnect_attachment.pairing_key
interconnect_attachment_region = interconnect_attachment.region
interconnect_attachment_remote_service = interconnect_attachment.remote_service
interconnect_attachment_l2_forwarding = interconnect_attachment.l2_forwarding
interconnect_attachment_labels = interconnect_attachment.labels
interconnect_attachment_attachment_group = interconnect_attachment.attachment_group
interconnect_attachment_candidate_subnets = interconnect_attachment.candidate_subnets
interconnect_attachment_satisfies_pzs = interconnect_attachment.satisfies_pzs
interconnect_attachment_partner_metadata = interconnect_attachment.partner_metadata
interconnect_attachment_kind = interconnect_attachment.kind
interconnect_attachment_stack_type = interconnect_attachment.stack_type
interconnect_attachment_self_link = interconnect_attachment.self_link
interconnect_attachment_candidate_ipv6_subnets = interconnect_attachment.candidate_ipv6_subnets
interconnect_attachment_interconnect = interconnect_attachment.interconnect
interconnect_attachment_partner_asn = interconnect_attachment.partner_asn
interconnect_attachment_bandwidth = interconnect_attachment.bandwidth
interconnect_attachment_admin_enabled = interconnect_attachment.admin_enabled
interconnect_attachment_subnet_length = interconnect_attachment.subnet_length
interconnect_attachment_encryption = interconnect_attachment.encryption
interconnect_attachment_dataplane_version = interconnect_attachment.dataplane_version
interconnect_attachment_cloud_router_ipv6_interface_id = interconnect_attachment.cloud_router_ipv6_interface_id
interconnect_attachment_customer_router_ipv6_interface_id = interconnect_attachment.customer_router_ipv6_interface_id
interconnect_attachment_type = interconnect_attachment.type
interconnect_attachment_id = interconnect_attachment.id
interconnect_attachment_router = interconnect_attachment.router
interconnect_attachment_name = interconnect_attachment.name
interconnect_attachment_description = interconnect_attachment.description
interconnect_attachment_mtu = interconnect_attachment.mtu
interconnect_attachment_label_fingerprint = interconnect_attachment.label_fingerprint
interconnect_attachment_private_interconnect_info = interconnect_attachment.private_interconnect_info
interconnect_attachment_edge_availability_domain = interconnect_attachment.edge_availability_domain
interconnect_attachment_state = interconnect_attachment.state
interconnect_attachment_customer_router_ipv6_address = interconnect_attachment.customer_router_ipv6_address
interconnect_attachment_customer_router_ip_address = interconnect_attachment.customer_router_ip_address
interconnect_attachment_vlan_tag8021q = interconnect_attachment.vlan_tag8021q
interconnect_attachment_params = interconnect_attachment.params
```

---


### Region_network_endpoint_group

Creates a network endpoint group in the specified project using the
parameters that are included in the request.

**Operations**: ✅ Create ✅ Read ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `psc_data` | String |  | Optional. Only valid when networkEndpointType isPRIVATE_SERVICE_CONNECT. |
| `region` | String |  | [Output Only] The URL of theregion
where the network endpoint group is located. |
| `size` | i64 |  | [Output only] Number of network endpoints in the network endpoint group. |
| `network_endpoint_type` | String |  | Type of network endpoints in this network endpoint group. Can be one ofGCE_VM_IP, GCE_VM_IP_PORT,NON_GCP_PRIVATE_IP_PORT, INTERNET_FQDN_PORT,INTERNET_IP_PORT, SERVERLESS,PRIVATE_SERVICE_CONNECT, GCE_VM_IP_PORTMAP. |
| `annotations` | HashMap<String, String> |  | Optional. Metadata defined as annotations on the network endpoint group. |
| `app_engine` | String |  | Optional. Only valid when networkEndpointType isSERVERLESS. Only one of cloudRun,appEngine or cloudFunction may be set. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `network` | String |  | The URL of the network to which all network endpoints in the NEG belong.
Uses default project network if unspecified. |
| `kind` | String |  | [Output Only] Type of the resource. Alwayscompute#networkEndpointGroup for network endpoint group. |
| `default_port` | i64 |  | The default port used if the port number is not specified in the network
endpoint.

Optional. If the network endpoint type is either GCE_VM_IP,SERVERLESS or PRIVATE_SERVICE_CONNECT, this
field must not be specified. |
| `name` | String |  | Name of the resource; provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must be
a dash, lowercase letter, or digit, except the last character, which cannot
be a dash. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `psc_target_service` | String |  | The target service url used to set up private service connection to
a Google API or a PSC Producer Service Attachment.
An example value is: asia-northeast3-cloudkms.googleapis.com.

Optional. Only valid when networkEndpointType isPRIVATE_SERVICE_CONNECT. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `cloud_run` | String |  | Optional. Only valid when networkEndpointType isSERVERLESS. Only one of cloudRun,appEngine or cloudFunction may be set. |
| `cloud_function` | String |  | Optional. Only valid when networkEndpointType isSERVERLESS. Only one of cloudRun,appEngine or cloudFunction may be set. |
| `subnetwork` | String |  | Optional URL of the subnetwork to which all network endpoints in the NEG
belong. |
| `zone` | String |  | [Output Only] The URL of thezone
where the network endpoint group is located. |
| `region` | String | ✅ | The name of the region where
you want to create the network endpoint group. It should comply with
RFC1035. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `psc_data` | String | Optional. Only valid when networkEndpointType isPRIVATE_SERVICE_CONNECT. |
| `region` | String | [Output Only] The URL of theregion
where the network endpoint group is located. |
| `size` | i64 | [Output only] Number of network endpoints in the network endpoint group. |
| `network_endpoint_type` | String | Type of network endpoints in this network endpoint group. Can be one ofGCE_VM_IP, GCE_VM_IP_PORT,NON_GCP_PRIVATE_IP_PORT, INTERNET_FQDN_PORT,INTERNET_IP_PORT, SERVERLESS,PRIVATE_SERVICE_CONNECT, GCE_VM_IP_PORTMAP. |
| `annotations` | HashMap<String, String> | Optional. Metadata defined as annotations on the network endpoint group. |
| `app_engine` | String | Optional. Only valid when networkEndpointType isSERVERLESS. Only one of cloudRun,appEngine or cloudFunction may be set. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `network` | String | The URL of the network to which all network endpoints in the NEG belong.
Uses default project network if unspecified. |
| `kind` | String | [Output Only] Type of the resource. Alwayscompute#networkEndpointGroup for network endpoint group. |
| `default_port` | i64 | The default port used if the port number is not specified in the network
endpoint.

Optional. If the network endpoint type is either GCE_VM_IP,SERVERLESS or PRIVATE_SERVICE_CONNECT, this
field must not be specified. |
| `name` | String | Name of the resource; provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must be
a dash, lowercase letter, or digit, except the last character, which cannot
be a dash. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `psc_target_service` | String | The target service url used to set up private service connection to
a Google API or a PSC Producer Service Attachment.
An example value is: asia-northeast3-cloudkms.googleapis.com.

Optional. Only valid when networkEndpointType isPRIVATE_SERVICE_CONNECT. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `cloud_run` | String | Optional. Only valid when networkEndpointType isSERVERLESS. Only one of cloudRun,appEngine or cloudFunction may be set. |
| `cloud_function` | String | Optional. Only valid when networkEndpointType isSERVERLESS. Only one of cloudRun,appEngine or cloudFunction may be set. |
| `subnetwork` | String | Optional URL of the subnetwork to which all network endpoints in the NEG
belong. |
| `zone` | String | [Output Only] The URL of thezone
where the network endpoint group is located. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create region_network_endpoint_group
region_network_endpoint_group = provider.compute_api.Region_network_endpoint_group {
    region = "value"  # The name of the region where
you want to create the network endpoint group. It should comply with
RFC1035.
    project = "value"  # Project ID for this request.
}

# Access region_network_endpoint_group outputs
region_network_endpoint_group_id = region_network_endpoint_group.id
region_network_endpoint_group_psc_data = region_network_endpoint_group.psc_data
region_network_endpoint_group_region = region_network_endpoint_group.region
region_network_endpoint_group_size = region_network_endpoint_group.size
region_network_endpoint_group_network_endpoint_type = region_network_endpoint_group.network_endpoint_type
region_network_endpoint_group_annotations = region_network_endpoint_group.annotations
region_network_endpoint_group_app_engine = region_network_endpoint_group.app_engine
region_network_endpoint_group_self_link = region_network_endpoint_group.self_link
region_network_endpoint_group_network = region_network_endpoint_group.network
region_network_endpoint_group_kind = region_network_endpoint_group.kind
region_network_endpoint_group_default_port = region_network_endpoint_group.default_port
region_network_endpoint_group_name = region_network_endpoint_group.name
region_network_endpoint_group_creation_timestamp = region_network_endpoint_group.creation_timestamp
region_network_endpoint_group_psc_target_service = region_network_endpoint_group.psc_target_service
region_network_endpoint_group_id = region_network_endpoint_group.id
region_network_endpoint_group_description = region_network_endpoint_group.description
region_network_endpoint_group_cloud_run = region_network_endpoint_group.cloud_run
region_network_endpoint_group_cloud_function = region_network_endpoint_group.cloud_function
region_network_endpoint_group_subnetwork = region_network_endpoint_group.subnetwork
region_network_endpoint_group_zone = region_network_endpoint_group.zone
```

---


### Ssl_certificate

Creates a SslCertificate resource in the specified project using the data
included in the request.

**Operations**: ✅ Create ✅ Read ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `expire_time` | String |  | [Output Only] Expire time of the certificate. RFC3339 |
| `kind` | String |  | [Output Only] Type of the resource. Alwayscompute#sslCertificate for SSL certificates. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `private_key` | String |  | A value read into memory from a write-only private key file. The private
key file must be in PEM format. For security, only insert
requests include this field. |
| `region` | String |  | [Output Only] URL of the region where the regional SSL Certificate
resides. This field is not applicable to global SSL Certificate. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `self_link` | String |  | [Output only] Server-defined URL for the resource. |
| `self_managed` | String |  | Configuration and status of a self-managed SSL certificate. |
| `certificate` | String |  | A value read into memory from a certificate file. The certificate file must
be in PEM format. The certificate chain must be no greater than 5 certs
long. The chain must include at least one intermediate cert. |
| `type` | String |  | (Optional) Specifies the type of SSL certificate, either "SELF_MANAGED" or
"MANAGED". If not specified, the certificate is self-managed and the fieldscertificate and private_key are used. |
| `managed` | String |  | Configuration and status of a managed SSL certificate. |
| `name` | String |  | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `subject_alternative_names` | Vec<String> |  | [Output Only] Domains associated with the certificate via Subject
Alternative Name. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `expire_time` | String | [Output Only] Expire time of the certificate. RFC3339 |
| `kind` | String | [Output Only] Type of the resource. Alwayscompute#sslCertificate for SSL certificates. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `private_key` | String | A value read into memory from a write-only private key file. The private
key file must be in PEM format. For security, only insert
requests include this field. |
| `region` | String | [Output Only] URL of the region where the regional SSL Certificate
resides. This field is not applicable to global SSL Certificate. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `self_link` | String | [Output only] Server-defined URL for the resource. |
| `self_managed` | String | Configuration and status of a self-managed SSL certificate. |
| `certificate` | String | A value read into memory from a certificate file. The certificate file must
be in PEM format. The certificate chain must be no greater than 5 certs
long. The chain must include at least one intermediate cert. |
| `type` | String | (Optional) Specifies the type of SSL certificate, either "SELF_MANAGED" or
"MANAGED". If not specified, the certificate is self-managed and the fieldscertificate and private_key are used. |
| `managed` | String | Configuration and status of a managed SSL certificate. |
| `name` | String | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `subject_alternative_names` | Vec<String> | [Output Only] Domains associated with the certificate via Subject
Alternative Name. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create ssl_certificate
ssl_certificate = provider.compute_api.Ssl_certificate {
    project = "value"  # Project ID for this request.
}

# Access ssl_certificate outputs
ssl_certificate_id = ssl_certificate.id
ssl_certificate_expire_time = ssl_certificate.expire_time
ssl_certificate_kind = ssl_certificate.kind
ssl_certificate_description = ssl_certificate.description
ssl_certificate_id = ssl_certificate.id
ssl_certificate_private_key = ssl_certificate.private_key
ssl_certificate_region = ssl_certificate.region
ssl_certificate_creation_timestamp = ssl_certificate.creation_timestamp
ssl_certificate_self_link = ssl_certificate.self_link
ssl_certificate_self_managed = ssl_certificate.self_managed
ssl_certificate_certificate = ssl_certificate.certificate
ssl_certificate_type = ssl_certificate.type
ssl_certificate_managed = ssl_certificate.managed
ssl_certificate_name = ssl_certificate.name
ssl_certificate_subject_alternative_names = ssl_certificate.subject_alternative_names
```

---


### Subnetwork

Creates a subnetwork in the specified project using the data
included in the request.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `ip_collection` | String |  | Reference to the source of IP, like a PublicDelegatedPrefix
(PDP) for BYOIP. The PDP must be a sub-PDP in
EXTERNAL_IPV6_SUBNETWORK_CREATION or INTERNAL_IPV6_SUBNETWORK_CREATION
mode.

Use one of the following formats to specify a sub-PDP when creating a dual
stack or IPv6-only subnetwork with external access using BYOIP:
   
   - 
   Full resource URL, as inhttps://www.googleapis.com/compute/v1/projects/projectId/regions/region/publicDelegatedPrefixes/sub-pdp-name
   - 
   Partial URL, as in
     
      
          - projects/projectId/regions/region/publicDelegatedPrefixes/sub-pdp-name
          - regions/region/publicDelegatedPrefixes/sub-pdp-name |
| `enable_flow_logs` | bool |  | Whether to enable flow logging for this subnetwork. If this field is not
explicitly set, it will not appear in get listings. If not set
the default behavior is determined by the org policy, if there is no org
policy specified, then it will default to disabled. This field isn't
supported if the subnet purpose field is set toREGIONAL_MANAGED_PROXY. |
| `ip_cidr_range` | String |  | The range of internal addresses that are owned by this subnetwork.
Provide this property when you create the subnetwork. For example,10.0.0.0/8 or 100.64.0.0/10. Ranges must
be unique and non-overlapping within a network. Only IPv4 is supported.
This field is set at resource creation time. The range can be any range
listed in theValid
ranges list. The range can be expanded after creation usingexpandIpCidrRange. |
| `purpose` | String |  |  |
| `ipv6_access_type` | String |  | The access type of IPv6 address this subnet holds. It's immutable and can
only be specified during creation or the first time the subnet is updated
into IPV4_IPV6 dual stack. |
| `system_reserved_internal_ipv6_ranges` | Vec<String> |  | Output only. [Output Only] The array of internal IPv6 network ranges reserved from
the subnetwork's internal IPv6 range for system use. |
| `system_reserved_external_ipv6_ranges` | Vec<String> |  | Output only. [Output Only] The array of external IPv6 network ranges reserved from
the subnetwork's external IPv6 range for system use. |
| `fingerprint` | String |  | Fingerprint of this resource. A hash of the contents stored in this object.
This field is used in optimistic locking. This field will be ignored when
inserting a Subnetwork. An up-to-date fingerprint must be
provided in order to update the Subnetwork, otherwise the
request will fail with error 412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve a Subnetwork. |
| `region` | String |  | URL of the region where the Subnetwork resides. This
field can be set only at resource creation time. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `name` | String |  | The name of the resource, provided by the client when initially creating
the resource. The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `private_ipv6_google_access` | String |  | This field is for internal use.

This field can be both set at resource creation time and updated usingpatch. |
| `reserved_internal_range` | String |  | The URL of the reserved internal range. |
| `internal_ipv6_prefix` | String |  | The internal IPv6 address range that is owned by this
subnetwork. |
| `network` | String |  | The URL of the network to which this subnetwork belongs, provided by the
client when initially creating the subnetwork. This field can be set only
at resource creation time. |
| `role` | String |  | The role of subnetwork. Currently, this field is only used when
purpose is set to GLOBAL_MANAGED_PROXY orREGIONAL_MANAGED_PROXY. The value can be set toACTIVE or BACKUP. An ACTIVE
subnetwork is one that is currently being used for Envoy-based load
balancers in a region. A BACKUP subnetwork is one that is
ready to be promoted to ACTIVE or is currently draining.
This field can be updated with a patch request. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. This field can be set only at resource creation time. |
| `utilization_details` | String |  | Output only. [Output Only] The current IP utilization of all subnetwork ranges. Contains
the total number of allocated and free IPs in each range. |
| `params` | String |  | Input only. [Input Only] Additional params passed with the request, but not persisted
as part of resource payload. |
| `private_ip_google_access` | bool |  | Whether the VMs in this subnet can access Google services without assigned
external IP addresses. This field can be both set at resource creation
time and updated using setPrivateIpGoogleAccess. |
| `secondary_ip_ranges` | Vec<String> |  | An array of configurations for secondary IP ranges for VM instances
contained in this subnetwork. The primary IP of such VM must belong to the
primary ipCidrRange of the subnetwork. The alias IPs may belong to either
primary or secondary ranges. This field can be updated with apatch request. |
| `gateway_address` | String |  | [Output Only] The gateway address for default routes to reach destination
addresses outside this subnetwork. |
| `ipv6_gce_endpoint` | String |  | [Output Only] Possible endpoints of this subnetwork. It can be one of the
following:
   
   - VM_ONLY: The subnetwork can be used for creating instances and
   IPv6 addresses with VM endpoint type. Such a subnetwork gets external IPv6
   ranges from a public delegated prefix and cannot be used to create NetLb.
   - VM_AND_FR: The subnetwork can be used for creating both VM
   instances and Forwarding Rules. It can also be used to reserve IPv6
   addresses with both VM and FR endpoint types. Such a subnetwork gets its
   IPv6 range from Google IP Pool directly. |
| `stack_type` | String |  | The stack type for the subnet. If set to IPV4_ONLY, new VMs
in the subnet are assigned IPv4 addresses only. If set toIPV4_IPV6, new VMs in the subnet can be assigned both IPv4 and
IPv6 addresses. If not specified, IPV4_ONLY is used.

This field can be both set at resource creation time and updated usingpatch. |
| `kind` | String |  | [Output Only] Type of the resource. Always compute#subnetwork
for Subnetwork resources. |
| `log_config` | String |  | This field denotes the VPC flow logging options for this subnetwork.
If logging is enabled, logs are exported to Cloud Logging. |
| `ipv6_cidr_range` | String |  | [Output Only] This field is for internal use. |
| `state` | String |  | [Output Only] The state of the subnetwork, which can be one of the
following values:READY: Subnetwork is created and ready to useDRAINING: only applicable to subnetworks that have the
purpose set to INTERNAL_HTTPS_LOAD_BALANCER and indicates that
connections to the load balancer are being drained. A subnetwork that is
draining cannot be used or modified until it reaches a status ofREADY |
| `external_ipv6_prefix` | String |  | The external IPv6 address range that is owned by this
subnetwork. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `project` | String | ✅ | Project ID for this request. |
| `region` | String | ✅ | Name of the region scoping this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `ip_collection` | String | Reference to the source of IP, like a PublicDelegatedPrefix
(PDP) for BYOIP. The PDP must be a sub-PDP in
EXTERNAL_IPV6_SUBNETWORK_CREATION or INTERNAL_IPV6_SUBNETWORK_CREATION
mode.

Use one of the following formats to specify a sub-PDP when creating a dual
stack or IPv6-only subnetwork with external access using BYOIP:
   
   - 
   Full resource URL, as inhttps://www.googleapis.com/compute/v1/projects/projectId/regions/region/publicDelegatedPrefixes/sub-pdp-name
   - 
   Partial URL, as in
     
      
          - projects/projectId/regions/region/publicDelegatedPrefixes/sub-pdp-name
          - regions/region/publicDelegatedPrefixes/sub-pdp-name |
| `enable_flow_logs` | bool | Whether to enable flow logging for this subnetwork. If this field is not
explicitly set, it will not appear in get listings. If not set
the default behavior is determined by the org policy, if there is no org
policy specified, then it will default to disabled. This field isn't
supported if the subnet purpose field is set toREGIONAL_MANAGED_PROXY. |
| `ip_cidr_range` | String | The range of internal addresses that are owned by this subnetwork.
Provide this property when you create the subnetwork. For example,10.0.0.0/8 or 100.64.0.0/10. Ranges must
be unique and non-overlapping within a network. Only IPv4 is supported.
This field is set at resource creation time. The range can be any range
listed in theValid
ranges list. The range can be expanded after creation usingexpandIpCidrRange. |
| `purpose` | String |  |
| `ipv6_access_type` | String | The access type of IPv6 address this subnet holds. It's immutable and can
only be specified during creation or the first time the subnet is updated
into IPV4_IPV6 dual stack. |
| `system_reserved_internal_ipv6_ranges` | Vec<String> | Output only. [Output Only] The array of internal IPv6 network ranges reserved from
the subnetwork's internal IPv6 range for system use. |
| `system_reserved_external_ipv6_ranges` | Vec<String> | Output only. [Output Only] The array of external IPv6 network ranges reserved from
the subnetwork's external IPv6 range for system use. |
| `fingerprint` | String | Fingerprint of this resource. A hash of the contents stored in this object.
This field is used in optimistic locking. This field will be ignored when
inserting a Subnetwork. An up-to-date fingerprint must be
provided in order to update the Subnetwork, otherwise the
request will fail with error 412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve a Subnetwork. |
| `region` | String | URL of the region where the Subnetwork resides. This
field can be set only at resource creation time. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `name` | String | The name of the resource, provided by the client when initially creating
the resource. The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `private_ipv6_google_access` | String | This field is for internal use.

This field can be both set at resource creation time and updated usingpatch. |
| `reserved_internal_range` | String | The URL of the reserved internal range. |
| `internal_ipv6_prefix` | String | The internal IPv6 address range that is owned by this
subnetwork. |
| `network` | String | The URL of the network to which this subnetwork belongs, provided by the
client when initially creating the subnetwork. This field can be set only
at resource creation time. |
| `role` | String | The role of subnetwork. Currently, this field is only used when
purpose is set to GLOBAL_MANAGED_PROXY orREGIONAL_MANAGED_PROXY. The value can be set toACTIVE or BACKUP. An ACTIVE
subnetwork is one that is currently being used for Envoy-based load
balancers in a region. A BACKUP subnetwork is one that is
ready to be promoted to ACTIVE or is currently draining.
This field can be updated with a patch request. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. This field can be set only at resource creation time. |
| `utilization_details` | String | Output only. [Output Only] The current IP utilization of all subnetwork ranges. Contains
the total number of allocated and free IPs in each range. |
| `params` | String | Input only. [Input Only] Additional params passed with the request, but not persisted
as part of resource payload. |
| `private_ip_google_access` | bool | Whether the VMs in this subnet can access Google services without assigned
external IP addresses. This field can be both set at resource creation
time and updated using setPrivateIpGoogleAccess. |
| `secondary_ip_ranges` | Vec<String> | An array of configurations for secondary IP ranges for VM instances
contained in this subnetwork. The primary IP of such VM must belong to the
primary ipCidrRange of the subnetwork. The alias IPs may belong to either
primary or secondary ranges. This field can be updated with apatch request. |
| `gateway_address` | String | [Output Only] The gateway address for default routes to reach destination
addresses outside this subnetwork. |
| `ipv6_gce_endpoint` | String | [Output Only] Possible endpoints of this subnetwork. It can be one of the
following:
   
   - VM_ONLY: The subnetwork can be used for creating instances and
   IPv6 addresses with VM endpoint type. Such a subnetwork gets external IPv6
   ranges from a public delegated prefix and cannot be used to create NetLb.
   - VM_AND_FR: The subnetwork can be used for creating both VM
   instances and Forwarding Rules. It can also be used to reserve IPv6
   addresses with both VM and FR endpoint types. Such a subnetwork gets its
   IPv6 range from Google IP Pool directly. |
| `stack_type` | String | The stack type for the subnet. If set to IPV4_ONLY, new VMs
in the subnet are assigned IPv4 addresses only. If set toIPV4_IPV6, new VMs in the subnet can be assigned both IPv4 and
IPv6 addresses. If not specified, IPV4_ONLY is used.

This field can be both set at resource creation time and updated usingpatch. |
| `kind` | String | [Output Only] Type of the resource. Always compute#subnetwork
for Subnetwork resources. |
| `log_config` | String | This field denotes the VPC flow logging options for this subnetwork.
If logging is enabled, logs are exported to Cloud Logging. |
| `ipv6_cidr_range` | String | [Output Only] This field is for internal use. |
| `state` | String | [Output Only] The state of the subnetwork, which can be one of the
following values:READY: Subnetwork is created and ready to useDRAINING: only applicable to subnetworks that have the
purpose set to INTERNAL_HTTPS_LOAD_BALANCER and indicates that
connections to the load balancer are being drained. A subnetwork that is
draining cannot be used or modified until it reaches a status ofREADY |
| `external_ipv6_prefix` | String | The external IPv6 address range that is owned by this
subnetwork. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create subnetwork
subnetwork = provider.compute_api.Subnetwork {
    project = "value"  # Project ID for this request.
    region = "value"  # Name of the region scoping this request.
}

# Access subnetwork outputs
subnetwork_id = subnetwork.id
subnetwork_ip_collection = subnetwork.ip_collection
subnetwork_enable_flow_logs = subnetwork.enable_flow_logs
subnetwork_ip_cidr_range = subnetwork.ip_cidr_range
subnetwork_purpose = subnetwork.purpose
subnetwork_ipv6_access_type = subnetwork.ipv6_access_type
subnetwork_system_reserved_internal_ipv6_ranges = subnetwork.system_reserved_internal_ipv6_ranges
subnetwork_system_reserved_external_ipv6_ranges = subnetwork.system_reserved_external_ipv6_ranges
subnetwork_fingerprint = subnetwork.fingerprint
subnetwork_region = subnetwork.region
subnetwork_creation_timestamp = subnetwork.creation_timestamp
subnetwork_id = subnetwork.id
subnetwork_name = subnetwork.name
subnetwork_private_ipv6_google_access = subnetwork.private_ipv6_google_access
subnetwork_reserved_internal_range = subnetwork.reserved_internal_range
subnetwork_internal_ipv6_prefix = subnetwork.internal_ipv6_prefix
subnetwork_network = subnetwork.network
subnetwork_role = subnetwork.role
subnetwork_description = subnetwork.description
subnetwork_utilization_details = subnetwork.utilization_details
subnetwork_params = subnetwork.params
subnetwork_private_ip_google_access = subnetwork.private_ip_google_access
subnetwork_secondary_ip_ranges = subnetwork.secondary_ip_ranges
subnetwork_gateway_address = subnetwork.gateway_address
subnetwork_ipv6_gce_endpoint = subnetwork.ipv6_gce_endpoint
subnetwork_stack_type = subnetwork.stack_type
subnetwork_kind = subnetwork.kind
subnetwork_log_config = subnetwork.log_config
subnetwork_ipv6_cidr_range = subnetwork.ipv6_cidr_range
subnetwork_state = subnetwork.state
subnetwork_external_ipv6_prefix = subnetwork.external_ipv6_prefix
subnetwork_self_link = subnetwork.self_link
```

---


### Interconnect

Creates an Interconnect in the specified project using
the data included in the request.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `google_reference_id` | String |  | [Output Only] Google reference ID to be used when raising support tickets
with Google or otherwise to debug backend connectivity issues. |
| `params` | String |  | Input only. [Input Only] Additional params passed with the request, but not persisted
as part of resource payload. |
| `circuit_infos` | Vec<String> |  | [Output Only] A list of CircuitInfo objects, that describe the individual
circuits in this LAG. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `aai_enabled` | bool |  | Enable or disable the application awareness feature on this Cloud
Interconnect. |
| `google_ip_address` | String |  | [Output Only] IP address configured on the Google side of the Interconnect
link. This can be used only for ping tests. |
| `peer_ip_address` | String |  | [Output Only] IP address configured on the customer side of the
Interconnect link. The customer should configure this IP address during
turnup when prompted by Google NOC. This can be used only for ping tests. |
| `operational_status` | String |  | [Output Only] The current status of this Interconnect's functionality,
which can take one of the following values:
   
   - OS_ACTIVE: A valid Interconnect, which is turned up and is ready to
   use. Attachments may be provisioned on this Interconnect.

- OS_UNPROVISIONED: An Interconnect that has not completed turnup. No
attachments may be provisioned on this Interconnect.
- OS_UNDER_MAINTENANCE: An Interconnect that is undergoing internal
maintenance. No attachments may be provisioned or updated on this
Interconnect. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `customer_name` | String |  | Customer name, to put in the Letter of Authorization as the party
authorized to request a crossconnect. |
| `macsec` | String |  | Configuration that enables Media Access Control security (MACsec) on the
Cloud Interconnect connection between Google and your on-premises router. |
| `application_aware_interconnect` | String |  | Configuration information for application awareness on this Cloud
Interconnect. |
| `state` | String |  | [Output Only] The current state of Interconnect functionality, which can
take one of the following values:
   
   - ACTIVE: The Interconnect is valid, turned up and ready to use.
   Attachments may be provisioned on this Interconnect.
   - UNPROVISIONED: The Interconnect has not completed turnup. No
   attachments may be provisioned on this Interconnect.
   - UNDER_MAINTENANCE: The Interconnect is undergoing internal maintenance.
   No attachments may be provisioned or updated on this
   Interconnect. |
| `name` | String |  | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must be
a dash, lowercase letter, or digit, except the last character, which cannot
be a dash. |
| `kind` | String |  | [Output Only] Type of the resource. Alwayscompute#interconnect for interconnects. |
| `wire_groups` | Vec<String> |  | [Output Only] A list of the URLs of all CrossSiteNetwork WireGroups
configured to use this Interconnect. The Interconnect cannot be deleted if
this list is non-empty. |
| `macsec_enabled` | bool |  | Enable or disable MACsec on this Interconnect connection. MACsec enablement
fails if the MACsec object is not specified. |
| `requested_features` | Vec<String> |  | Optional. This parameter can be provided only with Interconnect INSERT. It
isn't valid for Interconnect PATCH. List of features requested for this
Interconnect connection, which can take one of the following values:
   
   - IF_MACSEC: If specified, then the connection is created on MACsec
   capable hardware ports. If not specified, non-MACsec capable ports will
   also be considered.
   - IF_CROSS_SITE_NETWORK: If specified, then the connection is created
   exclusively for Cross-Site Networking. The connection can not be used for
   Cross-Site Networking unless this feature is specified. |
| `requested_link_count` | i64 |  | Target number of physical links in the link bundle, as requested by the
customer. |
| `interconnect_type` | String |  | Type of interconnect, which can take one of the following values:
   
   - PARTNER: A partner-managed interconnection shared between customers
   though a partner.
   - DEDICATED: A dedicated physical interconnection with the
   customer.


Note that a value IT_PRIVATE has been deprecated in favor of DEDICATED. |
| `remote_location` | String |  | Indicates that this is a Cross-Cloud Interconnect. This field specifies the
location outside of Google's network that the interconnect is connected to. |
| `expected_outages` | Vec<String> |  | [Output Only] A list of outages expected for this Interconnect. |
| `label_fingerprint` | String |  | A fingerprint for the labels being applied to this Interconnect, which
is essentially a hash of the labels set used for optimistic locking. The
fingerprint is initially generated by Compute Engine and changes after
every request to modify or update labels. You must always provide an
up-to-date fingerprint hash in order to update or change labels,
otherwise the request will fail with error412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve an Interconnect. |
| `link_type` | String |  | Type of link requested, which can take one of the following values:
   
   - LINK_TYPE_ETHERNET_10G_LR: A 10G Ethernet with LR optics
   - LINK_TYPE_ETHERNET_100G_LR: A 100G Ethernet with LR optics.
   - LINK_TYPE_ETHERNET_400G_LR4: A 400G Ethernet with LR4 optics.


 Note that this field indicates the speed of each of
the links in the bundle, not the speed of the entire bundle. |
| `admin_enabled` | bool |  | Administrative status of the interconnect. When this is set to true, the
Interconnect is functional and can carry traffic.
When set to false, no packets can be carried over the interconnect and
no BGP routes are exchanged over it. By default, the status is set to true. |
| `satisfies_pzs` | bool |  | [Output Only] Reserved for future use. |
| `interconnect_attachments` | Vec<String> |  | [Output Only] A list of the URLs of all InterconnectAttachments configured
to use  this Interconnect. |
| `interconnect_groups` | Vec<String> |  | [Output Only] URLs of InterconnectGroups that include this Interconnect.
Order is arbitrary and items are unique. |
| `provisioned_link_count` | i64 |  | [Output Only] Number of links actually provisioned in this interconnect. |
| `available_features` | Vec<String> |  | [Output only] List of features available for this Interconnect connection,
which can take one of the following values:
   
   - IF_MACSEC: If present, then the Interconnect connection is
   provisioned on MACsec capable hardware ports. If not present, then the
   Interconnect connection is provisioned on non-MACsec capable ports. Any
   attempt to enable MACsec will fail.
   - IF_CROSS_SITE_NETWORK: If present, then the Interconnect connection is
   provisioned exclusively for Cross-Site Networking. Any attempt to configure
   VLAN attachments will fail. If not present, then the Interconnect
   connection is not provisioned for Cross-Site Networking. Any attempt to use
   it for Cross-Site Networking will fail. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `location` | String |  | URL of the InterconnectLocation object that represents where this
connection is to be provisioned. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `subzone` | String |  | Specific subzone in the InterconnectLocation that represents where
this connection is to be provisioned. |
| `labels` | HashMap<String, String> |  | Labels for this resource. These can only be added or modified by thesetLabels method. Each label key/value pair must comply withRFC1035.
Label values may be empty. |
| `noc_contact_email` | String |  | Email address to contact the customer NOC for operations and maintenance
notifications regarding this Interconnect. If specified, this will be used
for notifications in addition to all other forms described, such as
Cloud Monitoring logs alerting and Cloud Notifications. This field is
required for users who sign up for Cloud Interconnect using
workforce identity federation. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `google_reference_id` | String | [Output Only] Google reference ID to be used when raising support tickets
with Google or otherwise to debug backend connectivity issues. |
| `params` | String | Input only. [Input Only] Additional params passed with the request, but not persisted
as part of resource payload. |
| `circuit_infos` | Vec<String> | [Output Only] A list of CircuitInfo objects, that describe the individual
circuits in this LAG. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `aai_enabled` | bool | Enable or disable the application awareness feature on this Cloud
Interconnect. |
| `google_ip_address` | String | [Output Only] IP address configured on the Google side of the Interconnect
link. This can be used only for ping tests. |
| `peer_ip_address` | String | [Output Only] IP address configured on the customer side of the
Interconnect link. The customer should configure this IP address during
turnup when prompted by Google NOC. This can be used only for ping tests. |
| `operational_status` | String | [Output Only] The current status of this Interconnect's functionality,
which can take one of the following values:
   
   - OS_ACTIVE: A valid Interconnect, which is turned up and is ready to
   use. Attachments may be provisioned on this Interconnect.

- OS_UNPROVISIONED: An Interconnect that has not completed turnup. No
attachments may be provisioned on this Interconnect.
- OS_UNDER_MAINTENANCE: An Interconnect that is undergoing internal
maintenance. No attachments may be provisioned or updated on this
Interconnect. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `customer_name` | String | Customer name, to put in the Letter of Authorization as the party
authorized to request a crossconnect. |
| `macsec` | String | Configuration that enables Media Access Control security (MACsec) on the
Cloud Interconnect connection between Google and your on-premises router. |
| `application_aware_interconnect` | String | Configuration information for application awareness on this Cloud
Interconnect. |
| `state` | String | [Output Only] The current state of Interconnect functionality, which can
take one of the following values:
   
   - ACTIVE: The Interconnect is valid, turned up and ready to use.
   Attachments may be provisioned on this Interconnect.
   - UNPROVISIONED: The Interconnect has not completed turnup. No
   attachments may be provisioned on this Interconnect.
   - UNDER_MAINTENANCE: The Interconnect is undergoing internal maintenance.
   No attachments may be provisioned or updated on this
   Interconnect. |
| `name` | String | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must be
a dash, lowercase letter, or digit, except the last character, which cannot
be a dash. |
| `kind` | String | [Output Only] Type of the resource. Alwayscompute#interconnect for interconnects. |
| `wire_groups` | Vec<String> | [Output Only] A list of the URLs of all CrossSiteNetwork WireGroups
configured to use this Interconnect. The Interconnect cannot be deleted if
this list is non-empty. |
| `macsec_enabled` | bool | Enable or disable MACsec on this Interconnect connection. MACsec enablement
fails if the MACsec object is not specified. |
| `requested_features` | Vec<String> | Optional. This parameter can be provided only with Interconnect INSERT. It
isn't valid for Interconnect PATCH. List of features requested for this
Interconnect connection, which can take one of the following values:
   
   - IF_MACSEC: If specified, then the connection is created on MACsec
   capable hardware ports. If not specified, non-MACsec capable ports will
   also be considered.
   - IF_CROSS_SITE_NETWORK: If specified, then the connection is created
   exclusively for Cross-Site Networking. The connection can not be used for
   Cross-Site Networking unless this feature is specified. |
| `requested_link_count` | i64 | Target number of physical links in the link bundle, as requested by the
customer. |
| `interconnect_type` | String | Type of interconnect, which can take one of the following values:
   
   - PARTNER: A partner-managed interconnection shared between customers
   though a partner.
   - DEDICATED: A dedicated physical interconnection with the
   customer.


Note that a value IT_PRIVATE has been deprecated in favor of DEDICATED. |
| `remote_location` | String | Indicates that this is a Cross-Cloud Interconnect. This field specifies the
location outside of Google's network that the interconnect is connected to. |
| `expected_outages` | Vec<String> | [Output Only] A list of outages expected for this Interconnect. |
| `label_fingerprint` | String | A fingerprint for the labels being applied to this Interconnect, which
is essentially a hash of the labels set used for optimistic locking. The
fingerprint is initially generated by Compute Engine and changes after
every request to modify or update labels. You must always provide an
up-to-date fingerprint hash in order to update or change labels,
otherwise the request will fail with error412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve an Interconnect. |
| `link_type` | String | Type of link requested, which can take one of the following values:
   
   - LINK_TYPE_ETHERNET_10G_LR: A 10G Ethernet with LR optics
   - LINK_TYPE_ETHERNET_100G_LR: A 100G Ethernet with LR optics.
   - LINK_TYPE_ETHERNET_400G_LR4: A 400G Ethernet with LR4 optics.


 Note that this field indicates the speed of each of
the links in the bundle, not the speed of the entire bundle. |
| `admin_enabled` | bool | Administrative status of the interconnect. When this is set to true, the
Interconnect is functional and can carry traffic.
When set to false, no packets can be carried over the interconnect and
no BGP routes are exchanged over it. By default, the status is set to true. |
| `satisfies_pzs` | bool | [Output Only] Reserved for future use. |
| `interconnect_attachments` | Vec<String> | [Output Only] A list of the URLs of all InterconnectAttachments configured
to use  this Interconnect. |
| `interconnect_groups` | Vec<String> | [Output Only] URLs of InterconnectGroups that include this Interconnect.
Order is arbitrary and items are unique. |
| `provisioned_link_count` | i64 | [Output Only] Number of links actually provisioned in this interconnect. |
| `available_features` | Vec<String> | [Output only] List of features available for this Interconnect connection,
which can take one of the following values:
   
   - IF_MACSEC: If present, then the Interconnect connection is
   provisioned on MACsec capable hardware ports. If not present, then the
   Interconnect connection is provisioned on non-MACsec capable ports. Any
   attempt to enable MACsec will fail.
   - IF_CROSS_SITE_NETWORK: If present, then the Interconnect connection is
   provisioned exclusively for Cross-Site Networking. Any attempt to configure
   VLAN attachments will fail. If not present, then the Interconnect
   connection is not provisioned for Cross-Site Networking. Any attempt to use
   it for Cross-Site Networking will fail. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `location` | String | URL of the InterconnectLocation object that represents where this
connection is to be provisioned. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `subzone` | String | Specific subzone in the InterconnectLocation that represents where
this connection is to be provisioned. |
| `labels` | HashMap<String, String> | Labels for this resource. These can only be added or modified by thesetLabels method. Each label key/value pair must comply withRFC1035.
Label values may be empty. |
| `noc_contact_email` | String | Email address to contact the customer NOC for operations and maintenance
notifications regarding this Interconnect. If specified, this will be used
for notifications in addition to all other forms described, such as
Cloud Monitoring logs alerting and Cloud Notifications. This field is
required for users who sign up for Cloud Interconnect using
workforce identity federation. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create interconnect
interconnect = provider.compute_api.Interconnect {
    project = "value"  # Project ID for this request.
}

# Access interconnect outputs
interconnect_id = interconnect.id
interconnect_google_reference_id = interconnect.google_reference_id
interconnect_params = interconnect.params
interconnect_circuit_infos = interconnect.circuit_infos
interconnect_id = interconnect.id
interconnect_aai_enabled = interconnect.aai_enabled
interconnect_google_ip_address = interconnect.google_ip_address
interconnect_peer_ip_address = interconnect.peer_ip_address
interconnect_operational_status = interconnect.operational_status
interconnect_creation_timestamp = interconnect.creation_timestamp
interconnect_customer_name = interconnect.customer_name
interconnect_macsec = interconnect.macsec
interconnect_application_aware_interconnect = interconnect.application_aware_interconnect
interconnect_state = interconnect.state
interconnect_name = interconnect.name
interconnect_kind = interconnect.kind
interconnect_wire_groups = interconnect.wire_groups
interconnect_macsec_enabled = interconnect.macsec_enabled
interconnect_requested_features = interconnect.requested_features
interconnect_requested_link_count = interconnect.requested_link_count
interconnect_interconnect_type = interconnect.interconnect_type
interconnect_remote_location = interconnect.remote_location
interconnect_expected_outages = interconnect.expected_outages
interconnect_label_fingerprint = interconnect.label_fingerprint
interconnect_link_type = interconnect.link_type
interconnect_admin_enabled = interconnect.admin_enabled
interconnect_satisfies_pzs = interconnect.satisfies_pzs
interconnect_interconnect_attachments = interconnect.interconnect_attachments
interconnect_interconnect_groups = interconnect.interconnect_groups
interconnect_provisioned_link_count = interconnect.provisioned_link_count
interconnect_available_features = interconnect.available_features
interconnect_description = interconnect.description
interconnect_location = interconnect.location
interconnect_self_link = interconnect.self_link
interconnect_subzone = interconnect.subzone
interconnect_labels = interconnect.labels
interconnect_noc_contact_email = interconnect.noc_contact_email
```

---


### Interconnect_attachment_group

Creates a InterconnectAttachmentGroup in the specified project in the given
scope using the parameters that are included in the request.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `logical_structure` | String |  |  |
| `name` | String |  | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `attachments` | HashMap<String, String> |  | Attachments in the AttachmentGroup. Keys are arbitrary user-specified
strings. Users are encouraged, but not required, to use their preferred
format for resource links as keys.
Note that there are add-members and remove-members methods in gcloud.
The size of this map is limited by an "Attachments per group" quota. |
| `intent` | String |  |  |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `id` | String |  | [Output Only] The unique identifier for the resource type. The server
generates this identifier. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `etag` | String |  | Opaque system-generated token that uniquely identifies the configuration.
If provided when patching a configuration in update mode, the provided
token must match the current token or the update is rejected. This provides
a reliable means of doing read-modify-write (optimistic locking) as
described byAIP 154. |
| `interconnect_group` | String |  | The URL of an InterconnectGroup that groups these Attachments'
Interconnects. Customers do not need to set this unless directed by
Google Support. |
| `configured` | String |  |  |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `kind` | String |  | [Output Only] Type of the resource. Always
compute#interconnectAttachmentGroup. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `logical_structure` | String |  |
| `name` | String | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `attachments` | HashMap<String, String> | Attachments in the AttachmentGroup. Keys are arbitrary user-specified
strings. Users are encouraged, but not required, to use their preferred
format for resource links as keys.
Note that there are add-members and remove-members methods in gcloud.
The size of this map is limited by an "Attachments per group" quota. |
| `intent` | String |  |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `id` | String | [Output Only] The unique identifier for the resource type. The server
generates this identifier. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `etag` | String | Opaque system-generated token that uniquely identifies the configuration.
If provided when patching a configuration in update mode, the provided
token must match the current token or the update is rejected. This provides
a reliable means of doing read-modify-write (optimistic locking) as
described byAIP 154. |
| `interconnect_group` | String | The URL of an InterconnectGroup that groups these Attachments'
Interconnects. Customers do not need to set this unless directed by
Google Support. |
| `configured` | String |  |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `kind` | String | [Output Only] Type of the resource. Always
compute#interconnectAttachmentGroup. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create interconnect_attachment_group
interconnect_attachment_group = provider.compute_api.Interconnect_attachment_group {
    project = "value"  # Project ID for this request.
}

# Access interconnect_attachment_group outputs
interconnect_attachment_group_id = interconnect_attachment_group.id
interconnect_attachment_group_logical_structure = interconnect_attachment_group.logical_structure
interconnect_attachment_group_name = interconnect_attachment_group.name
interconnect_attachment_group_attachments = interconnect_attachment_group.attachments
interconnect_attachment_group_intent = interconnect_attachment_group.intent
interconnect_attachment_group_self_link = interconnect_attachment_group.self_link
interconnect_attachment_group_id = interconnect_attachment_group.id
interconnect_attachment_group_creation_timestamp = interconnect_attachment_group.creation_timestamp
interconnect_attachment_group_etag = interconnect_attachment_group.etag
interconnect_attachment_group_interconnect_group = interconnect_attachment_group.interconnect_group
interconnect_attachment_group_configured = interconnect_attachment_group.configured
interconnect_attachment_group_description = interconnect_attachment_group.description
interconnect_attachment_group_kind = interconnect_attachment_group.kind
```

---


### Target_vpn_gateway

Creates a target VPN gateway in the specified project and region using
the data included in the request.

**Operations**: ✅ Create ✅ Read ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `status` | String |  | [Output Only] The status of the VPN gateway,
which can be one of the following: CREATING, READY, FAILED, or DELETING. |
| `tunnels` | Vec<String> |  | [Output Only] A list of URLs to VpnTunnel resources. VpnTunnels are
created using the compute.vpntunnels.insert method and
associated with a VPN gateway. |
| `name` | String |  | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `labels` | HashMap<String, String> |  | Labels for this resource. These can only be added or modified by thesetLabels method. Each label key/value pair must comply withRFC1035.
Label values may be empty. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `kind` | String |  | [Output Only] Type of resource. Alwayscompute#targetVpnGateway for target VPN gateways. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `region` | String |  | [Output Only] URL of the region where the target VPN gateway resides.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `label_fingerprint` | String |  | A fingerprint for the labels being applied to this TargetVpnGateway, which
is essentially a hash of the labels set used for optimistic locking. The
fingerprint is initially generated by Compute Engine and changes after
every request to modify or update labels. You must always provide an
up-to-date fingerprint hash in order to update or change labels,
otherwise the request will fail with error412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve a TargetVpnGateway. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `forwarding_rules` | Vec<String> |  | [Output Only] A list of URLs to the ForwardingRule resources.
ForwardingRules are created usingcompute.forwardingRules.insert and associated with a VPN
gateway. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `network` | String |  | URL of the network to which this VPN gateway is attached. Provided by the
client when the VPN gateway is created. |
| `region` | String | ✅ | Name of the region for this request. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `status` | String | [Output Only] The status of the VPN gateway,
which can be one of the following: CREATING, READY, FAILED, or DELETING. |
| `tunnels` | Vec<String> | [Output Only] A list of URLs to VpnTunnel resources. VpnTunnels are
created using the compute.vpntunnels.insert method and
associated with a VPN gateway. |
| `name` | String | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `labels` | HashMap<String, String> | Labels for this resource. These can only be added or modified by thesetLabels method. Each label key/value pair must comply withRFC1035.
Label values may be empty. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `kind` | String | [Output Only] Type of resource. Alwayscompute#targetVpnGateway for target VPN gateways. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `region` | String | [Output Only] URL of the region where the target VPN gateway resides.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `label_fingerprint` | String | A fingerprint for the labels being applied to this TargetVpnGateway, which
is essentially a hash of the labels set used for optimistic locking. The
fingerprint is initially generated by Compute Engine and changes after
every request to modify or update labels. You must always provide an
up-to-date fingerprint hash in order to update or change labels,
otherwise the request will fail with error412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve a TargetVpnGateway. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `forwarding_rules` | Vec<String> | [Output Only] A list of URLs to the ForwardingRule resources.
ForwardingRules are created usingcompute.forwardingRules.insert and associated with a VPN
gateway. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `network` | String | URL of the network to which this VPN gateway is attached. Provided by the
client when the VPN gateway is created. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create target_vpn_gateway
target_vpn_gateway = provider.compute_api.Target_vpn_gateway {
    region = "value"  # Name of the region for this request.
    project = "value"  # Project ID for this request.
}

# Access target_vpn_gateway outputs
target_vpn_gateway_id = target_vpn_gateway.id
target_vpn_gateway_status = target_vpn_gateway.status
target_vpn_gateway_tunnels = target_vpn_gateway.tunnels
target_vpn_gateway_name = target_vpn_gateway.name
target_vpn_gateway_labels = target_vpn_gateway.labels
target_vpn_gateway_description = target_vpn_gateway.description
target_vpn_gateway_kind = target_vpn_gateway.kind
target_vpn_gateway_id = target_vpn_gateway.id
target_vpn_gateway_region = target_vpn_gateway.region
target_vpn_gateway_label_fingerprint = target_vpn_gateway.label_fingerprint
target_vpn_gateway_self_link = target_vpn_gateway.self_link
target_vpn_gateway_forwarding_rules = target_vpn_gateway.forwarding_rules
target_vpn_gateway_creation_timestamp = target_vpn_gateway.creation_timestamp
target_vpn_gateway_network = target_vpn_gateway.network
```

---


### Forwarding_rule

Creates a ForwardingRule resource in the specified project and region using
the data included in the request.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `ports` | Vec<String> |  | The ports, portRange, and allPorts
fields are mutually exclusive. Only packets addressed to ports in the
specified range will be forwarded to the backends configured with this
forwarding rule.

The ports field has the following limitations:
   
   - It requires that the forwarding rule IPProtocol be TCP,
   UDP, or SCTP, and
   - It's applicable only to the following products: internal passthrough
   Network Load Balancers, backend service-based external passthrough Network
   Load Balancers, and internal protocol forwarding.
   - You can specify a list of up to five ports by number, separated by
   commas. The ports can be contiguous or discontiguous.



For external forwarding rules, two or more forwarding rules cannot use the
same [IPAddress, IPProtocol] pair if they share at least one
port number.

For internal forwarding rules within the same VPC network, two or more
forwarding rules cannot use the same [IPAddress, IPProtocol]
pair if they share at least one port number.

@pattern: \\d+(?:-\\d+)? |
| `base_forwarding_rule` | String |  | [Output Only] The URL for the corresponding base forwarding rule. By base
forwarding rule, we mean the forwarding rule that has the same IP address,
protocol, and port settings with the current forwarding rule, but without
sourceIPRanges specified.
Always empty if the current forwarding rule does not have sourceIPRanges
specified. |
| `load_balancing_scheme` | String |  | Specifies the forwarding rule type.

For more information about forwarding rules, refer to
Forwarding rule concepts. |
| `self_link_with_id` | String |  | [Output Only] Server-defined URL for this resource with the resource id. |
| `region` | String |  | [Output Only] URL of the region where the regional forwarding rule resides.
This field is not applicable to global forwarding rules.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `network_tier` | String |  | This signifies the networking tier used for configuring
this load balancer and can only take the following values:PREMIUM, STANDARD.

For regional ForwardingRule, the valid values are PREMIUM andSTANDARD. For GlobalForwardingRule, the valid value isPREMIUM.

If this field is not specified, it is assumed to be PREMIUM.
If IPAddress is specified, this value must be equal to the
networkTier of the Address. |
| `psc_connection_id` | String |  | [Output Only] The PSC connection id of the PSC forwarding rule. |
| `source_ip_ranges` | Vec<String> |  | If not empty, this forwarding rule will only forward the traffic when the
source IP address matches one of the IP addresses or CIDR ranges set here.
Note that a forwarding rule can only have up to 64 source IP ranges, and
this field can only be used with a regional forwarding rule whose scheme isEXTERNAL.
Each source_ip_range entry should be either an IP address (for
example, 1.2.3.4) or a CIDR range (for example, 1.2.3.0/24). |
| `port_range` | String |  | The ports, portRange, and allPorts
fields are mutually exclusive. Only packets addressed to ports in the
specified range will be forwarded to the backends configured with this
forwarding rule.

The portRange field has the following limitations:
   
   - It requires that the forwarding rule IPProtocol be TCP,
   UDP, or SCTP, and
   - It's applicable only to the following products: external passthrough
   Network Load Balancers, internal and external proxy Network Load Balancers,
   internal and external Application Load Balancers, external protocol
   forwarding, and Classic VPN.
   - Some products have restrictions on what ports can be used. See 
   port specifications for details.



For external forwarding rules, two or more forwarding rules cannot use the
same [IPAddress, IPProtocol] pair, and cannot have overlappingportRanges.

For internal forwarding rules within the same VPC network, two or more
forwarding rules cannot use the same [IPAddress, IPProtocol]
pair, and cannot have overlapping portRanges.

@pattern: \\d+(?:-\\d+)? |
| `name` | String |  | Name of the resource; provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash.

For Private Service Connect forwarding rules that forward traffic to Google
APIs, the forwarding rule name must be a 1-20 characters string with
lowercase letters and numbers and must start with a letter. |
| `backend_service` | String |  | Identifies the backend service to which the forwarding rule sends traffic.
Required for internal and external passthrough Network Load Balancers;
must be omitted for all other load balancer types. |
| `all_ports` | bool |  | The ports, portRange, and allPorts
fields are mutually exclusive. Only packets addressed to ports in the
specified range will be forwarded to the backends configured with this
forwarding rule.

The allPorts field has the following limitations:
   
   - It requires that the forwarding rule IPProtocol be TCP,
   UDP, SCTP, or L3_DEFAULT.
   - It's applicable only to the following products: internal passthrough
   Network Load Balancers, backend service-based external passthrough Network
   Load Balancers, and internal and external protocol forwarding.
   - Set this field to true to allow packets addressed to any port or
   packets lacking destination port information (for example, UDP fragments
   after the first fragment) to be forwarded to the backends configured with
   this forwarding rule. The L3_DEFAULT protocol requiresallPorts be set to true. |
| `fingerprint` | String |  | Fingerprint of this resource. A hash of the contents stored in this object.
This field is used in optimistic locking. This field will be ignored when
inserting a ForwardingRule. Include the fingerprint in patch request to
ensure that you do not overwrite changes that were applied from another
concurrent request.

To see the latest fingerprint, make a get() request to
retrieve a ForwardingRule. |
| `external_managed_backend_bucket_migration_state` | String |  | Specifies the canary migration state for the backend buckets attached to
this forwarding rule. Possible values are PREPARE, TEST_BY_PERCENTAGE, and
TEST_ALL_TRAFFIC.

To begin the migration from EXTERNAL to EXTERNAL_MANAGED, the state must be
changed to PREPARE. The state must be changed to TEST_ALL_TRAFFIC before
the loadBalancingScheme can be changed to EXTERNAL_MANAGED. Optionally, the
TEST_BY_PERCENTAGE state can be used to migrate traffic to backend buckets
attached to this forwarding rule by percentage using
externalManagedBackendBucketMigrationTestingPercentage.

Rolling back a migration requires the states to be set in reverse order. So
changing the scheme from EXTERNAL_MANAGED to EXTERNAL requires the state to
be set to TEST_ALL_TRAFFIC at the same time. Optionally, the
TEST_BY_PERCENTAGE state can be used to migrate some traffic back to
EXTERNAL or PREPARE can be used to migrate all traffic back to EXTERNAL. |
| `service_directory_registrations` | Vec<String> |  | Service Directory resources to register this forwarding rule with.
Currently, only supports a single Service Directory resource. |
| `service_label` | String |  | An optional prefix to the service name for this forwarding rule.
If specified, the prefix is the first label of the fully qualified service
name.

The label must be 1-63 characters long, and comply withRFC1035.
Specifically, the label must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash.

This field is only used for internal load balancing. |
| `ip_address` | String |  | IP address for which this forwarding rule accepts traffic. When a client
sends traffic to this IP address, the forwarding rule directs the traffic
to the referenced target or backendService.
While creating a forwarding rule, specifying an IPAddress is
required under the following circumstances:

   
   - When the target is set to targetGrpcProxy andvalidateForProxyless is set to true, theIPAddress should be set to 0.0.0.0.
   - When the target is a Private Service Connect Google APIs
   bundle, you must specify an IPAddress.


Otherwise, you can optionally specify an IP address that references an
existing static (reserved) IP address resource. When omitted, Google Cloud
assigns an ephemeral IP address.

Use one of the following formats to specify an IP address while creating a
forwarding rule:

* IP address number, as in `100.1.2.3`
* IPv6 address range, as in `2600:1234::/96`
* Full resource URL, as inhttps://www.googleapis.com/compute/v1/projects/project_id/regions/region/addresses/address-name
* Partial URL or by name, as in:
   
   - projects/project_id/regions/region/addresses/address-name
   - regions/region/addresses/address-name
   - global/addresses/address-name
   - address-name



The forwarding rule's target or backendService,
and in most cases, also the loadBalancingScheme, determine the
type of IP address that you can use. For detailed information, see
[IP address
specifications](https://cloud.google.com/load-balancing/docs/forwarding-rule-concepts#ip_address_specifications).

When reading an IPAddress, the API always returns the IP
address number. |
| `network` | String |  | This field is not used for global external load balancing.

For internal passthrough Network Load Balancers, this field identifies the
network that the load balanced IP should belong to for this forwarding
rule.
If the subnetwork is specified, the network of the subnetwork will be used.
If neither subnetwork nor this field is specified, the default network will
be used.

For Private Service Connect forwarding rules that forward traffic to Google
APIs, a network must be provided. |
| `kind` | String |  | [Output Only] Type of the resource. Alwayscompute#forwardingRule for forwarding rule resources. |
| `allow_psc_global_access` | bool |  | This is used in PSC consumer ForwardingRule to control whether the PSC
endpoint can be accessed from another region. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `ip_collection` | String |  | Resource reference of a PublicDelegatedPrefix. The PDP must
be a sub-PDP in EXTERNAL_IPV6_FORWARDING_RULE_CREATION mode.

Use one of the following formats to specify a sub-PDP when creating an IPv6
NetLB forwarding rule using BYOIP:
Full resource URL, as inhttps://www.googleapis.com/compute/v1/projects/project_id/regions/region/publicDelegatedPrefixes/sub-pdp-name
Partial URL, as in:
   
   - projects/project_id/regions/region/publicDelegatedPrefixes/sub-pdp-name
   - regions/region/publicDelegatedPrefixes/sub-pdp-name |
| `target` | String |  | The URL of the target resource to receive the matched traffic.  For
regional forwarding rules, this target must be in the same region as the
forwarding rule. For global forwarding rules, this target must be a global
load balancing resource.

The forwarded traffic must be of a type appropriate to the target object.
   
   
     -  For load balancers, see the "Target" column in [Port specifications](https://cloud.google.com/load-balancing/docs/forwarding-rule-concepts#ip_address_specifications).
     -  For Private Service Connect forwarding rules that forward traffic to Google APIs, provide the name of a supported Google API bundle:
       
      
            -  vpc-sc -  APIs that support VPC Service Controls. 
            -  all-apis - All supported Google APIs. 
          
     
     -  For Private Service Connect forwarding rules that forward traffic to managed services, the target must be a service attachment. The target is not mutable once set as a service attachment. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `ip_version` | String |  | The IP Version that will be used by this forwarding rule.  Valid options
are IPV4 or IPV6. |
| `external_managed_backend_bucket_migration_testing_percentage` | f64 |  | Determines the fraction of requests to backend buckets that should be
processed by the global external Application Load Balancer.

The value of this field must be in the range [0, 100].

This value can only be set if the loadBalancingScheme in the BackendService
is set to EXTERNAL (when using the classic Application Load Balancer) and
the migration state is TEST_BY_PERCENTAGE. |
| `label_fingerprint` | String |  | A fingerprint for the labels being applied to this resource, which is
essentially a hash of the labels set used for optimistic locking. The
fingerprint is initially generated by Compute Engine and changes after
every request to modify or update labels. You must always provide an
up-to-date fingerprint hash in order to update or change labels,
otherwise the request will fail with error412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve a ForwardingRule. |
| `is_mirroring_collector` | bool |  | Indicates whether or not this load balancer can be used as a collector for
packet mirroring. To prevent mirroring loops, instances behind this
load balancer will not have their traffic mirrored even if aPacketMirroring rule applies to them.
This can only be set to true for load balancers that have theirloadBalancingScheme set to INTERNAL. |
| `allow_global_access` | bool |  | If set to true, clients can access the internal passthrough Network Load
Balancers, the regional internal Application Load Balancer, and the
regional internal proxy Network Load Balancer from all regions.
If false, only allows access from the local region the load balancer is
located at. Note that for INTERNAL_MANAGED forwarding rules, this field
cannot be changed after the forwarding rule is created. |
| `labels` | HashMap<String, String> |  | Labels for this resource. These can only be added or modified by thesetLabels method. Each label key/value pair must comply withRFC1035.
Label values may be empty. |
| `no_automate_dns_zone` | bool |  | This is used in PSC consumer ForwardingRule to control whether it should
try to auto-generate a DNS zone or not. Non-PSC forwarding rules do not use
this field. Once set, this field is not mutable. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `ip_protocol` | String |  | The IP protocol to which this rule applies.

For protocol forwarding, valid
options are TCP, UDP, ESP,AH, SCTP, ICMP andL3_DEFAULT.

The valid IP protocols are different for different load balancing products
as described in [Load balancing
features](https://cloud.google.com/load-balancing/docs/features#protocols_from_the_load_balancer_to_the_backends). |
| `service_name` | String |  | [Output Only]
The internal fully qualified service name for this forwarding rule.

This field is only used for internal load balancing. |
| `psc_connection_status` | String |  |  |
| `subnetwork` | String |  | This field identifies the subnetwork that the load balanced IP should
belong to for this forwarding rule, used with internal load balancers and
external passthrough Network Load Balancers with IPv6.

If the network specified is in auto subnet mode, this field is optional.
However, a subnetwork must be specified if the network is in custom subnet
mode or when creating external forwarding rule with IPv6. |
| `metadata_filters` | Vec<String> |  | Opaque filter criteria used by load balancer to restrict routing
configuration to a limited set of xDS
compliant clients. In their xDS requests to load balancer, xDS clients
present node
metadata. When there is a match, the relevant configuration
is made available to those proxies. Otherwise, all the resources (e.g.TargetHttpProxy, UrlMap)
referenced by the ForwardingRule are not visible to
those proxies.

For each metadataFilter in this list, if itsfilterMatchCriteria is set to MATCH_ANY, at least one of thefilterLabels must match the corresponding label provided in
the metadata. If its filterMatchCriteria is set to
MATCH_ALL, then all of its filterLabels must match with
corresponding labels provided in the metadata. If multiplemetadataFilters are specified, all of them need to be satisfied
in order to be considered a match.

metadataFilters specified here will be applifed before
those specified in the UrlMap that thisForwardingRule references.

metadataFilters only applies to Loadbalancers that have
their loadBalancingScheme set toINTERNAL_SELF_MANAGED. |
| `project` | String | ✅ | Project ID for this request. |
| `region` | String | ✅ | Name of the region scoping this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `ports` | Vec<String> | The ports, portRange, and allPorts
fields are mutually exclusive. Only packets addressed to ports in the
specified range will be forwarded to the backends configured with this
forwarding rule.

The ports field has the following limitations:
   
   - It requires that the forwarding rule IPProtocol be TCP,
   UDP, or SCTP, and
   - It's applicable only to the following products: internal passthrough
   Network Load Balancers, backend service-based external passthrough Network
   Load Balancers, and internal protocol forwarding.
   - You can specify a list of up to five ports by number, separated by
   commas. The ports can be contiguous or discontiguous.



For external forwarding rules, two or more forwarding rules cannot use the
same [IPAddress, IPProtocol] pair if they share at least one
port number.

For internal forwarding rules within the same VPC network, two or more
forwarding rules cannot use the same [IPAddress, IPProtocol]
pair if they share at least one port number.

@pattern: \\d+(?:-\\d+)? |
| `base_forwarding_rule` | String | [Output Only] The URL for the corresponding base forwarding rule. By base
forwarding rule, we mean the forwarding rule that has the same IP address,
protocol, and port settings with the current forwarding rule, but without
sourceIPRanges specified.
Always empty if the current forwarding rule does not have sourceIPRanges
specified. |
| `load_balancing_scheme` | String | Specifies the forwarding rule type.

For more information about forwarding rules, refer to
Forwarding rule concepts. |
| `self_link_with_id` | String | [Output Only] Server-defined URL for this resource with the resource id. |
| `region` | String | [Output Only] URL of the region where the regional forwarding rule resides.
This field is not applicable to global forwarding rules.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `network_tier` | String | This signifies the networking tier used for configuring
this load balancer and can only take the following values:PREMIUM, STANDARD.

For regional ForwardingRule, the valid values are PREMIUM andSTANDARD. For GlobalForwardingRule, the valid value isPREMIUM.

If this field is not specified, it is assumed to be PREMIUM.
If IPAddress is specified, this value must be equal to the
networkTier of the Address. |
| `psc_connection_id` | String | [Output Only] The PSC connection id of the PSC forwarding rule. |
| `source_ip_ranges` | Vec<String> | If not empty, this forwarding rule will only forward the traffic when the
source IP address matches one of the IP addresses or CIDR ranges set here.
Note that a forwarding rule can only have up to 64 source IP ranges, and
this field can only be used with a regional forwarding rule whose scheme isEXTERNAL.
Each source_ip_range entry should be either an IP address (for
example, 1.2.3.4) or a CIDR range (for example, 1.2.3.0/24). |
| `port_range` | String | The ports, portRange, and allPorts
fields are mutually exclusive. Only packets addressed to ports in the
specified range will be forwarded to the backends configured with this
forwarding rule.

The portRange field has the following limitations:
   
   - It requires that the forwarding rule IPProtocol be TCP,
   UDP, or SCTP, and
   - It's applicable only to the following products: external passthrough
   Network Load Balancers, internal and external proxy Network Load Balancers,
   internal and external Application Load Balancers, external protocol
   forwarding, and Classic VPN.
   - Some products have restrictions on what ports can be used. See 
   port specifications for details.



For external forwarding rules, two or more forwarding rules cannot use the
same [IPAddress, IPProtocol] pair, and cannot have overlappingportRanges.

For internal forwarding rules within the same VPC network, two or more
forwarding rules cannot use the same [IPAddress, IPProtocol]
pair, and cannot have overlapping portRanges.

@pattern: \\d+(?:-\\d+)? |
| `name` | String | Name of the resource; provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash.

For Private Service Connect forwarding rules that forward traffic to Google
APIs, the forwarding rule name must be a 1-20 characters string with
lowercase letters and numbers and must start with a letter. |
| `backend_service` | String | Identifies the backend service to which the forwarding rule sends traffic.
Required for internal and external passthrough Network Load Balancers;
must be omitted for all other load balancer types. |
| `all_ports` | bool | The ports, portRange, and allPorts
fields are mutually exclusive. Only packets addressed to ports in the
specified range will be forwarded to the backends configured with this
forwarding rule.

The allPorts field has the following limitations:
   
   - It requires that the forwarding rule IPProtocol be TCP,
   UDP, SCTP, or L3_DEFAULT.
   - It's applicable only to the following products: internal passthrough
   Network Load Balancers, backend service-based external passthrough Network
   Load Balancers, and internal and external protocol forwarding.
   - Set this field to true to allow packets addressed to any port or
   packets lacking destination port information (for example, UDP fragments
   after the first fragment) to be forwarded to the backends configured with
   this forwarding rule. The L3_DEFAULT protocol requiresallPorts be set to true. |
| `fingerprint` | String | Fingerprint of this resource. A hash of the contents stored in this object.
This field is used in optimistic locking. This field will be ignored when
inserting a ForwardingRule. Include the fingerprint in patch request to
ensure that you do not overwrite changes that were applied from another
concurrent request.

To see the latest fingerprint, make a get() request to
retrieve a ForwardingRule. |
| `external_managed_backend_bucket_migration_state` | String | Specifies the canary migration state for the backend buckets attached to
this forwarding rule. Possible values are PREPARE, TEST_BY_PERCENTAGE, and
TEST_ALL_TRAFFIC.

To begin the migration from EXTERNAL to EXTERNAL_MANAGED, the state must be
changed to PREPARE. The state must be changed to TEST_ALL_TRAFFIC before
the loadBalancingScheme can be changed to EXTERNAL_MANAGED. Optionally, the
TEST_BY_PERCENTAGE state can be used to migrate traffic to backend buckets
attached to this forwarding rule by percentage using
externalManagedBackendBucketMigrationTestingPercentage.

Rolling back a migration requires the states to be set in reverse order. So
changing the scheme from EXTERNAL_MANAGED to EXTERNAL requires the state to
be set to TEST_ALL_TRAFFIC at the same time. Optionally, the
TEST_BY_PERCENTAGE state can be used to migrate some traffic back to
EXTERNAL or PREPARE can be used to migrate all traffic back to EXTERNAL. |
| `service_directory_registrations` | Vec<String> | Service Directory resources to register this forwarding rule with.
Currently, only supports a single Service Directory resource. |
| `service_label` | String | An optional prefix to the service name for this forwarding rule.
If specified, the prefix is the first label of the fully qualified service
name.

The label must be 1-63 characters long, and comply withRFC1035.
Specifically, the label must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash.

This field is only used for internal load balancing. |
| `ip_address` | String | IP address for which this forwarding rule accepts traffic. When a client
sends traffic to this IP address, the forwarding rule directs the traffic
to the referenced target or backendService.
While creating a forwarding rule, specifying an IPAddress is
required under the following circumstances:

   
   - When the target is set to targetGrpcProxy andvalidateForProxyless is set to true, theIPAddress should be set to 0.0.0.0.
   - When the target is a Private Service Connect Google APIs
   bundle, you must specify an IPAddress.


Otherwise, you can optionally specify an IP address that references an
existing static (reserved) IP address resource. When omitted, Google Cloud
assigns an ephemeral IP address.

Use one of the following formats to specify an IP address while creating a
forwarding rule:

* IP address number, as in `100.1.2.3`
* IPv6 address range, as in `2600:1234::/96`
* Full resource URL, as inhttps://www.googleapis.com/compute/v1/projects/project_id/regions/region/addresses/address-name
* Partial URL or by name, as in:
   
   - projects/project_id/regions/region/addresses/address-name
   - regions/region/addresses/address-name
   - global/addresses/address-name
   - address-name



The forwarding rule's target or backendService,
and in most cases, also the loadBalancingScheme, determine the
type of IP address that you can use. For detailed information, see
[IP address
specifications](https://cloud.google.com/load-balancing/docs/forwarding-rule-concepts#ip_address_specifications).

When reading an IPAddress, the API always returns the IP
address number. |
| `network` | String | This field is not used for global external load balancing.

For internal passthrough Network Load Balancers, this field identifies the
network that the load balanced IP should belong to for this forwarding
rule.
If the subnetwork is specified, the network of the subnetwork will be used.
If neither subnetwork nor this field is specified, the default network will
be used.

For Private Service Connect forwarding rules that forward traffic to Google
APIs, a network must be provided. |
| `kind` | String | [Output Only] Type of the resource. Alwayscompute#forwardingRule for forwarding rule resources. |
| `allow_psc_global_access` | bool | This is used in PSC consumer ForwardingRule to control whether the PSC
endpoint can be accessed from another region. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `ip_collection` | String | Resource reference of a PublicDelegatedPrefix. The PDP must
be a sub-PDP in EXTERNAL_IPV6_FORWARDING_RULE_CREATION mode.

Use one of the following formats to specify a sub-PDP when creating an IPv6
NetLB forwarding rule using BYOIP:
Full resource URL, as inhttps://www.googleapis.com/compute/v1/projects/project_id/regions/region/publicDelegatedPrefixes/sub-pdp-name
Partial URL, as in:
   
   - projects/project_id/regions/region/publicDelegatedPrefixes/sub-pdp-name
   - regions/region/publicDelegatedPrefixes/sub-pdp-name |
| `target` | String | The URL of the target resource to receive the matched traffic.  For
regional forwarding rules, this target must be in the same region as the
forwarding rule. For global forwarding rules, this target must be a global
load balancing resource.

The forwarded traffic must be of a type appropriate to the target object.
   
   
     -  For load balancers, see the "Target" column in [Port specifications](https://cloud.google.com/load-balancing/docs/forwarding-rule-concepts#ip_address_specifications).
     -  For Private Service Connect forwarding rules that forward traffic to Google APIs, provide the name of a supported Google API bundle:
       
      
            -  vpc-sc -  APIs that support VPC Service Controls. 
            -  all-apis - All supported Google APIs. 
          
     
     -  For Private Service Connect forwarding rules that forward traffic to managed services, the target must be a service attachment. The target is not mutable once set as a service attachment. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `ip_version` | String | The IP Version that will be used by this forwarding rule.  Valid options
are IPV4 or IPV6. |
| `external_managed_backend_bucket_migration_testing_percentage` | f64 | Determines the fraction of requests to backend buckets that should be
processed by the global external Application Load Balancer.

The value of this field must be in the range [0, 100].

This value can only be set if the loadBalancingScheme in the BackendService
is set to EXTERNAL (when using the classic Application Load Balancer) and
the migration state is TEST_BY_PERCENTAGE. |
| `label_fingerprint` | String | A fingerprint for the labels being applied to this resource, which is
essentially a hash of the labels set used for optimistic locking. The
fingerprint is initially generated by Compute Engine and changes after
every request to modify or update labels. You must always provide an
up-to-date fingerprint hash in order to update or change labels,
otherwise the request will fail with error412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve a ForwardingRule. |
| `is_mirroring_collector` | bool | Indicates whether or not this load balancer can be used as a collector for
packet mirroring. To prevent mirroring loops, instances behind this
load balancer will not have their traffic mirrored even if aPacketMirroring rule applies to them.
This can only be set to true for load balancers that have theirloadBalancingScheme set to INTERNAL. |
| `allow_global_access` | bool | If set to true, clients can access the internal passthrough Network Load
Balancers, the regional internal Application Load Balancer, and the
regional internal proxy Network Load Balancer from all regions.
If false, only allows access from the local region the load balancer is
located at. Note that for INTERNAL_MANAGED forwarding rules, this field
cannot be changed after the forwarding rule is created. |
| `labels` | HashMap<String, String> | Labels for this resource. These can only be added or modified by thesetLabels method. Each label key/value pair must comply withRFC1035.
Label values may be empty. |
| `no_automate_dns_zone` | bool | This is used in PSC consumer ForwardingRule to control whether it should
try to auto-generate a DNS zone or not. Non-PSC forwarding rules do not use
this field. Once set, this field is not mutable. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `ip_protocol` | String | The IP protocol to which this rule applies.

For protocol forwarding, valid
options are TCP, UDP, ESP,AH, SCTP, ICMP andL3_DEFAULT.

The valid IP protocols are different for different load balancing products
as described in [Load balancing
features](https://cloud.google.com/load-balancing/docs/features#protocols_from_the_load_balancer_to_the_backends). |
| `service_name` | String | [Output Only]
The internal fully qualified service name for this forwarding rule.

This field is only used for internal load balancing. |
| `psc_connection_status` | String |  |
| `subnetwork` | String | This field identifies the subnetwork that the load balanced IP should
belong to for this forwarding rule, used with internal load balancers and
external passthrough Network Load Balancers with IPv6.

If the network specified is in auto subnet mode, this field is optional.
However, a subnetwork must be specified if the network is in custom subnet
mode or when creating external forwarding rule with IPv6. |
| `metadata_filters` | Vec<String> | Opaque filter criteria used by load balancer to restrict routing
configuration to a limited set of xDS
compliant clients. In their xDS requests to load balancer, xDS clients
present node
metadata. When there is a match, the relevant configuration
is made available to those proxies. Otherwise, all the resources (e.g.TargetHttpProxy, UrlMap)
referenced by the ForwardingRule are not visible to
those proxies.

For each metadataFilter in this list, if itsfilterMatchCriteria is set to MATCH_ANY, at least one of thefilterLabels must match the corresponding label provided in
the metadata. If its filterMatchCriteria is set to
MATCH_ALL, then all of its filterLabels must match with
corresponding labels provided in the metadata. If multiplemetadataFilters are specified, all of them need to be satisfied
in order to be considered a match.

metadataFilters specified here will be applifed before
those specified in the UrlMap that thisForwardingRule references.

metadataFilters only applies to Loadbalancers that have
their loadBalancingScheme set toINTERNAL_SELF_MANAGED. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create forwarding_rule
forwarding_rule = provider.compute_api.Forwarding_rule {
    project = "value"  # Project ID for this request.
    region = "value"  # Name of the region scoping this request.
}

# Access forwarding_rule outputs
forwarding_rule_id = forwarding_rule.id
forwarding_rule_ports = forwarding_rule.ports
forwarding_rule_base_forwarding_rule = forwarding_rule.base_forwarding_rule
forwarding_rule_load_balancing_scheme = forwarding_rule.load_balancing_scheme
forwarding_rule_self_link_with_id = forwarding_rule.self_link_with_id
forwarding_rule_region = forwarding_rule.region
forwarding_rule_self_link = forwarding_rule.self_link
forwarding_rule_network_tier = forwarding_rule.network_tier
forwarding_rule_psc_connection_id = forwarding_rule.psc_connection_id
forwarding_rule_source_ip_ranges = forwarding_rule.source_ip_ranges
forwarding_rule_port_range = forwarding_rule.port_range
forwarding_rule_name = forwarding_rule.name
forwarding_rule_backend_service = forwarding_rule.backend_service
forwarding_rule_all_ports = forwarding_rule.all_ports
forwarding_rule_fingerprint = forwarding_rule.fingerprint
forwarding_rule_external_managed_backend_bucket_migration_state = forwarding_rule.external_managed_backend_bucket_migration_state
forwarding_rule_service_directory_registrations = forwarding_rule.service_directory_registrations
forwarding_rule_service_label = forwarding_rule.service_label
forwarding_rule_ip_address = forwarding_rule.ip_address
forwarding_rule_network = forwarding_rule.network
forwarding_rule_kind = forwarding_rule.kind
forwarding_rule_allow_psc_global_access = forwarding_rule.allow_psc_global_access
forwarding_rule_id = forwarding_rule.id
forwarding_rule_ip_collection = forwarding_rule.ip_collection
forwarding_rule_target = forwarding_rule.target
forwarding_rule_creation_timestamp = forwarding_rule.creation_timestamp
forwarding_rule_ip_version = forwarding_rule.ip_version
forwarding_rule_external_managed_backend_bucket_migration_testing_percentage = forwarding_rule.external_managed_backend_bucket_migration_testing_percentage
forwarding_rule_label_fingerprint = forwarding_rule.label_fingerprint
forwarding_rule_is_mirroring_collector = forwarding_rule.is_mirroring_collector
forwarding_rule_allow_global_access = forwarding_rule.allow_global_access
forwarding_rule_labels = forwarding_rule.labels
forwarding_rule_no_automate_dns_zone = forwarding_rule.no_automate_dns_zone
forwarding_rule_description = forwarding_rule.description
forwarding_rule_ip_protocol = forwarding_rule.ip_protocol
forwarding_rule_service_name = forwarding_rule.service_name
forwarding_rule_psc_connection_status = forwarding_rule.psc_connection_status
forwarding_rule_subnetwork = forwarding_rule.subnetwork
forwarding_rule_metadata_filters = forwarding_rule.metadata_filters
```

---


### Https_health_check

Creates a HttpsHealthCheck resource in the specified project using the data
included in the request.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `timeout_sec` | i64 |  | How long (in seconds) to wait before claiming failure. The default value is
5 seconds. It is invalid for timeoutSec to have a greater value than
checkIntervalSec. |
| `unhealthy_threshold` | i64 |  | A so-far healthy instance will be marked unhealthy after this
many consecutive failures. The default value is 2. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `kind` | String |  | Type of the resource. |
| `name` | String |  | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `check_interval_sec` | i64 |  | How often (in seconds) to send a health check. The default value is 5
seconds. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `healthy_threshold` | i64 |  | A so-far unhealthy instance will be marked healthy after this
many consecutive successes. The default value is 2. |
| `host` | String |  | The value of the host header in the HTTPS health check request. If left
empty (default value), the public IP on behalf of which this health check
is performed will be used. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `port` | i64 |  | The TCP port number for the HTTPS health check request. The default value
is 443. |
| `request_path` | String |  | The request path of the HTTPS health check request. The default value is
"/". Must comply withRFC3986. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `timeout_sec` | i64 | How long (in seconds) to wait before claiming failure. The default value is
5 seconds. It is invalid for timeoutSec to have a greater value than
checkIntervalSec. |
| `unhealthy_threshold` | i64 | A so-far healthy instance will be marked unhealthy after this
many consecutive failures. The default value is 2. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `kind` | String | Type of the resource. |
| `name` | String | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `check_interval_sec` | i64 | How often (in seconds) to send a health check. The default value is 5
seconds. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `healthy_threshold` | i64 | A so-far unhealthy instance will be marked healthy after this
many consecutive successes. The default value is 2. |
| `host` | String | The value of the host header in the HTTPS health check request. If left
empty (default value), the public IP on behalf of which this health check
is performed will be used. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `port` | i64 | The TCP port number for the HTTPS health check request. The default value
is 443. |
| `request_path` | String | The request path of the HTTPS health check request. The default value is
"/". Must comply withRFC3986. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create https_health_check
https_health_check = provider.compute_api.Https_health_check {
    project = "value"  # Project ID for this request.
}

# Access https_health_check outputs
https_health_check_id = https_health_check.id
https_health_check_timeout_sec = https_health_check.timeout_sec
https_health_check_unhealthy_threshold = https_health_check.unhealthy_threshold
https_health_check_self_link = https_health_check.self_link
https_health_check_kind = https_health_check.kind
https_health_check_name = https_health_check.name
https_health_check_creation_timestamp = https_health_check.creation_timestamp
https_health_check_check_interval_sec = https_health_check.check_interval_sec
https_health_check_description = https_health_check.description
https_health_check_healthy_threshold = https_health_check.healthy_threshold
https_health_check_host = https_health_check.host
https_health_check_id = https_health_check.id
https_health_check_port = https_health_check.port
https_health_check_request_path = https_health_check.request_path
```

---


### Region_target_tcp_proxie

Creates a TargetTcpProxy resource in the specified project and region using
the data included in the request.

**Operations**: ✅ Create ✅ Read ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `kind` | String |  | [Output Only] Type of the resource. Alwayscompute#targetTcpProxy for target TCP proxies. |
| `proxy_bind` | bool |  | This field only applies when the forwarding rule that references this
target proxy has a loadBalancingScheme set toINTERNAL_SELF_MANAGED.

When this field is set to true, Envoy proxies set up inbound
traffic interception and bind to the IP address and port specified in the
forwarding rule. This is generally useful when using Traffic Director to
configure Envoy as a gateway or middle proxy (in other words, not a
sidecar proxy). The Envoy proxy listens for inbound requests and handles
requests when it receives them.

The default is false. |
| `proxy_header` | String |  | Specifies the type of proxy header to append before sending data to the
backend, either NONE or PROXY_V1. The default
is NONE. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `name` | String |  | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `region` | String |  | [Output Only] URL of the region where the regional TCP proxy resides.
This field is not applicable to global TCP proxy. |
| `service` | String |  | URL to the BackendService resource. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `region` | String | ✅ | Name of the region scoping this request. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `kind` | String | [Output Only] Type of the resource. Alwayscompute#targetTcpProxy for target TCP proxies. |
| `proxy_bind` | bool | This field only applies when the forwarding rule that references this
target proxy has a loadBalancingScheme set toINTERNAL_SELF_MANAGED.

When this field is set to true, Envoy proxies set up inbound
traffic interception and bind to the IP address and port specified in the
forwarding rule. This is generally useful when using Traffic Director to
configure Envoy as a gateway or middle proxy (in other words, not a
sidecar proxy). The Envoy proxy listens for inbound requests and handles
requests when it receives them.

The default is false. |
| `proxy_header` | String | Specifies the type of proxy header to append before sending data to the
backend, either NONE or PROXY_V1. The default
is NONE. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `name` | String | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `region` | String | [Output Only] URL of the region where the regional TCP proxy resides.
This field is not applicable to global TCP proxy. |
| `service` | String | URL to the BackendService resource. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create region_target_tcp_proxie
region_target_tcp_proxie = provider.compute_api.Region_target_tcp_proxie {
    region = "value"  # Name of the region scoping this request.
    project = "value"  # Project ID for this request.
}

# Access region_target_tcp_proxie outputs
region_target_tcp_proxie_id = region_target_tcp_proxie.id
region_target_tcp_proxie_description = region_target_tcp_proxie.description
region_target_tcp_proxie_creation_timestamp = region_target_tcp_proxie.creation_timestamp
region_target_tcp_proxie_kind = region_target_tcp_proxie.kind
region_target_tcp_proxie_proxy_bind = region_target_tcp_proxie.proxy_bind
region_target_tcp_proxie_proxy_header = region_target_tcp_proxie.proxy_header
region_target_tcp_proxie_id = region_target_tcp_proxie.id
region_target_tcp_proxie_name = region_target_tcp_proxie.name
region_target_tcp_proxie_region = region_target_tcp_proxie.region
region_target_tcp_proxie_service = region_target_tcp_proxie.service
region_target_tcp_proxie_self_link = region_target_tcp_proxie.self_link
```

---


### Vpn_gateway

Creates a VPN gateway in the specified project and region using
the data included in the request.

**Operations**: ✅ Create ✅ Read ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `name` | String |  | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `network` | String |  | URL of the network to which this VPN gateway is attached. Provided by the
client when the VPN gateway is created. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `stack_type` | String |  | The stack type for this VPN gateway to identify the IP protocols that are
enabled. Possible values are: IPV4_ONLY,IPV4_IPV6, IPV6_ONLY. If not specified,IPV4_ONLY is used if the gateway IP version isIPV4, or IPV4_IPV6 if the gateway IP version isIPV6. |
| `label_fingerprint` | String |  | A fingerprint for the labels being applied to this VpnGateway, which
is essentially a hash of the labels set used for optimistic locking. The
fingerprint is initially generated by Compute Engine and changes after
every request to modify or update labels. You must always provide an
up-to-date fingerprint hash in order to update or change labels,
otherwise the request will fail with error412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve a VpnGateway. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `kind` | String |  | [Output Only] Type of resource. Always compute#vpnGateway for
VPN gateways. |
| `region` | String |  | [Output Only] URL of the region where the VPN gateway resides. |
| `gateway_ip_version` | String |  | The IP family of the gateway IPs for the HA-VPN gateway interfaces. If not
specified, IPV4 will be used. |
| `vpn_interfaces` | Vec<String> |  | The list of VPN interfaces associated with this VPN gateway. |
| `labels` | HashMap<String, String> |  | Labels for this resource. These can only be added or modified by thesetLabels method. Each label key/value pair must comply withRFC1035.
Label values may be empty. |
| `region` | String | ✅ | Name of the region for this request. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `name` | String | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `network` | String | URL of the network to which this VPN gateway is attached. Provided by the
client when the VPN gateway is created. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `stack_type` | String | The stack type for this VPN gateway to identify the IP protocols that are
enabled. Possible values are: IPV4_ONLY,IPV4_IPV6, IPV6_ONLY. If not specified,IPV4_ONLY is used if the gateway IP version isIPV4, or IPV4_IPV6 if the gateway IP version isIPV6. |
| `label_fingerprint` | String | A fingerprint for the labels being applied to this VpnGateway, which
is essentially a hash of the labels set used for optimistic locking. The
fingerprint is initially generated by Compute Engine and changes after
every request to modify or update labels. You must always provide an
up-to-date fingerprint hash in order to update or change labels,
otherwise the request will fail with error412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve a VpnGateway. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `kind` | String | [Output Only] Type of resource. Always compute#vpnGateway for
VPN gateways. |
| `region` | String | [Output Only] URL of the region where the VPN gateway resides. |
| `gateway_ip_version` | String | The IP family of the gateway IPs for the HA-VPN gateway interfaces. If not
specified, IPV4 will be used. |
| `vpn_interfaces` | Vec<String> | The list of VPN interfaces associated with this VPN gateway. |
| `labels` | HashMap<String, String> | Labels for this resource. These can only be added or modified by thesetLabels method. Each label key/value pair must comply withRFC1035.
Label values may be empty. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create vpn_gateway
vpn_gateway = provider.compute_api.Vpn_gateway {
    region = "value"  # Name of the region for this request.
    project = "value"  # Project ID for this request.
}

# Access vpn_gateway outputs
vpn_gateway_id = vpn_gateway.id
vpn_gateway_name = vpn_gateway.name
vpn_gateway_network = vpn_gateway.network
vpn_gateway_id = vpn_gateway.id
vpn_gateway_self_link = vpn_gateway.self_link
vpn_gateway_creation_timestamp = vpn_gateway.creation_timestamp
vpn_gateway_stack_type = vpn_gateway.stack_type
vpn_gateway_label_fingerprint = vpn_gateway.label_fingerprint
vpn_gateway_description = vpn_gateway.description
vpn_gateway_kind = vpn_gateway.kind
vpn_gateway_region = vpn_gateway.region
vpn_gateway_gateway_ip_version = vpn_gateway.gateway_ip_version
vpn_gateway_vpn_interfaces = vpn_gateway.vpn_interfaces
vpn_gateway_labels = vpn_gateway.labels
```

---


### Region_url_map

Creates a UrlMap resource in the specified project using
the data included in the request.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `host_rules` | Vec<String> |  | The list of host rules to use against the URL. |
| `name` | String |  | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `default_route_action` | String |  | defaultRouteAction takes effect when none of the 
hostRules match. The load balancer performs advanced routing
actions, such as URL rewrites and header transformations, before forwarding
the request to the selected backend.


Only one of defaultUrlRedirect, defaultService
or defaultRouteAction.weightedBackendService can be set.


 URL maps for classic Application Load Balancers only support
the urlRewrite action within defaultRouteAction.


defaultRouteAction has no effect when the URL map is bound
to a target gRPC proxy that has the validateForProxyless field
set to true. |
| `fingerprint` | String |  | Fingerprint of this resource. A hash of the contents stored in this object.
This field is used in optimistic locking. This field is ignored when
inserting a UrlMap. An up-to-date fingerprint must be provided
in order to update the UrlMap, otherwise the request will
fail with error 412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve a UrlMap. |
| `region` | String |  | [Output Only] URL of the region where the regional URL map resides.
This field is not applicable to global URL maps.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `header_action` | String |  | Specifies changes to request and response headers that need to take effect
for the selected backendService.

The headerAction specified here take effect afterheaderAction specified under pathMatcher.

headerAction is not supported for load balancers
that have
their loadBalancingScheme set to EXTERNAL.

Not supported when the URL map is bound to a target gRPC proxy that
has validateForProxyless field set to true. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `tests` | Vec<String> |  | The list of expected URL mapping tests. Request to update theUrlMap succeeds only if all test cases pass. You can specify a
maximum of 100 tests per UrlMap.

Not supported when the URL map is bound to a target gRPC proxy that
has validateForProxyless field set to true. |
| `default_service` | String |  | The full or partial URL of the defaultService resource to
which traffic is directed if none of the hostRules match.
If defaultRouteAction is also specified, advanced
routing actions, such as URL rewrites, take effect before sending the
request to the backend.


Only one of defaultUrlRedirect, defaultService
or defaultRouteAction.weightedBackendService can be set.


defaultService has no effect when the URL map is bound
to a target gRPC proxy that has the validateForProxyless field
set to true. |
| `path_matchers` | Vec<String> |  | The list of named PathMatchers to use against the URL. |
| `default_custom_error_response_policy` | String |  | defaultCustomErrorResponsePolicy specifies how the Load
Balancer returns error responses when BackendServiceorBackendBucket responds with an error. 

This policy takes
effect at the load balancer level and applies only when no policy has been
defined for the error code at lower levels like PathMatcher, RouteRule and
PathRule within this UrlMap. 

For example, consider a UrlMap with the
following configuration:
   
   
     - defaultCustomErrorResponsePolicy containing policies for
     responding to 5xx and 4xx errors
     - A PathMatcher configured for *.example.com has
     defaultCustomErrorResponsePolicy for 4xx.

If a request for http://www.example.com/ encounters a404, the policy inpathMatcher.defaultCustomErrorResponsePolicy will be enforced.
When the request for http://www.example.com/ encounters a502, the policy inUrlMap.defaultCustomErrorResponsePolicy will be enforced. When
a request that does not match any host in *.example.com such
as http://www.myotherexample.com/, encounters a404, UrlMap.defaultCustomErrorResponsePolicy
takes effect. 

When used in conjunction withdefaultRouteAction.retryPolicy, retries take precedence. Only
once all retries are exhausted, thedefaultCustomErrorResponsePolicy is applied. While attempting
a retry, if load balancer is successful in reaching the
service, the defaultCustomErrorResponsePolicy is ignored and
the response from the service is returned to the client.

defaultCustomErrorResponsePolicy is supported only for
global external Application Load Balancers. |
| `default_url_redirect` | String |  | When none of the specified hostRules match, the request
is redirected to a URL specified by defaultUrlRedirect.


Only one of defaultUrlRedirect, defaultService
or defaultRouteAction.weightedBackendService can be set.


Not supported when the URL map is bound to a target gRPC proxy. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `kind` | String |  | [Output Only] Type of the resource. Always compute#urlMaps for
url maps. |
| `project` | String | ✅ | Project ID for this request. |
| `region` | String | ✅ | Name of the region scoping this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `host_rules` | Vec<String> | The list of host rules to use against the URL. |
| `name` | String | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `default_route_action` | String | defaultRouteAction takes effect when none of the 
hostRules match. The load balancer performs advanced routing
actions, such as URL rewrites and header transformations, before forwarding
the request to the selected backend.


Only one of defaultUrlRedirect, defaultService
or defaultRouteAction.weightedBackendService can be set.


 URL maps for classic Application Load Balancers only support
the urlRewrite action within defaultRouteAction.


defaultRouteAction has no effect when the URL map is bound
to a target gRPC proxy that has the validateForProxyless field
set to true. |
| `fingerprint` | String | Fingerprint of this resource. A hash of the contents stored in this object.
This field is used in optimistic locking. This field is ignored when
inserting a UrlMap. An up-to-date fingerprint must be provided
in order to update the UrlMap, otherwise the request will
fail with error 412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve a UrlMap. |
| `region` | String | [Output Only] URL of the region where the regional URL map resides.
This field is not applicable to global URL maps.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `header_action` | String | Specifies changes to request and response headers that need to take effect
for the selected backendService.

The headerAction specified here take effect afterheaderAction specified under pathMatcher.

headerAction is not supported for load balancers
that have
their loadBalancingScheme set to EXTERNAL.

Not supported when the URL map is bound to a target gRPC proxy that
has validateForProxyless field set to true. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `tests` | Vec<String> | The list of expected URL mapping tests. Request to update theUrlMap succeeds only if all test cases pass. You can specify a
maximum of 100 tests per UrlMap.

Not supported when the URL map is bound to a target gRPC proxy that
has validateForProxyless field set to true. |
| `default_service` | String | The full or partial URL of the defaultService resource to
which traffic is directed if none of the hostRules match.
If defaultRouteAction is also specified, advanced
routing actions, such as URL rewrites, take effect before sending the
request to the backend.


Only one of defaultUrlRedirect, defaultService
or defaultRouteAction.weightedBackendService can be set.


defaultService has no effect when the URL map is bound
to a target gRPC proxy that has the validateForProxyless field
set to true. |
| `path_matchers` | Vec<String> | The list of named PathMatchers to use against the URL. |
| `default_custom_error_response_policy` | String | defaultCustomErrorResponsePolicy specifies how the Load
Balancer returns error responses when BackendServiceorBackendBucket responds with an error. 

This policy takes
effect at the load balancer level and applies only when no policy has been
defined for the error code at lower levels like PathMatcher, RouteRule and
PathRule within this UrlMap. 

For example, consider a UrlMap with the
following configuration:
   
   
     - defaultCustomErrorResponsePolicy containing policies for
     responding to 5xx and 4xx errors
     - A PathMatcher configured for *.example.com has
     defaultCustomErrorResponsePolicy for 4xx.

If a request for http://www.example.com/ encounters a404, the policy inpathMatcher.defaultCustomErrorResponsePolicy will be enforced.
When the request for http://www.example.com/ encounters a502, the policy inUrlMap.defaultCustomErrorResponsePolicy will be enforced. When
a request that does not match any host in *.example.com such
as http://www.myotherexample.com/, encounters a404, UrlMap.defaultCustomErrorResponsePolicy
takes effect. 

When used in conjunction withdefaultRouteAction.retryPolicy, retries take precedence. Only
once all retries are exhausted, thedefaultCustomErrorResponsePolicy is applied. While attempting
a retry, if load balancer is successful in reaching the
service, the defaultCustomErrorResponsePolicy is ignored and
the response from the service is returned to the client.

defaultCustomErrorResponsePolicy is supported only for
global external Application Load Balancers. |
| `default_url_redirect` | String | When none of the specified hostRules match, the request
is redirected to a URL specified by defaultUrlRedirect.


Only one of defaultUrlRedirect, defaultService
or defaultRouteAction.weightedBackendService can be set.


Not supported when the URL map is bound to a target gRPC proxy. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `kind` | String | [Output Only] Type of the resource. Always compute#urlMaps for
url maps. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create region_url_map
region_url_map = provider.compute_api.Region_url_map {
    project = "value"  # Project ID for this request.
    region = "value"  # Name of the region scoping this request.
}

# Access region_url_map outputs
region_url_map_id = region_url_map.id
region_url_map_host_rules = region_url_map.host_rules
region_url_map_name = region_url_map.name
region_url_map_default_route_action = region_url_map.default_route_action
region_url_map_fingerprint = region_url_map.fingerprint
region_url_map_region = region_url_map.region
region_url_map_creation_timestamp = region_url_map.creation_timestamp
region_url_map_header_action = region_url_map.header_action
region_url_map_description = region_url_map.description
region_url_map_tests = region_url_map.tests
region_url_map_default_service = region_url_map.default_service
region_url_map_path_matchers = region_url_map.path_matchers
region_url_map_default_custom_error_response_policy = region_url_map.default_custom_error_response_policy
region_url_map_default_url_redirect = region_url_map.default_url_redirect
region_url_map_id = region_url_map.id
region_url_map_self_link = region_url_map.self_link
region_url_map_kind = region_url_map.kind
```

---


### Region_health_check_service

Creates a regional HealthCheckService resource in the
specified project and region using the data included in the request.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `kind` | String |  | [Output only] Type of the resource. Alwayscompute#healthCheckServicefor health check services. |
| `health_status_aggregation_policy` | String |  | Optional. Policy for how the results from multiple health checks
for the same endpoint are aggregated. Defaults to NO_AGGREGATION
if unspecified.
   
   - NO_AGGREGATION. An EndpointHealth message is
   returned for each  pair in the health check
   service.
   - AND. If any health check of an endpoint reportsUNHEALTHY, then UNHEALTHY is theHealthState of the endpoint. If all health checks reportHEALTHY, the HealthState of the endpoint isHEALTHY.

.
This is only allowed with regional HealthCheckService. |
| `fingerprint` | String |  | Fingerprint of this resource. A hash of the contents stored in this object.
This field is used in optimistic locking. This field will be ignored when
inserting a HealthCheckService. An up-to-date fingerprint must
be provided in order to patch/update the HealthCheckService; Otherwise, the
request will fail with error 412 conditionNotMet. To see the
latest fingerprint, make a get() request to retrieve the
HealthCheckService. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `name` | String |  | Name of the resource. The name must be 1-63 characters long, and comply
with RFC1035. Specifically, the name must be 1-63 characters
long and match the regular expression
`[a-z]([-a-z0-9]*[a-z0-9])?` which means the first character
must be a lowercase letter, and all following characters must be a dash,
lowercase letter, or digit, except the last character, which cannot be a
dash. |
| `network_endpoint_groups` | Vec<String> |  | A list of URLs to the NetworkEndpointGroup
resources. Must not have more than 100.  For regionalHealthCheckService, NEGs must be in
zones in the region of the HealthCheckService. For globalHealthCheckServices, the NetworkEndpointGroups
must be global INTERNET_IP_PORT. |
| `region` | String |  | [Output Only] URL of the region where the health check service
resides. This field is not applicable to global health check services.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `health_checks` | Vec<String> |  | A list of URLs to the HealthCheck resources. Must have
at least one HealthCheck, and not more than 10 for regionalHealthCheckService, and not more than 1 for globalHealthCheckService.HealthCheck resources must haveportSpecification=USE_SERVING_PORT orportSpecification=USE_FIXED_PORT. For
regional HealthCheckService, theHealthCheck must be regional and in the same
region. For global HealthCheckService,HealthCheck must be global. Mix of regional and globalHealthChecks is not supported. Multiple regionalHealthChecks must belong to the same region. RegionalHealthChecks must belong to the same region as zones ofNetworkEndpointGroups. For globalHealthCheckService using globalINTERNET_IP_PORT NetworkEndpointGroups, the
global HealthChecks must specify sourceRegions,
and HealthChecks that specify sourceRegions can
only be used with global INTERNET_IP_PORTNetworkEndpointGroups. |
| `notification_endpoints` | Vec<String> |  | A list of URLs to the NotificationEndpoint
resources. Must not have more than 10.  A list of endpoints for
receiving notifications of change in health status. For regionalHealthCheckService,NotificationEndpoint must be regional and in the
same region. For global HealthCheckService,NotificationEndpoint must be global. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `project` | String | ✅ | Project ID for this request. |
| `region` | String | ✅ | Name of the region scoping this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `kind` | String | [Output only] Type of the resource. Alwayscompute#healthCheckServicefor health check services. |
| `health_status_aggregation_policy` | String | Optional. Policy for how the results from multiple health checks
for the same endpoint are aggregated. Defaults to NO_AGGREGATION
if unspecified.
   
   - NO_AGGREGATION. An EndpointHealth message is
   returned for each  pair in the health check
   service.
   - AND. If any health check of an endpoint reportsUNHEALTHY, then UNHEALTHY is theHealthState of the endpoint. If all health checks reportHEALTHY, the HealthState of the endpoint isHEALTHY.

.
This is only allowed with regional HealthCheckService. |
| `fingerprint` | String | Fingerprint of this resource. A hash of the contents stored in this object.
This field is used in optimistic locking. This field will be ignored when
inserting a HealthCheckService. An up-to-date fingerprint must
be provided in order to patch/update the HealthCheckService; Otherwise, the
request will fail with error 412 conditionNotMet. To see the
latest fingerprint, make a get() request to retrieve the
HealthCheckService. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `name` | String | Name of the resource. The name must be 1-63 characters long, and comply
with RFC1035. Specifically, the name must be 1-63 characters
long and match the regular expression
`[a-z]([-a-z0-9]*[a-z0-9])?` which means the first character
must be a lowercase letter, and all following characters must be a dash,
lowercase letter, or digit, except the last character, which cannot be a
dash. |
| `network_endpoint_groups` | Vec<String> | A list of URLs to the NetworkEndpointGroup
resources. Must not have more than 100.  For regionalHealthCheckService, NEGs must be in
zones in the region of the HealthCheckService. For globalHealthCheckServices, the NetworkEndpointGroups
must be global INTERNET_IP_PORT. |
| `region` | String | [Output Only] URL of the region where the health check service
resides. This field is not applicable to global health check services.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `health_checks` | Vec<String> | A list of URLs to the HealthCheck resources. Must have
at least one HealthCheck, and not more than 10 for regionalHealthCheckService, and not more than 1 for globalHealthCheckService.HealthCheck resources must haveportSpecification=USE_SERVING_PORT orportSpecification=USE_FIXED_PORT. For
regional HealthCheckService, theHealthCheck must be regional and in the same
region. For global HealthCheckService,HealthCheck must be global. Mix of regional and globalHealthChecks is not supported. Multiple regionalHealthChecks must belong to the same region. RegionalHealthChecks must belong to the same region as zones ofNetworkEndpointGroups. For globalHealthCheckService using globalINTERNET_IP_PORT NetworkEndpointGroups, the
global HealthChecks must specify sourceRegions,
and HealthChecks that specify sourceRegions can
only be used with global INTERNET_IP_PORTNetworkEndpointGroups. |
| `notification_endpoints` | Vec<String> | A list of URLs to the NotificationEndpoint
resources. Must not have more than 10.  A list of endpoints for
receiving notifications of change in health status. For regionalHealthCheckService,NotificationEndpoint must be regional and in the
same region. For global HealthCheckService,NotificationEndpoint must be global. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create region_health_check_service
region_health_check_service = provider.compute_api.Region_health_check_service {
    project = "value"  # Project ID for this request.
    region = "value"  # Name of the region scoping this request.
}

# Access region_health_check_service outputs
region_health_check_service_id = region_health_check_service.id
region_health_check_service_id = region_health_check_service.id
region_health_check_service_kind = region_health_check_service.kind
region_health_check_service_health_status_aggregation_policy = region_health_check_service.health_status_aggregation_policy
region_health_check_service_fingerprint = region_health_check_service.fingerprint
region_health_check_service_description = region_health_check_service.description
region_health_check_service_creation_timestamp = region_health_check_service.creation_timestamp
region_health_check_service_name = region_health_check_service.name
region_health_check_service_network_endpoint_groups = region_health_check_service.network_endpoint_groups
region_health_check_service_region = region_health_check_service.region
region_health_check_service_health_checks = region_health_check_service.health_checks
region_health_check_service_notification_endpoints = region_health_check_service.notification_endpoints
region_health_check_service_self_link = region_health_check_service.self_link
```

---


### Interconnect_group

Creates a InterconnectGroup in the specified project in the given scope
using the parameters that are included in the request.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `etag` | String |  | Opaque system-generated token that uniquely identifies the configuration.
If provided when patching a configuration in update mode, the provided
token must match the current token or the update is rejected. This provides
a reliable means of doing read-modify-write (optimistic locking) as
described by AIP 154. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `physical_structure` | String |  |  |
| `configured` | String |  |  |
| `name` | String |  | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `id` | String |  | [Output Only] The unique identifier for the resource type. The server
generates this identifier. |
| `interconnects` | HashMap<String, String> |  | Interconnects in the InterconnectGroup. Keys are arbitrary user-specified
strings. Users are encouraged, but not required, to use their preferred
format for resource links as keys.
Note that there are add-members and remove-members methods in gcloud.
The size of this map is limited by an "Interconnects per group" quota. |
| `intent` | String |  |  |
| `kind` | String |  | [Output Only] Type of the resource. Always compute#InterconnectGroup |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `etag` | String | Opaque system-generated token that uniquely identifies the configuration.
If provided when patching a configuration in update mode, the provided
token must match the current token or the update is rejected. This provides
a reliable means of doing read-modify-write (optimistic locking) as
described by AIP 154. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `physical_structure` | String |  |
| `configured` | String |  |
| `name` | String | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `id` | String | [Output Only] The unique identifier for the resource type. The server
generates this identifier. |
| `interconnects` | HashMap<String, String> | Interconnects in the InterconnectGroup. Keys are arbitrary user-specified
strings. Users are encouraged, but not required, to use their preferred
format for resource links as keys.
Note that there are add-members and remove-members methods in gcloud.
The size of this map is limited by an "Interconnects per group" quota. |
| `intent` | String |  |
| `kind` | String | [Output Only] Type of the resource. Always compute#InterconnectGroup |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create interconnect_group
interconnect_group = provider.compute_api.Interconnect_group {
    project = "value"  # Project ID for this request.
}

# Access interconnect_group outputs
interconnect_group_id = interconnect_group.id
interconnect_group_creation_timestamp = interconnect_group.creation_timestamp
interconnect_group_etag = interconnect_group.etag
interconnect_group_self_link = interconnect_group.self_link
interconnect_group_physical_structure = interconnect_group.physical_structure
interconnect_group_configured = interconnect_group.configured
interconnect_group_name = interconnect_group.name
interconnect_group_description = interconnect_group.description
interconnect_group_id = interconnect_group.id
interconnect_group_interconnects = interconnect_group.interconnects
interconnect_group_intent = interconnect_group.intent
interconnect_group_kind = interconnect_group.kind
```

---


### External_vpn_gateway

Creates a ExternalVpnGateway in the specified project using
the data included in the request.

**Operations**: ✅ Create ✅ Read ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `interfaces` | Vec<String> |  | A list of interfaces for this external VPN gateway.

If your peer-side gateway is an on-premises gateway and non-AWS cloud
providers' gateway, at most two interfaces can be provided for an external
VPN gateway. If your peer side is an AWS virtual private gateway, four
interfaces should be provided for an external VPN gateway. |
| `name` | String |  | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must be
a dash, lowercase letter, or digit, except the last character, which cannot
be a dash. |
| `label_fingerprint` | String |  | A fingerprint for the labels being applied to this ExternalVpnGateway,
which is essentially a hash of the labels set used for optimistic locking.
The fingerprint is initially generated by Compute Engine and changes after
every request to modify or update labels. You must always provide an
up-to-date fingerprint hash in order to update or change labels,
otherwise the request will fail with error412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve an ExternalVpnGateway. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `redundancy_type` | String |  | Indicates the user-supplied redundancy type of this external VPN gateway. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `labels` | HashMap<String, String> |  | Labels for this resource. These can only be added or modified by thesetLabels method. Each label key/value pair must comply withRFC1035.
Label values may be empty. |
| `kind` | String |  | [Output Only] Type of the resource. Alwayscompute#externalVpnGateway for externalVpnGateways. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `interfaces` | Vec<String> | A list of interfaces for this external VPN gateway.

If your peer-side gateway is an on-premises gateway and non-AWS cloud
providers' gateway, at most two interfaces can be provided for an external
VPN gateway. If your peer side is an AWS virtual private gateway, four
interfaces should be provided for an external VPN gateway. |
| `name` | String | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must be
a dash, lowercase letter, or digit, except the last character, which cannot
be a dash. |
| `label_fingerprint` | String | A fingerprint for the labels being applied to this ExternalVpnGateway,
which is essentially a hash of the labels set used for optimistic locking.
The fingerprint is initially generated by Compute Engine and changes after
every request to modify or update labels. You must always provide an
up-to-date fingerprint hash in order to update or change labels,
otherwise the request will fail with error412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve an ExternalVpnGateway. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `redundancy_type` | String | Indicates the user-supplied redundancy type of this external VPN gateway. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `labels` | HashMap<String, String> | Labels for this resource. These can only be added or modified by thesetLabels method. Each label key/value pair must comply withRFC1035.
Label values may be empty. |
| `kind` | String | [Output Only] Type of the resource. Alwayscompute#externalVpnGateway for externalVpnGateways. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create external_vpn_gateway
external_vpn_gateway = provider.compute_api.External_vpn_gateway {
    project = "value"  # Project ID for this request.
}

# Access external_vpn_gateway outputs
external_vpn_gateway_id = external_vpn_gateway.id
external_vpn_gateway_creation_timestamp = external_vpn_gateway.creation_timestamp
external_vpn_gateway_interfaces = external_vpn_gateway.interfaces
external_vpn_gateway_name = external_vpn_gateway.name
external_vpn_gateway_label_fingerprint = external_vpn_gateway.label_fingerprint
external_vpn_gateway_self_link = external_vpn_gateway.self_link
external_vpn_gateway_description = external_vpn_gateway.description
external_vpn_gateway_redundancy_type = external_vpn_gateway.redundancy_type
external_vpn_gateway_id = external_vpn_gateway.id
external_vpn_gateway_labels = external_vpn_gateway.labels
external_vpn_gateway_kind = external_vpn_gateway.kind
```

---


### Instance_group_manager_resize_request

Creates a new resize request that starts provisioning VMs immediately
or queues VM creation.

**Operations**: ✅ Create ✅ Read ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `id` | String |  | [Output Only] A unique identifier for this resource type. The server
generates this identifier. |
| `resize_by` | i64 |  | The number of instances to be created by this resize request. The group's
target size will be increased by this number. This field cannot be used
together with 'instances'. |
| `name` | String |  | The name of this resize request. The name must be 1-63 characters
long, and comply withRFC1035. |
| `self_link_with_id` | String |  | [Output Only] Server-defined URL for this resource with the resource id. |
| `state` | String |  | [Output only] Current state of the request. |
| `description` | String |  | An optional description of this resource. |
| `self_link` | String |  | [Output Only] The URL for this resize request. The server defines
this URL. |
| `creation_timestamp` | String |  | [Output Only] The creation timestamp for this resize request inRFC3339
text format. |
| `requested_run_duration` | String |  | Requested run duration for instances that will be created by this request.
At the end of the run duration instance will be deleted. |
| `status` | String |  | [Output only] Status of the request. |
| `zone` | String |  | [Output Only] The URL of azone
where the resize request is located. Populated only for zonal resize
requests. |
| `kind` | String |  | [Output Only] The resource type, which is alwayscompute#instanceGroupManagerResizeRequest for
resize requests. |
| `zone` | String | ✅ | The name of thezone where the managed
instance group is located and where the resize request will be created.
Name should conform to RFC1035. |
| `instance_group_manager` | String | ✅ | The name of the managed instance group to which the resize request will
be added.
Name should conform to RFC1035 or be a resource ID. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `id` | String | [Output Only] A unique identifier for this resource type. The server
generates this identifier. |
| `resize_by` | i64 | The number of instances to be created by this resize request. The group's
target size will be increased by this number. This field cannot be used
together with 'instances'. |
| `name` | String | The name of this resize request. The name must be 1-63 characters
long, and comply withRFC1035. |
| `self_link_with_id` | String | [Output Only] Server-defined URL for this resource with the resource id. |
| `state` | String | [Output only] Current state of the request. |
| `description` | String | An optional description of this resource. |
| `self_link` | String | [Output Only] The URL for this resize request. The server defines
this URL. |
| `creation_timestamp` | String | [Output Only] The creation timestamp for this resize request inRFC3339
text format. |
| `requested_run_duration` | String | Requested run duration for instances that will be created by this request.
At the end of the run duration instance will be deleted. |
| `status` | String | [Output only] Status of the request. |
| `zone` | String | [Output Only] The URL of azone
where the resize request is located. Populated only for zonal resize
requests. |
| `kind` | String | [Output Only] The resource type, which is alwayscompute#instanceGroupManagerResizeRequest for
resize requests. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create instance_group_manager_resize_request
instance_group_manager_resize_request = provider.compute_api.Instance_group_manager_resize_request {
    zone = "value"  # The name of thezone where the managed
instance group is located and where the resize request will be created.
Name should conform to RFC1035.
    instance_group_manager = "value"  # The name of the managed instance group to which the resize request will
be added.
Name should conform to RFC1035 or be a resource ID.
    project = "value"  # Project ID for this request.
}

# Access instance_group_manager_resize_request outputs
instance_group_manager_resize_request_id = instance_group_manager_resize_request.id
instance_group_manager_resize_request_id = instance_group_manager_resize_request.id
instance_group_manager_resize_request_resize_by = instance_group_manager_resize_request.resize_by
instance_group_manager_resize_request_name = instance_group_manager_resize_request.name
instance_group_manager_resize_request_self_link_with_id = instance_group_manager_resize_request.self_link_with_id
instance_group_manager_resize_request_state = instance_group_manager_resize_request.state
instance_group_manager_resize_request_description = instance_group_manager_resize_request.description
instance_group_manager_resize_request_self_link = instance_group_manager_resize_request.self_link
instance_group_manager_resize_request_creation_timestamp = instance_group_manager_resize_request.creation_timestamp
instance_group_manager_resize_request_requested_run_duration = instance_group_manager_resize_request.requested_run_duration
instance_group_manager_resize_request_status = instance_group_manager_resize_request.status
instance_group_manager_resize_request_zone = instance_group_manager_resize_request.zone
instance_group_manager_resize_request_kind = instance_group_manager_resize_request.kind
```

---


### Instance_group_manager

Creates a managed instance group using the information that you specify
in the request. After the group is created, instances in the group are
created using the specified instance template.
This operation is marked as DONE when the group is created
even if the instances in the group have not yet been created. You
must separately verify the status of the individual instances with thelistmanagedinstances
method.

A managed instance group can have up to 1000 VM instances per group. Please
contact Cloud Support if you need an increase in
this limit.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `named_ports` | Vec<String> |  | [Output Only] Named ports configured on the Instance Groups complementary
to this Instance Group Manager. |
| `kind` | String |  | [Output Only] The resource type, which is alwayscompute#instanceGroupManager for managed instance groups. |
| `stateful_policy` | String |  | Stateful configuration for this Instanced Group Manager |
| `status` | String |  | [Output Only] The status of this managed instance group. |
| `update_policy` | String |  | The update policy for this managed instance group. |
| `target_suspended_size` | i64 |  | The target number of suspended instances for this managed instance group.
This number changes when you: 
   
   - Suspend instance using the suspendInstances
   method or resume instances using the resumeInstances
   method.
   - Manually change the targetSuspendedSize using the update
   method. |
| `instance_flexibility_policy` | String |  | Instance flexibility allowing MIG to create VMs from multiple
types of machines.
Instance flexibility configuration on MIG overrides instance
template configuration. |
| `auto_healing_policies` | Vec<String> |  | The autohealing policy for this managed instance group. You can specify
only one value. |
| `zone` | String |  | [Output Only] The URL of azone
where the managed instance group is located (for zonal resources). |
| `creation_timestamp` | String |  | [Output Only] The creation timestamp for this managed instance group inRFC3339
text format. |
| `satisfies_pzs` | bool |  | [Output Only] Reserved for future use. |
| `self_link` | String |  | [Output Only] The URL for this managed instance group. The server defines
this URL. |
| `base_instance_name` | String |  | The base instance name is a prefix that you want to attach to the names of
all VMs in a MIG. The maximum character length is 58 and the name must
comply with RFC1035 format.

When a VM is created in the group, the MIG appends a hyphen and a random
four-character string to the base instance name. If you want the MIG to
assign sequential numbers instead of a random string, then end the base
instance name with a hyphen followed by one or more hash symbols. The hash
symbols indicate the number of digits. For example, a base instance name of
"vm-###" results in "vm-001" as a VM name.
@pattern
[a-z](([-a-z0-9]{0,57})|([-a-z0-9]{0,51}-#{1,10}(\\[[0-9]{1,10}\\])?)) |
| `list_managed_instances_results` | String |  | Pagination behavior of the listManagedInstances API method for
this managed instance group. |
| `all_instances_config` | String |  | Specifies configuration that overrides the instance template configuration
for the group. |
| `id` | String |  | [Output Only] A unique identifier for this resource type. The server
generates this identifier. |
| `resource_policies` | String |  | Resource policies for this managed instance group. |
| `name` | String |  | The name of the managed instance group. The name must be 1-63 characters
long, and comply withRFC1035. |
| `versions` | Vec<String> |  | Specifies the instance templates used by this managed instance group to
create instances.

Each version is defined by an instanceTemplate and aname. Every version can appear at most once per instance
group. This field overrides the top-level instanceTemplate
field. Read more about therelationships
between these fields. Exactly one version must leave thetargetSize field unset. That version will be applied to all
remaining instances. For more information, read aboutcanary
updates. |
| `instance_template` | String |  | The URL of the instance template that is specified for this managed
instance group. The group uses this template to create all new instances
in the managed instance group. The templates for existing instances in the
group do not change unless you run recreateInstances, runapplyUpdatesToInstances, or set the group'supdatePolicy.type to PROACTIVE. |
| `standby_policy` | String |  | Standby policy for stopped and suspended instances. |
| `distribution_policy` | String |  | Policy specifying the intended distribution of managed instances across
zones in a regional managed instance group. |
| `fingerprint` | String |  | Fingerprint of this resource. This field may be used in optimistic locking.
It will be ignored when inserting an InstanceGroupManager. An up-to-date
fingerprint must be provided in order to update the InstanceGroupManager,
otherwise the request will fail with error412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve an InstanceGroupManager. |
| `target_size` | i64 |  | The target number of running instances for this managed instance group.
You can reduce this number by using the instanceGroupManager
deleteInstances or abandonInstances methods. Resizing the group also
changes this number. |
| `satisfies_pzi` | bool |  | [Output Only] Reserved for future use. |
| `target_stopped_size` | i64 |  | The target number of stopped instances for this managed instance group.
This number changes when you: 
   
   - Stop instance using the stopInstances
   method or start instances using the startInstances
   method.
   - Manually change the targetStoppedSize using the update
   method. |
| `target_pools` | Vec<String> |  | The URLs for all TargetPool resources to which instances in theinstanceGroup field are added. The target pools automatically
apply to all of the instances in the managed instance group. |
| `instance_lifecycle_policy` | String |  | The repair policy for this managed instance group. |
| `instance_group` | String |  | [Output Only] The URL of the Instance Group resource. |
| `current_actions` | String |  | [Output Only] The list of instance actions and the number of instances
in this managed instance group that are scheduled for each of those
actions. |
| `region` | String |  | [Output Only] The URL of theregion
where the managed instance group resides (for regional resources). |
| `description` | String |  | An optional description of this resource. |
| `project` | String | ✅ | Project ID for this request. |
| `zone` | String | ✅ | The name of the zone
where you want to create the managed instance group. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `named_ports` | Vec<String> | [Output Only] Named ports configured on the Instance Groups complementary
to this Instance Group Manager. |
| `kind` | String | [Output Only] The resource type, which is alwayscompute#instanceGroupManager for managed instance groups. |
| `stateful_policy` | String | Stateful configuration for this Instanced Group Manager |
| `status` | String | [Output Only] The status of this managed instance group. |
| `update_policy` | String | The update policy for this managed instance group. |
| `target_suspended_size` | i64 | The target number of suspended instances for this managed instance group.
This number changes when you: 
   
   - Suspend instance using the suspendInstances
   method or resume instances using the resumeInstances
   method.
   - Manually change the targetSuspendedSize using the update
   method. |
| `instance_flexibility_policy` | String | Instance flexibility allowing MIG to create VMs from multiple
types of machines.
Instance flexibility configuration on MIG overrides instance
template configuration. |
| `auto_healing_policies` | Vec<String> | The autohealing policy for this managed instance group. You can specify
only one value. |
| `zone` | String | [Output Only] The URL of azone
where the managed instance group is located (for zonal resources). |
| `creation_timestamp` | String | [Output Only] The creation timestamp for this managed instance group inRFC3339
text format. |
| `satisfies_pzs` | bool | [Output Only] Reserved for future use. |
| `self_link` | String | [Output Only] The URL for this managed instance group. The server defines
this URL. |
| `base_instance_name` | String | The base instance name is a prefix that you want to attach to the names of
all VMs in a MIG. The maximum character length is 58 and the name must
comply with RFC1035 format.

When a VM is created in the group, the MIG appends a hyphen and a random
four-character string to the base instance name. If you want the MIG to
assign sequential numbers instead of a random string, then end the base
instance name with a hyphen followed by one or more hash symbols. The hash
symbols indicate the number of digits. For example, a base instance name of
"vm-###" results in "vm-001" as a VM name.
@pattern
[a-z](([-a-z0-9]{0,57})|([-a-z0-9]{0,51}-#{1,10}(\\[[0-9]{1,10}\\])?)) |
| `list_managed_instances_results` | String | Pagination behavior of the listManagedInstances API method for
this managed instance group. |
| `all_instances_config` | String | Specifies configuration that overrides the instance template configuration
for the group. |
| `id` | String | [Output Only] A unique identifier for this resource type. The server
generates this identifier. |
| `resource_policies` | String | Resource policies for this managed instance group. |
| `name` | String | The name of the managed instance group. The name must be 1-63 characters
long, and comply withRFC1035. |
| `versions` | Vec<String> | Specifies the instance templates used by this managed instance group to
create instances.

Each version is defined by an instanceTemplate and aname. Every version can appear at most once per instance
group. This field overrides the top-level instanceTemplate
field. Read more about therelationships
between these fields. Exactly one version must leave thetargetSize field unset. That version will be applied to all
remaining instances. For more information, read aboutcanary
updates. |
| `instance_template` | String | The URL of the instance template that is specified for this managed
instance group. The group uses this template to create all new instances
in the managed instance group. The templates for existing instances in the
group do not change unless you run recreateInstances, runapplyUpdatesToInstances, or set the group'supdatePolicy.type to PROACTIVE. |
| `standby_policy` | String | Standby policy for stopped and suspended instances. |
| `distribution_policy` | String | Policy specifying the intended distribution of managed instances across
zones in a regional managed instance group. |
| `fingerprint` | String | Fingerprint of this resource. This field may be used in optimistic locking.
It will be ignored when inserting an InstanceGroupManager. An up-to-date
fingerprint must be provided in order to update the InstanceGroupManager,
otherwise the request will fail with error412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve an InstanceGroupManager. |
| `target_size` | i64 | The target number of running instances for this managed instance group.
You can reduce this number by using the instanceGroupManager
deleteInstances or abandonInstances methods. Resizing the group also
changes this number. |
| `satisfies_pzi` | bool | [Output Only] Reserved for future use. |
| `target_stopped_size` | i64 | The target number of stopped instances for this managed instance group.
This number changes when you: 
   
   - Stop instance using the stopInstances
   method or start instances using the startInstances
   method.
   - Manually change the targetStoppedSize using the update
   method. |
| `target_pools` | Vec<String> | The URLs for all TargetPool resources to which instances in theinstanceGroup field are added. The target pools automatically
apply to all of the instances in the managed instance group. |
| `instance_lifecycle_policy` | String | The repair policy for this managed instance group. |
| `instance_group` | String | [Output Only] The URL of the Instance Group resource. |
| `current_actions` | String | [Output Only] The list of instance actions and the number of instances
in this managed instance group that are scheduled for each of those
actions. |
| `region` | String | [Output Only] The URL of theregion
where the managed instance group resides (for regional resources). |
| `description` | String | An optional description of this resource. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create instance_group_manager
instance_group_manager = provider.compute_api.Instance_group_manager {
    project = "value"  # Project ID for this request.
    zone = "value"  # The name of the zone
where you want to create the managed instance group.
}

# Access instance_group_manager outputs
instance_group_manager_id = instance_group_manager.id
instance_group_manager_named_ports = instance_group_manager.named_ports
instance_group_manager_kind = instance_group_manager.kind
instance_group_manager_stateful_policy = instance_group_manager.stateful_policy
instance_group_manager_status = instance_group_manager.status
instance_group_manager_update_policy = instance_group_manager.update_policy
instance_group_manager_target_suspended_size = instance_group_manager.target_suspended_size
instance_group_manager_instance_flexibility_policy = instance_group_manager.instance_flexibility_policy
instance_group_manager_auto_healing_policies = instance_group_manager.auto_healing_policies
instance_group_manager_zone = instance_group_manager.zone
instance_group_manager_creation_timestamp = instance_group_manager.creation_timestamp
instance_group_manager_satisfies_pzs = instance_group_manager.satisfies_pzs
instance_group_manager_self_link = instance_group_manager.self_link
instance_group_manager_base_instance_name = instance_group_manager.base_instance_name
instance_group_manager_list_managed_instances_results = instance_group_manager.list_managed_instances_results
instance_group_manager_all_instances_config = instance_group_manager.all_instances_config
instance_group_manager_id = instance_group_manager.id
instance_group_manager_resource_policies = instance_group_manager.resource_policies
instance_group_manager_name = instance_group_manager.name
instance_group_manager_versions = instance_group_manager.versions
instance_group_manager_instance_template = instance_group_manager.instance_template
instance_group_manager_standby_policy = instance_group_manager.standby_policy
instance_group_manager_distribution_policy = instance_group_manager.distribution_policy
instance_group_manager_fingerprint = instance_group_manager.fingerprint
instance_group_manager_target_size = instance_group_manager.target_size
instance_group_manager_satisfies_pzi = instance_group_manager.satisfies_pzi
instance_group_manager_target_stopped_size = instance_group_manager.target_stopped_size
instance_group_manager_target_pools = instance_group_manager.target_pools
instance_group_manager_instance_lifecycle_policy = instance_group_manager.instance_lifecycle_policy
instance_group_manager_instance_group = instance_group_manager.instance_group
instance_group_manager_current_actions = instance_group_manager.current_actions
instance_group_manager_region = instance_group_manager.region
instance_group_manager_description = instance_group_manager.description
```

---


### License_code

Returns permissions that a caller has on the specified resource.
 *Caution* This resource is intended
for use only by third-party partners who are creatingCloud Marketplace
images.

**Operations**: ✅ Create ✅ Read

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `permissions` | Vec<String> |  | The set of permissions to check for the 'resource'. Permissions with
wildcards (such as '*' or 'storage.*') are not allowed. |
| `project` | String | ✅ | Project ID for this request. |
| `resource` | String | ✅ | Name or id of the resource for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `transferable` | bool | [Output Only] If true, the license will remain attached when creating
images or snapshots from disks. Otherwise, the license is not transferred. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `kind` | String | [Output Only] Type of resource. Always compute#licenseCode for
licenses. |
| `state` | String | [Output Only] Current state of this License Code. |
| `name` | String | [Output Only] Name of the resource. The name is 1-20 characters long and
must be a valid 64 bit integer. |
| `license_alias` | Vec<String> | [Output Only] URL and description aliases of Licenses with the same
License Code. |
| `description` | String | [Output Only] Description of this License Code. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create license_code
license_code = provider.compute_api.License_code {
    project = "value"  # Project ID for this request.
    resource = "value"  # Name or id of the resource for this request.
}

# Access license_code outputs
license_code_id = license_code.id
license_code_id = license_code.id
license_code_transferable = license_code.transferable
license_code_self_link = license_code.self_link
license_code_kind = license_code.kind
license_code_state = license_code.state
license_code_name = license_code.name
license_code_license_alias = license_code.license_alias
license_code_description = license_code.description
license_code_creation_timestamp = license_code.creation_timestamp
```

---


### Security_policie

Creates a new policy in the specified project using the data included in
the request.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `associations` | Vec<String> |  | A list of associations that belong to this policy. |
| `type` | String |  | The type indicates the intended use of the security policy.
   
   - CLOUD_ARMOR: Cloud Armor backend security policies can
   be configured to filter incoming HTTP requests targeting backend services.
   They filter requests before they hit the origin servers.
   - CLOUD_ARMOR_EDGE: Cloud Armor edge security policies can
   be configured to filter incoming HTTP requests targeting backend services
   (including Cloud CDN-enabled) as well as backend buckets (Cloud Storage).
   They filter requests before the request is served from Google's cache.
   - CLOUD_ARMOR_INTERNAL_SERVICE (preview only): Cloud Armor
   internal service policies can be configured to filter HTTP requests
   targeting services managed by Traffic Director in a service mesh. They
   filter requests before the request is served from the application.

- CLOUD_ARMOR_NETWORK: Cloud Armor network policies
can be configured to filter packets targeting network load balancing
resources such as backend services, target pools, target instances, and
instances with external IPs. They filter requests before the request is
served from the application.


This field can be set only at resource creation time. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `ddos_protection_config` | String |  |  |
| `name` | String |  | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `advanced_options_config` | String |  |  |
| `recaptcha_options_config` | String |  |  |
| `kind` | String |  | [Output only] Type of the resource. Alwayscompute#securityPolicyfor security policies |
| `user_defined_fields` | Vec<String> |  | Definitions of user-defined fields for CLOUD_ARMOR_NETWORK policies. A
user-defined field consists of up to 4 bytes extracted from a fixed offset
in the packet, relative to the IPv4, IPv6, TCP, or UDP header, with an
optional mask to select certain bits. Rules may then specify matching
values for these fields.

Example:

 userDefinedFields:
 - name: "ipv4_fragment_offset"
   base: IPV4
   offset: 6
   size: 2
   mask: "0x1fff" |
| `adaptive_protection_config` | String |  |  |
| `rules` | Vec<String> |  | A list of rules that belong to this policy.
There must always be a default rule which is a rule with priority
2147483647 and match all condition (for the match condition this means
match  "*" for srcIpRanges and for the networkMatch condition every field
must be either match "*" or not set). If no rules are provided when
creating a security policy, a default rule with action "allow" will be
added. |
| `region` | String |  | [Output Only] URL of the region where the regional security policy
resides. This field is not applicable to global security policies. |
| `fingerprint` | String |  | Specifies a fingerprint for this resource, which is essentially a hash of
the metadata's contents and used for optimistic locking. The
fingerprint is initially generated by Compute Engine and changes after
every request to modify or update metadata. You must always provide an
up-to-date fingerprint hash in order to update or change metadata,
otherwise the request will fail with error412 conditionNotMet.

To see the latest fingerprint, make get() request to the
security policy. |
| `labels` | HashMap<String, String> |  | Labels for this resource. These can only be added or modified by thesetLabels method. Each label key/value pair must comply withRFC1035.
Label values may be empty. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `short_name` | String |  | User-provided name of the organization security policy. The name should be
unique in the organization in which the security policy is created. This
should only be used when SecurityPolicyType is CLOUD_ARMOR.
The name must be 1-63 characters long, and comply with
https://www.ietf.org/rfc/rfc1035.txt. Specifically, the name must be 1-63
characters long and match the regular expression
`[a-z]([-a-z0-9]*[a-z0-9])?` which means the first character must be a
lowercase letter, and all following characters must be a dash, lowercase
letter, or digit, except the last character, which cannot be a dash. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `label_fingerprint` | String |  | A fingerprint for the labels being applied to this security policy, which
is essentially a hash of the labels set used for optimistic locking. The
fingerprint is initially generated by Compute Engine and changes after
every request to modify or update labels. You must always provide an
up-to-date fingerprint hash in order to update or change labels.

To see the latest fingerprint, make get() request to the
security policy. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `associations` | Vec<String> | A list of associations that belong to this policy. |
| `type` | String | The type indicates the intended use of the security policy.
   
   - CLOUD_ARMOR: Cloud Armor backend security policies can
   be configured to filter incoming HTTP requests targeting backend services.
   They filter requests before they hit the origin servers.
   - CLOUD_ARMOR_EDGE: Cloud Armor edge security policies can
   be configured to filter incoming HTTP requests targeting backend services
   (including Cloud CDN-enabled) as well as backend buckets (Cloud Storage).
   They filter requests before the request is served from Google's cache.
   - CLOUD_ARMOR_INTERNAL_SERVICE (preview only): Cloud Armor
   internal service policies can be configured to filter HTTP requests
   targeting services managed by Traffic Director in a service mesh. They
   filter requests before the request is served from the application.

- CLOUD_ARMOR_NETWORK: Cloud Armor network policies
can be configured to filter packets targeting network load balancing
resources such as backend services, target pools, target instances, and
instances with external IPs. They filter requests before the request is
served from the application.


This field can be set only at resource creation time. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `ddos_protection_config` | String |  |
| `name` | String | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `advanced_options_config` | String |  |
| `recaptcha_options_config` | String |  |
| `kind` | String | [Output only] Type of the resource. Alwayscompute#securityPolicyfor security policies |
| `user_defined_fields` | Vec<String> | Definitions of user-defined fields for CLOUD_ARMOR_NETWORK policies. A
user-defined field consists of up to 4 bytes extracted from a fixed offset
in the packet, relative to the IPv4, IPv6, TCP, or UDP header, with an
optional mask to select certain bits. Rules may then specify matching
values for these fields.

Example:

 userDefinedFields:
 - name: "ipv4_fragment_offset"
   base: IPV4
   offset: 6
   size: 2
   mask: "0x1fff" |
| `adaptive_protection_config` | String |  |
| `rules` | Vec<String> | A list of rules that belong to this policy.
There must always be a default rule which is a rule with priority
2147483647 and match all condition (for the match condition this means
match  "*" for srcIpRanges and for the networkMatch condition every field
must be either match "*" or not set). If no rules are provided when
creating a security policy, a default rule with action "allow" will be
added. |
| `region` | String | [Output Only] URL of the region where the regional security policy
resides. This field is not applicable to global security policies. |
| `fingerprint` | String | Specifies a fingerprint for this resource, which is essentially a hash of
the metadata's contents and used for optimistic locking. The
fingerprint is initially generated by Compute Engine and changes after
every request to modify or update metadata. You must always provide an
up-to-date fingerprint hash in order to update or change metadata,
otherwise the request will fail with error412 conditionNotMet.

To see the latest fingerprint, make get() request to the
security policy. |
| `labels` | HashMap<String, String> | Labels for this resource. These can only be added or modified by thesetLabels method. Each label key/value pair must comply withRFC1035.
Label values may be empty. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `short_name` | String | User-provided name of the organization security policy. The name should be
unique in the organization in which the security policy is created. This
should only be used when SecurityPolicyType is CLOUD_ARMOR.
The name must be 1-63 characters long, and comply with
https://www.ietf.org/rfc/rfc1035.txt. Specifically, the name must be 1-63
characters long and match the regular expression
`[a-z]([-a-z0-9]*[a-z0-9])?` which means the first character must be a
lowercase letter, and all following characters must be a dash, lowercase
letter, or digit, except the last character, which cannot be a dash. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `label_fingerprint` | String | A fingerprint for the labels being applied to this security policy, which
is essentially a hash of the labels set used for optimistic locking. The
fingerprint is initially generated by Compute Engine and changes after
every request to modify or update labels. You must always provide an
up-to-date fingerprint hash in order to update or change labels.

To see the latest fingerprint, make get() request to the
security policy. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create security_policie
security_policie = provider.compute_api.Security_policie {
    project = "value"  # Project ID for this request.
}

# Access security_policie outputs
security_policie_id = security_policie.id
security_policie_associations = security_policie.associations
security_policie_type = security_policie.type
security_policie_id = security_policie.id
security_policie_creation_timestamp = security_policie.creation_timestamp
security_policie_ddos_protection_config = security_policie.ddos_protection_config
security_policie_name = security_policie.name
security_policie_advanced_options_config = security_policie.advanced_options_config
security_policie_recaptcha_options_config = security_policie.recaptcha_options_config
security_policie_kind = security_policie.kind
security_policie_user_defined_fields = security_policie.user_defined_fields
security_policie_adaptive_protection_config = security_policie.adaptive_protection_config
security_policie_rules = security_policie.rules
security_policie_region = security_policie.region
security_policie_fingerprint = security_policie.fingerprint
security_policie_labels = security_policie.labels
security_policie_self_link = security_policie.self_link
security_policie_short_name = security_policie.short_name
security_policie_description = security_policie.description
security_policie_label_fingerprint = security_policie.label_fingerprint
```

---


### Reservation

Creates a new reservation. For more information, readReserving zonal
resources.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `kind` | String |  | [Output Only] Type of the resource. Alwayscompute#reservations for reservations. |
| `delete_after_duration` | String |  | Duration time relative to reservation creation when Compute Engine will
automatically delete this resource. |
| `delete_at_time` | String |  | Absolute time in future when the reservation will be
 auto-deleted by Compute Engine. Timestamp is represented inRFC3339 text format. |
| `deployment_type` | String |  | Specifies the deployment strategy for this reservation. |
| `advanced_deployment_control` | String |  | Advanced control for cluster management, applicable only to DENSE
deployment type reservations. |
| `resource_policies` | HashMap<String, String> |  | Resource policies to be added to this reservation. The key is defined by
user, and the value is resource policy url. This is to define placement
policy with reservation. |
| `protection_tier` | String |  | Protection tier for the workload which specifies the workload expectations
in the event of infrastructure failures at data center (e.g. power
and/or cooling failures). |
| `aggregate_reservation` | String |  | Reservation for aggregated resources, providing shape flexibility. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `linked_commitments` | Vec<String> |  | [Output Only] Full or partial URL to parent commitments. This field
displays for reservations that are tied to multiple commitments. |
| `enable_emergent_maintenance` | bool |  | Indicates whether Compute Engine allows unplanned maintenance for your VMs;
for example, to fix hardware errors. |
| `self_link` | String |  | [Output Only] Server-defined fully-qualified URL for this resource. |
| `specific_reservation` | String |  | Reservation for instances with specific machine shapes. |
| `status` | String |  | [Output Only] The status of the reservation.
   
   
     - CREATING: Reservation resources are being
       allocated.
     - READY: Reservation resources have been allocated,
       and the reservation is ready for use.
     - DELETING: Reservation deletion is in progress.
     - UPDATING: Reservation update is in progress. |
| `specific_reservation_required` | bool |  | Indicates whether the reservation can be consumed by VMs with affinity
for "any" reservation. If the field is set, then only VMs that target
the reservation by name can consume from this reservation. |
| `reservation_sharing_policy` | String |  | Specify the reservation sharing policy. If unspecified, the reservation
will not be shared with Google Cloud managed services. |
| `commitment` | String |  | [Output Only] Full or partial URL to a parent commitment. This field
displays for reservations that are tied to a commitment. |
| `satisfies_pzs` | bool |  | [Output Only] Reserved for future use. |
| `zone` | String |  | Zone in which the reservation resides. A zone must be provided if the
reservation is created within a commitment. |
| `resource_status` | String |  | [Output Only] Status information for Reservation resource. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `share_settings` | String |  | Specify share-settings to create a shared reservation. This property is
optional. For more information about the syntax and options for this
field and its subfields, see the guide for creating
a shared reservation. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `name` | String |  | The name of the resource, provided by the client when initially creating
the resource. The resource name must be 1-63 characters long, and comply
withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must be
a dash, lowercase letter, or digit, except the last character, which cannot
be a dash. |
| `scheduling_type` | String |  | The type of maintenance for the reservation. |
| `zone` | String | ✅ | Name of the zone for this request. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `kind` | String | [Output Only] Type of the resource. Alwayscompute#reservations for reservations. |
| `delete_after_duration` | String | Duration time relative to reservation creation when Compute Engine will
automatically delete this resource. |
| `delete_at_time` | String | Absolute time in future when the reservation will be
 auto-deleted by Compute Engine. Timestamp is represented inRFC3339 text format. |
| `deployment_type` | String | Specifies the deployment strategy for this reservation. |
| `advanced_deployment_control` | String | Advanced control for cluster management, applicable only to DENSE
deployment type reservations. |
| `resource_policies` | HashMap<String, String> | Resource policies to be added to this reservation. The key is defined by
user, and the value is resource policy url. This is to define placement
policy with reservation. |
| `protection_tier` | String | Protection tier for the workload which specifies the workload expectations
in the event of infrastructure failures at data center (e.g. power
and/or cooling failures). |
| `aggregate_reservation` | String | Reservation for aggregated resources, providing shape flexibility. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `linked_commitments` | Vec<String> | [Output Only] Full or partial URL to parent commitments. This field
displays for reservations that are tied to multiple commitments. |
| `enable_emergent_maintenance` | bool | Indicates whether Compute Engine allows unplanned maintenance for your VMs;
for example, to fix hardware errors. |
| `self_link` | String | [Output Only] Server-defined fully-qualified URL for this resource. |
| `specific_reservation` | String | Reservation for instances with specific machine shapes. |
| `status` | String | [Output Only] The status of the reservation.
   
   
     - CREATING: Reservation resources are being
       allocated.
     - READY: Reservation resources have been allocated,
       and the reservation is ready for use.
     - DELETING: Reservation deletion is in progress.
     - UPDATING: Reservation update is in progress. |
| `specific_reservation_required` | bool | Indicates whether the reservation can be consumed by VMs with affinity
for "any" reservation. If the field is set, then only VMs that target
the reservation by name can consume from this reservation. |
| `reservation_sharing_policy` | String | Specify the reservation sharing policy. If unspecified, the reservation
will not be shared with Google Cloud managed services. |
| `commitment` | String | [Output Only] Full or partial URL to a parent commitment. This field
displays for reservations that are tied to a commitment. |
| `satisfies_pzs` | bool | [Output Only] Reserved for future use. |
| `zone` | String | Zone in which the reservation resides. A zone must be provided if the
reservation is created within a commitment. |
| `resource_status` | String | [Output Only] Status information for Reservation resource. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `share_settings` | String | Specify share-settings to create a shared reservation. This property is
optional. For more information about the syntax and options for this
field and its subfields, see the guide for creating
a shared reservation. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `name` | String | The name of the resource, provided by the client when initially creating
the resource. The resource name must be 1-63 characters long, and comply
withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must be
a dash, lowercase letter, or digit, except the last character, which cannot
be a dash. |
| `scheduling_type` | String | The type of maintenance for the reservation. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create reservation
reservation = provider.compute_api.Reservation {
    zone = "value"  # Name of the zone for this request.
    project = "value"  # Project ID for this request.
}

# Access reservation outputs
reservation_id = reservation.id
reservation_kind = reservation.kind
reservation_delete_after_duration = reservation.delete_after_duration
reservation_delete_at_time = reservation.delete_at_time
reservation_deployment_type = reservation.deployment_type
reservation_advanced_deployment_control = reservation.advanced_deployment_control
reservation_resource_policies = reservation.resource_policies
reservation_protection_tier = reservation.protection_tier
reservation_aggregate_reservation = reservation.aggregate_reservation
reservation_id = reservation.id
reservation_linked_commitments = reservation.linked_commitments
reservation_enable_emergent_maintenance = reservation.enable_emergent_maintenance
reservation_self_link = reservation.self_link
reservation_specific_reservation = reservation.specific_reservation
reservation_status = reservation.status
reservation_specific_reservation_required = reservation.specific_reservation_required
reservation_reservation_sharing_policy = reservation.reservation_sharing_policy
reservation_commitment = reservation.commitment
reservation_satisfies_pzs = reservation.satisfies_pzs
reservation_zone = reservation.zone
reservation_resource_status = reservation.resource_status
reservation_description = reservation.description
reservation_share_settings = reservation.share_settings
reservation_creation_timestamp = reservation.creation_timestamp
reservation_name = reservation.name
reservation_scheduling_type = reservation.scheduling_type
```

---


### Service_attachment

Creates a ServiceAttachment in the specified project in the given scope
using the parameters that are included in the request.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `connection_preference` | String |  | The connection preference of service attachment. The value can be set
to ACCEPT_AUTOMATIC. An ACCEPT_AUTOMATIC
service attachment is one that always accepts the connection from consumer
forwarding rules. |
| `kind` | String |  | [Output Only] Type of the resource. Alwayscompute#serviceAttachment for service attachments. |
| `propagated_connection_limit` | i64 |  | The number of consumer spokes that connected Private Service Connect
endpoints can be propagated to through Network Connectivity Center. This
limit lets the service producer limit how many propagated Private Service
Connect connections can be established to this service attachment from a
single consumer.

If the connection preference of the service attachment is ACCEPT_MANUAL,
the limit applies to each project or network that is listed in the consumer
accept list. If the connection preference of the service attachment is
ACCEPT_AUTOMATIC, the limit applies to each project that contains a
connected endpoint.

If unspecified, the default propagated connection limit is 250. |
| `region` | String |  | [Output Only] URL of the region where the service attachment resides.
This field applies only to the region resource. You must specify this
field as part of the HTTP request URL. It is not settable as a field in
the request body. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `fingerprint` | String |  | Fingerprint of this resource. A hash of the contents stored in this object.
This field is used in optimistic locking. This field will be ignored when
inserting a ServiceAttachment. An up-to-date fingerprint must
be provided in order to patch/update the ServiceAttachment; otherwise, the
request will fail with error 412 conditionNotMet. To see the
latest fingerprint, make a get() request to retrieve the
ServiceAttachment. |
| `id` | String |  | [Output Only] The unique identifier for the resource type. The server
generates this identifier. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `name` | String |  | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `domain_names` | Vec<String> |  | If specified, the domain name will be used during the integration between
the PSC connected endpoints and the Cloud DNS. For example, this is a valid
domain name: "p.mycompany.com.". Current max number of domain names
supported is 1. |
| `metadata` | HashMap<String, String> |  | Metadata of the service attachment. |
| `producer_forwarding_rule` | String |  | The URL of a forwarding rule with loadBalancingScheme INTERNAL* that is
serving the endpoint identified by this service attachment. |
| `enable_proxy_protocol` | bool |  | If true, enable the proxy protocol which is for supplying client TCP/IP
address data in TCP connections that traverse proxies on their way to
destination servers. |
| `reconcile_connections` | bool |  | This flag determines whether a consumer accept/reject list change can
reconcile the statuses of existing ACCEPTED or REJECTED PSC endpoints.
   
   
    -  If false, connection policy update will only affect existing PENDING
    PSC endpoints. Existing ACCEPTED/REJECTED endpoints will remain untouched
    regardless how the connection policy is modified . 
   -  If true,
    update will affect both PENDING and ACCEPTED/REJECTED PSC endpoints. For
    example, an ACCEPTED PSC endpoint will be moved to REJECTED if its project
    is added to the reject list.


For newly created service attachment, this boolean defaults to false. |
| `target_service` | String |  | The URL of a service serving the endpoint identified by this service
attachment. |
| `nat_subnets` | Vec<String> |  | An array of URLs where each entry is the URL of a subnet provided
by the service producer to use for NAT in this service attachment. |
| `connected_endpoints` | Vec<String> |  | [Output Only] An array of connections for all the consumers connected to
this service attachment. |
| `psc_service_attachment_id` | String |  | [Output Only] An 128-bit global unique ID of the PSC service attachment. |
| `consumer_accept_lists` | Vec<String> |  | Specifies which consumer projects or networks are allowed to connect to the
service attachment. Each project or network has a connection limit. A given
service attachment can manage connections at either the project or network
level. Therefore, both the accept and reject lists for a given service
attachment must contain either only projects or only networks or only
endpoints. |
| `consumer_reject_lists` | Vec<String> |  | Specifies a list of projects or networks that are not allowed to connect to
this service attachment. The project can be specified using its project ID
or project number and the network can be specified using its URL. A given
service attachment can manage connections at either the project or network
level. Therefore, both the reject and accept lists for a given service
attachment must contain either only projects or only networks. |
| `region` | String | ✅ | Name of the region of this request. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `connection_preference` | String | The connection preference of service attachment. The value can be set
to ACCEPT_AUTOMATIC. An ACCEPT_AUTOMATIC
service attachment is one that always accepts the connection from consumer
forwarding rules. |
| `kind` | String | [Output Only] Type of the resource. Alwayscompute#serviceAttachment for service attachments. |
| `propagated_connection_limit` | i64 | The number of consumer spokes that connected Private Service Connect
endpoints can be propagated to through Network Connectivity Center. This
limit lets the service producer limit how many propagated Private Service
Connect connections can be established to this service attachment from a
single consumer.

If the connection preference of the service attachment is ACCEPT_MANUAL,
the limit applies to each project or network that is listed in the consumer
accept list. If the connection preference of the service attachment is
ACCEPT_AUTOMATIC, the limit applies to each project that contains a
connected endpoint.

If unspecified, the default propagated connection limit is 250. |
| `region` | String | [Output Only] URL of the region where the service attachment resides.
This field applies only to the region resource. You must specify this
field as part of the HTTP request URL. It is not settable as a field in
the request body. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `fingerprint` | String | Fingerprint of this resource. A hash of the contents stored in this object.
This field is used in optimistic locking. This field will be ignored when
inserting a ServiceAttachment. An up-to-date fingerprint must
be provided in order to patch/update the ServiceAttachment; otherwise, the
request will fail with error 412 conditionNotMet. To see the
latest fingerprint, make a get() request to retrieve the
ServiceAttachment. |
| `id` | String | [Output Only] The unique identifier for the resource type. The server
generates this identifier. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `name` | String | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `domain_names` | Vec<String> | If specified, the domain name will be used during the integration between
the PSC connected endpoints and the Cloud DNS. For example, this is a valid
domain name: "p.mycompany.com.". Current max number of domain names
supported is 1. |
| `metadata` | HashMap<String, String> | Metadata of the service attachment. |
| `producer_forwarding_rule` | String | The URL of a forwarding rule with loadBalancingScheme INTERNAL* that is
serving the endpoint identified by this service attachment. |
| `enable_proxy_protocol` | bool | If true, enable the proxy protocol which is for supplying client TCP/IP
address data in TCP connections that traverse proxies on their way to
destination servers. |
| `reconcile_connections` | bool | This flag determines whether a consumer accept/reject list change can
reconcile the statuses of existing ACCEPTED or REJECTED PSC endpoints.
   
   
    -  If false, connection policy update will only affect existing PENDING
    PSC endpoints. Existing ACCEPTED/REJECTED endpoints will remain untouched
    regardless how the connection policy is modified . 
   -  If true,
    update will affect both PENDING and ACCEPTED/REJECTED PSC endpoints. For
    example, an ACCEPTED PSC endpoint will be moved to REJECTED if its project
    is added to the reject list.


For newly created service attachment, this boolean defaults to false. |
| `target_service` | String | The URL of a service serving the endpoint identified by this service
attachment. |
| `nat_subnets` | Vec<String> | An array of URLs where each entry is the URL of a subnet provided
by the service producer to use for NAT in this service attachment. |
| `connected_endpoints` | Vec<String> | [Output Only] An array of connections for all the consumers connected to
this service attachment. |
| `psc_service_attachment_id` | String | [Output Only] An 128-bit global unique ID of the PSC service attachment. |
| `consumer_accept_lists` | Vec<String> | Specifies which consumer projects or networks are allowed to connect to the
service attachment. Each project or network has a connection limit. A given
service attachment can manage connections at either the project or network
level. Therefore, both the accept and reject lists for a given service
attachment must contain either only projects or only networks or only
endpoints. |
| `consumer_reject_lists` | Vec<String> | Specifies a list of projects or networks that are not allowed to connect to
this service attachment. The project can be specified using its project ID
or project number and the network can be specified using its URL. A given
service attachment can manage connections at either the project or network
level. Therefore, both the reject and accept lists for a given service
attachment must contain either only projects or only networks. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create service_attachment
service_attachment = provider.compute_api.Service_attachment {
    region = "value"  # Name of the region of this request.
    project = "value"  # Project ID for this request.
}

# Access service_attachment outputs
service_attachment_id = service_attachment.id
service_attachment_connection_preference = service_attachment.connection_preference
service_attachment_kind = service_attachment.kind
service_attachment_propagated_connection_limit = service_attachment.propagated_connection_limit
service_attachment_region = service_attachment.region
service_attachment_creation_timestamp = service_attachment.creation_timestamp
service_attachment_fingerprint = service_attachment.fingerprint
service_attachment_id = service_attachment.id
service_attachment_self_link = service_attachment.self_link
service_attachment_description = service_attachment.description
service_attachment_name = service_attachment.name
service_attachment_domain_names = service_attachment.domain_names
service_attachment_metadata = service_attachment.metadata
service_attachment_producer_forwarding_rule = service_attachment.producer_forwarding_rule
service_attachment_enable_proxy_protocol = service_attachment.enable_proxy_protocol
service_attachment_reconcile_connections = service_attachment.reconcile_connections
service_attachment_target_service = service_attachment.target_service
service_attachment_nat_subnets = service_attachment.nat_subnets
service_attachment_connected_endpoints = service_attachment.connected_endpoints
service_attachment_psc_service_attachment_id = service_attachment.psc_service_attachment_id
service_attachment_consumer_accept_lists = service_attachment.consumer_accept_lists
service_attachment_consumer_reject_lists = service_attachment.consumer_reject_lists
```

---


### Node_type

Returns the specified node type.

**Operations**: ✅ Read

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `memory_mb` | i64 | [Output Only] The amount of physical memory available to the node type,
defined in MB. |
| `guest_cpus` | i64 | [Output Only] The number of virtual CPUs that are available to the node
type. |
| `zone` | String | [Output Only] The name of the zone where the node type resides,
such as us-central1-a. |
| `description` | String | [Output Only] An optional textual description of the resource. |
| `deprecated` | String | [Output Only] The deprecation status associated with this node type. |
| `local_ssd_gb` | i64 | [Output Only] Local SSD available to the node type, defined in GB. |
| `kind` | String | [Output Only] The type of the resource. Alwayscompute#nodeType for node types. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `max_vms` | i64 | [Output Only] Maximum number of VMs that can be created for this node type. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `cpu_platform` | String | [Output Only] The CPU platform used by this node type. |
| `name` | String | [Output Only] Name of the resource. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Access node_type outputs
node_type_id = node_type.id
node_type_memory_mb = node_type.memory_mb
node_type_guest_cpus = node_type.guest_cpus
node_type_zone = node_type.zone
node_type_description = node_type.description
node_type_deprecated = node_type.deprecated
node_type_local_ssd_gb = node_type.local_ssd_gb
node_type_kind = node_type.kind
node_type_id = node_type.id
node_type_max_vms = node_type.max_vms
node_type_creation_timestamp = node_type.creation_timestamp
node_type_self_link = node_type.self_link
node_type_cpu_platform = node_type.cpu_platform
node_type_name = node_type.name
```

---


### Interconnect_location

Returns the details for the specified interconnect location. Gets a list of
available interconnect locations by making a list() request.

**Operations**: ✅ Read

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `available_link_types` | Vec<String> | [Output only] List of link types available at this InterconnectLocation,
which can take one of the following values:
   
   - LINK_TYPE_ETHERNET_10G_LR
   - LINK_TYPE_ETHERNET_100G_LR
   - LINK_TYPE_ETHERNET_400G_LR4 |
| `address` | String | [Output Only] The postal address of the Point of Presence, each line in
the address is separated by a newline character. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `available_features` | Vec<String> | [Output only] List of features available at this InterconnectLocation,
which can take one of the following values:
   
   - IF_MACSEC
   - IF_CROSS_SITE_NETWORK |
| `availability_zone` | String | [Output Only] Availability zone for this InterconnectLocation. Within a
metropolitan area (metro), maintenance will not be simultaneously scheduled
in more than one availability zone.  Example: "zone1" or "zone2". |
| `kind` | String | [Output Only] Type of the resource. Alwayscompute#interconnectLocation for interconnect locations. |
| `peeringdb_facility_id` | String | [Output Only] The peeringdb identifier for this facility (corresponding
with a netfac type in peeringdb). |
| `single_region_production_critical_peer_locations` | Vec<String> | [Output Only] URLs of the other locations that can pair up with this
location to support Single-Region 99.99% SLA. E.g. iad-zone1-1 and
iad-zone2-5467 are Single-Region 99.99% peer locations of each other. |
| `status` | String | [Output Only] The status of this InterconnectLocation, which can take one
of the following values:
   
   - CLOSED: The InterconnectLocation is closed and is unavailable for
   provisioning new Interconnects.
   - AVAILABLE: The InterconnectLocation is available for provisioning new
   Interconnects. |
| `facility_provider_facility_id` | String | [Output Only] A provider-assigned Identifier for this facility (e.g.,
Ashburn-DC1). |
| `region_infos` | Vec<String> | [Output Only] A list of InterconnectLocation.RegionInfo objects, that
describe parameters pertaining to the relation between this
InterconnectLocation and various Google Cloud regions. |
| `city` | String | [Output Only] Metropolitan area designator that indicates which city an
interconnect is located.
For example: "Chicago, IL", "Amsterdam, Netherlands". |
| `continent` | String | [Output Only] Continent for this location, which can take one of the
following values:
   
   - AFRICA
   - ASIA_PAC
   - EUROPE
   - NORTH_AMERICA
   - SOUTH_AMERICA |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `name` | String | [Output Only] Name of the resource. |
| `supports_pzs` | bool | [Output Only] Reserved for future use. |
| `cross_site_interconnect_infos` | Vec<String> | [Output Only] A list of InterconnectLocation.CrossSiteInterconnectInfo
objects, that describe where Cross-Site Interconnect wires may connect to
from this location and associated connection parameters. Cross-Site
Interconnect isn't allowed to locations which are not listed. |
| `facility_provider` | String | [Output Only] The name of the provider for this facility (e.g., EQUINIX). |
| `description` | String | [Output Only] An optional description of the resource. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Access interconnect_location outputs
interconnect_location_id = interconnect_location.id
interconnect_location_available_link_types = interconnect_location.available_link_types
interconnect_location_address = interconnect_location.address
interconnect_location_creation_timestamp = interconnect_location.creation_timestamp
interconnect_location_available_features = interconnect_location.available_features
interconnect_location_availability_zone = interconnect_location.availability_zone
interconnect_location_kind = interconnect_location.kind
interconnect_location_peeringdb_facility_id = interconnect_location.peeringdb_facility_id
interconnect_location_single_region_production_critical_peer_locations = interconnect_location.single_region_production_critical_peer_locations
interconnect_location_status = interconnect_location.status
interconnect_location_facility_provider_facility_id = interconnect_location.facility_provider_facility_id
interconnect_location_region_infos = interconnect_location.region_infos
interconnect_location_city = interconnect_location.city
interconnect_location_continent = interconnect_location.continent
interconnect_location_self_link = interconnect_location.self_link
interconnect_location_id = interconnect_location.id
interconnect_location_name = interconnect_location.name
interconnect_location_supports_pzs = interconnect_location.supports_pzs
interconnect_location_cross_site_interconnect_infos = interconnect_location.cross_site_interconnect_infos
interconnect_location_facility_provider = interconnect_location.facility_provider
interconnect_location_description = interconnect_location.description
```

---


### Autoscaler

Creates an autoscaler in the specified project using the data
included in the request.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `scaling_schedule_status` | HashMap<String, String> |  | [Output Only] Status information of existing scaling schedules. |
| `autoscaling_policy` | String |  | The configuration parameters for the autoscaling algorithm. You can define
one or more signals for an autoscaler: cpuUtilization,customMetricUtilizations, andloadBalancingUtilization.

If none of these are specified, the default will be to autoscale based oncpuUtilization to 0.6 or 60%. |
| `name` | String |  | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `kind` | String |  | [Output Only] Type of the resource. Always compute#autoscaler
for autoscalers. |
| `status_details` | Vec<String> |  | [Output Only] Human-readable details about the current state of the
autoscaler. Read the documentation forCommonly
returned status messages for examples of status messages you might
encounter. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `recommended_size` | i64 |  | [Output Only] Target recommended MIG size (number of instances) computed by
autoscaler. Autoscaler calculates the recommended MIG size even when the
autoscaling policy mode is different from ON. This field is empty when
autoscaler is not connected to an existing managed instance group or
autoscaler did not generate its prediction. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `target` | String |  | URL of the managed instance group that this autoscaler will scale. This
field is required when creating an autoscaler. |
| `zone` | String |  | [Output Only] URL of thezone
where the instance group resides (for autoscalers living in zonal scope). |
| `status` | String |  | [Output Only] The status of the autoscaler configuration. Current set of
possible values:
   
   - PENDING:
     Autoscaler backend hasn't read new/updated configuration.
   - DELETING:
     Configuration is being deleted.
   - ACTIVE:
     Configuration is acknowledged to be effective. Some warnings might
     be present in the statusDetails field.
   - ERROR:
     Configuration has errors. Actionable for users. Details are present in
     the statusDetails field.


New values might be added in the future. |
| `region` | String |  | [Output Only] URL of theregion
where the instance group resides (for autoscalers living in regional
scope). |
| `zone` | String | ✅ | Name of the zone for this request. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `scaling_schedule_status` | HashMap<String, String> | [Output Only] Status information of existing scaling schedules. |
| `autoscaling_policy` | String | The configuration parameters for the autoscaling algorithm. You can define
one or more signals for an autoscaler: cpuUtilization,customMetricUtilizations, andloadBalancingUtilization.

If none of these are specified, the default will be to autoscale based oncpuUtilization to 0.6 or 60%. |
| `name` | String | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `kind` | String | [Output Only] Type of the resource. Always compute#autoscaler
for autoscalers. |
| `status_details` | Vec<String> | [Output Only] Human-readable details about the current state of the
autoscaler. Read the documentation forCommonly
returned status messages for examples of status messages you might
encounter. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `recommended_size` | i64 | [Output Only] Target recommended MIG size (number of instances) computed by
autoscaler. Autoscaler calculates the recommended MIG size even when the
autoscaling policy mode is different from ON. This field is empty when
autoscaler is not connected to an existing managed instance group or
autoscaler did not generate its prediction. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `target` | String | URL of the managed instance group that this autoscaler will scale. This
field is required when creating an autoscaler. |
| `zone` | String | [Output Only] URL of thezone
where the instance group resides (for autoscalers living in zonal scope). |
| `status` | String | [Output Only] The status of the autoscaler configuration. Current set of
possible values:
   
   - PENDING:
     Autoscaler backend hasn't read new/updated configuration.
   - DELETING:
     Configuration is being deleted.
   - ACTIVE:
     Configuration is acknowledged to be effective. Some warnings might
     be present in the statusDetails field.
   - ERROR:
     Configuration has errors. Actionable for users. Details are present in
     the statusDetails field.


New values might be added in the future. |
| `region` | String | [Output Only] URL of theregion
where the instance group resides (for autoscalers living in regional
scope). |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create autoscaler
autoscaler = provider.compute_api.Autoscaler {
    zone = "value"  # Name of the zone for this request.
    project = "value"  # Project ID for this request.
}

# Access autoscaler outputs
autoscaler_id = autoscaler.id
autoscaler_description = autoscaler.description
autoscaler_creation_timestamp = autoscaler.creation_timestamp
autoscaler_scaling_schedule_status = autoscaler.scaling_schedule_status
autoscaler_autoscaling_policy = autoscaler.autoscaling_policy
autoscaler_name = autoscaler.name
autoscaler_kind = autoscaler.kind
autoscaler_status_details = autoscaler.status_details
autoscaler_self_link = autoscaler.self_link
autoscaler_recommended_size = autoscaler.recommended_size
autoscaler_id = autoscaler.id
autoscaler_target = autoscaler.target
autoscaler_zone = autoscaler.zone
autoscaler_status = autoscaler.status
autoscaler_region = autoscaler.region
```

---


### Image_family_view

Returns the latest image that is part of an image family, is not
deprecated and is rolled out in the specified zone.

**Operations**: ✅ Read

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `image` | String | The latest image that is part of the specified image family in the
requested location, and that is not deprecated. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Access image_family_view outputs
image_family_view_id = image_family_view.id
image_family_view_image = image_family_view.image
```

---


### Snapshot_setting

Get snapshot settings.

**Operations**: ✅ Read ✅ Update

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `storage_location` | String |  | Policy of which storage location is going to be resolved, and additional
data that particularizes how the policy is going to be carried out. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `storage_location` | String | Policy of which storage location is going to be resolved, and additional
data that particularizes how the policy is going to be carried out. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Access snapshot_setting outputs
snapshot_setting_id = snapshot_setting.id
snapshot_setting_storage_location = snapshot_setting.storage_location
```

---


### Backend_bucket

Creates a BackendBucket resource in the specified project using
the data included in the request.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `id` | String |  | [Output Only] Unique identifier for the resource; defined by the server. |
| `used_by` | Vec<String> |  | [Output Only] List of resources referencing that backend bucket. |
| `load_balancing_scheme` | String |  | The value can only be INTERNAL_MANAGED for cross-region internal layer 7
load balancer.

If loadBalancingScheme is not specified, the backend bucket can be used by
classic global external load balancers, or global application external load
balancers, or both. |
| `name` | String |  | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `params` | String |  | Input only. [Input Only] Additional params passed with the request, but not persisted
as part of resource payload. |
| `custom_response_headers` | Vec<String> |  | Headers that the Application Load Balancer should add to proxied responses. |
| `kind` | String |  | Type of the resource. |
| `edge_security_policy` | String |  | [Output Only] The resource URL for the edge security policy associated with
this backend bucket. |
| `cdn_policy` | String |  | Cloud CDN configuration for this BackendBucket. |
| `compression_mode` | String |  | Compress text responses using Brotli or gzip compression, based on
the client's Accept-Encoding header. |
| `bucket_name` | String |  | Cloud Storage bucket name. |
| `description` | String |  | An optional textual description of the resource; provided by the client
when the resource is created. |
| `enable_cdn` | bool |  | If true, enable Cloud CDN for this BackendBucket. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `id` | String | [Output Only] Unique identifier for the resource; defined by the server. |
| `used_by` | Vec<String> | [Output Only] List of resources referencing that backend bucket. |
| `load_balancing_scheme` | String | The value can only be INTERNAL_MANAGED for cross-region internal layer 7
load balancer.

If loadBalancingScheme is not specified, the backend bucket can be used by
classic global external load balancers, or global application external load
balancers, or both. |
| `name` | String | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `params` | String | Input only. [Input Only] Additional params passed with the request, but not persisted
as part of resource payload. |
| `custom_response_headers` | Vec<String> | Headers that the Application Load Balancer should add to proxied responses. |
| `kind` | String | Type of the resource. |
| `edge_security_policy` | String | [Output Only] The resource URL for the edge security policy associated with
this backend bucket. |
| `cdn_policy` | String | Cloud CDN configuration for this BackendBucket. |
| `compression_mode` | String | Compress text responses using Brotli or gzip compression, based on
the client's Accept-Encoding header. |
| `bucket_name` | String | Cloud Storage bucket name. |
| `description` | String | An optional textual description of the resource; provided by the client
when the resource is created. |
| `enable_cdn` | bool | If true, enable Cloud CDN for this BackendBucket. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create backend_bucket
backend_bucket = provider.compute_api.Backend_bucket {
    project = "value"  # Project ID for this request.
}

# Access backend_bucket outputs
backend_bucket_id = backend_bucket.id
backend_bucket_creation_timestamp = backend_bucket.creation_timestamp
backend_bucket_id = backend_bucket.id
backend_bucket_used_by = backend_bucket.used_by
backend_bucket_load_balancing_scheme = backend_bucket.load_balancing_scheme
backend_bucket_name = backend_bucket.name
backend_bucket_self_link = backend_bucket.self_link
backend_bucket_params = backend_bucket.params
backend_bucket_custom_response_headers = backend_bucket.custom_response_headers
backend_bucket_kind = backend_bucket.kind
backend_bucket_edge_security_policy = backend_bucket.edge_security_policy
backend_bucket_cdn_policy = backend_bucket.cdn_policy
backend_bucket_compression_mode = backend_bucket.compression_mode
backend_bucket_bucket_name = backend_bucket.bucket_name
backend_bucket_description = backend_bucket.description
backend_bucket_enable_cdn = backend_bucket.enable_cdn
```

---


### Storage_pool

Creates a storage pool in the specified project using the data
in the request.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `name` | String |  | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?`
which means the first character must be a lowercase letter, and all
following characters must be a dash, lowercase letter, or digit, except
the last character, which cannot be a dash. |
| `performance_provisioning_type` | String |  | Provisioning type of the performance-related parameters of the pool,
such as throughput and IOPS. |
| `pool_provisioned_throughput` | String |  | Provisioned throughput of the storage pool in MiB/s. Only relevant if the
storage pool type is hyperdisk-balanced or hyperdisk-throughput. |
| `kind` | String |  | [Output Only] Type of the resource. Always compute#storagePool
for storage pools. |
| `capacity_provisioning_type` | String |  | Provisioning type of the byte capacity of the pool. |
| `resource_status` | String |  | [Output Only] Status information for the storage pool resource. |
| `labels` | HashMap<String, String> |  | Labels to apply to this storage pool. These can be later modified by
the setLabels method. |
| `state` | String |  | [Output Only] The status of storage pool creation.
   
   
     - CREATING: Storage pool is provisioning.
     storagePool.
     - FAILED: Storage pool creation failed.
     - READY: Storage pool is ready for use.
     - DELETING: Storage pool is deleting. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `label_fingerprint` | String |  | A fingerprint for the labels being applied to this storage pool, which is
essentially a hash of the labels set used for optimistic locking. The
fingerprint is initially generated by Compute Engine and changes after
every request to modify or update labels. You must always provide an
up-to-date fingerprint hash in order to update or change labels,
otherwise the request will fail with error412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve a storage pool. |
| `self_link_with_id` | String |  | [Output Only] Server-defined URL for this resource's resource id. |
| `zone` | String |  | [Output Only] URL of the zone where the storage pool resides.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `self_link` | String |  | [Output Only] Server-defined fully-qualified URL for this resource. |
| `pool_provisioned_iops` | String |  | Provisioned IOPS of the storage pool. Only relevant if the storage pool
type is hyperdisk-balanced. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `storage_pool_type` | String |  | Type of the storage pool. |
| `status` | String |  | [Output Only] Status information for the storage pool resource. |
| `pool_provisioned_capacity_gb` | String |  | Size of the storage pool in GiB. For more information about the size
limits, see https://cloud.google.com/compute/docs/disks/storage-pools. |
| `project` | String | ✅ | Project ID for this request. |
| `zone` | String | ✅ | The name of the zone for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `name` | String | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?`
which means the first character must be a lowercase letter, and all
following characters must be a dash, lowercase letter, or digit, except
the last character, which cannot be a dash. |
| `performance_provisioning_type` | String | Provisioning type of the performance-related parameters of the pool,
such as throughput and IOPS. |
| `pool_provisioned_throughput` | String | Provisioned throughput of the storage pool in MiB/s. Only relevant if the
storage pool type is hyperdisk-balanced or hyperdisk-throughput. |
| `kind` | String | [Output Only] Type of the resource. Always compute#storagePool
for storage pools. |
| `capacity_provisioning_type` | String | Provisioning type of the byte capacity of the pool. |
| `resource_status` | String | [Output Only] Status information for the storage pool resource. |
| `labels` | HashMap<String, String> | Labels to apply to this storage pool. These can be later modified by
the setLabels method. |
| `state` | String | [Output Only] The status of storage pool creation.
   
   
     - CREATING: Storage pool is provisioning.
     storagePool.
     - FAILED: Storage pool creation failed.
     - READY: Storage pool is ready for use.
     - DELETING: Storage pool is deleting. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `label_fingerprint` | String | A fingerprint for the labels being applied to this storage pool, which is
essentially a hash of the labels set used for optimistic locking. The
fingerprint is initially generated by Compute Engine and changes after
every request to modify or update labels. You must always provide an
up-to-date fingerprint hash in order to update or change labels,
otherwise the request will fail with error412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve a storage pool. |
| `self_link_with_id` | String | [Output Only] Server-defined URL for this resource's resource id. |
| `zone` | String | [Output Only] URL of the zone where the storage pool resides.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `self_link` | String | [Output Only] Server-defined fully-qualified URL for this resource. |
| `pool_provisioned_iops` | String | Provisioned IOPS of the storage pool. Only relevant if the storage pool
type is hyperdisk-balanced. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `storage_pool_type` | String | Type of the storage pool. |
| `status` | String | [Output Only] Status information for the storage pool resource. |
| `pool_provisioned_capacity_gb` | String | Size of the storage pool in GiB. For more information about the size
limits, see https://cloud.google.com/compute/docs/disks/storage-pools. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create storage_pool
storage_pool = provider.compute_api.Storage_pool {
    project = "value"  # Project ID for this request.
    zone = "value"  # The name of the zone for this request.
}

# Access storage_pool outputs
storage_pool_id = storage_pool.id
storage_pool_id = storage_pool.id
storage_pool_name = storage_pool.name
storage_pool_performance_provisioning_type = storage_pool.performance_provisioning_type
storage_pool_pool_provisioned_throughput = storage_pool.pool_provisioned_throughput
storage_pool_kind = storage_pool.kind
storage_pool_capacity_provisioning_type = storage_pool.capacity_provisioning_type
storage_pool_resource_status = storage_pool.resource_status
storage_pool_labels = storage_pool.labels
storage_pool_state = storage_pool.state
storage_pool_description = storage_pool.description
storage_pool_label_fingerprint = storage_pool.label_fingerprint
storage_pool_self_link_with_id = storage_pool.self_link_with_id
storage_pool_zone = storage_pool.zone
storage_pool_self_link = storage_pool.self_link
storage_pool_pool_provisioned_iops = storage_pool.pool_provisioned_iops
storage_pool_creation_timestamp = storage_pool.creation_timestamp
storage_pool_storage_pool_type = storage_pool.storage_pool_type
storage_pool_status = storage_pool.status
storage_pool_pool_provisioned_capacity_gb = storage_pool.pool_provisioned_capacity_gb
```

---


### Packet_mirroring

Creates a PacketMirroring resource in the specified project and region
using the data included in the request.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `priority` | i64 |  | The priority of applying this configuration. Priority is used to break ties
in cases where there is more than one matching rule. In the case of two
rules that apply for a given Instance, the one with the lowest-numbered
priority value wins.

Default value is 1000. Valid range is 0 through 65535. |
| `mirrored_resources` | String |  | PacketMirroring mirroredResourceInfos.
MirroredResourceInfo specifies a set of mirrored VM instances, subnetworks
and/or tags for which traffic from/to all VM instances will be mirrored. |
| `enable` | String |  | Indicates whether or not this packet mirroring takes effect.
If set to FALSE, this packet mirroring policy will not be enforced on the
network.

The default is TRUE. |
| `kind` | String |  | [Output Only] Type of the resource. Alwayscompute#packetMirroring for packet mirrorings. |
| `filter` | String |  | Filter for mirrored traffic. If unspecified, all IPv4 traffic is mirrored. |
| `collector_ilb` | String |  | The Forwarding Rule resource of typeloadBalancingScheme=INTERNAL that will be used as collector
for mirrored traffic.
The specified forwarding rule must have isMirroringCollector
set to true. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `name` | String |  | Name of the resource; provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must be
a dash, lowercase letter, or digit, except the last character, which cannot
be a dash. |
| `region` | String |  | [Output Only] URI of the region where the packetMirroring resides. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `network` | String |  | Specifies the mirrored VPC network. Only packets in this network will be
mirrored. All mirrored VMs should have a NIC in the given network.
All mirrored subnetworks should belong to the given network. |
| `project` | String | ✅ | Project ID for this request. |
| `region` | String | ✅ | Name of the region for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `priority` | i64 | The priority of applying this configuration. Priority is used to break ties
in cases where there is more than one matching rule. In the case of two
rules that apply for a given Instance, the one with the lowest-numbered
priority value wins.

Default value is 1000. Valid range is 0 through 65535. |
| `mirrored_resources` | String | PacketMirroring mirroredResourceInfos.
MirroredResourceInfo specifies a set of mirrored VM instances, subnetworks
and/or tags for which traffic from/to all VM instances will be mirrored. |
| `enable` | String | Indicates whether or not this packet mirroring takes effect.
If set to FALSE, this packet mirroring policy will not be enforced on the
network.

The default is TRUE. |
| `kind` | String | [Output Only] Type of the resource. Alwayscompute#packetMirroring for packet mirrorings. |
| `filter` | String | Filter for mirrored traffic. If unspecified, all IPv4 traffic is mirrored. |
| `collector_ilb` | String | The Forwarding Rule resource of typeloadBalancingScheme=INTERNAL that will be used as collector
for mirrored traffic.
The specified forwarding rule must have isMirroringCollector
set to true. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `name` | String | Name of the resource; provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must be
a dash, lowercase letter, or digit, except the last character, which cannot
be a dash. |
| `region` | String | [Output Only] URI of the region where the packetMirroring resides. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `network` | String | Specifies the mirrored VPC network. Only packets in this network will be
mirrored. All mirrored VMs should have a NIC in the given network.
All mirrored subnetworks should belong to the given network. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create packet_mirroring
packet_mirroring = provider.compute_api.Packet_mirroring {
    project = "value"  # Project ID for this request.
    region = "value"  # Name of the region for this request.
}

# Access packet_mirroring outputs
packet_mirroring_id = packet_mirroring.id
packet_mirroring_description = packet_mirroring.description
packet_mirroring_priority = packet_mirroring.priority
packet_mirroring_mirrored_resources = packet_mirroring.mirrored_resources
packet_mirroring_enable = packet_mirroring.enable
packet_mirroring_kind = packet_mirroring.kind
packet_mirroring_filter = packet_mirroring.filter
packet_mirroring_collector_ilb = packet_mirroring.collector_ilb
packet_mirroring_id = packet_mirroring.id
packet_mirroring_creation_timestamp = packet_mirroring.creation_timestamp
packet_mirroring_name = packet_mirroring.name
packet_mirroring_region = packet_mirroring.region
packet_mirroring_self_link = packet_mirroring.self_link
packet_mirroring_network = packet_mirroring.network
```

---


### Node_group

Creates a NodeGroup resource in the specified project using the data
included in the request.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `maintenance_interval` | String |  | Specifies the frequency of planned maintenance events. The accepted values
are: `AS_NEEDED` and `RECURRENT`. |
| `autoscaling_policy` | String |  | Specifies how autoscaling should behave. |
| `maintenance_window` | String |  |  |
| `node_template` | String |  | URL of the node template to create the node group from. |
| `fingerprint` | String |  |  |
| `status` | String |  |  |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `share_settings` | String |  | Share-settings for the node group |
| `location_hint` | String |  | An opaque location hint used to place the Node close to other
resources.
This field is for use by internal tools that use the public API.
The location hint here on the NodeGroup overrides any location_hint
present in the NodeTemplate. |
| `name` | String |  | The name of the resource, provided by the client when initially creating
the resource. The resource name must be 1-63 characters long, and comply
withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must be
a dash, lowercase letter, or digit, except the last character, which cannot
be a dash. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `size` | i64 |  | [Output Only] The total number of nodes in the node group. |
| `maintenance_policy` | String |  | Specifies how to handle instances when a node in the group undergoes
maintenance. Set to one of: DEFAULT,RESTART_IN_PLACE, or MIGRATE_WITHIN_NODE_GROUP.
The default value is DEFAULT. For more information, see
Maintenance policies. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `zone` | String |  | [Output Only] The name of the zone where the node group resides,
such as us-central1-a. |
| `kind` | String |  | [Output Only] The type of the resource. Alwayscompute#nodeGroup for node group. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `zone` | String | ✅ | The name of the zone for this request. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `maintenance_interval` | String | Specifies the frequency of planned maintenance events. The accepted values
are: `AS_NEEDED` and `RECURRENT`. |
| `autoscaling_policy` | String | Specifies how autoscaling should behave. |
| `maintenance_window` | String |  |
| `node_template` | String | URL of the node template to create the node group from. |
| `fingerprint` | String |  |
| `status` | String |  |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `share_settings` | String | Share-settings for the node group |
| `location_hint` | String | An opaque location hint used to place the Node close to other
resources.
This field is for use by internal tools that use the public API.
The location hint here on the NodeGroup overrides any location_hint
present in the NodeTemplate. |
| `name` | String | The name of the resource, provided by the client when initially creating
the resource. The resource name must be 1-63 characters long, and comply
withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must be
a dash, lowercase letter, or digit, except the last character, which cannot
be a dash. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `size` | i64 | [Output Only] The total number of nodes in the node group. |
| `maintenance_policy` | String | Specifies how to handle instances when a node in the group undergoes
maintenance. Set to one of: DEFAULT,RESTART_IN_PLACE, or MIGRATE_WITHIN_NODE_GROUP.
The default value is DEFAULT. For more information, see
Maintenance policies. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `zone` | String | [Output Only] The name of the zone where the node group resides,
such as us-central1-a. |
| `kind` | String | [Output Only] The type of the resource. Alwayscompute#nodeGroup for node group. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create node_group
node_group = provider.compute_api.Node_group {
    zone = "value"  # The name of the zone for this request.
    project = "value"  # Project ID for this request.
}

# Access node_group outputs
node_group_id = node_group.id
node_group_maintenance_interval = node_group.maintenance_interval
node_group_autoscaling_policy = node_group.autoscaling_policy
node_group_maintenance_window = node_group.maintenance_window
node_group_node_template = node_group.node_template
node_group_fingerprint = node_group.fingerprint
node_group_status = node_group.status
node_group_id = node_group.id
node_group_share_settings = node_group.share_settings
node_group_location_hint = node_group.location_hint
node_group_name = node_group.name
node_group_self_link = node_group.self_link
node_group_size = node_group.size
node_group_maintenance_policy = node_group.maintenance_policy
node_group_creation_timestamp = node_group.creation_timestamp
node_group_zone = node_group.zone
node_group_kind = node_group.kind
node_group_description = node_group.description
```

---


### Url_map

Creates a UrlMap resource in the specified project using
the data included in the request.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `host_rules` | Vec<String> |  | The list of host rules to use against the URL. |
| `name` | String |  | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `default_route_action` | String |  | defaultRouteAction takes effect when none of the 
hostRules match. The load balancer performs advanced routing
actions, such as URL rewrites and header transformations, before forwarding
the request to the selected backend.


Only one of defaultUrlRedirect, defaultService
or defaultRouteAction.weightedBackendService can be set.


 URL maps for classic Application Load Balancers only support
the urlRewrite action within defaultRouteAction.


defaultRouteAction has no effect when the URL map is bound
to a target gRPC proxy that has the validateForProxyless field
set to true. |
| `fingerprint` | String |  | Fingerprint of this resource. A hash of the contents stored in this object.
This field is used in optimistic locking. This field is ignored when
inserting a UrlMap. An up-to-date fingerprint must be provided
in order to update the UrlMap, otherwise the request will
fail with error 412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve a UrlMap. |
| `region` | String |  | [Output Only] URL of the region where the regional URL map resides.
This field is not applicable to global URL maps.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `header_action` | String |  | Specifies changes to request and response headers that need to take effect
for the selected backendService.

The headerAction specified here take effect afterheaderAction specified under pathMatcher.

headerAction is not supported for load balancers
that have
their loadBalancingScheme set to EXTERNAL.

Not supported when the URL map is bound to a target gRPC proxy that
has validateForProxyless field set to true. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `tests` | Vec<String> |  | The list of expected URL mapping tests. Request to update theUrlMap succeeds only if all test cases pass. You can specify a
maximum of 100 tests per UrlMap.

Not supported when the URL map is bound to a target gRPC proxy that
has validateForProxyless field set to true. |
| `default_service` | String |  | The full or partial URL of the defaultService resource to
which traffic is directed if none of the hostRules match.
If defaultRouteAction is also specified, advanced
routing actions, such as URL rewrites, take effect before sending the
request to the backend.


Only one of defaultUrlRedirect, defaultService
or defaultRouteAction.weightedBackendService can be set.


defaultService has no effect when the URL map is bound
to a target gRPC proxy that has the validateForProxyless field
set to true. |
| `path_matchers` | Vec<String> |  | The list of named PathMatchers to use against the URL. |
| `default_custom_error_response_policy` | String |  | defaultCustomErrorResponsePolicy specifies how the Load
Balancer returns error responses when BackendServiceorBackendBucket responds with an error. 

This policy takes
effect at the load balancer level and applies only when no policy has been
defined for the error code at lower levels like PathMatcher, RouteRule and
PathRule within this UrlMap. 

For example, consider a UrlMap with the
following configuration:
   
   
     - defaultCustomErrorResponsePolicy containing policies for
     responding to 5xx and 4xx errors
     - A PathMatcher configured for *.example.com has
     defaultCustomErrorResponsePolicy for 4xx.

If a request for http://www.example.com/ encounters a404, the policy inpathMatcher.defaultCustomErrorResponsePolicy will be enforced.
When the request for http://www.example.com/ encounters a502, the policy inUrlMap.defaultCustomErrorResponsePolicy will be enforced. When
a request that does not match any host in *.example.com such
as http://www.myotherexample.com/, encounters a404, UrlMap.defaultCustomErrorResponsePolicy
takes effect. 

When used in conjunction withdefaultRouteAction.retryPolicy, retries take precedence. Only
once all retries are exhausted, thedefaultCustomErrorResponsePolicy is applied. While attempting
a retry, if load balancer is successful in reaching the
service, the defaultCustomErrorResponsePolicy is ignored and
the response from the service is returned to the client.

defaultCustomErrorResponsePolicy is supported only for
global external Application Load Balancers. |
| `default_url_redirect` | String |  | When none of the specified hostRules match, the request
is redirected to a URL specified by defaultUrlRedirect.


Only one of defaultUrlRedirect, defaultService
or defaultRouteAction.weightedBackendService can be set.


Not supported when the URL map is bound to a target gRPC proxy. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `kind` | String |  | [Output Only] Type of the resource. Always compute#urlMaps for
url maps. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `host_rules` | Vec<String> | The list of host rules to use against the URL. |
| `name` | String | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `default_route_action` | String | defaultRouteAction takes effect when none of the 
hostRules match. The load balancer performs advanced routing
actions, such as URL rewrites and header transformations, before forwarding
the request to the selected backend.


Only one of defaultUrlRedirect, defaultService
or defaultRouteAction.weightedBackendService can be set.


 URL maps for classic Application Load Balancers only support
the urlRewrite action within defaultRouteAction.


defaultRouteAction has no effect when the URL map is bound
to a target gRPC proxy that has the validateForProxyless field
set to true. |
| `fingerprint` | String | Fingerprint of this resource. A hash of the contents stored in this object.
This field is used in optimistic locking. This field is ignored when
inserting a UrlMap. An up-to-date fingerprint must be provided
in order to update the UrlMap, otherwise the request will
fail with error 412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve a UrlMap. |
| `region` | String | [Output Only] URL of the region where the regional URL map resides.
This field is not applicable to global URL maps.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `header_action` | String | Specifies changes to request and response headers that need to take effect
for the selected backendService.

The headerAction specified here take effect afterheaderAction specified under pathMatcher.

headerAction is not supported for load balancers
that have
their loadBalancingScheme set to EXTERNAL.

Not supported when the URL map is bound to a target gRPC proxy that
has validateForProxyless field set to true. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `tests` | Vec<String> | The list of expected URL mapping tests. Request to update theUrlMap succeeds only if all test cases pass. You can specify a
maximum of 100 tests per UrlMap.

Not supported when the URL map is bound to a target gRPC proxy that
has validateForProxyless field set to true. |
| `default_service` | String | The full or partial URL of the defaultService resource to
which traffic is directed if none of the hostRules match.
If defaultRouteAction is also specified, advanced
routing actions, such as URL rewrites, take effect before sending the
request to the backend.


Only one of defaultUrlRedirect, defaultService
or defaultRouteAction.weightedBackendService can be set.


defaultService has no effect when the URL map is bound
to a target gRPC proxy that has the validateForProxyless field
set to true. |
| `path_matchers` | Vec<String> | The list of named PathMatchers to use against the URL. |
| `default_custom_error_response_policy` | String | defaultCustomErrorResponsePolicy specifies how the Load
Balancer returns error responses when BackendServiceorBackendBucket responds with an error. 

This policy takes
effect at the load balancer level and applies only when no policy has been
defined for the error code at lower levels like PathMatcher, RouteRule and
PathRule within this UrlMap. 

For example, consider a UrlMap with the
following configuration:
   
   
     - defaultCustomErrorResponsePolicy containing policies for
     responding to 5xx and 4xx errors
     - A PathMatcher configured for *.example.com has
     defaultCustomErrorResponsePolicy for 4xx.

If a request for http://www.example.com/ encounters a404, the policy inpathMatcher.defaultCustomErrorResponsePolicy will be enforced.
When the request for http://www.example.com/ encounters a502, the policy inUrlMap.defaultCustomErrorResponsePolicy will be enforced. When
a request that does not match any host in *.example.com such
as http://www.myotherexample.com/, encounters a404, UrlMap.defaultCustomErrorResponsePolicy
takes effect. 

When used in conjunction withdefaultRouteAction.retryPolicy, retries take precedence. Only
once all retries are exhausted, thedefaultCustomErrorResponsePolicy is applied. While attempting
a retry, if load balancer is successful in reaching the
service, the defaultCustomErrorResponsePolicy is ignored and
the response from the service is returned to the client.

defaultCustomErrorResponsePolicy is supported only for
global external Application Load Balancers. |
| `default_url_redirect` | String | When none of the specified hostRules match, the request
is redirected to a URL specified by defaultUrlRedirect.


Only one of defaultUrlRedirect, defaultService
or defaultRouteAction.weightedBackendService can be set.


Not supported when the URL map is bound to a target gRPC proxy. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `kind` | String | [Output Only] Type of the resource. Always compute#urlMaps for
url maps. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create url_map
url_map = provider.compute_api.Url_map {
    project = "value"  # Project ID for this request.
}

# Access url_map outputs
url_map_id = url_map.id
url_map_host_rules = url_map.host_rules
url_map_name = url_map.name
url_map_default_route_action = url_map.default_route_action
url_map_fingerprint = url_map.fingerprint
url_map_region = url_map.region
url_map_creation_timestamp = url_map.creation_timestamp
url_map_header_action = url_map.header_action
url_map_description = url_map.description
url_map_tests = url_map.tests
url_map_default_service = url_map.default_service
url_map_path_matchers = url_map.path_matchers
url_map_default_custom_error_response_policy = url_map.default_custom_error_response_policy
url_map_default_url_redirect = url_map.default_url_redirect
url_map_id = url_map.id
url_map_self_link = url_map.self_link
url_map_kind = url_map.kind
```

---


### Disk

Creates a persistent disk in the specified project using the data
in the request. You can create a disk from a source
(sourceImage, sourceSnapshot, orsourceDisk) or create an empty 500 GB data disk by
omitting all properties. You can also create a disk that is larger than
the default size by specifying the sizeGb property.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `size_gb` | String |  | Size, in GB, of the persistent disk. You can specify
this field when creating a persistent disk using thesourceImage, sourceSnapshot, orsourceDisk parameter, or specify it alone to create an empty
persistent disk.

If you specify this field along with a source, the value ofsizeGb must not be less than the size of the
source.
Acceptable values are greater than 0. |
| `status` | String |  | [Output Only] The status of disk creation.
   
   
     - CREATING: Disk is provisioning.
     - RESTORING: Source data is being copied into the
     disk.
     - FAILED: Disk creation failed.
     - READY: Disk is ready for use.
     - DELETING: Disk is deleting. |
| `type` | String |  | URL of the disk type resource describing which disk type to use to create
the disk. Provide this when creating the disk. For example:projects/project/zones/zone/diskTypes/pd-ssd. See Persistent disk
types. |
| `async_secondary_disks` | HashMap<String, String> |  | [Output Only] A list of disks this disk is asynchronously replicated to. |
| `last_detach_timestamp` | String |  | [Output Only] Last detach timestamp inRFC3339
text format. |
| `region` | String |  | [Output Only] URL of the region where the disk resides. Only applicable for
regional resources.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `licenses` | Vec<String> |  | A list of publicly visible licenses. Reserved for Google's use. |
| `last_attach_timestamp` | String |  | [Output Only] Last attach timestamp inRFC3339
text format. |
| `guest_os_features` | Vec<String> |  | A list of features to enable on the guest operating system. Applicable
only for bootable images. Read
Enabling guest operating system features to see a list of available
options. |
| `source_consistency_group_policy_id` | String |  | [Output Only] ID of the DiskConsistencyGroupPolicy for a secondary disk
that was created using a consistency group. |
| `zone` | String |  | [Output Only] URL of the zone where the disk resides.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `provisioned_iops` | String |  | Indicates how many IOPS to provision for the disk. This sets the number
of I/O operations per second that the disk can handle. Values must be
between 10,000 and 120,000. For more details, see theExtreme persistent
disk documentation. |
| `source_image_encryption_key` | String |  | Thecustomer-supplied
encryption key of the source image. Required if the source image is
protected by a customer-supplied encryption key. |
| `access_mode` | String |  | The access mode of the disk.
   
   
     - READ_WRITE_SINGLE: The default AccessMode, means the
     disk can be attached to single instance in RW mode.
     - READ_WRITE_MANY: The AccessMode means the disk can be
     attached to multiple instances in RW mode.
     - READ_ONLY_MANY: The AccessMode means the disk can be
     attached to multiple instances in RO mode.


The AccessMode is only valid for Hyperdisk disk types. |
| `async_primary_disk` | String |  | Disk asynchronously replicated into this disk. |
| `source_image` | String |  | The source image used to create this disk. If the source image is
deleted, this field will not be set.

To create a disk with one of the public operating system images, specify
the image by its family name. For example, specifyfamily/debian-9 to use the latest Debian 9 image:

projects/debian-cloud/global/images/family/debian-9


Alternatively, use a specific version of a public operating system image:

projects/debian-cloud/global/images/debian-9-stretch-vYYYYMMDD


To create a disk with a custom image that you created, specify the
image name in the following format:

global/images/my-custom-image


You can also specify a custom image by its image family, which returns
the latest version of the image in that family. Replace the image name
with family/family-name:

global/images/family/my-image-family |
| `source_snapshot_id` | String |  | [Output Only] The unique ID of the snapshot used to create this disk. This
value identifies the exact snapshot that was used to create this persistent
disk. For example, if you created the persistent disk from a snapshot that
was later deleted and recreated under the same name, the source snapshot ID
would identify the exact version of the snapshot that was used. |
| `name` | String |  | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?`
which means the first character must be a lowercase letter, and all
following characters must be a dash, lowercase letter, or digit, except
the last character, which cannot be a dash. |
| `options` | String |  | Internal use only. |
| `source_disk_id` | String |  | [Output Only] The unique ID of the disk used to create this disk. This
value identifies the exact disk that was used to create this persistent
disk. For example, if you created the persistent disk from a disk that
was later deleted and recreated under the same name, the source disk ID
would identify the exact version of the disk that was used. |
| `labels` | HashMap<String, String> |  | Labels to apply to this disk. These can be later modified by
the setLabels method. |
| `license_codes` | Vec<String> |  | Integer license codes indicating which licenses are attached to this disk. |
| `disk_encryption_key` | String |  | Encrypts the disk using a
customer-supplied encryption key or a 
customer-managed encryption key.

Encryption keys do not protect access to metadata of the disk.

After you encrypt a disk with a customer-supplied key, you must provide the
same key if you use the disk later. For example, to create a disk snapshot,
to create a disk image, to create a machine image, or to attach the disk to
a virtual machine.

After you encrypt a disk with a customer-managed key, thediskEncryptionKey.kmsKeyName is set to a key *version*
name once the disk is created. The disk is encrypted with this version of
the key. In the response, diskEncryptionKey.kmsKeyName appears
in the following format:

"diskEncryptionKey.kmsKeyName":
"projects/kms_project_id/locations/region/keyRings/
key_region/cryptoKeys/key
/cryptoKeysVersions/version

If you do not provide an encryption key when creating the disk, then the 
disk is encrypted using an automatically generated key and you don't need 
to provide a key to use the disk later. |
| `enable_confidential_compute` | bool |  | Whether this disk is using confidential compute mode. |
| `source_instant_snapshot` | String |  | The source instant snapshot used to create this disk. You can provide
this as a partial or full URL to the resource. For example, the following
are valid values:
   
   
     - https://www.googleapis.com/compute/v1/projects/project/zones/zone/instantSnapshots/instantSnapshot
     - projects/project/zones/zone/instantSnapshots/instantSnapshot 
   - zones/zone/instantSnapshots/instantSnapshot |
| `source_snapshot` | String |  | The source snapshot used to create this disk. You can provide this as a
partial or full URL to the resource. For example, the following are valid
values:
   
   
     - https://www.googleapis.com/compute/v1/projects/project/global/snapshots/snapshot 
   - projects/project/global/snapshots/snapshot
     - global/snapshots/snapshot |
| `source_disk` | String |  | The source disk used to create this disk. You can provide this as a
partial or full URL to the resource. For example, the following are valid
values:
   
   
     - 
       https://www.googleapis.com/compute/v1/projects/project/zones/zone/disks/disk
     
     - 
       https://www.googleapis.com/compute/v1/projects/project/regions/region/disks/disk
     
     - 
       projects/project/zones/zone/disks/disk
     
     - 
       projects/project/regions/region/disks/disk
     
     - 
       zones/zone/disks/disk
     
     - 
       regions/region/disks/disk |
| `source_storage_object` | String |  | The full Google Cloud Storage URI where the disk image is stored. This file
must be a gzip-compressed tarball whose name ends in .tar.gz or virtual
machine disk whose name ends in vmdk. Valid URIs may start with gs:// or
https://storage.googleapis.com/. This flag is not optimized for creating
multiple disks from a source storage object. To create many disks from a
source storage object, use gcloud compute images
import instead. |
| `physical_block_size_bytes` | String |  | Physical block size of the persistent disk, in bytes.
If not present in a request, a default value is used.
The currently supported size is 4096, other sizes may be added in
the future.
If an unsupported value is requested, the error message will list
the supported values for the caller's project. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `source_consistency_group_policy` | String |  | [Output Only] URL of the DiskConsistencyGroupPolicy for a secondary disk
that was created using a consistency group. |
| `self_link` | String |  | [Output Only] Server-defined fully-qualified URL for this resource. |
| `source_image_id` | String |  | [Output Only] The ID value of the image used to create this disk. This
value identifies the exact image that was used to create this persistent
disk. For example, if you created the persistent disk from an image that
was later deleted and recreated under the same name, the source image ID
would identify the exact version of the image that was used. |
| `location_hint` | String |  | An opaque location hint used to place the disk close to other resources.
This field is for use by internal tools that use the public API. |
| `label_fingerprint` | String |  | A fingerprint for the labels being applied to this disk, which is
essentially a hash of the labels set used for optimistic locking. The
fingerprint is initially generated by Compute Engine and changes after
every request to modify or update labels. You must always provide an
up-to-date fingerprint hash in order to update or change labels,
otherwise the request will fail with error412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve a disk. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `params` | String |  | Input only. [Input Only] Additional params passed with the request, but not persisted
as part of resource payload. |
| `provisioned_throughput` | String |  | Indicates how much throughput to provision for the disk. This sets the
number of throughput mb per second that the disk can handle. Values must be
greater than or equal to 1. |
| `resource_policies` | Vec<String> |  | Resource policies applied to this disk for automatic snapshot creations. |
| `replica_zones` | Vec<String> |  | URLs of the zones where the disk should be replicated to. Only applicable
for regional resources. |
| `storage_pool` | String |  | The storage pool in which the new disk is created. You can provide
this as a partial or full URL to the resource. For example, the following
are valid values:
   
   
     - https://www.googleapis.com/compute/v1/projects/project/zones/zone/storagePools/storagePool
     - projects/project/zones/zone/storagePools/storagePool 
   - zones/zone/storagePools/storagePool |
| `source_instant_snapshot_id` | String |  | [Output Only] The unique ID of the instant snapshot used to create this
disk. This value identifies the exact instant snapshot that was used to
create this persistent disk. For example, if you created the persistent
disk from an instant snapshot that was later deleted and recreated under
the same name, the source instant snapshot ID would identify the exact
version of the instant snapshot that was used. |
| `users` | Vec<String> |  | [Output Only] Links to the users of the disk (attached instances)
in form:projects/project/zones/zone/instances/instance |
| `kind` | String |  | [Output Only] Type of the resource. Always compute#disk for
disks. |
| `satisfies_pzs` | bool |  | [Output Only] Reserved for future use. |
| `source_snapshot_encryption_key` | String |  | Thecustomer-supplied
encryption key of the source snapshot. Required if the source snapshot
is protected by a customer-supplied encryption key. |
| `resource_status` | String |  | [Output Only] Status information for the disk resource. |
| `satisfies_pzi` | bool |  | Output only. Reserved for future use. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `architecture` | String |  | The architecture of the disk. Valid values are
ARM64 or X86_64. |
| `project` | String | ✅ | Project ID for this request. |
| `zone` | String | ✅ | The name of the zone for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `size_gb` | String | Size, in GB, of the persistent disk. You can specify
this field when creating a persistent disk using thesourceImage, sourceSnapshot, orsourceDisk parameter, or specify it alone to create an empty
persistent disk.

If you specify this field along with a source, the value ofsizeGb must not be less than the size of the
source.
Acceptable values are greater than 0. |
| `status` | String | [Output Only] The status of disk creation.
   
   
     - CREATING: Disk is provisioning.
     - RESTORING: Source data is being copied into the
     disk.
     - FAILED: Disk creation failed.
     - READY: Disk is ready for use.
     - DELETING: Disk is deleting. |
| `type` | String | URL of the disk type resource describing which disk type to use to create
the disk. Provide this when creating the disk. For example:projects/project/zones/zone/diskTypes/pd-ssd. See Persistent disk
types. |
| `async_secondary_disks` | HashMap<String, String> | [Output Only] A list of disks this disk is asynchronously replicated to. |
| `last_detach_timestamp` | String | [Output Only] Last detach timestamp inRFC3339
text format. |
| `region` | String | [Output Only] URL of the region where the disk resides. Only applicable for
regional resources.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `licenses` | Vec<String> | A list of publicly visible licenses. Reserved for Google's use. |
| `last_attach_timestamp` | String | [Output Only] Last attach timestamp inRFC3339
text format. |
| `guest_os_features` | Vec<String> | A list of features to enable on the guest operating system. Applicable
only for bootable images. Read
Enabling guest operating system features to see a list of available
options. |
| `source_consistency_group_policy_id` | String | [Output Only] ID of the DiskConsistencyGroupPolicy for a secondary disk
that was created using a consistency group. |
| `zone` | String | [Output Only] URL of the zone where the disk resides.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `provisioned_iops` | String | Indicates how many IOPS to provision for the disk. This sets the number
of I/O operations per second that the disk can handle. Values must be
between 10,000 and 120,000. For more details, see theExtreme persistent
disk documentation. |
| `source_image_encryption_key` | String | Thecustomer-supplied
encryption key of the source image. Required if the source image is
protected by a customer-supplied encryption key. |
| `access_mode` | String | The access mode of the disk.
   
   
     - READ_WRITE_SINGLE: The default AccessMode, means the
     disk can be attached to single instance in RW mode.
     - READ_WRITE_MANY: The AccessMode means the disk can be
     attached to multiple instances in RW mode.
     - READ_ONLY_MANY: The AccessMode means the disk can be
     attached to multiple instances in RO mode.


The AccessMode is only valid for Hyperdisk disk types. |
| `async_primary_disk` | String | Disk asynchronously replicated into this disk. |
| `source_image` | String | The source image used to create this disk. If the source image is
deleted, this field will not be set.

To create a disk with one of the public operating system images, specify
the image by its family name. For example, specifyfamily/debian-9 to use the latest Debian 9 image:

projects/debian-cloud/global/images/family/debian-9


Alternatively, use a specific version of a public operating system image:

projects/debian-cloud/global/images/debian-9-stretch-vYYYYMMDD


To create a disk with a custom image that you created, specify the
image name in the following format:

global/images/my-custom-image


You can also specify a custom image by its image family, which returns
the latest version of the image in that family. Replace the image name
with family/family-name:

global/images/family/my-image-family |
| `source_snapshot_id` | String | [Output Only] The unique ID of the snapshot used to create this disk. This
value identifies the exact snapshot that was used to create this persistent
disk. For example, if you created the persistent disk from a snapshot that
was later deleted and recreated under the same name, the source snapshot ID
would identify the exact version of the snapshot that was used. |
| `name` | String | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?`
which means the first character must be a lowercase letter, and all
following characters must be a dash, lowercase letter, or digit, except
the last character, which cannot be a dash. |
| `options` | String | Internal use only. |
| `source_disk_id` | String | [Output Only] The unique ID of the disk used to create this disk. This
value identifies the exact disk that was used to create this persistent
disk. For example, if you created the persistent disk from a disk that
was later deleted and recreated under the same name, the source disk ID
would identify the exact version of the disk that was used. |
| `labels` | HashMap<String, String> | Labels to apply to this disk. These can be later modified by
the setLabels method. |
| `license_codes` | Vec<String> | Integer license codes indicating which licenses are attached to this disk. |
| `disk_encryption_key` | String | Encrypts the disk using a
customer-supplied encryption key or a 
customer-managed encryption key.

Encryption keys do not protect access to metadata of the disk.

After you encrypt a disk with a customer-supplied key, you must provide the
same key if you use the disk later. For example, to create a disk snapshot,
to create a disk image, to create a machine image, or to attach the disk to
a virtual machine.

After you encrypt a disk with a customer-managed key, thediskEncryptionKey.kmsKeyName is set to a key *version*
name once the disk is created. The disk is encrypted with this version of
the key. In the response, diskEncryptionKey.kmsKeyName appears
in the following format:

"diskEncryptionKey.kmsKeyName":
"projects/kms_project_id/locations/region/keyRings/
key_region/cryptoKeys/key
/cryptoKeysVersions/version

If you do not provide an encryption key when creating the disk, then the 
disk is encrypted using an automatically generated key and you don't need 
to provide a key to use the disk later. |
| `enable_confidential_compute` | bool | Whether this disk is using confidential compute mode. |
| `source_instant_snapshot` | String | The source instant snapshot used to create this disk. You can provide
this as a partial or full URL to the resource. For example, the following
are valid values:
   
   
     - https://www.googleapis.com/compute/v1/projects/project/zones/zone/instantSnapshots/instantSnapshot
     - projects/project/zones/zone/instantSnapshots/instantSnapshot 
   - zones/zone/instantSnapshots/instantSnapshot |
| `source_snapshot` | String | The source snapshot used to create this disk. You can provide this as a
partial or full URL to the resource. For example, the following are valid
values:
   
   
     - https://www.googleapis.com/compute/v1/projects/project/global/snapshots/snapshot 
   - projects/project/global/snapshots/snapshot
     - global/snapshots/snapshot |
| `source_disk` | String | The source disk used to create this disk. You can provide this as a
partial or full URL to the resource. For example, the following are valid
values:
   
   
     - 
       https://www.googleapis.com/compute/v1/projects/project/zones/zone/disks/disk
     
     - 
       https://www.googleapis.com/compute/v1/projects/project/regions/region/disks/disk
     
     - 
       projects/project/zones/zone/disks/disk
     
     - 
       projects/project/regions/region/disks/disk
     
     - 
       zones/zone/disks/disk
     
     - 
       regions/region/disks/disk |
| `source_storage_object` | String | The full Google Cloud Storage URI where the disk image is stored. This file
must be a gzip-compressed tarball whose name ends in .tar.gz or virtual
machine disk whose name ends in vmdk. Valid URIs may start with gs:// or
https://storage.googleapis.com/. This flag is not optimized for creating
multiple disks from a source storage object. To create many disks from a
source storage object, use gcloud compute images
import instead. |
| `physical_block_size_bytes` | String | Physical block size of the persistent disk, in bytes.
If not present in a request, a default value is used.
The currently supported size is 4096, other sizes may be added in
the future.
If an unsupported value is requested, the error message will list
the supported values for the caller's project. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `source_consistency_group_policy` | String | [Output Only] URL of the DiskConsistencyGroupPolicy for a secondary disk
that was created using a consistency group. |
| `self_link` | String | [Output Only] Server-defined fully-qualified URL for this resource. |
| `source_image_id` | String | [Output Only] The ID value of the image used to create this disk. This
value identifies the exact image that was used to create this persistent
disk. For example, if you created the persistent disk from an image that
was later deleted and recreated under the same name, the source image ID
would identify the exact version of the image that was used. |
| `location_hint` | String | An opaque location hint used to place the disk close to other resources.
This field is for use by internal tools that use the public API. |
| `label_fingerprint` | String | A fingerprint for the labels being applied to this disk, which is
essentially a hash of the labels set used for optimistic locking. The
fingerprint is initially generated by Compute Engine and changes after
every request to modify or update labels. You must always provide an
up-to-date fingerprint hash in order to update or change labels,
otherwise the request will fail with error412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve a disk. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `params` | String | Input only. [Input Only] Additional params passed with the request, but not persisted
as part of resource payload. |
| `provisioned_throughput` | String | Indicates how much throughput to provision for the disk. This sets the
number of throughput mb per second that the disk can handle. Values must be
greater than or equal to 1. |
| `resource_policies` | Vec<String> | Resource policies applied to this disk for automatic snapshot creations. |
| `replica_zones` | Vec<String> | URLs of the zones where the disk should be replicated to. Only applicable
for regional resources. |
| `storage_pool` | String | The storage pool in which the new disk is created. You can provide
this as a partial or full URL to the resource. For example, the following
are valid values:
   
   
     - https://www.googleapis.com/compute/v1/projects/project/zones/zone/storagePools/storagePool
     - projects/project/zones/zone/storagePools/storagePool 
   - zones/zone/storagePools/storagePool |
| `source_instant_snapshot_id` | String | [Output Only] The unique ID of the instant snapshot used to create this
disk. This value identifies the exact instant snapshot that was used to
create this persistent disk. For example, if you created the persistent
disk from an instant snapshot that was later deleted and recreated under
the same name, the source instant snapshot ID would identify the exact
version of the instant snapshot that was used. |
| `users` | Vec<String> | [Output Only] Links to the users of the disk (attached instances)
in form:projects/project/zones/zone/instances/instance |
| `kind` | String | [Output Only] Type of the resource. Always compute#disk for
disks. |
| `satisfies_pzs` | bool | [Output Only] Reserved for future use. |
| `source_snapshot_encryption_key` | String | Thecustomer-supplied
encryption key of the source snapshot. Required if the source snapshot
is protected by a customer-supplied encryption key. |
| `resource_status` | String | [Output Only] Status information for the disk resource. |
| `satisfies_pzi` | bool | Output only. Reserved for future use. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `architecture` | String | The architecture of the disk. Valid values are
ARM64 or X86_64. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create disk
disk = provider.compute_api.Disk {
    project = "value"  # Project ID for this request.
    zone = "value"  # The name of the zone for this request.
}

# Access disk outputs
disk_id = disk.id
disk_size_gb = disk.size_gb
disk_status = disk.status
disk_type = disk.type
disk_async_secondary_disks = disk.async_secondary_disks
disk_last_detach_timestamp = disk.last_detach_timestamp
disk_region = disk.region
disk_licenses = disk.licenses
disk_last_attach_timestamp = disk.last_attach_timestamp
disk_guest_os_features = disk.guest_os_features
disk_source_consistency_group_policy_id = disk.source_consistency_group_policy_id
disk_zone = disk.zone
disk_provisioned_iops = disk.provisioned_iops
disk_source_image_encryption_key = disk.source_image_encryption_key
disk_access_mode = disk.access_mode
disk_async_primary_disk = disk.async_primary_disk
disk_source_image = disk.source_image
disk_source_snapshot_id = disk.source_snapshot_id
disk_name = disk.name
disk_options = disk.options
disk_source_disk_id = disk.source_disk_id
disk_labels = disk.labels
disk_license_codes = disk.license_codes
disk_disk_encryption_key = disk.disk_encryption_key
disk_enable_confidential_compute = disk.enable_confidential_compute
disk_source_instant_snapshot = disk.source_instant_snapshot
disk_source_snapshot = disk.source_snapshot
disk_source_disk = disk.source_disk
disk_source_storage_object = disk.source_storage_object
disk_physical_block_size_bytes = disk.physical_block_size_bytes
disk_id = disk.id
disk_source_consistency_group_policy = disk.source_consistency_group_policy
disk_self_link = disk.self_link
disk_source_image_id = disk.source_image_id
disk_location_hint = disk.location_hint
disk_label_fingerprint = disk.label_fingerprint
disk_description = disk.description
disk_params = disk.params
disk_provisioned_throughput = disk.provisioned_throughput
disk_resource_policies = disk.resource_policies
disk_replica_zones = disk.replica_zones
disk_storage_pool = disk.storage_pool
disk_source_instant_snapshot_id = disk.source_instant_snapshot_id
disk_users = disk.users
disk_kind = disk.kind
disk_satisfies_pzs = disk.satisfies_pzs
disk_source_snapshot_encryption_key = disk.source_snapshot_encryption_key
disk_resource_status = disk.resource_status
disk_satisfies_pzi = disk.satisfies_pzi
disk_creation_timestamp = disk.creation_timestamp
disk_architecture = disk.architecture
```

---


### Target_pool

Creates a target pool in the specified project and region using
the data included in the request.

**Operations**: ✅ Create ✅ Read ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `backup_pool` | String |  | The server-defined URL for the resource. This field is applicable only when
the containing target pool is serving a forwarding rule as the primary
pool, and its failoverRatio field is properly set to a value
between [0, 1].backupPool and failoverRatio together define
the fallback behavior of the primary target pool: if the ratio of the
healthy instances in the primary pool is at or belowfailoverRatio, traffic arriving at the load-balanced
IP will be directed to the backup pool.

In case where failoverRatio and backupPool
are not set, or all the instances in the backup pool are unhealthy,
the traffic will be directed back to the primary pool in the "force"
mode, where traffic will be spread to the healthy instances with the
best effort, or to all instances when no instance is healthy. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `region` | String |  | [Output Only] URL of the region where the target pool resides. |
| `session_affinity` | String |  | Session affinity option, must be one of the following values: 
NONE: Connections from the same client IP may go to any
    instance in the pool. 
CLIENT_IP: Connections from the same client IP will go
    to the same instance in
    the pool while that instance remains healthy. 
CLIENT_IP_PROTO: Connections from the same client IP
    with the same IP protocol will go to the same instance in the
    pool while that instance remains healthy. |
| `kind` | String |  | [Output Only] Type of the resource. Always compute#targetPool
for target pools. |
| `instances` | Vec<String> |  | A list of resource URLs to the virtual machine instances serving this pool.
They must live in zones contained in the same region as this pool. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `failover_ratio` | f64 |  | This field is applicable only when the containing target pool is serving a
forwarding rule as the primary pool (i.e., not as a backup pool to some
other target pool). The value of the field must be in [0, 1].

If set, backupPool must also be set. They together define
the fallback behavior of the primary target pool: if the ratio of the
healthy instances in the primary pool is at or below this number,
traffic arriving at the load-balanced IP will be directed to the
backup pool.

In case where failoverRatio is not set or all the
instances in the backup pool are unhealthy, the traffic will be
directed back to the primary pool in the "force" mode, where traffic
will be spread to the healthy instances with the
best effort, or to all instances when no instance is healthy. |
| `health_checks` | Vec<String> |  | The URL of the HttpHealthCheck resource. A member instance in this
pool is considered healthy if and only if the health checks pass.
Only legacy HttpHealthChecks are supported. Only one health check may be
specified. |
| `name` | String |  | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `security_policy` | String |  | [Output Only] The resource URL for the security policy associated with this
target pool. |
| `project` | String | ✅ | Project ID for this request. |
| `region` | String | ✅ | Name of the region scoping this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `backup_pool` | String | The server-defined URL for the resource. This field is applicable only when
the containing target pool is serving a forwarding rule as the primary
pool, and its failoverRatio field is properly set to a value
between [0, 1].backupPool and failoverRatio together define
the fallback behavior of the primary target pool: if the ratio of the
healthy instances in the primary pool is at or belowfailoverRatio, traffic arriving at the load-balanced
IP will be directed to the backup pool.

In case where failoverRatio and backupPool
are not set, or all the instances in the backup pool are unhealthy,
the traffic will be directed back to the primary pool in the "force"
mode, where traffic will be spread to the healthy instances with the
best effort, or to all instances when no instance is healthy. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `region` | String | [Output Only] URL of the region where the target pool resides. |
| `session_affinity` | String | Session affinity option, must be one of the following values: 
NONE: Connections from the same client IP may go to any
    instance in the pool. 
CLIENT_IP: Connections from the same client IP will go
    to the same instance in
    the pool while that instance remains healthy. 
CLIENT_IP_PROTO: Connections from the same client IP
    with the same IP protocol will go to the same instance in the
    pool while that instance remains healthy. |
| `kind` | String | [Output Only] Type of the resource. Always compute#targetPool
for target pools. |
| `instances` | Vec<String> | A list of resource URLs to the virtual machine instances serving this pool.
They must live in zones contained in the same region as this pool. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `failover_ratio` | f64 | This field is applicable only when the containing target pool is serving a
forwarding rule as the primary pool (i.e., not as a backup pool to some
other target pool). The value of the field must be in [0, 1].

If set, backupPool must also be set. They together define
the fallback behavior of the primary target pool: if the ratio of the
healthy instances in the primary pool is at or below this number,
traffic arriving at the load-balanced IP will be directed to the
backup pool.

In case where failoverRatio is not set or all the
instances in the backup pool are unhealthy, the traffic will be
directed back to the primary pool in the "force" mode, where traffic
will be spread to the healthy instances with the
best effort, or to all instances when no instance is healthy. |
| `health_checks` | Vec<String> | The URL of the HttpHealthCheck resource. A member instance in this
pool is considered healthy if and only if the health checks pass.
Only legacy HttpHealthChecks are supported. Only one health check may be
specified. |
| `name` | String | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `security_policy` | String | [Output Only] The resource URL for the security policy associated with this
target pool. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create target_pool
target_pool = provider.compute_api.Target_pool {
    project = "value"  # Project ID for this request.
    region = "value"  # Name of the region scoping this request.
}

# Access target_pool outputs
target_pool_id = target_pool.id
target_pool_backup_pool = target_pool.backup_pool
target_pool_description = target_pool.description
target_pool_creation_timestamp = target_pool.creation_timestamp
target_pool_id = target_pool.id
target_pool_region = target_pool.region
target_pool_session_affinity = target_pool.session_affinity
target_pool_kind = target_pool.kind
target_pool_instances = target_pool.instances
target_pool_self_link = target_pool.self_link
target_pool_failover_ratio = target_pool.failover_ratio
target_pool_health_checks = target_pool.health_checks
target_pool_name = target_pool.name
target_pool_security_policy = target_pool.security_policy
```

---


### Region_health_check

Creates a HealthCheck resource in the specified project using the data
included in the request.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `https_health_check` | String |  |  |
| `http_health_check` | String |  |  |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `source_regions` | Vec<String> |  | The list of cloud regions from which health checks are performed. If any
regions are specified, then exactly 3 regions should be specified. The
region names must be valid names of Google Cloud regions.
This can only be set for global health check.
If this list is non-empty, then there are restrictions
on what other health check fields are supported and what other resources
can use this health check:
   
   - SSL, HTTP2, and GRPC protocols are not supported.
   - The TCP request field is not supported.
   - The proxyHeader field for HTTP, HTTPS, and TCP is not
   supported.
   - The checkIntervalSec field must be at least 30.
   - The health check cannot be used with BackendService nor with managed
   instance group auto-healing. |
| `kind` | String |  | Type of the resource. |
| `healthy_threshold` | i64 |  | A so-far unhealthy instance will be marked healthy after this
many consecutive successes. The default value is 2. |
| `grpc_health_check` | String |  |  |
| `timeout_sec` | i64 |  | How long (in seconds) to wait before claiming failure. The default value is
5 seconds. It is invalid for timeoutSec to have greater
value than checkIntervalSec. |
| `ssl_health_check` | String |  |  |
| `name` | String |  | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
For example, a name that is 1-63 characters long, matches the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?`, and otherwise complies with
RFC1035. This regular expression describes a name where the first
character is a lowercase letter, and all following characters are a dash,
lowercase letter, or digit, except the last character, which isn't a dash. |
| `grpc_tls_health_check` | String |  |  |
| `type` | String |  | Specifies the type of the healthCheck, either TCP,SSL, HTTP, HTTPS,HTTP2 or GRPC. Exactly one of the
protocol-specific health check fields must be specified, which must matchtype field. |
| `unhealthy_threshold` | i64 |  | A so-far healthy instance will be marked unhealthy after this many
consecutive failures. The default value is 2. |
| `http2_health_check` | String |  |  |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp in3339
text format. |
| `region` | String |  | [Output Only] Region where the health check resides.  Not applicable to
global health checks. |
| `log_config` | String |  | Configure logging on this health check. |
| `tcp_health_check` | String |  |  |
| `check_interval_sec` | i64 |  | How often (in seconds) to send a health check. The default value is 5
seconds. |
| `project` | String | ✅ | Project ID for this request. |
| `region` | String | ✅ | Name of the region scoping this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `https_health_check` | String |  |
| `http_health_check` | String |  |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `source_regions` | Vec<String> | The list of cloud regions from which health checks are performed. If any
regions are specified, then exactly 3 regions should be specified. The
region names must be valid names of Google Cloud regions.
This can only be set for global health check.
If this list is non-empty, then there are restrictions
on what other health check fields are supported and what other resources
can use this health check:
   
   - SSL, HTTP2, and GRPC protocols are not supported.
   - The TCP request field is not supported.
   - The proxyHeader field for HTTP, HTTPS, and TCP is not
   supported.
   - The checkIntervalSec field must be at least 30.
   - The health check cannot be used with BackendService nor with managed
   instance group auto-healing. |
| `kind` | String | Type of the resource. |
| `healthy_threshold` | i64 | A so-far unhealthy instance will be marked healthy after this
many consecutive successes. The default value is 2. |
| `grpc_health_check` | String |  |
| `timeout_sec` | i64 | How long (in seconds) to wait before claiming failure. The default value is
5 seconds. It is invalid for timeoutSec to have greater
value than checkIntervalSec. |
| `ssl_health_check` | String |  |
| `name` | String | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
For example, a name that is 1-63 characters long, matches the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?`, and otherwise complies with
RFC1035. This regular expression describes a name where the first
character is a lowercase letter, and all following characters are a dash,
lowercase letter, or digit, except the last character, which isn't a dash. |
| `grpc_tls_health_check` | String |  |
| `type` | String | Specifies the type of the healthCheck, either TCP,SSL, HTTP, HTTPS,HTTP2 or GRPC. Exactly one of the
protocol-specific health check fields must be specified, which must matchtype field. |
| `unhealthy_threshold` | i64 | A so-far healthy instance will be marked unhealthy after this many
consecutive failures. The default value is 2. |
| `http2_health_check` | String |  |
| `creation_timestamp` | String | [Output Only] Creation timestamp in3339
text format. |
| `region` | String | [Output Only] Region where the health check resides.  Not applicable to
global health checks. |
| `log_config` | String | Configure logging on this health check. |
| `tcp_health_check` | String |  |
| `check_interval_sec` | i64 | How often (in seconds) to send a health check. The default value is 5
seconds. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create region_health_check
region_health_check = provider.compute_api.Region_health_check {
    project = "value"  # Project ID for this request.
    region = "value"  # Name of the region scoping this request.
}

# Access region_health_check outputs
region_health_check_id = region_health_check.id
region_health_check_description = region_health_check.description
region_health_check_https_health_check = region_health_check.https_health_check
region_health_check_http_health_check = region_health_check.http_health_check
region_health_check_self_link = region_health_check.self_link
region_health_check_id = region_health_check.id
region_health_check_source_regions = region_health_check.source_regions
region_health_check_kind = region_health_check.kind
region_health_check_healthy_threshold = region_health_check.healthy_threshold
region_health_check_grpc_health_check = region_health_check.grpc_health_check
region_health_check_timeout_sec = region_health_check.timeout_sec
region_health_check_ssl_health_check = region_health_check.ssl_health_check
region_health_check_name = region_health_check.name
region_health_check_grpc_tls_health_check = region_health_check.grpc_tls_health_check
region_health_check_type = region_health_check.type
region_health_check_unhealthy_threshold = region_health_check.unhealthy_threshold
region_health_check_http2_health_check = region_health_check.http2_health_check
region_health_check_creation_timestamp = region_health_check.creation_timestamp
region_health_check_region = region_health_check.region
region_health_check_log_config = region_health_check.log_config
region_health_check_tcp_health_check = region_health_check.tcp_health_check
region_health_check_check_interval_sec = region_health_check.check_interval_sec
```

---


### Global_operation

Waits for the specified Operation resource to return as `DONE`
or for the request to approach the 2 minute deadline, and retrieves the
specified Operation resource. This method differs from the
`GET` method in that it waits for no more than the default
deadline (2 minutes) and then returns the current state of the operation,
which might be `DONE` or still in progress.

This method is called on a best-effort basis. Specifically:
   
   
    - In uncommon cases, when the server is overloaded, the request might
    return before the default deadline is reached, or might return after zero
    seconds.
   - If the default deadline is reached, there is no guarantee that the
    operation is actually done when the method returns. Be prepared to retry
    if the operation is not `DONE`.

**Operations**: ✅ Create ✅ Read ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `project` | String | ✅ | Project ID for this request. |
| `operation` | String | ✅ | Name of the Operations resource to return, or its unique numeric
identifier. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `insert_time` | String | [Output Only] The time that this operation was requested.
This value is inRFC3339
text format. |
| `description` | String | [Output Only] A textual description of the operation, which is
set when the operation is created. |
| `region` | String | [Output Only] The URL of the region where the operation resides. Only
applicable when performing regional operations. |
| `end_time` | String | [Output Only] The time that this operation was completed. This value is inRFC3339
text format. |
| `http_error_message` | String | [Output Only] If the operation fails, this field contains the HTTP error
message that was returned, such as `NOT FOUND`. |
| `http_error_status_code` | i64 | [Output Only] If the operation fails, this field contains the HTTP error
status code that was returned. For example, a `404` means the
resource was not found. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `set_common_instance_metadata_operation_metadata` | String | [Output Only] If the operation is for projects.setCommonInstanceMetadata,
this field will contain information on all underlying zonal actions and
their state. |
| `start_time` | String | [Output Only] The time that this operation was started by the server.
This value is inRFC3339
text format. |
| `target_id` | String | [Output Only] The unique target ID, which identifies a specific incarnation
of the target resource. |
| `target_link` | String | [Output Only] The URL of the resource that the operation modifies. For
operations related to creating a snapshot, this points to the disk
that the snapshot was created from. |
| `operation_group_id` | String | [Output Only] An ID that represents a group of operations, such as when a
group of operations results from a `bulkInsert` API request. |
| `name` | String | [Output Only] Name of the operation. |
| `user` | String | [Output Only] User who requested the operation, for example:
`user@example.com` or
`alice_smith_identifier (global/workforcePools/example-com-us-employees)`. |
| `creation_timestamp` | String | [Deprecated] This field is deprecated. |
| `error` | String | [Output Only] If errors are generated during processing of the operation,
this field will be populated. |
| `client_operation_id` | String | [Output Only] The value of `requestId` if you provided it in the request.
Not present otherwise. |
| `status_message` | String | [Output Only] An optional textual description of the current status of the
operation. |
| `status` | String | [Output Only] The status of the operation, which can be one of the
following:
`PENDING`, `RUNNING`, or `DONE`. |
| `id` | String | [Output Only] The unique identifier for the operation. This identifier is
defined by the server. |
| `kind` | String | [Output Only] Type of the resource. Always `compute#operation` for
Operation resources. |
| `zone` | String | [Output Only] The URL of the zone where the operation resides. Only
applicable when performing per-zone operations. |
| `operation_type` | String | [Output Only] The type of operation, such as `insert`,
`update`, or `delete`, and so on. |
| `instances_bulk_insert_operation_metadata` | String |  |
| `progress` | i64 | [Output Only] An optional progress indicator that ranges from 0 to 100.
There is no requirement that this be linear or support any granularity of
operations. This should not be used to guess when the operation will be
complete. This number should monotonically increase as the operation
progresses. |
| `warnings` | Vec<String> | [Output Only] If warning messages are generated during processing of the
operation, this field will be populated. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create global_operation
global_operation = provider.compute_api.Global_operation {
    project = "value"  # Project ID for this request.
    operation = "value"  # Name of the Operations resource to return, or its unique numeric
identifier.
}

# Access global_operation outputs
global_operation_id = global_operation.id
global_operation_insert_time = global_operation.insert_time
global_operation_description = global_operation.description
global_operation_region = global_operation.region
global_operation_end_time = global_operation.end_time
global_operation_http_error_message = global_operation.http_error_message
global_operation_http_error_status_code = global_operation.http_error_status_code
global_operation_self_link = global_operation.self_link
global_operation_set_common_instance_metadata_operation_metadata = global_operation.set_common_instance_metadata_operation_metadata
global_operation_start_time = global_operation.start_time
global_operation_target_id = global_operation.target_id
global_operation_target_link = global_operation.target_link
global_operation_operation_group_id = global_operation.operation_group_id
global_operation_name = global_operation.name
global_operation_user = global_operation.user
global_operation_creation_timestamp = global_operation.creation_timestamp
global_operation_error = global_operation.error
global_operation_client_operation_id = global_operation.client_operation_id
global_operation_status_message = global_operation.status_message
global_operation_status = global_operation.status
global_operation_id = global_operation.id
global_operation_kind = global_operation.kind
global_operation_zone = global_operation.zone
global_operation_operation_type = global_operation.operation_type
global_operation_instances_bulk_insert_operation_metadata = global_operation.instances_bulk_insert_operation_metadata
global_operation_progress = global_operation.progress
global_operation_warnings = global_operation.warnings
```

---


### Public_advertised_prefixe

Creates a PublicAdvertisedPrefix in the specified project
using the parameters that are included in the request.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `kind` | String |  | [Output Only] Type of the resource. Alwayscompute#publicAdvertisedPrefix for public advertised prefixes. |
| `ip_cidr_range` | String |  | The address range, in CIDR format, represented by this public advertised
prefix. |
| `fingerprint` | String |  | Fingerprint of this resource. A hash of the contents stored in this object.
This field is used in optimistic locking. This field will be ignored when
inserting a new PublicAdvertisedPrefix. An up-to-date
fingerprint must be provided in order to update thePublicAdvertisedPrefix, otherwise the request will fail with
error 412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve a PublicAdvertisedPrefix. |
| `name` | String |  | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `shared_secret` | String |  | [Output Only] The shared secret to be used for reverse DNS verification. |
| `dns_verification_ip` | String |  | The address to be used for reverse DNS verification. |
| `byoip_api_version` | String |  | [Output Only] The version of BYOIP API. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `ipv6_access_type` | String |  | The internet access type for IPv6 Public Advertised Prefixes. |
| `id` | String |  | [Output Only] The unique identifier for the resource type. The server
generates this identifier. |
| `status` | String |  | The status of the public advertised prefix. Possible values include:
   
   
     - `INITIAL`: RPKI validation is complete.
     - `PTR_CONFIGURED`: User has configured the PTR.
     - `VALIDATED`: Reverse DNS lookup is successful.
     - `REVERSE_DNS_LOOKUP_FAILED`: Reverse DNS lookup failed.
     - `PREFIX_CONFIGURATION_IN_PROGRESS`: The prefix is being
     configured.
     - `PREFIX_CONFIGURATION_COMPLETE`: The prefix is fully configured.
     - `PREFIX_REMOVAL_IN_PROGRESS`: The prefix is being removed. |
| `public_delegated_prefixs` | Vec<String> |  | [Output Only] The list of public delegated prefixes that exist for this
public advertised prefix. |
| `pdp_scope` | String |  | Specifies how child public delegated prefix will be scoped. It could
be one of following values:
   
   
     - `REGIONAL`: The public delegated prefix is regional only. The
     provisioning will take a few minutes.
     - `GLOBAL`: The public delegated prefix is global only. The
     provisioning will take ~4 weeks.
     - `GLOBAL_AND_REGIONAL` [output only]: The public delegated prefixes is
      BYOIP V1 legacy prefix. This is output only value and no longer
      supported in BYOIP V2. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `kind` | String | [Output Only] Type of the resource. Alwayscompute#publicAdvertisedPrefix for public advertised prefixes. |
| `ip_cidr_range` | String | The address range, in CIDR format, represented by this public advertised
prefix. |
| `fingerprint` | String | Fingerprint of this resource. A hash of the contents stored in this object.
This field is used in optimistic locking. This field will be ignored when
inserting a new PublicAdvertisedPrefix. An up-to-date
fingerprint must be provided in order to update thePublicAdvertisedPrefix, otherwise the request will fail with
error 412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve a PublicAdvertisedPrefix. |
| `name` | String | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `shared_secret` | String | [Output Only] The shared secret to be used for reverse DNS verification. |
| `dns_verification_ip` | String | The address to be used for reverse DNS verification. |
| `byoip_api_version` | String | [Output Only] The version of BYOIP API. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `ipv6_access_type` | String | The internet access type for IPv6 Public Advertised Prefixes. |
| `id` | String | [Output Only] The unique identifier for the resource type. The server
generates this identifier. |
| `status` | String | The status of the public advertised prefix. Possible values include:
   
   
     - `INITIAL`: RPKI validation is complete.
     - `PTR_CONFIGURED`: User has configured the PTR.
     - `VALIDATED`: Reverse DNS lookup is successful.
     - `REVERSE_DNS_LOOKUP_FAILED`: Reverse DNS lookup failed.
     - `PREFIX_CONFIGURATION_IN_PROGRESS`: The prefix is being
     configured.
     - `PREFIX_CONFIGURATION_COMPLETE`: The prefix is fully configured.
     - `PREFIX_REMOVAL_IN_PROGRESS`: The prefix is being removed. |
| `public_delegated_prefixs` | Vec<String> | [Output Only] The list of public delegated prefixes that exist for this
public advertised prefix. |
| `pdp_scope` | String | Specifies how child public delegated prefix will be scoped. It could
be one of following values:
   
   
     - `REGIONAL`: The public delegated prefix is regional only. The
     provisioning will take a few minutes.
     - `GLOBAL`: The public delegated prefix is global only. The
     provisioning will take ~4 weeks.
     - `GLOBAL_AND_REGIONAL` [output only]: The public delegated prefixes is
      BYOIP V1 legacy prefix. This is output only value and no longer
      supported in BYOIP V2. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create public_advertised_prefixe
public_advertised_prefixe = provider.compute_api.Public_advertised_prefixe {
    project = "value"  # Project ID for this request.
}

# Access public_advertised_prefixe outputs
public_advertised_prefixe_id = public_advertised_prefixe.id
public_advertised_prefixe_description = public_advertised_prefixe.description
public_advertised_prefixe_kind = public_advertised_prefixe.kind
public_advertised_prefixe_ip_cidr_range = public_advertised_prefixe.ip_cidr_range
public_advertised_prefixe_fingerprint = public_advertised_prefixe.fingerprint
public_advertised_prefixe_name = public_advertised_prefixe.name
public_advertised_prefixe_self_link = public_advertised_prefixe.self_link
public_advertised_prefixe_shared_secret = public_advertised_prefixe.shared_secret
public_advertised_prefixe_dns_verification_ip = public_advertised_prefixe.dns_verification_ip
public_advertised_prefixe_byoip_api_version = public_advertised_prefixe.byoip_api_version
public_advertised_prefixe_creation_timestamp = public_advertised_prefixe.creation_timestamp
public_advertised_prefixe_ipv6_access_type = public_advertised_prefixe.ipv6_access_type
public_advertised_prefixe_id = public_advertised_prefixe.id
public_advertised_prefixe_status = public_advertised_prefixe.status
public_advertised_prefixe_public_delegated_prefixs = public_advertised_prefixe.public_delegated_prefixs
public_advertised_prefixe_pdp_scope = public_advertised_prefixe.pdp_scope
```

---


### Region_instance_group_manager

Creates a managed instance group using the information that you specify
in the request. After the group is created, instances in the group are
created using the specified instance template.
This operation is marked as DONE when the group is created
even if the instances in the group have not yet been created. You must
separately verify the status of the individual instances with thelistmanagedinstances
method.

A regional managed instance group can contain up to 2000 instances.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `named_ports` | Vec<String> |  | [Output Only] Named ports configured on the Instance Groups complementary
to this Instance Group Manager. |
| `kind` | String |  | [Output Only] The resource type, which is alwayscompute#instanceGroupManager for managed instance groups. |
| `stateful_policy` | String |  | Stateful configuration for this Instanced Group Manager |
| `status` | String |  | [Output Only] The status of this managed instance group. |
| `update_policy` | String |  | The update policy for this managed instance group. |
| `target_suspended_size` | i64 |  | The target number of suspended instances for this managed instance group.
This number changes when you: 
   
   - Suspend instance using the suspendInstances
   method or resume instances using the resumeInstances
   method.
   - Manually change the targetSuspendedSize using the update
   method. |
| `instance_flexibility_policy` | String |  | Instance flexibility allowing MIG to create VMs from multiple
types of machines.
Instance flexibility configuration on MIG overrides instance
template configuration. |
| `auto_healing_policies` | Vec<String> |  | The autohealing policy for this managed instance group. You can specify
only one value. |
| `zone` | String |  | [Output Only] The URL of azone
where the managed instance group is located (for zonal resources). |
| `creation_timestamp` | String |  | [Output Only] The creation timestamp for this managed instance group inRFC3339
text format. |
| `satisfies_pzs` | bool |  | [Output Only] Reserved for future use. |
| `self_link` | String |  | [Output Only] The URL for this managed instance group. The server defines
this URL. |
| `base_instance_name` | String |  | The base instance name is a prefix that you want to attach to the names of
all VMs in a MIG. The maximum character length is 58 and the name must
comply with RFC1035 format.

When a VM is created in the group, the MIG appends a hyphen and a random
four-character string to the base instance name. If you want the MIG to
assign sequential numbers instead of a random string, then end the base
instance name with a hyphen followed by one or more hash symbols. The hash
symbols indicate the number of digits. For example, a base instance name of
"vm-###" results in "vm-001" as a VM name.
@pattern
[a-z](([-a-z0-9]{0,57})|([-a-z0-9]{0,51}-#{1,10}(\\[[0-9]{1,10}\\])?)) |
| `list_managed_instances_results` | String |  | Pagination behavior of the listManagedInstances API method for
this managed instance group. |
| `all_instances_config` | String |  | Specifies configuration that overrides the instance template configuration
for the group. |
| `id` | String |  | [Output Only] A unique identifier for this resource type. The server
generates this identifier. |
| `resource_policies` | String |  | Resource policies for this managed instance group. |
| `name` | String |  | The name of the managed instance group. The name must be 1-63 characters
long, and comply withRFC1035. |
| `versions` | Vec<String> |  | Specifies the instance templates used by this managed instance group to
create instances.

Each version is defined by an instanceTemplate and aname. Every version can appear at most once per instance
group. This field overrides the top-level instanceTemplate
field. Read more about therelationships
between these fields. Exactly one version must leave thetargetSize field unset. That version will be applied to all
remaining instances. For more information, read aboutcanary
updates. |
| `instance_template` | String |  | The URL of the instance template that is specified for this managed
instance group. The group uses this template to create all new instances
in the managed instance group. The templates for existing instances in the
group do not change unless you run recreateInstances, runapplyUpdatesToInstances, or set the group'supdatePolicy.type to PROACTIVE. |
| `standby_policy` | String |  | Standby policy for stopped and suspended instances. |
| `distribution_policy` | String |  | Policy specifying the intended distribution of managed instances across
zones in a regional managed instance group. |
| `fingerprint` | String |  | Fingerprint of this resource. This field may be used in optimistic locking.
It will be ignored when inserting an InstanceGroupManager. An up-to-date
fingerprint must be provided in order to update the InstanceGroupManager,
otherwise the request will fail with error412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve an InstanceGroupManager. |
| `target_size` | i64 |  | The target number of running instances for this managed instance group.
You can reduce this number by using the instanceGroupManager
deleteInstances or abandonInstances methods. Resizing the group also
changes this number. |
| `satisfies_pzi` | bool |  | [Output Only] Reserved for future use. |
| `target_stopped_size` | i64 |  | The target number of stopped instances for this managed instance group.
This number changes when you: 
   
   - Stop instance using the stopInstances
   method or start instances using the startInstances
   method.
   - Manually change the targetStoppedSize using the update
   method. |
| `target_pools` | Vec<String> |  | The URLs for all TargetPool resources to which instances in theinstanceGroup field are added. The target pools automatically
apply to all of the instances in the managed instance group. |
| `instance_lifecycle_policy` | String |  | The repair policy for this managed instance group. |
| `instance_group` | String |  | [Output Only] The URL of the Instance Group resource. |
| `current_actions` | String |  | [Output Only] The list of instance actions and the number of instances
in this managed instance group that are scheduled for each of those
actions. |
| `region` | String |  | [Output Only] The URL of theregion
where the managed instance group resides (for regional resources). |
| `description` | String |  | An optional description of this resource. |
| `region` | String | ✅ | Name of the region scoping this request. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `named_ports` | Vec<String> | [Output Only] Named ports configured on the Instance Groups complementary
to this Instance Group Manager. |
| `kind` | String | [Output Only] The resource type, which is alwayscompute#instanceGroupManager for managed instance groups. |
| `stateful_policy` | String | Stateful configuration for this Instanced Group Manager |
| `status` | String | [Output Only] The status of this managed instance group. |
| `update_policy` | String | The update policy for this managed instance group. |
| `target_suspended_size` | i64 | The target number of suspended instances for this managed instance group.
This number changes when you: 
   
   - Suspend instance using the suspendInstances
   method or resume instances using the resumeInstances
   method.
   - Manually change the targetSuspendedSize using the update
   method. |
| `instance_flexibility_policy` | String | Instance flexibility allowing MIG to create VMs from multiple
types of machines.
Instance flexibility configuration on MIG overrides instance
template configuration. |
| `auto_healing_policies` | Vec<String> | The autohealing policy for this managed instance group. You can specify
only one value. |
| `zone` | String | [Output Only] The URL of azone
where the managed instance group is located (for zonal resources). |
| `creation_timestamp` | String | [Output Only] The creation timestamp for this managed instance group inRFC3339
text format. |
| `satisfies_pzs` | bool | [Output Only] Reserved for future use. |
| `self_link` | String | [Output Only] The URL for this managed instance group. The server defines
this URL. |
| `base_instance_name` | String | The base instance name is a prefix that you want to attach to the names of
all VMs in a MIG. The maximum character length is 58 and the name must
comply with RFC1035 format.

When a VM is created in the group, the MIG appends a hyphen and a random
four-character string to the base instance name. If you want the MIG to
assign sequential numbers instead of a random string, then end the base
instance name with a hyphen followed by one or more hash symbols. The hash
symbols indicate the number of digits. For example, a base instance name of
"vm-###" results in "vm-001" as a VM name.
@pattern
[a-z](([-a-z0-9]{0,57})|([-a-z0-9]{0,51}-#{1,10}(\\[[0-9]{1,10}\\])?)) |
| `list_managed_instances_results` | String | Pagination behavior of the listManagedInstances API method for
this managed instance group. |
| `all_instances_config` | String | Specifies configuration that overrides the instance template configuration
for the group. |
| `id` | String | [Output Only] A unique identifier for this resource type. The server
generates this identifier. |
| `resource_policies` | String | Resource policies for this managed instance group. |
| `name` | String | The name of the managed instance group. The name must be 1-63 characters
long, and comply withRFC1035. |
| `versions` | Vec<String> | Specifies the instance templates used by this managed instance group to
create instances.

Each version is defined by an instanceTemplate and aname. Every version can appear at most once per instance
group. This field overrides the top-level instanceTemplate
field. Read more about therelationships
between these fields. Exactly one version must leave thetargetSize field unset. That version will be applied to all
remaining instances. For more information, read aboutcanary
updates. |
| `instance_template` | String | The URL of the instance template that is specified for this managed
instance group. The group uses this template to create all new instances
in the managed instance group. The templates for existing instances in the
group do not change unless you run recreateInstances, runapplyUpdatesToInstances, or set the group'supdatePolicy.type to PROACTIVE. |
| `standby_policy` | String | Standby policy for stopped and suspended instances. |
| `distribution_policy` | String | Policy specifying the intended distribution of managed instances across
zones in a regional managed instance group. |
| `fingerprint` | String | Fingerprint of this resource. This field may be used in optimistic locking.
It will be ignored when inserting an InstanceGroupManager. An up-to-date
fingerprint must be provided in order to update the InstanceGroupManager,
otherwise the request will fail with error412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve an InstanceGroupManager. |
| `target_size` | i64 | The target number of running instances for this managed instance group.
You can reduce this number by using the instanceGroupManager
deleteInstances or abandonInstances methods. Resizing the group also
changes this number. |
| `satisfies_pzi` | bool | [Output Only] Reserved for future use. |
| `target_stopped_size` | i64 | The target number of stopped instances for this managed instance group.
This number changes when you: 
   
   - Stop instance using the stopInstances
   method or start instances using the startInstances
   method.
   - Manually change the targetStoppedSize using the update
   method. |
| `target_pools` | Vec<String> | The URLs for all TargetPool resources to which instances in theinstanceGroup field are added. The target pools automatically
apply to all of the instances in the managed instance group. |
| `instance_lifecycle_policy` | String | The repair policy for this managed instance group. |
| `instance_group` | String | [Output Only] The URL of the Instance Group resource. |
| `current_actions` | String | [Output Only] The list of instance actions and the number of instances
in this managed instance group that are scheduled for each of those
actions. |
| `region` | String | [Output Only] The URL of theregion
where the managed instance group resides (for regional resources). |
| `description` | String | An optional description of this resource. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create region_instance_group_manager
region_instance_group_manager = provider.compute_api.Region_instance_group_manager {
    region = "value"  # Name of the region scoping this request.
    project = "value"  # Project ID for this request.
}

# Access region_instance_group_manager outputs
region_instance_group_manager_id = region_instance_group_manager.id
region_instance_group_manager_named_ports = region_instance_group_manager.named_ports
region_instance_group_manager_kind = region_instance_group_manager.kind
region_instance_group_manager_stateful_policy = region_instance_group_manager.stateful_policy
region_instance_group_manager_status = region_instance_group_manager.status
region_instance_group_manager_update_policy = region_instance_group_manager.update_policy
region_instance_group_manager_target_suspended_size = region_instance_group_manager.target_suspended_size
region_instance_group_manager_instance_flexibility_policy = region_instance_group_manager.instance_flexibility_policy
region_instance_group_manager_auto_healing_policies = region_instance_group_manager.auto_healing_policies
region_instance_group_manager_zone = region_instance_group_manager.zone
region_instance_group_manager_creation_timestamp = region_instance_group_manager.creation_timestamp
region_instance_group_manager_satisfies_pzs = region_instance_group_manager.satisfies_pzs
region_instance_group_manager_self_link = region_instance_group_manager.self_link
region_instance_group_manager_base_instance_name = region_instance_group_manager.base_instance_name
region_instance_group_manager_list_managed_instances_results = region_instance_group_manager.list_managed_instances_results
region_instance_group_manager_all_instances_config = region_instance_group_manager.all_instances_config
region_instance_group_manager_id = region_instance_group_manager.id
region_instance_group_manager_resource_policies = region_instance_group_manager.resource_policies
region_instance_group_manager_name = region_instance_group_manager.name
region_instance_group_manager_versions = region_instance_group_manager.versions
region_instance_group_manager_instance_template = region_instance_group_manager.instance_template
region_instance_group_manager_standby_policy = region_instance_group_manager.standby_policy
region_instance_group_manager_distribution_policy = region_instance_group_manager.distribution_policy
region_instance_group_manager_fingerprint = region_instance_group_manager.fingerprint
region_instance_group_manager_target_size = region_instance_group_manager.target_size
region_instance_group_manager_satisfies_pzi = region_instance_group_manager.satisfies_pzi
region_instance_group_manager_target_stopped_size = region_instance_group_manager.target_stopped_size
region_instance_group_manager_target_pools = region_instance_group_manager.target_pools
region_instance_group_manager_instance_lifecycle_policy = region_instance_group_manager.instance_lifecycle_policy
region_instance_group_manager_instance_group = region_instance_group_manager.instance_group
region_instance_group_manager_current_actions = region_instance_group_manager.current_actions
region_instance_group_manager_region = region_instance_group_manager.region
region_instance_group_manager_description = region_instance_group_manager.description
```

---


### Http_health_check

Creates a HttpHealthCheck resource in the specified project using the data
included in the request.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `kind` | String |  | [Output Only] Type of the resource. Alwayscompute#httpHealthCheck for HTTP health checks. |
| `healthy_threshold` | i64 |  | A so-far unhealthy instance will be marked healthy after this
many consecutive successes. The default value is 2. |
| `name` | String |  | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `request_path` | String |  | The request path of the HTTP health check request. The default value is/. This field does not support query
parameters. Must comply withRFC3986. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `timeout_sec` | i64 |  | How long (in seconds) to wait before claiming failure. The default value is5 seconds. It is invalid for timeoutSec to have
greater value than checkIntervalSec. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `check_interval_sec` | i64 |  | How often (in seconds) to send a health check. The default value is5 seconds. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `port` | i64 |  | The TCP port number for the HTTP health check request. The default value is80. |
| `unhealthy_threshold` | i64 |  | A so-far healthy instance will be marked unhealthy after this
many consecutive failures. The default value is 2. |
| `host` | String |  | The value of the host header in the HTTP health check request. If left
empty (default value), the public IP on behalf of which this health check
is performed will be used. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `kind` | String | [Output Only] Type of the resource. Alwayscompute#httpHealthCheck for HTTP health checks. |
| `healthy_threshold` | i64 | A so-far unhealthy instance will be marked healthy after this
many consecutive successes. The default value is 2. |
| `name` | String | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `request_path` | String | The request path of the HTTP health check request. The default value is/. This field does not support query
parameters. Must comply withRFC3986. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `timeout_sec` | i64 | How long (in seconds) to wait before claiming failure. The default value is5 seconds. It is invalid for timeoutSec to have
greater value than checkIntervalSec. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `check_interval_sec` | i64 | How often (in seconds) to send a health check. The default value is5 seconds. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `port` | i64 | The TCP port number for the HTTP health check request. The default value is80. |
| `unhealthy_threshold` | i64 | A so-far healthy instance will be marked unhealthy after this
many consecutive failures. The default value is 2. |
| `host` | String | The value of the host header in the HTTP health check request. If left
empty (default value), the public IP on behalf of which this health check
is performed will be used. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create http_health_check
http_health_check = provider.compute_api.Http_health_check {
    project = "value"  # Project ID for this request.
}

# Access http_health_check outputs
http_health_check_id = http_health_check.id
http_health_check_kind = http_health_check.kind
http_health_check_healthy_threshold = http_health_check.healthy_threshold
http_health_check_name = http_health_check.name
http_health_check_request_path = http_health_check.request_path
http_health_check_creation_timestamp = http_health_check.creation_timestamp
http_health_check_timeout_sec = http_health_check.timeout_sec
http_health_check_self_link = http_health_check.self_link
http_health_check_check_interval_sec = http_health_check.check_interval_sec
http_health_check_id = http_health_check.id
http_health_check_description = http_health_check.description
http_health_check_port = http_health_check.port
http_health_check_unhealthy_threshold = http_health_check.unhealthy_threshold
http_health_check_host = http_health_check.host
```

---


### Network_edge_security_service

Creates a new service in the specified project using the data included in
the request.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `self_link_with_id` | String |  | [Output Only] Server-defined URL for this resource with the resource id. |
| `region` | String |  | [Output Only] URL of the region where the resource resides. You must
specify this field as part of the HTTP request URL. It is not settable as a
field in the request body. |
| `name` | String |  | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `kind` | String |  | [Output only] Type of the resource. Alwayscompute#networkEdgeSecurityService for
NetworkEdgeSecurityServices |
| `fingerprint` | String |  | Fingerprint of this resource. A hash of the contents stored in this object.
This field is used in optimistic locking. This field will be ignored when
inserting a NetworkEdgeSecurityService. An up-to-date fingerprint must be
provided in order to update the NetworkEdgeSecurityService,
otherwise the request will fail with error412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve a NetworkEdgeSecurityService. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `security_policy` | String |  | The resource URL for the network edge security service associated with this
network edge security service. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `project` | String | ✅ | Project ID for this request. |
| `region` | String | ✅ | Name of the region scoping this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `self_link_with_id` | String | [Output Only] Server-defined URL for this resource with the resource id. |
| `region` | String | [Output Only] URL of the region where the resource resides. You must
specify this field as part of the HTTP request URL. It is not settable as a
field in the request body. |
| `name` | String | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `kind` | String | [Output only] Type of the resource. Alwayscompute#networkEdgeSecurityService for
NetworkEdgeSecurityServices |
| `fingerprint` | String | Fingerprint of this resource. A hash of the contents stored in this object.
This field is used in optimistic locking. This field will be ignored when
inserting a NetworkEdgeSecurityService. An up-to-date fingerprint must be
provided in order to update the NetworkEdgeSecurityService,
otherwise the request will fail with error412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve a NetworkEdgeSecurityService. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `security_policy` | String | The resource URL for the network edge security service associated with this
network edge security service. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create network_edge_security_service
network_edge_security_service = provider.compute_api.Network_edge_security_service {
    project = "value"  # Project ID for this request.
    region = "value"  # Name of the region scoping this request.
}

# Access network_edge_security_service outputs
network_edge_security_service_id = network_edge_security_service.id
network_edge_security_service_self_link_with_id = network_edge_security_service.self_link_with_id
network_edge_security_service_region = network_edge_security_service.region
network_edge_security_service_name = network_edge_security_service.name
network_edge_security_service_kind = network_edge_security_service.kind
network_edge_security_service_fingerprint = network_edge_security_service.fingerprint
network_edge_security_service_creation_timestamp = network_edge_security_service.creation_timestamp
network_edge_security_service_self_link = network_edge_security_service.self_link
network_edge_security_service_security_policy = network_edge_security_service.security_policy
network_edge_security_service_id = network_edge_security_service.id
network_edge_security_service_description = network_edge_security_service.description
```

---


### Target_instance

Creates a TargetInstance resource in the specified project and zone using
the data included in the request.

**Operations**: ✅ Create ✅ Read ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `security_policy` | String |  | [Output Only] The resource URL for the security policy associated with this
target instance. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `name` | String |  | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `zone` | String |  | [Output Only] URL of the zone where the target instance resides.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `instance` | String |  | A URL to the virtual machine instance that handles traffic for this target
instance. When creating a target instance, you can provide the
fully-qualified URL or a valid partial URL to the desired virtual machine.
For example, the following are all valid URLs:
   
   - https://www.googleapis.com/compute/v1/projects/project/zones/zone/instances/instance 
   - projects/project/zones/zone/instances/instance 
   - zones/zone/instances/instance |
| `kind` | String |  | [Output Only] The type of the resource. Alwayscompute#targetInstance for target instances. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `network` | String |  | The URL of the network this target instance uses to forward traffic.
If not specified, the traffic will be forwarded to the network that
the default network interface belongs to. |
| `nat_policy` | String |  | Must have a value of NO_NAT.
Protocol forwarding delivers packets while preserving the destination IP
address of the forwarding rule referencing the target instance. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `zone` | String | ✅ | Name of the zone scoping this request. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `security_policy` | String | [Output Only] The resource URL for the security policy associated with this
target instance. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `name` | String | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `zone` | String | [Output Only] URL of the zone where the target instance resides.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `instance` | String | A URL to the virtual machine instance that handles traffic for this target
instance. When creating a target instance, you can provide the
fully-qualified URL or a valid partial URL to the desired virtual machine.
For example, the following are all valid URLs:
   
   - https://www.googleapis.com/compute/v1/projects/project/zones/zone/instances/instance 
   - projects/project/zones/zone/instances/instance 
   - zones/zone/instances/instance |
| `kind` | String | [Output Only] The type of the resource. Alwayscompute#targetInstance for target instances. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `network` | String | The URL of the network this target instance uses to forward traffic.
If not specified, the traffic will be forwarded to the network that
the default network interface belongs to. |
| `nat_policy` | String | Must have a value of NO_NAT.
Protocol forwarding delivers packets while preserving the destination IP
address of the forwarding rule referencing the target instance. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create target_instance
target_instance = provider.compute_api.Target_instance {
    zone = "value"  # Name of the zone scoping this request.
    project = "value"  # Project ID for this request.
}

# Access target_instance outputs
target_instance_id = target_instance.id
target_instance_security_policy = target_instance.security_policy
target_instance_self_link = target_instance.self_link
target_instance_name = target_instance.name
target_instance_creation_timestamp = target_instance.creation_timestamp
target_instance_zone = target_instance.zone
target_instance_instance = target_instance.instance
target_instance_kind = target_instance.kind
target_instance_id = target_instance.id
target_instance_network = target_instance.network
target_instance_nat_policy = target_instance.nat_policy
target_instance_description = target_instance.description
```

---


### Region_security_policie

Creates a new policy in the specified project using the data included in
the request.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `associations` | Vec<String> |  | A list of associations that belong to this policy. |
| `type` | String |  | The type indicates the intended use of the security policy.
   
   - CLOUD_ARMOR: Cloud Armor backend security policies can
   be configured to filter incoming HTTP requests targeting backend services.
   They filter requests before they hit the origin servers.
   - CLOUD_ARMOR_EDGE: Cloud Armor edge security policies can
   be configured to filter incoming HTTP requests targeting backend services
   (including Cloud CDN-enabled) as well as backend buckets (Cloud Storage).
   They filter requests before the request is served from Google's cache.
   - CLOUD_ARMOR_INTERNAL_SERVICE (preview only): Cloud Armor
   internal service policies can be configured to filter HTTP requests
   targeting services managed by Traffic Director in a service mesh. They
   filter requests before the request is served from the application.

- CLOUD_ARMOR_NETWORK: Cloud Armor network policies
can be configured to filter packets targeting network load balancing
resources such as backend services, target pools, target instances, and
instances with external IPs. They filter requests before the request is
served from the application.


This field can be set only at resource creation time. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `ddos_protection_config` | String |  |  |
| `name` | String |  | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `advanced_options_config` | String |  |  |
| `recaptcha_options_config` | String |  |  |
| `kind` | String |  | [Output only] Type of the resource. Alwayscompute#securityPolicyfor security policies |
| `user_defined_fields` | Vec<String> |  | Definitions of user-defined fields for CLOUD_ARMOR_NETWORK policies. A
user-defined field consists of up to 4 bytes extracted from a fixed offset
in the packet, relative to the IPv4, IPv6, TCP, or UDP header, with an
optional mask to select certain bits. Rules may then specify matching
values for these fields.

Example:

 userDefinedFields:
 - name: "ipv4_fragment_offset"
   base: IPV4
   offset: 6
   size: 2
   mask: "0x1fff" |
| `adaptive_protection_config` | String |  |  |
| `rules` | Vec<String> |  | A list of rules that belong to this policy.
There must always be a default rule which is a rule with priority
2147483647 and match all condition (for the match condition this means
match  "*" for srcIpRanges and for the networkMatch condition every field
must be either match "*" or not set). If no rules are provided when
creating a security policy, a default rule with action "allow" will be
added. |
| `region` | String |  | [Output Only] URL of the region where the regional security policy
resides. This field is not applicable to global security policies. |
| `fingerprint` | String |  | Specifies a fingerprint for this resource, which is essentially a hash of
the metadata's contents and used for optimistic locking. The
fingerprint is initially generated by Compute Engine and changes after
every request to modify or update metadata. You must always provide an
up-to-date fingerprint hash in order to update or change metadata,
otherwise the request will fail with error412 conditionNotMet.

To see the latest fingerprint, make get() request to the
security policy. |
| `labels` | HashMap<String, String> |  | Labels for this resource. These can only be added or modified by thesetLabels method. Each label key/value pair must comply withRFC1035.
Label values may be empty. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `short_name` | String |  | User-provided name of the organization security policy. The name should be
unique in the organization in which the security policy is created. This
should only be used when SecurityPolicyType is CLOUD_ARMOR.
The name must be 1-63 characters long, and comply with
https://www.ietf.org/rfc/rfc1035.txt. Specifically, the name must be 1-63
characters long and match the regular expression
`[a-z]([-a-z0-9]*[a-z0-9])?` which means the first character must be a
lowercase letter, and all following characters must be a dash, lowercase
letter, or digit, except the last character, which cannot be a dash. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `label_fingerprint` | String |  | A fingerprint for the labels being applied to this security policy, which
is essentially a hash of the labels set used for optimistic locking. The
fingerprint is initially generated by Compute Engine and changes after
every request to modify or update labels. You must always provide an
up-to-date fingerprint hash in order to update or change labels.

To see the latest fingerprint, make get() request to the
security policy. |
| `project` | String | ✅ | Project ID for this request. |
| `region` | String | ✅ | Name of the region scoping this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `associations` | Vec<String> | A list of associations that belong to this policy. |
| `type` | String | The type indicates the intended use of the security policy.
   
   - CLOUD_ARMOR: Cloud Armor backend security policies can
   be configured to filter incoming HTTP requests targeting backend services.
   They filter requests before they hit the origin servers.
   - CLOUD_ARMOR_EDGE: Cloud Armor edge security policies can
   be configured to filter incoming HTTP requests targeting backend services
   (including Cloud CDN-enabled) as well as backend buckets (Cloud Storage).
   They filter requests before the request is served from Google's cache.
   - CLOUD_ARMOR_INTERNAL_SERVICE (preview only): Cloud Armor
   internal service policies can be configured to filter HTTP requests
   targeting services managed by Traffic Director in a service mesh. They
   filter requests before the request is served from the application.

- CLOUD_ARMOR_NETWORK: Cloud Armor network policies
can be configured to filter packets targeting network load balancing
resources such as backend services, target pools, target instances, and
instances with external IPs. They filter requests before the request is
served from the application.


This field can be set only at resource creation time. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `ddos_protection_config` | String |  |
| `name` | String | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `advanced_options_config` | String |  |
| `recaptcha_options_config` | String |  |
| `kind` | String | [Output only] Type of the resource. Alwayscompute#securityPolicyfor security policies |
| `user_defined_fields` | Vec<String> | Definitions of user-defined fields for CLOUD_ARMOR_NETWORK policies. A
user-defined field consists of up to 4 bytes extracted from a fixed offset
in the packet, relative to the IPv4, IPv6, TCP, or UDP header, with an
optional mask to select certain bits. Rules may then specify matching
values for these fields.

Example:

 userDefinedFields:
 - name: "ipv4_fragment_offset"
   base: IPV4
   offset: 6
   size: 2
   mask: "0x1fff" |
| `adaptive_protection_config` | String |  |
| `rules` | Vec<String> | A list of rules that belong to this policy.
There must always be a default rule which is a rule with priority
2147483647 and match all condition (for the match condition this means
match  "*" for srcIpRanges and for the networkMatch condition every field
must be either match "*" or not set). If no rules are provided when
creating a security policy, a default rule with action "allow" will be
added. |
| `region` | String | [Output Only] URL of the region where the regional security policy
resides. This field is not applicable to global security policies. |
| `fingerprint` | String | Specifies a fingerprint for this resource, which is essentially a hash of
the metadata's contents and used for optimistic locking. The
fingerprint is initially generated by Compute Engine and changes after
every request to modify or update metadata. You must always provide an
up-to-date fingerprint hash in order to update or change metadata,
otherwise the request will fail with error412 conditionNotMet.

To see the latest fingerprint, make get() request to the
security policy. |
| `labels` | HashMap<String, String> | Labels for this resource. These can only be added or modified by thesetLabels method. Each label key/value pair must comply withRFC1035.
Label values may be empty. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `short_name` | String | User-provided name of the organization security policy. The name should be
unique in the organization in which the security policy is created. This
should only be used when SecurityPolicyType is CLOUD_ARMOR.
The name must be 1-63 characters long, and comply with
https://www.ietf.org/rfc/rfc1035.txt. Specifically, the name must be 1-63
characters long and match the regular expression
`[a-z]([-a-z0-9]*[a-z0-9])?` which means the first character must be a
lowercase letter, and all following characters must be a dash, lowercase
letter, or digit, except the last character, which cannot be a dash. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `label_fingerprint` | String | A fingerprint for the labels being applied to this security policy, which
is essentially a hash of the labels set used for optimistic locking. The
fingerprint is initially generated by Compute Engine and changes after
every request to modify or update labels. You must always provide an
up-to-date fingerprint hash in order to update or change labels.

To see the latest fingerprint, make get() request to the
security policy. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create region_security_policie
region_security_policie = provider.compute_api.Region_security_policie {
    project = "value"  # Project ID for this request.
    region = "value"  # Name of the region scoping this request.
}

# Access region_security_policie outputs
region_security_policie_id = region_security_policie.id
region_security_policie_associations = region_security_policie.associations
region_security_policie_type = region_security_policie.type
region_security_policie_id = region_security_policie.id
region_security_policie_creation_timestamp = region_security_policie.creation_timestamp
region_security_policie_ddos_protection_config = region_security_policie.ddos_protection_config
region_security_policie_name = region_security_policie.name
region_security_policie_advanced_options_config = region_security_policie.advanced_options_config
region_security_policie_recaptcha_options_config = region_security_policie.recaptcha_options_config
region_security_policie_kind = region_security_policie.kind
region_security_policie_user_defined_fields = region_security_policie.user_defined_fields
region_security_policie_adaptive_protection_config = region_security_policie.adaptive_protection_config
region_security_policie_rules = region_security_policie.rules
region_security_policie_region = region_security_policie.region
region_security_policie_fingerprint = region_security_policie.fingerprint
region_security_policie_labels = region_security_policie.labels
region_security_policie_self_link = region_security_policie.self_link
region_security_policie_short_name = region_security_policie.short_name
region_security_policie_description = region_security_policie.description
region_security_policie_label_fingerprint = region_security_policie.label_fingerprint
```

---


### Global_network_endpoint_group

Creates a network endpoint group in the specified project using the
parameters that are included in the request.

**Operations**: ✅ Create ✅ Read ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `psc_data` | String |  | Optional. Only valid when networkEndpointType isPRIVATE_SERVICE_CONNECT. |
| `region` | String |  | [Output Only] The URL of theregion
where the network endpoint group is located. |
| `size` | i64 |  | [Output only] Number of network endpoints in the network endpoint group. |
| `network_endpoint_type` | String |  | Type of network endpoints in this network endpoint group. Can be one ofGCE_VM_IP, GCE_VM_IP_PORT,NON_GCP_PRIVATE_IP_PORT, INTERNET_FQDN_PORT,INTERNET_IP_PORT, SERVERLESS,PRIVATE_SERVICE_CONNECT, GCE_VM_IP_PORTMAP. |
| `annotations` | HashMap<String, String> |  | Optional. Metadata defined as annotations on the network endpoint group. |
| `app_engine` | String |  | Optional. Only valid when networkEndpointType isSERVERLESS. Only one of cloudRun,appEngine or cloudFunction may be set. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `network` | String |  | The URL of the network to which all network endpoints in the NEG belong.
Uses default project network if unspecified. |
| `kind` | String |  | [Output Only] Type of the resource. Alwayscompute#networkEndpointGroup for network endpoint group. |
| `default_port` | i64 |  | The default port used if the port number is not specified in the network
endpoint.

Optional. If the network endpoint type is either GCE_VM_IP,SERVERLESS or PRIVATE_SERVICE_CONNECT, this
field must not be specified. |
| `name` | String |  | Name of the resource; provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must be
a dash, lowercase letter, or digit, except the last character, which cannot
be a dash. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `psc_target_service` | String |  | The target service url used to set up private service connection to
a Google API or a PSC Producer Service Attachment.
An example value is: asia-northeast3-cloudkms.googleapis.com.

Optional. Only valid when networkEndpointType isPRIVATE_SERVICE_CONNECT. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `cloud_run` | String |  | Optional. Only valid when networkEndpointType isSERVERLESS. Only one of cloudRun,appEngine or cloudFunction may be set. |
| `cloud_function` | String |  | Optional. Only valid when networkEndpointType isSERVERLESS. Only one of cloudRun,appEngine or cloudFunction may be set. |
| `subnetwork` | String |  | Optional URL of the subnetwork to which all network endpoints in the NEG
belong. |
| `zone` | String |  | [Output Only] The URL of thezone
where the network endpoint group is located. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `psc_data` | String | Optional. Only valid when networkEndpointType isPRIVATE_SERVICE_CONNECT. |
| `region` | String | [Output Only] The URL of theregion
where the network endpoint group is located. |
| `size` | i64 | [Output only] Number of network endpoints in the network endpoint group. |
| `network_endpoint_type` | String | Type of network endpoints in this network endpoint group. Can be one ofGCE_VM_IP, GCE_VM_IP_PORT,NON_GCP_PRIVATE_IP_PORT, INTERNET_FQDN_PORT,INTERNET_IP_PORT, SERVERLESS,PRIVATE_SERVICE_CONNECT, GCE_VM_IP_PORTMAP. |
| `annotations` | HashMap<String, String> | Optional. Metadata defined as annotations on the network endpoint group. |
| `app_engine` | String | Optional. Only valid when networkEndpointType isSERVERLESS. Only one of cloudRun,appEngine or cloudFunction may be set. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `network` | String | The URL of the network to which all network endpoints in the NEG belong.
Uses default project network if unspecified. |
| `kind` | String | [Output Only] Type of the resource. Alwayscompute#networkEndpointGroup for network endpoint group. |
| `default_port` | i64 | The default port used if the port number is not specified in the network
endpoint.

Optional. If the network endpoint type is either GCE_VM_IP,SERVERLESS or PRIVATE_SERVICE_CONNECT, this
field must not be specified. |
| `name` | String | Name of the resource; provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must be
a dash, lowercase letter, or digit, except the last character, which cannot
be a dash. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `psc_target_service` | String | The target service url used to set up private service connection to
a Google API or a PSC Producer Service Attachment.
An example value is: asia-northeast3-cloudkms.googleapis.com.

Optional. Only valid when networkEndpointType isPRIVATE_SERVICE_CONNECT. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `cloud_run` | String | Optional. Only valid when networkEndpointType isSERVERLESS. Only one of cloudRun,appEngine or cloudFunction may be set. |
| `cloud_function` | String | Optional. Only valid when networkEndpointType isSERVERLESS. Only one of cloudRun,appEngine or cloudFunction may be set. |
| `subnetwork` | String | Optional URL of the subnetwork to which all network endpoints in the NEG
belong. |
| `zone` | String | [Output Only] The URL of thezone
where the network endpoint group is located. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create global_network_endpoint_group
global_network_endpoint_group = provider.compute_api.Global_network_endpoint_group {
    project = "value"  # Project ID for this request.
}

# Access global_network_endpoint_group outputs
global_network_endpoint_group_id = global_network_endpoint_group.id
global_network_endpoint_group_psc_data = global_network_endpoint_group.psc_data
global_network_endpoint_group_region = global_network_endpoint_group.region
global_network_endpoint_group_size = global_network_endpoint_group.size
global_network_endpoint_group_network_endpoint_type = global_network_endpoint_group.network_endpoint_type
global_network_endpoint_group_annotations = global_network_endpoint_group.annotations
global_network_endpoint_group_app_engine = global_network_endpoint_group.app_engine
global_network_endpoint_group_self_link = global_network_endpoint_group.self_link
global_network_endpoint_group_network = global_network_endpoint_group.network
global_network_endpoint_group_kind = global_network_endpoint_group.kind
global_network_endpoint_group_default_port = global_network_endpoint_group.default_port
global_network_endpoint_group_name = global_network_endpoint_group.name
global_network_endpoint_group_creation_timestamp = global_network_endpoint_group.creation_timestamp
global_network_endpoint_group_psc_target_service = global_network_endpoint_group.psc_target_service
global_network_endpoint_group_id = global_network_endpoint_group.id
global_network_endpoint_group_description = global_network_endpoint_group.description
global_network_endpoint_group_cloud_run = global_network_endpoint_group.cloud_run
global_network_endpoint_group_cloud_function = global_network_endpoint_group.cloud_function
global_network_endpoint_group_subnetwork = global_network_endpoint_group.subnetwork
global_network_endpoint_group_zone = global_network_endpoint_group.zone
```

---


### Resource_policie

Creates a new resource policy.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `kind` | String |  | [Output Only] Type of the resource. Alwayscompute#resource_policies for resource policies. |
| `name` | String |  | The name of the resource, provided by the client when initially creating
the resource. The resource name must be 1-63 characters long, and comply
withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must be
a dash, lowercase letter, or digit, except the last character, which cannot
be a dash. |
| `instance_schedule_policy` | String |  | Resource policy for scheduling instance operations. |
| `region` | String |  |  |
| `resource_status` | String |  | [Output Only] The system status of the resource policy. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `self_link` | String |  | [Output Only] Server-defined fully-qualified URL for this resource. |
| `group_placement_policy` | String |  | Resource policy for instances for placement configuration. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `status` | String |  | [Output Only] The status of resource policy creation. |
| `disk_consistency_group_policy` | String |  | Resource policy for disk consistency groups. |
| `snapshot_schedule_policy` | String |  | Resource policy for persistent disks for creating snapshots. |
| `workload_policy` | String |  | Resource policy for defining instance placement for MIGs. |
| `description` | String |  |  |
| `project` | String | ✅ | Project ID for this request. |
| `region` | String | ✅ | Name of the region for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `kind` | String | [Output Only] Type of the resource. Alwayscompute#resource_policies for resource policies. |
| `name` | String | The name of the resource, provided by the client when initially creating
the resource. The resource name must be 1-63 characters long, and comply
withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must be
a dash, lowercase letter, or digit, except the last character, which cannot
be a dash. |
| `instance_schedule_policy` | String | Resource policy for scheduling instance operations. |
| `region` | String |  |
| `resource_status` | String | [Output Only] The system status of the resource policy. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `self_link` | String | [Output Only] Server-defined fully-qualified URL for this resource. |
| `group_placement_policy` | String | Resource policy for instances for placement configuration. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `status` | String | [Output Only] The status of resource policy creation. |
| `disk_consistency_group_policy` | String | Resource policy for disk consistency groups. |
| `snapshot_schedule_policy` | String | Resource policy for persistent disks for creating snapshots. |
| `workload_policy` | String | Resource policy for defining instance placement for MIGs. |
| `description` | String |  |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create resource_policie
resource_policie = provider.compute_api.Resource_policie {
    project = "value"  # Project ID for this request.
    region = "value"  # Name of the region for this request.
}

# Access resource_policie outputs
resource_policie_id = resource_policie.id
resource_policie_kind = resource_policie.kind
resource_policie_name = resource_policie.name
resource_policie_instance_schedule_policy = resource_policie.instance_schedule_policy
resource_policie_region = resource_policie.region
resource_policie_resource_status = resource_policie.resource_status
resource_policie_creation_timestamp = resource_policie.creation_timestamp
resource_policie_self_link = resource_policie.self_link
resource_policie_group_placement_policy = resource_policie.group_placement_policy
resource_policie_id = resource_policie.id
resource_policie_status = resource_policie.status
resource_policie_disk_consistency_group_policy = resource_policie.disk_consistency_group_policy
resource_policie_snapshot_schedule_policy = resource_policie.snapshot_schedule_policy
resource_policie_workload_policy = resource_policie.workload_policy
resource_policie_description = resource_policie.description
```

---


### Region_instant_snapshot

Creates an instant snapshot in the specified region.

**Operations**: ✅ Create ✅ Read ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `label_fingerprint` | String |  | A fingerprint for the labels being applied to this InstantSnapshot, which
is essentially a hash of the labels set used for optimistic locking. The
fingerprint is initially generated by Compute Engine and changes after
every request to modify or update labels. You must always provide an
up-to-date fingerprint hash in order to update or change labels,
otherwise the request will fail with error412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve a InstantSnapshot. |
| `region` | String |  | [Output Only] URL of the region where the instant snapshot resides.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `self_link_with_id` | String |  | [Output Only] Server-defined URL for this resource's resource id. |
| `architecture` | String |  | [Output Only] The architecture of the instant snapshot. Valid values are
ARM64 or X86_64. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `source_disk` | String |  | URL of the source disk used to create this instant snapshot.
Note that the source disk must be in the same zone/region as the
instant snapshot to be created. This can be a full or valid partial URL.
For example, the following are valid values:
   
   
     - 
       https://www.googleapis.com/compute/v1/projects/project/zones/zone/disks/disk
     
     - 
       https://www.googleapis.com/compute/v1/projects/project/regions/region/disks/disk
     
     - 
       projects/project/zones/zone/disks/disk
     
     - 
       projects/project/regions/region/disks/disk
     
     - 
       zones/zone/disks/disk
     
     - 
       regions/region/disks/disk |
| `name` | String |  | Name of the resource; provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must be
a dash, lowercase letter, or digit, except the last character, which cannot
be a dash. |
| `status` | String |  | [Output Only] The status of the instantSnapshot. This can beCREATING, DELETING, FAILED, orREADY. |
| `disk_size_gb` | String |  | [Output Only] Size of the source disk, specified in GB. |
| `satisfies_pzi` | bool |  | Output only. Reserved for future use. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `kind` | String |  | [Output Only] Type of the resource. Alwayscompute#instantSnapshot for InstantSnapshot resources. |
| `source_disk_id` | String |  | [Output Only] The ID value of the disk used to create this InstantSnapshot.
This value may be used to determine whether the InstantSnapshot
was taken from the current or a previous instance of a given disk name. |
| `zone` | String |  | [Output Only] URL of the zone where the instant snapshot resides.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `resource_status` | String |  | [Output Only] Status information for the instant snapshot resource. |
| `labels` | HashMap<String, String> |  | Labels to apply to this InstantSnapshot. These can be later modified by
the setLabels method.
Label values may be empty. |
| `satisfies_pzs` | bool |  | [Output Only] Reserved for future use. |
| `project` | String | ✅ | Project ID for this request. |
| `region` | String | ✅ | Name of the region for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `label_fingerprint` | String | A fingerprint for the labels being applied to this InstantSnapshot, which
is essentially a hash of the labels set used for optimistic locking. The
fingerprint is initially generated by Compute Engine and changes after
every request to modify or update labels. You must always provide an
up-to-date fingerprint hash in order to update or change labels,
otherwise the request will fail with error412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve a InstantSnapshot. |
| `region` | String | [Output Only] URL of the region where the instant snapshot resides.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `self_link_with_id` | String | [Output Only] Server-defined URL for this resource's resource id. |
| `architecture` | String | [Output Only] The architecture of the instant snapshot. Valid values are
ARM64 or X86_64. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `source_disk` | String | URL of the source disk used to create this instant snapshot.
Note that the source disk must be in the same zone/region as the
instant snapshot to be created. This can be a full or valid partial URL.
For example, the following are valid values:
   
   
     - 
       https://www.googleapis.com/compute/v1/projects/project/zones/zone/disks/disk
     
     - 
       https://www.googleapis.com/compute/v1/projects/project/regions/region/disks/disk
     
     - 
       projects/project/zones/zone/disks/disk
     
     - 
       projects/project/regions/region/disks/disk
     
     - 
       zones/zone/disks/disk
     
     - 
       regions/region/disks/disk |
| `name` | String | Name of the resource; provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must be
a dash, lowercase letter, or digit, except the last character, which cannot
be a dash. |
| `status` | String | [Output Only] The status of the instantSnapshot. This can beCREATING, DELETING, FAILED, orREADY. |
| `disk_size_gb` | String | [Output Only] Size of the source disk, specified in GB. |
| `satisfies_pzi` | bool | Output only. Reserved for future use. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `kind` | String | [Output Only] Type of the resource. Alwayscompute#instantSnapshot for InstantSnapshot resources. |
| `source_disk_id` | String | [Output Only] The ID value of the disk used to create this InstantSnapshot.
This value may be used to determine whether the InstantSnapshot
was taken from the current or a previous instance of a given disk name. |
| `zone` | String | [Output Only] URL of the zone where the instant snapshot resides.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `resource_status` | String | [Output Only] Status information for the instant snapshot resource. |
| `labels` | HashMap<String, String> | Labels to apply to this InstantSnapshot. These can be later modified by
the setLabels method.
Label values may be empty. |
| `satisfies_pzs` | bool | [Output Only] Reserved for future use. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create region_instant_snapshot
region_instant_snapshot = provider.compute_api.Region_instant_snapshot {
    project = "value"  # Project ID for this request.
    region = "value"  # Name of the region for this request.
}

# Access region_instant_snapshot outputs
region_instant_snapshot_id = region_instant_snapshot.id
region_instant_snapshot_description = region_instant_snapshot.description
region_instant_snapshot_label_fingerprint = region_instant_snapshot.label_fingerprint
region_instant_snapshot_region = region_instant_snapshot.region
region_instant_snapshot_self_link = region_instant_snapshot.self_link
region_instant_snapshot_self_link_with_id = region_instant_snapshot.self_link_with_id
region_instant_snapshot_architecture = region_instant_snapshot.architecture
region_instant_snapshot_creation_timestamp = region_instant_snapshot.creation_timestamp
region_instant_snapshot_source_disk = region_instant_snapshot.source_disk
region_instant_snapshot_name = region_instant_snapshot.name
region_instant_snapshot_status = region_instant_snapshot.status
region_instant_snapshot_disk_size_gb = region_instant_snapshot.disk_size_gb
region_instant_snapshot_satisfies_pzi = region_instant_snapshot.satisfies_pzi
region_instant_snapshot_id = region_instant_snapshot.id
region_instant_snapshot_kind = region_instant_snapshot.kind
region_instant_snapshot_source_disk_id = region_instant_snapshot.source_disk_id
region_instant_snapshot_zone = region_instant_snapshot.zone
region_instant_snapshot_resource_status = region_instant_snapshot.resource_status
region_instant_snapshot_labels = region_instant_snapshot.labels
region_instant_snapshot_satisfies_pzs = region_instant_snapshot.satisfies_pzs
```

---


### License

Create a License resource in the specified project.
 *Caution* This resource is intended
for use only by third-party partners who are creatingCloud Marketplace
images.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `removable_from_disk` | bool |  | If true, this license can be removed from a disk's set of licenses, with no
replacement license needed. |
| `update_timestamp` | String |  | [Output Only] Last update timestamp inRFC3339
text format. |
| `description` | String |  | An optional textual description of the resource; provided by the client
when the resource is created. |
| `allowed_replacement_licenses` | Vec<String> |  | Specifies licenseCodes of licenses that can replace this license. Note:
such replacements are allowed even if removable_from_disk is false. |
| `appendable_to_disk` | bool |  | If true, this license can be appended to an existing disk's set of
licenses. |
| `kind` | String |  | [Output Only] Type of resource. Always compute#license for
licenses. |
| `os_license` | bool |  | If true, indicates this is an OS license. Only one OS license can be
attached to a disk or image at a time. |
| `name` | String |  | Name of the resource. The name must be 1-63 characters long and
comply withRFC1035. |
| `multi_tenant_only` | bool |  | If true, this license can only be used on VMs on multi tenant nodes. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `incompatible_licenses` | Vec<String> |  | Specifies licenseCodes of licenses that are incompatible with this license.
If a license is incompatible with this license, it cannot be attached to
the same disk or image. |
| `self_link_with_id` | String |  | [Output Only] Server-defined URL for this resource with the resource id. |
| `sole_tenant_only` | bool |  | If true, this license can only be used on VMs on sole tenant nodes. |
| `license_code` | String |  | [Output Only] The unique code used to attach this license to images,
snapshots, and disks. |
| `required_coattached_licenses` | Vec<String> |  | Specifies the set of permissible coattached licenseCodes of licenses that
satisfy the coattachment requirement of this license. At least one license
from the set must be attached to the same disk or image as this license. |
| `transferable` | bool |  | If false, licenses will not be copied from the source resource when
creating an image from a disk, disk from snapshot, or snapshot from disk. |
| `charges_use_fee` | bool |  | [Output Only] Deprecated. This field no longer reflects whether a license
charges a usage fee. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `resource_requirements` | String |  | [Input Only] Deprecated. |
| `minimum_retention` | String |  | If set, this license will be unable to be removed or replaced once attached
to a disk until the minimum_retention period has passed. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `removable_from_disk` | bool | If true, this license can be removed from a disk's set of licenses, with no
replacement license needed. |
| `update_timestamp` | String | [Output Only] Last update timestamp inRFC3339
text format. |
| `description` | String | An optional textual description of the resource; provided by the client
when the resource is created. |
| `allowed_replacement_licenses` | Vec<String> | Specifies licenseCodes of licenses that can replace this license. Note:
such replacements are allowed even if removable_from_disk is false. |
| `appendable_to_disk` | bool | If true, this license can be appended to an existing disk's set of
licenses. |
| `kind` | String | [Output Only] Type of resource. Always compute#license for
licenses. |
| `os_license` | bool | If true, indicates this is an OS license. Only one OS license can be
attached to a disk or image at a time. |
| `name` | String | Name of the resource. The name must be 1-63 characters long and
comply withRFC1035. |
| `multi_tenant_only` | bool | If true, this license can only be used on VMs on multi tenant nodes. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `incompatible_licenses` | Vec<String> | Specifies licenseCodes of licenses that are incompatible with this license.
If a license is incompatible with this license, it cannot be attached to
the same disk or image. |
| `self_link_with_id` | String | [Output Only] Server-defined URL for this resource with the resource id. |
| `sole_tenant_only` | bool | If true, this license can only be used on VMs on sole tenant nodes. |
| `license_code` | String | [Output Only] The unique code used to attach this license to images,
snapshots, and disks. |
| `required_coattached_licenses` | Vec<String> | Specifies the set of permissible coattached licenseCodes of licenses that
satisfy the coattachment requirement of this license. At least one license
from the set must be attached to the same disk or image as this license. |
| `transferable` | bool | If false, licenses will not be copied from the source resource when
creating an image from a disk, disk from snapshot, or snapshot from disk. |
| `charges_use_fee` | bool | [Output Only] Deprecated. This field no longer reflects whether a license
charges a usage fee. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `resource_requirements` | String | [Input Only] Deprecated. |
| `minimum_retention` | String | If set, this license will be unable to be removed or replaced once attached
to a disk until the minimum_retention period has passed. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create license
license = provider.compute_api.License {
    project = "value"  # Project ID for this request.
}

# Access license outputs
license_id = license.id
license_removable_from_disk = license.removable_from_disk
license_update_timestamp = license.update_timestamp
license_description = license.description
license_allowed_replacement_licenses = license.allowed_replacement_licenses
license_appendable_to_disk = license.appendable_to_disk
license_kind = license.kind
license_os_license = license.os_license
license_name = license.name
license_multi_tenant_only = license.multi_tenant_only
license_self_link = license.self_link
license_id = license.id
license_incompatible_licenses = license.incompatible_licenses
license_self_link_with_id = license.self_link_with_id
license_sole_tenant_only = license.sole_tenant_only
license_license_code = license.license_code
license_required_coattached_licenses = license.required_coattached_licenses
license_transferable = license.transferable
license_charges_use_fee = license.charges_use_fee
license_creation_timestamp = license.creation_timestamp
license_resource_requirements = license.resource_requirements
license_minimum_retention = license.minimum_retention
```

---


### Ssl_policie

Returns the specified SSL policy resource.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `warnings` | Vec<String> |  | [Output Only] If potential misconfigurations are detected for this
SSL policy, this field will be populated with warning messages. |
| `enabled_features` | Vec<String> |  | [Output Only] The list of features enabled in the SSL policy. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `name` | String |  | Name of the resource. The name must be 1-63 characters long, and comply
with RFC1035. Specifically, the name must be 1-63 characters
long and match the regular expression
`[a-z]([-a-z0-9]*[a-z0-9])?` which means the first character
must be a lowercase letter, and all following characters must be a dash,
lowercase letter, or digit, except the last character, which cannot be a
dash. |
| `profile` | String |  | Profile specifies the set of SSL features that can be used by the load
balancer when negotiating SSL with clients. This can be one ofCOMPATIBLE, MODERN, RESTRICTED, orCUSTOM. If using CUSTOM, the set of SSL features
to enable must be specified in the customFeatures field. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `custom_features` | Vec<String> |  | A list of features enabled when the selected profile is CUSTOM. The
 method returns the set of features that can be
specified in this list. This field must be empty if the profile is notCUSTOM. |
| `kind` | String |  | [Output only] Type of the resource. Alwayscompute#sslPolicyfor SSL policies. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `fingerprint` | String |  | Fingerprint of this resource. A hash of the contents stored in this object.
This field is used in optimistic locking. This field will be ignored when
inserting a SslPolicy. An up-to-date fingerprint must be
provided in order to update the SslPolicy, otherwise the
request will fail with error 412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve an SslPolicy. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `min_tls_version` | String |  | The minimum version of SSL protocol that can be used by the clients to
establish a connection with the load balancer. This can be one ofTLS_1_0, TLS_1_1, TLS_1_2,TLS_1_3. When set to TLS_1_3, the profile field
must be set to RESTRICTED. |
| `region` | String |  | [Output Only] URL of the region where the regional SSL policy
resides. This field is not applicable to global SSL policies. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `warnings` | Vec<String> | [Output Only] If potential misconfigurations are detected for this
SSL policy, this field will be populated with warning messages. |
| `enabled_features` | Vec<String> | [Output Only] The list of features enabled in the SSL policy. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `name` | String | Name of the resource. The name must be 1-63 characters long, and comply
with RFC1035. Specifically, the name must be 1-63 characters
long and match the regular expression
`[a-z]([-a-z0-9]*[a-z0-9])?` which means the first character
must be a lowercase letter, and all following characters must be a dash,
lowercase letter, or digit, except the last character, which cannot be a
dash. |
| `profile` | String | Profile specifies the set of SSL features that can be used by the load
balancer when negotiating SSL with clients. This can be one ofCOMPATIBLE, MODERN, RESTRICTED, orCUSTOM. If using CUSTOM, the set of SSL features
to enable must be specified in the customFeatures field. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `custom_features` | Vec<String> | A list of features enabled when the selected profile is CUSTOM. The
 method returns the set of features that can be
specified in this list. This field must be empty if the profile is notCUSTOM. |
| `kind` | String | [Output only] Type of the resource. Alwayscompute#sslPolicyfor SSL policies. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `fingerprint` | String | Fingerprint of this resource. A hash of the contents stored in this object.
This field is used in optimistic locking. This field will be ignored when
inserting a SslPolicy. An up-to-date fingerprint must be
provided in order to update the SslPolicy, otherwise the
request will fail with error 412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve an SslPolicy. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `min_tls_version` | String | The minimum version of SSL protocol that can be used by the clients to
establish a connection with the load balancer. This can be one ofTLS_1_0, TLS_1_1, TLS_1_2,TLS_1_3. When set to TLS_1_3, the profile field
must be set to RESTRICTED. |
| `region` | String | [Output Only] URL of the region where the regional SSL policy
resides. This field is not applicable to global SSL policies. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create ssl_policie
ssl_policie = provider.compute_api.Ssl_policie {
    project = "value"  # Project ID for this request.
}

# Access ssl_policie outputs
ssl_policie_id = ssl_policie.id
ssl_policie_warnings = ssl_policie.warnings
ssl_policie_enabled_features = ssl_policie.enabled_features
ssl_policie_description = ssl_policie.description
ssl_policie_name = ssl_policie.name
ssl_policie_profile = ssl_policie.profile
ssl_policie_self_link = ssl_policie.self_link
ssl_policie_custom_features = ssl_policie.custom_features
ssl_policie_kind = ssl_policie.kind
ssl_policie_creation_timestamp = ssl_policie.creation_timestamp
ssl_policie_fingerprint = ssl_policie.fingerprint
ssl_policie_id = ssl_policie.id
ssl_policie_min_tls_version = ssl_policie.min_tls_version
ssl_policie_region = ssl_policie.region
```

---


### Global_forwarding_rule

Creates a GlobalForwardingRule resource in the specified project using
the data included in the request.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `ports` | Vec<String> |  | The ports, portRange, and allPorts
fields are mutually exclusive. Only packets addressed to ports in the
specified range will be forwarded to the backends configured with this
forwarding rule.

The ports field has the following limitations:
   
   - It requires that the forwarding rule IPProtocol be TCP,
   UDP, or SCTP, and
   - It's applicable only to the following products: internal passthrough
   Network Load Balancers, backend service-based external passthrough Network
   Load Balancers, and internal protocol forwarding.
   - You can specify a list of up to five ports by number, separated by
   commas. The ports can be contiguous or discontiguous.



For external forwarding rules, two or more forwarding rules cannot use the
same [IPAddress, IPProtocol] pair if they share at least one
port number.

For internal forwarding rules within the same VPC network, two or more
forwarding rules cannot use the same [IPAddress, IPProtocol]
pair if they share at least one port number.

@pattern: \\d+(?:-\\d+)? |
| `base_forwarding_rule` | String |  | [Output Only] The URL for the corresponding base forwarding rule. By base
forwarding rule, we mean the forwarding rule that has the same IP address,
protocol, and port settings with the current forwarding rule, but without
sourceIPRanges specified.
Always empty if the current forwarding rule does not have sourceIPRanges
specified. |
| `load_balancing_scheme` | String |  | Specifies the forwarding rule type.

For more information about forwarding rules, refer to
Forwarding rule concepts. |
| `self_link_with_id` | String |  | [Output Only] Server-defined URL for this resource with the resource id. |
| `region` | String |  | [Output Only] URL of the region where the regional forwarding rule resides.
This field is not applicable to global forwarding rules.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `network_tier` | String |  | This signifies the networking tier used for configuring
this load balancer and can only take the following values:PREMIUM, STANDARD.

For regional ForwardingRule, the valid values are PREMIUM andSTANDARD. For GlobalForwardingRule, the valid value isPREMIUM.

If this field is not specified, it is assumed to be PREMIUM.
If IPAddress is specified, this value must be equal to the
networkTier of the Address. |
| `psc_connection_id` | String |  | [Output Only] The PSC connection id of the PSC forwarding rule. |
| `source_ip_ranges` | Vec<String> |  | If not empty, this forwarding rule will only forward the traffic when the
source IP address matches one of the IP addresses or CIDR ranges set here.
Note that a forwarding rule can only have up to 64 source IP ranges, and
this field can only be used with a regional forwarding rule whose scheme isEXTERNAL.
Each source_ip_range entry should be either an IP address (for
example, 1.2.3.4) or a CIDR range (for example, 1.2.3.0/24). |
| `port_range` | String |  | The ports, portRange, and allPorts
fields are mutually exclusive. Only packets addressed to ports in the
specified range will be forwarded to the backends configured with this
forwarding rule.

The portRange field has the following limitations:
   
   - It requires that the forwarding rule IPProtocol be TCP,
   UDP, or SCTP, and
   - It's applicable only to the following products: external passthrough
   Network Load Balancers, internal and external proxy Network Load Balancers,
   internal and external Application Load Balancers, external protocol
   forwarding, and Classic VPN.
   - Some products have restrictions on what ports can be used. See 
   port specifications for details.



For external forwarding rules, two or more forwarding rules cannot use the
same [IPAddress, IPProtocol] pair, and cannot have overlappingportRanges.

For internal forwarding rules within the same VPC network, two or more
forwarding rules cannot use the same [IPAddress, IPProtocol]
pair, and cannot have overlapping portRanges.

@pattern: \\d+(?:-\\d+)? |
| `name` | String |  | Name of the resource; provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash.

For Private Service Connect forwarding rules that forward traffic to Google
APIs, the forwarding rule name must be a 1-20 characters string with
lowercase letters and numbers and must start with a letter. |
| `backend_service` | String |  | Identifies the backend service to which the forwarding rule sends traffic.
Required for internal and external passthrough Network Load Balancers;
must be omitted for all other load balancer types. |
| `all_ports` | bool |  | The ports, portRange, and allPorts
fields are mutually exclusive. Only packets addressed to ports in the
specified range will be forwarded to the backends configured with this
forwarding rule.

The allPorts field has the following limitations:
   
   - It requires that the forwarding rule IPProtocol be TCP,
   UDP, SCTP, or L3_DEFAULT.
   - It's applicable only to the following products: internal passthrough
   Network Load Balancers, backend service-based external passthrough Network
   Load Balancers, and internal and external protocol forwarding.
   - Set this field to true to allow packets addressed to any port or
   packets lacking destination port information (for example, UDP fragments
   after the first fragment) to be forwarded to the backends configured with
   this forwarding rule. The L3_DEFAULT protocol requiresallPorts be set to true. |
| `fingerprint` | String |  | Fingerprint of this resource. A hash of the contents stored in this object.
This field is used in optimistic locking. This field will be ignored when
inserting a ForwardingRule. Include the fingerprint in patch request to
ensure that you do not overwrite changes that were applied from another
concurrent request.

To see the latest fingerprint, make a get() request to
retrieve a ForwardingRule. |
| `external_managed_backend_bucket_migration_state` | String |  | Specifies the canary migration state for the backend buckets attached to
this forwarding rule. Possible values are PREPARE, TEST_BY_PERCENTAGE, and
TEST_ALL_TRAFFIC.

To begin the migration from EXTERNAL to EXTERNAL_MANAGED, the state must be
changed to PREPARE. The state must be changed to TEST_ALL_TRAFFIC before
the loadBalancingScheme can be changed to EXTERNAL_MANAGED. Optionally, the
TEST_BY_PERCENTAGE state can be used to migrate traffic to backend buckets
attached to this forwarding rule by percentage using
externalManagedBackendBucketMigrationTestingPercentage.

Rolling back a migration requires the states to be set in reverse order. So
changing the scheme from EXTERNAL_MANAGED to EXTERNAL requires the state to
be set to TEST_ALL_TRAFFIC at the same time. Optionally, the
TEST_BY_PERCENTAGE state can be used to migrate some traffic back to
EXTERNAL or PREPARE can be used to migrate all traffic back to EXTERNAL. |
| `service_directory_registrations` | Vec<String> |  | Service Directory resources to register this forwarding rule with.
Currently, only supports a single Service Directory resource. |
| `service_label` | String |  | An optional prefix to the service name for this forwarding rule.
If specified, the prefix is the first label of the fully qualified service
name.

The label must be 1-63 characters long, and comply withRFC1035.
Specifically, the label must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash.

This field is only used for internal load balancing. |
| `ip_address` | String |  | IP address for which this forwarding rule accepts traffic. When a client
sends traffic to this IP address, the forwarding rule directs the traffic
to the referenced target or backendService.
While creating a forwarding rule, specifying an IPAddress is
required under the following circumstances:

   
   - When the target is set to targetGrpcProxy andvalidateForProxyless is set to true, theIPAddress should be set to 0.0.0.0.
   - When the target is a Private Service Connect Google APIs
   bundle, you must specify an IPAddress.


Otherwise, you can optionally specify an IP address that references an
existing static (reserved) IP address resource. When omitted, Google Cloud
assigns an ephemeral IP address.

Use one of the following formats to specify an IP address while creating a
forwarding rule:

* IP address number, as in `100.1.2.3`
* IPv6 address range, as in `2600:1234::/96`
* Full resource URL, as inhttps://www.googleapis.com/compute/v1/projects/project_id/regions/region/addresses/address-name
* Partial URL or by name, as in:
   
   - projects/project_id/regions/region/addresses/address-name
   - regions/region/addresses/address-name
   - global/addresses/address-name
   - address-name



The forwarding rule's target or backendService,
and in most cases, also the loadBalancingScheme, determine the
type of IP address that you can use. For detailed information, see
[IP address
specifications](https://cloud.google.com/load-balancing/docs/forwarding-rule-concepts#ip_address_specifications).

When reading an IPAddress, the API always returns the IP
address number. |
| `network` | String |  | This field is not used for global external load balancing.

For internal passthrough Network Load Balancers, this field identifies the
network that the load balanced IP should belong to for this forwarding
rule.
If the subnetwork is specified, the network of the subnetwork will be used.
If neither subnetwork nor this field is specified, the default network will
be used.

For Private Service Connect forwarding rules that forward traffic to Google
APIs, a network must be provided. |
| `kind` | String |  | [Output Only] Type of the resource. Alwayscompute#forwardingRule for forwarding rule resources. |
| `allow_psc_global_access` | bool |  | This is used in PSC consumer ForwardingRule to control whether the PSC
endpoint can be accessed from another region. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `ip_collection` | String |  | Resource reference of a PublicDelegatedPrefix. The PDP must
be a sub-PDP in EXTERNAL_IPV6_FORWARDING_RULE_CREATION mode.

Use one of the following formats to specify a sub-PDP when creating an IPv6
NetLB forwarding rule using BYOIP:
Full resource URL, as inhttps://www.googleapis.com/compute/v1/projects/project_id/regions/region/publicDelegatedPrefixes/sub-pdp-name
Partial URL, as in:
   
   - projects/project_id/regions/region/publicDelegatedPrefixes/sub-pdp-name
   - regions/region/publicDelegatedPrefixes/sub-pdp-name |
| `target` | String |  | The URL of the target resource to receive the matched traffic.  For
regional forwarding rules, this target must be in the same region as the
forwarding rule. For global forwarding rules, this target must be a global
load balancing resource.

The forwarded traffic must be of a type appropriate to the target object.
   
   
     -  For load balancers, see the "Target" column in [Port specifications](https://cloud.google.com/load-balancing/docs/forwarding-rule-concepts#ip_address_specifications).
     -  For Private Service Connect forwarding rules that forward traffic to Google APIs, provide the name of a supported Google API bundle:
       
      
            -  vpc-sc -  APIs that support VPC Service Controls. 
            -  all-apis - All supported Google APIs. 
          
     
     -  For Private Service Connect forwarding rules that forward traffic to managed services, the target must be a service attachment. The target is not mutable once set as a service attachment. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `ip_version` | String |  | The IP Version that will be used by this forwarding rule.  Valid options
are IPV4 or IPV6. |
| `external_managed_backend_bucket_migration_testing_percentage` | f64 |  | Determines the fraction of requests to backend buckets that should be
processed by the global external Application Load Balancer.

The value of this field must be in the range [0, 100].

This value can only be set if the loadBalancingScheme in the BackendService
is set to EXTERNAL (when using the classic Application Load Balancer) and
the migration state is TEST_BY_PERCENTAGE. |
| `label_fingerprint` | String |  | A fingerprint for the labels being applied to this resource, which is
essentially a hash of the labels set used for optimistic locking. The
fingerprint is initially generated by Compute Engine and changes after
every request to modify or update labels. You must always provide an
up-to-date fingerprint hash in order to update or change labels,
otherwise the request will fail with error412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve a ForwardingRule. |
| `is_mirroring_collector` | bool |  | Indicates whether or not this load balancer can be used as a collector for
packet mirroring. To prevent mirroring loops, instances behind this
load balancer will not have their traffic mirrored even if aPacketMirroring rule applies to them.
This can only be set to true for load balancers that have theirloadBalancingScheme set to INTERNAL. |
| `allow_global_access` | bool |  | If set to true, clients can access the internal passthrough Network Load
Balancers, the regional internal Application Load Balancer, and the
regional internal proxy Network Load Balancer from all regions.
If false, only allows access from the local region the load balancer is
located at. Note that for INTERNAL_MANAGED forwarding rules, this field
cannot be changed after the forwarding rule is created. |
| `labels` | HashMap<String, String> |  | Labels for this resource. These can only be added or modified by thesetLabels method. Each label key/value pair must comply withRFC1035.
Label values may be empty. |
| `no_automate_dns_zone` | bool |  | This is used in PSC consumer ForwardingRule to control whether it should
try to auto-generate a DNS zone or not. Non-PSC forwarding rules do not use
this field. Once set, this field is not mutable. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `ip_protocol` | String |  | The IP protocol to which this rule applies.

For protocol forwarding, valid
options are TCP, UDP, ESP,AH, SCTP, ICMP andL3_DEFAULT.

The valid IP protocols are different for different load balancing products
as described in [Load balancing
features](https://cloud.google.com/load-balancing/docs/features#protocols_from_the_load_balancer_to_the_backends). |
| `service_name` | String |  | [Output Only]
The internal fully qualified service name for this forwarding rule.

This field is only used for internal load balancing. |
| `psc_connection_status` | String |  |  |
| `subnetwork` | String |  | This field identifies the subnetwork that the load balanced IP should
belong to for this forwarding rule, used with internal load balancers and
external passthrough Network Load Balancers with IPv6.

If the network specified is in auto subnet mode, this field is optional.
However, a subnetwork must be specified if the network is in custom subnet
mode or when creating external forwarding rule with IPv6. |
| `metadata_filters` | Vec<String> |  | Opaque filter criteria used by load balancer to restrict routing
configuration to a limited set of xDS
compliant clients. In their xDS requests to load balancer, xDS clients
present node
metadata. When there is a match, the relevant configuration
is made available to those proxies. Otherwise, all the resources (e.g.TargetHttpProxy, UrlMap)
referenced by the ForwardingRule are not visible to
those proxies.

For each metadataFilter in this list, if itsfilterMatchCriteria is set to MATCH_ANY, at least one of thefilterLabels must match the corresponding label provided in
the metadata. If its filterMatchCriteria is set to
MATCH_ALL, then all of its filterLabels must match with
corresponding labels provided in the metadata. If multiplemetadataFilters are specified, all of them need to be satisfied
in order to be considered a match.

metadataFilters specified here will be applifed before
those specified in the UrlMap that thisForwardingRule references.

metadataFilters only applies to Loadbalancers that have
their loadBalancingScheme set toINTERNAL_SELF_MANAGED. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `ports` | Vec<String> | The ports, portRange, and allPorts
fields are mutually exclusive. Only packets addressed to ports in the
specified range will be forwarded to the backends configured with this
forwarding rule.

The ports field has the following limitations:
   
   - It requires that the forwarding rule IPProtocol be TCP,
   UDP, or SCTP, and
   - It's applicable only to the following products: internal passthrough
   Network Load Balancers, backend service-based external passthrough Network
   Load Balancers, and internal protocol forwarding.
   - You can specify a list of up to five ports by number, separated by
   commas. The ports can be contiguous or discontiguous.



For external forwarding rules, two or more forwarding rules cannot use the
same [IPAddress, IPProtocol] pair if they share at least one
port number.

For internal forwarding rules within the same VPC network, two or more
forwarding rules cannot use the same [IPAddress, IPProtocol]
pair if they share at least one port number.

@pattern: \\d+(?:-\\d+)? |
| `base_forwarding_rule` | String | [Output Only] The URL for the corresponding base forwarding rule. By base
forwarding rule, we mean the forwarding rule that has the same IP address,
protocol, and port settings with the current forwarding rule, but without
sourceIPRanges specified.
Always empty if the current forwarding rule does not have sourceIPRanges
specified. |
| `load_balancing_scheme` | String | Specifies the forwarding rule type.

For more information about forwarding rules, refer to
Forwarding rule concepts. |
| `self_link_with_id` | String | [Output Only] Server-defined URL for this resource with the resource id. |
| `region` | String | [Output Only] URL of the region where the regional forwarding rule resides.
This field is not applicable to global forwarding rules.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `network_tier` | String | This signifies the networking tier used for configuring
this load balancer and can only take the following values:PREMIUM, STANDARD.

For regional ForwardingRule, the valid values are PREMIUM andSTANDARD. For GlobalForwardingRule, the valid value isPREMIUM.

If this field is not specified, it is assumed to be PREMIUM.
If IPAddress is specified, this value must be equal to the
networkTier of the Address. |
| `psc_connection_id` | String | [Output Only] The PSC connection id of the PSC forwarding rule. |
| `source_ip_ranges` | Vec<String> | If not empty, this forwarding rule will only forward the traffic when the
source IP address matches one of the IP addresses or CIDR ranges set here.
Note that a forwarding rule can only have up to 64 source IP ranges, and
this field can only be used with a regional forwarding rule whose scheme isEXTERNAL.
Each source_ip_range entry should be either an IP address (for
example, 1.2.3.4) or a CIDR range (for example, 1.2.3.0/24). |
| `port_range` | String | The ports, portRange, and allPorts
fields are mutually exclusive. Only packets addressed to ports in the
specified range will be forwarded to the backends configured with this
forwarding rule.

The portRange field has the following limitations:
   
   - It requires that the forwarding rule IPProtocol be TCP,
   UDP, or SCTP, and
   - It's applicable only to the following products: external passthrough
   Network Load Balancers, internal and external proxy Network Load Balancers,
   internal and external Application Load Balancers, external protocol
   forwarding, and Classic VPN.
   - Some products have restrictions on what ports can be used. See 
   port specifications for details.



For external forwarding rules, two or more forwarding rules cannot use the
same [IPAddress, IPProtocol] pair, and cannot have overlappingportRanges.

For internal forwarding rules within the same VPC network, two or more
forwarding rules cannot use the same [IPAddress, IPProtocol]
pair, and cannot have overlapping portRanges.

@pattern: \\d+(?:-\\d+)? |
| `name` | String | Name of the resource; provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash.

For Private Service Connect forwarding rules that forward traffic to Google
APIs, the forwarding rule name must be a 1-20 characters string with
lowercase letters and numbers and must start with a letter. |
| `backend_service` | String | Identifies the backend service to which the forwarding rule sends traffic.
Required for internal and external passthrough Network Load Balancers;
must be omitted for all other load balancer types. |
| `all_ports` | bool | The ports, portRange, and allPorts
fields are mutually exclusive. Only packets addressed to ports in the
specified range will be forwarded to the backends configured with this
forwarding rule.

The allPorts field has the following limitations:
   
   - It requires that the forwarding rule IPProtocol be TCP,
   UDP, SCTP, or L3_DEFAULT.
   - It's applicable only to the following products: internal passthrough
   Network Load Balancers, backend service-based external passthrough Network
   Load Balancers, and internal and external protocol forwarding.
   - Set this field to true to allow packets addressed to any port or
   packets lacking destination port information (for example, UDP fragments
   after the first fragment) to be forwarded to the backends configured with
   this forwarding rule. The L3_DEFAULT protocol requiresallPorts be set to true. |
| `fingerprint` | String | Fingerprint of this resource. A hash of the contents stored in this object.
This field is used in optimistic locking. This field will be ignored when
inserting a ForwardingRule. Include the fingerprint in patch request to
ensure that you do not overwrite changes that were applied from another
concurrent request.

To see the latest fingerprint, make a get() request to
retrieve a ForwardingRule. |
| `external_managed_backend_bucket_migration_state` | String | Specifies the canary migration state for the backend buckets attached to
this forwarding rule. Possible values are PREPARE, TEST_BY_PERCENTAGE, and
TEST_ALL_TRAFFIC.

To begin the migration from EXTERNAL to EXTERNAL_MANAGED, the state must be
changed to PREPARE. The state must be changed to TEST_ALL_TRAFFIC before
the loadBalancingScheme can be changed to EXTERNAL_MANAGED. Optionally, the
TEST_BY_PERCENTAGE state can be used to migrate traffic to backend buckets
attached to this forwarding rule by percentage using
externalManagedBackendBucketMigrationTestingPercentage.

Rolling back a migration requires the states to be set in reverse order. So
changing the scheme from EXTERNAL_MANAGED to EXTERNAL requires the state to
be set to TEST_ALL_TRAFFIC at the same time. Optionally, the
TEST_BY_PERCENTAGE state can be used to migrate some traffic back to
EXTERNAL or PREPARE can be used to migrate all traffic back to EXTERNAL. |
| `service_directory_registrations` | Vec<String> | Service Directory resources to register this forwarding rule with.
Currently, only supports a single Service Directory resource. |
| `service_label` | String | An optional prefix to the service name for this forwarding rule.
If specified, the prefix is the first label of the fully qualified service
name.

The label must be 1-63 characters long, and comply withRFC1035.
Specifically, the label must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash.

This field is only used for internal load balancing. |
| `ip_address` | String | IP address for which this forwarding rule accepts traffic. When a client
sends traffic to this IP address, the forwarding rule directs the traffic
to the referenced target or backendService.
While creating a forwarding rule, specifying an IPAddress is
required under the following circumstances:

   
   - When the target is set to targetGrpcProxy andvalidateForProxyless is set to true, theIPAddress should be set to 0.0.0.0.
   - When the target is a Private Service Connect Google APIs
   bundle, you must specify an IPAddress.


Otherwise, you can optionally specify an IP address that references an
existing static (reserved) IP address resource. When omitted, Google Cloud
assigns an ephemeral IP address.

Use one of the following formats to specify an IP address while creating a
forwarding rule:

* IP address number, as in `100.1.2.3`
* IPv6 address range, as in `2600:1234::/96`
* Full resource URL, as inhttps://www.googleapis.com/compute/v1/projects/project_id/regions/region/addresses/address-name
* Partial URL or by name, as in:
   
   - projects/project_id/regions/region/addresses/address-name
   - regions/region/addresses/address-name
   - global/addresses/address-name
   - address-name



The forwarding rule's target or backendService,
and in most cases, also the loadBalancingScheme, determine the
type of IP address that you can use. For detailed information, see
[IP address
specifications](https://cloud.google.com/load-balancing/docs/forwarding-rule-concepts#ip_address_specifications).

When reading an IPAddress, the API always returns the IP
address number. |
| `network` | String | This field is not used for global external load balancing.

For internal passthrough Network Load Balancers, this field identifies the
network that the load balanced IP should belong to for this forwarding
rule.
If the subnetwork is specified, the network of the subnetwork will be used.
If neither subnetwork nor this field is specified, the default network will
be used.

For Private Service Connect forwarding rules that forward traffic to Google
APIs, a network must be provided. |
| `kind` | String | [Output Only] Type of the resource. Alwayscompute#forwardingRule for forwarding rule resources. |
| `allow_psc_global_access` | bool | This is used in PSC consumer ForwardingRule to control whether the PSC
endpoint can be accessed from another region. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `ip_collection` | String | Resource reference of a PublicDelegatedPrefix. The PDP must
be a sub-PDP in EXTERNAL_IPV6_FORWARDING_RULE_CREATION mode.

Use one of the following formats to specify a sub-PDP when creating an IPv6
NetLB forwarding rule using BYOIP:
Full resource URL, as inhttps://www.googleapis.com/compute/v1/projects/project_id/regions/region/publicDelegatedPrefixes/sub-pdp-name
Partial URL, as in:
   
   - projects/project_id/regions/region/publicDelegatedPrefixes/sub-pdp-name
   - regions/region/publicDelegatedPrefixes/sub-pdp-name |
| `target` | String | The URL of the target resource to receive the matched traffic.  For
regional forwarding rules, this target must be in the same region as the
forwarding rule. For global forwarding rules, this target must be a global
load balancing resource.

The forwarded traffic must be of a type appropriate to the target object.
   
   
     -  For load balancers, see the "Target" column in [Port specifications](https://cloud.google.com/load-balancing/docs/forwarding-rule-concepts#ip_address_specifications).
     -  For Private Service Connect forwarding rules that forward traffic to Google APIs, provide the name of a supported Google API bundle:
       
      
            -  vpc-sc -  APIs that support VPC Service Controls. 
            -  all-apis - All supported Google APIs. 
          
     
     -  For Private Service Connect forwarding rules that forward traffic to managed services, the target must be a service attachment. The target is not mutable once set as a service attachment. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `ip_version` | String | The IP Version that will be used by this forwarding rule.  Valid options
are IPV4 or IPV6. |
| `external_managed_backend_bucket_migration_testing_percentage` | f64 | Determines the fraction of requests to backend buckets that should be
processed by the global external Application Load Balancer.

The value of this field must be in the range [0, 100].

This value can only be set if the loadBalancingScheme in the BackendService
is set to EXTERNAL (when using the classic Application Load Balancer) and
the migration state is TEST_BY_PERCENTAGE. |
| `label_fingerprint` | String | A fingerprint for the labels being applied to this resource, which is
essentially a hash of the labels set used for optimistic locking. The
fingerprint is initially generated by Compute Engine and changes after
every request to modify or update labels. You must always provide an
up-to-date fingerprint hash in order to update or change labels,
otherwise the request will fail with error412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve a ForwardingRule. |
| `is_mirroring_collector` | bool | Indicates whether or not this load balancer can be used as a collector for
packet mirroring. To prevent mirroring loops, instances behind this
load balancer will not have their traffic mirrored even if aPacketMirroring rule applies to them.
This can only be set to true for load balancers that have theirloadBalancingScheme set to INTERNAL. |
| `allow_global_access` | bool | If set to true, clients can access the internal passthrough Network Load
Balancers, the regional internal Application Load Balancer, and the
regional internal proxy Network Load Balancer from all regions.
If false, only allows access from the local region the load balancer is
located at. Note that for INTERNAL_MANAGED forwarding rules, this field
cannot be changed after the forwarding rule is created. |
| `labels` | HashMap<String, String> | Labels for this resource. These can only be added or modified by thesetLabels method. Each label key/value pair must comply withRFC1035.
Label values may be empty. |
| `no_automate_dns_zone` | bool | This is used in PSC consumer ForwardingRule to control whether it should
try to auto-generate a DNS zone or not. Non-PSC forwarding rules do not use
this field. Once set, this field is not mutable. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `ip_protocol` | String | The IP protocol to which this rule applies.

For protocol forwarding, valid
options are TCP, UDP, ESP,AH, SCTP, ICMP andL3_DEFAULT.

The valid IP protocols are different for different load balancing products
as described in [Load balancing
features](https://cloud.google.com/load-balancing/docs/features#protocols_from_the_load_balancer_to_the_backends). |
| `service_name` | String | [Output Only]
The internal fully qualified service name for this forwarding rule.

This field is only used for internal load balancing. |
| `psc_connection_status` | String |  |
| `subnetwork` | String | This field identifies the subnetwork that the load balanced IP should
belong to for this forwarding rule, used with internal load balancers and
external passthrough Network Load Balancers with IPv6.

If the network specified is in auto subnet mode, this field is optional.
However, a subnetwork must be specified if the network is in custom subnet
mode or when creating external forwarding rule with IPv6. |
| `metadata_filters` | Vec<String> | Opaque filter criteria used by load balancer to restrict routing
configuration to a limited set of xDS
compliant clients. In their xDS requests to load balancer, xDS clients
present node
metadata. When there is a match, the relevant configuration
is made available to those proxies. Otherwise, all the resources (e.g.TargetHttpProxy, UrlMap)
referenced by the ForwardingRule are not visible to
those proxies.

For each metadataFilter in this list, if itsfilterMatchCriteria is set to MATCH_ANY, at least one of thefilterLabels must match the corresponding label provided in
the metadata. If its filterMatchCriteria is set to
MATCH_ALL, then all of its filterLabels must match with
corresponding labels provided in the metadata. If multiplemetadataFilters are specified, all of them need to be satisfied
in order to be considered a match.

metadataFilters specified here will be applifed before
those specified in the UrlMap that thisForwardingRule references.

metadataFilters only applies to Loadbalancers that have
their loadBalancingScheme set toINTERNAL_SELF_MANAGED. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create global_forwarding_rule
global_forwarding_rule = provider.compute_api.Global_forwarding_rule {
    project = "value"  # Project ID for this request.
}

# Access global_forwarding_rule outputs
global_forwarding_rule_id = global_forwarding_rule.id
global_forwarding_rule_ports = global_forwarding_rule.ports
global_forwarding_rule_base_forwarding_rule = global_forwarding_rule.base_forwarding_rule
global_forwarding_rule_load_balancing_scheme = global_forwarding_rule.load_balancing_scheme
global_forwarding_rule_self_link_with_id = global_forwarding_rule.self_link_with_id
global_forwarding_rule_region = global_forwarding_rule.region
global_forwarding_rule_self_link = global_forwarding_rule.self_link
global_forwarding_rule_network_tier = global_forwarding_rule.network_tier
global_forwarding_rule_psc_connection_id = global_forwarding_rule.psc_connection_id
global_forwarding_rule_source_ip_ranges = global_forwarding_rule.source_ip_ranges
global_forwarding_rule_port_range = global_forwarding_rule.port_range
global_forwarding_rule_name = global_forwarding_rule.name
global_forwarding_rule_backend_service = global_forwarding_rule.backend_service
global_forwarding_rule_all_ports = global_forwarding_rule.all_ports
global_forwarding_rule_fingerprint = global_forwarding_rule.fingerprint
global_forwarding_rule_external_managed_backend_bucket_migration_state = global_forwarding_rule.external_managed_backend_bucket_migration_state
global_forwarding_rule_service_directory_registrations = global_forwarding_rule.service_directory_registrations
global_forwarding_rule_service_label = global_forwarding_rule.service_label
global_forwarding_rule_ip_address = global_forwarding_rule.ip_address
global_forwarding_rule_network = global_forwarding_rule.network
global_forwarding_rule_kind = global_forwarding_rule.kind
global_forwarding_rule_allow_psc_global_access = global_forwarding_rule.allow_psc_global_access
global_forwarding_rule_id = global_forwarding_rule.id
global_forwarding_rule_ip_collection = global_forwarding_rule.ip_collection
global_forwarding_rule_target = global_forwarding_rule.target
global_forwarding_rule_creation_timestamp = global_forwarding_rule.creation_timestamp
global_forwarding_rule_ip_version = global_forwarding_rule.ip_version
global_forwarding_rule_external_managed_backend_bucket_migration_testing_percentage = global_forwarding_rule.external_managed_backend_bucket_migration_testing_percentage
global_forwarding_rule_label_fingerprint = global_forwarding_rule.label_fingerprint
global_forwarding_rule_is_mirroring_collector = global_forwarding_rule.is_mirroring_collector
global_forwarding_rule_allow_global_access = global_forwarding_rule.allow_global_access
global_forwarding_rule_labels = global_forwarding_rule.labels
global_forwarding_rule_no_automate_dns_zone = global_forwarding_rule.no_automate_dns_zone
global_forwarding_rule_description = global_forwarding_rule.description
global_forwarding_rule_ip_protocol = global_forwarding_rule.ip_protocol
global_forwarding_rule_service_name = global_forwarding_rule.service_name
global_forwarding_rule_psc_connection_status = global_forwarding_rule.psc_connection_status
global_forwarding_rule_subnetwork = global_forwarding_rule.subnetwork
global_forwarding_rule_metadata_filters = global_forwarding_rule.metadata_filters
```

---


### Zone

Returns the specified Zone resource.

**Operations**: ✅ Read

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `kind` | String | [Output Only] Type of the resource. Always compute#zone for
zones. |
| `available_cpu_platforms` | Vec<String> | [Output Only] Available cpu/platform selections for the zone. |
| `status` | String | [Output Only] Status of the zone, either UP orDOWN. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `supports_pzs` | bool | [Output Only] Reserved for future use. |
| `name` | String | [Output Only] Name of the resource. |
| `deprecated` | String | [Output Only] The deprecation status associated with this zone. |
| `region` | String | [Output Only] Full URL reference to the region which hosts the zone. |
| `description` | String | [Output Only] Textual description of the resource. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Access zone outputs
zone_id = zone.id
zone_kind = zone.kind
zone_available_cpu_platforms = zone.available_cpu_platforms
zone_status = zone.status
zone_self_link = zone.self_link
zone_id = zone.id
zone_creation_timestamp = zone.creation_timestamp
zone_supports_pzs = zone.supports_pzs
zone_name = zone.name
zone_deprecated = zone.deprecated
zone_region = zone.region
zone_description = zone.description
```

---


### Zone_operation

Waits for the specified Operation resource to return as `DONE`
or for the request to approach the 2 minute deadline, and retrieves the
specified Operation resource. This method waits for no more than the
2 minutes and then returns the current state of the
operation, which might be `DONE` or still in progress.

This method is called on a best-effort basis. Specifically:
   
   
    - In uncommon cases, when the server is overloaded, the request might
    return before the default deadline is reached, or might return after zero
    seconds.
   - If the default deadline is reached, there is no guarantee that the
    operation is actually done when the method returns. Be prepared to retry
    if the operation is not `DONE`.

**Operations**: ✅ Create ✅ Read ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `operation` | String | ✅ | Name of the Operations resource to return, or its unique numeric
identifier. |
| `project` | String | ✅ | Project ID for this request. |
| `zone` | String | ✅ | Name of the zone for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `insert_time` | String | [Output Only] The time that this operation was requested.
This value is inRFC3339
text format. |
| `description` | String | [Output Only] A textual description of the operation, which is
set when the operation is created. |
| `region` | String | [Output Only] The URL of the region where the operation resides. Only
applicable when performing regional operations. |
| `end_time` | String | [Output Only] The time that this operation was completed. This value is inRFC3339
text format. |
| `http_error_message` | String | [Output Only] If the operation fails, this field contains the HTTP error
message that was returned, such as `NOT FOUND`. |
| `http_error_status_code` | i64 | [Output Only] If the operation fails, this field contains the HTTP error
status code that was returned. For example, a `404` means the
resource was not found. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `set_common_instance_metadata_operation_metadata` | String | [Output Only] If the operation is for projects.setCommonInstanceMetadata,
this field will contain information on all underlying zonal actions and
their state. |
| `start_time` | String | [Output Only] The time that this operation was started by the server.
This value is inRFC3339
text format. |
| `target_id` | String | [Output Only] The unique target ID, which identifies a specific incarnation
of the target resource. |
| `target_link` | String | [Output Only] The URL of the resource that the operation modifies. For
operations related to creating a snapshot, this points to the disk
that the snapshot was created from. |
| `operation_group_id` | String | [Output Only] An ID that represents a group of operations, such as when a
group of operations results from a `bulkInsert` API request. |
| `name` | String | [Output Only] Name of the operation. |
| `user` | String | [Output Only] User who requested the operation, for example:
`user@example.com` or
`alice_smith_identifier (global/workforcePools/example-com-us-employees)`. |
| `creation_timestamp` | String | [Deprecated] This field is deprecated. |
| `error` | String | [Output Only] If errors are generated during processing of the operation,
this field will be populated. |
| `client_operation_id` | String | [Output Only] The value of `requestId` if you provided it in the request.
Not present otherwise. |
| `status_message` | String | [Output Only] An optional textual description of the current status of the
operation. |
| `status` | String | [Output Only] The status of the operation, which can be one of the
following:
`PENDING`, `RUNNING`, or `DONE`. |
| `id` | String | [Output Only] The unique identifier for the operation. This identifier is
defined by the server. |
| `kind` | String | [Output Only] Type of the resource. Always `compute#operation` for
Operation resources. |
| `zone` | String | [Output Only] The URL of the zone where the operation resides. Only
applicable when performing per-zone operations. |
| `operation_type` | String | [Output Only] The type of operation, such as `insert`,
`update`, or `delete`, and so on. |
| `instances_bulk_insert_operation_metadata` | String |  |
| `progress` | i64 | [Output Only] An optional progress indicator that ranges from 0 to 100.
There is no requirement that this be linear or support any granularity of
operations. This should not be used to guess when the operation will be
complete. This number should monotonically increase as the operation
progresses. |
| `warnings` | Vec<String> | [Output Only] If warning messages are generated during processing of the
operation, this field will be populated. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create zone_operation
zone_operation = provider.compute_api.Zone_operation {
    operation = "value"  # Name of the Operations resource to return, or its unique numeric
identifier.
    project = "value"  # Project ID for this request.
    zone = "value"  # Name of the zone for this request.
}

# Access zone_operation outputs
zone_operation_id = zone_operation.id
zone_operation_insert_time = zone_operation.insert_time
zone_operation_description = zone_operation.description
zone_operation_region = zone_operation.region
zone_operation_end_time = zone_operation.end_time
zone_operation_http_error_message = zone_operation.http_error_message
zone_operation_http_error_status_code = zone_operation.http_error_status_code
zone_operation_self_link = zone_operation.self_link
zone_operation_set_common_instance_metadata_operation_metadata = zone_operation.set_common_instance_metadata_operation_metadata
zone_operation_start_time = zone_operation.start_time
zone_operation_target_id = zone_operation.target_id
zone_operation_target_link = zone_operation.target_link
zone_operation_operation_group_id = zone_operation.operation_group_id
zone_operation_name = zone_operation.name
zone_operation_user = zone_operation.user
zone_operation_creation_timestamp = zone_operation.creation_timestamp
zone_operation_error = zone_operation.error
zone_operation_client_operation_id = zone_operation.client_operation_id
zone_operation_status_message = zone_operation.status_message
zone_operation_status = zone_operation.status
zone_operation_id = zone_operation.id
zone_operation_kind = zone_operation.kind
zone_operation_zone = zone_operation.zone
zone_operation_operation_type = zone_operation.operation_type
zone_operation_instances_bulk_insert_operation_metadata = zone_operation.instances_bulk_insert_operation_metadata
zone_operation_progress = zone_operation.progress
zone_operation_warnings = zone_operation.warnings
```

---


### Region_operation

Waits for the specified Operation resource to return as `DONE`
or for the request to approach the 2 minute deadline, and retrieves the
specified Operation resource. This method differs from the
`GET` method in that it waits for no more than the default
deadline (2 minutes) and then returns the current state of the operation,
which might be `DONE` or still in progress.

This method is called on a best-effort basis. Specifically:
   
   
    - In uncommon cases, when the server is overloaded, the request might
    return before the default deadline is reached, or might return after zero
    seconds.
   - If the default deadline is reached, there is no guarantee that the
    operation is actually done when the method returns. Be prepared to retry
    if the operation is not `DONE`.

**Operations**: ✅ Create ✅ Read ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `region` | String | ✅ | Name of the region for this request. |
| `operation` | String | ✅ | Name of the Operations resource to return, or its unique numeric
identifier. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `insert_time` | String | [Output Only] The time that this operation was requested.
This value is inRFC3339
text format. |
| `description` | String | [Output Only] A textual description of the operation, which is
set when the operation is created. |
| `region` | String | [Output Only] The URL of the region where the operation resides. Only
applicable when performing regional operations. |
| `end_time` | String | [Output Only] The time that this operation was completed. This value is inRFC3339
text format. |
| `http_error_message` | String | [Output Only] If the operation fails, this field contains the HTTP error
message that was returned, such as `NOT FOUND`. |
| `http_error_status_code` | i64 | [Output Only] If the operation fails, this field contains the HTTP error
status code that was returned. For example, a `404` means the
resource was not found. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `set_common_instance_metadata_operation_metadata` | String | [Output Only] If the operation is for projects.setCommonInstanceMetadata,
this field will contain information on all underlying zonal actions and
their state. |
| `start_time` | String | [Output Only] The time that this operation was started by the server.
This value is inRFC3339
text format. |
| `target_id` | String | [Output Only] The unique target ID, which identifies a specific incarnation
of the target resource. |
| `target_link` | String | [Output Only] The URL of the resource that the operation modifies. For
operations related to creating a snapshot, this points to the disk
that the snapshot was created from. |
| `operation_group_id` | String | [Output Only] An ID that represents a group of operations, such as when a
group of operations results from a `bulkInsert` API request. |
| `name` | String | [Output Only] Name of the operation. |
| `user` | String | [Output Only] User who requested the operation, for example:
`user@example.com` or
`alice_smith_identifier (global/workforcePools/example-com-us-employees)`. |
| `creation_timestamp` | String | [Deprecated] This field is deprecated. |
| `error` | String | [Output Only] If errors are generated during processing of the operation,
this field will be populated. |
| `client_operation_id` | String | [Output Only] The value of `requestId` if you provided it in the request.
Not present otherwise. |
| `status_message` | String | [Output Only] An optional textual description of the current status of the
operation. |
| `status` | String | [Output Only] The status of the operation, which can be one of the
following:
`PENDING`, `RUNNING`, or `DONE`. |
| `id` | String | [Output Only] The unique identifier for the operation. This identifier is
defined by the server. |
| `kind` | String | [Output Only] Type of the resource. Always `compute#operation` for
Operation resources. |
| `zone` | String | [Output Only] The URL of the zone where the operation resides. Only
applicable when performing per-zone operations. |
| `operation_type` | String | [Output Only] The type of operation, such as `insert`,
`update`, or `delete`, and so on. |
| `instances_bulk_insert_operation_metadata` | String |  |
| `progress` | i64 | [Output Only] An optional progress indicator that ranges from 0 to 100.
There is no requirement that this be linear or support any granularity of
operations. This should not be used to guess when the operation will be
complete. This number should monotonically increase as the operation
progresses. |
| `warnings` | Vec<String> | [Output Only] If warning messages are generated during processing of the
operation, this field will be populated. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create region_operation
region_operation = provider.compute_api.Region_operation {
    region = "value"  # Name of the region for this request.
    operation = "value"  # Name of the Operations resource to return, or its unique numeric
identifier.
    project = "value"  # Project ID for this request.
}

# Access region_operation outputs
region_operation_id = region_operation.id
region_operation_insert_time = region_operation.insert_time
region_operation_description = region_operation.description
region_operation_region = region_operation.region
region_operation_end_time = region_operation.end_time
region_operation_http_error_message = region_operation.http_error_message
region_operation_http_error_status_code = region_operation.http_error_status_code
region_operation_self_link = region_operation.self_link
region_operation_set_common_instance_metadata_operation_metadata = region_operation.set_common_instance_metadata_operation_metadata
region_operation_start_time = region_operation.start_time
region_operation_target_id = region_operation.target_id
region_operation_target_link = region_operation.target_link
region_operation_operation_group_id = region_operation.operation_group_id
region_operation_name = region_operation.name
region_operation_user = region_operation.user
region_operation_creation_timestamp = region_operation.creation_timestamp
region_operation_error = region_operation.error
region_operation_client_operation_id = region_operation.client_operation_id
region_operation_status_message = region_operation.status_message
region_operation_status = region_operation.status
region_operation_id = region_operation.id
region_operation_kind = region_operation.kind
region_operation_zone = region_operation.zone
region_operation_operation_type = region_operation.operation_type
region_operation_instances_bulk_insert_operation_metadata = region_operation.instances_bulk_insert_operation_metadata
region_operation_progress = region_operation.progress
region_operation_warnings = region_operation.warnings
```

---


### Reservation_sub_block

Sets the access control policy on the specified resource.
Replaces any existing policy.

**Operations**: ✅ Create ✅ Read

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `bindings` | Vec<String> |  | Flatten Policy to create a backwacd compatible wire-format.
Deprecated. Use 'policy' to specify bindings. |
| `etag` | String |  | Flatten Policy to create a backward compatible wire-format.
Deprecated. Use 'policy' to specify the etag. |
| `policy` | String |  | REQUIRED: The complete policy to be applied to the 'resource'. The size of
the policy is limited to a few 10s of KB. An empty policy is in general a
valid policy but certain services (like Projects) might reject them. |
| `project` | String | ✅ | Project ID for this request. |
| `zone` | String | ✅ | The name of the zone for this request. |
| `resource` | String | ✅ | Name or id of the resource for this request. |
| `parent_resource` | String | ✅ | Name or id of parent resource of the resource for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `resource` | String |  |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create reservation_sub_block
reservation_sub_block = provider.compute_api.Reservation_sub_block {
    project = "value"  # Project ID for this request.
    zone = "value"  # The name of the zone for this request.
    resource = "value"  # Name or id of the resource for this request.
    parent_resource = "value"  # Name or id of parent resource of the resource for this request.
}

# Access reservation_sub_block outputs
reservation_sub_block_id = reservation_sub_block.id
reservation_sub_block_resource = reservation_sub_block.resource
```

---


### Reservation_block

Allows customers to perform maintenance on a reservation block

**Operations**: ✅ Create ✅ Read

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `maintenance_scope` | String |  | Specifies if all, running or unused hosts are in scope for this request. |
| `reservation_block` | String | ✅ | The name of the reservation block.
Name should conform to RFC1035 or be a resource ID. |
| `zone` | String | ✅ | Name of the zone for this request. Zone name should conform to RFC1035. |
| `reservation` | String | ✅ | The name of the reservation.
Name should conform to RFC1035 or be a resource ID. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `resource` | String |  |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create reservation_block
reservation_block = provider.compute_api.Reservation_block {
    reservation_block = "value"  # The name of the reservation block.
Name should conform to RFC1035 or be a resource ID.
    zone = "value"  # Name of the zone for this request. Zone name should conform to RFC1035.
    reservation = "value"  # The name of the reservation.
Name should conform to RFC1035 or be a resource ID.
    project = "value"  # Project ID for this request.
}

# Access reservation_block outputs
reservation_block_id = reservation_block.id
reservation_block_resource = reservation_block.resource
```

---


### Region_instance

Creates multiple instances in a given region. Count specifies the number of
instances to create.

**Operations**: ✅ Create

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `location_policy` | String |  | Policy for choosing target zone. For more information, seeCreate VMs in
bulk. |
| `source_instance_template` | String |  | Specifies the instance template from which to create instances. You may
combine sourceInstanceTemplate withinstanceProperties to override specific values from an
existing instance template. Bulk API follows the semantics of JSON Merge
Patch described by RFC
7396.

It can be a full or partial URL. For example, the following are
all valid URLs to an instance template: 
   
   
      - https://www.googleapis.com/compute/v1/projects/project/global/instanceTemplates/instanceTemplate
      - projects/project/global/instanceTemplates/instanceTemplate
      - global/instanceTemplates/instanceTemplate



This field is optional. |
| `instance_properties` | String |  | The instance properties defining the VM instances to be created. Required
if sourceInstanceTemplate is not provided. |
| `name_pattern` | String |  | The string pattern used for the names of the VMs.
Either name_pattern or per_instance_properties
must be set. The pattern must contain one continuous sequence of
placeholder hash characters (#) with each character corresponding to one
digit of the generated instance name. Example: a name_pattern
of inst-#### generates instance names such asinst-0001 and inst-0002. If existing instances
in the same project and zone have names that match the name pattern
then the generated instance numbers start after the biggest
existing number. For example, if there exists an instance with nameinst-0050, then instance names generated using the patterninst-#### begin with inst-0051. The name pattern
placeholder #...# can contain up to 18 characters. |
| `count` | String |  | The maximum number of instances to create. |
| `min_count` | String |  | The minimum number of instances to create. If no min_count is
specified then count is used as the default value. Ifmin_count instances cannot be created, then no instances will
be created and instances already created will be deleted. |
| `per_instance_properties` | HashMap<String, String> |  | Per-instance properties to be set on individual instances.
Keys of this map specify requested instance names.
Can be empty if name_pattern is used. |
| `region` | String | ✅ | The name of the region for this request. |
| `project` | String | ✅ | Project ID for this request. |



#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create region_instance
region_instance = provider.compute_api.Region_instance {
    region = "value"  # The name of the region for this request.
    project = "value"  # Project ID for this request.
}

```

---


### Cross_site_network

Creates a cross-site network in the specified project in the given scope
using the parameters that are included in the request.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `description` | String |  | An optional description of the cross-site network. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `name` | String |  | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `kind` | String |  | [Output Only] Type of the resource. Alwayscompute#crossSiteNetwork for cross-site networks. |
| `id` | String |  | [Output Only] The unique identifier for the resource type. The server
generates this identifier. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `description` | String | An optional description of the cross-site network. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `name` | String | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `kind` | String | [Output Only] Type of the resource. Alwayscompute#crossSiteNetwork for cross-site networks. |
| `id` | String | [Output Only] The unique identifier for the resource type. The server
generates this identifier. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create cross_site_network
cross_site_network = provider.compute_api.Cross_site_network {
    project = "value"  # Project ID for this request.
}

# Access cross_site_network outputs
cross_site_network_id = cross_site_network.id
cross_site_network_description = cross_site_network.description
cross_site_network_creation_timestamp = cross_site_network.creation_timestamp
cross_site_network_self_link = cross_site_network.self_link
cross_site_network_name = cross_site_network.name
cross_site_network_kind = cross_site_network.kind
cross_site_network_id = cross_site_network.id
```

---


### Region_backend_service

Creates a regional BackendService resource in the specified project using
the data included in the request. For more information, see
Backend services overview.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `locality_lb_policy` | String |  | The load balancing algorithm used within the scope of the locality. The
possible values are:
   
   - ROUND_ROBIN: This is a simple policy in which each healthy
   backend is selected in round robin order. This is the default.
   - LEAST_REQUEST: An O(1) algorithm which
   selects two random healthy hosts and picks the host which has fewer active
   requests.
   - RING_HASH: The ring/modulo hash load balancer implements
   consistent hashing to backends. The algorithm has the property that the
   addition/removal of a host from a set of N hosts only affects 1/N of the
   requests.
   - RANDOM: The load balancer selects a random healthy
   host.
   - ORIGINAL_DESTINATION: Backend host is selected
   based on the client connection metadata, i.e., connections are opened to
   the same address as the destination address of the incoming connection
   before the connection was redirected to the load balancer.
   - MAGLEV: used as a drop in replacement for the ring hash
   load balancer. Maglev is not as stable as ring hash but has faster table
   lookup build times and host selection times. For more information about
   Maglev, see Maglev:
   A Fast and Reliable Software Network Load Balancer.
   - WEIGHTED_ROUND_ROBIN: Per-endpoint Weighted Round Robin
   Load Balancing using weights computed from Backend reported Custom Metrics.
   If set, the Backend Service responses are expected to contain non-standard
   HTTP response header field Endpoint-Load-Metrics. The reported
   metrics to use for computing the weights are specified via thecustomMetrics field.
   
   This field is applicable to either:
      - A regional backend service with the service_protocol set to HTTP,
      HTTPS, HTTP2 or H2C, and load_balancing_scheme set to
      INTERNAL_MANAGED. 
      - A global backend service with the
      load_balancing_scheme set to INTERNAL_SELF_MANAGED, INTERNAL_MANAGED, or
      EXTERNAL_MANAGED.
   
   
   If sessionAffinity is not configured—that is, if session
   affinity remains at the default value of NONE—then the
   default value for localityLbPolicy
   is ROUND_ROBIN. If session affinity is set to a value other
   than NONE,
   then the default value for localityLbPolicy isMAGLEV.
   
   Only ROUND_ROBIN and RING_HASH are supported
   when the backend service is referenced by a URL map that is bound to
   target gRPC proxy that has validateForProxyless field set to true.
   
   localityLbPolicy cannot be specified with haPolicy. |
| `external_managed_migration_testing_percentage` | f64 |  | Determines the fraction of requests that should be processed by the Global
external Application Load Balancer.

The value of this field must be in the range [0, 100].

Session affinity options will slightly affect this routing behavior, for
more details, see:Session
Affinity.

This value can only be set if the loadBalancingScheme in the BackendService
is set to EXTERNAL (when using the classic Application Load Balancer) and
the migration state is TEST_BY_PERCENTAGE. |
| `custom_metrics` | Vec<String> |  | List of custom metrics that are used for theWEIGHTED_ROUND_ROBIN locality_lb_policy. |
| `max_stream_duration` | String |  | Specifies the default maximum duration (timeout) for streams to this
service. Duration is computed from the beginning of the stream until the
response has been completely processed, including all retries. A stream
that does not complete in this duration is closed.

If not specified, there will be no timeout limit, i.e. the maximum
duration is infinite.

This value can be overridden in the PathMatcher configuration of the
UrlMap that references this backend service.

This field is only allowed when the loadBalancingScheme of
the backend service is INTERNAL_SELF_MANAGED. |
| `name` | String |  | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `backends` | Vec<String> |  | The list of backends that serve this BackendService. |
| `protocol` | String |  | The protocol this BackendService uses to communicate
with backends.

Possible values are HTTP, HTTPS, HTTP2, H2C, TCP, SSL, UDP or GRPC.
depending on the chosen load balancer or Traffic Director configuration.
Refer to the documentation for the load balancers or for Traffic Director
for more information.

Must be set to GRPC when the backend service is referenced by a URL map
that is bound to target gRPC proxy. |
| `subsetting` | String |  | subsetting cannot be specified with haPolicy. |
| `network` | String |  | The URL of the network to which this backend service belongs.

This field must be set for Internal Passthrough Network Load Balancers when
the haPolicy is enabled, and for External Passthrough Network Load
Balancers when the haPolicy fastIpMove is enabled.

This field can only be specified when the load balancing scheme is set toINTERNAL, or when the load balancing scheme is set toEXTERNAL and haPolicy fastIpMove is enabled. |
| `connection_tracking_policy` | String |  | Connection Tracking configuration for this BackendService. Connection
tracking policy settings are only available for external passthrough
Network Load Balancers and internal passthrough Network Load Balancers.

connectionTrackingPolicy cannot be specified with haPolicy. |
| `service_lb_policy` | String |  | URL to networkservices.ServiceLbPolicy resource.

Can only be set if load balancing scheme is EXTERNAL_MANAGED,
INTERNAL_MANAGED or INTERNAL_SELF_MANAGED and the scope is global. |
| `outlier_detection` | String |  | Settings controlling the ejection of unhealthy backend endpoints from the
load balancing pool of each individual proxy instance that processes the
traffic for the given backend service. If not set, this feature is
considered disabled.

Results of the outlier detection algorithm (ejection of endpoints from the
load balancing pool and returning them back to the pool) are executed
independently by each proxy instance of the load balancer. In most cases,
more than one proxy instance handles the traffic received by a backend
service. Thus, it is possible that an unhealthy endpoint is detected and
ejected by only some of the proxies, and while this happens, other proxies
may continue to send requests to the same unhealthy endpoint until they
detect and eject the unhealthy endpoint.

Applicable backend endpoints can be:
   
   - VM instances in an Instance Group
   - Endpoints in a Zonal NEG (GCE_VM_IP, GCE_VM_IP_PORT)
   - Endpoints in a Hybrid Connectivity NEG (NON_GCP_PRIVATE_IP_PORT)
   - Serverless NEGs, that resolve to Cloud Run, App Engine, or Cloud
   Functions Services 
   - Private Service Connect NEGs, that resolve to
   Google-managed regional API endpoints or managed services published using
   Private Service Connect



Applicable backend service types can be:
   
   - A global backend service with the loadBalancingScheme set to
   INTERNAL_SELF_MANAGED or EXTERNAL_MANAGED. 
   - A regional backend
   service with the serviceProtocol set to HTTP, HTTPS, HTTP2 or H2C, and
   loadBalancingScheme set to INTERNAL_MANAGED or EXTERNAL_MANAGED. Not
   supported for Serverless NEGs.



Not supported when the backend service is referenced by a URL map that is
bound to target gRPC proxy that has validateForProxyless field set to true. |
| `region` | String |  | [Output Only] URL of the region where the regional backend service
resides. This field is not applicable to global backend services.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `external_managed_migration_state` | String |  | Specifies the canary migration state. Possible values are PREPARE,
TEST_BY_PERCENTAGE, and TEST_ALL_TRAFFIC.

To begin the migration from EXTERNAL to EXTERNAL_MANAGED, the state must be
changed to PREPARE. The state must be changed to TEST_ALL_TRAFFIC before
the loadBalancingScheme can be changed to EXTERNAL_MANAGED. Optionally, the
TEST_BY_PERCENTAGE state can be used to migrate traffic by percentage using
externalManagedMigrationTestingPercentage.

Rolling back a migration requires the states to be set in reverse order. So
changing the scheme from EXTERNAL_MANAGED to EXTERNAL requires the state to
be set to TEST_ALL_TRAFFIC at the same time. Optionally, the
TEST_BY_PERCENTAGE state can be used to migrate some traffic back to
EXTERNAL or PREPARE can be used to migrate all traffic back to EXTERNAL. |
| `locality_lb_policies` | Vec<String> |  | A list of locality load-balancing policies to be used in order of
preference. When you use localityLbPolicies, you must set at least one
value for either the localityLbPolicies[].policy or the
localityLbPolicies[].customPolicy field. localityLbPolicies overrides any
value set in the localityLbPolicy field.

For an example of how to use this field, seeDefine
a list of preferred policies.

Caution: This field and its children are intended for use in a service mesh
that includes gRPC clients only. Envoy proxies can't use backend services
that have this configuration. |
| `strong_session_affinity_cookie` | String |  | Describes the HTTP cookie used for stateful session affinity. This field is
applicable and required if the sessionAffinity is set toSTRONG_COOKIE_AFFINITY. |
| `log_config` | String |  | This field denotes the logging options for the load balancer traffic served
by this backend service. If logging is enabled, logs will be exported to
Stackdriver. |
| `ip_address_selection_policy` | String |  | Specifies a preference for traffic sent from the proxy to the backend (or
from the client to the backend for proxyless gRPC).
The possible values are:
   
   - IPV4_ONLY: Only send IPv4 traffic to the backends of the
   backend service (Instance Group, Managed Instance Group, Network Endpoint
   Group), regardless of traffic from the client to the proxy. Only IPv4
   health checks are used to check the health of the backends. This is the
   default setting.
   - PREFER_IPV6: Prioritize the connection to the endpoint's
   IPv6 address over its IPv4 address (provided there is a healthy IPv6
   address).
   - IPV6_ONLY: Only send IPv6 traffic to the backends of the
   backend service (Instance Group, Managed Instance Group, Network Endpoint
   Group), regardless of traffic from the client to the proxy. Only IPv6
   health checks are used to check the health of the backends.



This field is applicable to either:
   
   -  Advanced global external Application Load Balancer (load balancing
   scheme EXTERNAL_MANAGED), 
   -  Regional external Application Load
   Balancer, 
   -  Internal proxy Network Load Balancer (load balancing
   scheme INTERNAL_MANAGED), 
   -  Regional internal Application Load
   Balancer (load balancing scheme INTERNAL_MANAGED), 
   -  Traffic
   Director with Envoy proxies and proxyless gRPC (load balancing scheme
   INTERNAL_SELF_MANAGED). |
| `port` | i64 |  | Deprecated in favor of portName. The TCP port to connect on
the backend. The default value is 80.
For internal passthrough Network Load Balancers and external passthrough
Network Load Balancers, omit port. |
| `port_name` | String |  | A named port on a backend instance group representing the port for
communication to the backend VMs in that group. The
named port must be [defined on each backend instance
group](https://cloud.google.com/load-balancing/docs/backend-service#named_ports).
This parameter has no meaning if the backends are NEGs. For internal
passthrough Network Load Balancers and external passthrough Network Load
Balancers, omit port_name. |
| `iap` | String |  | The configurations for Identity-Aware Proxy on this resource.
Not available for internal passthrough Network Load Balancers and external
passthrough Network Load Balancers. |
| `tls_settings` | String |  | Configuration for Backend Authenticated TLS and mTLS. May only be specified
when the backend protocol is SSL, HTTPS or HTTP2. |
| `custom_request_headers` | Vec<String> |  | Headers that the load balancer adds to proxied requests. See [Creating
custom
headers](https://cloud.google.com/load-balancing/docs/custom-headers). |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `cdn_policy` | String |  | Cloud CDN configuration for this BackendService. Only available for
specified load balancer types. |
| `circuit_breakers` | String |  |  |
| `metadatas` | HashMap<String, String> |  | Deployment metadata associated with the resource to be set by a GKE hub
controller and read by the backend RCTH |
| `params` | String |  | Input only. [Input Only] Additional params passed with the request, but not persisted
as part of resource payload. |
| `timeout_sec` | i64 |  | The backend service timeout has a different meaning depending on the
type of load balancer. For more information see,
Backend service settings.
The default is 30 seconds.
The full range of timeout values allowed goes from 1
through 2,147,483,647 seconds.

This value can be overridden in the PathMatcher configuration of the
UrlMap that references this backend service.

Not supported when the backend service is referenced by a URL map that is
bound to target gRPC proxy that has validateForProxyless field set to true.
Instead, use maxStreamDuration. |
| `security_policy` | String |  | [Output Only] The resource URL for the security policy associated with this
backend service. |
| `load_balancing_scheme` | String |  | Specifies the load balancer type. A backend service
created for one type of load balancer cannot be used with another.
For more information, refer toChoosing
a load balancer. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `used_by` | Vec<String> |  | [Output Only] List of resources referencing given backend service. |
| `security_settings` | String |  | This field specifies the security settings that apply to this backend
service. This field is applicable to a global backend service with the
load_balancing_scheme set to INTERNAL_SELF_MANAGED. |
| `session_affinity` | String |  | Type of session affinity to use. The default is NONE.

Only NONE and HEADER_FIELD are supported
when the backend service is referenced by a URL map that is bound to
target gRPC proxy that has validateForProxyless field set to true.

For more details, see:
[Session
Affinity](https://cloud.google.com/load-balancing/docs/backend-service#session_affinity).

sessionAffinity cannot be specified with haPolicy. |
| `kind` | String |  | [Output Only] Type of resource. Always compute#backendService
for backend services. |
| `service_bindings` | Vec<String> |  | URLs of networkservices.ServiceBinding resources.

Can only be set if load balancing scheme is INTERNAL_SELF_MANAGED.
If set, lists of backends and health checks must be both empty. |
| `failover_policy` | String |  | Requires at least one backend instance group to be defined
as a backup (failover) backend.
For load balancers that have configurable failover:
[Internal passthrough Network Load
Balancers](https://cloud.google.com/load-balancing/docs/internal/failover-overview)
and [external passthrough Network Load
Balancers](https://cloud.google.com/load-balancing/docs/network/networklb-failover-overview).

failoverPolicy cannot be specified with haPolicy. |
| `connection_draining` | String |  | connectionDraining cannot be specified with haPolicy. |
| `enable_cdn` | bool |  | If true, enables Cloud CDN for the backend service of a
global external Application Load Balancer. |
| `ha_policy` | String |  | Configures self-managed High Availability (HA) for External and Internal
Protocol Forwarding.

The backends of this regional backend service must only specify zonal
network endpoint groups (NEGs) of type GCE_VM_IP.

When haPolicy is set for an Internal Passthrough Network Load Balancer, the
regional backend service must set the network field. All zonal NEGs must
belong to the same network. However, individual NEGs can
belong to different subnetworks of that network.

When haPolicy is specified, the set of attached network endpoints across
all backends comprise an High Availability domain from which one endpoint
is selected as the active endpoint (the leader) that receives all
traffic.

haPolicy can be added only at backend service creation time. Once set up,
it cannot be deleted.

Note that haPolicy is not for load balancing, and therefore cannot be
specified with sessionAffinity, connectionTrackingPolicy, and
failoverPolicy.

haPolicy requires customers to be responsible for tracking backend
endpoint health and electing a leader among the healthy endpoints.
Therefore, haPolicy cannot be specified with healthChecks.

haPolicy can only be specified for External Passthrough Network Load
Balancers and Internal Passthrough Network Load Balancers. |
| `affinity_cookie_ttl_sec` | i64 |  | Lifetime of cookies in seconds. This setting is applicable to Application
Load Balancers and Traffic Director and requires
GENERATED_COOKIE or HTTP_COOKIE session affinity.

If set to 0, the cookie is non-persistent and lasts only until
the end of the browser session (or equivalent). The maximum allowed value
is two weeks (1,209,600).

Not supported when the backend service is referenced by a URL map that is
bound to target gRPC proxy that has validateForProxyless field set to true. |
| `custom_response_headers` | Vec<String> |  | Headers that the load balancer adds to proxied responses. See [Creating
custom
headers](https://cloud.google.com/load-balancing/docs/custom-headers). |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `edge_security_policy` | String |  | [Output Only] The resource URL for the edge security policy associated with
this backend service. |
| `consistent_hash` | String |  | Consistent Hash-based load balancing can be used to provide soft session
affinity based on HTTP headers, cookies or other properties. This load
balancing policy is applicable only for HTTP connections. The affinity to a
particular destination host will be lost when one or more hosts are
added/removed from the destination service. This field specifies parameters
that control consistent hashing. This field is only applicable whenlocalityLbPolicy is set to MAGLEV orRING_HASH.

This field is applicable to either:
   
   - A regional backend service with the service_protocol set to HTTP,
   HTTPS, HTTP2 or H2C, and load_balancing_scheme set to
   INTERNAL_MANAGED. 
   - A global backend service with the
   load_balancing_scheme set to INTERNAL_SELF_MANAGED. |
| `health_checks` | Vec<String> |  | The list of URLs to the healthChecks, httpHealthChecks (legacy), or
httpsHealthChecks (legacy) resource for health checking this backend
service. Not all backend services support legacy health checks. See
Load balancer guide. Currently, at most one health check can be
specified for each backend service. Backend services with
instance group or zonal NEG backends must have a health check unless
haPolicy is specified. Backend services with internet or serverless NEG
backends must not have a health check.

healthChecks[] cannot be specified with haPolicy. |
| `fingerprint` | String |  | Fingerprint of this resource. A hash of the contents stored in this object.
This field is used in optimistic locking. This field will be ignored when
inserting a BackendService. An up-to-date fingerprint must be provided in
order to update the BackendService, otherwise the request will
fail with error 412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve a BackendService. |
| `compression_mode` | String |  | Compress text responses using Brotli or gzip compression, based on
the client's Accept-Encoding header. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `region` | String | ✅ | Name of the region scoping this request. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `locality_lb_policy` | String | The load balancing algorithm used within the scope of the locality. The
possible values are:
   
   - ROUND_ROBIN: This is a simple policy in which each healthy
   backend is selected in round robin order. This is the default.
   - LEAST_REQUEST: An O(1) algorithm which
   selects two random healthy hosts and picks the host which has fewer active
   requests.
   - RING_HASH: The ring/modulo hash load balancer implements
   consistent hashing to backends. The algorithm has the property that the
   addition/removal of a host from a set of N hosts only affects 1/N of the
   requests.
   - RANDOM: The load balancer selects a random healthy
   host.
   - ORIGINAL_DESTINATION: Backend host is selected
   based on the client connection metadata, i.e., connections are opened to
   the same address as the destination address of the incoming connection
   before the connection was redirected to the load balancer.
   - MAGLEV: used as a drop in replacement for the ring hash
   load balancer. Maglev is not as stable as ring hash but has faster table
   lookup build times and host selection times. For more information about
   Maglev, see Maglev:
   A Fast and Reliable Software Network Load Balancer.
   - WEIGHTED_ROUND_ROBIN: Per-endpoint Weighted Round Robin
   Load Balancing using weights computed from Backend reported Custom Metrics.
   If set, the Backend Service responses are expected to contain non-standard
   HTTP response header field Endpoint-Load-Metrics. The reported
   metrics to use for computing the weights are specified via thecustomMetrics field.
   
   This field is applicable to either:
      - A regional backend service with the service_protocol set to HTTP,
      HTTPS, HTTP2 or H2C, and load_balancing_scheme set to
      INTERNAL_MANAGED. 
      - A global backend service with the
      load_balancing_scheme set to INTERNAL_SELF_MANAGED, INTERNAL_MANAGED, or
      EXTERNAL_MANAGED.
   
   
   If sessionAffinity is not configured—that is, if session
   affinity remains at the default value of NONE—then the
   default value for localityLbPolicy
   is ROUND_ROBIN. If session affinity is set to a value other
   than NONE,
   then the default value for localityLbPolicy isMAGLEV.
   
   Only ROUND_ROBIN and RING_HASH are supported
   when the backend service is referenced by a URL map that is bound to
   target gRPC proxy that has validateForProxyless field set to true.
   
   localityLbPolicy cannot be specified with haPolicy. |
| `external_managed_migration_testing_percentage` | f64 | Determines the fraction of requests that should be processed by the Global
external Application Load Balancer.

The value of this field must be in the range [0, 100].

Session affinity options will slightly affect this routing behavior, for
more details, see:Session
Affinity.

This value can only be set if the loadBalancingScheme in the BackendService
is set to EXTERNAL (when using the classic Application Load Balancer) and
the migration state is TEST_BY_PERCENTAGE. |
| `custom_metrics` | Vec<String> | List of custom metrics that are used for theWEIGHTED_ROUND_ROBIN locality_lb_policy. |
| `max_stream_duration` | String | Specifies the default maximum duration (timeout) for streams to this
service. Duration is computed from the beginning of the stream until the
response has been completely processed, including all retries. A stream
that does not complete in this duration is closed.

If not specified, there will be no timeout limit, i.e. the maximum
duration is infinite.

This value can be overridden in the PathMatcher configuration of the
UrlMap that references this backend service.

This field is only allowed when the loadBalancingScheme of
the backend service is INTERNAL_SELF_MANAGED. |
| `name` | String | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `backends` | Vec<String> | The list of backends that serve this BackendService. |
| `protocol` | String | The protocol this BackendService uses to communicate
with backends.

Possible values are HTTP, HTTPS, HTTP2, H2C, TCP, SSL, UDP or GRPC.
depending on the chosen load balancer or Traffic Director configuration.
Refer to the documentation for the load balancers or for Traffic Director
for more information.

Must be set to GRPC when the backend service is referenced by a URL map
that is bound to target gRPC proxy. |
| `subsetting` | String | subsetting cannot be specified with haPolicy. |
| `network` | String | The URL of the network to which this backend service belongs.

This field must be set for Internal Passthrough Network Load Balancers when
the haPolicy is enabled, and for External Passthrough Network Load
Balancers when the haPolicy fastIpMove is enabled.

This field can only be specified when the load balancing scheme is set toINTERNAL, or when the load balancing scheme is set toEXTERNAL and haPolicy fastIpMove is enabled. |
| `connection_tracking_policy` | String | Connection Tracking configuration for this BackendService. Connection
tracking policy settings are only available for external passthrough
Network Load Balancers and internal passthrough Network Load Balancers.

connectionTrackingPolicy cannot be specified with haPolicy. |
| `service_lb_policy` | String | URL to networkservices.ServiceLbPolicy resource.

Can only be set if load balancing scheme is EXTERNAL_MANAGED,
INTERNAL_MANAGED or INTERNAL_SELF_MANAGED and the scope is global. |
| `outlier_detection` | String | Settings controlling the ejection of unhealthy backend endpoints from the
load balancing pool of each individual proxy instance that processes the
traffic for the given backend service. If not set, this feature is
considered disabled.

Results of the outlier detection algorithm (ejection of endpoints from the
load balancing pool and returning them back to the pool) are executed
independently by each proxy instance of the load balancer. In most cases,
more than one proxy instance handles the traffic received by a backend
service. Thus, it is possible that an unhealthy endpoint is detected and
ejected by only some of the proxies, and while this happens, other proxies
may continue to send requests to the same unhealthy endpoint until they
detect and eject the unhealthy endpoint.

Applicable backend endpoints can be:
   
   - VM instances in an Instance Group
   - Endpoints in a Zonal NEG (GCE_VM_IP, GCE_VM_IP_PORT)
   - Endpoints in a Hybrid Connectivity NEG (NON_GCP_PRIVATE_IP_PORT)
   - Serverless NEGs, that resolve to Cloud Run, App Engine, or Cloud
   Functions Services 
   - Private Service Connect NEGs, that resolve to
   Google-managed regional API endpoints or managed services published using
   Private Service Connect



Applicable backend service types can be:
   
   - A global backend service with the loadBalancingScheme set to
   INTERNAL_SELF_MANAGED or EXTERNAL_MANAGED. 
   - A regional backend
   service with the serviceProtocol set to HTTP, HTTPS, HTTP2 or H2C, and
   loadBalancingScheme set to INTERNAL_MANAGED or EXTERNAL_MANAGED. Not
   supported for Serverless NEGs.



Not supported when the backend service is referenced by a URL map that is
bound to target gRPC proxy that has validateForProxyless field set to true. |
| `region` | String | [Output Only] URL of the region where the regional backend service
resides. This field is not applicable to global backend services.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `external_managed_migration_state` | String | Specifies the canary migration state. Possible values are PREPARE,
TEST_BY_PERCENTAGE, and TEST_ALL_TRAFFIC.

To begin the migration from EXTERNAL to EXTERNAL_MANAGED, the state must be
changed to PREPARE. The state must be changed to TEST_ALL_TRAFFIC before
the loadBalancingScheme can be changed to EXTERNAL_MANAGED. Optionally, the
TEST_BY_PERCENTAGE state can be used to migrate traffic by percentage using
externalManagedMigrationTestingPercentage.

Rolling back a migration requires the states to be set in reverse order. So
changing the scheme from EXTERNAL_MANAGED to EXTERNAL requires the state to
be set to TEST_ALL_TRAFFIC at the same time. Optionally, the
TEST_BY_PERCENTAGE state can be used to migrate some traffic back to
EXTERNAL or PREPARE can be used to migrate all traffic back to EXTERNAL. |
| `locality_lb_policies` | Vec<String> | A list of locality load-balancing policies to be used in order of
preference. When you use localityLbPolicies, you must set at least one
value for either the localityLbPolicies[].policy or the
localityLbPolicies[].customPolicy field. localityLbPolicies overrides any
value set in the localityLbPolicy field.

For an example of how to use this field, seeDefine
a list of preferred policies.

Caution: This field and its children are intended for use in a service mesh
that includes gRPC clients only. Envoy proxies can't use backend services
that have this configuration. |
| `strong_session_affinity_cookie` | String | Describes the HTTP cookie used for stateful session affinity. This field is
applicable and required if the sessionAffinity is set toSTRONG_COOKIE_AFFINITY. |
| `log_config` | String | This field denotes the logging options for the load balancer traffic served
by this backend service. If logging is enabled, logs will be exported to
Stackdriver. |
| `ip_address_selection_policy` | String | Specifies a preference for traffic sent from the proxy to the backend (or
from the client to the backend for proxyless gRPC).
The possible values are:
   
   - IPV4_ONLY: Only send IPv4 traffic to the backends of the
   backend service (Instance Group, Managed Instance Group, Network Endpoint
   Group), regardless of traffic from the client to the proxy. Only IPv4
   health checks are used to check the health of the backends. This is the
   default setting.
   - PREFER_IPV6: Prioritize the connection to the endpoint's
   IPv6 address over its IPv4 address (provided there is a healthy IPv6
   address).
   - IPV6_ONLY: Only send IPv6 traffic to the backends of the
   backend service (Instance Group, Managed Instance Group, Network Endpoint
   Group), regardless of traffic from the client to the proxy. Only IPv6
   health checks are used to check the health of the backends.



This field is applicable to either:
   
   -  Advanced global external Application Load Balancer (load balancing
   scheme EXTERNAL_MANAGED), 
   -  Regional external Application Load
   Balancer, 
   -  Internal proxy Network Load Balancer (load balancing
   scheme INTERNAL_MANAGED), 
   -  Regional internal Application Load
   Balancer (load balancing scheme INTERNAL_MANAGED), 
   -  Traffic
   Director with Envoy proxies and proxyless gRPC (load balancing scheme
   INTERNAL_SELF_MANAGED). |
| `port` | i64 | Deprecated in favor of portName. The TCP port to connect on
the backend. The default value is 80.
For internal passthrough Network Load Balancers and external passthrough
Network Load Balancers, omit port. |
| `port_name` | String | A named port on a backend instance group representing the port for
communication to the backend VMs in that group. The
named port must be [defined on each backend instance
group](https://cloud.google.com/load-balancing/docs/backend-service#named_ports).
This parameter has no meaning if the backends are NEGs. For internal
passthrough Network Load Balancers and external passthrough Network Load
Balancers, omit port_name. |
| `iap` | String | The configurations for Identity-Aware Proxy on this resource.
Not available for internal passthrough Network Load Balancers and external
passthrough Network Load Balancers. |
| `tls_settings` | String | Configuration for Backend Authenticated TLS and mTLS. May only be specified
when the backend protocol is SSL, HTTPS or HTTP2. |
| `custom_request_headers` | Vec<String> | Headers that the load balancer adds to proxied requests. See [Creating
custom
headers](https://cloud.google.com/load-balancing/docs/custom-headers). |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `cdn_policy` | String | Cloud CDN configuration for this BackendService. Only available for
specified load balancer types. |
| `circuit_breakers` | String |  |
| `metadatas` | HashMap<String, String> | Deployment metadata associated with the resource to be set by a GKE hub
controller and read by the backend RCTH |
| `params` | String | Input only. [Input Only] Additional params passed with the request, but not persisted
as part of resource payload. |
| `timeout_sec` | i64 | The backend service timeout has a different meaning depending on the
type of load balancer. For more information see,
Backend service settings.
The default is 30 seconds.
The full range of timeout values allowed goes from 1
through 2,147,483,647 seconds.

This value can be overridden in the PathMatcher configuration of the
UrlMap that references this backend service.

Not supported when the backend service is referenced by a URL map that is
bound to target gRPC proxy that has validateForProxyless field set to true.
Instead, use maxStreamDuration. |
| `security_policy` | String | [Output Only] The resource URL for the security policy associated with this
backend service. |
| `load_balancing_scheme` | String | Specifies the load balancer type. A backend service
created for one type of load balancer cannot be used with another.
For more information, refer toChoosing
a load balancer. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `used_by` | Vec<String> | [Output Only] List of resources referencing given backend service. |
| `security_settings` | String | This field specifies the security settings that apply to this backend
service. This field is applicable to a global backend service with the
load_balancing_scheme set to INTERNAL_SELF_MANAGED. |
| `session_affinity` | String | Type of session affinity to use. The default is NONE.

Only NONE and HEADER_FIELD are supported
when the backend service is referenced by a URL map that is bound to
target gRPC proxy that has validateForProxyless field set to true.

For more details, see:
[Session
Affinity](https://cloud.google.com/load-balancing/docs/backend-service#session_affinity).

sessionAffinity cannot be specified with haPolicy. |
| `kind` | String | [Output Only] Type of resource. Always compute#backendService
for backend services. |
| `service_bindings` | Vec<String> | URLs of networkservices.ServiceBinding resources.

Can only be set if load balancing scheme is INTERNAL_SELF_MANAGED.
If set, lists of backends and health checks must be both empty. |
| `failover_policy` | String | Requires at least one backend instance group to be defined
as a backup (failover) backend.
For load balancers that have configurable failover:
[Internal passthrough Network Load
Balancers](https://cloud.google.com/load-balancing/docs/internal/failover-overview)
and [external passthrough Network Load
Balancers](https://cloud.google.com/load-balancing/docs/network/networklb-failover-overview).

failoverPolicy cannot be specified with haPolicy. |
| `connection_draining` | String | connectionDraining cannot be specified with haPolicy. |
| `enable_cdn` | bool | If true, enables Cloud CDN for the backend service of a
global external Application Load Balancer. |
| `ha_policy` | String | Configures self-managed High Availability (HA) for External and Internal
Protocol Forwarding.

The backends of this regional backend service must only specify zonal
network endpoint groups (NEGs) of type GCE_VM_IP.

When haPolicy is set for an Internal Passthrough Network Load Balancer, the
regional backend service must set the network field. All zonal NEGs must
belong to the same network. However, individual NEGs can
belong to different subnetworks of that network.

When haPolicy is specified, the set of attached network endpoints across
all backends comprise an High Availability domain from which one endpoint
is selected as the active endpoint (the leader) that receives all
traffic.

haPolicy can be added only at backend service creation time. Once set up,
it cannot be deleted.

Note that haPolicy is not for load balancing, and therefore cannot be
specified with sessionAffinity, connectionTrackingPolicy, and
failoverPolicy.

haPolicy requires customers to be responsible for tracking backend
endpoint health and electing a leader among the healthy endpoints.
Therefore, haPolicy cannot be specified with healthChecks.

haPolicy can only be specified for External Passthrough Network Load
Balancers and Internal Passthrough Network Load Balancers. |
| `affinity_cookie_ttl_sec` | i64 | Lifetime of cookies in seconds. This setting is applicable to Application
Load Balancers and Traffic Director and requires
GENERATED_COOKIE or HTTP_COOKIE session affinity.

If set to 0, the cookie is non-persistent and lasts only until
the end of the browser session (or equivalent). The maximum allowed value
is two weeks (1,209,600).

Not supported when the backend service is referenced by a URL map that is
bound to target gRPC proxy that has validateForProxyless field set to true. |
| `custom_response_headers` | Vec<String> | Headers that the load balancer adds to proxied responses. See [Creating
custom
headers](https://cloud.google.com/load-balancing/docs/custom-headers). |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `edge_security_policy` | String | [Output Only] The resource URL for the edge security policy associated with
this backend service. |
| `consistent_hash` | String | Consistent Hash-based load balancing can be used to provide soft session
affinity based on HTTP headers, cookies or other properties. This load
balancing policy is applicable only for HTTP connections. The affinity to a
particular destination host will be lost when one or more hosts are
added/removed from the destination service. This field specifies parameters
that control consistent hashing. This field is only applicable whenlocalityLbPolicy is set to MAGLEV orRING_HASH.

This field is applicable to either:
   
   - A regional backend service with the service_protocol set to HTTP,
   HTTPS, HTTP2 or H2C, and load_balancing_scheme set to
   INTERNAL_MANAGED. 
   - A global backend service with the
   load_balancing_scheme set to INTERNAL_SELF_MANAGED. |
| `health_checks` | Vec<String> | The list of URLs to the healthChecks, httpHealthChecks (legacy), or
httpsHealthChecks (legacy) resource for health checking this backend
service. Not all backend services support legacy health checks. See
Load balancer guide. Currently, at most one health check can be
specified for each backend service. Backend services with
instance group or zonal NEG backends must have a health check unless
haPolicy is specified. Backend services with internet or serverless NEG
backends must not have a health check.

healthChecks[] cannot be specified with haPolicy. |
| `fingerprint` | String | Fingerprint of this resource. A hash of the contents stored in this object.
This field is used in optimistic locking. This field will be ignored when
inserting a BackendService. An up-to-date fingerprint must be provided in
order to update the BackendService, otherwise the request will
fail with error 412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve a BackendService. |
| `compression_mode` | String | Compress text responses using Brotli or gzip compression, based on
the client's Accept-Encoding header. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create region_backend_service
region_backend_service = provider.compute_api.Region_backend_service {
    region = "value"  # Name of the region scoping this request.
    project = "value"  # Project ID for this request.
}

# Access region_backend_service outputs
region_backend_service_id = region_backend_service.id
region_backend_service_locality_lb_policy = region_backend_service.locality_lb_policy
region_backend_service_external_managed_migration_testing_percentage = region_backend_service.external_managed_migration_testing_percentage
region_backend_service_custom_metrics = region_backend_service.custom_metrics
region_backend_service_max_stream_duration = region_backend_service.max_stream_duration
region_backend_service_name = region_backend_service.name
region_backend_service_backends = region_backend_service.backends
region_backend_service_protocol = region_backend_service.protocol
region_backend_service_subsetting = region_backend_service.subsetting
region_backend_service_network = region_backend_service.network
region_backend_service_connection_tracking_policy = region_backend_service.connection_tracking_policy
region_backend_service_service_lb_policy = region_backend_service.service_lb_policy
region_backend_service_outlier_detection = region_backend_service.outlier_detection
region_backend_service_region = region_backend_service.region
region_backend_service_external_managed_migration_state = region_backend_service.external_managed_migration_state
region_backend_service_locality_lb_policies = region_backend_service.locality_lb_policies
region_backend_service_strong_session_affinity_cookie = region_backend_service.strong_session_affinity_cookie
region_backend_service_log_config = region_backend_service.log_config
region_backend_service_ip_address_selection_policy = region_backend_service.ip_address_selection_policy
region_backend_service_port = region_backend_service.port
region_backend_service_port_name = region_backend_service.port_name
region_backend_service_iap = region_backend_service.iap
region_backend_service_tls_settings = region_backend_service.tls_settings
region_backend_service_custom_request_headers = region_backend_service.custom_request_headers
region_backend_service_id = region_backend_service.id
region_backend_service_cdn_policy = region_backend_service.cdn_policy
region_backend_service_circuit_breakers = region_backend_service.circuit_breakers
region_backend_service_metadatas = region_backend_service.metadatas
region_backend_service_params = region_backend_service.params
region_backend_service_timeout_sec = region_backend_service.timeout_sec
region_backend_service_security_policy = region_backend_service.security_policy
region_backend_service_load_balancing_scheme = region_backend_service.load_balancing_scheme
region_backend_service_self_link = region_backend_service.self_link
region_backend_service_used_by = region_backend_service.used_by
region_backend_service_security_settings = region_backend_service.security_settings
region_backend_service_session_affinity = region_backend_service.session_affinity
region_backend_service_kind = region_backend_service.kind
region_backend_service_service_bindings = region_backend_service.service_bindings
region_backend_service_failover_policy = region_backend_service.failover_policy
region_backend_service_connection_draining = region_backend_service.connection_draining
region_backend_service_enable_cdn = region_backend_service.enable_cdn
region_backend_service_ha_policy = region_backend_service.ha_policy
region_backend_service_affinity_cookie_ttl_sec = region_backend_service.affinity_cookie_ttl_sec
region_backend_service_custom_response_headers = region_backend_service.custom_response_headers
region_backend_service_description = region_backend_service.description
region_backend_service_edge_security_policy = region_backend_service.edge_security_policy
region_backend_service_consistent_hash = region_backend_service.consistent_hash
region_backend_service_health_checks = region_backend_service.health_checks
region_backend_service_fingerprint = region_backend_service.fingerprint
region_backend_service_compression_mode = region_backend_service.compression_mode
region_backend_service_creation_timestamp = region_backend_service.creation_timestamp
```

---


### Disk_type

Returns the specified disk type.

**Operations**: ✅ Read

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `deprecated` | String | [Output Only] The deprecation status associated with this disk type. |
| `kind` | String | [Output Only] Type of the resource. Always compute#diskType
for disk types. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `default_disk_size_gb` | String | [Output Only] Server-defined default disk size in GB. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `name` | String | [Output Only] Name of the resource. |
| `description` | String | [Output Only] An optional description of this resource. |
| `region` | String | [Output Only] URL of the region where the disk type resides. Only
applicable for regional resources.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `valid_disk_size` | String | [Output Only] An optional textual description of the valid disk size,
such as "10GB-10TB". |
| `zone` | String | [Output Only] URL of the zone where the disk type resides.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Access disk_type outputs
disk_type_id = disk_type.id
disk_type_deprecated = disk_type.deprecated
disk_type_kind = disk_type.kind
disk_type_creation_timestamp = disk_type.creation_timestamp
disk_type_default_disk_size_gb = disk_type.default_disk_size_gb
disk_type_self_link = disk_type.self_link
disk_type_name = disk_type.name
disk_type_description = disk_type.description
disk_type_region = disk_type.region
disk_type_valid_disk_size = disk_type.valid_disk_size
disk_type_zone = disk_type.zone
disk_type_id = disk_type.id
```

---


### Machine_image

Creates a machine image in the specified project using the
data that is included in the request. If you are creating a new machine
image to update an existing instance, your new machine image should use the
same network or, if applicable, the same subnetwork as the original
instance.

**Operations**: ✅ Create ✅ Read ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `source_instance` | String |  | The source instance used to create the machine image. You can provide this
as a partial or full URL to the resource. For example, the following are
valid values: 
   
   
     - https://www.googleapis.com/compute/v1/projects/project/zones/zone/instances/instance 
   - projects/project/zones/zone/instances/instance |
| `source_instance_properties` | String |  | [Output Only] DEPRECATED: Please use instance_properties
instead for source instance related properties. New properties will not be
added to this field. |
| `label_fingerprint` | String |  | A fingerprint for the labels being applied to this machine image, which is
essentially a hash of the labels set used for optimistic locking. The
fingerprint is initially generated by Compute Engine and changes after
every request to modify or update labels. You must always provide an
up-to-date fingerprint hash in order to update or change labels.

To see the latest fingerprint, make get() request to the
machine image. |
| `labels` | HashMap<String, String> |  | Labels to apply to this machine image. These can be later modified by
the setLabels method. |
| `satisfies_pzs` | bool |  | [Output Only] Reserved for future use. |
| `storage_locations` | Vec<String> |  | The regional or multi-regional Cloud Storage bucket location where themachine image is
stored. |
| `kind` | String |  | [Output Only] The resource type, which is alwayscompute#machineImage for machine image. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `creation_timestamp` | String |  | [Output Only] The creation timestamp for this machine image inRFC3339
text format. |
| `saved_disks` | Vec<String> |  | An array of Machine Image specific properties for disks attached to the
source instance |
| `total_storage_bytes` | String |  | [Output Only] Total size of the storage used by the machine image. |
| `id` | String |  | [Output Only] A unique identifier for this machine image. The server
defines this identifier. |
| `instance_properties` | String |  | [Output Only] Properties of source instance |
| `status` | String |  | [Output Only] The status of the machine image. One of the following values:INVALID, CREATING, READY,DELETING, and UPLOADING. |
| `source_disk_encryption_keys` | Vec<String> |  | [Input Only] Thecustomer-supplied
encryption key of the disks attached to the source instance. Required
if the source disk is protected by a customer-supplied encryption key. |
| `name` | String |  | Name of the resource; provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must be
a dash, lowercase letter, or digit, except the last character, which cannot
be a dash. |
| `guest_flush` | bool |  | [Input Only] Whether to attempt an application consistent machine image by
informing the OS to prepare for the snapshot process. |
| `self_link` | String |  | [Output Only] The URL for this machine image. The server defines this URL. |
| `machine_image_encryption_key` | String |  | Encrypts the machine image using acustomer-supplied
encryption key.

After you encrypt a machine image using a customer-supplied key, you must
provide the same key if you use the machine image later. For example, you
must provide the encryption key when you create an instance from the
encrypted machine image in a future request.

Customer-supplied encryption keys do not protect access to metadata of the
machine image.

If you do not provide an encryption key when creating the machine image,
then the machine image will be encrypted using an automatically generated
key and you do not need to provide a key to use the machine image later. |
| `satisfies_pzi` | bool |  | Output only. Reserved for future use. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `source_instance` | String | The source instance used to create the machine image. You can provide this
as a partial or full URL to the resource. For example, the following are
valid values: 
   
   
     - https://www.googleapis.com/compute/v1/projects/project/zones/zone/instances/instance 
   - projects/project/zones/zone/instances/instance |
| `source_instance_properties` | String | [Output Only] DEPRECATED: Please use instance_properties
instead for source instance related properties. New properties will not be
added to this field. |
| `label_fingerprint` | String | A fingerprint for the labels being applied to this machine image, which is
essentially a hash of the labels set used for optimistic locking. The
fingerprint is initially generated by Compute Engine and changes after
every request to modify or update labels. You must always provide an
up-to-date fingerprint hash in order to update or change labels.

To see the latest fingerprint, make get() request to the
machine image. |
| `labels` | HashMap<String, String> | Labels to apply to this machine image. These can be later modified by
the setLabels method. |
| `satisfies_pzs` | bool | [Output Only] Reserved for future use. |
| `storage_locations` | Vec<String> | The regional or multi-regional Cloud Storage bucket location where themachine image is
stored. |
| `kind` | String | [Output Only] The resource type, which is alwayscompute#machineImage for machine image. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `creation_timestamp` | String | [Output Only] The creation timestamp for this machine image inRFC3339
text format. |
| `saved_disks` | Vec<String> | An array of Machine Image specific properties for disks attached to the
source instance |
| `total_storage_bytes` | String | [Output Only] Total size of the storage used by the machine image. |
| `id` | String | [Output Only] A unique identifier for this machine image. The server
defines this identifier. |
| `instance_properties` | String | [Output Only] Properties of source instance |
| `status` | String | [Output Only] The status of the machine image. One of the following values:INVALID, CREATING, READY,DELETING, and UPLOADING. |
| `source_disk_encryption_keys` | Vec<String> | [Input Only] Thecustomer-supplied
encryption key of the disks attached to the source instance. Required
if the source disk is protected by a customer-supplied encryption key. |
| `name` | String | Name of the resource; provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must be
a dash, lowercase letter, or digit, except the last character, which cannot
be a dash. |
| `guest_flush` | bool | [Input Only] Whether to attempt an application consistent machine image by
informing the OS to prepare for the snapshot process. |
| `self_link` | String | [Output Only] The URL for this machine image. The server defines this URL. |
| `machine_image_encryption_key` | String | Encrypts the machine image using acustomer-supplied
encryption key.

After you encrypt a machine image using a customer-supplied key, you must
provide the same key if you use the machine image later. For example, you
must provide the encryption key when you create an instance from the
encrypted machine image in a future request.

Customer-supplied encryption keys do not protect access to metadata of the
machine image.

If you do not provide an encryption key when creating the machine image,
then the machine image will be encrypted using an automatically generated
key and you do not need to provide a key to use the machine image later. |
| `satisfies_pzi` | bool | Output only. Reserved for future use. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create machine_image
machine_image = provider.compute_api.Machine_image {
    project = "value"  # Project ID for this request.
}

# Access machine_image outputs
machine_image_id = machine_image.id
machine_image_source_instance = machine_image.source_instance
machine_image_source_instance_properties = machine_image.source_instance_properties
machine_image_label_fingerprint = machine_image.label_fingerprint
machine_image_labels = machine_image.labels
machine_image_satisfies_pzs = machine_image.satisfies_pzs
machine_image_storage_locations = machine_image.storage_locations
machine_image_kind = machine_image.kind
machine_image_description = machine_image.description
machine_image_creation_timestamp = machine_image.creation_timestamp
machine_image_saved_disks = machine_image.saved_disks
machine_image_total_storage_bytes = machine_image.total_storage_bytes
machine_image_id = machine_image.id
machine_image_instance_properties = machine_image.instance_properties
machine_image_status = machine_image.status
machine_image_source_disk_encryption_keys = machine_image.source_disk_encryption_keys
machine_image_name = machine_image.name
machine_image_guest_flush = machine_image.guest_flush
machine_image_self_link = machine_image.self_link
machine_image_machine_image_encryption_key = machine_image.machine_image_encryption_key
machine_image_satisfies_pzi = machine_image.satisfies_pzi
```

---


### Target_tcp_proxie

Creates a TargetTcpProxy resource in the specified project using
the data included in the request.

**Operations**: ✅ Create ✅ Read ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `kind` | String |  | [Output Only] Type of the resource. Alwayscompute#targetTcpProxy for target TCP proxies. |
| `proxy_bind` | bool |  | This field only applies when the forwarding rule that references this
target proxy has a loadBalancingScheme set toINTERNAL_SELF_MANAGED.

When this field is set to true, Envoy proxies set up inbound
traffic interception and bind to the IP address and port specified in the
forwarding rule. This is generally useful when using Traffic Director to
configure Envoy as a gateway or middle proxy (in other words, not a
sidecar proxy). The Envoy proxy listens for inbound requests and handles
requests when it receives them.

The default is false. |
| `proxy_header` | String |  | Specifies the type of proxy header to append before sending data to the
backend, either NONE or PROXY_V1. The default
is NONE. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `name` | String |  | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `region` | String |  | [Output Only] URL of the region where the regional TCP proxy resides.
This field is not applicable to global TCP proxy. |
| `service` | String |  | URL to the BackendService resource. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `kind` | String | [Output Only] Type of the resource. Alwayscompute#targetTcpProxy for target TCP proxies. |
| `proxy_bind` | bool | This field only applies when the forwarding rule that references this
target proxy has a loadBalancingScheme set toINTERNAL_SELF_MANAGED.

When this field is set to true, Envoy proxies set up inbound
traffic interception and bind to the IP address and port specified in the
forwarding rule. This is generally useful when using Traffic Director to
configure Envoy as a gateway or middle proxy (in other words, not a
sidecar proxy). The Envoy proxy listens for inbound requests and handles
requests when it receives them.

The default is false. |
| `proxy_header` | String | Specifies the type of proxy header to append before sending data to the
backend, either NONE or PROXY_V1. The default
is NONE. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `name` | String | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `region` | String | [Output Only] URL of the region where the regional TCP proxy resides.
This field is not applicable to global TCP proxy. |
| `service` | String | URL to the BackendService resource. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create target_tcp_proxie
target_tcp_proxie = provider.compute_api.Target_tcp_proxie {
    project = "value"  # Project ID for this request.
}

# Access target_tcp_proxie outputs
target_tcp_proxie_id = target_tcp_proxie.id
target_tcp_proxie_description = target_tcp_proxie.description
target_tcp_proxie_creation_timestamp = target_tcp_proxie.creation_timestamp
target_tcp_proxie_kind = target_tcp_proxie.kind
target_tcp_proxie_proxy_bind = target_tcp_proxie.proxy_bind
target_tcp_proxie_proxy_header = target_tcp_proxie.proxy_header
target_tcp_proxie_id = target_tcp_proxie.id
target_tcp_proxie_name = target_tcp_proxie.name
target_tcp_proxie_region = target_tcp_proxie.region
target_tcp_proxie_service = target_tcp_proxie.service
target_tcp_proxie_self_link = target_tcp_proxie.self_link
```

---


### Target_https_proxie

Creates a TargetHttpsProxy resource in the specified
project using the data included in the request.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `name` | String |  | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `ssl_certificates` | Vec<String> |  | URLs to SslCertificate resources that are used to authenticate
connections between users and the load balancer. At least one SSL
certificate must be specified. SslCertificates do not apply when the load
balancing scheme is set to INTERNAL_SELF_MANAGED.

The URLs should refer to a SSL Certificate resource or Certificate Manager
Certificate resource. Mixing Classic Certificates and Certificate Manager
Certificates is not allowed. Certificate Manager Certificates must include
the certificatemanager API namespace. Using Certificate Manager
Certificates in this field is not supported by Global external Application
Load Balancer or Classic Application Load Balancer, use certificate_map
instead.

Currently, you may specify up to 15 Classic SSL Certificates or up to 100
Certificate Manager Certificates.

Certificate Manager Certificates accepted formats are:
   
   - //certificatemanager.googleapis.com/projects/{project}/locations/{location}/certificates/{resourceName}.
   - https://certificatemanager.googleapis.com/v1alpha1/projects/{project}/locations/{location}/certificates/{resourceName}. |
| `region` | String |  | [Output Only] URL of the region where the regional TargetHttpsProxy
resides. This field is not applicable to global TargetHttpsProxies. |
| `http_keep_alive_timeout_sec` | i64 |  | Specifies how long to keep a connection open, after completing a response,
while there is no matching traffic (in seconds). If an HTTP keep-alive is
not specified, a default value (610 seconds) will be used.

For global external Application Load Balancers, the minimum allowed value
is 5 seconds and the maximum allowed value is 1200 seconds.

For classic Application Load Balancers, this option is not supported. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `server_tls_policy` | String |  | Optional. A URL referring to a networksecurity.ServerTlsPolicy resource
that describes how the proxy should authenticate inbound traffic.

 serverTlsPolicy only applies to a globalTargetHttpsProxy attached toglobalForwardingRules with theloadBalancingScheme set to INTERNAL_SELF_MANAGED
or EXTERNAL orEXTERNAL_MANAGED or INTERNAL_MANAGED.
It also applies to a regional TargetHttpsProxy attached to
regional forwardingRules with theloadBalancingScheme set to EXTERNAL_MANAGED orINTERNAL_MANAGED. For details whichServerTlsPolicy resources are accepted withINTERNAL_SELF_MANAGED and which with EXTERNAL,INTERNAL_MANAGED, EXTERNAL_MANAGEDloadBalancingScheme consult ServerTlsPolicy
documentation.

  If left blank, communications are not encrypted. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `certificate_map` | String |  | URL of a certificate map that identifies a certificate map associated with
the given target proxy.
This field can only be set for Global external Application Load Balancer or
Classic Application Load Balancer. For other products use Certificate
Manager Certificates instead.

If set, sslCertificates will be ignored.

 Accepted format is//certificatemanager.googleapis.com/projects/{project}/locations/{location}/certificateMaps/{resourceName}. |
| `proxy_bind` | bool |  | This field only applies when the forwarding rule that references this
target proxy has a loadBalancingScheme set toINTERNAL_SELF_MANAGED.

When this field is set to true, Envoy proxies set up inbound
traffic interception and bind to the IP address and port specified in the
forwarding rule. This is generally useful when using Traffic Director to
configure Envoy as a gateway or middle proxy (in other words, not a
sidecar proxy). The Envoy proxy listens for inbound requests and handles
requests when it receives them.

The default is false. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `quic_override` | String |  | Specifies the QUIC override policy for this TargetHttpsProxy resource. This
setting determines whether the load balancer attempts to negotiate QUIC
with clients.
You can specify NONE, ENABLE, orDISABLE.
   
   - When quic-override is set to NONE,
   Google manages whether QUIC is used.
   - When quic-override is set to ENABLE, the
   load balancer uses QUIC when possible.
   - When quic-override is set to DISABLE, the
   load balancer doesn't use QUIC.
   - If the quic-override flag is not specified,NONE is implied. |
| `url_map` | String |  | A fully-qualified or valid partial URL to the UrlMap resource that defines
the mapping from URL to the BackendService. For example, the following are
all valid URLs for specifying a URL map:
   
   - https://www.googleapis.compute/v1/projects/project/global/urlMaps/url-map 
   - projects/project/global/urlMaps/url-map 
   - global/urlMaps/url-map |
| `authorization_policy` | String |  | Optional. A URL referring to a networksecurity.AuthorizationPolicy resource
that describes how the proxy should authorize inbound traffic. If left
blank, access will not be restricted by an authorization policy.

 Refer to the AuthorizationPolicy resource for additional
details.

 authorizationPolicy only applies to a globalTargetHttpsProxy attached toglobalForwardingRules with theloadBalancingScheme set to INTERNAL_SELF_MANAGED.

 Note: This field currently has no impact. |
| `fingerprint` | String |  | Fingerprint of this resource. A hash of the contents stored in this object.
This field is used in optimistic locking. This field will be ignored when
inserting a TargetHttpsProxy. An up-to-date fingerprint must
be provided in order to patch the TargetHttpsProxy; otherwise, the request
will fail with error 412 conditionNotMet. To see the latest
fingerprint, make a get() request to retrieve the
TargetHttpsProxy. |
| `ssl_policy` | String |  | URL of SslPolicy resource that will be associated with the TargetHttpsProxy
resource. If not set, the TargetHttpsProxy resource has no
SSL policy configured. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `kind` | String |  | [Output Only] Type of resource. Alwayscompute#targetHttpsProxy for target HTTPS proxies. |
| `tls_early_data` | String |  | Specifies whether TLS 1.3 0-RTT Data ("Early Data") should be accepted
for this service. Early Data allows a TLS resumption handshake to include
the initial application payload (a HTTP request) alongside the handshake,
reducing the effective round trips to "zero". This applies to TLS 1.3
connections over TCP (HTTP/2) as well as over UDP (QUIC/h3).


This can improve application performance, especially on networks where
interruptions may be common, such as on mobile.


Requests with Early Data will have the "Early-Data" HTTP header set on
the request, with a value of "1", to allow the backend to determine whether
Early Data was included.


Note: TLS Early Data may allow requests to be replayed, as the data is
sent to the backend before the handshake has fully completed. Applications
that allow idempotent HTTP methods to make non-idempotent changes, such as
a GET request updating a database, should not accept Early Data on those
requests, and reject requests with the "Early-Data: 1" HTTP header by
returning a HTTP 425 (Too Early) status code, in order to remain RFC
compliant.


The default value is DISABLED. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `name` | String | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `ssl_certificates` | Vec<String> | URLs to SslCertificate resources that are used to authenticate
connections between users and the load balancer. At least one SSL
certificate must be specified. SslCertificates do not apply when the load
balancing scheme is set to INTERNAL_SELF_MANAGED.

The URLs should refer to a SSL Certificate resource or Certificate Manager
Certificate resource. Mixing Classic Certificates and Certificate Manager
Certificates is not allowed. Certificate Manager Certificates must include
the certificatemanager API namespace. Using Certificate Manager
Certificates in this field is not supported by Global external Application
Load Balancer or Classic Application Load Balancer, use certificate_map
instead.

Currently, you may specify up to 15 Classic SSL Certificates or up to 100
Certificate Manager Certificates.

Certificate Manager Certificates accepted formats are:
   
   - //certificatemanager.googleapis.com/projects/{project}/locations/{location}/certificates/{resourceName}.
   - https://certificatemanager.googleapis.com/v1alpha1/projects/{project}/locations/{location}/certificates/{resourceName}. |
| `region` | String | [Output Only] URL of the region where the regional TargetHttpsProxy
resides. This field is not applicable to global TargetHttpsProxies. |
| `http_keep_alive_timeout_sec` | i64 | Specifies how long to keep a connection open, after completing a response,
while there is no matching traffic (in seconds). If an HTTP keep-alive is
not specified, a default value (610 seconds) will be used.

For global external Application Load Balancers, the minimum allowed value
is 5 seconds and the maximum allowed value is 1200 seconds.

For classic Application Load Balancers, this option is not supported. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `server_tls_policy` | String | Optional. A URL referring to a networksecurity.ServerTlsPolicy resource
that describes how the proxy should authenticate inbound traffic.

 serverTlsPolicy only applies to a globalTargetHttpsProxy attached toglobalForwardingRules with theloadBalancingScheme set to INTERNAL_SELF_MANAGED
or EXTERNAL orEXTERNAL_MANAGED or INTERNAL_MANAGED.
It also applies to a regional TargetHttpsProxy attached to
regional forwardingRules with theloadBalancingScheme set to EXTERNAL_MANAGED orINTERNAL_MANAGED. For details whichServerTlsPolicy resources are accepted withINTERNAL_SELF_MANAGED and which with EXTERNAL,INTERNAL_MANAGED, EXTERNAL_MANAGEDloadBalancingScheme consult ServerTlsPolicy
documentation.

  If left blank, communications are not encrypted. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `certificate_map` | String | URL of a certificate map that identifies a certificate map associated with
the given target proxy.
This field can only be set for Global external Application Load Balancer or
Classic Application Load Balancer. For other products use Certificate
Manager Certificates instead.

If set, sslCertificates will be ignored.

 Accepted format is//certificatemanager.googleapis.com/projects/{project}/locations/{location}/certificateMaps/{resourceName}. |
| `proxy_bind` | bool | This field only applies when the forwarding rule that references this
target proxy has a loadBalancingScheme set toINTERNAL_SELF_MANAGED.

When this field is set to true, Envoy proxies set up inbound
traffic interception and bind to the IP address and port specified in the
forwarding rule. This is generally useful when using Traffic Director to
configure Envoy as a gateway or middle proxy (in other words, not a
sidecar proxy). The Envoy proxy listens for inbound requests and handles
requests when it receives them.

The default is false. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `quic_override` | String | Specifies the QUIC override policy for this TargetHttpsProxy resource. This
setting determines whether the load balancer attempts to negotiate QUIC
with clients.
You can specify NONE, ENABLE, orDISABLE.
   
   - When quic-override is set to NONE,
   Google manages whether QUIC is used.
   - When quic-override is set to ENABLE, the
   load balancer uses QUIC when possible.
   - When quic-override is set to DISABLE, the
   load balancer doesn't use QUIC.
   - If the quic-override flag is not specified,NONE is implied. |
| `url_map` | String | A fully-qualified or valid partial URL to the UrlMap resource that defines
the mapping from URL to the BackendService. For example, the following are
all valid URLs for specifying a URL map:
   
   - https://www.googleapis.compute/v1/projects/project/global/urlMaps/url-map 
   - projects/project/global/urlMaps/url-map 
   - global/urlMaps/url-map |
| `authorization_policy` | String | Optional. A URL referring to a networksecurity.AuthorizationPolicy resource
that describes how the proxy should authorize inbound traffic. If left
blank, access will not be restricted by an authorization policy.

 Refer to the AuthorizationPolicy resource for additional
details.

 authorizationPolicy only applies to a globalTargetHttpsProxy attached toglobalForwardingRules with theloadBalancingScheme set to INTERNAL_SELF_MANAGED.

 Note: This field currently has no impact. |
| `fingerprint` | String | Fingerprint of this resource. A hash of the contents stored in this object.
This field is used in optimistic locking. This field will be ignored when
inserting a TargetHttpsProxy. An up-to-date fingerprint must
be provided in order to patch the TargetHttpsProxy; otherwise, the request
will fail with error 412 conditionNotMet. To see the latest
fingerprint, make a get() request to retrieve the
TargetHttpsProxy. |
| `ssl_policy` | String | URL of SslPolicy resource that will be associated with the TargetHttpsProxy
resource. If not set, the TargetHttpsProxy resource has no
SSL policy configured. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `kind` | String | [Output Only] Type of resource. Alwayscompute#targetHttpsProxy for target HTTPS proxies. |
| `tls_early_data` | String | Specifies whether TLS 1.3 0-RTT Data ("Early Data") should be accepted
for this service. Early Data allows a TLS resumption handshake to include
the initial application payload (a HTTP request) alongside the handshake,
reducing the effective round trips to "zero". This applies to TLS 1.3
connections over TCP (HTTP/2) as well as over UDP (QUIC/h3).


This can improve application performance, especially on networks where
interruptions may be common, such as on mobile.


Requests with Early Data will have the "Early-Data" HTTP header set on
the request, with a value of "1", to allow the backend to determine whether
Early Data was included.


Note: TLS Early Data may allow requests to be replayed, as the data is
sent to the backend before the handshake has fully completed. Applications
that allow idempotent HTTP methods to make non-idempotent changes, such as
a GET request updating a database, should not accept Early Data on those
requests, and reject requests with the "Early-Data: 1" HTTP header by
returning a HTTP 425 (Too Early) status code, in order to remain RFC
compliant.


The default value is DISABLED. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create target_https_proxie
target_https_proxie = provider.compute_api.Target_https_proxie {
    project = "value"  # Project ID for this request.
}

# Access target_https_proxie outputs
target_https_proxie_id = target_https_proxie.id
target_https_proxie_name = target_https_proxie.name
target_https_proxie_ssl_certificates = target_https_proxie.ssl_certificates
target_https_proxie_region = target_https_proxie.region
target_https_proxie_http_keep_alive_timeout_sec = target_https_proxie.http_keep_alive_timeout_sec
target_https_proxie_description = target_https_proxie.description
target_https_proxie_server_tls_policy = target_https_proxie.server_tls_policy
target_https_proxie_self_link = target_https_proxie.self_link
target_https_proxie_certificate_map = target_https_proxie.certificate_map
target_https_proxie_proxy_bind = target_https_proxie.proxy_bind
target_https_proxie_id = target_https_proxie.id
target_https_proxie_quic_override = target_https_proxie.quic_override
target_https_proxie_url_map = target_https_proxie.url_map
target_https_proxie_authorization_policy = target_https_proxie.authorization_policy
target_https_proxie_fingerprint = target_https_proxie.fingerprint
target_https_proxie_ssl_policy = target_https_proxie.ssl_policy
target_https_proxie_creation_timestamp = target_https_proxie.creation_timestamp
target_https_proxie_kind = target_https_proxie.kind
target_https_proxie_tls_early_data = target_https_proxie.tls_early_data
```

---


### Addresse

Creates an address resource in the specified project by using the data
included in the request.

**Operations**: ✅ Create ✅ Read ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `ipv6_endpoint_type` | String |  | The endpoint type of this address, which should be VM
or NETLB. This is used for deciding which type of endpoint
this address can be used after the external IPv6 address reservation. |
| `description` | String |  | An optional description of this resource. Provide this field when you
create the resource. |
| `labels` | HashMap<String, String> |  | Labels for this resource. These can only be added or modified by thesetLabels method. Each label key/value pair must comply withRFC1035.
Label values may be empty. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `subnetwork` | String |  | The URL of the subnetwork in which to reserve the address. If an IP address
is specified, it must be within the subnetwork's IP range. This field can
only be used with INTERNAL type with aGCE_ENDPOINT or DNS_RESOLVER purpose. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `label_fingerprint` | String |  | A fingerprint for the labels being applied to this Address, which is
essentially a hash of the labels set used for optimistic locking. The
fingerprint is initially generated by Compute Engine and changes after
every request to modify or update labels. You must always provide an
up-to-date fingerprint hash in order to update or change labels,
otherwise the request will fail with error412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve an Address. |
| `purpose` | String |  | The purpose of this resource, which can be one of the following values:
   
   
     - GCE_ENDPOINT for addresses that are used by VM
     instances, alias IP ranges, load balancers, and similar resources.
     - DNS_RESOLVER for a DNS resolver address in a subnetwork
       for a Cloud DNS  inbound
       forwarder IP addresses (regional internal IP address in a subnet of
       a VPC network)
     - VPC_PEERING for global internal IP addresses used for
      
          private services access allocated ranges.
     - NAT_AUTO for the regional external IP addresses used by
          Cloud NAT when allocating addresses using
          
          automatic NAT IP address allocation.
     - IPSEC_INTERCONNECT for addresses created from a private
     IP range that are reserved for a VLAN attachment in an
     *HA VPN over Cloud Interconnect* configuration. These addresses
     are regional resources.
     - `SHARED_LOADBALANCER_VIP` for an internal IP address that is assigned
     to multiple internal forwarding rules.
     - `PRIVATE_SERVICE_CONNECT` for a private network address that is
     used to configure Private Service Connect. Only global internal addresses
     can use this purpose. |
| `status` | String |  | [Output Only] The status of the address, which can be one ofRESERVING, RESERVED, or IN_USE.
An address that is RESERVING is currently in the process of
being reserved. A RESERVED address is currently reserved and
available to use. An IN_USE address is currently being used
by another resource and is not available. |
| `region` | String |  | [Output Only] The URL of the region where a regional address resides.
For regional addresses, you must specify the region as a path parameter in
the HTTP request URL. *This field is not applicable to global
addresses.* |
| `ip_version` | String |  | The IP version that will be used by this address. Valid options areIPV4 or IPV6. |
| `name` | String |  | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?`. The first character
must be a lowercase letter, and all following characters (except for the
last character) must be a dash, lowercase letter, or digit. The last
character must be a lowercase letter or digit. |
| `prefix_length` | i64 |  | The prefix length if the resource represents an IP range. |
| `kind` | String |  | [Output Only] Type of the resource. Always compute#address for
addresses. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `address_type` | String |  | The type of address to reserve, either INTERNAL orEXTERNAL. If unspecified, defaults to EXTERNAL. |
| `network_tier` | String |  | This signifies the networking tier used for configuring this address and
can only take the following values: PREMIUM orSTANDARD. Internal IP addresses are always Premium Tier;
global external IP addresses are always Premium Tier; regional external IP
addresses can be either Standard or Premium Tier.

If this field is not specified, it is assumed to be PREMIUM. |
| `users` | Vec<String> |  | [Output Only] The URLs of the resources that are using this address. |
| `address` | String |  | The static IP address represented by this resource. |
| `network` | String |  | The URL of the network in which to reserve the address. This field can
only be used with INTERNAL type with theVPC_PEERING purpose. |
| `project` | String | ✅ | Project ID for this request. |
| `region` | String | ✅ | Name of the region for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `ipv6_endpoint_type` | String | The endpoint type of this address, which should be VM
or NETLB. This is used for deciding which type of endpoint
this address can be used after the external IPv6 address reservation. |
| `description` | String | An optional description of this resource. Provide this field when you
create the resource. |
| `labels` | HashMap<String, String> | Labels for this resource. These can only be added or modified by thesetLabels method. Each label key/value pair must comply withRFC1035.
Label values may be empty. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `subnetwork` | String | The URL of the subnetwork in which to reserve the address. If an IP address
is specified, it must be within the subnetwork's IP range. This field can
only be used with INTERNAL type with aGCE_ENDPOINT or DNS_RESOLVER purpose. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `label_fingerprint` | String | A fingerprint for the labels being applied to this Address, which is
essentially a hash of the labels set used for optimistic locking. The
fingerprint is initially generated by Compute Engine and changes after
every request to modify or update labels. You must always provide an
up-to-date fingerprint hash in order to update or change labels,
otherwise the request will fail with error412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve an Address. |
| `purpose` | String | The purpose of this resource, which can be one of the following values:
   
   
     - GCE_ENDPOINT for addresses that are used by VM
     instances, alias IP ranges, load balancers, and similar resources.
     - DNS_RESOLVER for a DNS resolver address in a subnetwork
       for a Cloud DNS  inbound
       forwarder IP addresses (regional internal IP address in a subnet of
       a VPC network)
     - VPC_PEERING for global internal IP addresses used for
      
          private services access allocated ranges.
     - NAT_AUTO for the regional external IP addresses used by
          Cloud NAT when allocating addresses using
          
          automatic NAT IP address allocation.
     - IPSEC_INTERCONNECT for addresses created from a private
     IP range that are reserved for a VLAN attachment in an
     *HA VPN over Cloud Interconnect* configuration. These addresses
     are regional resources.
     - `SHARED_LOADBALANCER_VIP` for an internal IP address that is assigned
     to multiple internal forwarding rules.
     - `PRIVATE_SERVICE_CONNECT` for a private network address that is
     used to configure Private Service Connect. Only global internal addresses
     can use this purpose. |
| `status` | String | [Output Only] The status of the address, which can be one ofRESERVING, RESERVED, or IN_USE.
An address that is RESERVING is currently in the process of
being reserved. A RESERVED address is currently reserved and
available to use. An IN_USE address is currently being used
by another resource and is not available. |
| `region` | String | [Output Only] The URL of the region where a regional address resides.
For regional addresses, you must specify the region as a path parameter in
the HTTP request URL. *This field is not applicable to global
addresses.* |
| `ip_version` | String | The IP version that will be used by this address. Valid options areIPV4 or IPV6. |
| `name` | String | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?`. The first character
must be a lowercase letter, and all following characters (except for the
last character) must be a dash, lowercase letter, or digit. The last
character must be a lowercase letter or digit. |
| `prefix_length` | i64 | The prefix length if the resource represents an IP range. |
| `kind` | String | [Output Only] Type of the resource. Always compute#address for
addresses. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `address_type` | String | The type of address to reserve, either INTERNAL orEXTERNAL. If unspecified, defaults to EXTERNAL. |
| `network_tier` | String | This signifies the networking tier used for configuring this address and
can only take the following values: PREMIUM orSTANDARD. Internal IP addresses are always Premium Tier;
global external IP addresses are always Premium Tier; regional external IP
addresses can be either Standard or Premium Tier.

If this field is not specified, it is assumed to be PREMIUM. |
| `users` | Vec<String> | [Output Only] The URLs of the resources that are using this address. |
| `address` | String | The static IP address represented by this resource. |
| `network` | String | The URL of the network in which to reserve the address. This field can
only be used with INTERNAL type with theVPC_PEERING purpose. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create addresse
addresse = provider.compute_api.Addresse {
    project = "value"  # Project ID for this request.
    region = "value"  # Name of the region for this request.
}

# Access addresse outputs
addresse_id = addresse.id
addresse_ipv6_endpoint_type = addresse.ipv6_endpoint_type
addresse_description = addresse.description
addresse_labels = addresse.labels
addresse_self_link = addresse.self_link
addresse_subnetwork = addresse.subnetwork
addresse_id = addresse.id
addresse_label_fingerprint = addresse.label_fingerprint
addresse_purpose = addresse.purpose
addresse_status = addresse.status
addresse_region = addresse.region
addresse_ip_version = addresse.ip_version
addresse_name = addresse.name
addresse_prefix_length = addresse.prefix_length
addresse_kind = addresse.kind
addresse_creation_timestamp = addresse.creation_timestamp
addresse_address_type = addresse.address_type
addresse_network_tier = addresse.network_tier
addresse_users = addresse.users
addresse_address = addresse.address
addresse_network = addresse.network
```

---


### Organization_security_policie

Creates a new policy in the specified organization using the data included
in the request.

Use of this API to insert firewall policies is deprecated. Use
firewallPolicies.insert instead.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `associations` | Vec<String> |  | A list of associations that belong to this policy. |
| `type` | String |  | The type indicates the intended use of the security policy.
   
   - CLOUD_ARMOR: Cloud Armor backend security policies can
   be configured to filter incoming HTTP requests targeting backend services.
   They filter requests before they hit the origin servers.
   - CLOUD_ARMOR_EDGE: Cloud Armor edge security policies can
   be configured to filter incoming HTTP requests targeting backend services
   (including Cloud CDN-enabled) as well as backend buckets (Cloud Storage).
   They filter requests before the request is served from Google's cache.
   - CLOUD_ARMOR_INTERNAL_SERVICE (preview only): Cloud Armor
   internal service policies can be configured to filter HTTP requests
   targeting services managed by Traffic Director in a service mesh. They
   filter requests before the request is served from the application.

- CLOUD_ARMOR_NETWORK: Cloud Armor network policies
can be configured to filter packets targeting network load balancing
resources such as backend services, target pools, target instances, and
instances with external IPs. They filter requests before the request is
served from the application.


This field can be set only at resource creation time. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `ddos_protection_config` | String |  |  |
| `name` | String |  | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `advanced_options_config` | String |  |  |
| `recaptcha_options_config` | String |  |  |
| `kind` | String |  | [Output only] Type of the resource. Alwayscompute#securityPolicyfor security policies |
| `user_defined_fields` | Vec<String> |  | Definitions of user-defined fields for CLOUD_ARMOR_NETWORK policies. A
user-defined field consists of up to 4 bytes extracted from a fixed offset
in the packet, relative to the IPv4, IPv6, TCP, or UDP header, with an
optional mask to select certain bits. Rules may then specify matching
values for these fields.

Example:

 userDefinedFields:
 - name: "ipv4_fragment_offset"
   base: IPV4
   offset: 6
   size: 2
   mask: "0x1fff" |
| `adaptive_protection_config` | String |  |  |
| `rules` | Vec<String> |  | A list of rules that belong to this policy.
There must always be a default rule which is a rule with priority
2147483647 and match all condition (for the match condition this means
match  "*" for srcIpRanges and for the networkMatch condition every field
must be either match "*" or not set). If no rules are provided when
creating a security policy, a default rule with action "allow" will be
added. |
| `region` | String |  | [Output Only] URL of the region where the regional security policy
resides. This field is not applicable to global security policies. |
| `fingerprint` | String |  | Specifies a fingerprint for this resource, which is essentially a hash of
the metadata's contents and used for optimistic locking. The
fingerprint is initially generated by Compute Engine and changes after
every request to modify or update metadata. You must always provide an
up-to-date fingerprint hash in order to update or change metadata,
otherwise the request will fail with error412 conditionNotMet.

To see the latest fingerprint, make get() request to the
security policy. |
| `labels` | HashMap<String, String> |  | Labels for this resource. These can only be added or modified by thesetLabels method. Each label key/value pair must comply withRFC1035.
Label values may be empty. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `short_name` | String |  | User-provided name of the organization security policy. The name should be
unique in the organization in which the security policy is created. This
should only be used when SecurityPolicyType is CLOUD_ARMOR.
The name must be 1-63 characters long, and comply with
https://www.ietf.org/rfc/rfc1035.txt. Specifically, the name must be 1-63
characters long and match the regular expression
`[a-z]([-a-z0-9]*[a-z0-9])?` which means the first character must be a
lowercase letter, and all following characters must be a dash, lowercase
letter, or digit, except the last character, which cannot be a dash. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `label_fingerprint` | String |  | A fingerprint for the labels being applied to this security policy, which
is essentially a hash of the labels set used for optimistic locking. The
fingerprint is initially generated by Compute Engine and changes after
every request to modify or update labels. You must always provide an
up-to-date fingerprint hash in order to update or change labels.

To see the latest fingerprint, make get() request to the
security policy. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `associations` | Vec<String> | A list of associations that belong to this policy. |
| `type` | String | The type indicates the intended use of the security policy.
   
   - CLOUD_ARMOR: Cloud Armor backend security policies can
   be configured to filter incoming HTTP requests targeting backend services.
   They filter requests before they hit the origin servers.
   - CLOUD_ARMOR_EDGE: Cloud Armor edge security policies can
   be configured to filter incoming HTTP requests targeting backend services
   (including Cloud CDN-enabled) as well as backend buckets (Cloud Storage).
   They filter requests before the request is served from Google's cache.
   - CLOUD_ARMOR_INTERNAL_SERVICE (preview only): Cloud Armor
   internal service policies can be configured to filter HTTP requests
   targeting services managed by Traffic Director in a service mesh. They
   filter requests before the request is served from the application.

- CLOUD_ARMOR_NETWORK: Cloud Armor network policies
can be configured to filter packets targeting network load balancing
resources such as backend services, target pools, target instances, and
instances with external IPs. They filter requests before the request is
served from the application.


This field can be set only at resource creation time. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `ddos_protection_config` | String |  |
| `name` | String | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `advanced_options_config` | String |  |
| `recaptcha_options_config` | String |  |
| `kind` | String | [Output only] Type of the resource. Alwayscompute#securityPolicyfor security policies |
| `user_defined_fields` | Vec<String> | Definitions of user-defined fields for CLOUD_ARMOR_NETWORK policies. A
user-defined field consists of up to 4 bytes extracted from a fixed offset
in the packet, relative to the IPv4, IPv6, TCP, or UDP header, with an
optional mask to select certain bits. Rules may then specify matching
values for these fields.

Example:

 userDefinedFields:
 - name: "ipv4_fragment_offset"
   base: IPV4
   offset: 6
   size: 2
   mask: "0x1fff" |
| `adaptive_protection_config` | String |  |
| `rules` | Vec<String> | A list of rules that belong to this policy.
There must always be a default rule which is a rule with priority
2147483647 and match all condition (for the match condition this means
match  "*" for srcIpRanges and for the networkMatch condition every field
must be either match "*" or not set). If no rules are provided when
creating a security policy, a default rule with action "allow" will be
added. |
| `region` | String | [Output Only] URL of the region where the regional security policy
resides. This field is not applicable to global security policies. |
| `fingerprint` | String | Specifies a fingerprint for this resource, which is essentially a hash of
the metadata's contents and used for optimistic locking. The
fingerprint is initially generated by Compute Engine and changes after
every request to modify or update metadata. You must always provide an
up-to-date fingerprint hash in order to update or change metadata,
otherwise the request will fail with error412 conditionNotMet.

To see the latest fingerprint, make get() request to the
security policy. |
| `labels` | HashMap<String, String> | Labels for this resource. These can only be added or modified by thesetLabels method. Each label key/value pair must comply withRFC1035.
Label values may be empty. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `short_name` | String | User-provided name of the organization security policy. The name should be
unique in the organization in which the security policy is created. This
should only be used when SecurityPolicyType is CLOUD_ARMOR.
The name must be 1-63 characters long, and comply with
https://www.ietf.org/rfc/rfc1035.txt. Specifically, the name must be 1-63
characters long and match the regular expression
`[a-z]([-a-z0-9]*[a-z0-9])?` which means the first character must be a
lowercase letter, and all following characters must be a dash, lowercase
letter, or digit, except the last character, which cannot be a dash. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `label_fingerprint` | String | A fingerprint for the labels being applied to this security policy, which
is essentially a hash of the labels set used for optimistic locking. The
fingerprint is initially generated by Compute Engine and changes after
every request to modify or update labels. You must always provide an
up-to-date fingerprint hash in order to update or change labels.

To see the latest fingerprint, make get() request to the
security policy. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create organization_security_policie
organization_security_policie = provider.compute_api.Organization_security_policie {
}

# Access organization_security_policie outputs
organization_security_policie_id = organization_security_policie.id
organization_security_policie_associations = organization_security_policie.associations
organization_security_policie_type = organization_security_policie.type
organization_security_policie_id = organization_security_policie.id
organization_security_policie_creation_timestamp = organization_security_policie.creation_timestamp
organization_security_policie_ddos_protection_config = organization_security_policie.ddos_protection_config
organization_security_policie_name = organization_security_policie.name
organization_security_policie_advanced_options_config = organization_security_policie.advanced_options_config
organization_security_policie_recaptcha_options_config = organization_security_policie.recaptcha_options_config
organization_security_policie_kind = organization_security_policie.kind
organization_security_policie_user_defined_fields = organization_security_policie.user_defined_fields
organization_security_policie_adaptive_protection_config = organization_security_policie.adaptive_protection_config
organization_security_policie_rules = organization_security_policie.rules
organization_security_policie_region = organization_security_policie.region
organization_security_policie_fingerprint = organization_security_policie.fingerprint
organization_security_policie_labels = organization_security_policie.labels
organization_security_policie_self_link = organization_security_policie.self_link
organization_security_policie_short_name = organization_security_policie.short_name
organization_security_policie_description = organization_security_policie.description
organization_security_policie_label_fingerprint = organization_security_policie.label_fingerprint
```

---


### Network_profile

Returns the specified network profile.

**Operations**: ✅ Read

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `name` | String | [Output Only] Name of the resource. |
| `profile_type` | String | [Output Only] Type of the network profile. |
| `kind` | String | [Output Only] Type of the resource. Alwayscompute#networkProfile for network profiles. |
| `features` | String | [Output Only] Features supported by the network. |
| `self_link_with_id` | String | [Output Only] Server-defined URL for this resource with the resource id. |
| `location` | String | [Output Only] Location to which the network is restricted. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `description` | String | [Output Only] An optional description of this resource. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Access network_profile outputs
network_profile_id = network_profile.id
network_profile_name = network_profile.name
network_profile_profile_type = network_profile.profile_type
network_profile_kind = network_profile.kind
network_profile_features = network_profile.features
network_profile_self_link_with_id = network_profile.self_link_with_id
network_profile_location = network_profile.location
network_profile_creation_timestamp = network_profile.creation_timestamp
network_profile_description = network_profile.description
network_profile_id = network_profile.id
network_profile_self_link = network_profile.self_link
```

---


### Global_addresse

Creates an address resource in the specified project by using the data
included in the request.

**Operations**: ✅ Create ✅ Read ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `ipv6_endpoint_type` | String |  | The endpoint type of this address, which should be VM
or NETLB. This is used for deciding which type of endpoint
this address can be used after the external IPv6 address reservation. |
| `description` | String |  | An optional description of this resource. Provide this field when you
create the resource. |
| `labels` | HashMap<String, String> |  | Labels for this resource. These can only be added or modified by thesetLabels method. Each label key/value pair must comply withRFC1035.
Label values may be empty. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `subnetwork` | String |  | The URL of the subnetwork in which to reserve the address. If an IP address
is specified, it must be within the subnetwork's IP range. This field can
only be used with INTERNAL type with aGCE_ENDPOINT or DNS_RESOLVER purpose. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `label_fingerprint` | String |  | A fingerprint for the labels being applied to this Address, which is
essentially a hash of the labels set used for optimistic locking. The
fingerprint is initially generated by Compute Engine and changes after
every request to modify or update labels. You must always provide an
up-to-date fingerprint hash in order to update or change labels,
otherwise the request will fail with error412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve an Address. |
| `purpose` | String |  | The purpose of this resource, which can be one of the following values:
   
   
     - GCE_ENDPOINT for addresses that are used by VM
     instances, alias IP ranges, load balancers, and similar resources.
     - DNS_RESOLVER for a DNS resolver address in a subnetwork
       for a Cloud DNS  inbound
       forwarder IP addresses (regional internal IP address in a subnet of
       a VPC network)
     - VPC_PEERING for global internal IP addresses used for
      
          private services access allocated ranges.
     - NAT_AUTO for the regional external IP addresses used by
          Cloud NAT when allocating addresses using
          
          automatic NAT IP address allocation.
     - IPSEC_INTERCONNECT for addresses created from a private
     IP range that are reserved for a VLAN attachment in an
     *HA VPN over Cloud Interconnect* configuration. These addresses
     are regional resources.
     - `SHARED_LOADBALANCER_VIP` for an internal IP address that is assigned
     to multiple internal forwarding rules.
     - `PRIVATE_SERVICE_CONNECT` for a private network address that is
     used to configure Private Service Connect. Only global internal addresses
     can use this purpose. |
| `status` | String |  | [Output Only] The status of the address, which can be one ofRESERVING, RESERVED, or IN_USE.
An address that is RESERVING is currently in the process of
being reserved. A RESERVED address is currently reserved and
available to use. An IN_USE address is currently being used
by another resource and is not available. |
| `region` | String |  | [Output Only] The URL of the region where a regional address resides.
For regional addresses, you must specify the region as a path parameter in
the HTTP request URL. *This field is not applicable to global
addresses.* |
| `ip_version` | String |  | The IP version that will be used by this address. Valid options areIPV4 or IPV6. |
| `name` | String |  | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?`. The first character
must be a lowercase letter, and all following characters (except for the
last character) must be a dash, lowercase letter, or digit. The last
character must be a lowercase letter or digit. |
| `prefix_length` | i64 |  | The prefix length if the resource represents an IP range. |
| `kind` | String |  | [Output Only] Type of the resource. Always compute#address for
addresses. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `address_type` | String |  | The type of address to reserve, either INTERNAL orEXTERNAL. If unspecified, defaults to EXTERNAL. |
| `network_tier` | String |  | This signifies the networking tier used for configuring this address and
can only take the following values: PREMIUM orSTANDARD. Internal IP addresses are always Premium Tier;
global external IP addresses are always Premium Tier; regional external IP
addresses can be either Standard or Premium Tier.

If this field is not specified, it is assumed to be PREMIUM. |
| `users` | Vec<String> |  | [Output Only] The URLs of the resources that are using this address. |
| `address` | String |  | The static IP address represented by this resource. |
| `network` | String |  | The URL of the network in which to reserve the address. This field can
only be used with INTERNAL type with theVPC_PEERING purpose. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `ipv6_endpoint_type` | String | The endpoint type of this address, which should be VM
or NETLB. This is used for deciding which type of endpoint
this address can be used after the external IPv6 address reservation. |
| `description` | String | An optional description of this resource. Provide this field when you
create the resource. |
| `labels` | HashMap<String, String> | Labels for this resource. These can only be added or modified by thesetLabels method. Each label key/value pair must comply withRFC1035.
Label values may be empty. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `subnetwork` | String | The URL of the subnetwork in which to reserve the address. If an IP address
is specified, it must be within the subnetwork's IP range. This field can
only be used with INTERNAL type with aGCE_ENDPOINT or DNS_RESOLVER purpose. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `label_fingerprint` | String | A fingerprint for the labels being applied to this Address, which is
essentially a hash of the labels set used for optimistic locking. The
fingerprint is initially generated by Compute Engine and changes after
every request to modify or update labels. You must always provide an
up-to-date fingerprint hash in order to update or change labels,
otherwise the request will fail with error412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve an Address. |
| `purpose` | String | The purpose of this resource, which can be one of the following values:
   
   
     - GCE_ENDPOINT for addresses that are used by VM
     instances, alias IP ranges, load balancers, and similar resources.
     - DNS_RESOLVER for a DNS resolver address in a subnetwork
       for a Cloud DNS  inbound
       forwarder IP addresses (regional internal IP address in a subnet of
       a VPC network)
     - VPC_PEERING for global internal IP addresses used for
      
          private services access allocated ranges.
     - NAT_AUTO for the regional external IP addresses used by
          Cloud NAT when allocating addresses using
          
          automatic NAT IP address allocation.
     - IPSEC_INTERCONNECT for addresses created from a private
     IP range that are reserved for a VLAN attachment in an
     *HA VPN over Cloud Interconnect* configuration. These addresses
     are regional resources.
     - `SHARED_LOADBALANCER_VIP` for an internal IP address that is assigned
     to multiple internal forwarding rules.
     - `PRIVATE_SERVICE_CONNECT` for a private network address that is
     used to configure Private Service Connect. Only global internal addresses
     can use this purpose. |
| `status` | String | [Output Only] The status of the address, which can be one ofRESERVING, RESERVED, or IN_USE.
An address that is RESERVING is currently in the process of
being reserved. A RESERVED address is currently reserved and
available to use. An IN_USE address is currently being used
by another resource and is not available. |
| `region` | String | [Output Only] The URL of the region where a regional address resides.
For regional addresses, you must specify the region as a path parameter in
the HTTP request URL. *This field is not applicable to global
addresses.* |
| `ip_version` | String | The IP version that will be used by this address. Valid options areIPV4 or IPV6. |
| `name` | String | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?`. The first character
must be a lowercase letter, and all following characters (except for the
last character) must be a dash, lowercase letter, or digit. The last
character must be a lowercase letter or digit. |
| `prefix_length` | i64 | The prefix length if the resource represents an IP range. |
| `kind` | String | [Output Only] Type of the resource. Always compute#address for
addresses. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `address_type` | String | The type of address to reserve, either INTERNAL orEXTERNAL. If unspecified, defaults to EXTERNAL. |
| `network_tier` | String | This signifies the networking tier used for configuring this address and
can only take the following values: PREMIUM orSTANDARD. Internal IP addresses are always Premium Tier;
global external IP addresses are always Premium Tier; regional external IP
addresses can be either Standard or Premium Tier.

If this field is not specified, it is assumed to be PREMIUM. |
| `users` | Vec<String> | [Output Only] The URLs of the resources that are using this address. |
| `address` | String | The static IP address represented by this resource. |
| `network` | String | The URL of the network in which to reserve the address. This field can
only be used with INTERNAL type with theVPC_PEERING purpose. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create global_addresse
global_addresse = provider.compute_api.Global_addresse {
    project = "value"  # Project ID for this request.
}

# Access global_addresse outputs
global_addresse_id = global_addresse.id
global_addresse_ipv6_endpoint_type = global_addresse.ipv6_endpoint_type
global_addresse_description = global_addresse.description
global_addresse_labels = global_addresse.labels
global_addresse_self_link = global_addresse.self_link
global_addresse_subnetwork = global_addresse.subnetwork
global_addresse_id = global_addresse.id
global_addresse_label_fingerprint = global_addresse.label_fingerprint
global_addresse_purpose = global_addresse.purpose
global_addresse_status = global_addresse.status
global_addresse_region = global_addresse.region
global_addresse_ip_version = global_addresse.ip_version
global_addresse_name = global_addresse.name
global_addresse_prefix_length = global_addresse.prefix_length
global_addresse_kind = global_addresse.kind
global_addresse_creation_timestamp = global_addresse.creation_timestamp
global_addresse_address_type = global_addresse.address_type
global_addresse_network_tier = global_addresse.network_tier
global_addresse_users = global_addresse.users
global_addresse_address = global_addresse.address
global_addresse_network = global_addresse.network
```

---


### Preview_feature

Returns the details of the given PreviewFeature.

**Operations**: ✅ Read ✅ Update

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `activation_status` | String |  | Specifies whether the feature is enabled or disabled. |
| `status` | String |  | [Output only] Status of the feature. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `kind` | String |  | [Output only] The type of the feature. Always "compute#previewFeature" for
preview features. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `name` | String |  | Name of the feature. |
| `rollout_operation` | String |  | Rollout operation of the feature. |
| `description` | String |  | [Output Only] Description of the feature. |
| `preview_feature` | String | ✅ | Name of the PreviewFeature for this request. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `activation_status` | String | Specifies whether the feature is enabled or disabled. |
| `status` | String | [Output only] Status of the feature. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `kind` | String | [Output only] The type of the feature. Always "compute#previewFeature" for
preview features. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `name` | String | Name of the feature. |
| `rollout_operation` | String | Rollout operation of the feature. |
| `description` | String | [Output Only] Description of the feature. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Access preview_feature outputs
preview_feature_id = preview_feature.id
preview_feature_activation_status = preview_feature.activation_status
preview_feature_status = preview_feature.status
preview_feature_id = preview_feature.id
preview_feature_kind = preview_feature.kind
preview_feature_self_link = preview_feature.self_link
preview_feature_creation_timestamp = preview_feature.creation_timestamp
preview_feature_name = preview_feature.name
preview_feature_rollout_operation = preview_feature.rollout_operation
preview_feature_description = preview_feature.description
```

---


### Accelerator_type

Returns the specified accelerator type.

**Operations**: ✅ Read

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `zone` | String | [Output Only] The name of the zone where the accelerator type resides,
such as us-central1-a.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `deprecated` | String | [Output Only] The deprecation status associated with this accelerator type. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339 text format. |
| `name` | String | [Output Only] Name of the resource. |
| `kind` | String | [Output Only] The type of the resource. Alwayscompute#acceleratorType for accelerator types. |
| `self_link` | String | [Output Only] Server-defined, fully qualified URL for this resource. |
| `maximum_cards_per_instance` | i64 | [Output Only] Maximum number of accelerator cards allowed per instance. |
| `description` | String | [Output Only] An optional textual description of the resource. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Access accelerator_type outputs
accelerator_type_id = accelerator_type.id
accelerator_type_zone = accelerator_type.zone
accelerator_type_id = accelerator_type.id
accelerator_type_deprecated = accelerator_type.deprecated
accelerator_type_creation_timestamp = accelerator_type.creation_timestamp
accelerator_type_name = accelerator_type.name
accelerator_type_kind = accelerator_type.kind
accelerator_type_self_link = accelerator_type.self_link
accelerator_type_maximum_cards_per_instance = accelerator_type.maximum_cards_per_instance
accelerator_type_description = accelerator_type.description
```

---


### Region_target_http_proxie

Creates a TargetHttpProxy resource in the specified project and region
using the data included in the request.

**Operations**: ✅ Create ✅ Read ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `region` | String |  | [Output Only] URL of the region where the regional Target HTTP Proxy
resides. This field is not applicable to global Target HTTP Proxies. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `url_map` | String |  | URL to the UrlMap resource that defines the mapping from URL to
the BackendService. |
| `name` | String |  | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `http_keep_alive_timeout_sec` | i64 |  | Specifies how long to keep a connection open, after completing a response,
while there is no matching traffic (in seconds). If an HTTP keep-alive is
not specified, a default value (610 seconds) will be used.

For global external Application Load Balancers, the minimum allowed value
is 5 seconds and the maximum allowed value is 1200 seconds.

For classic Application Load Balancers, this option is not supported. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `kind` | String |  | [Output Only] Type of resource. Always compute#targetHttpProxy
for target HTTP proxies. |
| `fingerprint` | String |  | Fingerprint of this resource. A hash of the contents stored in this object.
This field is used in optimistic locking. This field will be ignored when
inserting a TargetHttpProxy. An up-to-date fingerprint must
be provided in order to patch/update the TargetHttpProxy; otherwise, the
request will fail with error 412 conditionNotMet. To see the
latest fingerprint, make a get() request to retrieve the
TargetHttpProxy. |
| `proxy_bind` | bool |  | This field only applies when the forwarding rule that references this
target proxy has a loadBalancingScheme set toINTERNAL_SELF_MANAGED.

When this field is set to true, Envoy proxies set up inbound
traffic interception and bind to the IP address and port specified in the
forwarding rule. This is generally useful when using Traffic Director to
configure Envoy as a gateway or middle proxy (in other words, not a
sidecar proxy). The Envoy proxy listens for inbound requests and handles
requests when it receives them.

The default is false. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `project` | String | ✅ | Project ID for this request. |
| `region` | String | ✅ | Name of the region scoping this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `region` | String | [Output Only] URL of the region where the regional Target HTTP Proxy
resides. This field is not applicable to global Target HTTP Proxies. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `url_map` | String | URL to the UrlMap resource that defines the mapping from URL to
the BackendService. |
| `name` | String | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `http_keep_alive_timeout_sec` | i64 | Specifies how long to keep a connection open, after completing a response,
while there is no matching traffic (in seconds). If an HTTP keep-alive is
not specified, a default value (610 seconds) will be used.

For global external Application Load Balancers, the minimum allowed value
is 5 seconds and the maximum allowed value is 1200 seconds.

For classic Application Load Balancers, this option is not supported. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `kind` | String | [Output Only] Type of resource. Always compute#targetHttpProxy
for target HTTP proxies. |
| `fingerprint` | String | Fingerprint of this resource. A hash of the contents stored in this object.
This field is used in optimistic locking. This field will be ignored when
inserting a TargetHttpProxy. An up-to-date fingerprint must
be provided in order to patch/update the TargetHttpProxy; otherwise, the
request will fail with error 412 conditionNotMet. To see the
latest fingerprint, make a get() request to retrieve the
TargetHttpProxy. |
| `proxy_bind` | bool | This field only applies when the forwarding rule that references this
target proxy has a loadBalancingScheme set toINTERNAL_SELF_MANAGED.

When this field is set to true, Envoy proxies set up inbound
traffic interception and bind to the IP address and port specified in the
forwarding rule. This is generally useful when using Traffic Director to
configure Envoy as a gateway or middle proxy (in other words, not a
sidecar proxy). The Envoy proxy listens for inbound requests and handles
requests when it receives them.

The default is false. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create region_target_http_proxie
region_target_http_proxie = provider.compute_api.Region_target_http_proxie {
    project = "value"  # Project ID for this request.
    region = "value"  # Name of the region scoping this request.
}

# Access region_target_http_proxie outputs
region_target_http_proxie_id = region_target_http_proxie.id
region_target_http_proxie_region = region_target_http_proxie.region
region_target_http_proxie_self_link = region_target_http_proxie.self_link
region_target_http_proxie_url_map = region_target_http_proxie.url_map
region_target_http_proxie_name = region_target_http_proxie.name
region_target_http_proxie_http_keep_alive_timeout_sec = region_target_http_proxie.http_keep_alive_timeout_sec
region_target_http_proxie_creation_timestamp = region_target_http_proxie.creation_timestamp
region_target_http_proxie_description = region_target_http_proxie.description
region_target_http_proxie_kind = region_target_http_proxie.kind
region_target_http_proxie_fingerprint = region_target_http_proxie.fingerprint
region_target_http_proxie_proxy_bind = region_target_http_proxie.proxy_bind
region_target_http_proxie_id = region_target_http_proxie.id
```

---


### Firewall_policie

Creates a new policy in the specified project using the data included in
the request.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `fingerprint` | String |  | Specifies a fingerprint for this resource, which is essentially a hash of
the metadata's contents and used for optimistic locking. The
fingerprint is initially generated by Compute Engine and changes after
every request to modify or update metadata. You must always provide an
up-to-date fingerprint hash in order to update or change metadata,
otherwise the request will fail with error412 conditionNotMet.

To see the latest fingerprint, make get() request to the
firewall policy. |
| `short_name` | String |  | User-provided name of the Organization firewall policy. The name should be
unique in the organization in which the firewall policy is created.
This field is not applicable to network firewall policies.
This name must be set on creation and cannot be changed. The name must be
1-63 characters long, and comply with RFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `policy_type` | String |  | The type of the firewall policy. This field can be eitherVPC_POLICY or RDMA_ROCE_POLICY.

Note: if not specified then VPC_POLICY will be used. |
| `packet_mirroring_rules` | Vec<String> |  | A list of packet mirroring rules that belong to this policy. |
| `self_link_with_id` | String |  | [Output Only] Server-defined URL for this resource with the resource id. |
| `rule_tuple_count` | i64 |  | [Output Only] Total count of all firewall policy rule tuples. A firewall
policy can not exceed a set number of tuples. |
| `region` | String |  | [Output Only] URL of the region where the regional firewall policy resides.
This field is not applicable to global firewall policies.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `kind` | String |  | [Output only] Type of the resource. Alwayscompute#firewallPolicyfor firewall policies |
| `parent` | String |  | [Output Only] The parent of the firewall policy.
This field is not applicable to network firewall policies. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `display_name` | String |  | Deprecated, please use short name instead. User-provided name of the
Organization firewall policy. The name should be unique in the organization
in which the firewall policy is created.
This field is not applicable to network firewall policies.
This name must be set on creation and cannot be changed.
The name must be 1-63 characters long, and comply
with RFC1035. Specifically, the name must be 1-63 characters
long and match the regular expression `[a-z]([-a-z0-9]*[a-z0-9])?` which
means the first character must be a lowercase letter, and all following
characters must be a dash, lowercase letter, or digit, except the last
character, which cannot be a dash. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `rules` | Vec<String> |  | A list of rules that belong to this policy.
There must always be a default rule (rule with priority 2147483647 and
match "*"). If no rules are provided when creating a firewall policy, a
default rule with action "allow" will be added. |
| `name` | String |  | Name of the resource. For Organization Firewall Policies it's a
[Output Only] numeric ID allocated by Google Cloud which uniquely
identifies the Organization Firewall Policy. |
| `associations` | Vec<String> |  | A list of associations that belong to this firewall policy. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `fingerprint` | String | Specifies a fingerprint for this resource, which is essentially a hash of
the metadata's contents and used for optimistic locking. The
fingerprint is initially generated by Compute Engine and changes after
every request to modify or update metadata. You must always provide an
up-to-date fingerprint hash in order to update or change metadata,
otherwise the request will fail with error412 conditionNotMet.

To see the latest fingerprint, make get() request to the
firewall policy. |
| `short_name` | String | User-provided name of the Organization firewall policy. The name should be
unique in the organization in which the firewall policy is created.
This field is not applicable to network firewall policies.
This name must be set on creation and cannot be changed. The name must be
1-63 characters long, and comply with RFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `policy_type` | String | The type of the firewall policy. This field can be eitherVPC_POLICY or RDMA_ROCE_POLICY.

Note: if not specified then VPC_POLICY will be used. |
| `packet_mirroring_rules` | Vec<String> | A list of packet mirroring rules that belong to this policy. |
| `self_link_with_id` | String | [Output Only] Server-defined URL for this resource with the resource id. |
| `rule_tuple_count` | i64 | [Output Only] Total count of all firewall policy rule tuples. A firewall
policy can not exceed a set number of tuples. |
| `region` | String | [Output Only] URL of the region where the regional firewall policy resides.
This field is not applicable to global firewall policies.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `kind` | String | [Output only] Type of the resource. Alwayscompute#firewallPolicyfor firewall policies |
| `parent` | String | [Output Only] The parent of the firewall policy.
This field is not applicable to network firewall policies. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `display_name` | String | Deprecated, please use short name instead. User-provided name of the
Organization firewall policy. The name should be unique in the organization
in which the firewall policy is created.
This field is not applicable to network firewall policies.
This name must be set on creation and cannot be changed.
The name must be 1-63 characters long, and comply
with RFC1035. Specifically, the name must be 1-63 characters
long and match the regular expression `[a-z]([-a-z0-9]*[a-z0-9])?` which
means the first character must be a lowercase letter, and all following
characters must be a dash, lowercase letter, or digit, except the last
character, which cannot be a dash. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `rules` | Vec<String> | A list of rules that belong to this policy.
There must always be a default rule (rule with priority 2147483647 and
match "*"). If no rules are provided when creating a firewall policy, a
default rule with action "allow" will be added. |
| `name` | String | Name of the resource. For Organization Firewall Policies it's a
[Output Only] numeric ID allocated by Google Cloud which uniquely
identifies the Organization Firewall Policy. |
| `associations` | Vec<String> | A list of associations that belong to this firewall policy. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create firewall_policie
firewall_policie = provider.compute_api.Firewall_policie {
}

# Access firewall_policie outputs
firewall_policie_id = firewall_policie.id
firewall_policie_fingerprint = firewall_policie.fingerprint
firewall_policie_short_name = firewall_policie.short_name
firewall_policie_policy_type = firewall_policie.policy_type
firewall_policie_packet_mirroring_rules = firewall_policie.packet_mirroring_rules
firewall_policie_self_link_with_id = firewall_policie.self_link_with_id
firewall_policie_rule_tuple_count = firewall_policie.rule_tuple_count
firewall_policie_region = firewall_policie.region
firewall_policie_kind = firewall_policie.kind
firewall_policie_parent = firewall_policie.parent
firewall_policie_creation_timestamp = firewall_policie.creation_timestamp
firewall_policie_display_name = firewall_policie.display_name
firewall_policie_description = firewall_policie.description
firewall_policie_id = firewall_policie.id
firewall_policie_rules = firewall_policie.rules
firewall_policie_name = firewall_policie.name
firewall_policie_associations = firewall_policie.associations
firewall_policie_self_link = firewall_policie.self_link
```

---


### Instance_group

Creates an instance group in the specified project using the
parameters that are included in the request.

**Operations**: ✅ Create ✅ Read ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `network` | String |  | [Output Only] The URL of the network to which all instances in the
instance group belong. If your instance has multiple network interfaces,
then the network and subnetwork fields only refer to the
network and subnet used by your primary interface (nic0). |
| `size` | i64 |  | [Output Only] The total number of instances in the instance group. |
| `zone` | String |  | [Output Only] The URL of thezone
where the instance group is located (for zonal resources). |
| `name` | String |  | The name of the instance group. The name must be 1-63 characters
long, and comply withRFC1035. |
| `kind` | String |  | [Output Only] The resource type, which is alwayscompute#instanceGroup for instance groups. |
| `creation_timestamp` | String |  | [Output Only] The creation timestamp for this instance group inRFC3339
text format. |
| `self_link` | String |  | [Output Only] The URL for this instance group. The server generates
this URL. |
| `subnetwork` | String |  | [Output Only] The URL of the subnetwork to which all instances in the
instance group belong. If your instance has multiple network interfaces,
then the network and subnetwork fields only refer to the
network and subnet used by your primary interface (nic0). |
| `region` | String |  | [Output Only] The URL of theregion
where the instance group is located (for regional resources). |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `named_ports` | Vec<String> |  | Optional. Assigns a name to a port number. For example:{name: "http", port: 80}

This
allows the system to reference ports by the assigned name instead of a
port number. Named ports can also contain multiple ports. For example:[{name: "app1", port: 8080}, {name:
"app1", port: 8081}, {name: "app2", port:
8082}]

Named ports apply to all instances in this instance group. |
| `fingerprint` | String |  | [Output Only] The fingerprint of the named ports. The system
uses this fingerprint to detect conflicts when multiple users change the
named ports concurrently. |
| `id` | String |  | [Output Only] A unique identifier for this instance group, generated
by the server. |
| `project` | String | ✅ | Project ID for this request. |
| `zone` | String | ✅ | The name of the zone
where you want to create the instance group. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `network` | String | [Output Only] The URL of the network to which all instances in the
instance group belong. If your instance has multiple network interfaces,
then the network and subnetwork fields only refer to the
network and subnet used by your primary interface (nic0). |
| `size` | i64 | [Output Only] The total number of instances in the instance group. |
| `zone` | String | [Output Only] The URL of thezone
where the instance group is located (for zonal resources). |
| `name` | String | The name of the instance group. The name must be 1-63 characters
long, and comply withRFC1035. |
| `kind` | String | [Output Only] The resource type, which is alwayscompute#instanceGroup for instance groups. |
| `creation_timestamp` | String | [Output Only] The creation timestamp for this instance group inRFC3339
text format. |
| `self_link` | String | [Output Only] The URL for this instance group. The server generates
this URL. |
| `subnetwork` | String | [Output Only] The URL of the subnetwork to which all instances in the
instance group belong. If your instance has multiple network interfaces,
then the network and subnetwork fields only refer to the
network and subnet used by your primary interface (nic0). |
| `region` | String | [Output Only] The URL of theregion
where the instance group is located (for regional resources). |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `named_ports` | Vec<String> | Optional. Assigns a name to a port number. For example:{name: "http", port: 80}

This
allows the system to reference ports by the assigned name instead of a
port number. Named ports can also contain multiple ports. For example:[{name: "app1", port: 8080}, {name:
"app1", port: 8081}, {name: "app2", port:
8082}]

Named ports apply to all instances in this instance group. |
| `fingerprint` | String | [Output Only] The fingerprint of the named ports. The system
uses this fingerprint to detect conflicts when multiple users change the
named ports concurrently. |
| `id` | String | [Output Only] A unique identifier for this instance group, generated
by the server. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create instance_group
instance_group = provider.compute_api.Instance_group {
    project = "value"  # Project ID for this request.
    zone = "value"  # The name of the zone
where you want to create the instance group.
}

# Access instance_group outputs
instance_group_id = instance_group.id
instance_group_network = instance_group.network
instance_group_size = instance_group.size
instance_group_zone = instance_group.zone
instance_group_name = instance_group.name
instance_group_kind = instance_group.kind
instance_group_creation_timestamp = instance_group.creation_timestamp
instance_group_self_link = instance_group.self_link
instance_group_subnetwork = instance_group.subnetwork
instance_group_region = instance_group.region
instance_group_description = instance_group.description
instance_group_named_ports = instance_group.named_ports
instance_group_fingerprint = instance_group.fingerprint
instance_group_id = instance_group.id
```

---


### Firewall

Creates a firewall rule in the specified project using the data
included in the request.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `source_ranges` | Vec<String> |  | If source ranges are specified, the firewall rule applies only to traffic
that has a source IP address in these ranges. These ranges must be
expressed inCIDR format. One or both of sourceRanges
and sourceTags may be set.
If both fields are set, the rule applies to traffic that has a
source IP address within sourceRanges OR a source IP
from a resource with a matching tag listed in thesourceTags field. The connection does not need to match
both fields for the rule to
apply. Both IPv4 and IPv6 are supported. |
| `target_tags` | Vec<String> |  | A list of tags that controls which instances the firewall rule
applies to. If targetTags are specified, then the firewall
rule applies only to instances in the VPC network that have one of those
tags. If no targetTags are specified, the firewall rule
applies to all instances on the specified network. |
| `disabled` | bool |  | Denotes whether the firewall rule is disabled. When set to true, the
firewall rule is not enforced and the network behaves as if it did not
exist. If this is unspecified, the firewall rule will be enabled. |
| `description` | String |  | An optional description of this resource. Provide this field when you
create the resource. |
| `destination_ranges` | Vec<String> |  | If destination ranges are specified, the firewall rule applies only to
traffic that has destination IP address in these ranges. These ranges must
be expressed inCIDR format. Both IPv4 and IPv6 are supported. |
| `name` | String |  | Name of the resource; provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?`. The first character
must be a lowercase letter, and all following characters (except for the
last character) must be a dash, lowercase letter, or digit. The last
character must be a lowercase letter or digit. |
| `target_service_accounts` | Vec<String> |  | A list of service accounts indicating sets of instances located in the
network that may make network connections as specified inallowed[].targetServiceAccounts cannot be used at the same time astargetTags or sourceTags.
If neither targetServiceAccounts nor targetTags
are specified, the firewall rule applies to all instances on the specified
network. |
| `source_service_accounts` | Vec<String> |  | If source service accounts are specified, the firewall rules apply only to
traffic originating from an instance with a service account in this list.
Source service accounts cannot be used to control traffic to an instance's
external IP address because service accounts are associated with an
instance, not an IP address.sourceRanges can be set at the same time assourceServiceAccounts.
If both are set, the firewall applies to traffic that
has a source IP address within the sourceRanges OR a source
IP that belongs to an instance with service account listed insourceServiceAccount. The connection does not need to match
both fields for the firewall to apply.sourceServiceAccounts cannot be used at the same time assourceTags or targetTags. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `denied` | Vec<String> |  | The list of DENY rules specified by this firewall. Each rule specifies a
protocol and port-range tuple that describes a denied connection. |
| `log_config` | String |  | This field denotes the logging options for a particular firewall rule. If
logging is enabled, logs will be exported to Cloud Logging. |
| `source_tags` | Vec<String> |  | If source tags are specified, the firewall rule applies only to traffic
with source IPs that match the primary network interfaces of VM instances
that have the tag and are in the same VPC network.
Source tags cannot be used to control traffic to an instance's external IP
address, it only applies to traffic between instances in the same virtual
network. Because tags are associated with instances, not IP addresses.
One or both of sourceRanges and sourceTags may be
set. If both fields are set, the firewall applies to traffic that has a
source IP address within sourceRanges OR a source IP from a
resource with a matching tag listed in the sourceTags
field. The connection does not need to match both fields for the
firewall to apply. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `params` | String |  | Input only. [Input Only] Additional params passed with the request, but not persisted
as part of resource payload. |
| `allowed` | Vec<String> |  | The list of ALLOW rules specified by this firewall. Each rule specifies a
protocol and port-range tuple that describes a permitted connection. |
| `network` | String |  | URL of the network resource for this firewall rule. If not
specified when creating a firewall rule, the default network
is used:

global/networks/default

If you choose to specify this field, you can specify the network as a full 
or partial URL. For example, the following are all valid URLs: 
   
   - 
   https://www.googleapis.com/compute/v1/projects/myproject/global/networks/my-network 
   - projects/myproject/global/networks/my-network 
   - global/networks/default |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `priority` | i64 |  | Priority for this rule.
This is an integer between `0` and `65535`, both inclusive.
The default value is `1000`.
Relative priorities determine which rule takes effect if multiple rules
apply. Lower values indicate higher priority. For example, a rule with
priority `0` has higher precedence than a rule with priority `1`.
DENY rules take precedence over ALLOW rules if they have equal priority.
Note that VPC networks have implied
rules with a priority of `65535`. To avoid conflicts with the implied
rules, use a priority number less than `65535`. |
| `direction` | String |  | Direction of traffic to which this firewall applies, either `INGRESS` or
`EGRESS`. The default is `INGRESS`. For `EGRESS` traffic, you cannot
specify the sourceTags fields. |
| `kind` | String |  | [Output Only] Type of the resource. Always compute#firewall
for firewall rules. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `source_ranges` | Vec<String> | If source ranges are specified, the firewall rule applies only to traffic
that has a source IP address in these ranges. These ranges must be
expressed inCIDR format. One or both of sourceRanges
and sourceTags may be set.
If both fields are set, the rule applies to traffic that has a
source IP address within sourceRanges OR a source IP
from a resource with a matching tag listed in thesourceTags field. The connection does not need to match
both fields for the rule to
apply. Both IPv4 and IPv6 are supported. |
| `target_tags` | Vec<String> | A list of tags that controls which instances the firewall rule
applies to. If targetTags are specified, then the firewall
rule applies only to instances in the VPC network that have one of those
tags. If no targetTags are specified, the firewall rule
applies to all instances on the specified network. |
| `disabled` | bool | Denotes whether the firewall rule is disabled. When set to true, the
firewall rule is not enforced and the network behaves as if it did not
exist. If this is unspecified, the firewall rule will be enabled. |
| `description` | String | An optional description of this resource. Provide this field when you
create the resource. |
| `destination_ranges` | Vec<String> | If destination ranges are specified, the firewall rule applies only to
traffic that has destination IP address in these ranges. These ranges must
be expressed inCIDR format. Both IPv4 and IPv6 are supported. |
| `name` | String | Name of the resource; provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?`. The first character
must be a lowercase letter, and all following characters (except for the
last character) must be a dash, lowercase letter, or digit. The last
character must be a lowercase letter or digit. |
| `target_service_accounts` | Vec<String> | A list of service accounts indicating sets of instances located in the
network that may make network connections as specified inallowed[].targetServiceAccounts cannot be used at the same time astargetTags or sourceTags.
If neither targetServiceAccounts nor targetTags
are specified, the firewall rule applies to all instances on the specified
network. |
| `source_service_accounts` | Vec<String> | If source service accounts are specified, the firewall rules apply only to
traffic originating from an instance with a service account in this list.
Source service accounts cannot be used to control traffic to an instance's
external IP address because service accounts are associated with an
instance, not an IP address.sourceRanges can be set at the same time assourceServiceAccounts.
If both are set, the firewall applies to traffic that
has a source IP address within the sourceRanges OR a source
IP that belongs to an instance with service account listed insourceServiceAccount. The connection does not need to match
both fields for the firewall to apply.sourceServiceAccounts cannot be used at the same time assourceTags or targetTags. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `denied` | Vec<String> | The list of DENY rules specified by this firewall. Each rule specifies a
protocol and port-range tuple that describes a denied connection. |
| `log_config` | String | This field denotes the logging options for a particular firewall rule. If
logging is enabled, logs will be exported to Cloud Logging. |
| `source_tags` | Vec<String> | If source tags are specified, the firewall rule applies only to traffic
with source IPs that match the primary network interfaces of VM instances
that have the tag and are in the same VPC network.
Source tags cannot be used to control traffic to an instance's external IP
address, it only applies to traffic between instances in the same virtual
network. Because tags are associated with instances, not IP addresses.
One or both of sourceRanges and sourceTags may be
set. If both fields are set, the firewall applies to traffic that has a
source IP address within sourceRanges OR a source IP from a
resource with a matching tag listed in the sourceTags
field. The connection does not need to match both fields for the
firewall to apply. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `params` | String | Input only. [Input Only] Additional params passed with the request, but not persisted
as part of resource payload. |
| `allowed` | Vec<String> | The list of ALLOW rules specified by this firewall. Each rule specifies a
protocol and port-range tuple that describes a permitted connection. |
| `network` | String | URL of the network resource for this firewall rule. If not
specified when creating a firewall rule, the default network
is used:

global/networks/default

If you choose to specify this field, you can specify the network as a full 
or partial URL. For example, the following are all valid URLs: 
   
   - 
   https://www.googleapis.com/compute/v1/projects/myproject/global/networks/my-network 
   - projects/myproject/global/networks/my-network 
   - global/networks/default |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `priority` | i64 | Priority for this rule.
This is an integer between `0` and `65535`, both inclusive.
The default value is `1000`.
Relative priorities determine which rule takes effect if multiple rules
apply. Lower values indicate higher priority. For example, a rule with
priority `0` has higher precedence than a rule with priority `1`.
DENY rules take precedence over ALLOW rules if they have equal priority.
Note that VPC networks have implied
rules with a priority of `65535`. To avoid conflicts with the implied
rules, use a priority number less than `65535`. |
| `direction` | String | Direction of traffic to which this firewall applies, either `INGRESS` or
`EGRESS`. The default is `INGRESS`. For `EGRESS` traffic, you cannot
specify the sourceTags fields. |
| `kind` | String | [Output Only] Type of the resource. Always compute#firewall
for firewall rules. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create firewall
firewall = provider.compute_api.Firewall {
    project = "value"  # Project ID for this request.
}

# Access firewall outputs
firewall_id = firewall.id
firewall_source_ranges = firewall.source_ranges
firewall_target_tags = firewall.target_tags
firewall_disabled = firewall.disabled
firewall_description = firewall.description
firewall_destination_ranges = firewall.destination_ranges
firewall_name = firewall.name
firewall_target_service_accounts = firewall.target_service_accounts
firewall_source_service_accounts = firewall.source_service_accounts
firewall_self_link = firewall.self_link
firewall_denied = firewall.denied
firewall_log_config = firewall.log_config
firewall_source_tags = firewall.source_tags
firewall_id = firewall.id
firewall_params = firewall.params
firewall_allowed = firewall.allowed
firewall_network = firewall.network
firewall_creation_timestamp = firewall.creation_timestamp
firewall_priority = firewall.priority
firewall_direction = firewall.direction
firewall_kind = firewall.kind
```

---


### Global_organization_operation

Retrieves the specified Operations resource. Gets a list of operations
by making a `list()` request.

**Operations**: ✅ Read ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `insert_time` | String | [Output Only] The time that this operation was requested.
This value is inRFC3339
text format. |
| `description` | String | [Output Only] A textual description of the operation, which is
set when the operation is created. |
| `region` | String | [Output Only] The URL of the region where the operation resides. Only
applicable when performing regional operations. |
| `end_time` | String | [Output Only] The time that this operation was completed. This value is inRFC3339
text format. |
| `http_error_message` | String | [Output Only] If the operation fails, this field contains the HTTP error
message that was returned, such as `NOT FOUND`. |
| `http_error_status_code` | i64 | [Output Only] If the operation fails, this field contains the HTTP error
status code that was returned. For example, a `404` means the
resource was not found. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `set_common_instance_metadata_operation_metadata` | String | [Output Only] If the operation is for projects.setCommonInstanceMetadata,
this field will contain information on all underlying zonal actions and
their state. |
| `start_time` | String | [Output Only] The time that this operation was started by the server.
This value is inRFC3339
text format. |
| `target_id` | String | [Output Only] The unique target ID, which identifies a specific incarnation
of the target resource. |
| `target_link` | String | [Output Only] The URL of the resource that the operation modifies. For
operations related to creating a snapshot, this points to the disk
that the snapshot was created from. |
| `operation_group_id` | String | [Output Only] An ID that represents a group of operations, such as when a
group of operations results from a `bulkInsert` API request. |
| `name` | String | [Output Only] Name of the operation. |
| `user` | String | [Output Only] User who requested the operation, for example:
`user@example.com` or
`alice_smith_identifier (global/workforcePools/example-com-us-employees)`. |
| `creation_timestamp` | String | [Deprecated] This field is deprecated. |
| `error` | String | [Output Only] If errors are generated during processing of the operation,
this field will be populated. |
| `client_operation_id` | String | [Output Only] The value of `requestId` if you provided it in the request.
Not present otherwise. |
| `status_message` | String | [Output Only] An optional textual description of the current status of the
operation. |
| `status` | String | [Output Only] The status of the operation, which can be one of the
following:
`PENDING`, `RUNNING`, or `DONE`. |
| `id` | String | [Output Only] The unique identifier for the operation. This identifier is
defined by the server. |
| `kind` | String | [Output Only] Type of the resource. Always `compute#operation` for
Operation resources. |
| `zone` | String | [Output Only] The URL of the zone where the operation resides. Only
applicable when performing per-zone operations. |
| `operation_type` | String | [Output Only] The type of operation, such as `insert`,
`update`, or `delete`, and so on. |
| `instances_bulk_insert_operation_metadata` | String |  |
| `progress` | i64 | [Output Only] An optional progress indicator that ranges from 0 to 100.
There is no requirement that this be linear or support any granularity of
operations. This should not be used to guess when the operation will be
complete. This number should monotonically increase as the operation
progresses. |
| `warnings` | Vec<String> | [Output Only] If warning messages are generated during processing of the
operation, this field will be populated. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Access global_organization_operation outputs
global_organization_operation_id = global_organization_operation.id
global_organization_operation_insert_time = global_organization_operation.insert_time
global_organization_operation_description = global_organization_operation.description
global_organization_operation_region = global_organization_operation.region
global_organization_operation_end_time = global_organization_operation.end_time
global_organization_operation_http_error_message = global_organization_operation.http_error_message
global_organization_operation_http_error_status_code = global_organization_operation.http_error_status_code
global_organization_operation_self_link = global_organization_operation.self_link
global_organization_operation_set_common_instance_metadata_operation_metadata = global_organization_operation.set_common_instance_metadata_operation_metadata
global_organization_operation_start_time = global_organization_operation.start_time
global_organization_operation_target_id = global_organization_operation.target_id
global_organization_operation_target_link = global_organization_operation.target_link
global_organization_operation_operation_group_id = global_organization_operation.operation_group_id
global_organization_operation_name = global_organization_operation.name
global_organization_operation_user = global_organization_operation.user
global_organization_operation_creation_timestamp = global_organization_operation.creation_timestamp
global_organization_operation_error = global_organization_operation.error
global_organization_operation_client_operation_id = global_organization_operation.client_operation_id
global_organization_operation_status_message = global_organization_operation.status_message
global_organization_operation_status = global_organization_operation.status
global_organization_operation_id = global_organization_operation.id
global_organization_operation_kind = global_organization_operation.kind
global_organization_operation_zone = global_organization_operation.zone
global_organization_operation_operation_type = global_organization_operation.operation_type
global_organization_operation_instances_bulk_insert_operation_metadata = global_organization_operation.instances_bulk_insert_operation_metadata
global_organization_operation_progress = global_organization_operation.progress
global_organization_operation_warnings = global_organization_operation.warnings
```

---


### Region_target_https_proxie

Creates a TargetHttpsProxy resource in the specified project and region
using the data included in the request.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `name` | String |  | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `ssl_certificates` | Vec<String> |  | URLs to SslCertificate resources that are used to authenticate
connections between users and the load balancer. At least one SSL
certificate must be specified. SslCertificates do not apply when the load
balancing scheme is set to INTERNAL_SELF_MANAGED.

The URLs should refer to a SSL Certificate resource or Certificate Manager
Certificate resource. Mixing Classic Certificates and Certificate Manager
Certificates is not allowed. Certificate Manager Certificates must include
the certificatemanager API namespace. Using Certificate Manager
Certificates in this field is not supported by Global external Application
Load Balancer or Classic Application Load Balancer, use certificate_map
instead.

Currently, you may specify up to 15 Classic SSL Certificates or up to 100
Certificate Manager Certificates.

Certificate Manager Certificates accepted formats are:
   
   - //certificatemanager.googleapis.com/projects/{project}/locations/{location}/certificates/{resourceName}.
   - https://certificatemanager.googleapis.com/v1alpha1/projects/{project}/locations/{location}/certificates/{resourceName}. |
| `region` | String |  | [Output Only] URL of the region where the regional TargetHttpsProxy
resides. This field is not applicable to global TargetHttpsProxies. |
| `http_keep_alive_timeout_sec` | i64 |  | Specifies how long to keep a connection open, after completing a response,
while there is no matching traffic (in seconds). If an HTTP keep-alive is
not specified, a default value (610 seconds) will be used.

For global external Application Load Balancers, the minimum allowed value
is 5 seconds and the maximum allowed value is 1200 seconds.

For classic Application Load Balancers, this option is not supported. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `server_tls_policy` | String |  | Optional. A URL referring to a networksecurity.ServerTlsPolicy resource
that describes how the proxy should authenticate inbound traffic.

 serverTlsPolicy only applies to a globalTargetHttpsProxy attached toglobalForwardingRules with theloadBalancingScheme set to INTERNAL_SELF_MANAGED
or EXTERNAL orEXTERNAL_MANAGED or INTERNAL_MANAGED.
It also applies to a regional TargetHttpsProxy attached to
regional forwardingRules with theloadBalancingScheme set to EXTERNAL_MANAGED orINTERNAL_MANAGED. For details whichServerTlsPolicy resources are accepted withINTERNAL_SELF_MANAGED and which with EXTERNAL,INTERNAL_MANAGED, EXTERNAL_MANAGEDloadBalancingScheme consult ServerTlsPolicy
documentation.

  If left blank, communications are not encrypted. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `certificate_map` | String |  | URL of a certificate map that identifies a certificate map associated with
the given target proxy.
This field can only be set for Global external Application Load Balancer or
Classic Application Load Balancer. For other products use Certificate
Manager Certificates instead.

If set, sslCertificates will be ignored.

 Accepted format is//certificatemanager.googleapis.com/projects/{project}/locations/{location}/certificateMaps/{resourceName}. |
| `proxy_bind` | bool |  | This field only applies when the forwarding rule that references this
target proxy has a loadBalancingScheme set toINTERNAL_SELF_MANAGED.

When this field is set to true, Envoy proxies set up inbound
traffic interception and bind to the IP address and port specified in the
forwarding rule. This is generally useful when using Traffic Director to
configure Envoy as a gateway or middle proxy (in other words, not a
sidecar proxy). The Envoy proxy listens for inbound requests and handles
requests when it receives them.

The default is false. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `quic_override` | String |  | Specifies the QUIC override policy for this TargetHttpsProxy resource. This
setting determines whether the load balancer attempts to negotiate QUIC
with clients.
You can specify NONE, ENABLE, orDISABLE.
   
   - When quic-override is set to NONE,
   Google manages whether QUIC is used.
   - When quic-override is set to ENABLE, the
   load balancer uses QUIC when possible.
   - When quic-override is set to DISABLE, the
   load balancer doesn't use QUIC.
   - If the quic-override flag is not specified,NONE is implied. |
| `url_map` | String |  | A fully-qualified or valid partial URL to the UrlMap resource that defines
the mapping from URL to the BackendService. For example, the following are
all valid URLs for specifying a URL map:
   
   - https://www.googleapis.compute/v1/projects/project/global/urlMaps/url-map 
   - projects/project/global/urlMaps/url-map 
   - global/urlMaps/url-map |
| `authorization_policy` | String |  | Optional. A URL referring to a networksecurity.AuthorizationPolicy resource
that describes how the proxy should authorize inbound traffic. If left
blank, access will not be restricted by an authorization policy.

 Refer to the AuthorizationPolicy resource for additional
details.

 authorizationPolicy only applies to a globalTargetHttpsProxy attached toglobalForwardingRules with theloadBalancingScheme set to INTERNAL_SELF_MANAGED.

 Note: This field currently has no impact. |
| `fingerprint` | String |  | Fingerprint of this resource. A hash of the contents stored in this object.
This field is used in optimistic locking. This field will be ignored when
inserting a TargetHttpsProxy. An up-to-date fingerprint must
be provided in order to patch the TargetHttpsProxy; otherwise, the request
will fail with error 412 conditionNotMet. To see the latest
fingerprint, make a get() request to retrieve the
TargetHttpsProxy. |
| `ssl_policy` | String |  | URL of SslPolicy resource that will be associated with the TargetHttpsProxy
resource. If not set, the TargetHttpsProxy resource has no
SSL policy configured. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `kind` | String |  | [Output Only] Type of resource. Alwayscompute#targetHttpsProxy for target HTTPS proxies. |
| `tls_early_data` | String |  | Specifies whether TLS 1.3 0-RTT Data ("Early Data") should be accepted
for this service. Early Data allows a TLS resumption handshake to include
the initial application payload (a HTTP request) alongside the handshake,
reducing the effective round trips to "zero". This applies to TLS 1.3
connections over TCP (HTTP/2) as well as over UDP (QUIC/h3).


This can improve application performance, especially on networks where
interruptions may be common, such as on mobile.


Requests with Early Data will have the "Early-Data" HTTP header set on
the request, with a value of "1", to allow the backend to determine whether
Early Data was included.


Note: TLS Early Data may allow requests to be replayed, as the data is
sent to the backend before the handshake has fully completed. Applications
that allow idempotent HTTP methods to make non-idempotent changes, such as
a GET request updating a database, should not accept Early Data on those
requests, and reject requests with the "Early-Data: 1" HTTP header by
returning a HTTP 425 (Too Early) status code, in order to remain RFC
compliant.


The default value is DISABLED. |
| `region` | String | ✅ | Name of the region scoping this request. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `name` | String | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `ssl_certificates` | Vec<String> | URLs to SslCertificate resources that are used to authenticate
connections between users and the load balancer. At least one SSL
certificate must be specified. SslCertificates do not apply when the load
balancing scheme is set to INTERNAL_SELF_MANAGED.

The URLs should refer to a SSL Certificate resource or Certificate Manager
Certificate resource. Mixing Classic Certificates and Certificate Manager
Certificates is not allowed. Certificate Manager Certificates must include
the certificatemanager API namespace. Using Certificate Manager
Certificates in this field is not supported by Global external Application
Load Balancer or Classic Application Load Balancer, use certificate_map
instead.

Currently, you may specify up to 15 Classic SSL Certificates or up to 100
Certificate Manager Certificates.

Certificate Manager Certificates accepted formats are:
   
   - //certificatemanager.googleapis.com/projects/{project}/locations/{location}/certificates/{resourceName}.
   - https://certificatemanager.googleapis.com/v1alpha1/projects/{project}/locations/{location}/certificates/{resourceName}. |
| `region` | String | [Output Only] URL of the region where the regional TargetHttpsProxy
resides. This field is not applicable to global TargetHttpsProxies. |
| `http_keep_alive_timeout_sec` | i64 | Specifies how long to keep a connection open, after completing a response,
while there is no matching traffic (in seconds). If an HTTP keep-alive is
not specified, a default value (610 seconds) will be used.

For global external Application Load Balancers, the minimum allowed value
is 5 seconds and the maximum allowed value is 1200 seconds.

For classic Application Load Balancers, this option is not supported. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `server_tls_policy` | String | Optional. A URL referring to a networksecurity.ServerTlsPolicy resource
that describes how the proxy should authenticate inbound traffic.

 serverTlsPolicy only applies to a globalTargetHttpsProxy attached toglobalForwardingRules with theloadBalancingScheme set to INTERNAL_SELF_MANAGED
or EXTERNAL orEXTERNAL_MANAGED or INTERNAL_MANAGED.
It also applies to a regional TargetHttpsProxy attached to
regional forwardingRules with theloadBalancingScheme set to EXTERNAL_MANAGED orINTERNAL_MANAGED. For details whichServerTlsPolicy resources are accepted withINTERNAL_SELF_MANAGED and which with EXTERNAL,INTERNAL_MANAGED, EXTERNAL_MANAGEDloadBalancingScheme consult ServerTlsPolicy
documentation.

  If left blank, communications are not encrypted. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `certificate_map` | String | URL of a certificate map that identifies a certificate map associated with
the given target proxy.
This field can only be set for Global external Application Load Balancer or
Classic Application Load Balancer. For other products use Certificate
Manager Certificates instead.

If set, sslCertificates will be ignored.

 Accepted format is//certificatemanager.googleapis.com/projects/{project}/locations/{location}/certificateMaps/{resourceName}. |
| `proxy_bind` | bool | This field only applies when the forwarding rule that references this
target proxy has a loadBalancingScheme set toINTERNAL_SELF_MANAGED.

When this field is set to true, Envoy proxies set up inbound
traffic interception and bind to the IP address and port specified in the
forwarding rule. This is generally useful when using Traffic Director to
configure Envoy as a gateway or middle proxy (in other words, not a
sidecar proxy). The Envoy proxy listens for inbound requests and handles
requests when it receives them.

The default is false. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `quic_override` | String | Specifies the QUIC override policy for this TargetHttpsProxy resource. This
setting determines whether the load balancer attempts to negotiate QUIC
with clients.
You can specify NONE, ENABLE, orDISABLE.
   
   - When quic-override is set to NONE,
   Google manages whether QUIC is used.
   - When quic-override is set to ENABLE, the
   load balancer uses QUIC when possible.
   - When quic-override is set to DISABLE, the
   load balancer doesn't use QUIC.
   - If the quic-override flag is not specified,NONE is implied. |
| `url_map` | String | A fully-qualified or valid partial URL to the UrlMap resource that defines
the mapping from URL to the BackendService. For example, the following are
all valid URLs for specifying a URL map:
   
   - https://www.googleapis.compute/v1/projects/project/global/urlMaps/url-map 
   - projects/project/global/urlMaps/url-map 
   - global/urlMaps/url-map |
| `authorization_policy` | String | Optional. A URL referring to a networksecurity.AuthorizationPolicy resource
that describes how the proxy should authorize inbound traffic. If left
blank, access will not be restricted by an authorization policy.

 Refer to the AuthorizationPolicy resource for additional
details.

 authorizationPolicy only applies to a globalTargetHttpsProxy attached toglobalForwardingRules with theloadBalancingScheme set to INTERNAL_SELF_MANAGED.

 Note: This field currently has no impact. |
| `fingerprint` | String | Fingerprint of this resource. A hash of the contents stored in this object.
This field is used in optimistic locking. This field will be ignored when
inserting a TargetHttpsProxy. An up-to-date fingerprint must
be provided in order to patch the TargetHttpsProxy; otherwise, the request
will fail with error 412 conditionNotMet. To see the latest
fingerprint, make a get() request to retrieve the
TargetHttpsProxy. |
| `ssl_policy` | String | URL of SslPolicy resource that will be associated with the TargetHttpsProxy
resource. If not set, the TargetHttpsProxy resource has no
SSL policy configured. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `kind` | String | [Output Only] Type of resource. Alwayscompute#targetHttpsProxy for target HTTPS proxies. |
| `tls_early_data` | String | Specifies whether TLS 1.3 0-RTT Data ("Early Data") should be accepted
for this service. Early Data allows a TLS resumption handshake to include
the initial application payload (a HTTP request) alongside the handshake,
reducing the effective round trips to "zero". This applies to TLS 1.3
connections over TCP (HTTP/2) as well as over UDP (QUIC/h3).


This can improve application performance, especially on networks where
interruptions may be common, such as on mobile.


Requests with Early Data will have the "Early-Data" HTTP header set on
the request, with a value of "1", to allow the backend to determine whether
Early Data was included.


Note: TLS Early Data may allow requests to be replayed, as the data is
sent to the backend before the handshake has fully completed. Applications
that allow idempotent HTTP methods to make non-idempotent changes, such as
a GET request updating a database, should not accept Early Data on those
requests, and reject requests with the "Early-Data: 1" HTTP header by
returning a HTTP 425 (Too Early) status code, in order to remain RFC
compliant.


The default value is DISABLED. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create region_target_https_proxie
region_target_https_proxie = provider.compute_api.Region_target_https_proxie {
    region = "value"  # Name of the region scoping this request.
    project = "value"  # Project ID for this request.
}

# Access region_target_https_proxie outputs
region_target_https_proxie_id = region_target_https_proxie.id
region_target_https_proxie_name = region_target_https_proxie.name
region_target_https_proxie_ssl_certificates = region_target_https_proxie.ssl_certificates
region_target_https_proxie_region = region_target_https_proxie.region
region_target_https_proxie_http_keep_alive_timeout_sec = region_target_https_proxie.http_keep_alive_timeout_sec
region_target_https_proxie_description = region_target_https_proxie.description
region_target_https_proxie_server_tls_policy = region_target_https_proxie.server_tls_policy
region_target_https_proxie_self_link = region_target_https_proxie.self_link
region_target_https_proxie_certificate_map = region_target_https_proxie.certificate_map
region_target_https_proxie_proxy_bind = region_target_https_proxie.proxy_bind
region_target_https_proxie_id = region_target_https_proxie.id
region_target_https_proxie_quic_override = region_target_https_proxie.quic_override
region_target_https_proxie_url_map = region_target_https_proxie.url_map
region_target_https_proxie_authorization_policy = region_target_https_proxie.authorization_policy
region_target_https_proxie_fingerprint = region_target_https_proxie.fingerprint
region_target_https_proxie_ssl_policy = region_target_https_proxie.ssl_policy
region_target_https_proxie_creation_timestamp = region_target_https_proxie.creation_timestamp
region_target_https_proxie_kind = region_target_https_proxie.kind
region_target_https_proxie_tls_early_data = region_target_https_proxie.tls_early_data
```

---


### Region_commitment

Creates a commitment in the specified project using the data
included in the request.

**Operations**: ✅ Create ✅ Read ✅ Update

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `category` | String |  | The category of the commitment; specifies whether the commitment is for
hardware or software resources. Category MACHINE specifies
that you are committing to hardware machine resources such asVCPU or MEMORY, listed in resources.
Category LICENSE specifies that you are committing to software
licenses, listed in licenseResources.
Note that if you specify MACHINE commitments, then you must
also specify a type to indicate the machine series of the
hardware resource that you are committing to. |
| `end_timestamp` | String |  | [Output Only] Commitment end time inRFC3339
text format. |
| `resource_status` | String |  | [Output Only] Status information for Commitment resource. |
| `status` | String |  | [Output Only] Status of the commitment with regards to eventual expiration
(each commitment has an end date defined). Status can be one of the
following values: NOT_YET_ACTIVE, ACTIVE, orEXPIRED. |
| `license_resource` | String |  | The license specification required as part of a license commitment. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `resources` | Vec<String> |  | The list of all the hardware resources, with their types and amounts, that
you want to commit to. Specify as a separate entry in the list for each
individual resource type. |
| `region` | String |  | [Output Only] URL of the region where the commitment and committed
resources are located. |
| `start_timestamp` | String |  | [Output Only] Commitment start time inRFC3339
text format. |
| `auto_renew` | bool |  | Specifies whether to automatically renew the commitment at the end of its
current term. The default value is false. If you set the field
to true, each time your commitment reaches the end of its
term, Compute Engine automatically renews it for another term. You can
update this field anytime before the commitment expires. For example, if
the commitment is set to expire at 12 AM UTC-8 on January 3, 2027, you can
update this field until 11:59 PM UTC-8 on January 2, 2027. |
| `custom_end_timestamp` | String |  | [Input Only] Optional, specifies the requested commitment end time inRFC3339 text format. Use this option when the desired
commitment's end date is later than the start date + term duration. |
| `description` | String |  | An optional description of the commitment. You can provide this property
when you create the resource. |
| `reservations` | Vec<String> |  | The list of new reservations that you want to create and attach to this
commitment.

You must attach reservations to your commitment if your commitment
specifies any GPUs or Local SSD disks. For more information, see 
Attach reservations to resource-based commitments.

Specify this property only if you want to create new
reservations to attach. To attach existing reservations, specify theexistingReservations property instead. |
| `split_source_commitment` | String |  | The source commitment from which you are transferring resources to create
the new split commitment. For more information, see 
Split commitments. |
| `type` | String |  | The type of commitment; specifies the
machine series for which you want to commit to purchasing resources.
The choice of machine series affects the discount rate and the eligible
resource types.

 The type must be one of the following:ACCELERATOR_OPTIMIZED, ACCELERATOR_OPTIMIZED_A3,ACCELERATOR_OPTIMIZED_A3_MEGA,COMPUTE_OPTIMIZED, COMPUTE_OPTIMIZED_C2D,
 COMPUTE_OPTIMIZED_C3, COMPUTE_OPTIMIZED_C3D,COMPUTE_OPTIMIZED_H3, GENERAL_PURPOSE,GENERAL_PURPOSE_C4, GENERAL_PURPOSE_E2,GENERAL_PURPOSE_N2, GENERAL_PURPOSE_N2D,GENERAL_PURPOSE_N4, GENERAL_PURPOSE_T2D,GRAPHICS_OPTIMIZED, MEMORY_OPTIMIZED,MEMORY_OPTIMIZED_M3, MEMORY_OPTIMIZED_X4,STORAGE_OPTIMIZED_Z3.
For example, type MEMORY_OPTIMIZED specifies a commitment
that applies only to eligible resources of memory optimized M1 and M2
machine series. Type GENERAL_PURPOSE specifies a commitment
that applies only to eligible resources of general purpose N1 machine
series. |
| `existing_reservations` | Vec<String> |  |  |
| `merge_source_commitments` | Vec<String> |  | The list of source commitments that you are merging to create the new
merged commitment. For more information, see
Merging commitments. |
| `plan` | String |  | The minimum time duration that you commit to purchasing resources.
The plan that you choose determines the preset term length of the
commitment (which is 1 year or 3 years) and affects the discount rate that
you receive for your resources. Committing to a longer time duration
typically gives you a higher discount rate. The supported values for this
field are TWELVE_MONTH (1 year), andTHIRTY_SIX_MONTH (3 years). |
| `kind` | String |  | [Output Only] Type of the resource. Always compute#commitment
for commitments. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `name` | String |  | Name of the commitment. You must specify a name when you purchase the
commitment. The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `status_message` | String |  | [Output Only] An optional, human-readable explanation of the status. |
| `region` | String | ✅ | Name of the region for this request. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `category` | String | The category of the commitment; specifies whether the commitment is for
hardware or software resources. Category MACHINE specifies
that you are committing to hardware machine resources such asVCPU or MEMORY, listed in resources.
Category LICENSE specifies that you are committing to software
licenses, listed in licenseResources.
Note that if you specify MACHINE commitments, then you must
also specify a type to indicate the machine series of the
hardware resource that you are committing to. |
| `end_timestamp` | String | [Output Only] Commitment end time inRFC3339
text format. |
| `resource_status` | String | [Output Only] Status information for Commitment resource. |
| `status` | String | [Output Only] Status of the commitment with regards to eventual expiration
(each commitment has an end date defined). Status can be one of the
following values: NOT_YET_ACTIVE, ACTIVE, orEXPIRED. |
| `license_resource` | String | The license specification required as part of a license commitment. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `resources` | Vec<String> | The list of all the hardware resources, with their types and amounts, that
you want to commit to. Specify as a separate entry in the list for each
individual resource type. |
| `region` | String | [Output Only] URL of the region where the commitment and committed
resources are located. |
| `start_timestamp` | String | [Output Only] Commitment start time inRFC3339
text format. |
| `auto_renew` | bool | Specifies whether to automatically renew the commitment at the end of its
current term. The default value is false. If you set the field
to true, each time your commitment reaches the end of its
term, Compute Engine automatically renews it for another term. You can
update this field anytime before the commitment expires. For example, if
the commitment is set to expire at 12 AM UTC-8 on January 3, 2027, you can
update this field until 11:59 PM UTC-8 on January 2, 2027. |
| `custom_end_timestamp` | String | [Input Only] Optional, specifies the requested commitment end time inRFC3339 text format. Use this option when the desired
commitment's end date is later than the start date + term duration. |
| `description` | String | An optional description of the commitment. You can provide this property
when you create the resource. |
| `reservations` | Vec<String> | The list of new reservations that you want to create and attach to this
commitment.

You must attach reservations to your commitment if your commitment
specifies any GPUs or Local SSD disks. For more information, see 
Attach reservations to resource-based commitments.

Specify this property only if you want to create new
reservations to attach. To attach existing reservations, specify theexistingReservations property instead. |
| `split_source_commitment` | String | The source commitment from which you are transferring resources to create
the new split commitment. For more information, see 
Split commitments. |
| `type` | String | The type of commitment; specifies the
machine series for which you want to commit to purchasing resources.
The choice of machine series affects the discount rate and the eligible
resource types.

 The type must be one of the following:ACCELERATOR_OPTIMIZED, ACCELERATOR_OPTIMIZED_A3,ACCELERATOR_OPTIMIZED_A3_MEGA,COMPUTE_OPTIMIZED, COMPUTE_OPTIMIZED_C2D,
 COMPUTE_OPTIMIZED_C3, COMPUTE_OPTIMIZED_C3D,COMPUTE_OPTIMIZED_H3, GENERAL_PURPOSE,GENERAL_PURPOSE_C4, GENERAL_PURPOSE_E2,GENERAL_PURPOSE_N2, GENERAL_PURPOSE_N2D,GENERAL_PURPOSE_N4, GENERAL_PURPOSE_T2D,GRAPHICS_OPTIMIZED, MEMORY_OPTIMIZED,MEMORY_OPTIMIZED_M3, MEMORY_OPTIMIZED_X4,STORAGE_OPTIMIZED_Z3.
For example, type MEMORY_OPTIMIZED specifies a commitment
that applies only to eligible resources of memory optimized M1 and M2
machine series. Type GENERAL_PURPOSE specifies a commitment
that applies only to eligible resources of general purpose N1 machine
series. |
| `existing_reservations` | Vec<String> |  |
| `merge_source_commitments` | Vec<String> | The list of source commitments that you are merging to create the new
merged commitment. For more information, see
Merging commitments. |
| `plan` | String | The minimum time duration that you commit to purchasing resources.
The plan that you choose determines the preset term length of the
commitment (which is 1 year or 3 years) and affects the discount rate that
you receive for your resources. Committing to a longer time duration
typically gives you a higher discount rate. The supported values for this
field are TWELVE_MONTH (1 year), andTHIRTY_SIX_MONTH (3 years). |
| `kind` | String | [Output Only] Type of the resource. Always compute#commitment
for commitments. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `name` | String | Name of the commitment. You must specify a name when you purchase the
commitment. The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `status_message` | String | [Output Only] An optional, human-readable explanation of the status. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create region_commitment
region_commitment = provider.compute_api.Region_commitment {
    region = "value"  # Name of the region for this request.
    project = "value"  # Project ID for this request.
}

# Access region_commitment outputs
region_commitment_id = region_commitment.id
region_commitment_category = region_commitment.category
region_commitment_end_timestamp = region_commitment.end_timestamp
region_commitment_resource_status = region_commitment.resource_status
region_commitment_status = region_commitment.status
region_commitment_license_resource = region_commitment.license_resource
region_commitment_creation_timestamp = region_commitment.creation_timestamp
region_commitment_resources = region_commitment.resources
region_commitment_region = region_commitment.region
region_commitment_start_timestamp = region_commitment.start_timestamp
region_commitment_auto_renew = region_commitment.auto_renew
region_commitment_custom_end_timestamp = region_commitment.custom_end_timestamp
region_commitment_description = region_commitment.description
region_commitment_reservations = region_commitment.reservations
region_commitment_split_source_commitment = region_commitment.split_source_commitment
region_commitment_type = region_commitment.type
region_commitment_existing_reservations = region_commitment.existing_reservations
region_commitment_merge_source_commitments = region_commitment.merge_source_commitments
region_commitment_plan = region_commitment.plan
region_commitment_kind = region_commitment.kind
region_commitment_self_link = region_commitment.self_link
region_commitment_id = region_commitment.id
region_commitment_name = region_commitment.name
region_commitment_status_message = region_commitment.status_message
```

---


### Route

Creates a Route resource in the specified project using the data included
in the request.

**Operations**: ✅ Create ✅ Read ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `params` | String |  | Input only. [Input Only] Additional params passed with the request, but not persisted
as part of resource payload. |
| `network` | String |  | Fully-qualified URL of the network that this route applies to. |
| `next_hop_hub` | String |  | [Output Only] The full resource name of the Network Connectivity Center hub
that will handle matching packets. |
| `next_hop_ip` | String |  | The network IP address of an instance that should handle matching packets.
Both IPv6 address and IPv4 addresses are supported.
Must specify an IPv4 address in dot-decimal notation (e.g. 192.0.2.99) or
an IPv6 address in RFC 4291 format (e.g. 2001:db8::2d9:51:0:0 or
2001:db8:0:0:2d9:51:0:0). IPv6 addresses will be displayed using RFC 5952
compressed format (e.g. 2001:db8::2d9:51:0:0). Should never be an
IPv4-mapped IPv6 address. |
| `next_hop_instance` | String |  | The URL to an instance that should handle matching packets. You can specify
this as a full or partial URL.
For example: 
https://www.googleapis.com/compute/v1/projects/project/zones/zone/instances/ |
| `name` | String |  | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?`. The first character must be a
lowercase letter, and all following characters (except for the last
character) must be a dash, lowercase letter, or digit. The last character
must be a lowercase letter or digit. |
| `next_hop_network` | String |  | The URL of the local network if it should handle matching packets. |
| `description` | String |  | An optional description of this resource. Provide this field when you
create the resource. |
| `route_status` | String |  | [Output only] The status of the route. This status only applies to
dynamic routes learned by Cloud Routers. This status is not applicable
to static routes. |
| `kind` | String |  | [Output Only] Type of this resource. Always compute#routes for
Route resources. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `warnings` | Vec<String> |  | [Output Only] If potential misconfigurations are detected for this
route, this field will be populated with warning messages. |
| `next_hop_origin` | String |  | [Output Only] Indicates the origin of the route. Can be IGP
(Interior Gateway Protocol), EGP (Exterior Gateway Protocol),
or INCOMPLETE. |
| `next_hop_gateway` | String |  | The URL to a gateway that should handle matching packets.
You can only specify the internet gateway using a full or
partial valid URL: projects/project/global/gateways/default-internet-gateway |
| `self_link` | String |  | [Output Only] Server-defined fully-qualified URL for this resource. |
| `tags` | Vec<String> |  | A list of instance tags to which this route applies. |
| `next_hop_med` | i64 |  | [Output Only] Multi-Exit Discriminator, a BGP route metric that indicates
the desirability of a particular route in a network. |
| `priority` | i64 |  | The priority of this route. Priority is used to break ties in cases
where there is more than one matching route of equal prefix length. In
cases where multiple routes have equal prefix length, the one with the
lowest-numbered priority value wins. The default value is `1000`. The
priority value must be from `0` to `65535`, inclusive. |
| `dest_range` | String |  | The destination range of outgoing packets that this route applies to. Both
IPv4 and IPv6 are supported.
Must specify an IPv4 range (e.g. 192.0.2.0/24) or an IPv6 range in RFC 4291
format (e.g. 2001:db8::/32). IPv6 range will be displayed using RFC 5952
compressed format. |
| `next_hop_ilb` | String |  | The URL to a forwarding rule of typeloadBalancingScheme=INTERNAL that should handle matching
packets or the IP address of the forwarding Rule.
For example, the following are all valid URLs:
   
   
      - https://www.googleapis.com/compute/v1/projects/project/regions/region/forwardingRules/forwardingRule 
   - regions/region/forwardingRules/forwardingRule


If an IP address is provided, must specify an IPv4 address in dot-decimal
notation or an IPv6 address in RFC 4291 format. For example, the following
are all valid IP addresses:
   
   
      - 10.128.0.56
      - 2001:db8::2d9:51:0:0
      - 2001:db8:0:0:2d9:51:0:0


IPv6 addresses will be displayed using RFC 5952 compressed format (e.g.
2001:db8::2d9:51:0:0). Should never be an IPv4-mapped IPv6 address. |
| `as_paths` | Vec<String> |  | [Output Only] AS path. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `next_hop_vpn_tunnel` | String |  | The URL to a VpnTunnel that should handle matching packets. |
| `route_type` | String |  | [Output Only] The type of this route, which can be one of the following
values:
- 'TRANSIT' for a transit route that this router learned from
another Cloud Router and will readvertise to one of its BGP peers 
- 'SUBNET' for a route from a subnet of the VPC 
- 'BGP' for a route learned from a BGP peer of this router 
- 'STATIC' for a static route |
| `next_hop_inter_region_cost` | i64 |  | [Output only] Internal fixed region-to-region cost that Google Cloud
calculates based on factors such as network performance, distance, and
available bandwidth between regions. |
| `next_hop_peering` | String |  | [Output Only] The network peering name that should handle matching packets,
which should conform to RFC1035. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `params` | String | Input only. [Input Only] Additional params passed with the request, but not persisted
as part of resource payload. |
| `network` | String | Fully-qualified URL of the network that this route applies to. |
| `next_hop_hub` | String | [Output Only] The full resource name of the Network Connectivity Center hub
that will handle matching packets. |
| `next_hop_ip` | String | The network IP address of an instance that should handle matching packets.
Both IPv6 address and IPv4 addresses are supported.
Must specify an IPv4 address in dot-decimal notation (e.g. 192.0.2.99) or
an IPv6 address in RFC 4291 format (e.g. 2001:db8::2d9:51:0:0 or
2001:db8:0:0:2d9:51:0:0). IPv6 addresses will be displayed using RFC 5952
compressed format (e.g. 2001:db8::2d9:51:0:0). Should never be an
IPv4-mapped IPv6 address. |
| `next_hop_instance` | String | The URL to an instance that should handle matching packets. You can specify
this as a full or partial URL.
For example: 
https://www.googleapis.com/compute/v1/projects/project/zones/zone/instances/ |
| `name` | String | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?`. The first character must be a
lowercase letter, and all following characters (except for the last
character) must be a dash, lowercase letter, or digit. The last character
must be a lowercase letter or digit. |
| `next_hop_network` | String | The URL of the local network if it should handle matching packets. |
| `description` | String | An optional description of this resource. Provide this field when you
create the resource. |
| `route_status` | String | [Output only] The status of the route. This status only applies to
dynamic routes learned by Cloud Routers. This status is not applicable
to static routes. |
| `kind` | String | [Output Only] Type of this resource. Always compute#routes for
Route resources. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `warnings` | Vec<String> | [Output Only] If potential misconfigurations are detected for this
route, this field will be populated with warning messages. |
| `next_hop_origin` | String | [Output Only] Indicates the origin of the route. Can be IGP
(Interior Gateway Protocol), EGP (Exterior Gateway Protocol),
or INCOMPLETE. |
| `next_hop_gateway` | String | The URL to a gateway that should handle matching packets.
You can only specify the internet gateway using a full or
partial valid URL: projects/project/global/gateways/default-internet-gateway |
| `self_link` | String | [Output Only] Server-defined fully-qualified URL for this resource. |
| `tags` | Vec<String> | A list of instance tags to which this route applies. |
| `next_hop_med` | i64 | [Output Only] Multi-Exit Discriminator, a BGP route metric that indicates
the desirability of a particular route in a network. |
| `priority` | i64 | The priority of this route. Priority is used to break ties in cases
where there is more than one matching route of equal prefix length. In
cases where multiple routes have equal prefix length, the one with the
lowest-numbered priority value wins. The default value is `1000`. The
priority value must be from `0` to `65535`, inclusive. |
| `dest_range` | String | The destination range of outgoing packets that this route applies to. Both
IPv4 and IPv6 are supported.
Must specify an IPv4 range (e.g. 192.0.2.0/24) or an IPv6 range in RFC 4291
format (e.g. 2001:db8::/32). IPv6 range will be displayed using RFC 5952
compressed format. |
| `next_hop_ilb` | String | The URL to a forwarding rule of typeloadBalancingScheme=INTERNAL that should handle matching
packets or the IP address of the forwarding Rule.
For example, the following are all valid URLs:
   
   
      - https://www.googleapis.com/compute/v1/projects/project/regions/region/forwardingRules/forwardingRule 
   - regions/region/forwardingRules/forwardingRule


If an IP address is provided, must specify an IPv4 address in dot-decimal
notation or an IPv6 address in RFC 4291 format. For example, the following
are all valid IP addresses:
   
   
      - 10.128.0.56
      - 2001:db8::2d9:51:0:0
      - 2001:db8:0:0:2d9:51:0:0


IPv6 addresses will be displayed using RFC 5952 compressed format (e.g.
2001:db8::2d9:51:0:0). Should never be an IPv4-mapped IPv6 address. |
| `as_paths` | Vec<String> | [Output Only] AS path. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `next_hop_vpn_tunnel` | String | The URL to a VpnTunnel that should handle matching packets. |
| `route_type` | String | [Output Only] The type of this route, which can be one of the following
values:
- 'TRANSIT' for a transit route that this router learned from
another Cloud Router and will readvertise to one of its BGP peers 
- 'SUBNET' for a route from a subnet of the VPC 
- 'BGP' for a route learned from a BGP peer of this router 
- 'STATIC' for a static route |
| `next_hop_inter_region_cost` | i64 | [Output only] Internal fixed region-to-region cost that Google Cloud
calculates based on factors such as network performance, distance, and
available bandwidth between regions. |
| `next_hop_peering` | String | [Output Only] The network peering name that should handle matching packets,
which should conform to RFC1035. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create route
route = provider.compute_api.Route {
    project = "value"  # Project ID for this request.
}

# Access route outputs
route_id = route.id
route_params = route.params
route_network = route.network
route_next_hop_hub = route.next_hop_hub
route_next_hop_ip = route.next_hop_ip
route_next_hop_instance = route.next_hop_instance
route_name = route.name
route_next_hop_network = route.next_hop_network
route_description = route.description
route_route_status = route.route_status
route_kind = route.kind
route_creation_timestamp = route.creation_timestamp
route_warnings = route.warnings
route_next_hop_origin = route.next_hop_origin
route_next_hop_gateway = route.next_hop_gateway
route_self_link = route.self_link
route_tags = route.tags
route_next_hop_med = route.next_hop_med
route_priority = route.priority
route_dest_range = route.dest_range
route_next_hop_ilb = route.next_hop_ilb
route_as_paths = route.as_paths
route_id = route.id
route_next_hop_vpn_tunnel = route.next_hop_vpn_tunnel
route_route_type = route.route_type
route_next_hop_inter_region_cost = route.next_hop_inter_region_cost
route_next_hop_peering = route.next_hop_peering
```

---


### Node_template

Creates a NodeTemplate resource in the specified project using the data
included in the request.

**Operations**: ✅ Create ✅ Read ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `name` | String |  | The name of the resource, provided by the client when initially creating
the resource. The resource name must be 1-63 characters long, and comply
withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must be
a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `node_affinity_labels` | HashMap<String, String> |  | Labels to use for node affinity, which will be used in instance scheduling. |
| `server_binding` | String |  | Sets the binding properties for the physical server. Valid values include:
   
   - *[Default]* RESTART_NODE_ON_ANY_SERVER:
   Restarts VMs on any available
   physical server
   - RESTART_NODE_ON_MINIMAL_SERVER: Restarts VMs on the same
   physical server whenever possible



See Sole-tenant
node options for more information. |
| `region` | String |  | [Output Only] The name of the region where the node template resides,
such as us-central1. |
| `node_type` | String |  | The node type to use for nodes group that are created from this template. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `cpu_overcommit_type` | String |  | CPU overcommit. |
| `status_message` | String |  | [Output Only] An optional, human-readable explanation of the status. |
| `disks` | Vec<String> |  |  |
| `node_type_flexibility` | String |  | Do not use. Instead, use the node_type property. |
| `accelerators` | Vec<String> |  |  |
| `status` | String |  | [Output Only] The status of the node template. One of the following values:CREATING, READY, and DELETING. |
| `kind` | String |  | [Output Only] The type of the resource. Alwayscompute#nodeTemplate for node templates. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `project` | String | ✅ | Project ID for this request. |
| `region` | String | ✅ | The name of the region for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `name` | String | The name of the resource, provided by the client when initially creating
the resource. The resource name must be 1-63 characters long, and comply
withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must be
a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `node_affinity_labels` | HashMap<String, String> | Labels to use for node affinity, which will be used in instance scheduling. |
| `server_binding` | String | Sets the binding properties for the physical server. Valid values include:
   
   - *[Default]* RESTART_NODE_ON_ANY_SERVER:
   Restarts VMs on any available
   physical server
   - RESTART_NODE_ON_MINIMAL_SERVER: Restarts VMs on the same
   physical server whenever possible



See Sole-tenant
node options for more information. |
| `region` | String | [Output Only] The name of the region where the node template resides,
such as us-central1. |
| `node_type` | String | The node type to use for nodes group that are created from this template. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `cpu_overcommit_type` | String | CPU overcommit. |
| `status_message` | String | [Output Only] An optional, human-readable explanation of the status. |
| `disks` | Vec<String> |  |
| `node_type_flexibility` | String | Do not use. Instead, use the node_type property. |
| `accelerators` | Vec<String> |  |
| `status` | String | [Output Only] The status of the node template. One of the following values:CREATING, READY, and DELETING. |
| `kind` | String | [Output Only] The type of the resource. Alwayscompute#nodeTemplate for node templates. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create node_template
node_template = provider.compute_api.Node_template {
    project = "value"  # Project ID for this request.
    region = "value"  # The name of the region for this request.
}

# Access node_template outputs
node_template_id = node_template.id
node_template_name = node_template.name
node_template_node_affinity_labels = node_template.node_affinity_labels
node_template_server_binding = node_template.server_binding
node_template_region = node_template.region
node_template_node_type = node_template.node_type
node_template_id = node_template.id
node_template_self_link = node_template.self_link
node_template_description = node_template.description
node_template_cpu_overcommit_type = node_template.cpu_overcommit_type
node_template_status_message = node_template.status_message
node_template_disks = node_template.disks
node_template_node_type_flexibility = node_template.node_type_flexibility
node_template_accelerators = node_template.accelerators
node_template_status = node_template.status
node_template_kind = node_template.kind
node_template_creation_timestamp = node_template.creation_timestamp
```

---


### Interconnect_remote_location

Returns the details for the specified interconnect remote location. Gets a
list of available interconnect remote locations by making alist() request.

**Operations**: ✅ Read

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `name` | String | [Output Only] Name of the resource. |
| `lacp` | String | [Output Only] Link Aggregation Control Protocol (LACP) constraints, which
can take one of the following values: LACP_SUPPORTED, LACP_UNSUPPORTED |
| `facility_provider` | String | [Output Only] The name of the provider for this facility (e.g., EQUINIX). |
| `constraints` | String | [Output Only] Constraints on the parameters for creating Cross-Cloud
Interconnect and associated InterconnectAttachments. |
| `address` | String | [Output Only] The postal address of the Point of Presence, each line in
the address is separated by a newline character. |
| `max_lag_size100_gbps` | i64 | [Output Only]
The maximum number of 100 Gbps ports supported in a link aggregation group
(LAG). When linkType is 100 Gbps, requestedLinkCount cannot exceed
max_lag_size_100_gbps. |
| `max_lag_size10_gbps` | i64 | [Output Only]
The maximum number of 10 Gbps ports supported in a link aggregation group
(LAG). When linkType is 10 Gbps, requestedLinkCount cannot exceed
max_lag_size_10_gbps. |
| `description` | String | [Output Only] An optional description of the resource. |
| `continent` | String | [Output Only] Continent for this location, which can take one of the
following values:
   
   - AFRICA
   - ASIA_PAC
   - EUROPE
   - NORTH_AMERICA
   - SOUTH_AMERICA |
| `peeringdb_facility_id` | String | [Output Only] The peeringdb identifier for this facility (corresponding
with a netfac type in peeringdb). |
| `max_lag_size400_gbps` | i64 | [Output Only]
The maximum number of 400 Gbps ports supported in a link aggregation group
(LAG). When linkType is 400 Gbps, requestedLinkCount cannot exceed
max_lag_size_400_gbps. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `facility_provider_facility_id` | String | [Output Only] A provider-assigned Identifier for this facility (e.g.,
Ashburn-DC1). |
| `city` | String | [Output Only] Metropolitan area designator that indicates which city an
interconnect is located.
For example: "Chicago, IL", "Amsterdam, Netherlands". |
| `kind` | String | [Output Only] Type of the resource. Alwayscompute#interconnectRemoteLocation for interconnect remote
locations. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `status` | String | [Output Only] The status of this InterconnectRemoteLocation, which can take
one of the following values:
   
   - CLOSED: The InterconnectRemoteLocation is closed and is unavailable
   for provisioning new Cross-Cloud Interconnects. 
   - AVAILABLE: The
   InterconnectRemoteLocation is available for provisioning new
   Cross-Cloud Interconnects. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `remote_service` | String | [Output Only] Indicates the service provider present at the remote
location. Example values: "Amazon Web Services", "Microsoft Azure". |
| `attachment_configuration_constraints` | String | [Output Only] Subset of fields from InterconnectAttachment's
|configurationConstraints| field that apply to all attachments for this
remote location. |
| `permitted_connections` | Vec<String> | [Output Only] Permitted connections. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Access interconnect_remote_location outputs
interconnect_remote_location_id = interconnect_remote_location.id
interconnect_remote_location_name = interconnect_remote_location.name
interconnect_remote_location_lacp = interconnect_remote_location.lacp
interconnect_remote_location_facility_provider = interconnect_remote_location.facility_provider
interconnect_remote_location_constraints = interconnect_remote_location.constraints
interconnect_remote_location_address = interconnect_remote_location.address
interconnect_remote_location_max_lag_size100_gbps = interconnect_remote_location.max_lag_size100_gbps
interconnect_remote_location_max_lag_size10_gbps = interconnect_remote_location.max_lag_size10_gbps
interconnect_remote_location_description = interconnect_remote_location.description
interconnect_remote_location_continent = interconnect_remote_location.continent
interconnect_remote_location_peeringdb_facility_id = interconnect_remote_location.peeringdb_facility_id
interconnect_remote_location_max_lag_size400_gbps = interconnect_remote_location.max_lag_size400_gbps
interconnect_remote_location_creation_timestamp = interconnect_remote_location.creation_timestamp
interconnect_remote_location_facility_provider_facility_id = interconnect_remote_location.facility_provider_facility_id
interconnect_remote_location_city = interconnect_remote_location.city
interconnect_remote_location_kind = interconnect_remote_location.kind
interconnect_remote_location_id = interconnect_remote_location.id
interconnect_remote_location_status = interconnect_remote_location.status
interconnect_remote_location_self_link = interconnect_remote_location.self_link
interconnect_remote_location_remote_service = interconnect_remote_location.remote_service
interconnect_remote_location_attachment_configuration_constraints = interconnect_remote_location.attachment_configuration_constraints
interconnect_remote_location_permitted_connections = interconnect_remote_location.permitted_connections
```

---


### Region_disk

Creates a persistent regional disk in the specified project using the data
included in the request.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `size_gb` | String |  | Size, in GB, of the persistent disk. You can specify
this field when creating a persistent disk using thesourceImage, sourceSnapshot, orsourceDisk parameter, or specify it alone to create an empty
persistent disk.

If you specify this field along with a source, the value ofsizeGb must not be less than the size of the
source.
Acceptable values are greater than 0. |
| `status` | String |  | [Output Only] The status of disk creation.
   
   
     - CREATING: Disk is provisioning.
     - RESTORING: Source data is being copied into the
     disk.
     - FAILED: Disk creation failed.
     - READY: Disk is ready for use.
     - DELETING: Disk is deleting. |
| `type` | String |  | URL of the disk type resource describing which disk type to use to create
the disk. Provide this when creating the disk. For example:projects/project/zones/zone/diskTypes/pd-ssd. See Persistent disk
types. |
| `async_secondary_disks` | HashMap<String, String> |  | [Output Only] A list of disks this disk is asynchronously replicated to. |
| `last_detach_timestamp` | String |  | [Output Only] Last detach timestamp inRFC3339
text format. |
| `region` | String |  | [Output Only] URL of the region where the disk resides. Only applicable for
regional resources.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `licenses` | Vec<String> |  | A list of publicly visible licenses. Reserved for Google's use. |
| `last_attach_timestamp` | String |  | [Output Only] Last attach timestamp inRFC3339
text format. |
| `guest_os_features` | Vec<String> |  | A list of features to enable on the guest operating system. Applicable
only for bootable images. Read
Enabling guest operating system features to see a list of available
options. |
| `source_consistency_group_policy_id` | String |  | [Output Only] ID of the DiskConsistencyGroupPolicy for a secondary disk
that was created using a consistency group. |
| `zone` | String |  | [Output Only] URL of the zone where the disk resides.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `provisioned_iops` | String |  | Indicates how many IOPS to provision for the disk. This sets the number
of I/O operations per second that the disk can handle. Values must be
between 10,000 and 120,000. For more details, see theExtreme persistent
disk documentation. |
| `source_image_encryption_key` | String |  | Thecustomer-supplied
encryption key of the source image. Required if the source image is
protected by a customer-supplied encryption key. |
| `access_mode` | String |  | The access mode of the disk.
   
   
     - READ_WRITE_SINGLE: The default AccessMode, means the
     disk can be attached to single instance in RW mode.
     - READ_WRITE_MANY: The AccessMode means the disk can be
     attached to multiple instances in RW mode.
     - READ_ONLY_MANY: The AccessMode means the disk can be
     attached to multiple instances in RO mode.


The AccessMode is only valid for Hyperdisk disk types. |
| `async_primary_disk` | String |  | Disk asynchronously replicated into this disk. |
| `source_image` | String |  | The source image used to create this disk. If the source image is
deleted, this field will not be set.

To create a disk with one of the public operating system images, specify
the image by its family name. For example, specifyfamily/debian-9 to use the latest Debian 9 image:

projects/debian-cloud/global/images/family/debian-9


Alternatively, use a specific version of a public operating system image:

projects/debian-cloud/global/images/debian-9-stretch-vYYYYMMDD


To create a disk with a custom image that you created, specify the
image name in the following format:

global/images/my-custom-image


You can also specify a custom image by its image family, which returns
the latest version of the image in that family. Replace the image name
with family/family-name:

global/images/family/my-image-family |
| `source_snapshot_id` | String |  | [Output Only] The unique ID of the snapshot used to create this disk. This
value identifies the exact snapshot that was used to create this persistent
disk. For example, if you created the persistent disk from a snapshot that
was later deleted and recreated under the same name, the source snapshot ID
would identify the exact version of the snapshot that was used. |
| `name` | String |  | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?`
which means the first character must be a lowercase letter, and all
following characters must be a dash, lowercase letter, or digit, except
the last character, which cannot be a dash. |
| `options` | String |  | Internal use only. |
| `source_disk_id` | String |  | [Output Only] The unique ID of the disk used to create this disk. This
value identifies the exact disk that was used to create this persistent
disk. For example, if you created the persistent disk from a disk that
was later deleted and recreated under the same name, the source disk ID
would identify the exact version of the disk that was used. |
| `labels` | HashMap<String, String> |  | Labels to apply to this disk. These can be later modified by
the setLabels method. |
| `license_codes` | Vec<String> |  | Integer license codes indicating which licenses are attached to this disk. |
| `disk_encryption_key` | String |  | Encrypts the disk using a
customer-supplied encryption key or a 
customer-managed encryption key.

Encryption keys do not protect access to metadata of the disk.

After you encrypt a disk with a customer-supplied key, you must provide the
same key if you use the disk later. For example, to create a disk snapshot,
to create a disk image, to create a machine image, or to attach the disk to
a virtual machine.

After you encrypt a disk with a customer-managed key, thediskEncryptionKey.kmsKeyName is set to a key *version*
name once the disk is created. The disk is encrypted with this version of
the key. In the response, diskEncryptionKey.kmsKeyName appears
in the following format:

"diskEncryptionKey.kmsKeyName":
"projects/kms_project_id/locations/region/keyRings/
key_region/cryptoKeys/key
/cryptoKeysVersions/version

If you do not provide an encryption key when creating the disk, then the 
disk is encrypted using an automatically generated key and you don't need 
to provide a key to use the disk later. |
| `enable_confidential_compute` | bool |  | Whether this disk is using confidential compute mode. |
| `source_instant_snapshot` | String |  | The source instant snapshot used to create this disk. You can provide
this as a partial or full URL to the resource. For example, the following
are valid values:
   
   
     - https://www.googleapis.com/compute/v1/projects/project/zones/zone/instantSnapshots/instantSnapshot
     - projects/project/zones/zone/instantSnapshots/instantSnapshot 
   - zones/zone/instantSnapshots/instantSnapshot |
| `source_snapshot` | String |  | The source snapshot used to create this disk. You can provide this as a
partial or full URL to the resource. For example, the following are valid
values:
   
   
     - https://www.googleapis.com/compute/v1/projects/project/global/snapshots/snapshot 
   - projects/project/global/snapshots/snapshot
     - global/snapshots/snapshot |
| `source_disk` | String |  | The source disk used to create this disk. You can provide this as a
partial or full URL to the resource. For example, the following are valid
values:
   
   
     - 
       https://www.googleapis.com/compute/v1/projects/project/zones/zone/disks/disk
     
     - 
       https://www.googleapis.com/compute/v1/projects/project/regions/region/disks/disk
     
     - 
       projects/project/zones/zone/disks/disk
     
     - 
       projects/project/regions/region/disks/disk
     
     - 
       zones/zone/disks/disk
     
     - 
       regions/region/disks/disk |
| `source_storage_object` | String |  | The full Google Cloud Storage URI where the disk image is stored. This file
must be a gzip-compressed tarball whose name ends in .tar.gz or virtual
machine disk whose name ends in vmdk. Valid URIs may start with gs:// or
https://storage.googleapis.com/. This flag is not optimized for creating
multiple disks from a source storage object. To create many disks from a
source storage object, use gcloud compute images
import instead. |
| `physical_block_size_bytes` | String |  | Physical block size of the persistent disk, in bytes.
If not present in a request, a default value is used.
The currently supported size is 4096, other sizes may be added in
the future.
If an unsupported value is requested, the error message will list
the supported values for the caller's project. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `source_consistency_group_policy` | String |  | [Output Only] URL of the DiskConsistencyGroupPolicy for a secondary disk
that was created using a consistency group. |
| `self_link` | String |  | [Output Only] Server-defined fully-qualified URL for this resource. |
| `source_image_id` | String |  | [Output Only] The ID value of the image used to create this disk. This
value identifies the exact image that was used to create this persistent
disk. For example, if you created the persistent disk from an image that
was later deleted and recreated under the same name, the source image ID
would identify the exact version of the image that was used. |
| `location_hint` | String |  | An opaque location hint used to place the disk close to other resources.
This field is for use by internal tools that use the public API. |
| `label_fingerprint` | String |  | A fingerprint for the labels being applied to this disk, which is
essentially a hash of the labels set used for optimistic locking. The
fingerprint is initially generated by Compute Engine and changes after
every request to modify or update labels. You must always provide an
up-to-date fingerprint hash in order to update or change labels,
otherwise the request will fail with error412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve a disk. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `params` | String |  | Input only. [Input Only] Additional params passed with the request, but not persisted
as part of resource payload. |
| `provisioned_throughput` | String |  | Indicates how much throughput to provision for the disk. This sets the
number of throughput mb per second that the disk can handle. Values must be
greater than or equal to 1. |
| `resource_policies` | Vec<String> |  | Resource policies applied to this disk for automatic snapshot creations. |
| `replica_zones` | Vec<String> |  | URLs of the zones where the disk should be replicated to. Only applicable
for regional resources. |
| `storage_pool` | String |  | The storage pool in which the new disk is created. You can provide
this as a partial or full URL to the resource. For example, the following
are valid values:
   
   
     - https://www.googleapis.com/compute/v1/projects/project/zones/zone/storagePools/storagePool
     - projects/project/zones/zone/storagePools/storagePool 
   - zones/zone/storagePools/storagePool |
| `source_instant_snapshot_id` | String |  | [Output Only] The unique ID of the instant snapshot used to create this
disk. This value identifies the exact instant snapshot that was used to
create this persistent disk. For example, if you created the persistent
disk from an instant snapshot that was later deleted and recreated under
the same name, the source instant snapshot ID would identify the exact
version of the instant snapshot that was used. |
| `users` | Vec<String> |  | [Output Only] Links to the users of the disk (attached instances)
in form:projects/project/zones/zone/instances/instance |
| `kind` | String |  | [Output Only] Type of the resource. Always compute#disk for
disks. |
| `satisfies_pzs` | bool |  | [Output Only] Reserved for future use. |
| `source_snapshot_encryption_key` | String |  | Thecustomer-supplied
encryption key of the source snapshot. Required if the source snapshot
is protected by a customer-supplied encryption key. |
| `resource_status` | String |  | [Output Only] Status information for the disk resource. |
| `satisfies_pzi` | bool |  | Output only. Reserved for future use. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `architecture` | String |  | The architecture of the disk. Valid values are
ARM64 or X86_64. |
| `project` | String | ✅ | Project ID for this request. |
| `region` | String | ✅ | Name of the region for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `size_gb` | String | Size, in GB, of the persistent disk. You can specify
this field when creating a persistent disk using thesourceImage, sourceSnapshot, orsourceDisk parameter, or specify it alone to create an empty
persistent disk.

If you specify this field along with a source, the value ofsizeGb must not be less than the size of the
source.
Acceptable values are greater than 0. |
| `status` | String | [Output Only] The status of disk creation.
   
   
     - CREATING: Disk is provisioning.
     - RESTORING: Source data is being copied into the
     disk.
     - FAILED: Disk creation failed.
     - READY: Disk is ready for use.
     - DELETING: Disk is deleting. |
| `type` | String | URL of the disk type resource describing which disk type to use to create
the disk. Provide this when creating the disk. For example:projects/project/zones/zone/diskTypes/pd-ssd. See Persistent disk
types. |
| `async_secondary_disks` | HashMap<String, String> | [Output Only] A list of disks this disk is asynchronously replicated to. |
| `last_detach_timestamp` | String | [Output Only] Last detach timestamp inRFC3339
text format. |
| `region` | String | [Output Only] URL of the region where the disk resides. Only applicable for
regional resources.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `licenses` | Vec<String> | A list of publicly visible licenses. Reserved for Google's use. |
| `last_attach_timestamp` | String | [Output Only] Last attach timestamp inRFC3339
text format. |
| `guest_os_features` | Vec<String> | A list of features to enable on the guest operating system. Applicable
only for bootable images. Read
Enabling guest operating system features to see a list of available
options. |
| `source_consistency_group_policy_id` | String | [Output Only] ID of the DiskConsistencyGroupPolicy for a secondary disk
that was created using a consistency group. |
| `zone` | String | [Output Only] URL of the zone where the disk resides.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `provisioned_iops` | String | Indicates how many IOPS to provision for the disk. This sets the number
of I/O operations per second that the disk can handle. Values must be
between 10,000 and 120,000. For more details, see theExtreme persistent
disk documentation. |
| `source_image_encryption_key` | String | Thecustomer-supplied
encryption key of the source image. Required if the source image is
protected by a customer-supplied encryption key. |
| `access_mode` | String | The access mode of the disk.
   
   
     - READ_WRITE_SINGLE: The default AccessMode, means the
     disk can be attached to single instance in RW mode.
     - READ_WRITE_MANY: The AccessMode means the disk can be
     attached to multiple instances in RW mode.
     - READ_ONLY_MANY: The AccessMode means the disk can be
     attached to multiple instances in RO mode.


The AccessMode is only valid for Hyperdisk disk types. |
| `async_primary_disk` | String | Disk asynchronously replicated into this disk. |
| `source_image` | String | The source image used to create this disk. If the source image is
deleted, this field will not be set.

To create a disk with one of the public operating system images, specify
the image by its family name. For example, specifyfamily/debian-9 to use the latest Debian 9 image:

projects/debian-cloud/global/images/family/debian-9


Alternatively, use a specific version of a public operating system image:

projects/debian-cloud/global/images/debian-9-stretch-vYYYYMMDD


To create a disk with a custom image that you created, specify the
image name in the following format:

global/images/my-custom-image


You can also specify a custom image by its image family, which returns
the latest version of the image in that family. Replace the image name
with family/family-name:

global/images/family/my-image-family |
| `source_snapshot_id` | String | [Output Only] The unique ID of the snapshot used to create this disk. This
value identifies the exact snapshot that was used to create this persistent
disk. For example, if you created the persistent disk from a snapshot that
was later deleted and recreated under the same name, the source snapshot ID
would identify the exact version of the snapshot that was used. |
| `name` | String | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?`
which means the first character must be a lowercase letter, and all
following characters must be a dash, lowercase letter, or digit, except
the last character, which cannot be a dash. |
| `options` | String | Internal use only. |
| `source_disk_id` | String | [Output Only] The unique ID of the disk used to create this disk. This
value identifies the exact disk that was used to create this persistent
disk. For example, if you created the persistent disk from a disk that
was later deleted and recreated under the same name, the source disk ID
would identify the exact version of the disk that was used. |
| `labels` | HashMap<String, String> | Labels to apply to this disk. These can be later modified by
the setLabels method. |
| `license_codes` | Vec<String> | Integer license codes indicating which licenses are attached to this disk. |
| `disk_encryption_key` | String | Encrypts the disk using a
customer-supplied encryption key or a 
customer-managed encryption key.

Encryption keys do not protect access to metadata of the disk.

After you encrypt a disk with a customer-supplied key, you must provide the
same key if you use the disk later. For example, to create a disk snapshot,
to create a disk image, to create a machine image, or to attach the disk to
a virtual machine.

After you encrypt a disk with a customer-managed key, thediskEncryptionKey.kmsKeyName is set to a key *version*
name once the disk is created. The disk is encrypted with this version of
the key. In the response, diskEncryptionKey.kmsKeyName appears
in the following format:

"diskEncryptionKey.kmsKeyName":
"projects/kms_project_id/locations/region/keyRings/
key_region/cryptoKeys/key
/cryptoKeysVersions/version

If you do not provide an encryption key when creating the disk, then the 
disk is encrypted using an automatically generated key and you don't need 
to provide a key to use the disk later. |
| `enable_confidential_compute` | bool | Whether this disk is using confidential compute mode. |
| `source_instant_snapshot` | String | The source instant snapshot used to create this disk. You can provide
this as a partial or full URL to the resource. For example, the following
are valid values:
   
   
     - https://www.googleapis.com/compute/v1/projects/project/zones/zone/instantSnapshots/instantSnapshot
     - projects/project/zones/zone/instantSnapshots/instantSnapshot 
   - zones/zone/instantSnapshots/instantSnapshot |
| `source_snapshot` | String | The source snapshot used to create this disk. You can provide this as a
partial or full URL to the resource. For example, the following are valid
values:
   
   
     - https://www.googleapis.com/compute/v1/projects/project/global/snapshots/snapshot 
   - projects/project/global/snapshots/snapshot
     - global/snapshots/snapshot |
| `source_disk` | String | The source disk used to create this disk. You can provide this as a
partial or full URL to the resource. For example, the following are valid
values:
   
   
     - 
       https://www.googleapis.com/compute/v1/projects/project/zones/zone/disks/disk
     
     - 
       https://www.googleapis.com/compute/v1/projects/project/regions/region/disks/disk
     
     - 
       projects/project/zones/zone/disks/disk
     
     - 
       projects/project/regions/region/disks/disk
     
     - 
       zones/zone/disks/disk
     
     - 
       regions/region/disks/disk |
| `source_storage_object` | String | The full Google Cloud Storage URI where the disk image is stored. This file
must be a gzip-compressed tarball whose name ends in .tar.gz or virtual
machine disk whose name ends in vmdk. Valid URIs may start with gs:// or
https://storage.googleapis.com/. This flag is not optimized for creating
multiple disks from a source storage object. To create many disks from a
source storage object, use gcloud compute images
import instead. |
| `physical_block_size_bytes` | String | Physical block size of the persistent disk, in bytes.
If not present in a request, a default value is used.
The currently supported size is 4096, other sizes may be added in
the future.
If an unsupported value is requested, the error message will list
the supported values for the caller's project. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `source_consistency_group_policy` | String | [Output Only] URL of the DiskConsistencyGroupPolicy for a secondary disk
that was created using a consistency group. |
| `self_link` | String | [Output Only] Server-defined fully-qualified URL for this resource. |
| `source_image_id` | String | [Output Only] The ID value of the image used to create this disk. This
value identifies the exact image that was used to create this persistent
disk. For example, if you created the persistent disk from an image that
was later deleted and recreated under the same name, the source image ID
would identify the exact version of the image that was used. |
| `location_hint` | String | An opaque location hint used to place the disk close to other resources.
This field is for use by internal tools that use the public API. |
| `label_fingerprint` | String | A fingerprint for the labels being applied to this disk, which is
essentially a hash of the labels set used for optimistic locking. The
fingerprint is initially generated by Compute Engine and changes after
every request to modify or update labels. You must always provide an
up-to-date fingerprint hash in order to update or change labels,
otherwise the request will fail with error412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve a disk. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `params` | String | Input only. [Input Only] Additional params passed with the request, but not persisted
as part of resource payload. |
| `provisioned_throughput` | String | Indicates how much throughput to provision for the disk. This sets the
number of throughput mb per second that the disk can handle. Values must be
greater than or equal to 1. |
| `resource_policies` | Vec<String> | Resource policies applied to this disk for automatic snapshot creations. |
| `replica_zones` | Vec<String> | URLs of the zones where the disk should be replicated to. Only applicable
for regional resources. |
| `storage_pool` | String | The storage pool in which the new disk is created. You can provide
this as a partial or full URL to the resource. For example, the following
are valid values:
   
   
     - https://www.googleapis.com/compute/v1/projects/project/zones/zone/storagePools/storagePool
     - projects/project/zones/zone/storagePools/storagePool 
   - zones/zone/storagePools/storagePool |
| `source_instant_snapshot_id` | String | [Output Only] The unique ID of the instant snapshot used to create this
disk. This value identifies the exact instant snapshot that was used to
create this persistent disk. For example, if you created the persistent
disk from an instant snapshot that was later deleted and recreated under
the same name, the source instant snapshot ID would identify the exact
version of the instant snapshot that was used. |
| `users` | Vec<String> | [Output Only] Links to the users of the disk (attached instances)
in form:projects/project/zones/zone/instances/instance |
| `kind` | String | [Output Only] Type of the resource. Always compute#disk for
disks. |
| `satisfies_pzs` | bool | [Output Only] Reserved for future use. |
| `source_snapshot_encryption_key` | String | Thecustomer-supplied
encryption key of the source snapshot. Required if the source snapshot
is protected by a customer-supplied encryption key. |
| `resource_status` | String | [Output Only] Status information for the disk resource. |
| `satisfies_pzi` | bool | Output only. Reserved for future use. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `architecture` | String | The architecture of the disk. Valid values are
ARM64 or X86_64. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create region_disk
region_disk = provider.compute_api.Region_disk {
    project = "value"  # Project ID for this request.
    region = "value"  # Name of the region for this request.
}

# Access region_disk outputs
region_disk_id = region_disk.id
region_disk_size_gb = region_disk.size_gb
region_disk_status = region_disk.status
region_disk_type = region_disk.type
region_disk_async_secondary_disks = region_disk.async_secondary_disks
region_disk_last_detach_timestamp = region_disk.last_detach_timestamp
region_disk_region = region_disk.region
region_disk_licenses = region_disk.licenses
region_disk_last_attach_timestamp = region_disk.last_attach_timestamp
region_disk_guest_os_features = region_disk.guest_os_features
region_disk_source_consistency_group_policy_id = region_disk.source_consistency_group_policy_id
region_disk_zone = region_disk.zone
region_disk_provisioned_iops = region_disk.provisioned_iops
region_disk_source_image_encryption_key = region_disk.source_image_encryption_key
region_disk_access_mode = region_disk.access_mode
region_disk_async_primary_disk = region_disk.async_primary_disk
region_disk_source_image = region_disk.source_image
region_disk_source_snapshot_id = region_disk.source_snapshot_id
region_disk_name = region_disk.name
region_disk_options = region_disk.options
region_disk_source_disk_id = region_disk.source_disk_id
region_disk_labels = region_disk.labels
region_disk_license_codes = region_disk.license_codes
region_disk_disk_encryption_key = region_disk.disk_encryption_key
region_disk_enable_confidential_compute = region_disk.enable_confidential_compute
region_disk_source_instant_snapshot = region_disk.source_instant_snapshot
region_disk_source_snapshot = region_disk.source_snapshot
region_disk_source_disk = region_disk.source_disk
region_disk_source_storage_object = region_disk.source_storage_object
region_disk_physical_block_size_bytes = region_disk.physical_block_size_bytes
region_disk_id = region_disk.id
region_disk_source_consistency_group_policy = region_disk.source_consistency_group_policy
region_disk_self_link = region_disk.self_link
region_disk_source_image_id = region_disk.source_image_id
region_disk_location_hint = region_disk.location_hint
region_disk_label_fingerprint = region_disk.label_fingerprint
region_disk_description = region_disk.description
region_disk_params = region_disk.params
region_disk_provisioned_throughput = region_disk.provisioned_throughput
region_disk_resource_policies = region_disk.resource_policies
region_disk_replica_zones = region_disk.replica_zones
region_disk_storage_pool = region_disk.storage_pool
region_disk_source_instant_snapshot_id = region_disk.source_instant_snapshot_id
region_disk_users = region_disk.users
region_disk_kind = region_disk.kind
region_disk_satisfies_pzs = region_disk.satisfies_pzs
region_disk_source_snapshot_encryption_key = region_disk.source_snapshot_encryption_key
region_disk_resource_status = region_disk.resource_status
region_disk_satisfies_pzi = region_disk.satisfies_pzi
region_disk_creation_timestamp = region_disk.creation_timestamp
region_disk_architecture = region_disk.architecture
```

---


### Project

Enable this project as a shared VPC host project.

**Operations**: ✅ Create ✅ Read

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `name` | String | The project ID. For example: my-example-project. Use the
project ID to make requests to Compute Engine. |
| `quotas` | Vec<String> | [Output Only] Quotas assigned to this project. |
| `vm_dns_setting` | String | [Output Only] Default internal DNS setting used by VMs running in
this project. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. This is *not* the project ID, and is
just a unique ID used by Compute Engine to identify resources. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `cloud_armor_tier` | String | [Output Only] The Cloud Armor tier for this project. It can be one of the
following values: CA_STANDARD,CA_ENTERPRISE_PAYGO.

If this field is not specified, it is assumed to beCA_STANDARD. |
| `description` | String | An optional textual description of the resource. |
| `usage_export_location` | String | An optional naming prefix for daily usage reports and the Google Cloud
Storage bucket where they are stored. |
| `common_instance_metadata` | String | Metadata key/value pairs available to all instances contained in this
project. See Custom
metadata for more information. |
| `xpn_project_status` | String | [Output Only] The role this project has in a shared VPC configuration.
Currently, only projects with the host role, which is specified by the
value HOST, are differentiated. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `default_network_tier` | String | This signifies the default network tier used for configuring resources of
the project and can only take the following values:PREMIUM, STANDARD. Initially the default network
tier is PREMIUM. |
| `enabled_features` | Vec<String> | An optional list of restricted features enabled for use on this project. |
| `default_service_account` | String | [Output Only] Default service account used by VMs running in this project. |
| `kind` | String | [Output Only] Type of the resource. Always compute#project for
projects. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create project
project = provider.compute_api.Project {
    project = "value"  # Project ID for this request.
}

# Access project outputs
project_id = project.id
project_name = project.name
project_quotas = project.quotas
project_vm_dns_setting = project.vm_dns_setting
project_id = project.id
project_self_link = project.self_link
project_cloud_armor_tier = project.cloud_armor_tier
project_description = project.description
project_usage_export_location = project.usage_export_location
project_common_instance_metadata = project.common_instance_metadata
project_xpn_project_status = project.xpn_project_status
project_creation_timestamp = project.creation_timestamp
project_default_network_tier = project.default_network_tier
project_enabled_features = project.enabled_features
project_default_service_account = project.default_service_account
project_kind = project.kind
```

---


### Region_instance_group

Lists the instances in the specified instance group and displays
information about the named ports. Depending on the specified options, this
method can list all instances or only the instances that are running.
The orderBy query parameter is not supported.

**Operations**: ✅ Create ✅ Read

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `instance_state` | String |  | Instances in which state should be returned. Valid options are:
'ALL', 'RUNNING'. By default, it lists all instances. |
| `port_name` | String |  | Name of port user is interested in. It is optional. If it is set, only
information about this ports will be returned. If it is not set, all the
named ports will be returned.
Always lists all instances. |
| `project` | String | ✅ | Project ID for this request. |
| `instance_group` | String | ✅ | Name of the regional instance group for which we want to list the
instances. |
| `region` | String | ✅ | Name of the region scoping this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `network` | String | [Output Only] The URL of the network to which all instances in the
instance group belong. If your instance has multiple network interfaces,
then the network and subnetwork fields only refer to the
network and subnet used by your primary interface (nic0). |
| `size` | i64 | [Output Only] The total number of instances in the instance group. |
| `zone` | String | [Output Only] The URL of thezone
where the instance group is located (for zonal resources). |
| `name` | String | The name of the instance group. The name must be 1-63 characters
long, and comply withRFC1035. |
| `kind` | String | [Output Only] The resource type, which is alwayscompute#instanceGroup for instance groups. |
| `creation_timestamp` | String | [Output Only] The creation timestamp for this instance group inRFC3339
text format. |
| `self_link` | String | [Output Only] The URL for this instance group. The server generates
this URL. |
| `subnetwork` | String | [Output Only] The URL of the subnetwork to which all instances in the
instance group belong. If your instance has multiple network interfaces,
then the network and subnetwork fields only refer to the
network and subnet used by your primary interface (nic0). |
| `region` | String | [Output Only] The URL of theregion
where the instance group is located (for regional resources). |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `named_ports` | Vec<String> | Optional. Assigns a name to a port number. For example:{name: "http", port: 80}

This
allows the system to reference ports by the assigned name instead of a
port number. Named ports can also contain multiple ports. For example:[{name: "app1", port: 8080}, {name:
"app1", port: 8081}, {name: "app2", port:
8082}]

Named ports apply to all instances in this instance group. |
| `fingerprint` | String | [Output Only] The fingerprint of the named ports. The system
uses this fingerprint to detect conflicts when multiple users change the
named ports concurrently. |
| `id` | String | [Output Only] A unique identifier for this instance group, generated
by the server. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create region_instance_group
region_instance_group = provider.compute_api.Region_instance_group {
    project = "value"  # Project ID for this request.
    instance_group = "value"  # Name of the regional instance group for which we want to list the
instances.
    region = "value"  # Name of the region scoping this request.
}

# Access region_instance_group outputs
region_instance_group_id = region_instance_group.id
region_instance_group_network = region_instance_group.network
region_instance_group_size = region_instance_group.size
region_instance_group_zone = region_instance_group.zone
region_instance_group_name = region_instance_group.name
region_instance_group_kind = region_instance_group.kind
region_instance_group_creation_timestamp = region_instance_group.creation_timestamp
region_instance_group_self_link = region_instance_group.self_link
region_instance_group_subnetwork = region_instance_group.subnetwork
region_instance_group_region = region_instance_group.region
region_instance_group_description = region_instance_group.description
region_instance_group_named_ports = region_instance_group.named_ports
region_instance_group_fingerprint = region_instance_group.fingerprint
region_instance_group_id = region_instance_group.id
```

---


### Router

Creates a Router resource in the specified project and region using
the data included in the request.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `bgp_peers` | Vec<String> |  | BGP information that must be configured into the routing stack to
establish BGP peering. This information must specify the peer ASN and
either the interface name, IP address, or peer IP address. Please refer toRFC4273. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `network` | String |  | URI of the network to which this router belongs. |
| `bgp` | String |  | BGP information specific to this router. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `region` | String |  | [Output Only] URI of the region where the router resides.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `kind` | String |  | [Output Only] Type of resource. Always compute#router for
routers. |
| `name` | String |  | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `encrypted_interconnect_router` | bool |  | Indicates if a router is dedicated for use with encrypted VLAN
attachments (interconnectAttachments). |
| `nats` | Vec<String> |  | A list of NAT services created in this router. |
| `md5_authentication_keys` | Vec<String> |  | Keys used for MD5 authentication. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `params` | String |  | Input only. [Input Only] Additional params passed with the request, but not persisted
as part of resource payload. |
| `interfaces` | Vec<String> |  | Router interfaces.
To create a BGP peer that uses a router interface,
the interface must have one of the following fields specified:
   
   - linkedVpnTunnel
   - linkedInterconnectAttachment
   - subnetwork


You can create a router interface without any of these fields specified.
However, you cannot create a BGP peer that uses that interface. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `region` | String | ✅ | Name of the region for this request. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `bgp_peers` | Vec<String> | BGP information that must be configured into the routing stack to
establish BGP peering. This information must specify the peer ASN and
either the interface name, IP address, or peer IP address. Please refer toRFC4273. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `network` | String | URI of the network to which this router belongs. |
| `bgp` | String | BGP information specific to this router. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `region` | String | [Output Only] URI of the region where the router resides.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `kind` | String | [Output Only] Type of resource. Always compute#router for
routers. |
| `name` | String | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `encrypted_interconnect_router` | bool | Indicates if a router is dedicated for use with encrypted VLAN
attachments (interconnectAttachments). |
| `nats` | Vec<String> | A list of NAT services created in this router. |
| `md5_authentication_keys` | Vec<String> | Keys used for MD5 authentication. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `params` | String | Input only. [Input Only] Additional params passed with the request, but not persisted
as part of resource payload. |
| `interfaces` | Vec<String> | Router interfaces.
To create a BGP peer that uses a router interface,
the interface must have one of the following fields specified:
   
   - linkedVpnTunnel
   - linkedInterconnectAttachment
   - subnetwork


You can create a router interface without any of these fields specified.
However, you cannot create a BGP peer that uses that interface. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create router
router = provider.compute_api.Router {
    region = "value"  # Name of the region for this request.
    project = "value"  # Project ID for this request.
}

# Access router outputs
router_id = router.id
router_bgp_peers = router.bgp_peers
router_id = router.id
router_network = router.network
router_bgp = router.bgp
router_self_link = router.self_link
router_region = router.region
router_kind = router.kind
router_name = router.name
router_encrypted_interconnect_router = router.encrypted_interconnect_router
router_nats = router.nats
router_md5_authentication_keys = router.md5_authentication_keys
router_description = router.description
router_params = router.params
router_interfaces = router.interfaces
router_creation_timestamp = router.creation_timestamp
```

---


### Network_firewall_policie

Creates a new policy in the specified project using the data included in
the request.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `fingerprint` | String |  | Specifies a fingerprint for this resource, which is essentially a hash of
the metadata's contents and used for optimistic locking. The
fingerprint is initially generated by Compute Engine and changes after
every request to modify or update metadata. You must always provide an
up-to-date fingerprint hash in order to update or change metadata,
otherwise the request will fail with error412 conditionNotMet.

To see the latest fingerprint, make get() request to the
firewall policy. |
| `short_name` | String |  | User-provided name of the Organization firewall policy. The name should be
unique in the organization in which the firewall policy is created.
This field is not applicable to network firewall policies.
This name must be set on creation and cannot be changed. The name must be
1-63 characters long, and comply with RFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `policy_type` | String |  | The type of the firewall policy. This field can be eitherVPC_POLICY or RDMA_ROCE_POLICY.

Note: if not specified then VPC_POLICY will be used. |
| `packet_mirroring_rules` | Vec<String> |  | A list of packet mirroring rules that belong to this policy. |
| `self_link_with_id` | String |  | [Output Only] Server-defined URL for this resource with the resource id. |
| `rule_tuple_count` | i64 |  | [Output Only] Total count of all firewall policy rule tuples. A firewall
policy can not exceed a set number of tuples. |
| `region` | String |  | [Output Only] URL of the region where the regional firewall policy resides.
This field is not applicable to global firewall policies.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `kind` | String |  | [Output only] Type of the resource. Alwayscompute#firewallPolicyfor firewall policies |
| `parent` | String |  | [Output Only] The parent of the firewall policy.
This field is not applicable to network firewall policies. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `display_name` | String |  | Deprecated, please use short name instead. User-provided name of the
Organization firewall policy. The name should be unique in the organization
in which the firewall policy is created.
This field is not applicable to network firewall policies.
This name must be set on creation and cannot be changed.
The name must be 1-63 characters long, and comply
with RFC1035. Specifically, the name must be 1-63 characters
long and match the regular expression `[a-z]([-a-z0-9]*[a-z0-9])?` which
means the first character must be a lowercase letter, and all following
characters must be a dash, lowercase letter, or digit, except the last
character, which cannot be a dash. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `rules` | Vec<String> |  | A list of rules that belong to this policy.
There must always be a default rule (rule with priority 2147483647 and
match "*"). If no rules are provided when creating a firewall policy, a
default rule with action "allow" will be added. |
| `name` | String |  | Name of the resource. For Organization Firewall Policies it's a
[Output Only] numeric ID allocated by Google Cloud which uniquely
identifies the Organization Firewall Policy. |
| `associations` | Vec<String> |  | A list of associations that belong to this firewall policy. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `fingerprint` | String | Specifies a fingerprint for this resource, which is essentially a hash of
the metadata's contents and used for optimistic locking. The
fingerprint is initially generated by Compute Engine and changes after
every request to modify or update metadata. You must always provide an
up-to-date fingerprint hash in order to update or change metadata,
otherwise the request will fail with error412 conditionNotMet.

To see the latest fingerprint, make get() request to the
firewall policy. |
| `short_name` | String | User-provided name of the Organization firewall policy. The name should be
unique in the organization in which the firewall policy is created.
This field is not applicable to network firewall policies.
This name must be set on creation and cannot be changed. The name must be
1-63 characters long, and comply with RFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `policy_type` | String | The type of the firewall policy. This field can be eitherVPC_POLICY or RDMA_ROCE_POLICY.

Note: if not specified then VPC_POLICY will be used. |
| `packet_mirroring_rules` | Vec<String> | A list of packet mirroring rules that belong to this policy. |
| `self_link_with_id` | String | [Output Only] Server-defined URL for this resource with the resource id. |
| `rule_tuple_count` | i64 | [Output Only] Total count of all firewall policy rule tuples. A firewall
policy can not exceed a set number of tuples. |
| `region` | String | [Output Only] URL of the region where the regional firewall policy resides.
This field is not applicable to global firewall policies.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `kind` | String | [Output only] Type of the resource. Alwayscompute#firewallPolicyfor firewall policies |
| `parent` | String | [Output Only] The parent of the firewall policy.
This field is not applicable to network firewall policies. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `display_name` | String | Deprecated, please use short name instead. User-provided name of the
Organization firewall policy. The name should be unique in the organization
in which the firewall policy is created.
This field is not applicable to network firewall policies.
This name must be set on creation and cannot be changed.
The name must be 1-63 characters long, and comply
with RFC1035. Specifically, the name must be 1-63 characters
long and match the regular expression `[a-z]([-a-z0-9]*[a-z0-9])?` which
means the first character must be a lowercase letter, and all following
characters must be a dash, lowercase letter, or digit, except the last
character, which cannot be a dash. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `rules` | Vec<String> | A list of rules that belong to this policy.
There must always be a default rule (rule with priority 2147483647 and
match "*"). If no rules are provided when creating a firewall policy, a
default rule with action "allow" will be added. |
| `name` | String | Name of the resource. For Organization Firewall Policies it's a
[Output Only] numeric ID allocated by Google Cloud which uniquely
identifies the Organization Firewall Policy. |
| `associations` | Vec<String> | A list of associations that belong to this firewall policy. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create network_firewall_policie
network_firewall_policie = provider.compute_api.Network_firewall_policie {
    project = "value"  # Project ID for this request.
}

# Access network_firewall_policie outputs
network_firewall_policie_id = network_firewall_policie.id
network_firewall_policie_fingerprint = network_firewall_policie.fingerprint
network_firewall_policie_short_name = network_firewall_policie.short_name
network_firewall_policie_policy_type = network_firewall_policie.policy_type
network_firewall_policie_packet_mirroring_rules = network_firewall_policie.packet_mirroring_rules
network_firewall_policie_self_link_with_id = network_firewall_policie.self_link_with_id
network_firewall_policie_rule_tuple_count = network_firewall_policie.rule_tuple_count
network_firewall_policie_region = network_firewall_policie.region
network_firewall_policie_kind = network_firewall_policie.kind
network_firewall_policie_parent = network_firewall_policie.parent
network_firewall_policie_creation_timestamp = network_firewall_policie.creation_timestamp
network_firewall_policie_display_name = network_firewall_policie.display_name
network_firewall_policie_description = network_firewall_policie.description
network_firewall_policie_id = network_firewall_policie.id
network_firewall_policie_rules = network_firewall_policie.rules
network_firewall_policie_name = network_firewall_policie.name
network_firewall_policie_associations = network_firewall_policie.associations
network_firewall_policie_self_link = network_firewall_policie.self_link
```

---


### Storage_pool_type

Returns the specified storage pool type.

**Operations**: ✅ Read

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `kind` | String | [Output Only] Type of the resource. Alwayscompute#storagePoolType for storage pool types. |
| `deprecated` | String | [Output Only] The deprecation status associated with this storage pool
type. |
| `description` | String | [Output Only] An optional description of this resource. |
| `max_pool_provisioned_throughput` | String | [Output Only] Maximum provisioned throughput. |
| `max_pool_provisioned_capacity_gb` | String | [Output Only] Maximum storage pool size in GB. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `min_pool_provisioned_capacity_gb` | String | [Output Only] Minimum storage pool size in GB. |
| `min_size_gb` | String | [Deprecated] This field is deprecated. Use minPoolProvisionedCapacityGb
instead. |
| `self_link_with_id` | String | [Output Only] Server-defined URL for this resource with the resource id. |
| `zone` | String | [Output Only] URL of the zone where the storage pool type resides.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `max_pool_provisioned_iops` | String | [Output Only] Maximum provisioned IOPS. |
| `supported_disk_types` | Vec<String> | [Output Only] The list of disk types supported in this storage pool type. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `min_pool_provisioned_iops` | String | [Output Only] Minimum provisioned IOPS. |
| `min_pool_provisioned_throughput` | String | [Output Only] Minimum provisioned throughput. |
| `name` | String | [Output Only] Name of the resource. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Access storage_pool_type outputs
storage_pool_type_id = storage_pool_type.id
storage_pool_type_kind = storage_pool_type.kind
storage_pool_type_deprecated = storage_pool_type.deprecated
storage_pool_type_description = storage_pool_type.description
storage_pool_type_max_pool_provisioned_throughput = storage_pool_type.max_pool_provisioned_throughput
storage_pool_type_max_pool_provisioned_capacity_gb = storage_pool_type.max_pool_provisioned_capacity_gb
storage_pool_type_self_link = storage_pool_type.self_link
storage_pool_type_min_pool_provisioned_capacity_gb = storage_pool_type.min_pool_provisioned_capacity_gb
storage_pool_type_min_size_gb = storage_pool_type.min_size_gb
storage_pool_type_self_link_with_id = storage_pool_type.self_link_with_id
storage_pool_type_zone = storage_pool_type.zone
storage_pool_type_max_pool_provisioned_iops = storage_pool_type.max_pool_provisioned_iops
storage_pool_type_supported_disk_types = storage_pool_type.supported_disk_types
storage_pool_type_creation_timestamp = storage_pool_type.creation_timestamp
storage_pool_type_min_pool_provisioned_iops = storage_pool_type.min_pool_provisioned_iops
storage_pool_type_min_pool_provisioned_throughput = storage_pool_type.min_pool_provisioned_throughput
storage_pool_type_name = storage_pool_type.name
storage_pool_type_id = storage_pool_type.id
```

---


### Image

Creates an image in the specified project using the data included
in the request.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `storage_locations` | Vec<String> |  | Cloud Storage bucket storage location of the image (regional or
multi-regional). |
| `source_snapshot_id` | String |  | [Output Only]
The ID value of the snapshot used to create this image. This value may be
used to determine whether the snapshot was taken from the current or a
previous instance of a given snapshot name. |
| `guest_os_features` | Vec<String> |  | A list of features to enable on the guest operating system. Applicable
only for bootable images. To see a list of available options, see theguestOSfeatures[].type parameter. |
| `satisfies_pzi` | bool |  | Output only. Reserved for future use. |
| `raw_disk` | String |  | The parameters of the raw disk image. |
| `source_image` | String |  | URL of the source image used to create this image.
The following are valid formats for the URL:
   
   - https://www.googleapis.com/compute/v1/projects/project_id/global/
   images/image_name
   - projects/project_id/global/images/image_name



In order to create an image, you must provide the full or partial URL of
one of the following:
   
   - The rawDisk.source URL 
   - The sourceDisk URL 
   - The sourceImage URL 
   - The sourceSnapshot URL |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `source_image_encryption_key` | String |  | The customer-supplied encryption key of the source image. Required if the
source image is protected by a customer-supplied encryption key. |
| `license_codes` | Vec<String> |  | Integer license codes indicating which licenses are attached to this image. |
| `params` | String |  | Input only. [Input Only] Additional params passed with the request, but not persisted
as part of resource payload. |
| `labels` | HashMap<String, String> |  | Labels to apply to this image. These can be later modified by
the setLabels method. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `shielded_instance_initial_state` | String |  | Set the secure boot keys of shielded instance. |
| `name` | String |  | Name of the resource; provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must be
a dash, lowercase letter, or digit, except the last character, which cannot
be a dash. |
| `source_disk_id` | String |  | [Output Only]
The ID value of the disk used to create this image. This value may be used
to determine whether the image was taken from the current or a previous
instance of a given disk name. |
| `source_image_id` | String |  | [Output Only]
The ID value of the image used to create this image. This value may be used
to determine whether the image was taken from the current or a previous
instance of a given image name. |
| `enable_confidential_compute` | bool |  | Whether this image is created from a confidential compute mode disk.
[Output Only]: This field is not set by user, but from source disk. |
| `status` | String |  | [Output Only] The status of the image. An image can be used to create other
resources, such as instances, only after the image has been successfully
created and the status is set to READY. Possible
values are FAILED, PENDING, orREADY. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `archive_size_bytes` | String |  | Size of the image tar.gz archive stored in Google Cloud
Storage (in bytes). |
| `disk_size_gb` | String |  | Size of the image when restored onto a persistent disk (in GB). |
| `source_type` | String |  | The type of the image used to create this disk. The
default and only valid value is RAW. |
| `kind` | String |  | [Output Only] Type of the resource. Always compute#image for
images. |
| `image_encryption_key` | String |  | Encrypts the image using acustomer-supplied
encryption key.

After you encrypt an image with a customer-supplied key, you must provide
the same key if you use the image later (e.g. to create a disk from
the image).

Customer-supplied encryption keys do not protect access to metadata
of the disk.

If you do not provide an encryption key when creating the image, then the
disk will be encrypted using an automatically generated key and you do not
need to provide a key to use the image later. |
| `source_snapshot_encryption_key` | String |  | The customer-supplied encryption key of the source snapshot. Required if
the source snapshot is protected by a customer-supplied encryption key. |
| `source_disk_encryption_key` | String |  | Thecustomer-supplied
encryption key of the source disk. Required if the source disk is
protected by a customer-supplied encryption key. |
| `family` | String |  | The name of the image family to which this image belongs. The image
family name can be from a publicly managed image family provided by
Compute Engine, or from a custom image family you create. For example,centos-stream-9 is a publicly available image family.
For more information, see Image
family best practices.

When creating disks, you can specify an image family instead of a specific
image name. The image family always returns its latest image that is not
deprecated. The name of the image family must comply with RFC1035. |
| `label_fingerprint` | String |  | A fingerprint for the labels being applied to this image, which is
essentially a hash of the labels used for optimistic locking. The
fingerprint is initially generated by Compute Engine and changes after
every request to modify or update labels. You must always provide an
up-to-date fingerprint hash in order to update or change labels,
otherwise the request will fail with error412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve an image. |
| `deprecated` | String |  | The deprecation status associated with this image. |
| `source_disk` | String |  | URL of the source disk used to create this image.
For example, the following are valid values:
   
   - https://www.googleapis.com/compute/v1/projects/project/zones/zone/disks/disk 
   - projects/project/zones/zone/disks/disk 
   - zones/zone/disks/disk



In order to create an image, you must provide the full or partial URL of
one of the following:
   
   - The rawDisk.source URL 
   - The sourceDisk URL 
   - The sourceImage URL 
   - The sourceSnapshot URL |
| `licenses` | Vec<String> |  | Any applicable license URI. |
| `satisfies_pzs` | bool |  | [Output Only] Reserved for future use. |
| `architecture` | String |  | The architecture of the image. Valid values are
ARM64 or X86_64. |
| `source_snapshot` | String |  | URL of the source snapshot used to create this image.
The following are valid formats for the URL:
   
   - https://www.googleapis.com/compute/v1/projects/project_id/global/
   snapshots/snapshot_name
   - projects/project_id/global/snapshots/snapshot_name



In order to create an image, you must provide the full or partial URL of
one of the following:
   
   - The rawDisk.source URL 
   - The sourceDisk URL 
   - The sourceImage URL 
   - The sourceSnapshot URL |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `storage_locations` | Vec<String> | Cloud Storage bucket storage location of the image (regional or
multi-regional). |
| `source_snapshot_id` | String | [Output Only]
The ID value of the snapshot used to create this image. This value may be
used to determine whether the snapshot was taken from the current or a
previous instance of a given snapshot name. |
| `guest_os_features` | Vec<String> | A list of features to enable on the guest operating system. Applicable
only for bootable images. To see a list of available options, see theguestOSfeatures[].type parameter. |
| `satisfies_pzi` | bool | Output only. Reserved for future use. |
| `raw_disk` | String | The parameters of the raw disk image. |
| `source_image` | String | URL of the source image used to create this image.
The following are valid formats for the URL:
   
   - https://www.googleapis.com/compute/v1/projects/project_id/global/
   images/image_name
   - projects/project_id/global/images/image_name



In order to create an image, you must provide the full or partial URL of
one of the following:
   
   - The rawDisk.source URL 
   - The sourceDisk URL 
   - The sourceImage URL 
   - The sourceSnapshot URL |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `source_image_encryption_key` | String | The customer-supplied encryption key of the source image. Required if the
source image is protected by a customer-supplied encryption key. |
| `license_codes` | Vec<String> | Integer license codes indicating which licenses are attached to this image. |
| `params` | String | Input only. [Input Only] Additional params passed with the request, but not persisted
as part of resource payload. |
| `labels` | HashMap<String, String> | Labels to apply to this image. These can be later modified by
the setLabels method. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `shielded_instance_initial_state` | String | Set the secure boot keys of shielded instance. |
| `name` | String | Name of the resource; provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must be
a dash, lowercase letter, or digit, except the last character, which cannot
be a dash. |
| `source_disk_id` | String | [Output Only]
The ID value of the disk used to create this image. This value may be used
to determine whether the image was taken from the current or a previous
instance of a given disk name. |
| `source_image_id` | String | [Output Only]
The ID value of the image used to create this image. This value may be used
to determine whether the image was taken from the current or a previous
instance of a given image name. |
| `enable_confidential_compute` | bool | Whether this image is created from a confidential compute mode disk.
[Output Only]: This field is not set by user, but from source disk. |
| `status` | String | [Output Only] The status of the image. An image can be used to create other
resources, such as instances, only after the image has been successfully
created and the status is set to READY. Possible
values are FAILED, PENDING, orREADY. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `archive_size_bytes` | String | Size of the image tar.gz archive stored in Google Cloud
Storage (in bytes). |
| `disk_size_gb` | String | Size of the image when restored onto a persistent disk (in GB). |
| `source_type` | String | The type of the image used to create this disk. The
default and only valid value is RAW. |
| `kind` | String | [Output Only] Type of the resource. Always compute#image for
images. |
| `image_encryption_key` | String | Encrypts the image using acustomer-supplied
encryption key.

After you encrypt an image with a customer-supplied key, you must provide
the same key if you use the image later (e.g. to create a disk from
the image).

Customer-supplied encryption keys do not protect access to metadata
of the disk.

If you do not provide an encryption key when creating the image, then the
disk will be encrypted using an automatically generated key and you do not
need to provide a key to use the image later. |
| `source_snapshot_encryption_key` | String | The customer-supplied encryption key of the source snapshot. Required if
the source snapshot is protected by a customer-supplied encryption key. |
| `source_disk_encryption_key` | String | Thecustomer-supplied
encryption key of the source disk. Required if the source disk is
protected by a customer-supplied encryption key. |
| `family` | String | The name of the image family to which this image belongs. The image
family name can be from a publicly managed image family provided by
Compute Engine, or from a custom image family you create. For example,centos-stream-9 is a publicly available image family.
For more information, see Image
family best practices.

When creating disks, you can specify an image family instead of a specific
image name. The image family always returns its latest image that is not
deprecated. The name of the image family must comply with RFC1035. |
| `label_fingerprint` | String | A fingerprint for the labels being applied to this image, which is
essentially a hash of the labels used for optimistic locking. The
fingerprint is initially generated by Compute Engine and changes after
every request to modify or update labels. You must always provide an
up-to-date fingerprint hash in order to update or change labels,
otherwise the request will fail with error412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve an image. |
| `deprecated` | String | The deprecation status associated with this image. |
| `source_disk` | String | URL of the source disk used to create this image.
For example, the following are valid values:
   
   - https://www.googleapis.com/compute/v1/projects/project/zones/zone/disks/disk 
   - projects/project/zones/zone/disks/disk 
   - zones/zone/disks/disk



In order to create an image, you must provide the full or partial URL of
one of the following:
   
   - The rawDisk.source URL 
   - The sourceDisk URL 
   - The sourceImage URL 
   - The sourceSnapshot URL |
| `licenses` | Vec<String> | Any applicable license URI. |
| `satisfies_pzs` | bool | [Output Only] Reserved for future use. |
| `architecture` | String | The architecture of the image. Valid values are
ARM64 or X86_64. |
| `source_snapshot` | String | URL of the source snapshot used to create this image.
The following are valid formats for the URL:
   
   - https://www.googleapis.com/compute/v1/projects/project_id/global/
   snapshots/snapshot_name
   - projects/project_id/global/snapshots/snapshot_name



In order to create an image, you must provide the full or partial URL of
one of the following:
   
   - The rawDisk.source URL 
   - The sourceDisk URL 
   - The sourceImage URL 
   - The sourceSnapshot URL |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create image
image = provider.compute_api.Image {
    project = "value"  # Project ID for this request.
}

# Access image outputs
image_id = image.id
image_id = image.id
image_storage_locations = image.storage_locations
image_source_snapshot_id = image.source_snapshot_id
image_guest_os_features = image.guest_os_features
image_satisfies_pzi = image.satisfies_pzi
image_raw_disk = image.raw_disk
image_source_image = image.source_image
image_description = image.description
image_source_image_encryption_key = image.source_image_encryption_key
image_license_codes = image.license_codes
image_params = image.params
image_labels = image.labels
image_creation_timestamp = image.creation_timestamp
image_shielded_instance_initial_state = image.shielded_instance_initial_state
image_name = image.name
image_source_disk_id = image.source_disk_id
image_source_image_id = image.source_image_id
image_enable_confidential_compute = image.enable_confidential_compute
image_status = image.status
image_self_link = image.self_link
image_archive_size_bytes = image.archive_size_bytes
image_disk_size_gb = image.disk_size_gb
image_source_type = image.source_type
image_kind = image.kind
image_image_encryption_key = image.image_encryption_key
image_source_snapshot_encryption_key = image.source_snapshot_encryption_key
image_source_disk_encryption_key = image.source_disk_encryption_key
image_family = image.family
image_label_fingerprint = image.label_fingerprint
image_deprecated = image.deprecated
image_source_disk = image.source_disk
image_licenses = image.licenses
image_satisfies_pzs = image.satisfies_pzs
image_architecture = image.architecture
image_source_snapshot = image.source_snapshot
```

---


### Region_network_firewall_policie

Creates a new network firewall policy in the specified project and region.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `fingerprint` | String |  | Specifies a fingerprint for this resource, which is essentially a hash of
the metadata's contents and used for optimistic locking. The
fingerprint is initially generated by Compute Engine and changes after
every request to modify or update metadata. You must always provide an
up-to-date fingerprint hash in order to update or change metadata,
otherwise the request will fail with error412 conditionNotMet.

To see the latest fingerprint, make get() request to the
firewall policy. |
| `short_name` | String |  | User-provided name of the Organization firewall policy. The name should be
unique in the organization in which the firewall policy is created.
This field is not applicable to network firewall policies.
This name must be set on creation and cannot be changed. The name must be
1-63 characters long, and comply with RFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `policy_type` | String |  | The type of the firewall policy. This field can be eitherVPC_POLICY or RDMA_ROCE_POLICY.

Note: if not specified then VPC_POLICY will be used. |
| `packet_mirroring_rules` | Vec<String> |  | A list of packet mirroring rules that belong to this policy. |
| `self_link_with_id` | String |  | [Output Only] Server-defined URL for this resource with the resource id. |
| `rule_tuple_count` | i64 |  | [Output Only] Total count of all firewall policy rule tuples. A firewall
policy can not exceed a set number of tuples. |
| `region` | String |  | [Output Only] URL of the region where the regional firewall policy resides.
This field is not applicable to global firewall policies.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `kind` | String |  | [Output only] Type of the resource. Alwayscompute#firewallPolicyfor firewall policies |
| `parent` | String |  | [Output Only] The parent of the firewall policy.
This field is not applicable to network firewall policies. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `display_name` | String |  | Deprecated, please use short name instead. User-provided name of the
Organization firewall policy. The name should be unique in the organization
in which the firewall policy is created.
This field is not applicable to network firewall policies.
This name must be set on creation and cannot be changed.
The name must be 1-63 characters long, and comply
with RFC1035. Specifically, the name must be 1-63 characters
long and match the regular expression `[a-z]([-a-z0-9]*[a-z0-9])?` which
means the first character must be a lowercase letter, and all following
characters must be a dash, lowercase letter, or digit, except the last
character, which cannot be a dash. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `rules` | Vec<String> |  | A list of rules that belong to this policy.
There must always be a default rule (rule with priority 2147483647 and
match "*"). If no rules are provided when creating a firewall policy, a
default rule with action "allow" will be added. |
| `name` | String |  | Name of the resource. For Organization Firewall Policies it's a
[Output Only] numeric ID allocated by Google Cloud which uniquely
identifies the Organization Firewall Policy. |
| `associations` | Vec<String> |  | A list of associations that belong to this firewall policy. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `region` | String | ✅ | Name of the region scoping this request. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `fingerprint` | String | Specifies a fingerprint for this resource, which is essentially a hash of
the metadata's contents and used for optimistic locking. The
fingerprint is initially generated by Compute Engine and changes after
every request to modify or update metadata. You must always provide an
up-to-date fingerprint hash in order to update or change metadata,
otherwise the request will fail with error412 conditionNotMet.

To see the latest fingerprint, make get() request to the
firewall policy. |
| `short_name` | String | User-provided name of the Organization firewall policy. The name should be
unique in the organization in which the firewall policy is created.
This field is not applicable to network firewall policies.
This name must be set on creation and cannot be changed. The name must be
1-63 characters long, and comply with RFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `policy_type` | String | The type of the firewall policy. This field can be eitherVPC_POLICY or RDMA_ROCE_POLICY.

Note: if not specified then VPC_POLICY will be used. |
| `packet_mirroring_rules` | Vec<String> | A list of packet mirroring rules that belong to this policy. |
| `self_link_with_id` | String | [Output Only] Server-defined URL for this resource with the resource id. |
| `rule_tuple_count` | i64 | [Output Only] Total count of all firewall policy rule tuples. A firewall
policy can not exceed a set number of tuples. |
| `region` | String | [Output Only] URL of the region where the regional firewall policy resides.
This field is not applicable to global firewall policies.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `kind` | String | [Output only] Type of the resource. Alwayscompute#firewallPolicyfor firewall policies |
| `parent` | String | [Output Only] The parent of the firewall policy.
This field is not applicable to network firewall policies. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `display_name` | String | Deprecated, please use short name instead. User-provided name of the
Organization firewall policy. The name should be unique in the organization
in which the firewall policy is created.
This field is not applicable to network firewall policies.
This name must be set on creation and cannot be changed.
The name must be 1-63 characters long, and comply
with RFC1035. Specifically, the name must be 1-63 characters
long and match the regular expression `[a-z]([-a-z0-9]*[a-z0-9])?` which
means the first character must be a lowercase letter, and all following
characters must be a dash, lowercase letter, or digit, except the last
character, which cannot be a dash. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `rules` | Vec<String> | A list of rules that belong to this policy.
There must always be a default rule (rule with priority 2147483647 and
match "*"). If no rules are provided when creating a firewall policy, a
default rule with action "allow" will be added. |
| `name` | String | Name of the resource. For Organization Firewall Policies it's a
[Output Only] numeric ID allocated by Google Cloud which uniquely
identifies the Organization Firewall Policy. |
| `associations` | Vec<String> | A list of associations that belong to this firewall policy. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create region_network_firewall_policie
region_network_firewall_policie = provider.compute_api.Region_network_firewall_policie {
    region = "value"  # Name of the region scoping this request.
    project = "value"  # Project ID for this request.
}

# Access region_network_firewall_policie outputs
region_network_firewall_policie_id = region_network_firewall_policie.id
region_network_firewall_policie_fingerprint = region_network_firewall_policie.fingerprint
region_network_firewall_policie_short_name = region_network_firewall_policie.short_name
region_network_firewall_policie_policy_type = region_network_firewall_policie.policy_type
region_network_firewall_policie_packet_mirroring_rules = region_network_firewall_policie.packet_mirroring_rules
region_network_firewall_policie_self_link_with_id = region_network_firewall_policie.self_link_with_id
region_network_firewall_policie_rule_tuple_count = region_network_firewall_policie.rule_tuple_count
region_network_firewall_policie_region = region_network_firewall_policie.region
region_network_firewall_policie_kind = region_network_firewall_policie.kind
region_network_firewall_policie_parent = region_network_firewall_policie.parent
region_network_firewall_policie_creation_timestamp = region_network_firewall_policie.creation_timestamp
region_network_firewall_policie_display_name = region_network_firewall_policie.display_name
region_network_firewall_policie_description = region_network_firewall_policie.description
region_network_firewall_policie_id = region_network_firewall_policie.id
region_network_firewall_policie_rules = region_network_firewall_policie.rules
region_network_firewall_policie_name = region_network_firewall_policie.name
region_network_firewall_policie_associations = region_network_firewall_policie.associations
region_network_firewall_policie_self_link = region_network_firewall_policie.self_link
```

---


### Backend_service

Creates a BackendService resource in the specified project using
the data included in the request. For more information, see
Backend services overview.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `locality_lb_policy` | String |  | The load balancing algorithm used within the scope of the locality. The
possible values are:
   
   - ROUND_ROBIN: This is a simple policy in which each healthy
   backend is selected in round robin order. This is the default.
   - LEAST_REQUEST: An O(1) algorithm which
   selects two random healthy hosts and picks the host which has fewer active
   requests.
   - RING_HASH: The ring/modulo hash load balancer implements
   consistent hashing to backends. The algorithm has the property that the
   addition/removal of a host from a set of N hosts only affects 1/N of the
   requests.
   - RANDOM: The load balancer selects a random healthy
   host.
   - ORIGINAL_DESTINATION: Backend host is selected
   based on the client connection metadata, i.e., connections are opened to
   the same address as the destination address of the incoming connection
   before the connection was redirected to the load balancer.
   - MAGLEV: used as a drop in replacement for the ring hash
   load balancer. Maglev is not as stable as ring hash but has faster table
   lookup build times and host selection times. For more information about
   Maglev, see Maglev:
   A Fast and Reliable Software Network Load Balancer.
   - WEIGHTED_ROUND_ROBIN: Per-endpoint Weighted Round Robin
   Load Balancing using weights computed from Backend reported Custom Metrics.
   If set, the Backend Service responses are expected to contain non-standard
   HTTP response header field Endpoint-Load-Metrics. The reported
   metrics to use for computing the weights are specified via thecustomMetrics field.
   
   This field is applicable to either:
      - A regional backend service with the service_protocol set to HTTP,
      HTTPS, HTTP2 or H2C, and load_balancing_scheme set to
      INTERNAL_MANAGED. 
      - A global backend service with the
      load_balancing_scheme set to INTERNAL_SELF_MANAGED, INTERNAL_MANAGED, or
      EXTERNAL_MANAGED.
   
   
   If sessionAffinity is not configured—that is, if session
   affinity remains at the default value of NONE—then the
   default value for localityLbPolicy
   is ROUND_ROBIN. If session affinity is set to a value other
   than NONE,
   then the default value for localityLbPolicy isMAGLEV.
   
   Only ROUND_ROBIN and RING_HASH are supported
   when the backend service is referenced by a URL map that is bound to
   target gRPC proxy that has validateForProxyless field set to true.
   
   localityLbPolicy cannot be specified with haPolicy. |
| `external_managed_migration_testing_percentage` | f64 |  | Determines the fraction of requests that should be processed by the Global
external Application Load Balancer.

The value of this field must be in the range [0, 100].

Session affinity options will slightly affect this routing behavior, for
more details, see:Session
Affinity.

This value can only be set if the loadBalancingScheme in the BackendService
is set to EXTERNAL (when using the classic Application Load Balancer) and
the migration state is TEST_BY_PERCENTAGE. |
| `custom_metrics` | Vec<String> |  | List of custom metrics that are used for theWEIGHTED_ROUND_ROBIN locality_lb_policy. |
| `max_stream_duration` | String |  | Specifies the default maximum duration (timeout) for streams to this
service. Duration is computed from the beginning of the stream until the
response has been completely processed, including all retries. A stream
that does not complete in this duration is closed.

If not specified, there will be no timeout limit, i.e. the maximum
duration is infinite.

This value can be overridden in the PathMatcher configuration of the
UrlMap that references this backend service.

This field is only allowed when the loadBalancingScheme of
the backend service is INTERNAL_SELF_MANAGED. |
| `name` | String |  | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `backends` | Vec<String> |  | The list of backends that serve this BackendService. |
| `protocol` | String |  | The protocol this BackendService uses to communicate
with backends.

Possible values are HTTP, HTTPS, HTTP2, H2C, TCP, SSL, UDP or GRPC.
depending on the chosen load balancer or Traffic Director configuration.
Refer to the documentation for the load balancers or for Traffic Director
for more information.

Must be set to GRPC when the backend service is referenced by a URL map
that is bound to target gRPC proxy. |
| `subsetting` | String |  | subsetting cannot be specified with haPolicy. |
| `network` | String |  | The URL of the network to which this backend service belongs.

This field must be set for Internal Passthrough Network Load Balancers when
the haPolicy is enabled, and for External Passthrough Network Load
Balancers when the haPolicy fastIpMove is enabled.

This field can only be specified when the load balancing scheme is set toINTERNAL, or when the load balancing scheme is set toEXTERNAL and haPolicy fastIpMove is enabled. |
| `connection_tracking_policy` | String |  | Connection Tracking configuration for this BackendService. Connection
tracking policy settings are only available for external passthrough
Network Load Balancers and internal passthrough Network Load Balancers.

connectionTrackingPolicy cannot be specified with haPolicy. |
| `service_lb_policy` | String |  | URL to networkservices.ServiceLbPolicy resource.

Can only be set if load balancing scheme is EXTERNAL_MANAGED,
INTERNAL_MANAGED or INTERNAL_SELF_MANAGED and the scope is global. |
| `outlier_detection` | String |  | Settings controlling the ejection of unhealthy backend endpoints from the
load balancing pool of each individual proxy instance that processes the
traffic for the given backend service. If not set, this feature is
considered disabled.

Results of the outlier detection algorithm (ejection of endpoints from the
load balancing pool and returning them back to the pool) are executed
independently by each proxy instance of the load balancer. In most cases,
more than one proxy instance handles the traffic received by a backend
service. Thus, it is possible that an unhealthy endpoint is detected and
ejected by only some of the proxies, and while this happens, other proxies
may continue to send requests to the same unhealthy endpoint until they
detect and eject the unhealthy endpoint.

Applicable backend endpoints can be:
   
   - VM instances in an Instance Group
   - Endpoints in a Zonal NEG (GCE_VM_IP, GCE_VM_IP_PORT)
   - Endpoints in a Hybrid Connectivity NEG (NON_GCP_PRIVATE_IP_PORT)
   - Serverless NEGs, that resolve to Cloud Run, App Engine, or Cloud
   Functions Services 
   - Private Service Connect NEGs, that resolve to
   Google-managed regional API endpoints or managed services published using
   Private Service Connect



Applicable backend service types can be:
   
   - A global backend service with the loadBalancingScheme set to
   INTERNAL_SELF_MANAGED or EXTERNAL_MANAGED. 
   - A regional backend
   service with the serviceProtocol set to HTTP, HTTPS, HTTP2 or H2C, and
   loadBalancingScheme set to INTERNAL_MANAGED or EXTERNAL_MANAGED. Not
   supported for Serverless NEGs.



Not supported when the backend service is referenced by a URL map that is
bound to target gRPC proxy that has validateForProxyless field set to true. |
| `region` | String |  | [Output Only] URL of the region where the regional backend service
resides. This field is not applicable to global backend services.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `external_managed_migration_state` | String |  | Specifies the canary migration state. Possible values are PREPARE,
TEST_BY_PERCENTAGE, and TEST_ALL_TRAFFIC.

To begin the migration from EXTERNAL to EXTERNAL_MANAGED, the state must be
changed to PREPARE. The state must be changed to TEST_ALL_TRAFFIC before
the loadBalancingScheme can be changed to EXTERNAL_MANAGED. Optionally, the
TEST_BY_PERCENTAGE state can be used to migrate traffic by percentage using
externalManagedMigrationTestingPercentage.

Rolling back a migration requires the states to be set in reverse order. So
changing the scheme from EXTERNAL_MANAGED to EXTERNAL requires the state to
be set to TEST_ALL_TRAFFIC at the same time. Optionally, the
TEST_BY_PERCENTAGE state can be used to migrate some traffic back to
EXTERNAL or PREPARE can be used to migrate all traffic back to EXTERNAL. |
| `locality_lb_policies` | Vec<String> |  | A list of locality load-balancing policies to be used in order of
preference. When you use localityLbPolicies, you must set at least one
value for either the localityLbPolicies[].policy or the
localityLbPolicies[].customPolicy field. localityLbPolicies overrides any
value set in the localityLbPolicy field.

For an example of how to use this field, seeDefine
a list of preferred policies.

Caution: This field and its children are intended for use in a service mesh
that includes gRPC clients only. Envoy proxies can't use backend services
that have this configuration. |
| `strong_session_affinity_cookie` | String |  | Describes the HTTP cookie used for stateful session affinity. This field is
applicable and required if the sessionAffinity is set toSTRONG_COOKIE_AFFINITY. |
| `log_config` | String |  | This field denotes the logging options for the load balancer traffic served
by this backend service. If logging is enabled, logs will be exported to
Stackdriver. |
| `ip_address_selection_policy` | String |  | Specifies a preference for traffic sent from the proxy to the backend (or
from the client to the backend for proxyless gRPC).
The possible values are:
   
   - IPV4_ONLY: Only send IPv4 traffic to the backends of the
   backend service (Instance Group, Managed Instance Group, Network Endpoint
   Group), regardless of traffic from the client to the proxy. Only IPv4
   health checks are used to check the health of the backends. This is the
   default setting.
   - PREFER_IPV6: Prioritize the connection to the endpoint's
   IPv6 address over its IPv4 address (provided there is a healthy IPv6
   address).
   - IPV6_ONLY: Only send IPv6 traffic to the backends of the
   backend service (Instance Group, Managed Instance Group, Network Endpoint
   Group), regardless of traffic from the client to the proxy. Only IPv6
   health checks are used to check the health of the backends.



This field is applicable to either:
   
   -  Advanced global external Application Load Balancer (load balancing
   scheme EXTERNAL_MANAGED), 
   -  Regional external Application Load
   Balancer, 
   -  Internal proxy Network Load Balancer (load balancing
   scheme INTERNAL_MANAGED), 
   -  Regional internal Application Load
   Balancer (load balancing scheme INTERNAL_MANAGED), 
   -  Traffic
   Director with Envoy proxies and proxyless gRPC (load balancing scheme
   INTERNAL_SELF_MANAGED). |
| `port` | i64 |  | Deprecated in favor of portName. The TCP port to connect on
the backend. The default value is 80.
For internal passthrough Network Load Balancers and external passthrough
Network Load Balancers, omit port. |
| `port_name` | String |  | A named port on a backend instance group representing the port for
communication to the backend VMs in that group. The
named port must be [defined on each backend instance
group](https://cloud.google.com/load-balancing/docs/backend-service#named_ports).
This parameter has no meaning if the backends are NEGs. For internal
passthrough Network Load Balancers and external passthrough Network Load
Balancers, omit port_name. |
| `iap` | String |  | The configurations for Identity-Aware Proxy on this resource.
Not available for internal passthrough Network Load Balancers and external
passthrough Network Load Balancers. |
| `tls_settings` | String |  | Configuration for Backend Authenticated TLS and mTLS. May only be specified
when the backend protocol is SSL, HTTPS or HTTP2. |
| `custom_request_headers` | Vec<String> |  | Headers that the load balancer adds to proxied requests. See [Creating
custom
headers](https://cloud.google.com/load-balancing/docs/custom-headers). |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `cdn_policy` | String |  | Cloud CDN configuration for this BackendService. Only available for
specified load balancer types. |
| `circuit_breakers` | String |  |  |
| `metadatas` | HashMap<String, String> |  | Deployment metadata associated with the resource to be set by a GKE hub
controller and read by the backend RCTH |
| `params` | String |  | Input only. [Input Only] Additional params passed with the request, but not persisted
as part of resource payload. |
| `timeout_sec` | i64 |  | The backend service timeout has a different meaning depending on the
type of load balancer. For more information see,
Backend service settings.
The default is 30 seconds.
The full range of timeout values allowed goes from 1
through 2,147,483,647 seconds.

This value can be overridden in the PathMatcher configuration of the
UrlMap that references this backend service.

Not supported when the backend service is referenced by a URL map that is
bound to target gRPC proxy that has validateForProxyless field set to true.
Instead, use maxStreamDuration. |
| `security_policy` | String |  | [Output Only] The resource URL for the security policy associated with this
backend service. |
| `load_balancing_scheme` | String |  | Specifies the load balancer type. A backend service
created for one type of load balancer cannot be used with another.
For more information, refer toChoosing
a load balancer. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `used_by` | Vec<String> |  | [Output Only] List of resources referencing given backend service. |
| `security_settings` | String |  | This field specifies the security settings that apply to this backend
service. This field is applicable to a global backend service with the
load_balancing_scheme set to INTERNAL_SELF_MANAGED. |
| `session_affinity` | String |  | Type of session affinity to use. The default is NONE.

Only NONE and HEADER_FIELD are supported
when the backend service is referenced by a URL map that is bound to
target gRPC proxy that has validateForProxyless field set to true.

For more details, see:
[Session
Affinity](https://cloud.google.com/load-balancing/docs/backend-service#session_affinity).

sessionAffinity cannot be specified with haPolicy. |
| `kind` | String |  | [Output Only] Type of resource. Always compute#backendService
for backend services. |
| `service_bindings` | Vec<String> |  | URLs of networkservices.ServiceBinding resources.

Can only be set if load balancing scheme is INTERNAL_SELF_MANAGED.
If set, lists of backends and health checks must be both empty. |
| `failover_policy` | String |  | Requires at least one backend instance group to be defined
as a backup (failover) backend.
For load balancers that have configurable failover:
[Internal passthrough Network Load
Balancers](https://cloud.google.com/load-balancing/docs/internal/failover-overview)
and [external passthrough Network Load
Balancers](https://cloud.google.com/load-balancing/docs/network/networklb-failover-overview).

failoverPolicy cannot be specified with haPolicy. |
| `connection_draining` | String |  | connectionDraining cannot be specified with haPolicy. |
| `enable_cdn` | bool |  | If true, enables Cloud CDN for the backend service of a
global external Application Load Balancer. |
| `ha_policy` | String |  | Configures self-managed High Availability (HA) for External and Internal
Protocol Forwarding.

The backends of this regional backend service must only specify zonal
network endpoint groups (NEGs) of type GCE_VM_IP.

When haPolicy is set for an Internal Passthrough Network Load Balancer, the
regional backend service must set the network field. All zonal NEGs must
belong to the same network. However, individual NEGs can
belong to different subnetworks of that network.

When haPolicy is specified, the set of attached network endpoints across
all backends comprise an High Availability domain from which one endpoint
is selected as the active endpoint (the leader) that receives all
traffic.

haPolicy can be added only at backend service creation time. Once set up,
it cannot be deleted.

Note that haPolicy is not for load balancing, and therefore cannot be
specified with sessionAffinity, connectionTrackingPolicy, and
failoverPolicy.

haPolicy requires customers to be responsible for tracking backend
endpoint health and electing a leader among the healthy endpoints.
Therefore, haPolicy cannot be specified with healthChecks.

haPolicy can only be specified for External Passthrough Network Load
Balancers and Internal Passthrough Network Load Balancers. |
| `affinity_cookie_ttl_sec` | i64 |  | Lifetime of cookies in seconds. This setting is applicable to Application
Load Balancers and Traffic Director and requires
GENERATED_COOKIE or HTTP_COOKIE session affinity.

If set to 0, the cookie is non-persistent and lasts only until
the end of the browser session (or equivalent). The maximum allowed value
is two weeks (1,209,600).

Not supported when the backend service is referenced by a URL map that is
bound to target gRPC proxy that has validateForProxyless field set to true. |
| `custom_response_headers` | Vec<String> |  | Headers that the load balancer adds to proxied responses. See [Creating
custom
headers](https://cloud.google.com/load-balancing/docs/custom-headers). |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `edge_security_policy` | String |  | [Output Only] The resource URL for the edge security policy associated with
this backend service. |
| `consistent_hash` | String |  | Consistent Hash-based load balancing can be used to provide soft session
affinity based on HTTP headers, cookies or other properties. This load
balancing policy is applicable only for HTTP connections. The affinity to a
particular destination host will be lost when one or more hosts are
added/removed from the destination service. This field specifies parameters
that control consistent hashing. This field is only applicable whenlocalityLbPolicy is set to MAGLEV orRING_HASH.

This field is applicable to either:
   
   - A regional backend service with the service_protocol set to HTTP,
   HTTPS, HTTP2 or H2C, and load_balancing_scheme set to
   INTERNAL_MANAGED. 
   - A global backend service with the
   load_balancing_scheme set to INTERNAL_SELF_MANAGED. |
| `health_checks` | Vec<String> |  | The list of URLs to the healthChecks, httpHealthChecks (legacy), or
httpsHealthChecks (legacy) resource for health checking this backend
service. Not all backend services support legacy health checks. See
Load balancer guide. Currently, at most one health check can be
specified for each backend service. Backend services with
instance group or zonal NEG backends must have a health check unless
haPolicy is specified. Backend services with internet or serverless NEG
backends must not have a health check.

healthChecks[] cannot be specified with haPolicy. |
| `fingerprint` | String |  | Fingerprint of this resource. A hash of the contents stored in this object.
This field is used in optimistic locking. This field will be ignored when
inserting a BackendService. An up-to-date fingerprint must be provided in
order to update the BackendService, otherwise the request will
fail with error 412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve a BackendService. |
| `compression_mode` | String |  | Compress text responses using Brotli or gzip compression, based on
the client's Accept-Encoding header. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `locality_lb_policy` | String | The load balancing algorithm used within the scope of the locality. The
possible values are:
   
   - ROUND_ROBIN: This is a simple policy in which each healthy
   backend is selected in round robin order. This is the default.
   - LEAST_REQUEST: An O(1) algorithm which
   selects two random healthy hosts and picks the host which has fewer active
   requests.
   - RING_HASH: The ring/modulo hash load balancer implements
   consistent hashing to backends. The algorithm has the property that the
   addition/removal of a host from a set of N hosts only affects 1/N of the
   requests.
   - RANDOM: The load balancer selects a random healthy
   host.
   - ORIGINAL_DESTINATION: Backend host is selected
   based on the client connection metadata, i.e., connections are opened to
   the same address as the destination address of the incoming connection
   before the connection was redirected to the load balancer.
   - MAGLEV: used as a drop in replacement for the ring hash
   load balancer. Maglev is not as stable as ring hash but has faster table
   lookup build times and host selection times. For more information about
   Maglev, see Maglev:
   A Fast and Reliable Software Network Load Balancer.
   - WEIGHTED_ROUND_ROBIN: Per-endpoint Weighted Round Robin
   Load Balancing using weights computed from Backend reported Custom Metrics.
   If set, the Backend Service responses are expected to contain non-standard
   HTTP response header field Endpoint-Load-Metrics. The reported
   metrics to use for computing the weights are specified via thecustomMetrics field.
   
   This field is applicable to either:
      - A regional backend service with the service_protocol set to HTTP,
      HTTPS, HTTP2 or H2C, and load_balancing_scheme set to
      INTERNAL_MANAGED. 
      - A global backend service with the
      load_balancing_scheme set to INTERNAL_SELF_MANAGED, INTERNAL_MANAGED, or
      EXTERNAL_MANAGED.
   
   
   If sessionAffinity is not configured—that is, if session
   affinity remains at the default value of NONE—then the
   default value for localityLbPolicy
   is ROUND_ROBIN. If session affinity is set to a value other
   than NONE,
   then the default value for localityLbPolicy isMAGLEV.
   
   Only ROUND_ROBIN and RING_HASH are supported
   when the backend service is referenced by a URL map that is bound to
   target gRPC proxy that has validateForProxyless field set to true.
   
   localityLbPolicy cannot be specified with haPolicy. |
| `external_managed_migration_testing_percentage` | f64 | Determines the fraction of requests that should be processed by the Global
external Application Load Balancer.

The value of this field must be in the range [0, 100].

Session affinity options will slightly affect this routing behavior, for
more details, see:Session
Affinity.

This value can only be set if the loadBalancingScheme in the BackendService
is set to EXTERNAL (when using the classic Application Load Balancer) and
the migration state is TEST_BY_PERCENTAGE. |
| `custom_metrics` | Vec<String> | List of custom metrics that are used for theWEIGHTED_ROUND_ROBIN locality_lb_policy. |
| `max_stream_duration` | String | Specifies the default maximum duration (timeout) for streams to this
service. Duration is computed from the beginning of the stream until the
response has been completely processed, including all retries. A stream
that does not complete in this duration is closed.

If not specified, there will be no timeout limit, i.e. the maximum
duration is infinite.

This value can be overridden in the PathMatcher configuration of the
UrlMap that references this backend service.

This field is only allowed when the loadBalancingScheme of
the backend service is INTERNAL_SELF_MANAGED. |
| `name` | String | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `backends` | Vec<String> | The list of backends that serve this BackendService. |
| `protocol` | String | The protocol this BackendService uses to communicate
with backends.

Possible values are HTTP, HTTPS, HTTP2, H2C, TCP, SSL, UDP or GRPC.
depending on the chosen load balancer or Traffic Director configuration.
Refer to the documentation for the load balancers or for Traffic Director
for more information.

Must be set to GRPC when the backend service is referenced by a URL map
that is bound to target gRPC proxy. |
| `subsetting` | String | subsetting cannot be specified with haPolicy. |
| `network` | String | The URL of the network to which this backend service belongs.

This field must be set for Internal Passthrough Network Load Balancers when
the haPolicy is enabled, and for External Passthrough Network Load
Balancers when the haPolicy fastIpMove is enabled.

This field can only be specified when the load balancing scheme is set toINTERNAL, or when the load balancing scheme is set toEXTERNAL and haPolicy fastIpMove is enabled. |
| `connection_tracking_policy` | String | Connection Tracking configuration for this BackendService. Connection
tracking policy settings are only available for external passthrough
Network Load Balancers and internal passthrough Network Load Balancers.

connectionTrackingPolicy cannot be specified with haPolicy. |
| `service_lb_policy` | String | URL to networkservices.ServiceLbPolicy resource.

Can only be set if load balancing scheme is EXTERNAL_MANAGED,
INTERNAL_MANAGED or INTERNAL_SELF_MANAGED and the scope is global. |
| `outlier_detection` | String | Settings controlling the ejection of unhealthy backend endpoints from the
load balancing pool of each individual proxy instance that processes the
traffic for the given backend service. If not set, this feature is
considered disabled.

Results of the outlier detection algorithm (ejection of endpoints from the
load balancing pool and returning them back to the pool) are executed
independently by each proxy instance of the load balancer. In most cases,
more than one proxy instance handles the traffic received by a backend
service. Thus, it is possible that an unhealthy endpoint is detected and
ejected by only some of the proxies, and while this happens, other proxies
may continue to send requests to the same unhealthy endpoint until they
detect and eject the unhealthy endpoint.

Applicable backend endpoints can be:
   
   - VM instances in an Instance Group
   - Endpoints in a Zonal NEG (GCE_VM_IP, GCE_VM_IP_PORT)
   - Endpoints in a Hybrid Connectivity NEG (NON_GCP_PRIVATE_IP_PORT)
   - Serverless NEGs, that resolve to Cloud Run, App Engine, or Cloud
   Functions Services 
   - Private Service Connect NEGs, that resolve to
   Google-managed regional API endpoints or managed services published using
   Private Service Connect



Applicable backend service types can be:
   
   - A global backend service with the loadBalancingScheme set to
   INTERNAL_SELF_MANAGED or EXTERNAL_MANAGED. 
   - A regional backend
   service with the serviceProtocol set to HTTP, HTTPS, HTTP2 or H2C, and
   loadBalancingScheme set to INTERNAL_MANAGED or EXTERNAL_MANAGED. Not
   supported for Serverless NEGs.



Not supported when the backend service is referenced by a URL map that is
bound to target gRPC proxy that has validateForProxyless field set to true. |
| `region` | String | [Output Only] URL of the region where the regional backend service
resides. This field is not applicable to global backend services.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `external_managed_migration_state` | String | Specifies the canary migration state. Possible values are PREPARE,
TEST_BY_PERCENTAGE, and TEST_ALL_TRAFFIC.

To begin the migration from EXTERNAL to EXTERNAL_MANAGED, the state must be
changed to PREPARE. The state must be changed to TEST_ALL_TRAFFIC before
the loadBalancingScheme can be changed to EXTERNAL_MANAGED. Optionally, the
TEST_BY_PERCENTAGE state can be used to migrate traffic by percentage using
externalManagedMigrationTestingPercentage.

Rolling back a migration requires the states to be set in reverse order. So
changing the scheme from EXTERNAL_MANAGED to EXTERNAL requires the state to
be set to TEST_ALL_TRAFFIC at the same time. Optionally, the
TEST_BY_PERCENTAGE state can be used to migrate some traffic back to
EXTERNAL or PREPARE can be used to migrate all traffic back to EXTERNAL. |
| `locality_lb_policies` | Vec<String> | A list of locality load-balancing policies to be used in order of
preference. When you use localityLbPolicies, you must set at least one
value for either the localityLbPolicies[].policy or the
localityLbPolicies[].customPolicy field. localityLbPolicies overrides any
value set in the localityLbPolicy field.

For an example of how to use this field, seeDefine
a list of preferred policies.

Caution: This field and its children are intended for use in a service mesh
that includes gRPC clients only. Envoy proxies can't use backend services
that have this configuration. |
| `strong_session_affinity_cookie` | String | Describes the HTTP cookie used for stateful session affinity. This field is
applicable and required if the sessionAffinity is set toSTRONG_COOKIE_AFFINITY. |
| `log_config` | String | This field denotes the logging options for the load balancer traffic served
by this backend service. If logging is enabled, logs will be exported to
Stackdriver. |
| `ip_address_selection_policy` | String | Specifies a preference for traffic sent from the proxy to the backend (or
from the client to the backend for proxyless gRPC).
The possible values are:
   
   - IPV4_ONLY: Only send IPv4 traffic to the backends of the
   backend service (Instance Group, Managed Instance Group, Network Endpoint
   Group), regardless of traffic from the client to the proxy. Only IPv4
   health checks are used to check the health of the backends. This is the
   default setting.
   - PREFER_IPV6: Prioritize the connection to the endpoint's
   IPv6 address over its IPv4 address (provided there is a healthy IPv6
   address).
   - IPV6_ONLY: Only send IPv6 traffic to the backends of the
   backend service (Instance Group, Managed Instance Group, Network Endpoint
   Group), regardless of traffic from the client to the proxy. Only IPv6
   health checks are used to check the health of the backends.



This field is applicable to either:
   
   -  Advanced global external Application Load Balancer (load balancing
   scheme EXTERNAL_MANAGED), 
   -  Regional external Application Load
   Balancer, 
   -  Internal proxy Network Load Balancer (load balancing
   scheme INTERNAL_MANAGED), 
   -  Regional internal Application Load
   Balancer (load balancing scheme INTERNAL_MANAGED), 
   -  Traffic
   Director with Envoy proxies and proxyless gRPC (load balancing scheme
   INTERNAL_SELF_MANAGED). |
| `port` | i64 | Deprecated in favor of portName. The TCP port to connect on
the backend. The default value is 80.
For internal passthrough Network Load Balancers and external passthrough
Network Load Balancers, omit port. |
| `port_name` | String | A named port on a backend instance group representing the port for
communication to the backend VMs in that group. The
named port must be [defined on each backend instance
group](https://cloud.google.com/load-balancing/docs/backend-service#named_ports).
This parameter has no meaning if the backends are NEGs. For internal
passthrough Network Load Balancers and external passthrough Network Load
Balancers, omit port_name. |
| `iap` | String | The configurations for Identity-Aware Proxy on this resource.
Not available for internal passthrough Network Load Balancers and external
passthrough Network Load Balancers. |
| `tls_settings` | String | Configuration for Backend Authenticated TLS and mTLS. May only be specified
when the backend protocol is SSL, HTTPS or HTTP2. |
| `custom_request_headers` | Vec<String> | Headers that the load balancer adds to proxied requests. See [Creating
custom
headers](https://cloud.google.com/load-balancing/docs/custom-headers). |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `cdn_policy` | String | Cloud CDN configuration for this BackendService. Only available for
specified load balancer types. |
| `circuit_breakers` | String |  |
| `metadatas` | HashMap<String, String> | Deployment metadata associated with the resource to be set by a GKE hub
controller and read by the backend RCTH |
| `params` | String | Input only. [Input Only] Additional params passed with the request, but not persisted
as part of resource payload. |
| `timeout_sec` | i64 | The backend service timeout has a different meaning depending on the
type of load balancer. For more information see,
Backend service settings.
The default is 30 seconds.
The full range of timeout values allowed goes from 1
through 2,147,483,647 seconds.

This value can be overridden in the PathMatcher configuration of the
UrlMap that references this backend service.

Not supported when the backend service is referenced by a URL map that is
bound to target gRPC proxy that has validateForProxyless field set to true.
Instead, use maxStreamDuration. |
| `security_policy` | String | [Output Only] The resource URL for the security policy associated with this
backend service. |
| `load_balancing_scheme` | String | Specifies the load balancer type. A backend service
created for one type of load balancer cannot be used with another.
For more information, refer toChoosing
a load balancer. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `used_by` | Vec<String> | [Output Only] List of resources referencing given backend service. |
| `security_settings` | String | This field specifies the security settings that apply to this backend
service. This field is applicable to a global backend service with the
load_balancing_scheme set to INTERNAL_SELF_MANAGED. |
| `session_affinity` | String | Type of session affinity to use. The default is NONE.

Only NONE and HEADER_FIELD are supported
when the backend service is referenced by a URL map that is bound to
target gRPC proxy that has validateForProxyless field set to true.

For more details, see:
[Session
Affinity](https://cloud.google.com/load-balancing/docs/backend-service#session_affinity).

sessionAffinity cannot be specified with haPolicy. |
| `kind` | String | [Output Only] Type of resource. Always compute#backendService
for backend services. |
| `service_bindings` | Vec<String> | URLs of networkservices.ServiceBinding resources.

Can only be set if load balancing scheme is INTERNAL_SELF_MANAGED.
If set, lists of backends and health checks must be both empty. |
| `failover_policy` | String | Requires at least one backend instance group to be defined
as a backup (failover) backend.
For load balancers that have configurable failover:
[Internal passthrough Network Load
Balancers](https://cloud.google.com/load-balancing/docs/internal/failover-overview)
and [external passthrough Network Load
Balancers](https://cloud.google.com/load-balancing/docs/network/networklb-failover-overview).

failoverPolicy cannot be specified with haPolicy. |
| `connection_draining` | String | connectionDraining cannot be specified with haPolicy. |
| `enable_cdn` | bool | If true, enables Cloud CDN for the backend service of a
global external Application Load Balancer. |
| `ha_policy` | String | Configures self-managed High Availability (HA) for External and Internal
Protocol Forwarding.

The backends of this regional backend service must only specify zonal
network endpoint groups (NEGs) of type GCE_VM_IP.

When haPolicy is set for an Internal Passthrough Network Load Balancer, the
regional backend service must set the network field. All zonal NEGs must
belong to the same network. However, individual NEGs can
belong to different subnetworks of that network.

When haPolicy is specified, the set of attached network endpoints across
all backends comprise an High Availability domain from which one endpoint
is selected as the active endpoint (the leader) that receives all
traffic.

haPolicy can be added only at backend service creation time. Once set up,
it cannot be deleted.

Note that haPolicy is not for load balancing, and therefore cannot be
specified with sessionAffinity, connectionTrackingPolicy, and
failoverPolicy.

haPolicy requires customers to be responsible for tracking backend
endpoint health and electing a leader among the healthy endpoints.
Therefore, haPolicy cannot be specified with healthChecks.

haPolicy can only be specified for External Passthrough Network Load
Balancers and Internal Passthrough Network Load Balancers. |
| `affinity_cookie_ttl_sec` | i64 | Lifetime of cookies in seconds. This setting is applicable to Application
Load Balancers and Traffic Director and requires
GENERATED_COOKIE or HTTP_COOKIE session affinity.

If set to 0, the cookie is non-persistent and lasts only until
the end of the browser session (or equivalent). The maximum allowed value
is two weeks (1,209,600).

Not supported when the backend service is referenced by a URL map that is
bound to target gRPC proxy that has validateForProxyless field set to true. |
| `custom_response_headers` | Vec<String> | Headers that the load balancer adds to proxied responses. See [Creating
custom
headers](https://cloud.google.com/load-balancing/docs/custom-headers). |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `edge_security_policy` | String | [Output Only] The resource URL for the edge security policy associated with
this backend service. |
| `consistent_hash` | String | Consistent Hash-based load balancing can be used to provide soft session
affinity based on HTTP headers, cookies or other properties. This load
balancing policy is applicable only for HTTP connections. The affinity to a
particular destination host will be lost when one or more hosts are
added/removed from the destination service. This field specifies parameters
that control consistent hashing. This field is only applicable whenlocalityLbPolicy is set to MAGLEV orRING_HASH.

This field is applicable to either:
   
   - A regional backend service with the service_protocol set to HTTP,
   HTTPS, HTTP2 or H2C, and load_balancing_scheme set to
   INTERNAL_MANAGED. 
   - A global backend service with the
   load_balancing_scheme set to INTERNAL_SELF_MANAGED. |
| `health_checks` | Vec<String> | The list of URLs to the healthChecks, httpHealthChecks (legacy), or
httpsHealthChecks (legacy) resource for health checking this backend
service. Not all backend services support legacy health checks. See
Load balancer guide. Currently, at most one health check can be
specified for each backend service. Backend services with
instance group or zonal NEG backends must have a health check unless
haPolicy is specified. Backend services with internet or serverless NEG
backends must not have a health check.

healthChecks[] cannot be specified with haPolicy. |
| `fingerprint` | String | Fingerprint of this resource. A hash of the contents stored in this object.
This field is used in optimistic locking. This field will be ignored when
inserting a BackendService. An up-to-date fingerprint must be provided in
order to update the BackendService, otherwise the request will
fail with error 412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve a BackendService. |
| `compression_mode` | String | Compress text responses using Brotli or gzip compression, based on
the client's Accept-Encoding header. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create backend_service
backend_service = provider.compute_api.Backend_service {
    project = "value"  # Project ID for this request.
}

# Access backend_service outputs
backend_service_id = backend_service.id
backend_service_locality_lb_policy = backend_service.locality_lb_policy
backend_service_external_managed_migration_testing_percentage = backend_service.external_managed_migration_testing_percentage
backend_service_custom_metrics = backend_service.custom_metrics
backend_service_max_stream_duration = backend_service.max_stream_duration
backend_service_name = backend_service.name
backend_service_backends = backend_service.backends
backend_service_protocol = backend_service.protocol
backend_service_subsetting = backend_service.subsetting
backend_service_network = backend_service.network
backend_service_connection_tracking_policy = backend_service.connection_tracking_policy
backend_service_service_lb_policy = backend_service.service_lb_policy
backend_service_outlier_detection = backend_service.outlier_detection
backend_service_region = backend_service.region
backend_service_external_managed_migration_state = backend_service.external_managed_migration_state
backend_service_locality_lb_policies = backend_service.locality_lb_policies
backend_service_strong_session_affinity_cookie = backend_service.strong_session_affinity_cookie
backend_service_log_config = backend_service.log_config
backend_service_ip_address_selection_policy = backend_service.ip_address_selection_policy
backend_service_port = backend_service.port
backend_service_port_name = backend_service.port_name
backend_service_iap = backend_service.iap
backend_service_tls_settings = backend_service.tls_settings
backend_service_custom_request_headers = backend_service.custom_request_headers
backend_service_id = backend_service.id
backend_service_cdn_policy = backend_service.cdn_policy
backend_service_circuit_breakers = backend_service.circuit_breakers
backend_service_metadatas = backend_service.metadatas
backend_service_params = backend_service.params
backend_service_timeout_sec = backend_service.timeout_sec
backend_service_security_policy = backend_service.security_policy
backend_service_load_balancing_scheme = backend_service.load_balancing_scheme
backend_service_self_link = backend_service.self_link
backend_service_used_by = backend_service.used_by
backend_service_security_settings = backend_service.security_settings
backend_service_session_affinity = backend_service.session_affinity
backend_service_kind = backend_service.kind
backend_service_service_bindings = backend_service.service_bindings
backend_service_failover_policy = backend_service.failover_policy
backend_service_connection_draining = backend_service.connection_draining
backend_service_enable_cdn = backend_service.enable_cdn
backend_service_ha_policy = backend_service.ha_policy
backend_service_affinity_cookie_ttl_sec = backend_service.affinity_cookie_ttl_sec
backend_service_custom_response_headers = backend_service.custom_response_headers
backend_service_description = backend_service.description
backend_service_edge_security_policy = backend_service.edge_security_policy
backend_service_consistent_hash = backend_service.consistent_hash
backend_service_health_checks = backend_service.health_checks
backend_service_fingerprint = backend_service.fingerprint
backend_service_compression_mode = backend_service.compression_mode
backend_service_creation_timestamp = backend_service.creation_timestamp
```

---


### Health_check

Creates a HealthCheck resource in the specified project using the data
included in the request.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `https_health_check` | String |  |  |
| `http_health_check` | String |  |  |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `source_regions` | Vec<String> |  | The list of cloud regions from which health checks are performed. If any
regions are specified, then exactly 3 regions should be specified. The
region names must be valid names of Google Cloud regions.
This can only be set for global health check.
If this list is non-empty, then there are restrictions
on what other health check fields are supported and what other resources
can use this health check:
   
   - SSL, HTTP2, and GRPC protocols are not supported.
   - The TCP request field is not supported.
   - The proxyHeader field for HTTP, HTTPS, and TCP is not
   supported.
   - The checkIntervalSec field must be at least 30.
   - The health check cannot be used with BackendService nor with managed
   instance group auto-healing. |
| `kind` | String |  | Type of the resource. |
| `healthy_threshold` | i64 |  | A so-far unhealthy instance will be marked healthy after this
many consecutive successes. The default value is 2. |
| `grpc_health_check` | String |  |  |
| `timeout_sec` | i64 |  | How long (in seconds) to wait before claiming failure. The default value is
5 seconds. It is invalid for timeoutSec to have greater
value than checkIntervalSec. |
| `ssl_health_check` | String |  |  |
| `name` | String |  | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
For example, a name that is 1-63 characters long, matches the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?`, and otherwise complies with
RFC1035. This regular expression describes a name where the first
character is a lowercase letter, and all following characters are a dash,
lowercase letter, or digit, except the last character, which isn't a dash. |
| `grpc_tls_health_check` | String |  |  |
| `type` | String |  | Specifies the type of the healthCheck, either TCP,SSL, HTTP, HTTPS,HTTP2 or GRPC. Exactly one of the
protocol-specific health check fields must be specified, which must matchtype field. |
| `unhealthy_threshold` | i64 |  | A so-far healthy instance will be marked unhealthy after this many
consecutive failures. The default value is 2. |
| `http2_health_check` | String |  |  |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp in3339
text format. |
| `region` | String |  | [Output Only] Region where the health check resides.  Not applicable to
global health checks. |
| `log_config` | String |  | Configure logging on this health check. |
| `tcp_health_check` | String |  |  |
| `check_interval_sec` | i64 |  | How often (in seconds) to send a health check. The default value is 5
seconds. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `https_health_check` | String |  |
| `http_health_check` | String |  |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `source_regions` | Vec<String> | The list of cloud regions from which health checks are performed. If any
regions are specified, then exactly 3 regions should be specified. The
region names must be valid names of Google Cloud regions.
This can only be set for global health check.
If this list is non-empty, then there are restrictions
on what other health check fields are supported and what other resources
can use this health check:
   
   - SSL, HTTP2, and GRPC protocols are not supported.
   - The TCP request field is not supported.
   - The proxyHeader field for HTTP, HTTPS, and TCP is not
   supported.
   - The checkIntervalSec field must be at least 30.
   - The health check cannot be used with BackendService nor with managed
   instance group auto-healing. |
| `kind` | String | Type of the resource. |
| `healthy_threshold` | i64 | A so-far unhealthy instance will be marked healthy after this
many consecutive successes. The default value is 2. |
| `grpc_health_check` | String |  |
| `timeout_sec` | i64 | How long (in seconds) to wait before claiming failure. The default value is
5 seconds. It is invalid for timeoutSec to have greater
value than checkIntervalSec. |
| `ssl_health_check` | String |  |
| `name` | String | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
For example, a name that is 1-63 characters long, matches the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?`, and otherwise complies with
RFC1035. This regular expression describes a name where the first
character is a lowercase letter, and all following characters are a dash,
lowercase letter, or digit, except the last character, which isn't a dash. |
| `grpc_tls_health_check` | String |  |
| `type` | String | Specifies the type of the healthCheck, either TCP,SSL, HTTP, HTTPS,HTTP2 or GRPC. Exactly one of the
protocol-specific health check fields must be specified, which must matchtype field. |
| `unhealthy_threshold` | i64 | A so-far healthy instance will be marked unhealthy after this many
consecutive failures. The default value is 2. |
| `http2_health_check` | String |  |
| `creation_timestamp` | String | [Output Only] Creation timestamp in3339
text format. |
| `region` | String | [Output Only] Region where the health check resides.  Not applicable to
global health checks. |
| `log_config` | String | Configure logging on this health check. |
| `tcp_health_check` | String |  |
| `check_interval_sec` | i64 | How often (in seconds) to send a health check. The default value is 5
seconds. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create health_check
health_check = provider.compute_api.Health_check {
    project = "value"  # Project ID for this request.
}

# Access health_check outputs
health_check_id = health_check.id
health_check_description = health_check.description
health_check_https_health_check = health_check.https_health_check
health_check_http_health_check = health_check.http_health_check
health_check_self_link = health_check.self_link
health_check_id = health_check.id
health_check_source_regions = health_check.source_regions
health_check_kind = health_check.kind
health_check_healthy_threshold = health_check.healthy_threshold
health_check_grpc_health_check = health_check.grpc_health_check
health_check_timeout_sec = health_check.timeout_sec
health_check_ssl_health_check = health_check.ssl_health_check
health_check_name = health_check.name
health_check_grpc_tls_health_check = health_check.grpc_tls_health_check
health_check_type = health_check.type
health_check_unhealthy_threshold = health_check.unhealthy_threshold
health_check_http2_health_check = health_check.http2_health_check
health_check_creation_timestamp = health_check.creation_timestamp
health_check_region = health_check.region
health_check_log_config = health_check.log_config
health_check_tcp_health_check = health_check.tcp_health_check
health_check_check_interval_sec = health_check.check_interval_sec
```

---


### Region_zone

Retrieves the list of Zone resources under the specific region available to
the specified project.

**Operations**: ✅ Read

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `items` | Vec<String> | A list of Zone resources. |
| `kind` | String | Type of resource. |
| `id` | String | [Output Only] Unique identifier for the resource; defined by the server. |
| `next_page_token` | String | [Output Only] This token allows you to get the next page of results for
list requests. If the number of results is larger thanmaxResults, use the nextPageToken as a value for
the query parameter pageToken in the next list request.
Subsequent list requests will have their own nextPageToken to
continue paging through the results. |
| `warning` | String | [Output Only] Informational warning message. |
| `self_link` | String | [Output Only] Server-defined URL for this resource. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Access region_zone outputs
region_zone_id = region_zone.id
region_zone_items = region_zone.items
region_zone_kind = region_zone.kind
region_zone_id = region_zone.id
region_zone_next_page_token = region_zone.next_page_token
region_zone_warning = region_zone.warning
region_zone_self_link = region_zone.self_link
```

---


### Global_public_delegated_prefixe

Creates a global PublicDelegatedPrefix in the specified project using the
parameters that are included in the request.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `public_delegated_sub_prefixs` | Vec<String> |  | The list of sub public delegated prefixes that exist for this public
delegated prefix. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `ip_cidr_range` | String |  | The IP address range, in CIDR format, represented by this public
delegated prefix. |
| `parent_prefix` | String |  | The URL of parent prefix. Either PublicAdvertisedPrefix or
PublicDelegatedPrefix. |
| `name` | String |  | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `id` | String |  | [Output Only] The unique identifier for the resource type. The server
generates this identifier. |
| `status` | String |  | [Output Only] The status of the public delegated prefix, which can be one
of following values:
   
   
     - `INITIALIZING` The public delegated prefix is being initialized and
     addresses cannot be created yet.
     - `READY_TO_ANNOUNCE` The public delegated prefix is a live migration
     prefix and is active.
     - `ANNOUNCED` The public delegated prefix is announced and ready to
     use.
     - `DELETING` The public delegated prefix is being deprovsioned.
     - `ACTIVE` The public delegated prefix is ready to use. |
| `byoip_api_version` | String |  | [Output Only] The version of BYOIP API. |
| `allocatable_prefix_length` | i64 |  | The allocatable prefix length supported by this public delegated prefix.
This field is optional and cannot be set for prefixes in DELEGATION mode.
It cannot be set for IPv4 prefixes either, and it always defaults to 32. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `ipv6_access_type` | String |  | [Output Only] The internet access type for IPv6 Public Delegated Prefixes.
Inherited from parent prefix. |
| `fingerprint` | String |  | Fingerprint of this resource. A hash of the contents stored in this object.
This field is used in optimistic locking. This field will be ignored when
inserting a new PublicDelegatedPrefix. An up-to-date
fingerprint must be provided in order to update thePublicDelegatedPrefix, otherwise the request will fail with
error 412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve a PublicDelegatedPrefix. |
| `is_live_migration` | bool |  | If true, the prefix will be live migrated. |
| `region` | String |  | [Output Only] URL of the region where the public delegated prefix resides.
This field applies only to the region resource. You must specify this
field as part of the HTTP request URL. It is not settable as a field in
the request body. |
| `mode` | String |  | The public delegated prefix mode for IPv6 only. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `kind` | String |  | [Output Only] Type of the resource. Alwayscompute#publicDelegatedPrefix for public delegated prefixes. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `public_delegated_sub_prefixs` | Vec<String> | The list of sub public delegated prefixes that exist for this public
delegated prefix. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `ip_cidr_range` | String | The IP address range, in CIDR format, represented by this public
delegated prefix. |
| `parent_prefix` | String | The URL of parent prefix. Either PublicAdvertisedPrefix or
PublicDelegatedPrefix. |
| `name` | String | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `id` | String | [Output Only] The unique identifier for the resource type. The server
generates this identifier. |
| `status` | String | [Output Only] The status of the public delegated prefix, which can be one
of following values:
   
   
     - `INITIALIZING` The public delegated prefix is being initialized and
     addresses cannot be created yet.
     - `READY_TO_ANNOUNCE` The public delegated prefix is a live migration
     prefix and is active.
     - `ANNOUNCED` The public delegated prefix is announced and ready to
     use.
     - `DELETING` The public delegated prefix is being deprovsioned.
     - `ACTIVE` The public delegated prefix is ready to use. |
| `byoip_api_version` | String | [Output Only] The version of BYOIP API. |
| `allocatable_prefix_length` | i64 | The allocatable prefix length supported by this public delegated prefix.
This field is optional and cannot be set for prefixes in DELEGATION mode.
It cannot be set for IPv4 prefixes either, and it always defaults to 32. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `ipv6_access_type` | String | [Output Only] The internet access type for IPv6 Public Delegated Prefixes.
Inherited from parent prefix. |
| `fingerprint` | String | Fingerprint of this resource. A hash of the contents stored in this object.
This field is used in optimistic locking. This field will be ignored when
inserting a new PublicDelegatedPrefix. An up-to-date
fingerprint must be provided in order to update thePublicDelegatedPrefix, otherwise the request will fail with
error 412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve a PublicDelegatedPrefix. |
| `is_live_migration` | bool | If true, the prefix will be live migrated. |
| `region` | String | [Output Only] URL of the region where the public delegated prefix resides.
This field applies only to the region resource. You must specify this
field as part of the HTTP request URL. It is not settable as a field in
the request body. |
| `mode` | String | The public delegated prefix mode for IPv6 only. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `kind` | String | [Output Only] Type of the resource. Alwayscompute#publicDelegatedPrefix for public delegated prefixes. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create global_public_delegated_prefixe
global_public_delegated_prefixe = provider.compute_api.Global_public_delegated_prefixe {
    project = "value"  # Project ID for this request.
}

# Access global_public_delegated_prefixe outputs
global_public_delegated_prefixe_id = global_public_delegated_prefixe.id
global_public_delegated_prefixe_public_delegated_sub_prefixs = global_public_delegated_prefixe.public_delegated_sub_prefixs
global_public_delegated_prefixe_self_link = global_public_delegated_prefixe.self_link
global_public_delegated_prefixe_ip_cidr_range = global_public_delegated_prefixe.ip_cidr_range
global_public_delegated_prefixe_parent_prefix = global_public_delegated_prefixe.parent_prefix
global_public_delegated_prefixe_name = global_public_delegated_prefixe.name
global_public_delegated_prefixe_id = global_public_delegated_prefixe.id
global_public_delegated_prefixe_status = global_public_delegated_prefixe.status
global_public_delegated_prefixe_byoip_api_version = global_public_delegated_prefixe.byoip_api_version
global_public_delegated_prefixe_allocatable_prefix_length = global_public_delegated_prefixe.allocatable_prefix_length
global_public_delegated_prefixe_description = global_public_delegated_prefixe.description
global_public_delegated_prefixe_ipv6_access_type = global_public_delegated_prefixe.ipv6_access_type
global_public_delegated_prefixe_fingerprint = global_public_delegated_prefixe.fingerprint
global_public_delegated_prefixe_is_live_migration = global_public_delegated_prefixe.is_live_migration
global_public_delegated_prefixe_region = global_public_delegated_prefixe.region
global_public_delegated_prefixe_mode = global_public_delegated_prefixe.mode
global_public_delegated_prefixe_creation_timestamp = global_public_delegated_prefixe.creation_timestamp
global_public_delegated_prefixe_kind = global_public_delegated_prefixe.kind
```

---


### Instance_setting

Get Instance settings.

**Operations**: ✅ Read ✅ Update

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `fingerprint` | String |  | Specifies a fingerprint for instance settings, which is essentially a hash
of the instance settings resource's contents and used for optimistic
locking. The fingerprint is initially generated by Compute Engine and
changes after every request to modify or update the instance settings
resource. You must always provide an up-to-date fingerprint hash in order
to update or change the resource, otherwise the request will fail with
error 412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve the resource. |
| `kind` | String |  | [Output Only] Type of the resource. Alwayscompute#instance_settings for instance settings. |
| `metadata` | String |  | The metadata key/value pairs assigned to all the instances in the
corresponding scope. |
| `zone` | String |  | [Output Only] URL of the zone where the resource resides
You must specify this field as part of the HTTP request URL. It is not
settable as a field in the request body. |
| `zone` | String | ✅ | The zone scoping this request. It should conform to RFC1035. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `fingerprint` | String | Specifies a fingerprint for instance settings, which is essentially a hash
of the instance settings resource's contents and used for optimistic
locking. The fingerprint is initially generated by Compute Engine and
changes after every request to modify or update the instance settings
resource. You must always provide an up-to-date fingerprint hash in order
to update or change the resource, otherwise the request will fail with
error 412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve the resource. |
| `kind` | String | [Output Only] Type of the resource. Alwayscompute#instance_settings for instance settings. |
| `metadata` | String | The metadata key/value pairs assigned to all the instances in the
corresponding scope. |
| `zone` | String | [Output Only] URL of the zone where the resource resides
You must specify this field as part of the HTTP request URL. It is not
settable as a field in the request body. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Access instance_setting outputs
instance_setting_id = instance_setting.id
instance_setting_fingerprint = instance_setting.fingerprint
instance_setting_kind = instance_setting.kind
instance_setting_metadata = instance_setting.metadata
instance_setting_zone = instance_setting.zone
```

---


### Future_reservation

Creates a new Future Reservation.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `status` | String |  | [Output only] Status of the Future Reservation |
| `time_window` | String |  | Time window for this Future Reservation. |
| `specific_sku_properties` | String |  | Future Reservation configuration to indicate instance properties and
total count. |
| `auto_delete_auto_created_reservations` | bool |  | Setting for enabling or disabling automatic deletion for auto-created
reservation. If set to true, auto-created reservations will be
deleted at Future Reservation's end time (default) or at user's defined
timestamp if any of the
[auto_created_reservations_delete_time, auto_created_reservations_duration]
values is specified.
For keeping auto-created reservation indefinitely, this value should be set
to false. |
| `name_prefix` | String |  | Name prefix for the reservations to be created at the time of
delivery. The name prefix must comply with RFC1035.
Maximum allowed length for name prefix is 20. Automatically created
reservations name format will be -date-####. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the future reservation. |
| `zone` | String |  | [Output Only] URL of the Zone where this future reservation resides. |
| `auto_created_reservations_delete_time` | String |  | Future timestamp when the FR auto-created reservations will be deleted by
Compute Engine. Format of this field must be a valid
href="https://www.ietf.org/rfc/rfc3339.txt">RFC3339 value. |
| `aggregate_reservation` | String |  | Aggregate reservation details for the future reservation. |
| `auto_created_reservations_duration` | String |  | Specifies the duration of auto-created reservations.
It represents relative time to future reservation start_time when
auto-created reservations will be automatically deleted by Compute
Engine. Duration time unit is represented as a count of seconds
and fractions of seconds at nanosecond resolution. |
| `id` | String |  | [Output Only] A unique identifier for this future reservation. The server
defines this identifier. |
| `self_link_with_id` | String |  | [Output Only] Server-defined URL for this resource with the resource id. |
| `specific_reservation_required` | bool |  | Indicates whether the auto-created reservation can be consumed by VMs with
affinity for "any" reservation. If the field is set, then only VMs that
target the reservation by name can consume from the delivered reservation. |
| `creation_timestamp` | String |  | [Output Only] The creation timestamp for this future reservation inRFC3339
text format. |
| `self_link` | String |  | [Output Only] Server-defined fully-qualified URL for this resource. |
| `deployment_type` | String |  | Type of the deployment requested as part of future reservation. |
| `kind` | String |  | [Output Only] Type of the resource. Alwayscompute#futureReservation for future reservations. |
| `planning_status` | String |  | Planning state before being submitted for evaluation |
| `scheduling_type` | String |  | Maintenance information for this reservation |
| `share_settings` | String |  | List of Projects/Folders to share with. |
| `reservation_name` | String |  | Name of reservations where the capacity is provisioned at the time of
delivery of  future reservations. If the reservation with the given name
does not exist already, it is created automatically at the time of Approval
with INACTIVE state till specified start-time. Either provide the
reservation_name or a name_prefix. |
| `enable_emergent_maintenance` | bool |  | Indicates if this group of VMs have emergent maintenance enabled. |
| `name` | String |  | The name of the resource, provided by the client when initially creating
the resource. The resource name must be 1-63 characters long, and comply
withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must be
a dash, lowercase letter, or digit, except the last character, which cannot
be a dash. |
| `commitment_info` | String |  | If not present, then FR will not deliver a new commitment or update an
existing commitment. |
| `reservation_mode` | String |  | The reservation mode which determines reservation-termination behavior and
expected pricing. |
| `zone` | String | ✅ | Name of the zone for this request. Name should conform to RFC1035. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `status` | String | [Output only] Status of the Future Reservation |
| `time_window` | String | Time window for this Future Reservation. |
| `specific_sku_properties` | String | Future Reservation configuration to indicate instance properties and
total count. |
| `auto_delete_auto_created_reservations` | bool | Setting for enabling or disabling automatic deletion for auto-created
reservation. If set to true, auto-created reservations will be
deleted at Future Reservation's end time (default) or at user's defined
timestamp if any of the
[auto_created_reservations_delete_time, auto_created_reservations_duration]
values is specified.
For keeping auto-created reservation indefinitely, this value should be set
to false. |
| `name_prefix` | String | Name prefix for the reservations to be created at the time of
delivery. The name prefix must comply with RFC1035.
Maximum allowed length for name prefix is 20. Automatically created
reservations name format will be -date-####. |
| `description` | String | An optional description of this resource. Provide this property when you
create the future reservation. |
| `zone` | String | [Output Only] URL of the Zone where this future reservation resides. |
| `auto_created_reservations_delete_time` | String | Future timestamp when the FR auto-created reservations will be deleted by
Compute Engine. Format of this field must be a valid
href="https://www.ietf.org/rfc/rfc3339.txt">RFC3339 value. |
| `aggregate_reservation` | String | Aggregate reservation details for the future reservation. |
| `auto_created_reservations_duration` | String | Specifies the duration of auto-created reservations.
It represents relative time to future reservation start_time when
auto-created reservations will be automatically deleted by Compute
Engine. Duration time unit is represented as a count of seconds
and fractions of seconds at nanosecond resolution. |
| `id` | String | [Output Only] A unique identifier for this future reservation. The server
defines this identifier. |
| `self_link_with_id` | String | [Output Only] Server-defined URL for this resource with the resource id. |
| `specific_reservation_required` | bool | Indicates whether the auto-created reservation can be consumed by VMs with
affinity for "any" reservation. If the field is set, then only VMs that
target the reservation by name can consume from the delivered reservation. |
| `creation_timestamp` | String | [Output Only] The creation timestamp for this future reservation inRFC3339
text format. |
| `self_link` | String | [Output Only] Server-defined fully-qualified URL for this resource. |
| `deployment_type` | String | Type of the deployment requested as part of future reservation. |
| `kind` | String | [Output Only] Type of the resource. Alwayscompute#futureReservation for future reservations. |
| `planning_status` | String | Planning state before being submitted for evaluation |
| `scheduling_type` | String | Maintenance information for this reservation |
| `share_settings` | String | List of Projects/Folders to share with. |
| `reservation_name` | String | Name of reservations where the capacity is provisioned at the time of
delivery of  future reservations. If the reservation with the given name
does not exist already, it is created automatically at the time of Approval
with INACTIVE state till specified start-time. Either provide the
reservation_name or a name_prefix. |
| `enable_emergent_maintenance` | bool | Indicates if this group of VMs have emergent maintenance enabled. |
| `name` | String | The name of the resource, provided by the client when initially creating
the resource. The resource name must be 1-63 characters long, and comply
withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must be
a dash, lowercase letter, or digit, except the last character, which cannot
be a dash. |
| `commitment_info` | String | If not present, then FR will not deliver a new commitment or update an
existing commitment. |
| `reservation_mode` | String | The reservation mode which determines reservation-termination behavior and
expected pricing. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create future_reservation
future_reservation = provider.compute_api.Future_reservation {
    zone = "value"  # Name of the zone for this request. Name should conform to RFC1035.
    project = "value"  # Project ID for this request.
}

# Access future_reservation outputs
future_reservation_id = future_reservation.id
future_reservation_status = future_reservation.status
future_reservation_time_window = future_reservation.time_window
future_reservation_specific_sku_properties = future_reservation.specific_sku_properties
future_reservation_auto_delete_auto_created_reservations = future_reservation.auto_delete_auto_created_reservations
future_reservation_name_prefix = future_reservation.name_prefix
future_reservation_description = future_reservation.description
future_reservation_zone = future_reservation.zone
future_reservation_auto_created_reservations_delete_time = future_reservation.auto_created_reservations_delete_time
future_reservation_aggregate_reservation = future_reservation.aggregate_reservation
future_reservation_auto_created_reservations_duration = future_reservation.auto_created_reservations_duration
future_reservation_id = future_reservation.id
future_reservation_self_link_with_id = future_reservation.self_link_with_id
future_reservation_specific_reservation_required = future_reservation.specific_reservation_required
future_reservation_creation_timestamp = future_reservation.creation_timestamp
future_reservation_self_link = future_reservation.self_link
future_reservation_deployment_type = future_reservation.deployment_type
future_reservation_kind = future_reservation.kind
future_reservation_planning_status = future_reservation.planning_status
future_reservation_scheduling_type = future_reservation.scheduling_type
future_reservation_share_settings = future_reservation.share_settings
future_reservation_reservation_name = future_reservation.reservation_name
future_reservation_enable_emergent_maintenance = future_reservation.enable_emergent_maintenance
future_reservation_name = future_reservation.name
future_reservation_commitment_info = future_reservation.commitment_info
future_reservation_reservation_mode = future_reservation.reservation_mode
```

---


### Target_http_proxie

Creates a TargetHttpProxy resource in the specified
project using the data included in the request.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `region` | String |  | [Output Only] URL of the region where the regional Target HTTP Proxy
resides. This field is not applicable to global Target HTTP Proxies. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `url_map` | String |  | URL to the UrlMap resource that defines the mapping from URL to
the BackendService. |
| `name` | String |  | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `http_keep_alive_timeout_sec` | i64 |  | Specifies how long to keep a connection open, after completing a response,
while there is no matching traffic (in seconds). If an HTTP keep-alive is
not specified, a default value (610 seconds) will be used.

For global external Application Load Balancers, the minimum allowed value
is 5 seconds and the maximum allowed value is 1200 seconds.

For classic Application Load Balancers, this option is not supported. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `kind` | String |  | [Output Only] Type of resource. Always compute#targetHttpProxy
for target HTTP proxies. |
| `fingerprint` | String |  | Fingerprint of this resource. A hash of the contents stored in this object.
This field is used in optimistic locking. This field will be ignored when
inserting a TargetHttpProxy. An up-to-date fingerprint must
be provided in order to patch/update the TargetHttpProxy; otherwise, the
request will fail with error 412 conditionNotMet. To see the
latest fingerprint, make a get() request to retrieve the
TargetHttpProxy. |
| `proxy_bind` | bool |  | This field only applies when the forwarding rule that references this
target proxy has a loadBalancingScheme set toINTERNAL_SELF_MANAGED.

When this field is set to true, Envoy proxies set up inbound
traffic interception and bind to the IP address and port specified in the
forwarding rule. This is generally useful when using Traffic Director to
configure Envoy as a gateway or middle proxy (in other words, not a
sidecar proxy). The Envoy proxy listens for inbound requests and handles
requests when it receives them.

The default is false. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `region` | String | [Output Only] URL of the region where the regional Target HTTP Proxy
resides. This field is not applicable to global Target HTTP Proxies. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `url_map` | String | URL to the UrlMap resource that defines the mapping from URL to
the BackendService. |
| `name` | String | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `http_keep_alive_timeout_sec` | i64 | Specifies how long to keep a connection open, after completing a response,
while there is no matching traffic (in seconds). If an HTTP keep-alive is
not specified, a default value (610 seconds) will be used.

For global external Application Load Balancers, the minimum allowed value
is 5 seconds and the maximum allowed value is 1200 seconds.

For classic Application Load Balancers, this option is not supported. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `kind` | String | [Output Only] Type of resource. Always compute#targetHttpProxy
for target HTTP proxies. |
| `fingerprint` | String | Fingerprint of this resource. A hash of the contents stored in this object.
This field is used in optimistic locking. This field will be ignored when
inserting a TargetHttpProxy. An up-to-date fingerprint must
be provided in order to patch/update the TargetHttpProxy; otherwise, the
request will fail with error 412 conditionNotMet. To see the
latest fingerprint, make a get() request to retrieve the
TargetHttpProxy. |
| `proxy_bind` | bool | This field only applies when the forwarding rule that references this
target proxy has a loadBalancingScheme set toINTERNAL_SELF_MANAGED.

When this field is set to true, Envoy proxies set up inbound
traffic interception and bind to the IP address and port specified in the
forwarding rule. This is generally useful when using Traffic Director to
configure Envoy as a gateway or middle proxy (in other words, not a
sidecar proxy). The Envoy proxy listens for inbound requests and handles
requests when it receives them.

The default is false. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create target_http_proxie
target_http_proxie = provider.compute_api.Target_http_proxie {
    project = "value"  # Project ID for this request.
}

# Access target_http_proxie outputs
target_http_proxie_id = target_http_proxie.id
target_http_proxie_region = target_http_proxie.region
target_http_proxie_self_link = target_http_proxie.self_link
target_http_proxie_url_map = target_http_proxie.url_map
target_http_proxie_name = target_http_proxie.name
target_http_proxie_http_keep_alive_timeout_sec = target_http_proxie.http_keep_alive_timeout_sec
target_http_proxie_creation_timestamp = target_http_proxie.creation_timestamp
target_http_proxie_description = target_http_proxie.description
target_http_proxie_kind = target_http_proxie.kind
target_http_proxie_fingerprint = target_http_proxie.fingerprint
target_http_proxie_proxy_bind = target_http_proxie.proxy_bind
target_http_proxie_id = target_http_proxie.id
```

---


### Instance

Creates an instance resource in the specified project using the data
included in the request.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `tags` | String |  | Tags to apply to this instance. Tags are used to identify valid
sources or targets for network firewalls and are specified by the client
during instance creation. The tags can be later modified by the setTags
method. Each tag within the list must comply withRFC1035.
Multiple tags can be specified via the 'tags.items' field. |
| `metadata` | String |  | The metadata key/value pairs assigned
to this instance. This includes metadata keys that were explicitly defined
for the instance. |
| `cpu_platform` | String |  | [Output Only] The CPU platform used by this instance. |
| `source_machine_image` | String |  | Source machine image |
| `label_fingerprint` | String |  | A fingerprint for this request, which is essentially a hash of
the label's contents and used for optimistic locking. The
fingerprint is initially generated by Compute Engine and changes after
every request to modify or update labels. You must always provide an
up-to-date fingerprint hash in order to update or change labels.

To see the latest fingerprint, make get() request to the
instance. |
| `can_ip_forward` | bool |  | Allows this instance to send and receive packets with non-matching
destination or source IPs. This is required if you plan to use this
instance to forward routes. For more information, seeEnabling IP Forwarding. |
| `last_stop_timestamp` | String |  | [Output Only] Last stop timestamp inRFC3339 text format. |
| `network_performance_config` | String |  |  |
| `machine_type` | String |  | Full or partial URL of the machine type resource to use for this instance,
in the format:zones/zone/machineTypes/machine-type. This is provided by the client
when the instance is created. For example, the following is a valid partial
url to a predefined
machine type:

zones/us-central1-f/machineTypes/n1-standard-1


To create acustom
machine type, provide a URL to a machine type in the following format,
where CPUS is 1 or an even number up to 32 (2,
4, 6, ... 24, etc), and MEMORY is the total
memory for this instance. Memory must be a multiple of 256 MB and must
be supplied in MB (e.g. 5 GB of memory is 5120 MB):

zones/zone/machineTypes/custom-CPUS-MEMORY


For example: zones/us-central1-f/machineTypes/custom-4-5120
For a full list of restrictions, read theSpecifications
for custom machine types. |
| `name` | String |  | The name of the resource, provided by the client when initially creating
the resource. The resource name must be 1-63 characters long, and comply
withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must be
a dash, lowercase letter, or digit, except the last character, which cannot
be a dash. |
| `disks` | Vec<String> |  | Array of disks associated with this instance. Persistent disks must be
created before you can assign them. |
| `self_link` | String |  | [Output Only] Server-defined URL for this resource. |
| `key_revocation_action_type` | String |  | KeyRevocationActionType of the instance. Supported options are "STOP" and
"NONE". The default value is "NONE" if it is not specified. |
| `display_device` | String |  | Enables display device for the instance. |
| `last_suspended_timestamp` | String |  | [Output Only] Last suspended timestamp inRFC3339 text format. |
| `shielded_instance_integrity_policy` | String |  |  |
| `status_message` | String |  | [Output Only] An optional, human-readable explanation of the status. |
| `network_interfaces` | Vec<String> |  | An array of network configurations for this instance. These specify how
interfaces are configured to interact with other network services, such as
connecting to the internet. Multiple interfaces are supported
per instance. |
| `guest_accelerators` | Vec<String> |  | A list of the type and count of accelerator cards attached to the instance. |
| `min_cpu_platform` | String |  | Specifies aminimum CPU
platform for the VM instance. Applicable values are the friendly names
of CPU platforms, such as minCpuPlatform: "Intel
Haswell" or minCpuPlatform: "Intel Sandy
Bridge". |
| `shielded_instance_config` | String |  |  |
| `advanced_machine_features` | String |  | Controls for advanced machine-related behavior features. |
| `params` | String |  | Input only. [Input Only] Additional params passed with the request, but not persisted
as part of resource payload. |
| `deletion_protection` | bool |  | Whether the resource should be protected against deletion. |
| `fingerprint` | String |  | Specifies a fingerprint for this resource, which is essentially a hash of
the instance's contents and used for optimistic locking. The
fingerprint is initially generated by Compute Engine and changes after
every request to modify or update the instance. You must always provide an
up-to-date fingerprint hash in order to update the instance.

To see the latest fingerprint, make get() request to the
instance. |
| `last_start_timestamp` | String |  | [Output Only] Last start timestamp inRFC3339 text format. |
| `satisfies_pzi` | bool |  | [Output Only] Reserved for future use. |
| `instance_encryption_key` | String |  | Encrypts suspended data for an instance with acustomer-managed
encryption key.

If you are creating a new instance, this field will encrypt the local SSD
and in-memory contents of the instance during the suspend operation.

If you do not provide an encryption key when creating the instance, then
the local SSD and in-memory contents will be encrypted using
an automatically generated key during the suspend operation. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `private_ipv6_google_access` | String |  | The private IPv6 google access type for the VM.
If not specified, use  INHERIT_FROM_SUBNETWORK as default. |
| `satisfies_pzs` | bool |  | [Output Only] Reserved for future use. |
| `service_accounts` | Vec<String> |  | A list of service accounts, with their specified scopes, authorized for
this instance. Only one service account per VM instance is supported.

Service accounts generate access tokens that can be accessed
through the metadata server and used to authenticate applications on the
instance. SeeService Accounts
for more information. |
| `start_restricted` | bool |  | [Output Only] Whether a VM has been restricted for start because Compute
Engine has detected suspicious activity. |
| `resource_policies` | Vec<String> |  | Resource policies applied to this instance. |
| `kind` | String |  | [Output Only] Type of the resource. Always compute#instance
for instances. |
| `reservation_affinity` | String |  | Specifies the reservations that this instance can consume from. |
| `scheduling` | String |  | Sets the scheduling options for this instance. |
| `source_machine_image_encryption_key` | String |  | Source machine image encryption key when creating an instance from a
machine image. |
| `status` | String |  | [Output Only] The status of the instance. One of the
following values: PROVISIONING, STAGING,RUNNING, STOPPING, SUSPENDING,SUSPENDED, REPAIRING, andTERMINATED. For more information about the status of the
instance, see 
Instance life cycle. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339 text format. |
| `hostname` | String |  | Specifies the hostname of the instance. The specified hostname must be
RFC1035 compliant. If hostname is not specified, the default hostname is
[INSTANCE_NAME].c.[PROJECT_ID].internal when using the global DNS, and
[INSTANCE_NAME].[ZONE].c.[PROJECT_ID].internal when using zonal DNS. |
| `confidential_instance_config` | String |  |  |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `zone` | String |  | [Output Only] URL of the zone where the instance resides.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `labels` | HashMap<String, String> |  | Labels to apply to this instance. These can be later modified by
the setLabels method. |
| `resource_status` | String |  | [Output Only] Specifies values set for instance attributes as
compared to the values requested by user in the corresponding input only
field. |
| `project` | String | ✅ | Project ID for this request. |
| `zone` | String | ✅ | The name of the zone for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `tags` | String | Tags to apply to this instance. Tags are used to identify valid
sources or targets for network firewalls and are specified by the client
during instance creation. The tags can be later modified by the setTags
method. Each tag within the list must comply withRFC1035.
Multiple tags can be specified via the 'tags.items' field. |
| `metadata` | String | The metadata key/value pairs assigned
to this instance. This includes metadata keys that were explicitly defined
for the instance. |
| `cpu_platform` | String | [Output Only] The CPU platform used by this instance. |
| `source_machine_image` | String | Source machine image |
| `label_fingerprint` | String | A fingerprint for this request, which is essentially a hash of
the label's contents and used for optimistic locking. The
fingerprint is initially generated by Compute Engine and changes after
every request to modify or update labels. You must always provide an
up-to-date fingerprint hash in order to update or change labels.

To see the latest fingerprint, make get() request to the
instance. |
| `can_ip_forward` | bool | Allows this instance to send and receive packets with non-matching
destination or source IPs. This is required if you plan to use this
instance to forward routes. For more information, seeEnabling IP Forwarding. |
| `last_stop_timestamp` | String | [Output Only] Last stop timestamp inRFC3339 text format. |
| `network_performance_config` | String |  |
| `machine_type` | String | Full or partial URL of the machine type resource to use for this instance,
in the format:zones/zone/machineTypes/machine-type. This is provided by the client
when the instance is created. For example, the following is a valid partial
url to a predefined
machine type:

zones/us-central1-f/machineTypes/n1-standard-1


To create acustom
machine type, provide a URL to a machine type in the following format,
where CPUS is 1 or an even number up to 32 (2,
4, 6, ... 24, etc), and MEMORY is the total
memory for this instance. Memory must be a multiple of 256 MB and must
be supplied in MB (e.g. 5 GB of memory is 5120 MB):

zones/zone/machineTypes/custom-CPUS-MEMORY


For example: zones/us-central1-f/machineTypes/custom-4-5120
For a full list of restrictions, read theSpecifications
for custom machine types. |
| `name` | String | The name of the resource, provided by the client when initially creating
the resource. The resource name must be 1-63 characters long, and comply
withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must be
a dash, lowercase letter, or digit, except the last character, which cannot
be a dash. |
| `disks` | Vec<String> | Array of disks associated with this instance. Persistent disks must be
created before you can assign them. |
| `self_link` | String | [Output Only] Server-defined URL for this resource. |
| `key_revocation_action_type` | String | KeyRevocationActionType of the instance. Supported options are "STOP" and
"NONE". The default value is "NONE" if it is not specified. |
| `display_device` | String | Enables display device for the instance. |
| `last_suspended_timestamp` | String | [Output Only] Last suspended timestamp inRFC3339 text format. |
| `shielded_instance_integrity_policy` | String |  |
| `status_message` | String | [Output Only] An optional, human-readable explanation of the status. |
| `network_interfaces` | Vec<String> | An array of network configurations for this instance. These specify how
interfaces are configured to interact with other network services, such as
connecting to the internet. Multiple interfaces are supported
per instance. |
| `guest_accelerators` | Vec<String> | A list of the type and count of accelerator cards attached to the instance. |
| `min_cpu_platform` | String | Specifies aminimum CPU
platform for the VM instance. Applicable values are the friendly names
of CPU platforms, such as minCpuPlatform: "Intel
Haswell" or minCpuPlatform: "Intel Sandy
Bridge". |
| `shielded_instance_config` | String |  |
| `advanced_machine_features` | String | Controls for advanced machine-related behavior features. |
| `params` | String | Input only. [Input Only] Additional params passed with the request, but not persisted
as part of resource payload. |
| `deletion_protection` | bool | Whether the resource should be protected against deletion. |
| `fingerprint` | String | Specifies a fingerprint for this resource, which is essentially a hash of
the instance's contents and used for optimistic locking. The
fingerprint is initially generated by Compute Engine and changes after
every request to modify or update the instance. You must always provide an
up-to-date fingerprint hash in order to update the instance.

To see the latest fingerprint, make get() request to the
instance. |
| `last_start_timestamp` | String | [Output Only] Last start timestamp inRFC3339 text format. |
| `satisfies_pzi` | bool | [Output Only] Reserved for future use. |
| `instance_encryption_key` | String | Encrypts suspended data for an instance with acustomer-managed
encryption key.

If you are creating a new instance, this field will encrypt the local SSD
and in-memory contents of the instance during the suspend operation.

If you do not provide an encryption key when creating the instance, then
the local SSD and in-memory contents will be encrypted using
an automatically generated key during the suspend operation. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `private_ipv6_google_access` | String | The private IPv6 google access type for the VM.
If not specified, use  INHERIT_FROM_SUBNETWORK as default. |
| `satisfies_pzs` | bool | [Output Only] Reserved for future use. |
| `service_accounts` | Vec<String> | A list of service accounts, with their specified scopes, authorized for
this instance. Only one service account per VM instance is supported.

Service accounts generate access tokens that can be accessed
through the metadata server and used to authenticate applications on the
instance. SeeService Accounts
for more information. |
| `start_restricted` | bool | [Output Only] Whether a VM has been restricted for start because Compute
Engine has detected suspicious activity. |
| `resource_policies` | Vec<String> | Resource policies applied to this instance. |
| `kind` | String | [Output Only] Type of the resource. Always compute#instance
for instances. |
| `reservation_affinity` | String | Specifies the reservations that this instance can consume from. |
| `scheduling` | String | Sets the scheduling options for this instance. |
| `source_machine_image_encryption_key` | String | Source machine image encryption key when creating an instance from a
machine image. |
| `status` | String | [Output Only] The status of the instance. One of the
following values: PROVISIONING, STAGING,RUNNING, STOPPING, SUSPENDING,SUSPENDED, REPAIRING, andTERMINATED. For more information about the status of the
instance, see 
Instance life cycle. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339 text format. |
| `hostname` | String | Specifies the hostname of the instance. The specified hostname must be
RFC1035 compliant. If hostname is not specified, the default hostname is
[INSTANCE_NAME].c.[PROJECT_ID].internal when using the global DNS, and
[INSTANCE_NAME].[ZONE].c.[PROJECT_ID].internal when using zonal DNS. |
| `confidential_instance_config` | String |  |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `zone` | String | [Output Only] URL of the zone where the instance resides.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `labels` | HashMap<String, String> | Labels to apply to this instance. These can be later modified by
the setLabels method. |
| `resource_status` | String | [Output Only] Specifies values set for instance attributes as
compared to the values requested by user in the corresponding input only
field. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create instance
instance = provider.compute_api.Instance {
    project = "value"  # Project ID for this request.
    zone = "value"  # The name of the zone for this request.
}

# Access instance outputs
instance_id = instance.id
instance_tags = instance.tags
instance_metadata = instance.metadata
instance_cpu_platform = instance.cpu_platform
instance_source_machine_image = instance.source_machine_image
instance_label_fingerprint = instance.label_fingerprint
instance_can_ip_forward = instance.can_ip_forward
instance_last_stop_timestamp = instance.last_stop_timestamp
instance_network_performance_config = instance.network_performance_config
instance_machine_type = instance.machine_type
instance_name = instance.name
instance_disks = instance.disks
instance_self_link = instance.self_link
instance_key_revocation_action_type = instance.key_revocation_action_type
instance_display_device = instance.display_device
instance_last_suspended_timestamp = instance.last_suspended_timestamp
instance_shielded_instance_integrity_policy = instance.shielded_instance_integrity_policy
instance_status_message = instance.status_message
instance_network_interfaces = instance.network_interfaces
instance_guest_accelerators = instance.guest_accelerators
instance_min_cpu_platform = instance.min_cpu_platform
instance_shielded_instance_config = instance.shielded_instance_config
instance_advanced_machine_features = instance.advanced_machine_features
instance_params = instance.params
instance_deletion_protection = instance.deletion_protection
instance_fingerprint = instance.fingerprint
instance_last_start_timestamp = instance.last_start_timestamp
instance_satisfies_pzi = instance.satisfies_pzi
instance_instance_encryption_key = instance.instance_encryption_key
instance_description = instance.description
instance_private_ipv6_google_access = instance.private_ipv6_google_access
instance_satisfies_pzs = instance.satisfies_pzs
instance_service_accounts = instance.service_accounts
instance_start_restricted = instance.start_restricted
instance_resource_policies = instance.resource_policies
instance_kind = instance.kind
instance_reservation_affinity = instance.reservation_affinity
instance_scheduling = instance.scheduling
instance_source_machine_image_encryption_key = instance.source_machine_image_encryption_key
instance_status = instance.status
instance_creation_timestamp = instance.creation_timestamp
instance_hostname = instance.hostname
instance_confidential_instance_config = instance.confidential_instance_config
instance_id = instance.id
instance_zone = instance.zone
instance_labels = instance.labels
instance_resource_status = instance.resource_status
```

---


### Snapshot

Creates a snapshot in the specified project using the data included
in the request. For regular snapshot creation, consider using this method
instead of disks.createSnapshot,
as this method supports more features, such as creating snapshots in a
project different from the source disk project.

**Operations**: ✅ Create ✅ Read ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `guest_flush` | bool |  | [Input Only] Whether to attempt an application consistent snapshot by
informing the OS to prepare for the snapshot process. |
| `storage_bytes` | String |  | [Output Only] A size of the storage used by the snapshot. As snapshots
share storage, this number is expected to change with snapshot
creation/deletion. |
| `architecture` | String |  | [Output Only] The architecture of the snapshot. Valid values are
ARM64 or X86_64. |
| `snapshot_encryption_key` | String |  | Encrypts the snapshot using acustomer-supplied
encryption key.

After you encrypt a snapshot using a customer-supplied key, you must
provide the same key if you use the snapshot later. For example, you must
provide the encryption key when you create a disk from the encrypted
snapshot in a future request.

Customer-supplied encryption keys do not protect access to metadata
of the snapshot.

If you do not provide an encryption key when creating the snapshot, then
the snapshot will be encrypted using an automatically generated key and you
do not need to provide a key to use the snapshot later. |
| `source_instant_snapshot_id` | String |  | [Output Only] The unique ID of the instant snapshot used to create this
snapshot. This value identifies the exact instant snapshot that was used to
create this snapshot. For example, if you created the snapshot from an
instant snapshot that was later deleted and recreated under the same name,
the source instant snapshot ID would identify the exact instant snapshot
that was used. |
| `storage_bytes_status` | String |  | [Output Only] An indicator whether storageBytes is in a
stable state or it is being adjusted as a result of shared storage
reallocation. This status can either be UPDATING, meaning
the size of the snapshot is being updated, or UP_TO_DATE,
meaning the size of the snapshot is up-to-date. |
| `params` | String |  | Input only. [Input Only] Additional params passed with the request, but not persisted
as part of resource payload. |
| `disk_size_gb` | String |  | [Output Only] Size of the source disk, specified in GB. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `source_snapshot_schedule_policy_id` | String |  | [Output Only] ID of the resource policy which created this
scheduled snapshot. |
| `chain_name` | String |  | Creates the new snapshot in the snapshot chain labeled with the
specified name. The chain name must be 1-63 characters long and comply
with RFC1035. This is an uncommon option only for advanced service
owners who needs to create separate snapshot chains, for example,
for chargeback tracking. When you describe your snapshot resource, this
field is visible only if it has a non-empty value. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `source_disk` | String |  | The source disk used to create this snapshot. |
| `source_snapshot_schedule_policy` | String |  | [Output Only] URL of the resource policy which created this
scheduled snapshot. |
| `status` | String |  | [Output Only] The status of the snapshot. This can beCREATING, DELETING, FAILED,READY, or UPLOADING. |
| `storage_locations` | Vec<String> |  | Cloud Storage bucket storage location of the snapshot (regional or
multi-regional). |
| `source_disk_for_recovery_checkpoint` | String |  | The source disk whose recovery checkpoint will be used to create this
snapshot. |
| `license_codes` | Vec<String> |  | [Output Only] Integer license codes indicating which licenses are attached
to this snapshot. |
| `source_disk_id` | String |  | [Output Only] The ID value of the disk used to create this snapshot. This
value may be used to determine whether the snapshot was taken from the
current or a previous instance of a given disk name. |
| `download_bytes` | String |  | [Output Only] Number of bytes downloaded to restore a snapshot to a disk. |
| `labels` | HashMap<String, String> |  | Labels to apply to this snapshot. These can be later modified by
the setLabels method.
Label values may be empty. |
| `licenses` | Vec<String> |  | [Output Only] A list of public visible licenses that apply to this
snapshot. This can be because the original image had licenses attached
(such as a Windows image). |
| `source_instant_snapshot` | String |  | The source instant snapshot used to create this snapshot. You can provide
this as a partial or full URL to the resource. For example, the following
are valid values:
   
   
     - https://www.googleapis.com/compute/v1/projects/project/zones/zone/instantSnapshots/instantSnapshot
     - projects/project/zones/zone/instantSnapshots/instantSnapshot 
   - zones/zone/instantSnapshots/instantSnapshot |
| `snapshot_type` | String |  | Indicates the type of the snapshot. |
| `source_disk_encryption_key` | String |  | The customer-supplied
encryption key of the source disk. Required if the source disk is
protected by a customer-supplied encryption key. |
| `location_hint` | String |  | An opaque location hint used to place the snapshot close to other
resources.
This field is for use by internal tools that use the public API. |
| `auto_created` | bool |  | [Output Only] Set to true if snapshots are automatically created by
applying resource policy on the target disk. |
| `name` | String |  | Name of the resource; provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must be
a dash, lowercase letter, or digit, except the last character, which cannot
be a dash. |
| `satisfies_pzi` | bool |  | Output only. Reserved for future use. |
| `satisfies_pzs` | bool |  | [Output Only] Reserved for future use. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `label_fingerprint` | String |  | A fingerprint for the labels being applied to this snapshot, which is
essentially a hash of the labels set used for optimistic locking. The
fingerprint is initially generated by Compute Engine and changes after
every request to modify or update labels. You must always provide an
up-to-date fingerprint hash in order to update or change labels,
otherwise the request will fail with error412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve a snapshot. |
| `enable_confidential_compute` | bool |  | Whether this snapshot is created from a confidential compute mode disk.
[Output Only]: This field is not set by user, but from source disk. |
| `creation_size_bytes` | String |  | [Output Only] Size in bytes of the snapshot at creation time. |
| `source_instant_snapshot_encryption_key` | String |  | Customer provided encryption key when creating Snapshot from Instant
Snapshot. |
| `kind` | String |  | [Output Only] Type of the resource. Always compute#snapshot
for Snapshot resources. |
| `guest_os_features` | Vec<String> |  | [Output Only] A list of features to enable on the guest operating system.
Applicable only for bootable images. Read 
Enabling guest operating system features to see a list of available
options. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `guest_flush` | bool | [Input Only] Whether to attempt an application consistent snapshot by
informing the OS to prepare for the snapshot process. |
| `storage_bytes` | String | [Output Only] A size of the storage used by the snapshot. As snapshots
share storage, this number is expected to change with snapshot
creation/deletion. |
| `architecture` | String | [Output Only] The architecture of the snapshot. Valid values are
ARM64 or X86_64. |
| `snapshot_encryption_key` | String | Encrypts the snapshot using acustomer-supplied
encryption key.

After you encrypt a snapshot using a customer-supplied key, you must
provide the same key if you use the snapshot later. For example, you must
provide the encryption key when you create a disk from the encrypted
snapshot in a future request.

Customer-supplied encryption keys do not protect access to metadata
of the snapshot.

If you do not provide an encryption key when creating the snapshot, then
the snapshot will be encrypted using an automatically generated key and you
do not need to provide a key to use the snapshot later. |
| `source_instant_snapshot_id` | String | [Output Only] The unique ID of the instant snapshot used to create this
snapshot. This value identifies the exact instant snapshot that was used to
create this snapshot. For example, if you created the snapshot from an
instant snapshot that was later deleted and recreated under the same name,
the source instant snapshot ID would identify the exact instant snapshot
that was used. |
| `storage_bytes_status` | String | [Output Only] An indicator whether storageBytes is in a
stable state or it is being adjusted as a result of shared storage
reallocation. This status can either be UPDATING, meaning
the size of the snapshot is being updated, or UP_TO_DATE,
meaning the size of the snapshot is up-to-date. |
| `params` | String | Input only. [Input Only] Additional params passed with the request, but not persisted
as part of resource payload. |
| `disk_size_gb` | String | [Output Only] Size of the source disk, specified in GB. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `source_snapshot_schedule_policy_id` | String | [Output Only] ID of the resource policy which created this
scheduled snapshot. |
| `chain_name` | String | Creates the new snapshot in the snapshot chain labeled with the
specified name. The chain name must be 1-63 characters long and comply
with RFC1035. This is an uncommon option only for advanced service
owners who needs to create separate snapshot chains, for example,
for chargeback tracking. When you describe your snapshot resource, this
field is visible only if it has a non-empty value. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `source_disk` | String | The source disk used to create this snapshot. |
| `source_snapshot_schedule_policy` | String | [Output Only] URL of the resource policy which created this
scheduled snapshot. |
| `status` | String | [Output Only] The status of the snapshot. This can beCREATING, DELETING, FAILED,READY, or UPLOADING. |
| `storage_locations` | Vec<String> | Cloud Storage bucket storage location of the snapshot (regional or
multi-regional). |
| `source_disk_for_recovery_checkpoint` | String | The source disk whose recovery checkpoint will be used to create this
snapshot. |
| `license_codes` | Vec<String> | [Output Only] Integer license codes indicating which licenses are attached
to this snapshot. |
| `source_disk_id` | String | [Output Only] The ID value of the disk used to create this snapshot. This
value may be used to determine whether the snapshot was taken from the
current or a previous instance of a given disk name. |
| `download_bytes` | String | [Output Only] Number of bytes downloaded to restore a snapshot to a disk. |
| `labels` | HashMap<String, String> | Labels to apply to this snapshot. These can be later modified by
the setLabels method.
Label values may be empty. |
| `licenses` | Vec<String> | [Output Only] A list of public visible licenses that apply to this
snapshot. This can be because the original image had licenses attached
(such as a Windows image). |
| `source_instant_snapshot` | String | The source instant snapshot used to create this snapshot. You can provide
this as a partial or full URL to the resource. For example, the following
are valid values:
   
   
     - https://www.googleapis.com/compute/v1/projects/project/zones/zone/instantSnapshots/instantSnapshot
     - projects/project/zones/zone/instantSnapshots/instantSnapshot 
   - zones/zone/instantSnapshots/instantSnapshot |
| `snapshot_type` | String | Indicates the type of the snapshot. |
| `source_disk_encryption_key` | String | The customer-supplied
encryption key of the source disk. Required if the source disk is
protected by a customer-supplied encryption key. |
| `location_hint` | String | An opaque location hint used to place the snapshot close to other
resources.
This field is for use by internal tools that use the public API. |
| `auto_created` | bool | [Output Only] Set to true if snapshots are automatically created by
applying resource policy on the target disk. |
| `name` | String | Name of the resource; provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must be
a dash, lowercase letter, or digit, except the last character, which cannot
be a dash. |
| `satisfies_pzi` | bool | Output only. Reserved for future use. |
| `satisfies_pzs` | bool | [Output Only] Reserved for future use. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `label_fingerprint` | String | A fingerprint for the labels being applied to this snapshot, which is
essentially a hash of the labels set used for optimistic locking. The
fingerprint is initially generated by Compute Engine and changes after
every request to modify or update labels. You must always provide an
up-to-date fingerprint hash in order to update or change labels,
otherwise the request will fail with error412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve a snapshot. |
| `enable_confidential_compute` | bool | Whether this snapshot is created from a confidential compute mode disk.
[Output Only]: This field is not set by user, but from source disk. |
| `creation_size_bytes` | String | [Output Only] Size in bytes of the snapshot at creation time. |
| `source_instant_snapshot_encryption_key` | String | Customer provided encryption key when creating Snapshot from Instant
Snapshot. |
| `kind` | String | [Output Only] Type of the resource. Always compute#snapshot
for Snapshot resources. |
| `guest_os_features` | Vec<String> | [Output Only] A list of features to enable on the guest operating system.
Applicable only for bootable images. Read 
Enabling guest operating system features to see a list of available
options. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create snapshot
snapshot = provider.compute_api.Snapshot {
    project = "value"  # Project ID for this request.
}

# Access snapshot outputs
snapshot_id = snapshot.id
snapshot_guest_flush = snapshot.guest_flush
snapshot_storage_bytes = snapshot.storage_bytes
snapshot_architecture = snapshot.architecture
snapshot_snapshot_encryption_key = snapshot.snapshot_encryption_key
snapshot_source_instant_snapshot_id = snapshot.source_instant_snapshot_id
snapshot_storage_bytes_status = snapshot.storage_bytes_status
snapshot_params = snapshot.params
snapshot_disk_size_gb = snapshot.disk_size_gb
snapshot_description = snapshot.description
snapshot_source_snapshot_schedule_policy_id = snapshot.source_snapshot_schedule_policy_id
snapshot_chain_name = snapshot.chain_name
snapshot_self_link = snapshot.self_link
snapshot_source_disk = snapshot.source_disk
snapshot_source_snapshot_schedule_policy = snapshot.source_snapshot_schedule_policy
snapshot_status = snapshot.status
snapshot_storage_locations = snapshot.storage_locations
snapshot_source_disk_for_recovery_checkpoint = snapshot.source_disk_for_recovery_checkpoint
snapshot_license_codes = snapshot.license_codes
snapshot_source_disk_id = snapshot.source_disk_id
snapshot_download_bytes = snapshot.download_bytes
snapshot_labels = snapshot.labels
snapshot_licenses = snapshot.licenses
snapshot_source_instant_snapshot = snapshot.source_instant_snapshot
snapshot_snapshot_type = snapshot.snapshot_type
snapshot_source_disk_encryption_key = snapshot.source_disk_encryption_key
snapshot_location_hint = snapshot.location_hint
snapshot_auto_created = snapshot.auto_created
snapshot_name = snapshot.name
snapshot_satisfies_pzi = snapshot.satisfies_pzi
snapshot_satisfies_pzs = snapshot.satisfies_pzs
snapshot_id = snapshot.id
snapshot_creation_timestamp = snapshot.creation_timestamp
snapshot_label_fingerprint = snapshot.label_fingerprint
snapshot_enable_confidential_compute = snapshot.enable_confidential_compute
snapshot_creation_size_bytes = snapshot.creation_size_bytes
snapshot_source_instant_snapshot_encryption_key = snapshot.source_instant_snapshot_encryption_key
snapshot_kind = snapshot.kind
snapshot_guest_os_features = snapshot.guest_os_features
```

---


### Instance_template

Creates an instance template in the specified project using the
data that is included in the request. If you are creating a new template to
update an existing instance group, your new instance template must use the
same network or, if applicable, the same subnetwork as the original
template.

**Operations**: ✅ Create ✅ Read ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `name` | String |  | Name of the resource; provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must be
a dash, lowercase letter, or digit, except the last character, which cannot
be a dash. |
| `source_instance_params` | String |  | The source instance params to use to create this instance template. |
| `creation_timestamp` | String |  | [Output Only] The creation timestamp for this instance template inRFC3339
text format. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `kind` | String |  | [Output Only] The resource type, which is alwayscompute#instanceTemplate for instance templates. |
| `self_link` | String |  | [Output Only] The URL for this instance template. The server defines this
URL. |
| `region` | String |  | [Output Only] URL of the region where the instance template resides. Only
applicable for regional resources. |
| `source_instance` | String |  | The source instance used to create the template. You can provide this as a
partial or full URL to the resource. For example, the following are valid
values:
   
   
     - https://www.googleapis.com/compute/v1/projects/project/zones/zone/instances/instance 
   - projects/project/zones/zone/instances/instance |
| `id` | String |  | [Output Only] A unique identifier for this instance template. The server
defines this identifier. |
| `properties` | String |  | The instance properties for this instance template. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `name` | String | Name of the resource; provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must be
a dash, lowercase letter, or digit, except the last character, which cannot
be a dash. |
| `source_instance_params` | String | The source instance params to use to create this instance template. |
| `creation_timestamp` | String | [Output Only] The creation timestamp for this instance template inRFC3339
text format. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `kind` | String | [Output Only] The resource type, which is alwayscompute#instanceTemplate for instance templates. |
| `self_link` | String | [Output Only] The URL for this instance template. The server defines this
URL. |
| `region` | String | [Output Only] URL of the region where the instance template resides. Only
applicable for regional resources. |
| `source_instance` | String | The source instance used to create the template. You can provide this as a
partial or full URL to the resource. For example, the following are valid
values:
   
   
     - https://www.googleapis.com/compute/v1/projects/project/zones/zone/instances/instance 
   - projects/project/zones/zone/instances/instance |
| `id` | String | [Output Only] A unique identifier for this instance template. The server
defines this identifier. |
| `properties` | String | The instance properties for this instance template. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create instance_template
instance_template = provider.compute_api.Instance_template {
    project = "value"  # Project ID for this request.
}

# Access instance_template outputs
instance_template_id = instance_template.id
instance_template_name = instance_template.name
instance_template_source_instance_params = instance_template.source_instance_params
instance_template_creation_timestamp = instance_template.creation_timestamp
instance_template_description = instance_template.description
instance_template_kind = instance_template.kind
instance_template_self_link = instance_template.self_link
instance_template_region = instance_template.region
instance_template_source_instance = instance_template.source_instance
instance_template_id = instance_template.id
instance_template_properties = instance_template.properties
```

---


### Region_notification_endpoint

Create a NotificationEndpoint in the specified project in the given region
using the parameters that are included in the request.

**Operations**: ✅ Create ✅ Read ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `kind` | String |  | [Output Only] Type of the resource. Alwayscompute#notificationEndpoint for notification endpoints. |
| `grpc_settings` | String |  | Settings of the gRPC notification endpoint including the endpoint URL and
the retry duration. |
| `id` | String |  | [Output Only] A unique identifier for this resource type. The server
generates this identifier. |
| `name` | String |  | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `region` | String |  | [Output Only] URL of the region where the notification endpoint resides.
This field applies only to the regional resource. You must specify this
field as part of the HTTP request URL. It is not settable as a field in
the request body. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `project` | String | ✅ | Project ID for this request. |
| `region` | String | ✅ | Name of the region scoping this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `kind` | String | [Output Only] Type of the resource. Alwayscompute#notificationEndpoint for notification endpoints. |
| `grpc_settings` | String | Settings of the gRPC notification endpoint including the endpoint URL and
the retry duration. |
| `id` | String | [Output Only] A unique identifier for this resource type. The server
generates this identifier. |
| `name` | String | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `region` | String | [Output Only] URL of the region where the notification endpoint resides.
This field applies only to the regional resource. You must specify this
field as part of the HTTP request URL. It is not settable as a field in
the request body. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create region_notification_endpoint
region_notification_endpoint = provider.compute_api.Region_notification_endpoint {
    project = "value"  # Project ID for this request.
    region = "value"  # Name of the region scoping this request.
}

# Access region_notification_endpoint outputs
region_notification_endpoint_id = region_notification_endpoint.id
region_notification_endpoint_kind = region_notification_endpoint.kind
region_notification_endpoint_grpc_settings = region_notification_endpoint.grpc_settings
region_notification_endpoint_id = region_notification_endpoint.id
region_notification_endpoint_name = region_notification_endpoint.name
region_notification_endpoint_description = region_notification_endpoint.description
region_notification_endpoint_region = region_notification_endpoint.region
region_notification_endpoint_creation_timestamp = region_notification_endpoint.creation_timestamp
region_notification_endpoint_self_link = region_notification_endpoint.self_link
```

---


### Network_endpoint_group

Creates a network endpoint group in the specified project using the
parameters that are included in the request.

**Operations**: ✅ Create ✅ Read ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `psc_data` | String |  | Optional. Only valid when networkEndpointType isPRIVATE_SERVICE_CONNECT. |
| `region` | String |  | [Output Only] The URL of theregion
where the network endpoint group is located. |
| `size` | i64 |  | [Output only] Number of network endpoints in the network endpoint group. |
| `network_endpoint_type` | String |  | Type of network endpoints in this network endpoint group. Can be one ofGCE_VM_IP, GCE_VM_IP_PORT,NON_GCP_PRIVATE_IP_PORT, INTERNET_FQDN_PORT,INTERNET_IP_PORT, SERVERLESS,PRIVATE_SERVICE_CONNECT, GCE_VM_IP_PORTMAP. |
| `annotations` | HashMap<String, String> |  | Optional. Metadata defined as annotations on the network endpoint group. |
| `app_engine` | String |  | Optional. Only valid when networkEndpointType isSERVERLESS. Only one of cloudRun,appEngine or cloudFunction may be set. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `network` | String |  | The URL of the network to which all network endpoints in the NEG belong.
Uses default project network if unspecified. |
| `kind` | String |  | [Output Only] Type of the resource. Alwayscompute#networkEndpointGroup for network endpoint group. |
| `default_port` | i64 |  | The default port used if the port number is not specified in the network
endpoint.

Optional. If the network endpoint type is either GCE_VM_IP,SERVERLESS or PRIVATE_SERVICE_CONNECT, this
field must not be specified. |
| `name` | String |  | Name of the resource; provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must be
a dash, lowercase letter, or digit, except the last character, which cannot
be a dash. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `psc_target_service` | String |  | The target service url used to set up private service connection to
a Google API or a PSC Producer Service Attachment.
An example value is: asia-northeast3-cloudkms.googleapis.com.

Optional. Only valid when networkEndpointType isPRIVATE_SERVICE_CONNECT. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `cloud_run` | String |  | Optional. Only valid when networkEndpointType isSERVERLESS. Only one of cloudRun,appEngine or cloudFunction may be set. |
| `cloud_function` | String |  | Optional. Only valid when networkEndpointType isSERVERLESS. Only one of cloudRun,appEngine or cloudFunction may be set. |
| `subnetwork` | String |  | Optional URL of the subnetwork to which all network endpoints in the NEG
belong. |
| `zone` | String |  | [Output Only] The URL of thezone
where the network endpoint group is located. |
| `project` | String | ✅ | Project ID for this request. |
| `zone` | String | ✅ | The name of the zone where
you want to create the network endpoint group. It should comply with
RFC1035. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `psc_data` | String | Optional. Only valid when networkEndpointType isPRIVATE_SERVICE_CONNECT. |
| `region` | String | [Output Only] The URL of theregion
where the network endpoint group is located. |
| `size` | i64 | [Output only] Number of network endpoints in the network endpoint group. |
| `network_endpoint_type` | String | Type of network endpoints in this network endpoint group. Can be one ofGCE_VM_IP, GCE_VM_IP_PORT,NON_GCP_PRIVATE_IP_PORT, INTERNET_FQDN_PORT,INTERNET_IP_PORT, SERVERLESS,PRIVATE_SERVICE_CONNECT, GCE_VM_IP_PORTMAP. |
| `annotations` | HashMap<String, String> | Optional. Metadata defined as annotations on the network endpoint group. |
| `app_engine` | String | Optional. Only valid when networkEndpointType isSERVERLESS. Only one of cloudRun,appEngine or cloudFunction may be set. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `network` | String | The URL of the network to which all network endpoints in the NEG belong.
Uses default project network if unspecified. |
| `kind` | String | [Output Only] Type of the resource. Alwayscompute#networkEndpointGroup for network endpoint group. |
| `default_port` | i64 | The default port used if the port number is not specified in the network
endpoint.

Optional. If the network endpoint type is either GCE_VM_IP,SERVERLESS or PRIVATE_SERVICE_CONNECT, this
field must not be specified. |
| `name` | String | Name of the resource; provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must be
a dash, lowercase letter, or digit, except the last character, which cannot
be a dash. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `psc_target_service` | String | The target service url used to set up private service connection to
a Google API or a PSC Producer Service Attachment.
An example value is: asia-northeast3-cloudkms.googleapis.com.

Optional. Only valid when networkEndpointType isPRIVATE_SERVICE_CONNECT. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `cloud_run` | String | Optional. Only valid when networkEndpointType isSERVERLESS. Only one of cloudRun,appEngine or cloudFunction may be set. |
| `cloud_function` | String | Optional. Only valid when networkEndpointType isSERVERLESS. Only one of cloudRun,appEngine or cloudFunction may be set. |
| `subnetwork` | String | Optional URL of the subnetwork to which all network endpoints in the NEG
belong. |
| `zone` | String | [Output Only] The URL of thezone
where the network endpoint group is located. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create network_endpoint_group
network_endpoint_group = provider.compute_api.Network_endpoint_group {
    project = "value"  # Project ID for this request.
    zone = "value"  # The name of the zone where
you want to create the network endpoint group. It should comply with
RFC1035.
}

# Access network_endpoint_group outputs
network_endpoint_group_id = network_endpoint_group.id
network_endpoint_group_psc_data = network_endpoint_group.psc_data
network_endpoint_group_region = network_endpoint_group.region
network_endpoint_group_size = network_endpoint_group.size
network_endpoint_group_network_endpoint_type = network_endpoint_group.network_endpoint_type
network_endpoint_group_annotations = network_endpoint_group.annotations
network_endpoint_group_app_engine = network_endpoint_group.app_engine
network_endpoint_group_self_link = network_endpoint_group.self_link
network_endpoint_group_network = network_endpoint_group.network
network_endpoint_group_kind = network_endpoint_group.kind
network_endpoint_group_default_port = network_endpoint_group.default_port
network_endpoint_group_name = network_endpoint_group.name
network_endpoint_group_creation_timestamp = network_endpoint_group.creation_timestamp
network_endpoint_group_psc_target_service = network_endpoint_group.psc_target_service
network_endpoint_group_id = network_endpoint_group.id
network_endpoint_group_description = network_endpoint_group.description
network_endpoint_group_cloud_run = network_endpoint_group.cloud_run
network_endpoint_group_cloud_function = network_endpoint_group.cloud_function
network_endpoint_group_subnetwork = network_endpoint_group.subnetwork
network_endpoint_group_zone = network_endpoint_group.zone
```

---


### Region_autoscaler

Creates an autoscaler in the specified project using
the data included in the request.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `scaling_schedule_status` | HashMap<String, String> |  | [Output Only] Status information of existing scaling schedules. |
| `autoscaling_policy` | String |  | The configuration parameters for the autoscaling algorithm. You can define
one or more signals for an autoscaler: cpuUtilization,customMetricUtilizations, andloadBalancingUtilization.

If none of these are specified, the default will be to autoscale based oncpuUtilization to 0.6 or 60%. |
| `name` | String |  | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `kind` | String |  | [Output Only] Type of the resource. Always compute#autoscaler
for autoscalers. |
| `status_details` | Vec<String> |  | [Output Only] Human-readable details about the current state of the
autoscaler. Read the documentation forCommonly
returned status messages for examples of status messages you might
encounter. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `recommended_size` | i64 |  | [Output Only] Target recommended MIG size (number of instances) computed by
autoscaler. Autoscaler calculates the recommended MIG size even when the
autoscaling policy mode is different from ON. This field is empty when
autoscaler is not connected to an existing managed instance group or
autoscaler did not generate its prediction. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `target` | String |  | URL of the managed instance group that this autoscaler will scale. This
field is required when creating an autoscaler. |
| `zone` | String |  | [Output Only] URL of thezone
where the instance group resides (for autoscalers living in zonal scope). |
| `status` | String |  | [Output Only] The status of the autoscaler configuration. Current set of
possible values:
   
   - PENDING:
     Autoscaler backend hasn't read new/updated configuration.
   - DELETING:
     Configuration is being deleted.
   - ACTIVE:
     Configuration is acknowledged to be effective. Some warnings might
     be present in the statusDetails field.
   - ERROR:
     Configuration has errors. Actionable for users. Details are present in
     the statusDetails field.


New values might be added in the future. |
| `region` | String |  | [Output Only] URL of theregion
where the instance group resides (for autoscalers living in regional
scope). |
| `project` | String | ✅ | Project ID for this request. |
| `region` | String | ✅ | Name of the region scoping this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `scaling_schedule_status` | HashMap<String, String> | [Output Only] Status information of existing scaling schedules. |
| `autoscaling_policy` | String | The configuration parameters for the autoscaling algorithm. You can define
one or more signals for an autoscaler: cpuUtilization,customMetricUtilizations, andloadBalancingUtilization.

If none of these are specified, the default will be to autoscale based oncpuUtilization to 0.6 or 60%. |
| `name` | String | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `kind` | String | [Output Only] Type of the resource. Always compute#autoscaler
for autoscalers. |
| `status_details` | Vec<String> | [Output Only] Human-readable details about the current state of the
autoscaler. Read the documentation forCommonly
returned status messages for examples of status messages you might
encounter. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `recommended_size` | i64 | [Output Only] Target recommended MIG size (number of instances) computed by
autoscaler. Autoscaler calculates the recommended MIG size even when the
autoscaling policy mode is different from ON. This field is empty when
autoscaler is not connected to an existing managed instance group or
autoscaler did not generate its prediction. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `target` | String | URL of the managed instance group that this autoscaler will scale. This
field is required when creating an autoscaler. |
| `zone` | String | [Output Only] URL of thezone
where the instance group resides (for autoscalers living in zonal scope). |
| `status` | String | [Output Only] The status of the autoscaler configuration. Current set of
possible values:
   
   - PENDING:
     Autoscaler backend hasn't read new/updated configuration.
   - DELETING:
     Configuration is being deleted.
   - ACTIVE:
     Configuration is acknowledged to be effective. Some warnings might
     be present in the statusDetails field.
   - ERROR:
     Configuration has errors. Actionable for users. Details are present in
     the statusDetails field.


New values might be added in the future. |
| `region` | String | [Output Only] URL of theregion
where the instance group resides (for autoscalers living in regional
scope). |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create region_autoscaler
region_autoscaler = provider.compute_api.Region_autoscaler {
    project = "value"  # Project ID for this request.
    region = "value"  # Name of the region scoping this request.
}

# Access region_autoscaler outputs
region_autoscaler_id = region_autoscaler.id
region_autoscaler_description = region_autoscaler.description
region_autoscaler_creation_timestamp = region_autoscaler.creation_timestamp
region_autoscaler_scaling_schedule_status = region_autoscaler.scaling_schedule_status
region_autoscaler_autoscaling_policy = region_autoscaler.autoscaling_policy
region_autoscaler_name = region_autoscaler.name
region_autoscaler_kind = region_autoscaler.kind
region_autoscaler_status_details = region_autoscaler.status_details
region_autoscaler_self_link = region_autoscaler.self_link
region_autoscaler_recommended_size = region_autoscaler.recommended_size
region_autoscaler_id = region_autoscaler.id
region_autoscaler_target = region_autoscaler.target
region_autoscaler_zone = region_autoscaler.zone
region_autoscaler_status = region_autoscaler.status
region_autoscaler_region = region_autoscaler.region
```

---


### Target_ssl_proxie

Creates a TargetSslProxy resource in the specified project using
the data included in the request.

**Operations**: ✅ Create ✅ Read ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `ssl_certificates` | Vec<String> |  | URLs to SslCertificate resources that are used to
authenticate connections to Backends. At least one SSL certificate
must be specified. Currently, you may specify up to 15 SSL certificates.
sslCertificates do not apply when the load balancing scheme is set to
INTERNAL_SELF_MANAGED. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `kind` | String |  | [Output Only] Type of the resource. Alwayscompute#targetSslProxy for target SSL proxies. |
| `ssl_policy` | String |  | URL of SslPolicy resource that will be associated with the TargetSslProxy
resource. If not set, the TargetSslProxy resource will not have any
SSL policy configured. |
| `certificate_map` | String |  | URL of a certificate map that identifies a certificate map associated with
the given target proxy.
This field can only be set for global target proxies.
If set, sslCertificates will be ignored.

 Accepted format is//certificatemanager.googleapis.com/projects/{project}/locations/{location}/certificateMaps/{resourceName}. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `service` | String |  | URL to the BackendService resource. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `name` | String |  | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `proxy_header` | String |  | Specifies the type of proxy header to append before sending data to the
backend, either NONE or PROXY_V1. The default
is NONE. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `ssl_certificates` | Vec<String> | URLs to SslCertificate resources that are used to
authenticate connections to Backends. At least one SSL certificate
must be specified. Currently, you may specify up to 15 SSL certificates.
sslCertificates do not apply when the load balancing scheme is set to
INTERNAL_SELF_MANAGED. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `kind` | String | [Output Only] Type of the resource. Alwayscompute#targetSslProxy for target SSL proxies. |
| `ssl_policy` | String | URL of SslPolicy resource that will be associated with the TargetSslProxy
resource. If not set, the TargetSslProxy resource will not have any
SSL policy configured. |
| `certificate_map` | String | URL of a certificate map that identifies a certificate map associated with
the given target proxy.
This field can only be set for global target proxies.
If set, sslCertificates will be ignored.

 Accepted format is//certificatemanager.googleapis.com/projects/{project}/locations/{location}/certificateMaps/{resourceName}. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `service` | String | URL to the BackendService resource. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `name` | String | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `proxy_header` | String | Specifies the type of proxy header to append before sending data to the
backend, either NONE or PROXY_V1. The default
is NONE. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create target_ssl_proxie
target_ssl_proxie = provider.compute_api.Target_ssl_proxie {
    project = "value"  # Project ID for this request.
}

# Access target_ssl_proxie outputs
target_ssl_proxie_id = target_ssl_proxie.id
target_ssl_proxie_creation_timestamp = target_ssl_proxie.creation_timestamp
target_ssl_proxie_ssl_certificates = target_ssl_proxie.ssl_certificates
target_ssl_proxie_id = target_ssl_proxie.id
target_ssl_proxie_kind = target_ssl_proxie.kind
target_ssl_proxie_ssl_policy = target_ssl_proxie.ssl_policy
target_ssl_proxie_certificate_map = target_ssl_proxie.certificate_map
target_ssl_proxie_self_link = target_ssl_proxie.self_link
target_ssl_proxie_service = target_ssl_proxie.service
target_ssl_proxie_description = target_ssl_proxie.description
target_ssl_proxie_name = target_ssl_proxie.name
target_ssl_proxie_proxy_header = target_ssl_proxie.proxy_header
```

---


### Region

Returns the specified Region resource.

To decrease latency for this method, you can optionally omit any unneeded
information from the response by using a field mask. This practice is
especially recommended for unused quota information (the `quotas` field).
To exclude one or more fields, set your request's `fields` query parameter
to only include the fields you need. For example, to only include the `id`
and `selfLink` fields, add the query parameter `?fields=id,selfLink` to
your request.

This method fails if the quota information is unavailable for the region
and if the organization policy constraint
compute.requireBasicQuotaInResponse is enforced. This
constraint, when enforced, disables the fail-open behaviour when quota
information (the `items.quotas` field) is unavailable for the region.
It is recommended to use the default setting
for the constraint unless your application requires the fail-closed
behaviour for this method.

**Operations**: ✅ Read

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `zones` | Vec<String> | [Output Only] A list of zones available in this region, in the form of
resource URLs. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `quota_status_warning` | String | [Output Only] Warning of fetching the `quotas` field for this region. This
field is populated only if fetching of the `quotas` field fails. |
| `deprecated` | String | [Output Only] The deprecation status associated with this region. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `kind` | String | [Output Only] Type of the resource. Always compute#region for
regions. |
| `quotas` | Vec<String> | [Output Only] Quotas assigned to this region. |
| `name` | String | [Output Only] Name of the resource. |
| `status` | String | [Output Only] Status of the region, either UP orDOWN. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `description` | String | [Output Only] Textual description of the resource. |
| `supports_pzs` | bool | [Output Only] Reserved for future use. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Access region outputs
region_id = region.id
region_zones = region.zones
region_self_link = region.self_link
region_quota_status_warning = region.quota_status_warning
region_deprecated = region.deprecated
region_id = region.id
region_kind = region.kind
region_quotas = region.quotas
region_name = region.name
region_status = region.status
region_creation_timestamp = region.creation_timestamp
region_description = region.description
region_supports_pzs = region.supports_pzs
```

---


### Network

Creates a network in the specified project using the data included
in the request.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `gateway_i_pv4` | String |  | [Output Only] The gateway address for default routing out of the network,
selected by Google Cloud. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `params` | String |  | Input only. [Input Only] Additional params passed with the request, but not persisted
as part of resource payload. |
| `enable_ula_internal_ipv6` | bool |  | Enable ULA internal ipv6 on this network. Enabling this feature will assign
a /48 from google defined ULA prefix fd20::/20.
. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `routing_config` | String |  | The network-level routing configuration for this network.  Used by Cloud
Router to determine what type of network-wide routing behavior to enforce. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `i_pv4_range` | String |  | Deprecated in favor of subnet mode networks.
The range of internal addresses that are legal on this network. This
range is aCIDR specification, for example:192.168.0.0/16. Provided by the client when the network is
created. |
| `kind` | String |  | [Output Only] Type of the resource. Always compute#network for
networks. |
| `peerings` | Vec<String> |  | [Output Only] A list of network peerings for the resource. |
| `self_link_with_id` | String |  | [Output Only] Server-defined URL for this resource with the resource id. |
| `subnetworks` | Vec<String> |  | [Output Only] Server-defined fully-qualified URLs for all subnetworks
in this VPC network. |
| `mtu` | i64 |  | Maximum Transmission Unit in bytes.
The minimum value for this field is 1300 and the maximum value is 8896.
The suggested value is 1500, which is the default MTU used on the
Internet, or 8896 if you want to use Jumbo frames. If unspecified, the
value defaults to 1460. |
| `auto_create_subnetworks` | bool |  | Must be set to create a VPC network. If not set, a legacy network is
created.

When set to true, the VPC network is created in auto mode.
When set to false, the VPC network is created in custom mode.

An auto mode VPC network starts with one subnet per region. Each subnet
has a predetermined range as described inAuto mode VPC network IP ranges.

For custom mode VPC networks, you can add subnets using the subnetworksinsert
method. |
| `description` | String |  | An optional description of this resource. Provide this field when you
create the resource. |
| `network_firewall_policy_enforcement_order` | String |  | The network firewall policy enforcement order. Can be either
AFTER_CLASSIC_FIREWALL or BEFORE_CLASSIC_FIREWALL. Defaults to
AFTER_CLASSIC_FIREWALL if the field is not specified. |
| `network_profile` | String |  | A full or partial URL of the network profile to apply to this network.
This field can be set only at resource creation time. For example, the
following are valid URLs: 
   
   - https://www.googleapis.com/compute/{api_version}/projects/{project_id}/global/networkProfiles/{network_profile_name}
   - projects/{project_id}/global/networkProfiles/{network_profile_name} |
| `firewall_policy` | String |  | [Output Only] URL of the firewall policy the network is associated with. |
| `name` | String |  | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?`. The first character must be a
lowercase letter, and all following characters (except for the last
character) must be a dash, lowercase letter, or digit. The last character
must be a lowercase letter or digit. |
| `internal_ipv6_range` | String |  | When enabling ula internal ipv6, caller optionally can specify the /48
range they want from the google defined ULA prefix fd20::/20. The input
must be a valid /48 ULA IPv6 address and must be within the fd20::/20.
Operation will fail if the speficied /48 is already in used by another
resource. If the field is not speficied, then a /48 range will be randomly
allocated from fd20::/20 and returned via this field.
. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `gateway_i_pv4` | String | [Output Only] The gateway address for default routing out of the network,
selected by Google Cloud. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `params` | String | Input only. [Input Only] Additional params passed with the request, but not persisted
as part of resource payload. |
| `enable_ula_internal_ipv6` | bool | Enable ULA internal ipv6 on this network. Enabling this feature will assign
a /48 from google defined ULA prefix fd20::/20.
. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `routing_config` | String | The network-level routing configuration for this network.  Used by Cloud
Router to determine what type of network-wide routing behavior to enforce. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `i_pv4_range` | String | Deprecated in favor of subnet mode networks.
The range of internal addresses that are legal on this network. This
range is aCIDR specification, for example:192.168.0.0/16. Provided by the client when the network is
created. |
| `kind` | String | [Output Only] Type of the resource. Always compute#network for
networks. |
| `peerings` | Vec<String> | [Output Only] A list of network peerings for the resource. |
| `self_link_with_id` | String | [Output Only] Server-defined URL for this resource with the resource id. |
| `subnetworks` | Vec<String> | [Output Only] Server-defined fully-qualified URLs for all subnetworks
in this VPC network. |
| `mtu` | i64 | Maximum Transmission Unit in bytes.
The minimum value for this field is 1300 and the maximum value is 8896.
The suggested value is 1500, which is the default MTU used on the
Internet, or 8896 if you want to use Jumbo frames. If unspecified, the
value defaults to 1460. |
| `auto_create_subnetworks` | bool | Must be set to create a VPC network. If not set, a legacy network is
created.

When set to true, the VPC network is created in auto mode.
When set to false, the VPC network is created in custom mode.

An auto mode VPC network starts with one subnet per region. Each subnet
has a predetermined range as described inAuto mode VPC network IP ranges.

For custom mode VPC networks, you can add subnets using the subnetworksinsert
method. |
| `description` | String | An optional description of this resource. Provide this field when you
create the resource. |
| `network_firewall_policy_enforcement_order` | String | The network firewall policy enforcement order. Can be either
AFTER_CLASSIC_FIREWALL or BEFORE_CLASSIC_FIREWALL. Defaults to
AFTER_CLASSIC_FIREWALL if the field is not specified. |
| `network_profile` | String | A full or partial URL of the network profile to apply to this network.
This field can be set only at resource creation time. For example, the
following are valid URLs: 
   
   - https://www.googleapis.com/compute/{api_version}/projects/{project_id}/global/networkProfiles/{network_profile_name}
   - projects/{project_id}/global/networkProfiles/{network_profile_name} |
| `firewall_policy` | String | [Output Only] URL of the firewall policy the network is associated with. |
| `name` | String | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?`. The first character must be a
lowercase letter, and all following characters (except for the last
character) must be a dash, lowercase letter, or digit. The last character
must be a lowercase letter or digit. |
| `internal_ipv6_range` | String | When enabling ula internal ipv6, caller optionally can specify the /48
range they want from the google defined ULA prefix fd20::/20. The input
must be a valid /48 ULA IPv6 address and must be within the fd20::/20.
Operation will fail if the speficied /48 is already in used by another
resource. If the field is not speficied, then a /48 range will be randomly
allocated from fd20::/20 and returned via this field.
. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create network
network = provider.compute_api.Network {
    project = "value"  # Project ID for this request.
}

# Access network outputs
network_id = network.id
network_gateway_i_pv4 = network.gateway_i_pv4
network_id = network.id
network_params = network.params
network_enable_ula_internal_ipv6 = network.enable_ula_internal_ipv6
network_creation_timestamp = network.creation_timestamp
network_routing_config = network.routing_config
network_self_link = network.self_link
network_i_pv4_range = network.i_pv4_range
network_kind = network.kind
network_peerings = network.peerings
network_self_link_with_id = network.self_link_with_id
network_subnetworks = network.subnetworks
network_mtu = network.mtu
network_auto_create_subnetworks = network.auto_create_subnetworks
network_description = network.description
network_network_firewall_policy_enforcement_order = network.network_firewall_policy_enforcement_order
network_network_profile = network.network_profile
network_firewall_policy = network.firewall_policy
network_name = network.name
network_internal_ipv6_range = network.internal_ipv6_range
```

---


### Machine_type

Returns the specified machine type.

**Operations**: ✅ Read

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `description` | String | [Output Only] An optional textual description of the resource. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `guest_cpus` | i64 | [Output Only] The number of virtual CPUs that are available to the
instance. |
| `is_shared_cpu` | bool | [Output Only] Whether this machine type has a shared CPU. SeeShared-core machine
types for more information. |
| `name` | String | [Output Only] Name of the resource. |
| `accelerators` | Vec<String> | [Output Only] A list of accelerator configurations assigned to this
machine type. |
| `kind` | String | [Output Only] The type of the resource. Alwayscompute#machineType for machine types. |
| `architecture` | String | [Output Only] The architecture of the machine type. |
| `zone` | String | [Output Only] The name of the zone where the machine type resides,
such as us-central1-a. |
| `deprecated` | String | [Output Only] The deprecation status associated with this machine type.
Only applicable if the machine type is unavailable. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `image_space_gb` | i64 | [Deprecated] This property is deprecated and will never be populated with
any relevant values. |
| `maximum_persistent_disks_size_gb` | String | [Output Only] Maximum total persistent disks size (GB) allowed. |
| `maximum_persistent_disks` | i64 | [Output Only] Maximum persistent disks allowed. |
| `memory_mb` | i64 | [Output Only] The amount of physical memory available to the instance,
defined in MB. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Access machine_type outputs
machine_type_id = machine_type.id
machine_type_description = machine_type.description
machine_type_creation_timestamp = machine_type.creation_timestamp
machine_type_guest_cpus = machine_type.guest_cpus
machine_type_is_shared_cpu = machine_type.is_shared_cpu
machine_type_name = machine_type.name
machine_type_accelerators = machine_type.accelerators
machine_type_kind = machine_type.kind
machine_type_architecture = machine_type.architecture
machine_type_zone = machine_type.zone
machine_type_deprecated = machine_type.deprecated
machine_type_id = machine_type.id
machine_type_image_space_gb = machine_type.image_space_gb
machine_type_maximum_persistent_disks_size_gb = machine_type.maximum_persistent_disks_size_gb
machine_type_maximum_persistent_disks = machine_type.maximum_persistent_disks
machine_type_memory_mb = machine_type.memory_mb
machine_type_self_link = machine_type.self_link
```

---


### Public_delegated_prefixe

Creates a PublicDelegatedPrefix in the specified project in the given
region using the parameters that are included in the request.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `public_delegated_sub_prefixs` | Vec<String> |  | The list of sub public delegated prefixes that exist for this public
delegated prefix. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `ip_cidr_range` | String |  | The IP address range, in CIDR format, represented by this public
delegated prefix. |
| `parent_prefix` | String |  | The URL of parent prefix. Either PublicAdvertisedPrefix or
PublicDelegatedPrefix. |
| `name` | String |  | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `id` | String |  | [Output Only] The unique identifier for the resource type. The server
generates this identifier. |
| `status` | String |  | [Output Only] The status of the public delegated prefix, which can be one
of following values:
   
   
     - `INITIALIZING` The public delegated prefix is being initialized and
     addresses cannot be created yet.
     - `READY_TO_ANNOUNCE` The public delegated prefix is a live migration
     prefix and is active.
     - `ANNOUNCED` The public delegated prefix is announced and ready to
     use.
     - `DELETING` The public delegated prefix is being deprovsioned.
     - `ACTIVE` The public delegated prefix is ready to use. |
| `byoip_api_version` | String |  | [Output Only] The version of BYOIP API. |
| `allocatable_prefix_length` | i64 |  | The allocatable prefix length supported by this public delegated prefix.
This field is optional and cannot be set for prefixes in DELEGATION mode.
It cannot be set for IPv4 prefixes either, and it always defaults to 32. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `ipv6_access_type` | String |  | [Output Only] The internet access type for IPv6 Public Delegated Prefixes.
Inherited from parent prefix. |
| `fingerprint` | String |  | Fingerprint of this resource. A hash of the contents stored in this object.
This field is used in optimistic locking. This field will be ignored when
inserting a new PublicDelegatedPrefix. An up-to-date
fingerprint must be provided in order to update thePublicDelegatedPrefix, otherwise the request will fail with
error 412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve a PublicDelegatedPrefix. |
| `is_live_migration` | bool |  | If true, the prefix will be live migrated. |
| `region` | String |  | [Output Only] URL of the region where the public delegated prefix resides.
This field applies only to the region resource. You must specify this
field as part of the HTTP request URL. It is not settable as a field in
the request body. |
| `mode` | String |  | The public delegated prefix mode for IPv6 only. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `kind` | String |  | [Output Only] Type of the resource. Alwayscompute#publicDelegatedPrefix for public delegated prefixes. |
| `region` | String | ✅ | Name of the region of this request. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `public_delegated_sub_prefixs` | Vec<String> | The list of sub public delegated prefixes that exist for this public
delegated prefix. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `ip_cidr_range` | String | The IP address range, in CIDR format, represented by this public
delegated prefix. |
| `parent_prefix` | String | The URL of parent prefix. Either PublicAdvertisedPrefix or
PublicDelegatedPrefix. |
| `name` | String | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `id` | String | [Output Only] The unique identifier for the resource type. The server
generates this identifier. |
| `status` | String | [Output Only] The status of the public delegated prefix, which can be one
of following values:
   
   
     - `INITIALIZING` The public delegated prefix is being initialized and
     addresses cannot be created yet.
     - `READY_TO_ANNOUNCE` The public delegated prefix is a live migration
     prefix and is active.
     - `ANNOUNCED` The public delegated prefix is announced and ready to
     use.
     - `DELETING` The public delegated prefix is being deprovsioned.
     - `ACTIVE` The public delegated prefix is ready to use. |
| `byoip_api_version` | String | [Output Only] The version of BYOIP API. |
| `allocatable_prefix_length` | i64 | The allocatable prefix length supported by this public delegated prefix.
This field is optional and cannot be set for prefixes in DELEGATION mode.
It cannot be set for IPv4 prefixes either, and it always defaults to 32. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `ipv6_access_type` | String | [Output Only] The internet access type for IPv6 Public Delegated Prefixes.
Inherited from parent prefix. |
| `fingerprint` | String | Fingerprint of this resource. A hash of the contents stored in this object.
This field is used in optimistic locking. This field will be ignored when
inserting a new PublicDelegatedPrefix. An up-to-date
fingerprint must be provided in order to update thePublicDelegatedPrefix, otherwise the request will fail with
error 412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve a PublicDelegatedPrefix. |
| `is_live_migration` | bool | If true, the prefix will be live migrated. |
| `region` | String | [Output Only] URL of the region where the public delegated prefix resides.
This field applies only to the region resource. You must specify this
field as part of the HTTP request URL. It is not settable as a field in
the request body. |
| `mode` | String | The public delegated prefix mode for IPv6 only. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `kind` | String | [Output Only] Type of the resource. Alwayscompute#publicDelegatedPrefix for public delegated prefixes. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create public_delegated_prefixe
public_delegated_prefixe = provider.compute_api.Public_delegated_prefixe {
    region = "value"  # Name of the region of this request.
    project = "value"  # Project ID for this request.
}

# Access public_delegated_prefixe outputs
public_delegated_prefixe_id = public_delegated_prefixe.id
public_delegated_prefixe_public_delegated_sub_prefixs = public_delegated_prefixe.public_delegated_sub_prefixs
public_delegated_prefixe_self_link = public_delegated_prefixe.self_link
public_delegated_prefixe_ip_cidr_range = public_delegated_prefixe.ip_cidr_range
public_delegated_prefixe_parent_prefix = public_delegated_prefixe.parent_prefix
public_delegated_prefixe_name = public_delegated_prefixe.name
public_delegated_prefixe_id = public_delegated_prefixe.id
public_delegated_prefixe_status = public_delegated_prefixe.status
public_delegated_prefixe_byoip_api_version = public_delegated_prefixe.byoip_api_version
public_delegated_prefixe_allocatable_prefix_length = public_delegated_prefixe.allocatable_prefix_length
public_delegated_prefixe_description = public_delegated_prefixe.description
public_delegated_prefixe_ipv6_access_type = public_delegated_prefixe.ipv6_access_type
public_delegated_prefixe_fingerprint = public_delegated_prefixe.fingerprint
public_delegated_prefixe_is_live_migration = public_delegated_prefixe.is_live_migration
public_delegated_prefixe_region = public_delegated_prefixe.region
public_delegated_prefixe_mode = public_delegated_prefixe.mode
public_delegated_prefixe_creation_timestamp = public_delegated_prefixe.creation_timestamp
public_delegated_prefixe_kind = public_delegated_prefixe.kind
```

---


### Network_attachment

Creates a NetworkAttachment in the specified project in the given scope
using the parameters that are included in the request.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `producer_accept_lists` | Vec<String> |  | Projects that are allowed to connect to this network attachment.
The project can be specified using its id or number. |
| `region` | String |  | [Output Only] URL of the region where the network attachment resides.
This field applies only to the region resource. You must specify this
field as part of the HTTP request URL. It is not settable as a field in
the request body. |
| `self_link_with_id` | String |  | [Output Only] Server-defined URL for this resource's resource id. |
| `connection_preference` | String |  |  |
| `producer_reject_lists` | Vec<String> |  | Projects that are not allowed to connect to this network attachment.
The project can be specified using its id or number. |
| `id` | String |  | [Output Only] The unique identifier for the resource type. The server
generates this identifier. |
| `fingerprint` | String |  | Fingerprint of this resource. A hash of the contents stored
in this object. This field is used in optimistic locking. An up-to-date
fingerprint must be provided in order to patch. |
| `kind` | String |  | [Output Only] Type of the resource. |
| `connection_endpoints` | Vec<String> |  | [Output Only] An array of connections for all the producers connected
to this network attachment. |
| `network` | String |  | [Output Only] The URL of the network which the Network Attachment
belongs to. Practically it is inferred by fetching the network of the first
subnetwork associated. Because it is required that all the subnetworks must
be from the same network, it is assured that the Network Attachment belongs
to the same network as all the subnetworks. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `subnetworks` | Vec<String> |  | An array of URLs where each entry is the URL of a subnet
provided by the service consumer to use for
endpoints in the producers that connect to this network attachment. |
| `name` | String |  | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `region` | String | ✅ | Name of the region of this request. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `producer_accept_lists` | Vec<String> | Projects that are allowed to connect to this network attachment.
The project can be specified using its id or number. |
| `region` | String | [Output Only] URL of the region where the network attachment resides.
This field applies only to the region resource. You must specify this
field as part of the HTTP request URL. It is not settable as a field in
the request body. |
| `self_link_with_id` | String | [Output Only] Server-defined URL for this resource's resource id. |
| `connection_preference` | String |  |
| `producer_reject_lists` | Vec<String> | Projects that are not allowed to connect to this network attachment.
The project can be specified using its id or number. |
| `id` | String | [Output Only] The unique identifier for the resource type. The server
generates this identifier. |
| `fingerprint` | String | Fingerprint of this resource. A hash of the contents stored
in this object. This field is used in optimistic locking. An up-to-date
fingerprint must be provided in order to patch. |
| `kind` | String | [Output Only] Type of the resource. |
| `connection_endpoints` | Vec<String> | [Output Only] An array of connections for all the producers connected
to this network attachment. |
| `network` | String | [Output Only] The URL of the network which the Network Attachment
belongs to. Practically it is inferred by fetching the network of the first
subnetwork associated. Because it is required that all the subnetworks must
be from the same network, it is assured that the Network Attachment belongs
to the same network as all the subnetworks. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `subnetworks` | Vec<String> | An array of URLs where each entry is the URL of a subnet
provided by the service consumer to use for
endpoints in the producers that connect to this network attachment. |
| `name` | String | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create network_attachment
network_attachment = provider.compute_api.Network_attachment {
    region = "value"  # Name of the region of this request.
    project = "value"  # Project ID for this request.
}

# Access network_attachment outputs
network_attachment_id = network_attachment.id
network_attachment_description = network_attachment.description
network_attachment_producer_accept_lists = network_attachment.producer_accept_lists
network_attachment_region = network_attachment.region
network_attachment_self_link_with_id = network_attachment.self_link_with_id
network_attachment_connection_preference = network_attachment.connection_preference
network_attachment_producer_reject_lists = network_attachment.producer_reject_lists
network_attachment_id = network_attachment.id
network_attachment_fingerprint = network_attachment.fingerprint
network_attachment_kind = network_attachment.kind
network_attachment_connection_endpoints = network_attachment.connection_endpoints
network_attachment_network = network_attachment.network
network_attachment_creation_timestamp = network_attachment.creation_timestamp
network_attachment_subnetworks = network_attachment.subnetworks
network_attachment_name = network_attachment.name
network_attachment_self_link = network_attachment.self_link
```

---


### Region_autoscaler

Creates an autoscaler in the specified project using
the data included in the request.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `kind` | String |  | [Output Only] Type of the resource. Always compute#autoscaler
for autoscalers. |
| `region` | String |  | [Output Only] URL of theregion
where the instance group resides (for autoscalers living in regional
scope). |
| `scaling_schedule_status` | HashMap<String, String> |  | [Output Only] Status information of existing scaling schedules. |
| `status_details` | Vec<String> |  | [Output Only] Human-readable details about the current state of the
autoscaler. Read the documentation forCommonly
returned status messages for examples of status messages you might
encounter. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `zone` | String |  | [Output Only] URL of thezone
where the instance group resides (for autoscalers living in zonal scope). |
| `autoscaling_policy` | String |  | The configuration parameters for the autoscaling algorithm. You can define
one or more signals for an autoscaler: cpuUtilization,customMetricUtilizations, andloadBalancingUtilization.

If none of these are specified, the default will be to autoscale based oncpuUtilization to 0.6 or 60%. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `target` | String |  | URL of the managed instance group that this autoscaler will scale. This
field is required when creating an autoscaler. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `self_link_with_id` | String |  | [Output Only] Server-defined URL for this resource with the resource id. |
| `name` | String |  | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `status` | String |  | [Output Only] The status of the autoscaler configuration. Current set of
possible values:
   
   - PENDING:
     Autoscaler backend hasn't read new/updated configuration.
   - DELETING:
     Configuration is being deleted.
   - ACTIVE:
     Configuration is acknowledged to be effective. Some warnings might
     be present in the statusDetails field.
   - ERROR:
     Configuration has errors. Actionable for users. Details are present in
     the statusDetails field.


New values might be added in the future. |
| `recommended_size` | i64 |  | [Output Only] Target recommended MIG size (number of instances) computed by
autoscaler. Autoscaler calculates the recommended MIG size even when the
autoscaling policy mode is different from ON. This field is empty when
autoscaler is not connected to an existing managed instance group or
autoscaler did not generate its prediction. |
| `region` | String | ✅ | Name of the region scoping this request. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `kind` | String | [Output Only] Type of the resource. Always compute#autoscaler
for autoscalers. |
| `region` | String | [Output Only] URL of theregion
where the instance group resides (for autoscalers living in regional
scope). |
| `scaling_schedule_status` | HashMap<String, String> | [Output Only] Status information of existing scaling schedules. |
| `status_details` | Vec<String> | [Output Only] Human-readable details about the current state of the
autoscaler. Read the documentation forCommonly
returned status messages for examples of status messages you might
encounter. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `zone` | String | [Output Only] URL of thezone
where the instance group resides (for autoscalers living in zonal scope). |
| `autoscaling_policy` | String | The configuration parameters for the autoscaling algorithm. You can define
one or more signals for an autoscaler: cpuUtilization,customMetricUtilizations, andloadBalancingUtilization.

If none of these are specified, the default will be to autoscale based oncpuUtilization to 0.6 or 60%. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `target` | String | URL of the managed instance group that this autoscaler will scale. This
field is required when creating an autoscaler. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `self_link_with_id` | String | [Output Only] Server-defined URL for this resource with the resource id. |
| `name` | String | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `status` | String | [Output Only] The status of the autoscaler configuration. Current set of
possible values:
   
   - PENDING:
     Autoscaler backend hasn't read new/updated configuration.
   - DELETING:
     Configuration is being deleted.
   - ACTIVE:
     Configuration is acknowledged to be effective. Some warnings might
     be present in the statusDetails field.
   - ERROR:
     Configuration has errors. Actionable for users. Details are present in
     the statusDetails field.


New values might be added in the future. |
| `recommended_size` | i64 | [Output Only] Target recommended MIG size (number of instances) computed by
autoscaler. Autoscaler calculates the recommended MIG size even when the
autoscaling policy mode is different from ON. This field is empty when
autoscaler is not connected to an existing managed instance group or
autoscaler did not generate its prediction. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create region_autoscaler
region_autoscaler = provider.compute_api.Region_autoscaler {
    region = "value"  # Name of the region scoping this request.
    project = "value"  # Project ID for this request.
}

# Access region_autoscaler outputs
region_autoscaler_id = region_autoscaler.id
region_autoscaler_kind = region_autoscaler.kind
region_autoscaler_region = region_autoscaler.region
region_autoscaler_scaling_schedule_status = region_autoscaler.scaling_schedule_status
region_autoscaler_status_details = region_autoscaler.status_details
region_autoscaler_self_link = region_autoscaler.self_link
region_autoscaler_zone = region_autoscaler.zone
region_autoscaler_autoscaling_policy = region_autoscaler.autoscaling_policy
region_autoscaler_id = region_autoscaler.id
region_autoscaler_target = region_autoscaler.target
region_autoscaler_description = region_autoscaler.description
region_autoscaler_self_link_with_id = region_autoscaler.self_link_with_id
region_autoscaler_name = region_autoscaler.name
region_autoscaler_creation_timestamp = region_autoscaler.creation_timestamp
region_autoscaler_status = region_autoscaler.status
region_autoscaler_recommended_size = region_autoscaler.recommended_size
```

---


### Vpn_gateway

Creates a VPN gateway in the specified project and region using
the data included in the request.

**Operations**: ✅ Create ✅ Read ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `gateway_ip_version` | String |  | The IP family of the gateway IPs for the HA-VPN gateway interfaces. If not
specified, IPV4 will be used. |
| `kind` | String |  | [Output Only] Type of resource. Always compute#vpnGateway for
VPN gateways. |
| `labels` | HashMap<String, String> |  | Labels for this resource. These can only be added or modified by thesetLabels method. Each label key/value pair must comply withRFC1035.
Label values may be empty. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `name` | String |  | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `region` | String |  | [Output Only] URL of the region where the VPN gateway resides. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `network` | String |  | URL of the network to which this VPN gateway is attached. Provided by the
client when the VPN gateway is created. |
| `stack_type` | String |  | The stack type for this VPN gateway to identify the IP protocols that are
enabled. Possible values are: IPV4_ONLY,IPV4_IPV6, IPV6_ONLY. If not specified,IPV4_ONLY is used if the gateway IP version isIPV4, or IPV4_IPV6 if the gateway IP version isIPV6. |
| `params` | String |  | Input only. [Input Only] Additional params passed with the request, but not persisted
as part of resource payload. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `vpn_interfaces` | Vec<String> |  | The list of VPN interfaces associated with this VPN gateway. |
| `label_fingerprint` | String |  | A fingerprint for the labels being applied to this VpnGateway, which
is essentially a hash of the labels set used for optimistic locking. The
fingerprint is initially generated by Compute Engine and changes after
every request to modify or update labels. You must always provide an
up-to-date fingerprint hash in order to update or change labels,
otherwise the request will fail with error412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve a VpnGateway. |
| `region` | String | ✅ | Name of the region for this request. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `gateway_ip_version` | String | The IP family of the gateway IPs for the HA-VPN gateway interfaces. If not
specified, IPV4 will be used. |
| `kind` | String | [Output Only] Type of resource. Always compute#vpnGateway for
VPN gateways. |
| `labels` | HashMap<String, String> | Labels for this resource. These can only be added or modified by thesetLabels method. Each label key/value pair must comply withRFC1035.
Label values may be empty. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `name` | String | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `region` | String | [Output Only] URL of the region where the VPN gateway resides. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `network` | String | URL of the network to which this VPN gateway is attached. Provided by the
client when the VPN gateway is created. |
| `stack_type` | String | The stack type for this VPN gateway to identify the IP protocols that are
enabled. Possible values are: IPV4_ONLY,IPV4_IPV6, IPV6_ONLY. If not specified,IPV4_ONLY is used if the gateway IP version isIPV4, or IPV4_IPV6 if the gateway IP version isIPV6. |
| `params` | String | Input only. [Input Only] Additional params passed with the request, but not persisted
as part of resource payload. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `vpn_interfaces` | Vec<String> | The list of VPN interfaces associated with this VPN gateway. |
| `label_fingerprint` | String | A fingerprint for the labels being applied to this VpnGateway, which
is essentially a hash of the labels set used for optimistic locking. The
fingerprint is initially generated by Compute Engine and changes after
every request to modify or update labels. You must always provide an
up-to-date fingerprint hash in order to update or change labels,
otherwise the request will fail with error412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve a VpnGateway. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create vpn_gateway
vpn_gateway = provider.compute_api.Vpn_gateway {
    region = "value"  # Name of the region for this request.
    project = "value"  # Project ID for this request.
}

# Access vpn_gateway outputs
vpn_gateway_id = vpn_gateway.id
vpn_gateway_id = vpn_gateway.id
vpn_gateway_gateway_ip_version = vpn_gateway.gateway_ip_version
vpn_gateway_kind = vpn_gateway.kind
vpn_gateway_labels = vpn_gateway.labels
vpn_gateway_creation_timestamp = vpn_gateway.creation_timestamp
vpn_gateway_name = vpn_gateway.name
vpn_gateway_region = vpn_gateway.region
vpn_gateway_self_link = vpn_gateway.self_link
vpn_gateway_network = vpn_gateway.network
vpn_gateway_stack_type = vpn_gateway.stack_type
vpn_gateway_params = vpn_gateway.params
vpn_gateway_description = vpn_gateway.description
vpn_gateway_vpn_interfaces = vpn_gateway.vpn_interfaces
vpn_gateway_label_fingerprint = vpn_gateway.label_fingerprint
```

---


### Region_notification_endpoint

Create a NotificationEndpoint in the specified project in the given region
using the parameters that are included in the request.

**Operations**: ✅ Create ✅ Read ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `id` | String |  | [Output Only] A unique identifier for this resource type. The server
generates this identifier. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `grpc_settings` | String |  | Settings of the gRPC notification endpoint including the endpoint URL and
the retry duration. |
| `name` | String |  | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `kind` | String |  | [Output Only] Type of the resource. Alwayscompute#notificationEndpoint for notification endpoints. |
| `region` | String |  | [Output Only] URL of the region where the notification endpoint resides.
This field applies only to the regional resource. You must specify this
field as part of the HTTP request URL. It is not settable as a field in
the request body. |
| `region` | String | ✅ | Name of the region scoping this request. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `id` | String | [Output Only] A unique identifier for this resource type. The server
generates this identifier. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `grpc_settings` | String | Settings of the gRPC notification endpoint including the endpoint URL and
the retry duration. |
| `name` | String | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `kind` | String | [Output Only] Type of the resource. Alwayscompute#notificationEndpoint for notification endpoints. |
| `region` | String | [Output Only] URL of the region where the notification endpoint resides.
This field applies only to the regional resource. You must specify this
field as part of the HTTP request URL. It is not settable as a field in
the request body. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create region_notification_endpoint
region_notification_endpoint = provider.compute_api.Region_notification_endpoint {
    region = "value"  # Name of the region scoping this request.
    project = "value"  # Project ID for this request.
}

# Access region_notification_endpoint outputs
region_notification_endpoint_id = region_notification_endpoint.id
region_notification_endpoint_id = region_notification_endpoint.id
region_notification_endpoint_description = region_notification_endpoint.description
region_notification_endpoint_creation_timestamp = region_notification_endpoint.creation_timestamp
region_notification_endpoint_grpc_settings = region_notification_endpoint.grpc_settings
region_notification_endpoint_name = region_notification_endpoint.name
region_notification_endpoint_self_link = region_notification_endpoint.self_link
region_notification_endpoint_kind = region_notification_endpoint.kind
region_notification_endpoint_region = region_notification_endpoint.region
```

---


### Preview_feature

Returns the details of the given PreviewFeature.

**Operations**: ✅ Read ✅ Update

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `activation_status` | String |  | Specifies whether the feature is enabled or disabled. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `name` | String |  | Name of the feature. |
| `description` | String |  | [Output Only] Description of the feature. |
| `status` | String |  | [Output only] Status of the feature. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `kind` | String |  | [Output only] The type of the feature. Always "compute#previewFeature" for
preview features. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `rollout_operation` | String |  | Rollout operation of the feature. |
| `preview_feature` | String | ✅ | Name of the PreviewFeature for this request. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `activation_status` | String | Specifies whether the feature is enabled or disabled. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `name` | String | Name of the feature. |
| `description` | String | [Output Only] Description of the feature. |
| `status` | String | [Output only] Status of the feature. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `kind` | String | [Output only] The type of the feature. Always "compute#previewFeature" for
preview features. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `rollout_operation` | String | Rollout operation of the feature. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Access preview_feature outputs
preview_feature_id = preview_feature.id
preview_feature_activation_status = preview_feature.activation_status
preview_feature_self_link = preview_feature.self_link
preview_feature_name = preview_feature.name
preview_feature_description = preview_feature.description
preview_feature_status = preview_feature.status
preview_feature_id = preview_feature.id
preview_feature_kind = preview_feature.kind
preview_feature_creation_timestamp = preview_feature.creation_timestamp
preview_feature_rollout_operation = preview_feature.rollout_operation
```

---


### License_code

Sets the access control policy on the specified resource.
Replaces any existing policy.
 *Caution* This resource is intended
for use only by third-party partners who are creatingCloud Marketplace
images.

**Operations**: ✅ Create ✅ Read

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `etag` | String |  | Flatten Policy to create a backward compatible wire-format.
Deprecated. Use 'policy' to specify the etag. |
| `policy` | String |  | REQUIRED: The complete policy to be applied to the 'resource'. The size of
the policy is limited to a few 10s of KB. An empty policy is in general a
valid policy but certain services (like Projects) might reject them. |
| `bindings` | Vec<String> |  | Flatten Policy to create a backward compatible wire-format.
Deprecated. Use 'policy' to specify bindings. |
| `resource` | String | ✅ | Name or id of the resource for this request. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `multi_tenant_only` | bool | If true, this license can only be used on VMs on multi tenant nodes. |
| `required_coattached_licenses` | Vec<String> | Specifies the set of permissible coattached licenseCodes of licenses that
satisfy the coattachment requirement of this license. At least one license
from the set must be attached to the same disk or image as this license. |
| `transferable` | bool | [Output Only] If true, the license will remain attached when creating
images or snapshots from disks. Otherwise, the license is not transferred. |
| `name` | String | [Output Only] Name of the resource. The name is 1-20 characters long and
must be a valid 64 bit integer. |
| `description` | String | [Output Only] Description of this License Code. |
| `removable_from_disk` | bool | If true, this license can be removed from a disk's set of licenses, with no
replacement license needed. |
| `update_timestamp` | String | [Output Only] Last update timestamp inRFC3339
text format. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `allowed_replacement_licenses` | Vec<String> | Specifies licenseCodes of licenses that can replace this license. Note:
such replacements are allowed even if removable_from_disk is false. |
| `minimum_retention` | String | If set, this license will be unable to be removed or replaced once attached
to a disk until the minimum_retention period has passed. |
| `os_license` | bool | If true, indicates this is an OS license. Only one OS license can be
attached to a disk or image at a time. |
| `sole_tenant_only` | bool | If true, this license can only be used on VMs on sole tenant nodes. |
| `state` | String | [Output Only] Current state of this License Code. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `incompatible_licenses` | Vec<String> | Specifies licenseCodes of licenses that are incompatible with this license.
If a license is incompatible with this license, it cannot be attached to
the same disk or image. |
| `kind` | String | [Output Only] Type of resource. Always compute#licenseCode for
licenses. |
| `appendable_to_disk` | bool | If true, this license can be appended to an existing disk's set of
licenses. |
| `license_alias` | Vec<String> | [Output Only] URL and description aliases of Licenses with the same
License Code. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create license_code
license_code = provider.compute_api.License_code {
    resource = "value"  # Name or id of the resource for this request.
    project = "value"  # Project ID for this request.
}

# Access license_code outputs
license_code_id = license_code.id
license_code_multi_tenant_only = license_code.multi_tenant_only
license_code_required_coattached_licenses = license_code.required_coattached_licenses
license_code_transferable = license_code.transferable
license_code_name = license_code.name
license_code_description = license_code.description
license_code_removable_from_disk = license_code.removable_from_disk
license_code_update_timestamp = license_code.update_timestamp
license_code_id = license_code.id
license_code_allowed_replacement_licenses = license_code.allowed_replacement_licenses
license_code_minimum_retention = license_code.minimum_retention
license_code_os_license = license_code.os_license
license_code_sole_tenant_only = license_code.sole_tenant_only
license_code_state = license_code.state
license_code_creation_timestamp = license_code.creation_timestamp
license_code_incompatible_licenses = license_code.incompatible_licenses
license_code_kind = license_code.kind
license_code_appendable_to_disk = license_code.appendable_to_disk
license_code_license_alias = license_code.license_alias
license_code_self_link = license_code.self_link
```

---


### Region_instance_template

Creates an instance template in the specified project and region using the
global instance template whose URL is included in the request.

**Operations**: ✅ Create ✅ Read ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `properties` | String |  | The instance properties for this instance template. |
| `name` | String |  | Name of the resource; provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must be
a dash, lowercase letter, or digit, except the last character, which cannot
be a dash. |
| `id` | String |  | [Output Only] A unique identifier for this instance template. The server
defines this identifier. |
| `self_link_with_id` | String |  | [Output Only] Server-defined URL for this resource with the resource id. |
| `kind` | String |  | [Output Only] The resource type, which is alwayscompute#instanceTemplate for instance templates. |
| `region` | String |  | [Output Only] URL of the region where the instance template resides. Only
applicable for regional resources. |
| `self_link` | String |  | [Output Only] The URL for this instance template. The server defines this
URL. |
| `source_instance_params` | String |  | The source instance params to use to create this instance template. |
| `creation_timestamp` | String |  | [Output Only] The creation timestamp for this instance template inRFC3339
text format. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `source_instance` | String |  | The source instance used to create the template. You can provide this as a
partial or full URL to the resource. For example, the following are valid
values:
   
   
     - https://www.googleapis.com/compute/v1/projects/project/zones/zone/instances/instance 
   - projects/project/zones/zone/instances/instance |
| `region` | String | ✅ | The name of the region for this request. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `properties` | String | The instance properties for this instance template. |
| `name` | String | Name of the resource; provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must be
a dash, lowercase letter, or digit, except the last character, which cannot
be a dash. |
| `id` | String | [Output Only] A unique identifier for this instance template. The server
defines this identifier. |
| `self_link_with_id` | String | [Output Only] Server-defined URL for this resource with the resource id. |
| `kind` | String | [Output Only] The resource type, which is alwayscompute#instanceTemplate for instance templates. |
| `region` | String | [Output Only] URL of the region where the instance template resides. Only
applicable for regional resources. |
| `self_link` | String | [Output Only] The URL for this instance template. The server defines this
URL. |
| `source_instance_params` | String | The source instance params to use to create this instance template. |
| `creation_timestamp` | String | [Output Only] The creation timestamp for this instance template inRFC3339
text format. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `source_instance` | String | The source instance used to create the template. You can provide this as a
partial or full URL to the resource. For example, the following are valid
values:
   
   
     - https://www.googleapis.com/compute/v1/projects/project/zones/zone/instances/instance 
   - projects/project/zones/zone/instances/instance |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create region_instance_template
region_instance_template = provider.compute_api.Region_instance_template {
    region = "value"  # The name of the region for this request.
    project = "value"  # Project ID for this request.
}

# Access region_instance_template outputs
region_instance_template_id = region_instance_template.id
region_instance_template_properties = region_instance_template.properties
region_instance_template_name = region_instance_template.name
region_instance_template_id = region_instance_template.id
region_instance_template_self_link_with_id = region_instance_template.self_link_with_id
region_instance_template_kind = region_instance_template.kind
region_instance_template_region = region_instance_template.region
region_instance_template_self_link = region_instance_template.self_link
region_instance_template_source_instance_params = region_instance_template.source_instance_params
region_instance_template_creation_timestamp = region_instance_template.creation_timestamp
region_instance_template_description = region_instance_template.description
region_instance_template_source_instance = region_instance_template.source_instance
```

---


### Machine_type

Returns the specified machine type.

**Operations**: ✅ Read

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `kind` | String | [Output Only] The type of the resource. Alwayscompute#machineType for machine types. |
| `memory_mb` | i64 | [Output Only] The amount of physical memory available to the instance,
defined in MB. |
| `description` | String | [Output Only] An optional textual description of the resource. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `architecture` | String | [Output Only] The architecture of the machine type. |
| `deprecated` | String | [Output Only] The deprecation status associated with this machine type.
Only applicable if the machine type is unavailable. |
| `guest_cpus` | i64 | [Output Only] The number of virtual CPUs that are available to the
instance. |
| `self_link_with_id` | String | [Output Only] Server-defined URL for this resource with the resource id. |
| `maximum_persistent_disks_size_gb` | String | [Output Only] Maximum total persistent disks size (GB) allowed. |
| `name` | String | [Output Only] Name of the resource. |
| `zone` | String | [Output Only] The name of the zone where the machine type resides,
such as us-central1-a. |
| `maximum_persistent_disks` | i64 | [Output Only] Maximum persistent disks allowed. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `bundled_local_ssds` | String | [Output Only] The configuration of bundled local SSD for the machine type. |
| `accelerators` | Vec<String> | [Output Only] A list of accelerator configurations assigned to this
machine type. |
| `is_shared_cpu` | bool | [Output Only] Whether this machine type has a shared CPU. SeeShared-core machine
types for more information. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Access machine_type outputs
machine_type_id = machine_type.id
machine_type_self_link = machine_type.self_link
machine_type_kind = machine_type.kind
machine_type_memory_mb = machine_type.memory_mb
machine_type_description = machine_type.description
machine_type_id = machine_type.id
machine_type_architecture = machine_type.architecture
machine_type_deprecated = machine_type.deprecated
machine_type_guest_cpus = machine_type.guest_cpus
machine_type_self_link_with_id = machine_type.self_link_with_id
machine_type_maximum_persistent_disks_size_gb = machine_type.maximum_persistent_disks_size_gb
machine_type_name = machine_type.name
machine_type_zone = machine_type.zone
machine_type_maximum_persistent_disks = machine_type.maximum_persistent_disks
machine_type_creation_timestamp = machine_type.creation_timestamp
machine_type_bundled_local_ssds = machine_type.bundled_local_ssds
machine_type_accelerators = machine_type.accelerators
machine_type_is_shared_cpu = machine_type.is_shared_cpu
```

---


### Reservation_sub_block

Allows customers to report a faulty subBlock.

**Operations**: ✅ Create ✅ Read

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `fault_reasons` | Vec<String> |  | The reasons for the fault experienced with the subBlock. |
| `disruption_schedule` | String |  | The disruption schedule for the subBlock. |
| `failure_component` | String |  | The component that experienced the fault. |
| `project` | String | ✅ | Project ID for this request. |
| `reservation_sub_block` | String | ✅ | The name of the reservation subBlock.
Name should conform to RFC1035 or be a resource ID. |
| `parent_name` | String | ✅ | The name of the parent reservation and parent block. In the format of
reservations/{reservation_name}/reservationBlocks/{reservation_block_name} |
| `zone` | String | ✅ | Name of the zone for this request. Zone name should conform to RFC1035. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `resource` | String |  |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create reservation_sub_block
reservation_sub_block = provider.compute_api.Reservation_sub_block {
    project = "value"  # Project ID for this request.
    reservation_sub_block = "value"  # The name of the reservation subBlock.
Name should conform to RFC1035 or be a resource ID.
    parent_name = "value"  # The name of the parent reservation and parent block. In the format of
reservations/{reservation_name}/reservationBlocks/{reservation_block_name}
    zone = "value"  # Name of the zone for this request. Zone name should conform to RFC1035.
}

# Access reservation_sub_block outputs
reservation_sub_block_id = reservation_sub_block.id
reservation_sub_block_resource = reservation_sub_block.resource
```

---


### Target_pool

Creates a target pool in the specified project and region using
the data included in the request.

**Operations**: ✅ Create ✅ Read ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `backup_pool` | String |  | The server-defined URL for the resource. This field is applicable only when
the containing target pool is serving a forwarding rule as the primary
pool, and its failoverRatio field is properly set to a value
between [0, 1].backupPool and failoverRatio together define
the fallback behavior of the primary target pool: if the ratio of the
healthy instances in the primary pool is at or belowfailoverRatio, traffic arriving at the load-balanced
IP will be directed to the backup pool.

In case where failoverRatio and backupPool
are not set, or all the instances in the backup pool are unhealthy,
the traffic will be directed back to the primary pool in the "force"
mode, where traffic will be spread to the healthy instances with the
best effort, or to all instances when no instance is healthy. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `instances` | Vec<String> |  | A list of resource URLs to the virtual machine instances serving this pool.
They must live in zones contained in the same region as this pool. |
| `failover_ratio` | f64 |  | This field is applicable only when the containing target pool is serving a
forwarding rule as the primary pool (i.e., not as a backup pool to some
other target pool). The value of the field must be in [0, 1].

If set, backupPool must also be set. They together define
the fallback behavior of the primary target pool: if the ratio of the
healthy instances in the primary pool is at or below this number,
traffic arriving at the load-balanced IP will be directed to the
backup pool.

In case where failoverRatio is not set or all the
instances in the backup pool are unhealthy, the traffic will be
directed back to the primary pool in the "force" mode, where traffic
will be spread to the healthy instances with the
best effort, or to all instances when no instance is healthy. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `security_policy` | String |  | [Output Only] The resource URL for the security policy associated with this
target pool. |
| `kind` | String |  | [Output Only] Type of the resource. Always compute#targetPool
for target pools. |
| `health_checks` | Vec<String> |  | The URL of the HttpHealthCheck resource. A member instance in this
pool is considered healthy if and only if the health checks pass.
Only legacy HttpHealthChecks are supported. Only one health check may be
specified. |
| `session_affinity` | String |  | Session affinity option, must be one of the following values: 
NONE: Connections from the same client IP may go to any
    instance in the pool. 
CLIENT_IP: Connections from the same client IP will go
    to the same instance in
    the pool while that instance remains healthy. 
CLIENT_IP_PROTO: Connections from the same client IP
    with the same IP protocol will go to the same instance in the
    pool while that instance remains healthy. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `name` | String |  | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `self_link_with_id` | String |  | [Output Only] Server-defined URL for this resource with the resource id. |
| `region` | String |  | [Output Only] URL of the region where the target pool resides. |
| `region` | String | ✅ | Name of the region scoping this request. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `backup_pool` | String | The server-defined URL for the resource. This field is applicable only when
the containing target pool is serving a forwarding rule as the primary
pool, and its failoverRatio field is properly set to a value
between [0, 1].backupPool and failoverRatio together define
the fallback behavior of the primary target pool: if the ratio of the
healthy instances in the primary pool is at or belowfailoverRatio, traffic arriving at the load-balanced
IP will be directed to the backup pool.

In case where failoverRatio and backupPool
are not set, or all the instances in the backup pool are unhealthy,
the traffic will be directed back to the primary pool in the "force"
mode, where traffic will be spread to the healthy instances with the
best effort, or to all instances when no instance is healthy. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `instances` | Vec<String> | A list of resource URLs to the virtual machine instances serving this pool.
They must live in zones contained in the same region as this pool. |
| `failover_ratio` | f64 | This field is applicable only when the containing target pool is serving a
forwarding rule as the primary pool (i.e., not as a backup pool to some
other target pool). The value of the field must be in [0, 1].

If set, backupPool must also be set. They together define
the fallback behavior of the primary target pool: if the ratio of the
healthy instances in the primary pool is at or below this number,
traffic arriving at the load-balanced IP will be directed to the
backup pool.

In case where failoverRatio is not set or all the
instances in the backup pool are unhealthy, the traffic will be
directed back to the primary pool in the "force" mode, where traffic
will be spread to the healthy instances with the
best effort, or to all instances when no instance is healthy. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `security_policy` | String | [Output Only] The resource URL for the security policy associated with this
target pool. |
| `kind` | String | [Output Only] Type of the resource. Always compute#targetPool
for target pools. |
| `health_checks` | Vec<String> | The URL of the HttpHealthCheck resource. A member instance in this
pool is considered healthy if and only if the health checks pass.
Only legacy HttpHealthChecks are supported. Only one health check may be
specified. |
| `session_affinity` | String | Session affinity option, must be one of the following values: 
NONE: Connections from the same client IP may go to any
    instance in the pool. 
CLIENT_IP: Connections from the same client IP will go
    to the same instance in
    the pool while that instance remains healthy. 
CLIENT_IP_PROTO: Connections from the same client IP
    with the same IP protocol will go to the same instance in the
    pool while that instance remains healthy. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `name` | String | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `self_link_with_id` | String | [Output Only] Server-defined URL for this resource with the resource id. |
| `region` | String | [Output Only] URL of the region where the target pool resides. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create target_pool
target_pool = provider.compute_api.Target_pool {
    region = "value"  # Name of the region scoping this request.
    project = "value"  # Project ID for this request.
}

# Access target_pool outputs
target_pool_id = target_pool.id
target_pool_backup_pool = target_pool.backup_pool
target_pool_creation_timestamp = target_pool.creation_timestamp
target_pool_instances = target_pool.instances
target_pool_failover_ratio = target_pool.failover_ratio
target_pool_description = target_pool.description
target_pool_security_policy = target_pool.security_policy
target_pool_kind = target_pool.kind
target_pool_health_checks = target_pool.health_checks
target_pool_session_affinity = target_pool.session_affinity
target_pool_id = target_pool.id
target_pool_name = target_pool.name
target_pool_self_link = target_pool.self_link
target_pool_self_link_with_id = target_pool.self_link_with_id
target_pool_region = target_pool.region
```

---


### Packet_mirroring

Creates a PacketMirroring resource in the specified project and region
using the data included in the request.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `collector_ilb` | String |  | The Forwarding Rule resource of typeloadBalancingScheme=INTERNAL that will be used as collector
for mirrored traffic.
The specified forwarding rule must have isMirroringCollector
set to true. |
| `region` | String |  | [Output Only] URI of the region where the packetMirroring resides. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `enable` | String |  | Indicates whether or not this packet mirroring takes effect.
If set to FALSE, this packet mirroring policy will not be enforced on the
network.

The default is TRUE. |
| `mirrored_resources` | String |  | PacketMirroring mirroredResourceInfos.
MirroredResourceInfo specifies a set of mirrored VM instances, subnetworks
and/or tags for which traffic from/to all VM instances will be mirrored. |
| `kind` | String |  | [Output Only] Type of the resource. Alwayscompute#packetMirroring for packet mirrorings. |
| `network` | String |  | Specifies the mirrored VPC network. Only packets in this network will be
mirrored. All mirrored VMs should have a NIC in the given network.
All mirrored subnetworks should belong to the given network. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `priority` | i64 |  | The priority of applying this configuration. Priority is used to break ties
in cases where there is more than one matching rule. In the case of two
rules that apply for a given Instance, the one with the lowest-numbered
priority value wins.

Default value is 1000. Valid range is 0 through 65535. |
| `name` | String |  | Name of the resource; provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must be
a dash, lowercase letter, or digit, except the last character, which cannot
be a dash. |
| `self_link_with_id` | String |  | [Output Only] Server-defined URL for this resource with the resource id. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `filter` | String |  | Filter for mirrored traffic. If unspecified, all IPv4 traffic is mirrored. |
| `region` | String | ✅ | Name of the region for this request. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `collector_ilb` | String | The Forwarding Rule resource of typeloadBalancingScheme=INTERNAL that will be used as collector
for mirrored traffic.
The specified forwarding rule must have isMirroringCollector
set to true. |
| `region` | String | [Output Only] URI of the region where the packetMirroring resides. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `enable` | String | Indicates whether or not this packet mirroring takes effect.
If set to FALSE, this packet mirroring policy will not be enforced on the
network.

The default is TRUE. |
| `mirrored_resources` | String | PacketMirroring mirroredResourceInfos.
MirroredResourceInfo specifies a set of mirrored VM instances, subnetworks
and/or tags for which traffic from/to all VM instances will be mirrored. |
| `kind` | String | [Output Only] Type of the resource. Alwayscompute#packetMirroring for packet mirrorings. |
| `network` | String | Specifies the mirrored VPC network. Only packets in this network will be
mirrored. All mirrored VMs should have a NIC in the given network.
All mirrored subnetworks should belong to the given network. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `priority` | i64 | The priority of applying this configuration. Priority is used to break ties
in cases where there is more than one matching rule. In the case of two
rules that apply for a given Instance, the one with the lowest-numbered
priority value wins.

Default value is 1000. Valid range is 0 through 65535. |
| `name` | String | Name of the resource; provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must be
a dash, lowercase letter, or digit, except the last character, which cannot
be a dash. |
| `self_link_with_id` | String | [Output Only] Server-defined URL for this resource with the resource id. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `filter` | String | Filter for mirrored traffic. If unspecified, all IPv4 traffic is mirrored. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create packet_mirroring
packet_mirroring = provider.compute_api.Packet_mirroring {
    region = "value"  # Name of the region for this request.
    project = "value"  # Project ID for this request.
}

# Access packet_mirroring outputs
packet_mirroring_id = packet_mirroring.id
packet_mirroring_collector_ilb = packet_mirroring.collector_ilb
packet_mirroring_region = packet_mirroring.region
packet_mirroring_description = packet_mirroring.description
packet_mirroring_enable = packet_mirroring.enable
packet_mirroring_mirrored_resources = packet_mirroring.mirrored_resources
packet_mirroring_kind = packet_mirroring.kind
packet_mirroring_network = packet_mirroring.network
packet_mirroring_creation_timestamp = packet_mirroring.creation_timestamp
packet_mirroring_priority = packet_mirroring.priority
packet_mirroring_name = packet_mirroring.name
packet_mirroring_self_link_with_id = packet_mirroring.self_link_with_id
packet_mirroring_id = packet_mirroring.id
packet_mirroring_self_link = packet_mirroring.self_link
packet_mirroring_filter = packet_mirroring.filter
```

---


### Interconnect_location

Returns the details for the specified interconnect location. Gets a list of
available interconnect locations by making a list() request.

**Operations**: ✅ Read

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `single_region_production_critical_peer_locations` | Vec<String> | [Output Only] URLs of the other locations that can pair up with this
location to support Single-Region 99.99% SLA. E.g. iad-zone1-1 and
iad-zone2-5467 are Single-Region 99.99% peer locations of each other. |
| `available_features` | Vec<String> | [Output only] List of features available at this InterconnectLocation,
which can take one of the following values:
   
   - IF_MACSEC
   - IF_CROSS_SITE_NETWORK |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `availability_zone` | String | [Output Only] Availability zone for this InterconnectLocation. Within a
metropolitan area (metro), maintenance will not be simultaneously scheduled
in more than one availability zone.  Example: "zone1" or "zone2". |
| `continent` | String | [Output Only] Continent for this location, which can take one of the
following values:
   
   - AFRICA
   - ASIA_PAC
   - EUROPE
   - NORTH_AMERICA
   - SOUTH_AMERICA |
| `address` | String | [Output Only] The postal address of the Point of Presence, each line in
the address is separated by a newline character. |
| `description` | String | [Output Only] An optional description of the resource. |
| `facility_provider` | String | [Output Only] The name of the provider for this facility (e.g., EQUINIX). |
| `supports_pzs` | bool | [Output Only] Reserved for future use. |
| `region_infos` | Vec<String> | [Output Only] A list of InterconnectLocation.RegionInfo objects, that
describe parameters pertaining to the relation between this
InterconnectLocation and various Google Cloud regions. |
| `name` | String | [Output Only] Name of the resource. |
| `status` | String | [Output Only] The status of this InterconnectLocation, which can take one
of the following values:
   
   - CLOSED: The InterconnectLocation is closed and is unavailable for
   provisioning new Interconnects.
   - AVAILABLE: The InterconnectLocation is available for provisioning new
   Interconnects. |
| `peeringdb_facility_id` | String | [Output Only] The peeringdb identifier for this facility (corresponding
with a netfac type in peeringdb). |
| `city` | String | [Output Only] Metropolitan area designator that indicates which city an
interconnect is located.
For example: "Chicago, IL", "Amsterdam, Netherlands". |
| `kind` | String | [Output Only] Type of the resource. Alwayscompute#interconnectLocation for interconnect locations. |
| `facility_provider_facility_id` | String | [Output Only] A provider-assigned Identifier for this facility (e.g.,
Ashburn-DC1). |
| `self_link_with_id` | String | [Output Only] Server-defined URL for this resource with the resource id. |
| `available_link_types` | Vec<String> | [Output only] List of link types available at this InterconnectLocation,
which can take one of the following values:
   
   - LINK_TYPE_ETHERNET_10G_LR
   - LINK_TYPE_ETHERNET_100G_LR
   - LINK_TYPE_ETHERNET_400G_LR4 |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `cross_site_interconnect_infos` | Vec<String> | [Output Only] A list of InterconnectLocation.CrossSiteInterconnectInfo
objects, that describe where Cross-Site Interconnect wires may connect to
from this location and associated connection parameters. Cross-Site
Interconnect isn't allowed to locations which are not listed. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Access interconnect_location outputs
interconnect_location_id = interconnect_location.id
interconnect_location_single_region_production_critical_peer_locations = interconnect_location.single_region_production_critical_peer_locations
interconnect_location_available_features = interconnect_location.available_features
interconnect_location_self_link = interconnect_location.self_link
interconnect_location_creation_timestamp = interconnect_location.creation_timestamp
interconnect_location_availability_zone = interconnect_location.availability_zone
interconnect_location_continent = interconnect_location.continent
interconnect_location_address = interconnect_location.address
interconnect_location_description = interconnect_location.description
interconnect_location_facility_provider = interconnect_location.facility_provider
interconnect_location_supports_pzs = interconnect_location.supports_pzs
interconnect_location_region_infos = interconnect_location.region_infos
interconnect_location_name = interconnect_location.name
interconnect_location_status = interconnect_location.status
interconnect_location_peeringdb_facility_id = interconnect_location.peeringdb_facility_id
interconnect_location_city = interconnect_location.city
interconnect_location_kind = interconnect_location.kind
interconnect_location_facility_provider_facility_id = interconnect_location.facility_provider_facility_id
interconnect_location_self_link_with_id = interconnect_location.self_link_with_id
interconnect_location_available_link_types = interconnect_location.available_link_types
interconnect_location_id = interconnect_location.id
interconnect_location_cross_site_interconnect_infos = interconnect_location.cross_site_interconnect_infos
```

---


### Security_policie

Creates a new policy in the specified project using the data included in
the request.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `adaptive_protection_config` | String |  |  |
| `advanced_options_config` | String |  |  |
| `associations` | Vec<String> |  | A list of associations that belong to this policy. |
| `name` | String |  | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `ddos_protection_config` | String |  |  |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `region` | String |  | [Output Only] URL of the region where the regional security policy
resides. This field is not applicable to global security policies. |
| `short_name` | String |  | User-provided name of the organization security policy. The name should be
unique in the organization in which the security policy is created. This
should only be used when SecurityPolicyType is CLOUD_ARMOR.
The name must be 1-63 characters long, and comply with
https://www.ietf.org/rfc/rfc1035.txt. Specifically, the name must be 1-63
characters long and match the regular expression
`[a-z]([-a-z0-9]*[a-z0-9])?` which means the first character must be a
lowercase letter, and all following characters must be a dash, lowercase
letter, or digit, except the last character, which cannot be a dash. |
| `type` | String |  | The type indicates the intended use of the security policy.
   
   - CLOUD_ARMOR: Cloud Armor backend security policies can
   be configured to filter incoming HTTP requests targeting backend services.
   They filter requests before they hit the origin servers.
   - CLOUD_ARMOR_EDGE: Cloud Armor edge security policies can
   be configured to filter incoming HTTP requests targeting backend services
   (including Cloud CDN-enabled) as well as backend buckets (Cloud Storage).
   They filter requests before the request is served from Google's cache.
   - CLOUD_ARMOR_INTERNAL_SERVICE (preview only): Cloud Armor
   internal service policies can be configured to filter HTTP requests
   targeting services managed by Traffic Director in a service mesh. They
   filter requests before the request is served from the application.

- CLOUD_ARMOR_NETWORK: Cloud Armor network policies
can be configured to filter packets targeting network load balancing
resources such as backend services, target pools, target instances, and
instances with external IPs. They filter requests before the request is
served from the application.


This field can be set only at resource creation time. |
| `fingerprint` | String |  | Specifies a fingerprint for this resource, which is essentially a hash of
the metadata's contents and used for optimistic locking. The
fingerprint is initially generated by Compute Engine and changes after
every request to modify or update metadata. You must always provide an
up-to-date fingerprint hash in order to update or change metadata,
otherwise the request will fail with error412 conditionNotMet.

To see the latest fingerprint, make get() request to the
security policy. |
| `user_defined_fields` | Vec<String> |  | Definitions of user-defined fields for CLOUD_ARMOR_NETWORK policies. A
user-defined field consists of up to 4 bytes extracted from a fixed offset
in the packet, relative to the IPv4, IPv6, TCP, or UDP header, with an
optional mask to select certain bits. Rules may then specify matching
values for these fields.

Example:

 userDefinedFields:
 - name: "ipv4_fragment_offset"
   base: IPV4
   offset: 6
   size: 2
   mask: "0x1fff" |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `labels` | HashMap<String, String> |  | Labels for this resource. These can only be added or modified by thesetLabels method. Each label key/value pair must comply withRFC1035.
Label values may be empty. |
| `label_fingerprint` | String |  | A fingerprint for the labels being applied to this security policy, which
is essentially a hash of the labels set used for optimistic locking. The
fingerprint is initially generated by Compute Engine and changes after
every request to modify or update labels. You must always provide an
up-to-date fingerprint hash in order to update or change labels.

To see the latest fingerprint, make get() request to the
security policy. |
| `kind` | String |  | [Output only] Type of the resource. Alwayscompute#securityPolicyfor security policies |
| `parent` | String |  | [Output Only] The parent of the security policy. |
| `rule_tuple_count` | i64 |  | [Output Only] Total count of all security policy rule tuples. A security
policy can not exceed a set number of tuples. |
| `cloud_armor_config` | String |  |  |
| `rules` | Vec<String> |  | A list of rules that belong to this policy.
There must always be a default rule which is a rule with priority
2147483647 and match all condition (for the match condition this means
match  "*" for srcIpRanges and for the networkMatch condition every field
must be either match "*" or not set). If no rules are provided when
creating a security policy, a default rule with action "allow" will be
added. |
| `self_link_with_id` | String |  | [Output Only] Server-defined URL for this resource with the resource id. |
| `display_name` | String |  | User-provided name of the organization security policy. The name should be
unique in the organization in which the security policy is created. This
should only be used when SecurityPolicyType is FIREWALL.
The name must be 1-63 characters long, and comply with
https://www.ietf.org/rfc/rfc1035.txt. Specifically, the name must be 1-63
characters long and match the regular expression
`[a-z]([-a-z0-9]*[a-z0-9])?` which means the first character must be a
lowercase letter, and all following characters must be a dash, lowercase
letter, or digit, except the last character, which cannot be a dash. |
| `recaptcha_options_config` | String |  |  |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `adaptive_protection_config` | String |  |
| `advanced_options_config` | String |  |
| `associations` | Vec<String> | A list of associations that belong to this policy. |
| `name` | String | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `ddos_protection_config` | String |  |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `region` | String | [Output Only] URL of the region where the regional security policy
resides. This field is not applicable to global security policies. |
| `short_name` | String | User-provided name of the organization security policy. The name should be
unique in the organization in which the security policy is created. This
should only be used when SecurityPolicyType is CLOUD_ARMOR.
The name must be 1-63 characters long, and comply with
https://www.ietf.org/rfc/rfc1035.txt. Specifically, the name must be 1-63
characters long and match the regular expression
`[a-z]([-a-z0-9]*[a-z0-9])?` which means the first character must be a
lowercase letter, and all following characters must be a dash, lowercase
letter, or digit, except the last character, which cannot be a dash. |
| `type` | String | The type indicates the intended use of the security policy.
   
   - CLOUD_ARMOR: Cloud Armor backend security policies can
   be configured to filter incoming HTTP requests targeting backend services.
   They filter requests before they hit the origin servers.
   - CLOUD_ARMOR_EDGE: Cloud Armor edge security policies can
   be configured to filter incoming HTTP requests targeting backend services
   (including Cloud CDN-enabled) as well as backend buckets (Cloud Storage).
   They filter requests before the request is served from Google's cache.
   - CLOUD_ARMOR_INTERNAL_SERVICE (preview only): Cloud Armor
   internal service policies can be configured to filter HTTP requests
   targeting services managed by Traffic Director in a service mesh. They
   filter requests before the request is served from the application.

- CLOUD_ARMOR_NETWORK: Cloud Armor network policies
can be configured to filter packets targeting network load balancing
resources such as backend services, target pools, target instances, and
instances with external IPs. They filter requests before the request is
served from the application.


This field can be set only at resource creation time. |
| `fingerprint` | String | Specifies a fingerprint for this resource, which is essentially a hash of
the metadata's contents and used for optimistic locking. The
fingerprint is initially generated by Compute Engine and changes after
every request to modify or update metadata. You must always provide an
up-to-date fingerprint hash in order to update or change metadata,
otherwise the request will fail with error412 conditionNotMet.

To see the latest fingerprint, make get() request to the
security policy. |
| `user_defined_fields` | Vec<String> | Definitions of user-defined fields for CLOUD_ARMOR_NETWORK policies. A
user-defined field consists of up to 4 bytes extracted from a fixed offset
in the packet, relative to the IPv4, IPv6, TCP, or UDP header, with an
optional mask to select certain bits. Rules may then specify matching
values for these fields.

Example:

 userDefinedFields:
 - name: "ipv4_fragment_offset"
   base: IPV4
   offset: 6
   size: 2
   mask: "0x1fff" |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `labels` | HashMap<String, String> | Labels for this resource. These can only be added or modified by thesetLabels method. Each label key/value pair must comply withRFC1035.
Label values may be empty. |
| `label_fingerprint` | String | A fingerprint for the labels being applied to this security policy, which
is essentially a hash of the labels set used for optimistic locking. The
fingerprint is initially generated by Compute Engine and changes after
every request to modify or update labels. You must always provide an
up-to-date fingerprint hash in order to update or change labels.

To see the latest fingerprint, make get() request to the
security policy. |
| `kind` | String | [Output only] Type of the resource. Alwayscompute#securityPolicyfor security policies |
| `parent` | String | [Output Only] The parent of the security policy. |
| `rule_tuple_count` | i64 | [Output Only] Total count of all security policy rule tuples. A security
policy can not exceed a set number of tuples. |
| `cloud_armor_config` | String |  |
| `rules` | Vec<String> | A list of rules that belong to this policy.
There must always be a default rule which is a rule with priority
2147483647 and match all condition (for the match condition this means
match  "*" for srcIpRanges and for the networkMatch condition every field
must be either match "*" or not set). If no rules are provided when
creating a security policy, a default rule with action "allow" will be
added. |
| `self_link_with_id` | String | [Output Only] Server-defined URL for this resource with the resource id. |
| `display_name` | String | User-provided name of the organization security policy. The name should be
unique in the organization in which the security policy is created. This
should only be used when SecurityPolicyType is FIREWALL.
The name must be 1-63 characters long, and comply with
https://www.ietf.org/rfc/rfc1035.txt. Specifically, the name must be 1-63
characters long and match the regular expression
`[a-z]([-a-z0-9]*[a-z0-9])?` which means the first character must be a
lowercase letter, and all following characters must be a dash, lowercase
letter, or digit, except the last character, which cannot be a dash. |
| `recaptcha_options_config` | String |  |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create security_policie
security_policie = provider.compute_api.Security_policie {
    project = "value"  # Project ID for this request.
}

# Access security_policie outputs
security_policie_id = security_policie.id
security_policie_adaptive_protection_config = security_policie.adaptive_protection_config
security_policie_advanced_options_config = security_policie.advanced_options_config
security_policie_associations = security_policie.associations
security_policie_name = security_policie.name
security_policie_id = security_policie.id
security_policie_ddos_protection_config = security_policie.ddos_protection_config
security_policie_description = security_policie.description
security_policie_self_link = security_policie.self_link
security_policie_region = security_policie.region
security_policie_short_name = security_policie.short_name
security_policie_type = security_policie.type
security_policie_fingerprint = security_policie.fingerprint
security_policie_user_defined_fields = security_policie.user_defined_fields
security_policie_creation_timestamp = security_policie.creation_timestamp
security_policie_labels = security_policie.labels
security_policie_label_fingerprint = security_policie.label_fingerprint
security_policie_kind = security_policie.kind
security_policie_parent = security_policie.parent
security_policie_rule_tuple_count = security_policie.rule_tuple_count
security_policie_cloud_armor_config = security_policie.cloud_armor_config
security_policie_rules = security_policie.rules
security_policie_self_link_with_id = security_policie.self_link_with_id
security_policie_display_name = security_policie.display_name
security_policie_recaptcha_options_config = security_policie.recaptcha_options_config
```

---


### Zone

Returns the specified Zone resource.

**Operations**: ✅ Read

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `region` | String | [Output Only] Full URL reference to the region which hosts the zone. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `available_cpu_platforms` | Vec<String> | [Output Only] Available cpu/platform selections for the zone. |
| `status` | String | [Output Only] Status of the zone, either UP orDOWN. |
| `supports_pzs` | bool | [Output Only] Reserved for future use. |
| `name` | String | [Output Only] Name of the resource. |
| `kind` | String | [Output Only] Type of the resource. Always compute#zone for
zones. |
| `description` | String | [Output Only] Textual description of the resource. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `deprecated` | String | [Output Only] The deprecation status associated with this zone. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Access zone outputs
zone_id = zone.id
zone_region = zone.region
zone_self_link = zone.self_link
zone_available_cpu_platforms = zone.available_cpu_platforms
zone_status = zone.status
zone_supports_pzs = zone.supports_pzs
zone_name = zone.name
zone_kind = zone.kind
zone_description = zone.description
zone_id = zone.id
zone_creation_timestamp = zone.creation_timestamp
zone_deprecated = zone.deprecated
```

---


### Health_check

Creates a HealthCheck resource in the specified project using the data
included in the request.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `http2_health_check` | String |  |  |
| `kind` | String |  | Type of the resource. |
| `self_link_with_id` | String |  | [Output Only] Server-defined URL for this resource with the resource id. |
| `grpc_tls_health_check` | String |  |  |
| `source_regions` | Vec<String> |  | The list of cloud regions from which health checks are performed. If any
regions are specified, then exactly 3 regions should be specified. The
region names must be valid names of Google Cloud regions.
This can only be set for global health check.
If this list is non-empty, then there are restrictions
on what other health check fields are supported and what other resources
can use this health check:
   
   - SSL, HTTP2, and GRPC protocols are not supported.
   - The TCP request field is not supported.
   - The proxyHeader field for HTTP, HTTPS, and TCP is not
   supported.
   - The checkIntervalSec field must be at least 30.
   - The health check cannot be used with BackendService nor with managed
   instance group auto-healing. |
| `ssl_health_check` | String |  |  |
| `region` | String |  | [Output Only] Region where the health check resides.  Not applicable to
global health checks. |
| `healthy_threshold` | i64 |  | A so-far unhealthy instance will be marked healthy after this
many consecutive successes. The default value is 2. |
| `http_health_check` | String |  |  |
| `tcp_health_check` | String |  |  |
| `udp_health_check` | String |  |  |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp in3339
text format. |
| `grpc_health_check` | String |  |  |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `log_config` | String |  | Configure logging on this health check. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `name` | String |  | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
For example, a name that is 1-63 characters long, matches the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?`, and otherwise complies with
RFC1035. This regular expression describes a name where the first
character is a lowercase letter, and all following characters are a dash,
lowercase letter, or digit, except the last character, which isn't a dash. |
| `check_interval_sec` | i64 |  | How often (in seconds) to send a health check. The default value is 5
seconds. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `type` | String |  | Specifies the type of the healthCheck, either TCP,SSL, HTTP, HTTPS,HTTP2 or GRPC. Exactly one of the
protocol-specific health check fields must be specified, which must matchtype field. |
| `unhealthy_threshold` | i64 |  | A so-far healthy instance will be marked unhealthy after this many
consecutive failures. The default value is 2. |
| `https_health_check` | String |  |  |
| `timeout_sec` | i64 |  | How long (in seconds) to wait before claiming failure. The default value is
5 seconds. It is invalid for timeoutSec to have greater
value than checkIntervalSec. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `http2_health_check` | String |  |
| `kind` | String | Type of the resource. |
| `self_link_with_id` | String | [Output Only] Server-defined URL for this resource with the resource id. |
| `grpc_tls_health_check` | String |  |
| `source_regions` | Vec<String> | The list of cloud regions from which health checks are performed. If any
regions are specified, then exactly 3 regions should be specified. The
region names must be valid names of Google Cloud regions.
This can only be set for global health check.
If this list is non-empty, then there are restrictions
on what other health check fields are supported and what other resources
can use this health check:
   
   - SSL, HTTP2, and GRPC protocols are not supported.
   - The TCP request field is not supported.
   - The proxyHeader field for HTTP, HTTPS, and TCP is not
   supported.
   - The checkIntervalSec field must be at least 30.
   - The health check cannot be used with BackendService nor with managed
   instance group auto-healing. |
| `ssl_health_check` | String |  |
| `region` | String | [Output Only] Region where the health check resides.  Not applicable to
global health checks. |
| `healthy_threshold` | i64 | A so-far unhealthy instance will be marked healthy after this
many consecutive successes. The default value is 2. |
| `http_health_check` | String |  |
| `tcp_health_check` | String |  |
| `udp_health_check` | String |  |
| `creation_timestamp` | String | [Output Only] Creation timestamp in3339
text format. |
| `grpc_health_check` | String |  |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `log_config` | String | Configure logging on this health check. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `name` | String | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
For example, a name that is 1-63 characters long, matches the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?`, and otherwise complies with
RFC1035. This regular expression describes a name where the first
character is a lowercase letter, and all following characters are a dash,
lowercase letter, or digit, except the last character, which isn't a dash. |
| `check_interval_sec` | i64 | How often (in seconds) to send a health check. The default value is 5
seconds. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `type` | String | Specifies the type of the healthCheck, either TCP,SSL, HTTP, HTTPS,HTTP2 or GRPC. Exactly one of the
protocol-specific health check fields must be specified, which must matchtype field. |
| `unhealthy_threshold` | i64 | A so-far healthy instance will be marked unhealthy after this many
consecutive failures. The default value is 2. |
| `https_health_check` | String |  |
| `timeout_sec` | i64 | How long (in seconds) to wait before claiming failure. The default value is
5 seconds. It is invalid for timeoutSec to have greater
value than checkIntervalSec. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create health_check
health_check = provider.compute_api.Health_check {
    project = "value"  # Project ID for this request.
}

# Access health_check outputs
health_check_id = health_check.id
health_check_http2_health_check = health_check.http2_health_check
health_check_kind = health_check.kind
health_check_self_link_with_id = health_check.self_link_with_id
health_check_grpc_tls_health_check = health_check.grpc_tls_health_check
health_check_source_regions = health_check.source_regions
health_check_ssl_health_check = health_check.ssl_health_check
health_check_region = health_check.region
health_check_healthy_threshold = health_check.healthy_threshold
health_check_http_health_check = health_check.http_health_check
health_check_tcp_health_check = health_check.tcp_health_check
health_check_udp_health_check = health_check.udp_health_check
health_check_creation_timestamp = health_check.creation_timestamp
health_check_grpc_health_check = health_check.grpc_health_check
health_check_id = health_check.id
health_check_log_config = health_check.log_config
health_check_self_link = health_check.self_link
health_check_name = health_check.name
health_check_check_interval_sec = health_check.check_interval_sec
health_check_description = health_check.description
health_check_type = health_check.type
health_check_unhealthy_threshold = health_check.unhealthy_threshold
health_check_https_health_check = health_check.https_health_check
health_check_timeout_sec = health_check.timeout_sec
```

---


### Global_forwarding_rule

Creates a GlobalForwardingRule resource in the specified project using
the data included in the request.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `fingerprint` | String |  | Fingerprint of this resource. A hash of the contents stored in this object.
This field is used in optimistic locking. This field will be ignored when
inserting a ForwardingRule. Include the fingerprint in patch request to
ensure that you do not overwrite changes that were applied from another
concurrent request.

To see the latest fingerprint, make a get() request to
retrieve a ForwardingRule. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `region` | String |  | [Output Only] URL of the region where the regional forwarding rule resides.
This field is not applicable to global forwarding rules.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `allow_psc_packet_injection` | bool |  | This is used in PSC consumer ForwardingRule to control whether the producer
is allowed to inject packets into the consumer's network. If set to true,
the target service attachment must have tunneling enabled and
TunnelingConfig.RoutingMode set to PACKET_INJECTION
Non-PSC forwarding rules should not use this field.

This field was never released to any customers and is deprecated and
will be removed in the future. |
| `name` | String |  | Name of the resource; provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash.

For Private Service Connect forwarding rules that forward traffic to Google
APIs, the forwarding rule name must be a 1-20 characters string with
lowercase letters and numbers and must start with a letter. |
| `network_tier` | String |  | This signifies the networking tier used for configuring
this load balancer and can only take the following values:PREMIUM, STANDARD.

For regional ForwardingRule, the valid values are PREMIUM andSTANDARD. For GlobalForwardingRule, the valid value isPREMIUM.

If this field is not specified, it is assumed to be PREMIUM.
If IPAddress is specified, this value must be equal to the
networkTier of the Address. |
| `psc_connection_status` | String |  |  |
| `subnetwork` | String |  | This field identifies the subnetwork that the load balanced IP should
belong to for this forwarding rule, used with internal load balancers and
external passthrough Network Load Balancers with IPv6.

If the network specified is in auto subnet mode, this field is optional.
However, a subnetwork must be specified if the network is in custom subnet
mode or when creating external forwarding rule with IPv6. |
| `availability_group` | String |  | [Output Only] Specifies the availability group of the forwarding rule. This
field is for use by global external passthrough load balancers (load
balancing scheme EXTERNAL_PASSTHROUGH) and is set for the child forwarding
rules only. |
| `is_mirroring_collector` | bool |  | Indicates whether or not this load balancer can be used as a collector for
packet mirroring. To prevent mirroring loops, instances behind this
load balancer will not have their traffic mirrored even if aPacketMirroring rule applies to them.
This can only be set to true for load balancers that have theirloadBalancingScheme set to INTERNAL. |
| `ip_address` | String |  | IP address for which this forwarding rule accepts traffic. When a client
sends traffic to this IP address, the forwarding rule directs the traffic
to the referenced target or backendService.
While creating a forwarding rule, specifying an IPAddress is
required under the following circumstances:

   
   - When the target is set to targetGrpcProxy andvalidateForProxyless is set to true, theIPAddress should be set to 0.0.0.0.
   - When the target is a Private Service Connect Google APIs
   bundle, you must specify an IPAddress.


Otherwise, you can optionally specify an IP address that references an
existing static (reserved) IP address resource. When omitted, Google Cloud
assigns an ephemeral IP address.

Use one of the following formats to specify an IP address while creating a
forwarding rule:

* IP address number, as in `100.1.2.3`
* IPv6 address range, as in `2600:1234::/96`
* Full resource URL, as inhttps://www.googleapis.com/compute/v1/projects/project_id/regions/region/addresses/address-name
* Partial URL or by name, as in:
   
   - projects/project_id/regions/region/addresses/address-name
   - regions/region/addresses/address-name
   - global/addresses/address-name
   - address-name



The forwarding rule's target or backendService,
and in most cases, also the loadBalancingScheme, determine the
type of IP address that you can use. For detailed information, see
[IP address
specifications](https://cloud.google.com/load-balancing/docs/forwarding-rule-concepts#ip_address_specifications).

When reading an IPAddress, the API always returns the IP
address number. |
| `external_managed_backend_bucket_migration_testing_percentage` | f64 |  | Determines the fraction of requests to backend buckets that should be
processed by the global external Application Load Balancer.

The value of this field must be in the range [0, 100].

This value can only be set if the loadBalancingScheme in the BackendService
is set to EXTERNAL (when using the classic Application Load Balancer) and
the migration state is TEST_BY_PERCENTAGE. |
| `service_directory_registrations` | Vec<String> |  | Service Directory resources to register this forwarding rule with.
Currently, only supports a single Service Directory resource. |
| `ports` | Vec<String> |  | The ports, portRange, and allPorts
fields are mutually exclusive. Only packets addressed to ports in the
specified range will be forwarded to the backends configured with this
forwarding rule.

The ports field has the following limitations:
   
   - It requires that the forwarding rule IPProtocol be TCP,
   UDP, or SCTP, and
   - It's applicable only to the following products: internal passthrough
   Network Load Balancers, backend service-based external passthrough Network
   Load Balancers, and internal protocol forwarding.
   - You can specify a list of up to five ports by number, separated by
   commas. The ports can be contiguous or discontiguous.



For external forwarding rules, two or more forwarding rules cannot use the
same [IPAddress, IPProtocol] pair if they share at least one
port number.

For internal forwarding rules within the same VPC network, two or more
forwarding rules cannot use the same [IPAddress, IPProtocol]
pair if they share at least one port number.

@pattern: \\d+(?:-\\d+)? |
| `label_fingerprint` | String |  | A fingerprint for the labels being applied to this resource, which is
essentially a hash of the labels set used for optimistic locking. The
fingerprint is initially generated by Compute Engine and changes after
every request to modify or update labels. You must always provide an
up-to-date fingerprint hash in order to update or change labels,
otherwise the request will fail with error412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve a ForwardingRule. |
| `child_forwarding_rules` | Vec<String> |  | [Output Only] Applicable only to the parent forwarding rule of global
external passthrough load balancers. This field contains the list of child
forwarding rule URLs associated with the parent forwarding rule: one for
each availability group. AVAILABILITY_GROUP0 will be the first element, and
AVAILABILITY_GROUP1 will be the second element. |
| `external_managed_backend_bucket_migration_state` | String |  | Specifies the canary migration state for the backend buckets attached to
this forwarding rule. Possible values are PREPARE, TEST_BY_PERCENTAGE, and
TEST_ALL_TRAFFIC.

To begin the migration from EXTERNAL to EXTERNAL_MANAGED, the state must be
changed to PREPARE. The state must be changed to TEST_ALL_TRAFFIC before
the loadBalancingScheme can be changed to EXTERNAL_MANAGED. Optionally, the
TEST_BY_PERCENTAGE state can be used to migrate traffic to backend buckets
attached to this forwarding rule by percentage using
externalManagedBackendBucketMigrationTestingPercentage.

Rolling back a migration requires the states to be set in reverse order. So
changing the scheme from EXTERNAL_MANAGED to EXTERNAL requires the state to
be set to TEST_ALL_TRAFFIC at the same time. Optionally, the
TEST_BY_PERCENTAGE state can be used to migrate some traffic back to
EXTERNAL or PREPARE can be used to migrate all traffic back to EXTERNAL. |
| `parent_forwarding_rule` | String |  | [Output Only] Applicable only to the child forwarding rules of global external
passthrough load balancers. This field contains the URL of the parent
forwarding rule. |
| `metadata_filters` | Vec<String> |  | Opaque filter criteria used by load balancer to restrict routing
configuration to a limited set of xDS
compliant clients. In their xDS requests to load balancer, xDS clients
present node
metadata. When there is a match, the relevant configuration
is made available to those proxies. Otherwise, all the resources (e.g.TargetHttpProxy, UrlMap)
referenced by the ForwardingRule are not visible to
those proxies.

For each metadataFilter in this list, if itsfilterMatchCriteria is set to MATCH_ANY, at least one of thefilterLabels must match the corresponding label provided in
the metadata. If its filterMatchCriteria is set to
MATCH_ALL, then all of its filterLabels must match with
corresponding labels provided in the metadata. If multiplemetadataFilters are specified, all of them need to be satisfied
in order to be considered a match.

metadataFilters specified here will be applifed before
those specified in the UrlMap that thisForwardingRule references.

metadataFilters only applies to Loadbalancers that have
their loadBalancingScheme set toINTERNAL_SELF_MANAGED. |
| `source_ip_ranges` | Vec<String> |  | If not empty, this forwarding rule will only forward the traffic when the
source IP address matches one of the IP addresses or CIDR ranges set here.
Note that a forwarding rule can only have up to 64 source IP ranges, and
this field can only be used with a regional forwarding rule whose scheme isEXTERNAL.
Each source_ip_range entry should be either an IP address (for
example, 1.2.3.4) or a CIDR range (for example, 1.2.3.0/24). |
| `kind` | String |  | [Output Only] Type of the resource. Alwayscompute#forwardingRule for forwarding rule resources. |
| `all_ports` | bool |  | The ports, portRange, and allPorts
fields are mutually exclusive. Only packets addressed to ports in the
specified range will be forwarded to the backends configured with this
forwarding rule.

The allPorts field has the following limitations:
   
   - It requires that the forwarding rule IPProtocol be TCP,
   UDP, SCTP, or L3_DEFAULT.
   - It's applicable only to the following products: internal passthrough
   Network Load Balancers, backend service-based external passthrough Network
   Load Balancers, and internal and external protocol forwarding.
   - Set this field to true to allow packets addressed to any port or
   packets lacking destination port information (for example, UDP fragments
   after the first fragment) to be forwarded to the backends configured with
   this forwarding rule. The L3_DEFAULT protocol requiresallPorts be set to true. |
| `ip_version` | String |  | The IP Version that will be used by this forwarding rule.  Valid options
are IPV4 or IPV6. |
| `port_range` | String |  | The ports, portRange, and allPorts
fields are mutually exclusive. Only packets addressed to ports in the
specified range will be forwarded to the backends configured with this
forwarding rule.

The portRange field has the following limitations:
   
   - It requires that the forwarding rule IPProtocol be TCP,
   UDP, or SCTP, and
   - It's applicable only to the following products: external passthrough
   Network Load Balancers, internal and external proxy Network Load Balancers,
   internal and external Application Load Balancers, external protocol
   forwarding, and Classic VPN.
   - Some products have restrictions on what ports can be used. See 
   port specifications for details.



For external forwarding rules, two or more forwarding rules cannot use the
same [IPAddress, IPProtocol] pair, and cannot have overlappingportRanges.

For internal forwarding rules within the same VPC network, two or more
forwarding rules cannot use the same [IPAddress, IPProtocol]
pair, and cannot have overlapping portRanges.

@pattern: \\d+(?:-\\d+)? |
| `allow_psc_global_access` | bool |  | This is used in PSC consumer ForwardingRule to control whether the PSC
endpoint can be accessed from another region. |
| `service_label` | String |  | An optional prefix to the service name for this forwarding rule.
If specified, the prefix is the first label of the fully qualified service
name.

The label must be 1-63 characters long, and comply withRFC1035.
Specifically, the label must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash.

This field is only used for internal load balancing. |
| `allow_global_access` | bool |  | If set to true, clients can access the internal passthrough Network Load
Balancers, the regional internal Application Load Balancer, and the
regional internal proxy Network Load Balancer from all regions.
If false, only allows access from the local region the load balancer is
located at. Note that for INTERNAL_MANAGED forwarding rules, this field
cannot be changed after the forwarding rule is created. |
| `backend_service` | String |  | Identifies the backend service to which the forwarding rule sends traffic.
Required for internal and external passthrough Network Load Balancers;
must be omitted for all other load balancer types. |
| `base_forwarding_rule` | String |  | [Output Only] The URL for the corresponding base forwarding rule. By base
forwarding rule, we mean the forwarding rule that has the same IP address,
protocol, and port settings with the current forwarding rule, but without
sourceIPRanges specified.
Always empty if the current forwarding rule does not have sourceIPRanges
specified. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `psc_connection_id` | String |  | [Output Only] The PSC connection id of the PSC forwarding rule. |
| `load_balancing_scheme` | String |  | Specifies the forwarding rule type.

For more information about forwarding rules, refer to
Forwarding rule concepts. |
| `labels` | HashMap<String, String> |  | Labels for this resource. These can only be added or modified by thesetLabels method. Each label key/value pair must comply withRFC1035.
Label values may be empty. |
| `network` | String |  | This field is not used for global external load balancing.

For internal passthrough Network Load Balancers, this field identifies the
network that the load balanced IP should belong to for this forwarding
rule.
If the subnetwork is specified, the network of the subnetwork will be used.
If neither subnetwork nor this field is specified, the default network will
be used.

For Private Service Connect forwarding rules that forward traffic to Google
APIs, a network must be provided. |
| `self_link_with_id` | String |  | [Output Only] Server-defined URL for this resource with the resource id. |
| `ip_protocol` | String |  | The IP protocol to which this rule applies.

For protocol forwarding, valid
options are TCP, UDP, ESP,AH, SCTP, ICMP andL3_DEFAULT.

The valid IP protocols are different for different load balancing products
as described in [Load balancing
features](https://cloud.google.com/load-balancing/docs/features#protocols_from_the_load_balancer_to_the_backends). |
| `no_automate_dns_zone` | bool |  | This is used in PSC consumer ForwardingRule to control whether it should
try to auto-generate a DNS zone or not. Non-PSC forwarding rules do not use
this field. Once set, this field is not mutable. |
| `service_name` | String |  | [Output Only]
The internal fully qualified service name for this forwarding rule.

This field is only used for internal load balancing. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `target` | String |  | The URL of the target resource to receive the matched traffic.  For
regional forwarding rules, this target must be in the same region as the
forwarding rule. For global forwarding rules, this target must be a global
load balancing resource.

The forwarded traffic must be of a type appropriate to the target object.
   
   
     -  For load balancers, see the "Target" column in [Port specifications](https://cloud.google.com/load-balancing/docs/forwarding-rule-concepts#ip_address_specifications).
     -  For Private Service Connect forwarding rules that forward traffic to Google APIs, provide the name of a supported Google API bundle:
       
      
            -  vpc-sc -  APIs that support VPC Service Controls. 
            -  all-apis - All supported Google APIs. 
          
     
     -  For Private Service Connect forwarding rules that forward traffic to managed services, the target must be a service attachment. The target is not mutable once set as a service attachment. |
| `ip_collection` | String |  | Resource reference of a PublicDelegatedPrefix. The PDP must
be a sub-PDP in EXTERNAL_IPV6_FORWARDING_RULE_CREATION mode.

Use one of the following formats to specify a sub-PDP when creating an IPv6
NetLB forwarding rule using BYOIP:
Full resource URL, as inhttps://www.googleapis.com/compute/v1/projects/project_id/regions/region/publicDelegatedPrefixes/sub-pdp-name
Partial URL, as in:
   
   - projects/project_id/regions/region/publicDelegatedPrefixes/sub-pdp-name
   - regions/region/publicDelegatedPrefixes/sub-pdp-name |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `fingerprint` | String | Fingerprint of this resource. A hash of the contents stored in this object.
This field is used in optimistic locking. This field will be ignored when
inserting a ForwardingRule. Include the fingerprint in patch request to
ensure that you do not overwrite changes that were applied from another
concurrent request.

To see the latest fingerprint, make a get() request to
retrieve a ForwardingRule. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `region` | String | [Output Only] URL of the region where the regional forwarding rule resides.
This field is not applicable to global forwarding rules.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `allow_psc_packet_injection` | bool | This is used in PSC consumer ForwardingRule to control whether the producer
is allowed to inject packets into the consumer's network. If set to true,
the target service attachment must have tunneling enabled and
TunnelingConfig.RoutingMode set to PACKET_INJECTION
Non-PSC forwarding rules should not use this field.

This field was never released to any customers and is deprecated and
will be removed in the future. |
| `name` | String | Name of the resource; provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash.

For Private Service Connect forwarding rules that forward traffic to Google
APIs, the forwarding rule name must be a 1-20 characters string with
lowercase letters and numbers and must start with a letter. |
| `network_tier` | String | This signifies the networking tier used for configuring
this load balancer and can only take the following values:PREMIUM, STANDARD.

For regional ForwardingRule, the valid values are PREMIUM andSTANDARD. For GlobalForwardingRule, the valid value isPREMIUM.

If this field is not specified, it is assumed to be PREMIUM.
If IPAddress is specified, this value must be equal to the
networkTier of the Address. |
| `psc_connection_status` | String |  |
| `subnetwork` | String | This field identifies the subnetwork that the load balanced IP should
belong to for this forwarding rule, used with internal load balancers and
external passthrough Network Load Balancers with IPv6.

If the network specified is in auto subnet mode, this field is optional.
However, a subnetwork must be specified if the network is in custom subnet
mode or when creating external forwarding rule with IPv6. |
| `availability_group` | String | [Output Only] Specifies the availability group of the forwarding rule. This
field is for use by global external passthrough load balancers (load
balancing scheme EXTERNAL_PASSTHROUGH) and is set for the child forwarding
rules only. |
| `is_mirroring_collector` | bool | Indicates whether or not this load balancer can be used as a collector for
packet mirroring. To prevent mirroring loops, instances behind this
load balancer will not have their traffic mirrored even if aPacketMirroring rule applies to them.
This can only be set to true for load balancers that have theirloadBalancingScheme set to INTERNAL. |
| `ip_address` | String | IP address for which this forwarding rule accepts traffic. When a client
sends traffic to this IP address, the forwarding rule directs the traffic
to the referenced target or backendService.
While creating a forwarding rule, specifying an IPAddress is
required under the following circumstances:

   
   - When the target is set to targetGrpcProxy andvalidateForProxyless is set to true, theIPAddress should be set to 0.0.0.0.
   - When the target is a Private Service Connect Google APIs
   bundle, you must specify an IPAddress.


Otherwise, you can optionally specify an IP address that references an
existing static (reserved) IP address resource. When omitted, Google Cloud
assigns an ephemeral IP address.

Use one of the following formats to specify an IP address while creating a
forwarding rule:

* IP address number, as in `100.1.2.3`
* IPv6 address range, as in `2600:1234::/96`
* Full resource URL, as inhttps://www.googleapis.com/compute/v1/projects/project_id/regions/region/addresses/address-name
* Partial URL or by name, as in:
   
   - projects/project_id/regions/region/addresses/address-name
   - regions/region/addresses/address-name
   - global/addresses/address-name
   - address-name



The forwarding rule's target or backendService,
and in most cases, also the loadBalancingScheme, determine the
type of IP address that you can use. For detailed information, see
[IP address
specifications](https://cloud.google.com/load-balancing/docs/forwarding-rule-concepts#ip_address_specifications).

When reading an IPAddress, the API always returns the IP
address number. |
| `external_managed_backend_bucket_migration_testing_percentage` | f64 | Determines the fraction of requests to backend buckets that should be
processed by the global external Application Load Balancer.

The value of this field must be in the range [0, 100].

This value can only be set if the loadBalancingScheme in the BackendService
is set to EXTERNAL (when using the classic Application Load Balancer) and
the migration state is TEST_BY_PERCENTAGE. |
| `service_directory_registrations` | Vec<String> | Service Directory resources to register this forwarding rule with.
Currently, only supports a single Service Directory resource. |
| `ports` | Vec<String> | The ports, portRange, and allPorts
fields are mutually exclusive. Only packets addressed to ports in the
specified range will be forwarded to the backends configured with this
forwarding rule.

The ports field has the following limitations:
   
   - It requires that the forwarding rule IPProtocol be TCP,
   UDP, or SCTP, and
   - It's applicable only to the following products: internal passthrough
   Network Load Balancers, backend service-based external passthrough Network
   Load Balancers, and internal protocol forwarding.
   - You can specify a list of up to five ports by number, separated by
   commas. The ports can be contiguous or discontiguous.



For external forwarding rules, two or more forwarding rules cannot use the
same [IPAddress, IPProtocol] pair if they share at least one
port number.

For internal forwarding rules within the same VPC network, two or more
forwarding rules cannot use the same [IPAddress, IPProtocol]
pair if they share at least one port number.

@pattern: \\d+(?:-\\d+)? |
| `label_fingerprint` | String | A fingerprint for the labels being applied to this resource, which is
essentially a hash of the labels set used for optimistic locking. The
fingerprint is initially generated by Compute Engine and changes after
every request to modify or update labels. You must always provide an
up-to-date fingerprint hash in order to update or change labels,
otherwise the request will fail with error412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve a ForwardingRule. |
| `child_forwarding_rules` | Vec<String> | [Output Only] Applicable only to the parent forwarding rule of global
external passthrough load balancers. This field contains the list of child
forwarding rule URLs associated with the parent forwarding rule: one for
each availability group. AVAILABILITY_GROUP0 will be the first element, and
AVAILABILITY_GROUP1 will be the second element. |
| `external_managed_backend_bucket_migration_state` | String | Specifies the canary migration state for the backend buckets attached to
this forwarding rule. Possible values are PREPARE, TEST_BY_PERCENTAGE, and
TEST_ALL_TRAFFIC.

To begin the migration from EXTERNAL to EXTERNAL_MANAGED, the state must be
changed to PREPARE. The state must be changed to TEST_ALL_TRAFFIC before
the loadBalancingScheme can be changed to EXTERNAL_MANAGED. Optionally, the
TEST_BY_PERCENTAGE state can be used to migrate traffic to backend buckets
attached to this forwarding rule by percentage using
externalManagedBackendBucketMigrationTestingPercentage.

Rolling back a migration requires the states to be set in reverse order. So
changing the scheme from EXTERNAL_MANAGED to EXTERNAL requires the state to
be set to TEST_ALL_TRAFFIC at the same time. Optionally, the
TEST_BY_PERCENTAGE state can be used to migrate some traffic back to
EXTERNAL or PREPARE can be used to migrate all traffic back to EXTERNAL. |
| `parent_forwarding_rule` | String | [Output Only] Applicable only to the child forwarding rules of global external
passthrough load balancers. This field contains the URL of the parent
forwarding rule. |
| `metadata_filters` | Vec<String> | Opaque filter criteria used by load balancer to restrict routing
configuration to a limited set of xDS
compliant clients. In their xDS requests to load balancer, xDS clients
present node
metadata. When there is a match, the relevant configuration
is made available to those proxies. Otherwise, all the resources (e.g.TargetHttpProxy, UrlMap)
referenced by the ForwardingRule are not visible to
those proxies.

For each metadataFilter in this list, if itsfilterMatchCriteria is set to MATCH_ANY, at least one of thefilterLabels must match the corresponding label provided in
the metadata. If its filterMatchCriteria is set to
MATCH_ALL, then all of its filterLabels must match with
corresponding labels provided in the metadata. If multiplemetadataFilters are specified, all of them need to be satisfied
in order to be considered a match.

metadataFilters specified here will be applifed before
those specified in the UrlMap that thisForwardingRule references.

metadataFilters only applies to Loadbalancers that have
their loadBalancingScheme set toINTERNAL_SELF_MANAGED. |
| `source_ip_ranges` | Vec<String> | If not empty, this forwarding rule will only forward the traffic when the
source IP address matches one of the IP addresses or CIDR ranges set here.
Note that a forwarding rule can only have up to 64 source IP ranges, and
this field can only be used with a regional forwarding rule whose scheme isEXTERNAL.
Each source_ip_range entry should be either an IP address (for
example, 1.2.3.4) or a CIDR range (for example, 1.2.3.0/24). |
| `kind` | String | [Output Only] Type of the resource. Alwayscompute#forwardingRule for forwarding rule resources. |
| `all_ports` | bool | The ports, portRange, and allPorts
fields are mutually exclusive. Only packets addressed to ports in the
specified range will be forwarded to the backends configured with this
forwarding rule.

The allPorts field has the following limitations:
   
   - It requires that the forwarding rule IPProtocol be TCP,
   UDP, SCTP, or L3_DEFAULT.
   - It's applicable only to the following products: internal passthrough
   Network Load Balancers, backend service-based external passthrough Network
   Load Balancers, and internal and external protocol forwarding.
   - Set this field to true to allow packets addressed to any port or
   packets lacking destination port information (for example, UDP fragments
   after the first fragment) to be forwarded to the backends configured with
   this forwarding rule. The L3_DEFAULT protocol requiresallPorts be set to true. |
| `ip_version` | String | The IP Version that will be used by this forwarding rule.  Valid options
are IPV4 or IPV6. |
| `port_range` | String | The ports, portRange, and allPorts
fields are mutually exclusive. Only packets addressed to ports in the
specified range will be forwarded to the backends configured with this
forwarding rule.

The portRange field has the following limitations:
   
   - It requires that the forwarding rule IPProtocol be TCP,
   UDP, or SCTP, and
   - It's applicable only to the following products: external passthrough
   Network Load Balancers, internal and external proxy Network Load Balancers,
   internal and external Application Load Balancers, external protocol
   forwarding, and Classic VPN.
   - Some products have restrictions on what ports can be used. See 
   port specifications for details.



For external forwarding rules, two or more forwarding rules cannot use the
same [IPAddress, IPProtocol] pair, and cannot have overlappingportRanges.

For internal forwarding rules within the same VPC network, two or more
forwarding rules cannot use the same [IPAddress, IPProtocol]
pair, and cannot have overlapping portRanges.

@pattern: \\d+(?:-\\d+)? |
| `allow_psc_global_access` | bool | This is used in PSC consumer ForwardingRule to control whether the PSC
endpoint can be accessed from another region. |
| `service_label` | String | An optional prefix to the service name for this forwarding rule.
If specified, the prefix is the first label of the fully qualified service
name.

The label must be 1-63 characters long, and comply withRFC1035.
Specifically, the label must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash.

This field is only used for internal load balancing. |
| `allow_global_access` | bool | If set to true, clients can access the internal passthrough Network Load
Balancers, the regional internal Application Load Balancer, and the
regional internal proxy Network Load Balancer from all regions.
If false, only allows access from the local region the load balancer is
located at. Note that for INTERNAL_MANAGED forwarding rules, this field
cannot be changed after the forwarding rule is created. |
| `backend_service` | String | Identifies the backend service to which the forwarding rule sends traffic.
Required for internal and external passthrough Network Load Balancers;
must be omitted for all other load balancer types. |
| `base_forwarding_rule` | String | [Output Only] The URL for the corresponding base forwarding rule. By base
forwarding rule, we mean the forwarding rule that has the same IP address,
protocol, and port settings with the current forwarding rule, but without
sourceIPRanges specified.
Always empty if the current forwarding rule does not have sourceIPRanges
specified. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `psc_connection_id` | String | [Output Only] The PSC connection id of the PSC forwarding rule. |
| `load_balancing_scheme` | String | Specifies the forwarding rule type.

For more information about forwarding rules, refer to
Forwarding rule concepts. |
| `labels` | HashMap<String, String> | Labels for this resource. These can only be added or modified by thesetLabels method. Each label key/value pair must comply withRFC1035.
Label values may be empty. |
| `network` | String | This field is not used for global external load balancing.

For internal passthrough Network Load Balancers, this field identifies the
network that the load balanced IP should belong to for this forwarding
rule.
If the subnetwork is specified, the network of the subnetwork will be used.
If neither subnetwork nor this field is specified, the default network will
be used.

For Private Service Connect forwarding rules that forward traffic to Google
APIs, a network must be provided. |
| `self_link_with_id` | String | [Output Only] Server-defined URL for this resource with the resource id. |
| `ip_protocol` | String | The IP protocol to which this rule applies.

For protocol forwarding, valid
options are TCP, UDP, ESP,AH, SCTP, ICMP andL3_DEFAULT.

The valid IP protocols are different for different load balancing products
as described in [Load balancing
features](https://cloud.google.com/load-balancing/docs/features#protocols_from_the_load_balancer_to_the_backends). |
| `no_automate_dns_zone` | bool | This is used in PSC consumer ForwardingRule to control whether it should
try to auto-generate a DNS zone or not. Non-PSC forwarding rules do not use
this field. Once set, this field is not mutable. |
| `service_name` | String | [Output Only]
The internal fully qualified service name for this forwarding rule.

This field is only used for internal load balancing. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `target` | String | The URL of the target resource to receive the matched traffic.  For
regional forwarding rules, this target must be in the same region as the
forwarding rule. For global forwarding rules, this target must be a global
load balancing resource.

The forwarded traffic must be of a type appropriate to the target object.
   
   
     -  For load balancers, see the "Target" column in [Port specifications](https://cloud.google.com/load-balancing/docs/forwarding-rule-concepts#ip_address_specifications).
     -  For Private Service Connect forwarding rules that forward traffic to Google APIs, provide the name of a supported Google API bundle:
       
      
            -  vpc-sc -  APIs that support VPC Service Controls. 
            -  all-apis - All supported Google APIs. 
          
     
     -  For Private Service Connect forwarding rules that forward traffic to managed services, the target must be a service attachment. The target is not mutable once set as a service attachment. |
| `ip_collection` | String | Resource reference of a PublicDelegatedPrefix. The PDP must
be a sub-PDP in EXTERNAL_IPV6_FORWARDING_RULE_CREATION mode.

Use one of the following formats to specify a sub-PDP when creating an IPv6
NetLB forwarding rule using BYOIP:
Full resource URL, as inhttps://www.googleapis.com/compute/v1/projects/project_id/regions/region/publicDelegatedPrefixes/sub-pdp-name
Partial URL, as in:
   
   - projects/project_id/regions/region/publicDelegatedPrefixes/sub-pdp-name
   - regions/region/publicDelegatedPrefixes/sub-pdp-name |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create global_forwarding_rule
global_forwarding_rule = provider.compute_api.Global_forwarding_rule {
    project = "value"  # Project ID for this request.
}

# Access global_forwarding_rule outputs
global_forwarding_rule_id = global_forwarding_rule.id
global_forwarding_rule_fingerprint = global_forwarding_rule.fingerprint
global_forwarding_rule_id = global_forwarding_rule.id
global_forwarding_rule_region = global_forwarding_rule.region
global_forwarding_rule_self_link = global_forwarding_rule.self_link
global_forwarding_rule_allow_psc_packet_injection = global_forwarding_rule.allow_psc_packet_injection
global_forwarding_rule_name = global_forwarding_rule.name
global_forwarding_rule_network_tier = global_forwarding_rule.network_tier
global_forwarding_rule_psc_connection_status = global_forwarding_rule.psc_connection_status
global_forwarding_rule_subnetwork = global_forwarding_rule.subnetwork
global_forwarding_rule_availability_group = global_forwarding_rule.availability_group
global_forwarding_rule_is_mirroring_collector = global_forwarding_rule.is_mirroring_collector
global_forwarding_rule_ip_address = global_forwarding_rule.ip_address
global_forwarding_rule_external_managed_backend_bucket_migration_testing_percentage = global_forwarding_rule.external_managed_backend_bucket_migration_testing_percentage
global_forwarding_rule_service_directory_registrations = global_forwarding_rule.service_directory_registrations
global_forwarding_rule_ports = global_forwarding_rule.ports
global_forwarding_rule_label_fingerprint = global_forwarding_rule.label_fingerprint
global_forwarding_rule_child_forwarding_rules = global_forwarding_rule.child_forwarding_rules
global_forwarding_rule_external_managed_backend_bucket_migration_state = global_forwarding_rule.external_managed_backend_bucket_migration_state
global_forwarding_rule_parent_forwarding_rule = global_forwarding_rule.parent_forwarding_rule
global_forwarding_rule_metadata_filters = global_forwarding_rule.metadata_filters
global_forwarding_rule_source_ip_ranges = global_forwarding_rule.source_ip_ranges
global_forwarding_rule_kind = global_forwarding_rule.kind
global_forwarding_rule_all_ports = global_forwarding_rule.all_ports
global_forwarding_rule_ip_version = global_forwarding_rule.ip_version
global_forwarding_rule_port_range = global_forwarding_rule.port_range
global_forwarding_rule_allow_psc_global_access = global_forwarding_rule.allow_psc_global_access
global_forwarding_rule_service_label = global_forwarding_rule.service_label
global_forwarding_rule_allow_global_access = global_forwarding_rule.allow_global_access
global_forwarding_rule_backend_service = global_forwarding_rule.backend_service
global_forwarding_rule_base_forwarding_rule = global_forwarding_rule.base_forwarding_rule
global_forwarding_rule_description = global_forwarding_rule.description
global_forwarding_rule_psc_connection_id = global_forwarding_rule.psc_connection_id
global_forwarding_rule_load_balancing_scheme = global_forwarding_rule.load_balancing_scheme
global_forwarding_rule_labels = global_forwarding_rule.labels
global_forwarding_rule_network = global_forwarding_rule.network
global_forwarding_rule_self_link_with_id = global_forwarding_rule.self_link_with_id
global_forwarding_rule_ip_protocol = global_forwarding_rule.ip_protocol
global_forwarding_rule_no_automate_dns_zone = global_forwarding_rule.no_automate_dns_zone
global_forwarding_rule_service_name = global_forwarding_rule.service_name
global_forwarding_rule_creation_timestamp = global_forwarding_rule.creation_timestamp
global_forwarding_rule_target = global_forwarding_rule.target
global_forwarding_rule_ip_collection = global_forwarding_rule.ip_collection
```

---


### Interconnect_group

Creates a InterconnectGroup in the specified project in the given scope
using the parameters that are included in the request.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `physical_structure` | String |  |  |
| `id` | String |  | [Output Only] The unique identifier for the resource type. The server
generates this identifier. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `configured` | String |  |  |
| `interconnects` | HashMap<String, String> |  | Interconnects in the InterconnectGroup. Keys are arbitrary user-specified
strings. Users are encouraged, but not required, to use their preferred
format for resource links as keys.
Note that there are add-members and remove-members methods in gcloud.
The size of this map is limited by an "Interconnects per group" quota. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `name` | String |  | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `intent` | String |  |  |
| `kind` | String |  | [Output Only] Type of the resource. Always compute#InterconnectGroup |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `etag` | String |  | Opaque system-generated token that uniquely identifies the configuration.
If provided when patching a configuration in update mode, the provided
token must match the current token or the update is rejected. This provides
a reliable means of doing read-modify-write (optimistic locking) as
described by AIP 154. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `physical_structure` | String |  |
| `id` | String | [Output Only] The unique identifier for the resource type. The server
generates this identifier. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `configured` | String |  |
| `interconnects` | HashMap<String, String> | Interconnects in the InterconnectGroup. Keys are arbitrary user-specified
strings. Users are encouraged, but not required, to use their preferred
format for resource links as keys.
Note that there are add-members and remove-members methods in gcloud.
The size of this map is limited by an "Interconnects per group" quota. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `name` | String | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `intent` | String |  |
| `kind` | String | [Output Only] Type of the resource. Always compute#InterconnectGroup |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `etag` | String | Opaque system-generated token that uniquely identifies the configuration.
If provided when patching a configuration in update mode, the provided
token must match the current token or the update is rejected. This provides
a reliable means of doing read-modify-write (optimistic locking) as
described by AIP 154. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create interconnect_group
interconnect_group = provider.compute_api.Interconnect_group {
    project = "value"  # Project ID for this request.
}

# Access interconnect_group outputs
interconnect_group_id = interconnect_group.id
interconnect_group_physical_structure = interconnect_group.physical_structure
interconnect_group_id = interconnect_group.id
interconnect_group_creation_timestamp = interconnect_group.creation_timestamp
interconnect_group_configured = interconnect_group.configured
interconnect_group_interconnects = interconnect_group.interconnects
interconnect_group_description = interconnect_group.description
interconnect_group_name = interconnect_group.name
interconnect_group_intent = interconnect_group.intent
interconnect_group_kind = interconnect_group.kind
interconnect_group_self_link = interconnect_group.self_link
interconnect_group_etag = interconnect_group.etag
```

---


### Region_backend_bucket

Creates a RegionBackendBucket in the specified project in the given scope
using the parameters that are included in the request.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `cdn_policy` | String |  | Cloud CDN configuration for this BackendBucket. |
| `enable_cdn` | bool |  | If true, enable Cloud CDN for this BackendBucket. |
| `name` | String |  | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `region` | String |  | [Output Only] URL of the region where the regional backend bucket
resides. This field is not applicable to global backend buckets.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `params` | String |  | Input only. [Input Only] Additional params passed with the request, but not persisted
as part of resource payload. |
| `edge_security_policy` | String |  | [Output Only] The resource URL for the edge security policy associated with
this backend bucket. |
| `description` | String |  | An optional textual description of the resource; provided by the client
when the resource is created. |
| `kind` | String |  | Type of the resource. |
| `load_balancing_scheme` | String |  | The value can only be INTERNAL_MANAGED for cross-region internal layer 7
load balancer.

If loadBalancingScheme is not specified, the backend bucket can be used by
classic global external load balancers, or global application external load
balancers, or both. |
| `self_link_with_id` | String |  | [Output Only] Server-defined URL for this resource with the resource id. |
| `bucket_name` | String |  | Cloud Storage bucket name. |
| `used_by` | Vec<String> |  | [Output Only] List of resources referencing that backend bucket. |
| `id` | String |  | [Output Only] Unique identifier for the resource; defined by the server. |
| `compression_mode` | String |  | Compress text responses using Brotli or gzip compression, based on
the client's Accept-Encoding header. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `custom_response_headers` | Vec<String> |  | Headers that the Application Load Balancer should add to proxied responses. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `region` | String | ✅ | Name of the region of this request. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `cdn_policy` | String | Cloud CDN configuration for this BackendBucket. |
| `enable_cdn` | bool | If true, enable Cloud CDN for this BackendBucket. |
| `name` | String | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `region` | String | [Output Only] URL of the region where the regional backend bucket
resides. This field is not applicable to global backend buckets.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `params` | String | Input only. [Input Only] Additional params passed with the request, but not persisted
as part of resource payload. |
| `edge_security_policy` | String | [Output Only] The resource URL for the edge security policy associated with
this backend bucket. |
| `description` | String | An optional textual description of the resource; provided by the client
when the resource is created. |
| `kind` | String | Type of the resource. |
| `load_balancing_scheme` | String | The value can only be INTERNAL_MANAGED for cross-region internal layer 7
load balancer.

If loadBalancingScheme is not specified, the backend bucket can be used by
classic global external load balancers, or global application external load
balancers, or both. |
| `self_link_with_id` | String | [Output Only] Server-defined URL for this resource with the resource id. |
| `bucket_name` | String | Cloud Storage bucket name. |
| `used_by` | Vec<String> | [Output Only] List of resources referencing that backend bucket. |
| `id` | String | [Output Only] Unique identifier for the resource; defined by the server. |
| `compression_mode` | String | Compress text responses using Brotli or gzip compression, based on
the client's Accept-Encoding header. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `custom_response_headers` | Vec<String> | Headers that the Application Load Balancer should add to proxied responses. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create region_backend_bucket
region_backend_bucket = provider.compute_api.Region_backend_bucket {
    region = "value"  # Name of the region of this request.
    project = "value"  # Project ID for this request.
}

# Access region_backend_bucket outputs
region_backend_bucket_id = region_backend_bucket.id
region_backend_bucket_cdn_policy = region_backend_bucket.cdn_policy
region_backend_bucket_enable_cdn = region_backend_bucket.enable_cdn
region_backend_bucket_name = region_backend_bucket.name
region_backend_bucket_region = region_backend_bucket.region
region_backend_bucket_params = region_backend_bucket.params
region_backend_bucket_edge_security_policy = region_backend_bucket.edge_security_policy
region_backend_bucket_description = region_backend_bucket.description
region_backend_bucket_kind = region_backend_bucket.kind
region_backend_bucket_load_balancing_scheme = region_backend_bucket.load_balancing_scheme
region_backend_bucket_self_link_with_id = region_backend_bucket.self_link_with_id
region_backend_bucket_bucket_name = region_backend_bucket.bucket_name
region_backend_bucket_used_by = region_backend_bucket.used_by
region_backend_bucket_id = region_backend_bucket.id
region_backend_bucket_compression_mode = region_backend_bucket.compression_mode
region_backend_bucket_creation_timestamp = region_backend_bucket.creation_timestamp
region_backend_bucket_custom_response_headers = region_backend_bucket.custom_response_headers
region_backend_bucket_self_link = region_backend_bucket.self_link
```

---


### Region_health_aggregation_policie

Create a HealthAggregationPolicy in the specified project in the given
region using the parameters that are included in the request.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `name` | String |  | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `healthy_percent_threshold` | i64 |  | Can only be set if the policyType field isBACKEND_SERVICE_POLICY. Specifies the threshold (as a
percentage) of healthy endpoints required in order to consider the
aggregated health result HEALTHY. Defaults to 60. Must be in
range [0, 100]. Not applicable if the policyType field isDNB_PUBLIC_IP_POLICY. Can be mutated. This field is optional,
and will be set to the default if unspecified. Note that both this
threshold and minHealthyThreshold must be satisfied in order
for HEALTHY to be the aggregated result. "Endpoints" refers to network
endpoints within a Network Endpoint Group or instances within an Instance
Group. |
| `kind` | String |  | [Output Only] Type of the resource. Alwayscompute#healthAggregationPolicy for health aggregation
policies. |
| `policy_type` | String |  | Specifies the type of the healthAggregationPolicy. The only allowed value
for global resources is DNS_PUBLIC_IP_POLICY. The only allowed
value for regional resources is BACKEND_SERVICE_POLICY. Must
be specified when the healthAggregationPolicy is created, and cannot be
mutated. |
| `region` | String |  | [Output Only] URL of the region where the health aggregation policy
resides. This field applies only to the regional resource. You must specify
this field as part of the HTTP request URL. It is not settable as a field
in the request body. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `min_healthy_threshold` | i64 |  | Can only be set if the policyType field isBACKEND_SERVICE_POLICY. Specifies the minimum number of
healthy endpoints required in order to consider the aggregated health
result HEALTHY. Defaults to 1. Must be positive. Not
applicable if the policyType field isDNB_PUBLIC_IP_POLICY. Can be mutated. This field is optional,
and will be set to the default if unspecified. Note that both this
threshold and healthyPercentThreshold must be satisfied in
order for HEALTHY to be the aggregated result. "Endpoints" refers to
network endpoints within a Network Endpoint Group or instances within an
Instance Group. |
| `self_link_with_id` | String |  | [Output Only] Server-defined URL with id for the resource. |
| `fingerprint` | String |  | Fingerprint of this resource. A hash of the contents stored in this object.
This field is used in optimistic locking. This field will be ignored when
inserting a HealthAggregationPolicy. An up-to-date fingerprint
must be provided in order to patch the HealthAggregationPolicy; Otherwise,
the request will fail with error 412 conditionNotMet. To see
the latest fingerprint, make a get() request to retrieve the
HealthAggregationPolicy. |
| `project` | String | ✅ | Project ID for this request. |
| `region` | String | ✅ | Name of the region scoping this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `name` | String | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `healthy_percent_threshold` | i64 | Can only be set if the policyType field isBACKEND_SERVICE_POLICY. Specifies the threshold (as a
percentage) of healthy endpoints required in order to consider the
aggregated health result HEALTHY. Defaults to 60. Must be in
range [0, 100]. Not applicable if the policyType field isDNB_PUBLIC_IP_POLICY. Can be mutated. This field is optional,
and will be set to the default if unspecified. Note that both this
threshold and minHealthyThreshold must be satisfied in order
for HEALTHY to be the aggregated result. "Endpoints" refers to network
endpoints within a Network Endpoint Group or instances within an Instance
Group. |
| `kind` | String | [Output Only] Type of the resource. Alwayscompute#healthAggregationPolicy for health aggregation
policies. |
| `policy_type` | String | Specifies the type of the healthAggregationPolicy. The only allowed value
for global resources is DNS_PUBLIC_IP_POLICY. The only allowed
value for regional resources is BACKEND_SERVICE_POLICY. Must
be specified when the healthAggregationPolicy is created, and cannot be
mutated. |
| `region` | String | [Output Only] URL of the region where the health aggregation policy
resides. This field applies only to the regional resource. You must specify
this field as part of the HTTP request URL. It is not settable as a field
in the request body. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `min_healthy_threshold` | i64 | Can only be set if the policyType field isBACKEND_SERVICE_POLICY. Specifies the minimum number of
healthy endpoints required in order to consider the aggregated health
result HEALTHY. Defaults to 1. Must be positive. Not
applicable if the policyType field isDNB_PUBLIC_IP_POLICY. Can be mutated. This field is optional,
and will be set to the default if unspecified. Note that both this
threshold and healthyPercentThreshold must be satisfied in
order for HEALTHY to be the aggregated result. "Endpoints" refers to
network endpoints within a Network Endpoint Group or instances within an
Instance Group. |
| `self_link_with_id` | String | [Output Only] Server-defined URL with id for the resource. |
| `fingerprint` | String | Fingerprint of this resource. A hash of the contents stored in this object.
This field is used in optimistic locking. This field will be ignored when
inserting a HealthAggregationPolicy. An up-to-date fingerprint
must be provided in order to patch the HealthAggregationPolicy; Otherwise,
the request will fail with error 412 conditionNotMet. To see
the latest fingerprint, make a get() request to retrieve the
HealthAggregationPolicy. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create region_health_aggregation_policie
region_health_aggregation_policie = provider.compute_api.Region_health_aggregation_policie {
    project = "value"  # Project ID for this request.
    region = "value"  # Name of the region scoping this request.
}

# Access region_health_aggregation_policie outputs
region_health_aggregation_policie_id = region_health_aggregation_policie.id
region_health_aggregation_policie_self_link = region_health_aggregation_policie.self_link
region_health_aggregation_policie_description = region_health_aggregation_policie.description
region_health_aggregation_policie_id = region_health_aggregation_policie.id
region_health_aggregation_policie_name = region_health_aggregation_policie.name
region_health_aggregation_policie_healthy_percent_threshold = region_health_aggregation_policie.healthy_percent_threshold
region_health_aggregation_policie_kind = region_health_aggregation_policie.kind
region_health_aggregation_policie_policy_type = region_health_aggregation_policie.policy_type
region_health_aggregation_policie_region = region_health_aggregation_policie.region
region_health_aggregation_policie_creation_timestamp = region_health_aggregation_policie.creation_timestamp
region_health_aggregation_policie_min_healthy_threshold = region_health_aggregation_policie.min_healthy_threshold
region_health_aggregation_policie_self_link_with_id = region_health_aggregation_policie.self_link_with_id
region_health_aggregation_policie_fingerprint = region_health_aggregation_policie.fingerprint
```

---


### External_vpn_gateway

Creates a ExternalVpnGateway in the specified project using
the data included in the request.

**Operations**: ✅ Create ✅ Read ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `name` | String |  | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must be
a dash, lowercase letter, or digit, except the last character, which cannot
be a dash. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `params` | String |  | Input only. [Input Only] Additional params passed with the request, but not persisted
as part of resource payload. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `kind` | String |  | [Output Only] Type of the resource. Alwayscompute#externalVpnGateway for externalVpnGateways. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `labels` | HashMap<String, String> |  | Labels for this resource. These can only be added or modified by thesetLabels method. Each label key/value pair must comply withRFC1035.
Label values may be empty. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `interfaces` | Vec<String> |  | A list of interfaces for this external VPN gateway.

If your peer-side gateway is an on-premises gateway and non-AWS cloud
providers' gateway, at most two interfaces can be provided for an external
VPN gateway. If your peer side is an AWS virtual private gateway, four
interfaces should be provided for an external VPN gateway. |
| `label_fingerprint` | String |  | A fingerprint for the labels being applied to this ExternalVpnGateway,
which is essentially a hash of the labels set used for optimistic locking.
The fingerprint is initially generated by Compute Engine and changes after
every request to modify or update labels. You must always provide an
up-to-date fingerprint hash in order to update or change labels,
otherwise the request will fail with error412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve an ExternalVpnGateway. |
| `redundancy_type` | String |  | Indicates the user-supplied redundancy type of this external VPN gateway. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `name` | String | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must be
a dash, lowercase letter, or digit, except the last character, which cannot
be a dash. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `params` | String | Input only. [Input Only] Additional params passed with the request, but not persisted
as part of resource payload. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `kind` | String | [Output Only] Type of the resource. Alwayscompute#externalVpnGateway for externalVpnGateways. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `labels` | HashMap<String, String> | Labels for this resource. These can only be added or modified by thesetLabels method. Each label key/value pair must comply withRFC1035.
Label values may be empty. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `interfaces` | Vec<String> | A list of interfaces for this external VPN gateway.

If your peer-side gateway is an on-premises gateway and non-AWS cloud
providers' gateway, at most two interfaces can be provided for an external
VPN gateway. If your peer side is an AWS virtual private gateway, four
interfaces should be provided for an external VPN gateway. |
| `label_fingerprint` | String | A fingerprint for the labels being applied to this ExternalVpnGateway,
which is essentially a hash of the labels set used for optimistic locking.
The fingerprint is initially generated by Compute Engine and changes after
every request to modify or update labels. You must always provide an
up-to-date fingerprint hash in order to update or change labels,
otherwise the request will fail with error412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve an ExternalVpnGateway. |
| `redundancy_type` | String | Indicates the user-supplied redundancy type of this external VPN gateway. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create external_vpn_gateway
external_vpn_gateway = provider.compute_api.External_vpn_gateway {
    project = "value"  # Project ID for this request.
}

# Access external_vpn_gateway outputs
external_vpn_gateway_id = external_vpn_gateway.id
external_vpn_gateway_name = external_vpn_gateway.name
external_vpn_gateway_self_link = external_vpn_gateway.self_link
external_vpn_gateway_params = external_vpn_gateway.params
external_vpn_gateway_description = external_vpn_gateway.description
external_vpn_gateway_kind = external_vpn_gateway.kind
external_vpn_gateway_creation_timestamp = external_vpn_gateway.creation_timestamp
external_vpn_gateway_labels = external_vpn_gateway.labels
external_vpn_gateway_id = external_vpn_gateway.id
external_vpn_gateway_interfaces = external_vpn_gateway.interfaces
external_vpn_gateway_label_fingerprint = external_vpn_gateway.label_fingerprint
external_vpn_gateway_redundancy_type = external_vpn_gateway.redundancy_type
```

---


### Region_backend_service

Creates a regional BackendService resource in the specified project using
the data included in the request. For more information, see
Backend services overview.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `backends` | Vec<String> |  | The list of backends that serve this BackendService. |
| `log_config` | String |  | This field denotes the logging options for the load balancer traffic served
by this backend service. If logging is enabled, logs will be exported to
Stackdriver. |
| `compression_mode` | String |  | Compress text responses using Brotli or gzip compression, based on
the client's Accept-Encoding header. |
| `name` | String |  | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `params` | String |  | Input only. [Input Only] Additional params passed with the request, but not persisted
as part of resource payload. |
| `security_policy` | String |  | [Output Only] The resource URL for the security policy associated with this
backend service. |
| `kind` | String |  | [Output Only] Type of resource. Always compute#backendService
for backend services. |
| `outlier_detection` | String |  | Settings controlling the ejection of unhealthy backend endpoints from the
load balancing pool of each individual proxy instance that processes the
traffic for the given backend service. If not set, this feature is
considered disabled.

Results of the outlier detection algorithm (ejection of endpoints from the
load balancing pool and returning them back to the pool) are executed
independently by each proxy instance of the load balancer. In most cases,
more than one proxy instance handles the traffic received by a backend
service. Thus, it is possible that an unhealthy endpoint is detected and
ejected by only some of the proxies, and while this happens, other proxies
may continue to send requests to the same unhealthy endpoint until they
detect and eject the unhealthy endpoint.

Applicable backend endpoints can be:
   
   - VM instances in an Instance Group
   - Endpoints in a Zonal NEG (GCE_VM_IP, GCE_VM_IP_PORT)
   - Endpoints in a Hybrid Connectivity NEG (NON_GCP_PRIVATE_IP_PORT)
   - Serverless NEGs, that resolve to Cloud Run, App Engine, or Cloud
   Functions Services 
   - Private Service Connect NEGs, that resolve to
   Google-managed regional API endpoints or managed services published using
   Private Service Connect



Applicable backend service types can be:
   
   - A global backend service with the loadBalancingScheme set to
   INTERNAL_SELF_MANAGED or EXTERNAL_MANAGED. 
   - A regional backend
   service with the serviceProtocol set to HTTP, HTTPS, HTTP2 or H2C, and
   loadBalancingScheme set to INTERNAL_MANAGED or EXTERNAL_MANAGED. Not
   supported for Serverless NEGs.



Not supported when the backend service is referenced by a URL map that is
bound to target gRPC proxy that has validateForProxyless field set to true. |
| `service_bindings` | Vec<String> |  | URLs of networkservices.ServiceBinding resources.

Can only be set if load balancing scheme is INTERNAL_SELF_MANAGED.
If set, lists of backends and health checks must be both empty. |
| `custom_request_headers` | Vec<String> |  | Headers that the load balancer adds to proxied requests. See [Creating
custom
headers](https://cloud.google.com/load-balancing/docs/custom-headers). |
| `health_checks` | Vec<String> |  | The list of URLs to the healthChecks, httpHealthChecks (legacy), or
httpsHealthChecks (legacy) resource for health checking this backend
service. Not all backend services support legacy health checks. See
Load balancer guide. Currently, at most one health check can be
specified for each backend service. Backend services with
instance group or zonal NEG backends must have a health check unless
haPolicy is specified. Backend services with internet or serverless NEG
backends must not have a health check.

healthChecks[] cannot be specified with haPolicy. |
| `load_balancing_scheme` | String |  | Specifies the load balancer type. A backend service
created for one type of load balancer cannot be used with another.
For more information, refer toChoosing
a load balancer. |
| `max_stream_duration` | String |  | Specifies the default maximum duration (timeout) for streams to this
service. Duration is computed from the beginning of the stream until the
response has been completely processed, including all retries. A stream
that does not complete in this duration is closed.

If not specified, there will be no timeout limit, i.e. the maximum
duration is infinite.

This value can be overridden in the PathMatcher configuration of the
UrlMap that references this backend service.

This field is only allowed when the loadBalancingScheme of
the backend service is INTERNAL_SELF_MANAGED. |
| `network` | String |  | The URL of the network to which this backend service belongs.

This field must be set for Internal Passthrough Network Load Balancers when
the haPolicy is enabled, and for External Passthrough Network Load
Balancers when the haPolicy fastIpMove is enabled.

This field can only be specified when the load balancing scheme is set toINTERNAL, or when the load balancing scheme is set toEXTERNAL and haPolicy fastIpMove is enabled. |
| `custom_response_headers` | Vec<String> |  | Headers that the load balancer adds to proxied responses. See [Creating
custom
headers](https://cloud.google.com/load-balancing/docs/custom-headers). |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `subsetting` | String |  | subsetting cannot be specified with haPolicy. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `timeout_sec` | i64 |  | The backend service timeout has a different meaning depending on the
type of load balancer. For more information see,
Backend service settings.
The default is 30 seconds.
The full range of timeout values allowed goes from 1
through 2,147,483,647 seconds.

This value can be overridden in the PathMatcher configuration of the
UrlMap that references this backend service.

Not supported when the backend service is referenced by a URL map that is
bound to target gRPC proxy that has validateForProxyless field set to true.
Instead, use maxStreamDuration. |
| `connection_tracking_policy` | String |  | Connection Tracking configuration for this BackendService. Connection
tracking policy settings are only available for external passthrough
Network Load Balancers and internal passthrough Network Load Balancers.

connectionTrackingPolicy cannot be specified with haPolicy. |
| `session_affinity` | String |  | Type of session affinity to use. The default is NONE.

Only NONE and HEADER_FIELD are supported
when the backend service is referenced by a URL map that is bound to
target gRPC proxy that has validateForProxyless field set to true.

For more details, see:
[Session
Affinity](https://cloud.google.com/load-balancing/docs/backend-service#session_affinity).

sessionAffinity cannot be specified with haPolicy. |
| `port` | i64 |  | Deprecated in favor of portName. The TCP port to connect on
the backend. The default value is 80.
For internal passthrough Network Load Balancers and external passthrough
Network Load Balancers, omit port. |
| `iap` | String |  | The configurations for Identity-Aware Proxy on this resource.
Not available for internal passthrough Network Load Balancers and external
passthrough Network Load Balancers. |
| `self_link_with_id` | String |  | [Output Only] Server-defined URL for this resource with the resource id. |
| `ip_address_selection_policy` | String |  | Specifies a preference for traffic sent from the proxy to the backend (or
from the client to the backend for proxyless gRPC).
The possible values are:
   
   - IPV4_ONLY: Only send IPv4 traffic to the backends of the
   backend service (Instance Group, Managed Instance Group, Network Endpoint
   Group), regardless of traffic from the client to the proxy. Only IPv4
   health checks are used to check the health of the backends. This is the
   default setting.
   - PREFER_IPV6: Prioritize the connection to the endpoint's
   IPv6 address over its IPv4 address (provided there is a healthy IPv6
   address).
   - IPV6_ONLY: Only send IPv6 traffic to the backends of the
   backend service (Instance Group, Managed Instance Group, Network Endpoint
   Group), regardless of traffic from the client to the proxy. Only IPv6
   health checks are used to check the health of the backends.



This field is applicable to either:
   
   -  Advanced global external Application Load Balancer (load balancing
   scheme EXTERNAL_MANAGED), 
   -  Regional external Application Load
   Balancer, 
   -  Internal proxy Network Load Balancer (load balancing
   scheme INTERNAL_MANAGED), 
   -  Regional internal Application Load
   Balancer (load balancing scheme INTERNAL_MANAGED), 
   -  Traffic
   Director with Envoy proxies and proxyless gRPC (load balancing scheme
   INTERNAL_SELF_MANAGED). |
| `circuit_breakers` | String |  |  |
| `metadatas` | HashMap<String, String> |  | Deployment metadata associated with the resource to be set by a GKE hub
controller and read by the backend RCTH |
| `external_managed_migration_state` | String |  | Specifies the canary migration state. Possible values are PREPARE,
TEST_BY_PERCENTAGE, and TEST_ALL_TRAFFIC.

To begin the migration from EXTERNAL to EXTERNAL_MANAGED, the state must be
changed to PREPARE. The state must be changed to TEST_ALL_TRAFFIC before
the loadBalancingScheme can be changed to EXTERNAL_MANAGED. Optionally, the
TEST_BY_PERCENTAGE state can be used to migrate traffic by percentage using
externalManagedMigrationTestingPercentage.

Rolling back a migration requires the states to be set in reverse order. So
changing the scheme from EXTERNAL_MANAGED to EXTERNAL requires the state to
be set to TEST_ALL_TRAFFIC at the same time. Optionally, the
TEST_BY_PERCENTAGE state can be used to migrate some traffic back to
EXTERNAL or PREPARE can be used to migrate all traffic back to EXTERNAL. |
| `tls_settings` | String |  | Configuration for Backend Authenticated TLS and mTLS. May only be specified
when the backend protocol is SSL, HTTPS or HTTP2. |
| `dynamic_forwarding` | String |  | Dynamic forwarding configuration. This field is used to configure the
backend service with dynamic forwarding feature which together with Service
Extension allows customized and complex routing logic. |
| `used_by` | Vec<String> |  | [Output Only] List of resources referencing given backend service. |
| `connection_draining` | String |  | connectionDraining cannot be specified with haPolicy. |
| `external_managed_migration_testing_percentage` | f64 |  | Determines the fraction of requests that should be processed by the Global
external Application Load Balancer.

The value of this field must be in the range [0, 100].

Session affinity options will slightly affect this routing behavior, for
more details, see:Session
Affinity.

This value can only be set if the loadBalancingScheme in the BackendService
is set to EXTERNAL (when using the classic Application Load Balancer) and
the migration state is TEST_BY_PERCENTAGE. |
| `region` | String |  | [Output Only] URL of the region where the regional backend service
resides. This field is not applicable to global backend services.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `strong_session_affinity_cookie` | String |  | Describes the HTTP cookie used for stateful session affinity. This field is
applicable and required if the sessionAffinity is set toSTRONG_COOKIE_AFFINITY. |
| `locality_lb_policy` | String |  | The load balancing algorithm used within the scope of the locality. The
possible values are:
   
   - ROUND_ROBIN: This is a simple policy in which each healthy
   backend is selected in round robin order. This is the default.
   - LEAST_REQUEST: An O(1) algorithm which
   selects two random healthy hosts and picks the host which has fewer active
   requests.
   - RING_HASH: The ring/modulo hash load balancer implements
   consistent hashing to backends. The algorithm has the property that the
   addition/removal of a host from a set of N hosts only affects 1/N of the
   requests.
   - RANDOM: The load balancer selects a random healthy
   host.
   - ORIGINAL_DESTINATION: Backend host is selected
   based on the client connection metadata, i.e., connections are opened to
   the same address as the destination address of the incoming connection
   before the connection was redirected to the load balancer.
   - MAGLEV: used as a drop in replacement for the ring hash
   load balancer. Maglev is not as stable as ring hash but has faster table
   lookup build times and host selection times. For more information about
   Maglev, see Maglev:
   A Fast and Reliable Software Network Load Balancer.
   - WEIGHTED_ROUND_ROBIN: Per-endpoint Weighted Round Robin
   Load Balancing using weights computed from Backend reported Custom Metrics.
   If set, the Backend Service responses are expected to contain non-standard
   HTTP response header field Endpoint-Load-Metrics. The reported
   metrics to use for computing the weights are specified via thecustomMetrics field.
   
   This field is applicable to either:
      - A regional backend service with the service_protocol set to HTTP,
      HTTPS, HTTP2 or H2C, and load_balancing_scheme set to
      INTERNAL_MANAGED. 
      - A global backend service with the
      load_balancing_scheme set to INTERNAL_SELF_MANAGED, INTERNAL_MANAGED, or
      EXTERNAL_MANAGED.
   
   
   If sessionAffinity is not configured—that is, if session
   affinity remains at the default value of NONE—then the
   default value for localityLbPolicy
   is ROUND_ROBIN. If session affinity is set to a value other
   than NONE,
   then the default value for localityLbPolicy isMAGLEV.
   
   Only ROUND_ROBIN and RING_HASH are supported
   when the backend service is referenced by a URL map that is bound to
   target gRPC proxy that has validateForProxyless field set to true.
   
   localityLbPolicy cannot be specified with haPolicy. |
| `vpc_network_scope` | String |  | The network scope of the backends that can be added to the backend
service. This field can be either GLOBAL_VPC_NETWORK orREGIONAL_VPC_NETWORK.

A backend service with the VPC scope set to GLOBAL_VPC_NETWORK
is only allowed to have backends in global VPC networks.

When the VPC scope is set to REGIONAL_VPC_NETWORK the backend
service is only allowed to have backends in regional networks in the same
scope as the backend service.
Note: if not specified then GLOBAL_VPC_NETWORK will be used. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `edge_security_policy` | String |  | [Output Only] The resource URL for the edge security policy associated with
this backend service. |
| `network_pass_through_lb_traffic_policy` | String |  | Configures traffic steering properties of internal passthrough Network
Load Balancers.

networkPassThroughLbTrafficPolicy cannot be specified with haPolicy. |
| `allow_multinetwork` | bool |  | A boolean flag enabling multi-network mesh. This field is only allowed with
load balancing scheme set to INTERNAL_SELF_MANAGED. |
| `enable_cdn` | bool |  | If true, enables Cloud CDN for the backend service of a
global external Application Load Balancer. |
| `affinity_cookie_ttl_sec` | i64 |  | Lifetime of cookies in seconds. This setting is applicable to Application
Load Balancers and Traffic Director and requires
GENERATED_COOKIE or HTTP_COOKIE session affinity.

If set to 0, the cookie is non-persistent and lasts only until
the end of the browser session (or equivalent). The maximum allowed value
is two weeks (1,209,600).

Not supported when the backend service is referenced by a URL map that is
bound to target gRPC proxy that has validateForProxyless field set to true. |
| `protocol` | String |  | The protocol this BackendService uses to communicate
with backends.

Possible values are HTTP, HTTPS, HTTP2, H2C, TCP, SSL, UDP or GRPC.
depending on the chosen load balancer or Traffic Director configuration.
Refer to the documentation for the load balancers or for Traffic Director
for more information.

Must be set to GRPC when the backend service is referenced by a URL map
that is bound to target gRPC proxy. |
| `service_lb_policy` | String |  | URL to networkservices.ServiceLbPolicy resource.

Can only be set if load balancing scheme is EXTERNAL_MANAGED,
INTERNAL_MANAGED or INTERNAL_SELF_MANAGED and the scope is global. |
| `consistent_hash` | String |  | Consistent Hash-based load balancing can be used to provide soft session
affinity based on HTTP headers, cookies or other properties. This load
balancing policy is applicable only for HTTP connections. The affinity to a
particular destination host will be lost when one or more hosts are
added/removed from the destination service. This field specifies parameters
that control consistent hashing. This field is only applicable whenlocalityLbPolicy is set to MAGLEV orRING_HASH.

This field is applicable to either:
   
   - A regional backend service with the service_protocol set to HTTP,
   HTTPS, HTTP2 or H2C, and load_balancing_scheme set to
   INTERNAL_MANAGED. 
   - A global backend service with the
   load_balancing_scheme set to INTERNAL_SELF_MANAGED. |
| `locality_lb_policies` | Vec<String> |  | A list of locality load-balancing policies to be used in order of
preference. When you use localityLbPolicies, you must set at least one
value for either the localityLbPolicies[].policy or the
localityLbPolicies[].customPolicy field. localityLbPolicies overrides any
value set in the localityLbPolicy field.

For an example of how to use this field, seeDefine
a list of preferred policies.

Caution: This field and its children are intended for use in a service mesh
that includes gRPC clients only. Envoy proxies can't use backend services
that have this configuration. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `port_name` | String |  | A named port on a backend instance group representing the port for
communication to the backend VMs in that group. The
named port must be [defined on each backend instance
group](https://cloud.google.com/load-balancing/docs/backend-service#named_ports).
This parameter has no meaning if the backends are NEGs. For internal
passthrough Network Load Balancers and external passthrough Network Load
Balancers, omit port_name. |
| `ha_policy` | String |  | Configures self-managed High Availability (HA) for External and Internal
Protocol Forwarding.

The backends of this regional backend service must only specify zonal
network endpoint groups (NEGs) of type GCE_VM_IP.

When haPolicy is set for an Internal Passthrough Network Load Balancer, the
regional backend service must set the network field. All zonal NEGs must
belong to the same network. However, individual NEGs can
belong to different subnetworks of that network.

When haPolicy is specified, the set of attached network endpoints across
all backends comprise an High Availability domain from which one endpoint
is selected as the active endpoint (the leader) that receives all
traffic.

haPolicy can be added only at backend service creation time. Once set up,
it cannot be deleted.

Note that haPolicy is not for load balancing, and therefore cannot be
specified with sessionAffinity, connectionTrackingPolicy, and
failoverPolicy.

haPolicy requires customers to be responsible for tracking backend
endpoint health and electing a leader among the healthy endpoints.
Therefore, haPolicy cannot be specified with healthChecks.

haPolicy can only be specified for External Passthrough Network Load
Balancers and Internal Passthrough Network Load Balancers. |
| `cdn_policy` | String |  | Cloud CDN configuration for this BackendService. Only available for
specified load balancer types. |
| `fingerprint` | String |  | Fingerprint of this resource. A hash of the contents stored in this object.
This field is used in optimistic locking. This field will be ignored when
inserting a BackendService. An up-to-date fingerprint must be provided in
order to update the BackendService, otherwise the request will
fail with error 412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve a BackendService. |
| `security_settings` | String |  | This field specifies the security settings that apply to this backend
service. This field is applicable to a global backend service with the
load_balancing_scheme set to INTERNAL_SELF_MANAGED. |
| `custom_metrics` | Vec<String> |  | List of custom metrics that are used for theWEIGHTED_ROUND_ROBIN locality_lb_policy. |
| `failover_policy` | String |  | Requires at least one backend instance group to be defined
as a backup (failover) backend.
For load balancers that have configurable failover:
[Internal passthrough Network Load
Balancers](https://cloud.google.com/load-balancing/docs/internal/failover-overview)
and [external passthrough Network Load
Balancers](https://cloud.google.com/load-balancing/docs/network/networklb-failover-overview).

failoverPolicy cannot be specified with haPolicy. |
| `region` | String | ✅ | Name of the region scoping this request. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `backends` | Vec<String> | The list of backends that serve this BackendService. |
| `log_config` | String | This field denotes the logging options for the load balancer traffic served
by this backend service. If logging is enabled, logs will be exported to
Stackdriver. |
| `compression_mode` | String | Compress text responses using Brotli or gzip compression, based on
the client's Accept-Encoding header. |
| `name` | String | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `params` | String | Input only. [Input Only] Additional params passed with the request, but not persisted
as part of resource payload. |
| `security_policy` | String | [Output Only] The resource URL for the security policy associated with this
backend service. |
| `kind` | String | [Output Only] Type of resource. Always compute#backendService
for backend services. |
| `outlier_detection` | String | Settings controlling the ejection of unhealthy backend endpoints from the
load balancing pool of each individual proxy instance that processes the
traffic for the given backend service. If not set, this feature is
considered disabled.

Results of the outlier detection algorithm (ejection of endpoints from the
load balancing pool and returning them back to the pool) are executed
independently by each proxy instance of the load balancer. In most cases,
more than one proxy instance handles the traffic received by a backend
service. Thus, it is possible that an unhealthy endpoint is detected and
ejected by only some of the proxies, and while this happens, other proxies
may continue to send requests to the same unhealthy endpoint until they
detect and eject the unhealthy endpoint.

Applicable backend endpoints can be:
   
   - VM instances in an Instance Group
   - Endpoints in a Zonal NEG (GCE_VM_IP, GCE_VM_IP_PORT)
   - Endpoints in a Hybrid Connectivity NEG (NON_GCP_PRIVATE_IP_PORT)
   - Serverless NEGs, that resolve to Cloud Run, App Engine, or Cloud
   Functions Services 
   - Private Service Connect NEGs, that resolve to
   Google-managed regional API endpoints or managed services published using
   Private Service Connect



Applicable backend service types can be:
   
   - A global backend service with the loadBalancingScheme set to
   INTERNAL_SELF_MANAGED or EXTERNAL_MANAGED. 
   - A regional backend
   service with the serviceProtocol set to HTTP, HTTPS, HTTP2 or H2C, and
   loadBalancingScheme set to INTERNAL_MANAGED or EXTERNAL_MANAGED. Not
   supported for Serverless NEGs.



Not supported when the backend service is referenced by a URL map that is
bound to target gRPC proxy that has validateForProxyless field set to true. |
| `service_bindings` | Vec<String> | URLs of networkservices.ServiceBinding resources.

Can only be set if load balancing scheme is INTERNAL_SELF_MANAGED.
If set, lists of backends and health checks must be both empty. |
| `custom_request_headers` | Vec<String> | Headers that the load balancer adds to proxied requests. See [Creating
custom
headers](https://cloud.google.com/load-balancing/docs/custom-headers). |
| `health_checks` | Vec<String> | The list of URLs to the healthChecks, httpHealthChecks (legacy), or
httpsHealthChecks (legacy) resource for health checking this backend
service. Not all backend services support legacy health checks. See
Load balancer guide. Currently, at most one health check can be
specified for each backend service. Backend services with
instance group or zonal NEG backends must have a health check unless
haPolicy is specified. Backend services with internet or serverless NEG
backends must not have a health check.

healthChecks[] cannot be specified with haPolicy. |
| `load_balancing_scheme` | String | Specifies the load balancer type. A backend service
created for one type of load balancer cannot be used with another.
For more information, refer toChoosing
a load balancer. |
| `max_stream_duration` | String | Specifies the default maximum duration (timeout) for streams to this
service. Duration is computed from the beginning of the stream until the
response has been completely processed, including all retries. A stream
that does not complete in this duration is closed.

If not specified, there will be no timeout limit, i.e. the maximum
duration is infinite.

This value can be overridden in the PathMatcher configuration of the
UrlMap that references this backend service.

This field is only allowed when the loadBalancingScheme of
the backend service is INTERNAL_SELF_MANAGED. |
| `network` | String | The URL of the network to which this backend service belongs.

This field must be set for Internal Passthrough Network Load Balancers when
the haPolicy is enabled, and for External Passthrough Network Load
Balancers when the haPolicy fastIpMove is enabled.

This field can only be specified when the load balancing scheme is set toINTERNAL, or when the load balancing scheme is set toEXTERNAL and haPolicy fastIpMove is enabled. |
| `custom_response_headers` | Vec<String> | Headers that the load balancer adds to proxied responses. See [Creating
custom
headers](https://cloud.google.com/load-balancing/docs/custom-headers). |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `subsetting` | String | subsetting cannot be specified with haPolicy. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `timeout_sec` | i64 | The backend service timeout has a different meaning depending on the
type of load balancer. For more information see,
Backend service settings.
The default is 30 seconds.
The full range of timeout values allowed goes from 1
through 2,147,483,647 seconds.

This value can be overridden in the PathMatcher configuration of the
UrlMap that references this backend service.

Not supported when the backend service is referenced by a URL map that is
bound to target gRPC proxy that has validateForProxyless field set to true.
Instead, use maxStreamDuration. |
| `connection_tracking_policy` | String | Connection Tracking configuration for this BackendService. Connection
tracking policy settings are only available for external passthrough
Network Load Balancers and internal passthrough Network Load Balancers.

connectionTrackingPolicy cannot be specified with haPolicy. |
| `session_affinity` | String | Type of session affinity to use. The default is NONE.

Only NONE and HEADER_FIELD are supported
when the backend service is referenced by a URL map that is bound to
target gRPC proxy that has validateForProxyless field set to true.

For more details, see:
[Session
Affinity](https://cloud.google.com/load-balancing/docs/backend-service#session_affinity).

sessionAffinity cannot be specified with haPolicy. |
| `port` | i64 | Deprecated in favor of portName. The TCP port to connect on
the backend. The default value is 80.
For internal passthrough Network Load Balancers and external passthrough
Network Load Balancers, omit port. |
| `iap` | String | The configurations for Identity-Aware Proxy on this resource.
Not available for internal passthrough Network Load Balancers and external
passthrough Network Load Balancers. |
| `self_link_with_id` | String | [Output Only] Server-defined URL for this resource with the resource id. |
| `ip_address_selection_policy` | String | Specifies a preference for traffic sent from the proxy to the backend (or
from the client to the backend for proxyless gRPC).
The possible values are:
   
   - IPV4_ONLY: Only send IPv4 traffic to the backends of the
   backend service (Instance Group, Managed Instance Group, Network Endpoint
   Group), regardless of traffic from the client to the proxy. Only IPv4
   health checks are used to check the health of the backends. This is the
   default setting.
   - PREFER_IPV6: Prioritize the connection to the endpoint's
   IPv6 address over its IPv4 address (provided there is a healthy IPv6
   address).
   - IPV6_ONLY: Only send IPv6 traffic to the backends of the
   backend service (Instance Group, Managed Instance Group, Network Endpoint
   Group), regardless of traffic from the client to the proxy. Only IPv6
   health checks are used to check the health of the backends.



This field is applicable to either:
   
   -  Advanced global external Application Load Balancer (load balancing
   scheme EXTERNAL_MANAGED), 
   -  Regional external Application Load
   Balancer, 
   -  Internal proxy Network Load Balancer (load balancing
   scheme INTERNAL_MANAGED), 
   -  Regional internal Application Load
   Balancer (load balancing scheme INTERNAL_MANAGED), 
   -  Traffic
   Director with Envoy proxies and proxyless gRPC (load balancing scheme
   INTERNAL_SELF_MANAGED). |
| `circuit_breakers` | String |  |
| `metadatas` | HashMap<String, String> | Deployment metadata associated with the resource to be set by a GKE hub
controller and read by the backend RCTH |
| `external_managed_migration_state` | String | Specifies the canary migration state. Possible values are PREPARE,
TEST_BY_PERCENTAGE, and TEST_ALL_TRAFFIC.

To begin the migration from EXTERNAL to EXTERNAL_MANAGED, the state must be
changed to PREPARE. The state must be changed to TEST_ALL_TRAFFIC before
the loadBalancingScheme can be changed to EXTERNAL_MANAGED. Optionally, the
TEST_BY_PERCENTAGE state can be used to migrate traffic by percentage using
externalManagedMigrationTestingPercentage.

Rolling back a migration requires the states to be set in reverse order. So
changing the scheme from EXTERNAL_MANAGED to EXTERNAL requires the state to
be set to TEST_ALL_TRAFFIC at the same time. Optionally, the
TEST_BY_PERCENTAGE state can be used to migrate some traffic back to
EXTERNAL or PREPARE can be used to migrate all traffic back to EXTERNAL. |
| `tls_settings` | String | Configuration for Backend Authenticated TLS and mTLS. May only be specified
when the backend protocol is SSL, HTTPS or HTTP2. |
| `dynamic_forwarding` | String | Dynamic forwarding configuration. This field is used to configure the
backend service with dynamic forwarding feature which together with Service
Extension allows customized and complex routing logic. |
| `used_by` | Vec<String> | [Output Only] List of resources referencing given backend service. |
| `connection_draining` | String | connectionDraining cannot be specified with haPolicy. |
| `external_managed_migration_testing_percentage` | f64 | Determines the fraction of requests that should be processed by the Global
external Application Load Balancer.

The value of this field must be in the range [0, 100].

Session affinity options will slightly affect this routing behavior, for
more details, see:Session
Affinity.

This value can only be set if the loadBalancingScheme in the BackendService
is set to EXTERNAL (when using the classic Application Load Balancer) and
the migration state is TEST_BY_PERCENTAGE. |
| `region` | String | [Output Only] URL of the region where the regional backend service
resides. This field is not applicable to global backend services.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `strong_session_affinity_cookie` | String | Describes the HTTP cookie used for stateful session affinity. This field is
applicable and required if the sessionAffinity is set toSTRONG_COOKIE_AFFINITY. |
| `locality_lb_policy` | String | The load balancing algorithm used within the scope of the locality. The
possible values are:
   
   - ROUND_ROBIN: This is a simple policy in which each healthy
   backend is selected in round robin order. This is the default.
   - LEAST_REQUEST: An O(1) algorithm which
   selects two random healthy hosts and picks the host which has fewer active
   requests.
   - RING_HASH: The ring/modulo hash load balancer implements
   consistent hashing to backends. The algorithm has the property that the
   addition/removal of a host from a set of N hosts only affects 1/N of the
   requests.
   - RANDOM: The load balancer selects a random healthy
   host.
   - ORIGINAL_DESTINATION: Backend host is selected
   based on the client connection metadata, i.e., connections are opened to
   the same address as the destination address of the incoming connection
   before the connection was redirected to the load balancer.
   - MAGLEV: used as a drop in replacement for the ring hash
   load balancer. Maglev is not as stable as ring hash but has faster table
   lookup build times and host selection times. For more information about
   Maglev, see Maglev:
   A Fast and Reliable Software Network Load Balancer.
   - WEIGHTED_ROUND_ROBIN: Per-endpoint Weighted Round Robin
   Load Balancing using weights computed from Backend reported Custom Metrics.
   If set, the Backend Service responses are expected to contain non-standard
   HTTP response header field Endpoint-Load-Metrics. The reported
   metrics to use for computing the weights are specified via thecustomMetrics field.
   
   This field is applicable to either:
      - A regional backend service with the service_protocol set to HTTP,
      HTTPS, HTTP2 or H2C, and load_balancing_scheme set to
      INTERNAL_MANAGED. 
      - A global backend service with the
      load_balancing_scheme set to INTERNAL_SELF_MANAGED, INTERNAL_MANAGED, or
      EXTERNAL_MANAGED.
   
   
   If sessionAffinity is not configured—that is, if session
   affinity remains at the default value of NONE—then the
   default value for localityLbPolicy
   is ROUND_ROBIN. If session affinity is set to a value other
   than NONE,
   then the default value for localityLbPolicy isMAGLEV.
   
   Only ROUND_ROBIN and RING_HASH are supported
   when the backend service is referenced by a URL map that is bound to
   target gRPC proxy that has validateForProxyless field set to true.
   
   localityLbPolicy cannot be specified with haPolicy. |
| `vpc_network_scope` | String | The network scope of the backends that can be added to the backend
service. This field can be either GLOBAL_VPC_NETWORK orREGIONAL_VPC_NETWORK.

A backend service with the VPC scope set to GLOBAL_VPC_NETWORK
is only allowed to have backends in global VPC networks.

When the VPC scope is set to REGIONAL_VPC_NETWORK the backend
service is only allowed to have backends in regional networks in the same
scope as the backend service.
Note: if not specified then GLOBAL_VPC_NETWORK will be used. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `edge_security_policy` | String | [Output Only] The resource URL for the edge security policy associated with
this backend service. |
| `network_pass_through_lb_traffic_policy` | String | Configures traffic steering properties of internal passthrough Network
Load Balancers.

networkPassThroughLbTrafficPolicy cannot be specified with haPolicy. |
| `allow_multinetwork` | bool | A boolean flag enabling multi-network mesh. This field is only allowed with
load balancing scheme set to INTERNAL_SELF_MANAGED. |
| `enable_cdn` | bool | If true, enables Cloud CDN for the backend service of a
global external Application Load Balancer. |
| `affinity_cookie_ttl_sec` | i64 | Lifetime of cookies in seconds. This setting is applicable to Application
Load Balancers and Traffic Director and requires
GENERATED_COOKIE or HTTP_COOKIE session affinity.

If set to 0, the cookie is non-persistent and lasts only until
the end of the browser session (or equivalent). The maximum allowed value
is two weeks (1,209,600).

Not supported when the backend service is referenced by a URL map that is
bound to target gRPC proxy that has validateForProxyless field set to true. |
| `protocol` | String | The protocol this BackendService uses to communicate
with backends.

Possible values are HTTP, HTTPS, HTTP2, H2C, TCP, SSL, UDP or GRPC.
depending on the chosen load balancer or Traffic Director configuration.
Refer to the documentation for the load balancers or for Traffic Director
for more information.

Must be set to GRPC when the backend service is referenced by a URL map
that is bound to target gRPC proxy. |
| `service_lb_policy` | String | URL to networkservices.ServiceLbPolicy resource.

Can only be set if load balancing scheme is EXTERNAL_MANAGED,
INTERNAL_MANAGED or INTERNAL_SELF_MANAGED and the scope is global. |
| `consistent_hash` | String | Consistent Hash-based load balancing can be used to provide soft session
affinity based on HTTP headers, cookies or other properties. This load
balancing policy is applicable only for HTTP connections. The affinity to a
particular destination host will be lost when one or more hosts are
added/removed from the destination service. This field specifies parameters
that control consistent hashing. This field is only applicable whenlocalityLbPolicy is set to MAGLEV orRING_HASH.

This field is applicable to either:
   
   - A regional backend service with the service_protocol set to HTTP,
   HTTPS, HTTP2 or H2C, and load_balancing_scheme set to
   INTERNAL_MANAGED. 
   - A global backend service with the
   load_balancing_scheme set to INTERNAL_SELF_MANAGED. |
| `locality_lb_policies` | Vec<String> | A list of locality load-balancing policies to be used in order of
preference. When you use localityLbPolicies, you must set at least one
value for either the localityLbPolicies[].policy or the
localityLbPolicies[].customPolicy field. localityLbPolicies overrides any
value set in the localityLbPolicy field.

For an example of how to use this field, seeDefine
a list of preferred policies.

Caution: This field and its children are intended for use in a service mesh
that includes gRPC clients only. Envoy proxies can't use backend services
that have this configuration. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `port_name` | String | A named port on a backend instance group representing the port for
communication to the backend VMs in that group. The
named port must be [defined on each backend instance
group](https://cloud.google.com/load-balancing/docs/backend-service#named_ports).
This parameter has no meaning if the backends are NEGs. For internal
passthrough Network Load Balancers and external passthrough Network Load
Balancers, omit port_name. |
| `ha_policy` | String | Configures self-managed High Availability (HA) for External and Internal
Protocol Forwarding.

The backends of this regional backend service must only specify zonal
network endpoint groups (NEGs) of type GCE_VM_IP.

When haPolicy is set for an Internal Passthrough Network Load Balancer, the
regional backend service must set the network field. All zonal NEGs must
belong to the same network. However, individual NEGs can
belong to different subnetworks of that network.

When haPolicy is specified, the set of attached network endpoints across
all backends comprise an High Availability domain from which one endpoint
is selected as the active endpoint (the leader) that receives all
traffic.

haPolicy can be added only at backend service creation time. Once set up,
it cannot be deleted.

Note that haPolicy is not for load balancing, and therefore cannot be
specified with sessionAffinity, connectionTrackingPolicy, and
failoverPolicy.

haPolicy requires customers to be responsible for tracking backend
endpoint health and electing a leader among the healthy endpoints.
Therefore, haPolicy cannot be specified with healthChecks.

haPolicy can only be specified for External Passthrough Network Load
Balancers and Internal Passthrough Network Load Balancers. |
| `cdn_policy` | String | Cloud CDN configuration for this BackendService. Only available for
specified load balancer types. |
| `fingerprint` | String | Fingerprint of this resource. A hash of the contents stored in this object.
This field is used in optimistic locking. This field will be ignored when
inserting a BackendService. An up-to-date fingerprint must be provided in
order to update the BackendService, otherwise the request will
fail with error 412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve a BackendService. |
| `security_settings` | String | This field specifies the security settings that apply to this backend
service. This field is applicable to a global backend service with the
load_balancing_scheme set to INTERNAL_SELF_MANAGED. |
| `custom_metrics` | Vec<String> | List of custom metrics that are used for theWEIGHTED_ROUND_ROBIN locality_lb_policy. |
| `failover_policy` | String | Requires at least one backend instance group to be defined
as a backup (failover) backend.
For load balancers that have configurable failover:
[Internal passthrough Network Load
Balancers](https://cloud.google.com/load-balancing/docs/internal/failover-overview)
and [external passthrough Network Load
Balancers](https://cloud.google.com/load-balancing/docs/network/networklb-failover-overview).

failoverPolicy cannot be specified with haPolicy. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create region_backend_service
region_backend_service = provider.compute_api.Region_backend_service {
    region = "value"  # Name of the region scoping this request.
    project = "value"  # Project ID for this request.
}

# Access region_backend_service outputs
region_backend_service_id = region_backend_service.id
region_backend_service_backends = region_backend_service.backends
region_backend_service_log_config = region_backend_service.log_config
region_backend_service_compression_mode = region_backend_service.compression_mode
region_backend_service_name = region_backend_service.name
region_backend_service_params = region_backend_service.params
region_backend_service_security_policy = region_backend_service.security_policy
region_backend_service_kind = region_backend_service.kind
region_backend_service_outlier_detection = region_backend_service.outlier_detection
region_backend_service_service_bindings = region_backend_service.service_bindings
region_backend_service_custom_request_headers = region_backend_service.custom_request_headers
region_backend_service_health_checks = region_backend_service.health_checks
region_backend_service_load_balancing_scheme = region_backend_service.load_balancing_scheme
region_backend_service_max_stream_duration = region_backend_service.max_stream_duration
region_backend_service_network = region_backend_service.network
region_backend_service_custom_response_headers = region_backend_service.custom_response_headers
region_backend_service_self_link = region_backend_service.self_link
region_backend_service_subsetting = region_backend_service.subsetting
region_backend_service_creation_timestamp = region_backend_service.creation_timestamp
region_backend_service_timeout_sec = region_backend_service.timeout_sec
region_backend_service_connection_tracking_policy = region_backend_service.connection_tracking_policy
region_backend_service_session_affinity = region_backend_service.session_affinity
region_backend_service_port = region_backend_service.port
region_backend_service_iap = region_backend_service.iap
region_backend_service_self_link_with_id = region_backend_service.self_link_with_id
region_backend_service_ip_address_selection_policy = region_backend_service.ip_address_selection_policy
region_backend_service_circuit_breakers = region_backend_service.circuit_breakers
region_backend_service_metadatas = region_backend_service.metadatas
region_backend_service_external_managed_migration_state = region_backend_service.external_managed_migration_state
region_backend_service_tls_settings = region_backend_service.tls_settings
region_backend_service_dynamic_forwarding = region_backend_service.dynamic_forwarding
region_backend_service_used_by = region_backend_service.used_by
region_backend_service_connection_draining = region_backend_service.connection_draining
region_backend_service_external_managed_migration_testing_percentage = region_backend_service.external_managed_migration_testing_percentage
region_backend_service_region = region_backend_service.region
region_backend_service_strong_session_affinity_cookie = region_backend_service.strong_session_affinity_cookie
region_backend_service_locality_lb_policy = region_backend_service.locality_lb_policy
region_backend_service_vpc_network_scope = region_backend_service.vpc_network_scope
region_backend_service_description = region_backend_service.description
region_backend_service_edge_security_policy = region_backend_service.edge_security_policy
region_backend_service_network_pass_through_lb_traffic_policy = region_backend_service.network_pass_through_lb_traffic_policy
region_backend_service_allow_multinetwork = region_backend_service.allow_multinetwork
region_backend_service_enable_cdn = region_backend_service.enable_cdn
region_backend_service_affinity_cookie_ttl_sec = region_backend_service.affinity_cookie_ttl_sec
region_backend_service_protocol = region_backend_service.protocol
region_backend_service_service_lb_policy = region_backend_service.service_lb_policy
region_backend_service_consistent_hash = region_backend_service.consistent_hash
region_backend_service_locality_lb_policies = region_backend_service.locality_lb_policies
region_backend_service_id = region_backend_service.id
region_backend_service_port_name = region_backend_service.port_name
region_backend_service_ha_policy = region_backend_service.ha_policy
region_backend_service_cdn_policy = region_backend_service.cdn_policy
region_backend_service_fingerprint = region_backend_service.fingerprint
region_backend_service_security_settings = region_backend_service.security_settings
region_backend_service_custom_metrics = region_backend_service.custom_metrics
region_backend_service_failover_policy = region_backend_service.failover_policy
```

---


### Global_vm_extension_policie

Creates a new project level GlobalVmExtensionPolicy.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `extension_policies` | HashMap<String, String> |  | Required. Map from extension (eg: "cloudops") to its policy configuration.
The key is the name of the extension. |
| `instance_selectors` | Vec<String> |  | Optional. Selector to target VMs for a policy.
There is a logical "AND" between instance_selectors. |
| `self_link_with_id` | String |  | [Output Only] Server-defined URL for this resource's resource id. |
| `self_link` | String |  | [Output Only] Server-defined fully-qualified URL for this resource. |
| `name` | String |  | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?`
which means the first character must be a lowercase letter, and all
following characters must be a dash, lowercase letter, or digit, except
the last character, which cannot be a dash. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `priority` | i64 |  | Optional. Used to resolve conflicts when multiple policies are active for the same
extension. Defaults to 0.

Larger the number, higher the priority. When the priority is the same,
the policy with the newer create time has higher priority. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `update_timestamp` | String |  | [Output Only] Update timestamp inRFC3339
text format. |
| `kind` | String |  | [Output Only] Type of the resource. Alwayscompute#globalVmExtensionPolicy for globalVmExtensionPolicies. |
| `scoped_resource_status` | String |  | [Output Only] The scoped resource status. It's only for tracking the
purging status of the policy. |
| `rollout_operation` | String |  | Required. The rollout strategy and status. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `extension_policies` | HashMap<String, String> | Required. Map from extension (eg: "cloudops") to its policy configuration.
The key is the name of the extension. |
| `instance_selectors` | Vec<String> | Optional. Selector to target VMs for a policy.
There is a logical "AND" between instance_selectors. |
| `self_link_with_id` | String | [Output Only] Server-defined URL for this resource's resource id. |
| `self_link` | String | [Output Only] Server-defined fully-qualified URL for this resource. |
| `name` | String | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?`
which means the first character must be a lowercase letter, and all
following characters must be a dash, lowercase letter, or digit, except
the last character, which cannot be a dash. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `priority` | i64 | Optional. Used to resolve conflicts when multiple policies are active for the same
extension. Defaults to 0.

Larger the number, higher the priority. When the priority is the same,
the policy with the newer create time has higher priority. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `update_timestamp` | String | [Output Only] Update timestamp inRFC3339
text format. |
| `kind` | String | [Output Only] Type of the resource. Alwayscompute#globalVmExtensionPolicy for globalVmExtensionPolicies. |
| `scoped_resource_status` | String | [Output Only] The scoped resource status. It's only for tracking the
purging status of the policy. |
| `rollout_operation` | String | Required. The rollout strategy and status. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create global_vm_extension_policie
global_vm_extension_policie = provider.compute_api.Global_vm_extension_policie {
    project = "value"  # Project ID for this request.
}

# Access global_vm_extension_policie outputs
global_vm_extension_policie_id = global_vm_extension_policie.id
global_vm_extension_policie_extension_policies = global_vm_extension_policie.extension_policies
global_vm_extension_policie_instance_selectors = global_vm_extension_policie.instance_selectors
global_vm_extension_policie_self_link_with_id = global_vm_extension_policie.self_link_with_id
global_vm_extension_policie_self_link = global_vm_extension_policie.self_link
global_vm_extension_policie_name = global_vm_extension_policie.name
global_vm_extension_policie_id = global_vm_extension_policie.id
global_vm_extension_policie_description = global_vm_extension_policie.description
global_vm_extension_policie_priority = global_vm_extension_policie.priority
global_vm_extension_policie_creation_timestamp = global_vm_extension_policie.creation_timestamp
global_vm_extension_policie_update_timestamp = global_vm_extension_policie.update_timestamp
global_vm_extension_policie_kind = global_vm_extension_policie.kind
global_vm_extension_policie_scoped_resource_status = global_vm_extension_policie.scoped_resource_status
global_vm_extension_policie_rollout_operation = global_vm_extension_policie.rollout_operation
```

---


### Node_group

Creates a NodeGroup resource in the specified project using the data
included in the request.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `autoscaling_policy` | String |  | Specifies how autoscaling should behave. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `maintenance_policy` | String |  | Specifies how to handle instances when a node in the group undergoes
maintenance. Set to one of: DEFAULT,RESTART_IN_PLACE, or MIGRATE_WITHIN_NODE_GROUP.
The default value is DEFAULT. For more information, see
Maintenance policies. |
| `maintenance_interval` | String |  | Specifies the frequency of planned maintenance events. The accepted values
are: `AS_NEEDED` and `RECURRENT`. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `location_hint` | String |  | An opaque location hint used to place the Node close to other
resources.
This field is for use by internal tools that use the public API.
The location hint here on the NodeGroup overrides any location_hint
present in the NodeTemplate. |
| `kind` | String |  | [Output Only] The type of the resource. Alwayscompute#nodeGroup for node group. |
| `size` | i64 |  | [Output Only] The total number of nodes in the node group. |
| `maintenance_window` | String |  |  |
| `node_template` | String |  | URL of the node template to create the node group from. |
| `fingerprint` | String |  |  |
| `name` | String |  | The name of the resource, provided by the client when initially creating
the resource. The resource name must be 1-63 characters long, and comply
withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must be
a dash, lowercase letter, or digit, except the last character, which cannot
be a dash. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `share_settings` | String |  | Share-settings for the node group |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `self_link_with_id` | String |  | [Output Only] Server-defined URL for this resource with the resource id. |
| `status` | String |  |  |
| `zone` | String |  | [Output Only] The name of the zone where the node group resides,
such as us-central1-a. |
| `project` | String | ✅ | Project ID for this request. |
| `zone` | String | ✅ | The name of the zone for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `autoscaling_policy` | String | Specifies how autoscaling should behave. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `maintenance_policy` | String | Specifies how to handle instances when a node in the group undergoes
maintenance. Set to one of: DEFAULT,RESTART_IN_PLACE, or MIGRATE_WITHIN_NODE_GROUP.
The default value is DEFAULT. For more information, see
Maintenance policies. |
| `maintenance_interval` | String | Specifies the frequency of planned maintenance events. The accepted values
are: `AS_NEEDED` and `RECURRENT`. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `location_hint` | String | An opaque location hint used to place the Node close to other
resources.
This field is for use by internal tools that use the public API.
The location hint here on the NodeGroup overrides any location_hint
present in the NodeTemplate. |
| `kind` | String | [Output Only] The type of the resource. Alwayscompute#nodeGroup for node group. |
| `size` | i64 | [Output Only] The total number of nodes in the node group. |
| `maintenance_window` | String |  |
| `node_template` | String | URL of the node template to create the node group from. |
| `fingerprint` | String |  |
| `name` | String | The name of the resource, provided by the client when initially creating
the resource. The resource name must be 1-63 characters long, and comply
withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must be
a dash, lowercase letter, or digit, except the last character, which cannot
be a dash. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `share_settings` | String | Share-settings for the node group |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `self_link_with_id` | String | [Output Only] Server-defined URL for this resource with the resource id. |
| `status` | String |  |
| `zone` | String | [Output Only] The name of the zone where the node group resides,
such as us-central1-a. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create node_group
node_group = provider.compute_api.Node_group {
    project = "value"  # Project ID for this request.
    zone = "value"  # The name of the zone for this request.
}

# Access node_group outputs
node_group_id = node_group.id
node_group_autoscaling_policy = node_group.autoscaling_policy
node_group_self_link = node_group.self_link
node_group_maintenance_policy = node_group.maintenance_policy
node_group_maintenance_interval = node_group.maintenance_interval
node_group_creation_timestamp = node_group.creation_timestamp
node_group_location_hint = node_group.location_hint
node_group_kind = node_group.kind
node_group_size = node_group.size
node_group_maintenance_window = node_group.maintenance_window
node_group_node_template = node_group.node_template
node_group_fingerprint = node_group.fingerprint
node_group_name = node_group.name
node_group_id = node_group.id
node_group_share_settings = node_group.share_settings
node_group_description = node_group.description
node_group_self_link_with_id = node_group.self_link_with_id
node_group_status = node_group.status
node_group_zone = node_group.zone
```

---


### Region_ssl_policie

Creates a new policy in the specified project and region using the data
included in the request.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `enabled_features` | Vec<String> |  | [Output Only] The list of features enabled in the SSL policy. |
| `tls_settings` | String |  | Security settings for the proxy. This field is only applicable to a
global backend service with the loadBalancingScheme set toINTERNAL_SELF_MANAGED. |
| `kind` | String |  | [Output only] Type of the resource. Alwayscompute#sslPolicyfor SSL policies. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `name` | String |  | Name of the resource. The name must be 1-63 characters long, and comply
with RFC1035. Specifically, the name must be 1-63 characters
long and match the regular expression
`[a-z]([-a-z0-9]*[a-z0-9])?` which means the first character
must be a lowercase letter, and all following characters must be a dash,
lowercase letter, or digit, except the last character, which cannot be a
dash. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `warnings` | Vec<String> |  | [Output Only] If potential misconfigurations are detected for this
SSL policy, this field will be populated with warning messages. |
| `fingerprint` | String |  | Fingerprint of this resource. A hash of the contents stored in this object.
This field is used in optimistic locking. This field will be ignored when
inserting a SslPolicy. An up-to-date fingerprint must be
provided in order to update the SslPolicy, otherwise the
request will fail with error 412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve an SslPolicy. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `profile` | String |  | Profile specifies the set of SSL features that can be used by the load
balancer when negotiating SSL with clients. This can be one ofCOMPATIBLE, MODERN, RESTRICTED, orCUSTOM. If using CUSTOM, the set of SSL features
to enable must be specified in the customFeatures field. |
| `self_link_with_id` | String |  | [Output Only] Server-defined URL for this resource with the resource id. |
| `min_tls_version` | String |  | The minimum version of SSL protocol that can be used by the clients to
establish a connection with the load balancer. This can be one ofTLS_1_0, TLS_1_1, TLS_1_2,TLS_1_3. When set to TLS_1_3, the profile field
must be set to RESTRICTED. |
| `custom_features` | Vec<String> |  | A list of features enabled when the selected profile is CUSTOM. The
 method returns the set of features that can be
specified in this list. This field must be empty if the profile is notCUSTOM. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `region` | String |  | [Output Only] URL of the region where the regional SSL policy
resides. This field is not applicable to global SSL policies. |
| `project` | String | ✅ | Project ID for this request. |
| `region` | String | ✅ | Name of the region scoping this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `enabled_features` | Vec<String> | [Output Only] The list of features enabled in the SSL policy. |
| `tls_settings` | String | Security settings for the proxy. This field is only applicable to a
global backend service with the loadBalancingScheme set toINTERNAL_SELF_MANAGED. |
| `kind` | String | [Output only] Type of the resource. Alwayscompute#sslPolicyfor SSL policies. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `name` | String | Name of the resource. The name must be 1-63 characters long, and comply
with RFC1035. Specifically, the name must be 1-63 characters
long and match the regular expression
`[a-z]([-a-z0-9]*[a-z0-9])?` which means the first character
must be a lowercase letter, and all following characters must be a dash,
lowercase letter, or digit, except the last character, which cannot be a
dash. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `warnings` | Vec<String> | [Output Only] If potential misconfigurations are detected for this
SSL policy, this field will be populated with warning messages. |
| `fingerprint` | String | Fingerprint of this resource. A hash of the contents stored in this object.
This field is used in optimistic locking. This field will be ignored when
inserting a SslPolicy. An up-to-date fingerprint must be
provided in order to update the SslPolicy, otherwise the
request will fail with error 412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve an SslPolicy. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `profile` | String | Profile specifies the set of SSL features that can be used by the load
balancer when negotiating SSL with clients. This can be one ofCOMPATIBLE, MODERN, RESTRICTED, orCUSTOM. If using CUSTOM, the set of SSL features
to enable must be specified in the customFeatures field. |
| `self_link_with_id` | String | [Output Only] Server-defined URL for this resource with the resource id. |
| `min_tls_version` | String | The minimum version of SSL protocol that can be used by the clients to
establish a connection with the load balancer. This can be one ofTLS_1_0, TLS_1_1, TLS_1_2,TLS_1_3. When set to TLS_1_3, the profile field
must be set to RESTRICTED. |
| `custom_features` | Vec<String> | A list of features enabled when the selected profile is CUSTOM. The
 method returns the set of features that can be
specified in this list. This field must be empty if the profile is notCUSTOM. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `region` | String | [Output Only] URL of the region where the regional SSL policy
resides. This field is not applicable to global SSL policies. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create region_ssl_policie
region_ssl_policie = provider.compute_api.Region_ssl_policie {
    project = "value"  # Project ID for this request.
    region = "value"  # Name of the region scoping this request.
}

# Access region_ssl_policie outputs
region_ssl_policie_id = region_ssl_policie.id
region_ssl_policie_enabled_features = region_ssl_policie.enabled_features
region_ssl_policie_tls_settings = region_ssl_policie.tls_settings
region_ssl_policie_kind = region_ssl_policie.kind
region_ssl_policie_id = region_ssl_policie.id
region_ssl_policie_name = region_ssl_policie.name
region_ssl_policie_self_link = region_ssl_policie.self_link
region_ssl_policie_warnings = region_ssl_policie.warnings
region_ssl_policie_fingerprint = region_ssl_policie.fingerprint
region_ssl_policie_creation_timestamp = region_ssl_policie.creation_timestamp
region_ssl_policie_profile = region_ssl_policie.profile
region_ssl_policie_self_link_with_id = region_ssl_policie.self_link_with_id
region_ssl_policie_min_tls_version = region_ssl_policie.min_tls_version
region_ssl_policie_custom_features = region_ssl_policie.custom_features
region_ssl_policie_description = region_ssl_policie.description
region_ssl_policie_region = region_ssl_policie.region
```

---


### Snapshot_setting

Get snapshot settings.

**Operations**: ✅ Read ✅ Update

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `access_location` | String |  | (Regional snapshots use only)Policy of which location is allowed to access
snapshot. |
| `storage_location` | String |  | Policy of which storage location is going to be resolved, and additional
data that particularizes how the policy is going to be carried out. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `access_location` | String | (Regional snapshots use only)Policy of which location is allowed to access
snapshot. |
| `storage_location` | String | Policy of which storage location is going to be resolved, and additional
data that particularizes how the policy is going to be carried out. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Access snapshot_setting outputs
snapshot_setting_id = snapshot_setting.id
snapshot_setting_access_location = snapshot_setting.access_location
snapshot_setting_storage_location = snapshot_setting.storage_location
```

---


### Instance_group_manager_resize_request

Creates a new resize request that starts provisioning VMs immediately
or queues VM creation.

**Operations**: ✅ Create ✅ Read ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `description` | String |  | An optional description of this resource. |
| `region` | String |  | [Output Only] The URL of aregion
where the resize request is located. Populated only for regional resize
requests. |
| `kind` | String |  | [Output Only] The resource type, which is alwayscompute#instanceGroupManagerResizeRequest for
resize requests. |
| `requested_run_duration` | String |  | Requested run duration for instances that will be created by this request.
At the end of the run duration instance will be deleted. |
| `creation_timestamp` | String |  | [Output Only] The creation timestamp for this resize request inRFC3339
text format. |
| `resize_by` | i64 |  | The number of instances to be created by this resize request. The group's
target size will be increased by this number. This field cannot be used
together with 'instances'. |
| `state` | String |  | [Output only] Current state of the request. |
| `name` | String |  | The name of this resize request. The name must be 1-63 characters
long, and comply withRFC1035. |
| `id` | String |  | [Output Only] A unique identifier for this resource type. The server
generates this identifier. |
| `self_link_with_id` | String |  | [Output Only] Server-defined URL for this resource with the resource id. |
| `status` | String |  | [Output only] Status of the request. |
| `zone` | String |  | [Output Only] The URL of azone
where the resize request is located. Populated only for zonal resize
requests. |
| `queuing_policy` | String |  | This field is deprecated, ResizeRequests would not be provisioned
immediately and would stay in the queue until explicitly cancelled. When
set, defines queing parameters for the requested deferred capacity. When
unset, the request starts provisioning immediately, or fails if immediate
provisioning is not possible. |
| `count` | i64 |  | This field is deprecated, please use resize_by instead.
The count of instances to create as part of this resize request. |
| `instances` | Vec<String> |  | The names of instances to be created by this resize request. The number of
names specified determines the number of instances to create. The group's
target size will be increased by this number. This field cannot be used
together with 'resize_by'. |
| `self_link` | String |  | [Output Only] The URL for this resize request. The server defines
this URL. |
| `project` | String | ✅ | Project ID for this request. |
| `instance_group_manager` | String | ✅ | The name of the managed instance group to which the resize request will
be added.
Name should conform to RFC1035 or be a resource ID. |
| `zone` | String | ✅ | The name of thezone where the managed
instance group is located and where the resize request will be created.
Name should conform to RFC1035. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `description` | String | An optional description of this resource. |
| `region` | String | [Output Only] The URL of aregion
where the resize request is located. Populated only for regional resize
requests. |
| `kind` | String | [Output Only] The resource type, which is alwayscompute#instanceGroupManagerResizeRequest for
resize requests. |
| `requested_run_duration` | String | Requested run duration for instances that will be created by this request.
At the end of the run duration instance will be deleted. |
| `creation_timestamp` | String | [Output Only] The creation timestamp for this resize request inRFC3339
text format. |
| `resize_by` | i64 | The number of instances to be created by this resize request. The group's
target size will be increased by this number. This field cannot be used
together with 'instances'. |
| `state` | String | [Output only] Current state of the request. |
| `name` | String | The name of this resize request. The name must be 1-63 characters
long, and comply withRFC1035. |
| `id` | String | [Output Only] A unique identifier for this resource type. The server
generates this identifier. |
| `self_link_with_id` | String | [Output Only] Server-defined URL for this resource with the resource id. |
| `status` | String | [Output only] Status of the request. |
| `zone` | String | [Output Only] The URL of azone
where the resize request is located. Populated only for zonal resize
requests. |
| `queuing_policy` | String | This field is deprecated, ResizeRequests would not be provisioned
immediately and would stay in the queue until explicitly cancelled. When
set, defines queing parameters for the requested deferred capacity. When
unset, the request starts provisioning immediately, or fails if immediate
provisioning is not possible. |
| `count` | i64 | This field is deprecated, please use resize_by instead.
The count of instances to create as part of this resize request. |
| `instances` | Vec<String> | The names of instances to be created by this resize request. The number of
names specified determines the number of instances to create. The group's
target size will be increased by this number. This field cannot be used
together with 'resize_by'. |
| `self_link` | String | [Output Only] The URL for this resize request. The server defines
this URL. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create instance_group_manager_resize_request
instance_group_manager_resize_request = provider.compute_api.Instance_group_manager_resize_request {
    project = "value"  # Project ID for this request.
    instance_group_manager = "value"  # The name of the managed instance group to which the resize request will
be added.
Name should conform to RFC1035 or be a resource ID.
    zone = "value"  # The name of thezone where the managed
instance group is located and where the resize request will be created.
Name should conform to RFC1035.
}

# Access instance_group_manager_resize_request outputs
instance_group_manager_resize_request_id = instance_group_manager_resize_request.id
instance_group_manager_resize_request_description = instance_group_manager_resize_request.description
instance_group_manager_resize_request_region = instance_group_manager_resize_request.region
instance_group_manager_resize_request_kind = instance_group_manager_resize_request.kind
instance_group_manager_resize_request_requested_run_duration = instance_group_manager_resize_request.requested_run_duration
instance_group_manager_resize_request_creation_timestamp = instance_group_manager_resize_request.creation_timestamp
instance_group_manager_resize_request_resize_by = instance_group_manager_resize_request.resize_by
instance_group_manager_resize_request_state = instance_group_manager_resize_request.state
instance_group_manager_resize_request_name = instance_group_manager_resize_request.name
instance_group_manager_resize_request_id = instance_group_manager_resize_request.id
instance_group_manager_resize_request_self_link_with_id = instance_group_manager_resize_request.self_link_with_id
instance_group_manager_resize_request_status = instance_group_manager_resize_request.status
instance_group_manager_resize_request_zone = instance_group_manager_resize_request.zone
instance_group_manager_resize_request_queuing_policy = instance_group_manager_resize_request.queuing_policy
instance_group_manager_resize_request_count = instance_group_manager_resize_request.count
instance_group_manager_resize_request_instances = instance_group_manager_resize_request.instances
instance_group_manager_resize_request_self_link = instance_group_manager_resize_request.self_link
```

---


### Disk_type

Returns the specified disk type.

**Operations**: ✅ Read

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `deprecated` | String | [Output Only] The deprecation status associated with this disk type. |
| `name` | String | [Output Only] Name of the resource. |
| `self_link_with_id` | String | [Output Only] Server-defined URL for this resource with the resource id. |
| `region` | String | [Output Only] URL of the region where the disk type resides. Only
applicable for regional resources.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `description` | String | [Output Only] An optional description of this resource. |
| `kind` | String | [Output Only] Type of the resource. Always compute#diskType
for disk types. |
| `valid_disk_size` | String | [Output Only] An optional textual description of the valid disk size,
such as "10GB-10TB". |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `default_disk_size_gb` | String | [Output Only] Server-defined default disk size in GB. |
| `zone` | String | [Output Only] URL of the zone where the disk type resides.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Access disk_type outputs
disk_type_id = disk_type.id
disk_type_creation_timestamp = disk_type.creation_timestamp
disk_type_deprecated = disk_type.deprecated
disk_type_name = disk_type.name
disk_type_self_link_with_id = disk_type.self_link_with_id
disk_type_region = disk_type.region
disk_type_self_link = disk_type.self_link
disk_type_description = disk_type.description
disk_type_kind = disk_type.kind
disk_type_valid_disk_size = disk_type.valid_disk_size
disk_type_id = disk_type.id
disk_type_default_disk_size_gb = disk_type.default_disk_size_gb
disk_type_zone = disk_type.zone
```

---


### Subnetwork

Creates a subnetwork in the specified project using the data
included in the request.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `params` | String |  | Input only. [Input Only] Additional params passed with the request, but not persisted
as part of resource payload. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `stack_type` | String |  | The stack type for the subnet. If set to IPV4_ONLY, new VMs
in the subnet are assigned IPv4 addresses only. If set toIPV4_IPV6, new VMs in the subnet can be assigned both IPv4 and
IPv6 addresses. If not specified, IPV4_ONLY is used.

This field can be both set at resource creation time and updated usingpatch. |
| `vlans` | Vec<i64> |  | A repeated field indicating the VLAN IDs supported on this subnetwork.
During Subnet creation, specifying vlan is valid only if enable_l2 is true.
During Subnet Update, specifying vlan is allowed only for l2 enabled
subnets. Restricted to only one VLAN. |
| `log_config` | String |  | This field denotes the VPC flow logging options for this subnetwork.
If logging is enabled, logs are exported to Cloud Logging. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `network` | String |  | The URL of the network to which this subnetwork belongs, provided by the
client when initially creating the subnetwork. This field can be set only
at resource creation time. |
| `fingerprint` | String |  | Fingerprint of this resource. A hash of the contents stored in this object.
This field is used in optimistic locking. This field will be ignored when
inserting a Subnetwork. An up-to-date fingerprint must be
provided in order to update the Subnetwork, otherwise the
request will fail with error 412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve a Subnetwork. |
| `ipv6_access_type` | String |  | The access type of IPv6 address this subnet holds. It's immutable and can
only be specified during creation or the first time the subnet is updated
into IPV4_IPV6 dual stack. |
| `enable_l2` | bool |  | Enables Layer2 communication on the subnetwork. |
| `allow_subnet_cidr_routes_overlap` | bool |  | Whether this subnetwork's ranges can conflict with existing static routes.
Setting this to true allows this subnetwork's primary and secondary ranges
to overlap with (and contain) static routes that have already been
configured on the corresponding network.

For example if a static route has range 10.1.0.0/16, a subnet
range 10.0.0.0/8 could only be created if allow_conflicting_routes=true.

Overlapping is only allowed on subnetwork operations; routes
whose ranges conflict with this subnetwork's ranges won't be allowed unless
route.allow_conflicting_subnetworks is set to true.

Typically packets destined to IPs within the subnetwork (which may contain
private/sensitive data) are prevented from leaving the virtual network.
Setting this field to true will disable this feature.

The default value is false and applies to all existing subnetworks and
automatically created subnetworks.

This field cannot be set to true at resource creation time. |
| `state` | String |  | [Output Only] The state of the subnetwork, which can be one of the
following values:READY: Subnetwork is created and ready to useDRAINING: only applicable to subnetworks that have the
purpose set to INTERNAL_HTTPS_LOAD_BALANCER and indicates that
connections to the load balancer are being drained. A subnetwork that is
draining cannot be used or modified until it reaches a status ofREADY |
| `flow_sampling` | f64 |  | Can only be specified if VPC flow logging for this subnetwork is enabled.
The value of the field must be in [0, 1]. Set the sampling rate of VPC flow
logs within the subnetwork where 1.0 means all collected logs
are reported and 0.0 means no logs are reported. Default is0.5 unless otherwise specified by the org policy, which means
half of all collected logs are reported. |
| `ip_collection` | String |  | Reference to the source of IP, like a PublicDelegatedPrefix
(PDP) for BYOIP. The PDP must be a sub-PDP in
EXTERNAL_IPV6_SUBNETWORK_CREATION or INTERNAL_IPV6_SUBNETWORK_CREATION
mode.

Use one of the following formats to specify a sub-PDP when creating a dual
stack or IPv6-only subnetwork with external access using BYOIP:
   
   - 
   Full resource URL, as inhttps://www.googleapis.com/compute/v1/projects/projectId/regions/region/publicDelegatedPrefixes/sub-pdp-name
   - 
   Partial URL, as in
     
      
          - projects/projectId/regions/region/publicDelegatedPrefixes/sub-pdp-name
          - regions/region/publicDelegatedPrefixes/sub-pdp-name |
| `private_ip_google_access` | bool |  | Whether the VMs in this subnet can access Google services without assigned
external IP addresses. This field can be both set at resource creation
time and updated using setPrivateIpGoogleAccess. |
| `ip_cidr_range` | String |  | The range of internal addresses that are owned by this subnetwork.
Provide this property when you create the subnetwork. For example,10.0.0.0/8 or 100.64.0.0/10. Ranges must
be unique and non-overlapping within a network. Only IPv4 is supported.
This field is set at resource creation time. The range can be any range
listed in theValid
ranges list. The range can be expanded after creation usingexpandIpCidrRange. |
| `reserved_internal_range` | String |  | The URL of the reserved internal range. |
| `purpose` | String |  |  |
| `ipv6_gce_endpoint` | String |  | [Output Only] Possible endpoints of this subnetwork. It can be one of the
following:
   
   - VM_ONLY: The subnetwork can be used for creating instances and
   IPv6 addresses with VM endpoint type. Such a subnetwork gets external IPv6
   ranges from a public delegated prefix and cannot be used to create NetLb.
   - VM_AND_FR: The subnetwork can be used for creating both VM
   instances and Forwarding Rules. It can also be used to reserve IPv6
   addresses with both VM and FR endpoint types. Such a subnetwork gets its
   IPv6 range from Google IP Pool directly. |
| `secondary_ip_ranges` | Vec<String> |  | An array of configurations for secondary IP ranges for VM instances
contained in this subnetwork. The primary IP of such VM must belong to the
primary ipCidrRange of the subnetwork. The alias IPs may belong to either
primary or secondary ranges. This field can be updated with apatch request. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `gateway_address` | String |  | [Output Only] The gateway address for default routes to reach destination
addresses outside this subnetwork. |
| `kind` | String |  | [Output Only] Type of the resource. Always compute#subnetwork
for Subnetwork resources. |
| `internal_ipv6_prefix` | String |  | The internal IPv6 address range that is owned by this
subnetwork. |
| `system_reserved_internal_ipv6_ranges` | Vec<String> |  | Output only. [Output Only] The array of internal IPv6 network ranges reserved from
the subnetwork's internal IPv6 range for system use. |
| `ipv6_cidr_range` | String |  | [Output Only] This field is for internal use. |
| `name` | String |  | The name of the resource, provided by the client when initially creating
the resource. The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `region` | String |  | URL of the region where the Subnetwork resides. This
field can be set only at resource creation time. |
| `role` | String |  | The role of subnetwork. Currently, this field is only used when
purpose is set to GLOBAL_MANAGED_PROXY orREGIONAL_MANAGED_PROXY. The value can be set toACTIVE or BACKUP. An ACTIVE
subnetwork is one that is currently being used for Envoy-based load
balancers in a region. A BACKUP subnetwork is one that is
ready to be promoted to ACTIVE or is currently draining.
This field can be updated with a patch request. |
| `self_link_with_id` | String |  | [Output Only] Server-defined URL for this resource with the resource id. |
| `metadata` | String |  | Can only be specified if VPC flow logging for this subnetwork is enabled.
Configures whether metadata fields should be added to the reported VPC flow
logs. Options are INCLUDE_ALL_METADATA,EXCLUDE_ALL_METADATA, and CUSTOM_METADATA.
Default is EXCLUDE_ALL_METADATA. |
| `enable_flow_logs` | bool |  | Whether to enable flow logging for this subnetwork. If this field is not
explicitly set, it will not appear in get listings. If not set
the default behavior is determined by the org policy, if there is no org
policy specified, then it will default to disabled. This field isn't
supported if the subnet purpose field is set toREGIONAL_MANAGED_PROXY. |
| `private_ipv6_google_access` | String |  | This field is for internal use.

This field can be both set at resource creation time and updated usingpatch. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. This field can be set only at resource creation time. |
| `resolve_subnet_mask` | String |  | Configures subnet mask resolution for this subnetwork. |
| `system_reserved_external_ipv6_ranges` | Vec<String> |  | Output only. [Output Only] The array of external IPv6 network ranges reserved from
the subnetwork's external IPv6 range for system use. |
| `aggregation_interval` | String |  | Can only be specified if VPC flow logging for this subnetwork is enabled.
Sets the aggregation interval for collecting flow logs. Increasing the
interval time reduces the amount of generated flow logs for long-lasting
connections. Default is an interval of 5 seconds per connection.
Valid values: INTERVAL_5_SEC, INTERVAL_30_SEC,INTERVAL_1_MIN, INTERVAL_5_MIN,INTERVAL_10_MIN, INTERVAL_15_MIN. |
| `enable_private_v6_access` | bool |  | Deprecated in favor of enable in PrivateIpv6GoogleAccess.
Whether the VMs in this subnet can directly access Google services via
internal IPv6 addresses. This field can be both set at resource creation
time and updated using patch. |
| `utilization_details` | String |  | Output only. [Output Only] The current IP utilization of all subnetwork ranges. Contains
the total number of allocated and free IPs in each range. |
| `external_ipv6_prefix` | String |  | The external IPv6 address range that is owned by this
subnetwork. |
| `project` | String | ✅ | Project ID for this request. |
| `region` | String | ✅ | Name of the region scoping this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `params` | String | Input only. [Input Only] Additional params passed with the request, but not persisted
as part of resource payload. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `stack_type` | String | The stack type for the subnet. If set to IPV4_ONLY, new VMs
in the subnet are assigned IPv4 addresses only. If set toIPV4_IPV6, new VMs in the subnet can be assigned both IPv4 and
IPv6 addresses. If not specified, IPV4_ONLY is used.

This field can be both set at resource creation time and updated usingpatch. |
| `vlans` | Vec<i64> | A repeated field indicating the VLAN IDs supported on this subnetwork.
During Subnet creation, specifying vlan is valid only if enable_l2 is true.
During Subnet Update, specifying vlan is allowed only for l2 enabled
subnets. Restricted to only one VLAN. |
| `log_config` | String | This field denotes the VPC flow logging options for this subnetwork.
If logging is enabled, logs are exported to Cloud Logging. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `network` | String | The URL of the network to which this subnetwork belongs, provided by the
client when initially creating the subnetwork. This field can be set only
at resource creation time. |
| `fingerprint` | String | Fingerprint of this resource. A hash of the contents stored in this object.
This field is used in optimistic locking. This field will be ignored when
inserting a Subnetwork. An up-to-date fingerprint must be
provided in order to update the Subnetwork, otherwise the
request will fail with error 412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve a Subnetwork. |
| `ipv6_access_type` | String | The access type of IPv6 address this subnet holds. It's immutable and can
only be specified during creation or the first time the subnet is updated
into IPV4_IPV6 dual stack. |
| `enable_l2` | bool | Enables Layer2 communication on the subnetwork. |
| `allow_subnet_cidr_routes_overlap` | bool | Whether this subnetwork's ranges can conflict with existing static routes.
Setting this to true allows this subnetwork's primary and secondary ranges
to overlap with (and contain) static routes that have already been
configured on the corresponding network.

For example if a static route has range 10.1.0.0/16, a subnet
range 10.0.0.0/8 could only be created if allow_conflicting_routes=true.

Overlapping is only allowed on subnetwork operations; routes
whose ranges conflict with this subnetwork's ranges won't be allowed unless
route.allow_conflicting_subnetworks is set to true.

Typically packets destined to IPs within the subnetwork (which may contain
private/sensitive data) are prevented from leaving the virtual network.
Setting this field to true will disable this feature.

The default value is false and applies to all existing subnetworks and
automatically created subnetworks.

This field cannot be set to true at resource creation time. |
| `state` | String | [Output Only] The state of the subnetwork, which can be one of the
following values:READY: Subnetwork is created and ready to useDRAINING: only applicable to subnetworks that have the
purpose set to INTERNAL_HTTPS_LOAD_BALANCER and indicates that
connections to the load balancer are being drained. A subnetwork that is
draining cannot be used or modified until it reaches a status ofREADY |
| `flow_sampling` | f64 | Can only be specified if VPC flow logging for this subnetwork is enabled.
The value of the field must be in [0, 1]. Set the sampling rate of VPC flow
logs within the subnetwork where 1.0 means all collected logs
are reported and 0.0 means no logs are reported. Default is0.5 unless otherwise specified by the org policy, which means
half of all collected logs are reported. |
| `ip_collection` | String | Reference to the source of IP, like a PublicDelegatedPrefix
(PDP) for BYOIP. The PDP must be a sub-PDP in
EXTERNAL_IPV6_SUBNETWORK_CREATION or INTERNAL_IPV6_SUBNETWORK_CREATION
mode.

Use one of the following formats to specify a sub-PDP when creating a dual
stack or IPv6-only subnetwork with external access using BYOIP:
   
   - 
   Full resource URL, as inhttps://www.googleapis.com/compute/v1/projects/projectId/regions/region/publicDelegatedPrefixes/sub-pdp-name
   - 
   Partial URL, as in
     
      
          - projects/projectId/regions/region/publicDelegatedPrefixes/sub-pdp-name
          - regions/region/publicDelegatedPrefixes/sub-pdp-name |
| `private_ip_google_access` | bool | Whether the VMs in this subnet can access Google services without assigned
external IP addresses. This field can be both set at resource creation
time and updated using setPrivateIpGoogleAccess. |
| `ip_cidr_range` | String | The range of internal addresses that are owned by this subnetwork.
Provide this property when you create the subnetwork. For example,10.0.0.0/8 or 100.64.0.0/10. Ranges must
be unique and non-overlapping within a network. Only IPv4 is supported.
This field is set at resource creation time. The range can be any range
listed in theValid
ranges list. The range can be expanded after creation usingexpandIpCidrRange. |
| `reserved_internal_range` | String | The URL of the reserved internal range. |
| `purpose` | String |  |
| `ipv6_gce_endpoint` | String | [Output Only] Possible endpoints of this subnetwork. It can be one of the
following:
   
   - VM_ONLY: The subnetwork can be used for creating instances and
   IPv6 addresses with VM endpoint type. Such a subnetwork gets external IPv6
   ranges from a public delegated prefix and cannot be used to create NetLb.
   - VM_AND_FR: The subnetwork can be used for creating both VM
   instances and Forwarding Rules. It can also be used to reserve IPv6
   addresses with both VM and FR endpoint types. Such a subnetwork gets its
   IPv6 range from Google IP Pool directly. |
| `secondary_ip_ranges` | Vec<String> | An array of configurations for secondary IP ranges for VM instances
contained in this subnetwork. The primary IP of such VM must belong to the
primary ipCidrRange of the subnetwork. The alias IPs may belong to either
primary or secondary ranges. This field can be updated with apatch request. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `gateway_address` | String | [Output Only] The gateway address for default routes to reach destination
addresses outside this subnetwork. |
| `kind` | String | [Output Only] Type of the resource. Always compute#subnetwork
for Subnetwork resources. |
| `internal_ipv6_prefix` | String | The internal IPv6 address range that is owned by this
subnetwork. |
| `system_reserved_internal_ipv6_ranges` | Vec<String> | Output only. [Output Only] The array of internal IPv6 network ranges reserved from
the subnetwork's internal IPv6 range for system use. |
| `ipv6_cidr_range` | String | [Output Only] This field is for internal use. |
| `name` | String | The name of the resource, provided by the client when initially creating
the resource. The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `region` | String | URL of the region where the Subnetwork resides. This
field can be set only at resource creation time. |
| `role` | String | The role of subnetwork. Currently, this field is only used when
purpose is set to GLOBAL_MANAGED_PROXY orREGIONAL_MANAGED_PROXY. The value can be set toACTIVE or BACKUP. An ACTIVE
subnetwork is one that is currently being used for Envoy-based load
balancers in a region. A BACKUP subnetwork is one that is
ready to be promoted to ACTIVE or is currently draining.
This field can be updated with a patch request. |
| `self_link_with_id` | String | [Output Only] Server-defined URL for this resource with the resource id. |
| `metadata` | String | Can only be specified if VPC flow logging for this subnetwork is enabled.
Configures whether metadata fields should be added to the reported VPC flow
logs. Options are INCLUDE_ALL_METADATA,EXCLUDE_ALL_METADATA, and CUSTOM_METADATA.
Default is EXCLUDE_ALL_METADATA. |
| `enable_flow_logs` | bool | Whether to enable flow logging for this subnetwork. If this field is not
explicitly set, it will not appear in get listings. If not set
the default behavior is determined by the org policy, if there is no org
policy specified, then it will default to disabled. This field isn't
supported if the subnet purpose field is set toREGIONAL_MANAGED_PROXY. |
| `private_ipv6_google_access` | String | This field is for internal use.

This field can be both set at resource creation time and updated usingpatch. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. This field can be set only at resource creation time. |
| `resolve_subnet_mask` | String | Configures subnet mask resolution for this subnetwork. |
| `system_reserved_external_ipv6_ranges` | Vec<String> | Output only. [Output Only] The array of external IPv6 network ranges reserved from
the subnetwork's external IPv6 range for system use. |
| `aggregation_interval` | String | Can only be specified if VPC flow logging for this subnetwork is enabled.
Sets the aggregation interval for collecting flow logs. Increasing the
interval time reduces the amount of generated flow logs for long-lasting
connections. Default is an interval of 5 seconds per connection.
Valid values: INTERVAL_5_SEC, INTERVAL_30_SEC,INTERVAL_1_MIN, INTERVAL_5_MIN,INTERVAL_10_MIN, INTERVAL_15_MIN. |
| `enable_private_v6_access` | bool | Deprecated in favor of enable in PrivateIpv6GoogleAccess.
Whether the VMs in this subnet can directly access Google services via
internal IPv6 addresses. This field can be both set at resource creation
time and updated using patch. |
| `utilization_details` | String | Output only. [Output Only] The current IP utilization of all subnetwork ranges. Contains
the total number of allocated and free IPs in each range. |
| `external_ipv6_prefix` | String | The external IPv6 address range that is owned by this
subnetwork. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create subnetwork
subnetwork = provider.compute_api.Subnetwork {
    project = "value"  # Project ID for this request.
    region = "value"  # Name of the region scoping this request.
}

# Access subnetwork outputs
subnetwork_id = subnetwork.id
subnetwork_params = subnetwork.params
subnetwork_creation_timestamp = subnetwork.creation_timestamp
subnetwork_stack_type = subnetwork.stack_type
subnetwork_vlans = subnetwork.vlans
subnetwork_log_config = subnetwork.log_config
subnetwork_id = subnetwork.id
subnetwork_network = subnetwork.network
subnetwork_fingerprint = subnetwork.fingerprint
subnetwork_ipv6_access_type = subnetwork.ipv6_access_type
subnetwork_enable_l2 = subnetwork.enable_l2
subnetwork_allow_subnet_cidr_routes_overlap = subnetwork.allow_subnet_cidr_routes_overlap
subnetwork_state = subnetwork.state
subnetwork_flow_sampling = subnetwork.flow_sampling
subnetwork_ip_collection = subnetwork.ip_collection
subnetwork_private_ip_google_access = subnetwork.private_ip_google_access
subnetwork_ip_cidr_range = subnetwork.ip_cidr_range
subnetwork_reserved_internal_range = subnetwork.reserved_internal_range
subnetwork_purpose = subnetwork.purpose
subnetwork_ipv6_gce_endpoint = subnetwork.ipv6_gce_endpoint
subnetwork_secondary_ip_ranges = subnetwork.secondary_ip_ranges
subnetwork_self_link = subnetwork.self_link
subnetwork_gateway_address = subnetwork.gateway_address
subnetwork_kind = subnetwork.kind
subnetwork_internal_ipv6_prefix = subnetwork.internal_ipv6_prefix
subnetwork_system_reserved_internal_ipv6_ranges = subnetwork.system_reserved_internal_ipv6_ranges
subnetwork_ipv6_cidr_range = subnetwork.ipv6_cidr_range
subnetwork_name = subnetwork.name
subnetwork_region = subnetwork.region
subnetwork_role = subnetwork.role
subnetwork_self_link_with_id = subnetwork.self_link_with_id
subnetwork_metadata = subnetwork.metadata
subnetwork_enable_flow_logs = subnetwork.enable_flow_logs
subnetwork_private_ipv6_google_access = subnetwork.private_ipv6_google_access
subnetwork_description = subnetwork.description
subnetwork_resolve_subnet_mask = subnetwork.resolve_subnet_mask
subnetwork_system_reserved_external_ipv6_ranges = subnetwork.system_reserved_external_ipv6_ranges
subnetwork_aggregation_interval = subnetwork.aggregation_interval
subnetwork_enable_private_v6_access = subnetwork.enable_private_v6_access
subnetwork_utilization_details = subnetwork.utilization_details
subnetwork_external_ipv6_prefix = subnetwork.external_ipv6_prefix
```

---


### Region_zone

Retrieves the list of Zone resources under the specific region available to
the specified project.

**Operations**: ✅ Read

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `items` | Vec<String> | A list of Zone resources. |
| `kind` | String | Type of resource. |
| `id` | String | [Output Only] Unique identifier for the resource; defined by the server. |
| `self_link` | String | [Output Only] Server-defined URL for this resource. |
| `warning` | String | [Output Only] Informational warning message. |
| `next_page_token` | String | [Output Only] This token allows you to get the next page of results for
list requests. If the number of results is larger thanmaxResults, use the nextPageToken as a value for
the query parameter pageToken in the next list request.
Subsequent list requests will have their own nextPageToken to
continue paging through the results. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Access region_zone outputs
region_zone_id = region_zone.id
region_zone_items = region_zone.items
region_zone_kind = region_zone.kind
region_zone_id = region_zone.id
region_zone_self_link = region_zone.self_link
region_zone_warning = region_zone.warning
region_zone_next_page_token = region_zone.next_page_token
```

---


### Instance

Creates an instance resource in the specified project using the data
included in the request.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `shielded_vm_config` | String |  | Deprecating, please use shielded_instance_config. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339 text format. |
| `hostname` | String |  | Specifies the hostname of the instance. The specified hostname must be
RFC1035 compliant. If hostname is not specified, the default hostname is
[INSTANCE_NAME].c.[PROJECT_ID].internal when using the global DNS, and
[INSTANCE_NAME].[ZONE].c.[PROJECT_ID].internal when using zonal DNS. |
| `resource_policies` | Vec<String> |  | Resource policies applied to this instance. |
| `resource_status` | String |  | [Output Only] Specifies values set for instance attributes as
compared to the values requested by user in the corresponding input only
field. |
| `kind` | String |  | [Output Only] Type of the resource. Always compute#instance
for instances. |
| `last_suspended_timestamp` | String |  | [Output Only] Last suspended timestamp inRFC3339 text format. |
| `source_machine_image_encryption_key` | String |  | Source machine image encryption key when creating an instance from a
machine image. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `advanced_machine_features` | String |  | Controls for advanced machine-related behavior features. |
| `metadata` | String |  | The metadata key/value pairs assigned
to this instance. This includes metadata keys that were explicitly defined
for the instance. |
| `preserved_state_size_gb` | String |  | Total amount of preserved state for SUSPENDED instances.
Read-only in the api. |
| `disks` | Vec<String> |  | Array of disks associated with this instance. Persistent disks must be
created before you can assign them. |
| `upcoming_maintenance` | String |  | [Output Only] DEPRECATED: Please useresource_status.upcoming_maintenance instead. Specifies
upcoming maintenance for the instance. |
| `display_device` | String |  | Enables display device for the instance. |
| `key_revocation_action_type` | String |  | KeyRevocationActionType of the instance. Supported options are "STOP" and
"NONE". The default value is "NONE" if it is not specified. |
| `name` | String |  | The name of the resource, provided by the client when initially creating
the resource. The resource name must be 1-63 characters long, and comply
withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must be
a dash, lowercase letter, or digit, except the last character, which cannot
be a dash. |
| `can_ip_forward` | bool |  | Allows this instance to send and receive packets with non-matching
destination or source IPs. This is required if you plan to use this
instance to forward routes. For more information, seeEnabling IP Forwarding. |
| `shielded_vm_integrity_policy` | String |  | Deprecating, please use shielded_instance_integrity_policy. |
| `status_message` | String |  | [Output Only] An optional, human-readable explanation of the status. |
| `zone` | String |  | [Output Only] URL of the zone where the instance resides.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `fingerprint` | String |  | Specifies a fingerprint for this resource, which is essentially a hash of
the instance's contents and used for optimistic locking. The
fingerprint is initially generated by Compute Engine and changes after
every request to modify or update the instance. You must always provide an
up-to-date fingerprint hash in order to update the instance.

To see the latest fingerprint, make get() request to the
instance. |
| `labels` | HashMap<String, String> |  | Labels to apply to this instance. These can be later modified by
the setLabels method. |
| `network_performance_config` | String |  |  |
| `self_link_with_id` | String |  | [Output Only] Server-defined URL for this resource with the resource id. |
| `guest_accelerators` | Vec<String> |  | A list of the type and count of accelerator cards attached to the instance. |
| `erase_windows_vss_signature` | bool |  | Specifies whether the disks restored from source snapshots or source
machine image should erase Windows specific VSS signature. |
| `shielded_instance_config` | String |  |  |
| `reservation_affinity` | String |  | Specifies the reservations that this instance can consume from. |
| `satisfies_pzs` | bool |  | [Output Only] Reserved for future use. |
| `partner_metadata` | HashMap<String, String> |  | Partner Metadata assigned to the instance. A map from a subdomain
(namespace) to entries map. |
| `params` | String |  | Input only. [Input Only] Additional params passed with the request, but not persisted
as part of resource payload. |
| `confidential_instance_config` | String |  |  |
| `start_restricted` | bool |  | [Output Only] Whether a VM has been restricted for start because Compute
Engine has detected suspicious activity. |
| `post_key_revocation_action_type` | String |  | PostKeyRevocationActionType of the instance. |
| `scheduling` | String |  | Sets the scheduling options for this instance. |
| `identity_certificate` | bool |  |  |
| `min_cpu_platform` | String |  | Specifies aminimum CPU
platform for the VM instance. Applicable values are the friendly names
of CPU platforms, such as minCpuPlatform: "Intel
Haswell" or minCpuPlatform: "Intel Sandy
Bridge". |
| `status` | String |  | [Output Only] The status of the instance. One of the
following values: PROVISIONING, STAGING,RUNNING, STOPPING, SUSPENDING,SUSPENDED, REPAIRING, andTERMINATED. For more information about the status of the
instance, see 
Instance life cycle. |
| `instance_encryption_key` | String |  | Encrypts suspended data for an instance with acustomer-managed
encryption key.

If you are creating a new instance, this field will encrypt the local SSD
and in-memory contents of the instance during the suspend operation.

If you do not provide an encryption key when creating the instance, then
the local SSD and in-memory contents will be encrypted using
an automatically generated key during the suspend operation. |
| `cpu_platform` | String |  | [Output Only] The CPU platform used by this instance. |
| `workload_identity_config` | String |  |  |
| `tags` | String |  | Tags to apply to this instance. Tags are used to identify valid
sources or targets for network firewalls and are specified by the client
during instance creation. The tags can be later modified by the setTags
method. Each tag within the list must comply withRFC1035.
Multiple tags can be specified via the 'tags.items' field. |
| `deletion_protection` | bool |  | Whether the resource should be protected against deletion. |
| `label_fingerprint` | String |  | A fingerprint for this request, which is essentially a hash of
the label's contents and used for optimistic locking. The
fingerprint is initially generated by Compute Engine and changes after
every request to modify or update labels. You must always provide an
up-to-date fingerprint hash in order to update or change labels.

To see the latest fingerprint, make get() request to the
instance. |
| `source_machine_image` | String |  | Source machine image |
| `shielded_instance_integrity_policy` | String |  |  |
| `self_link` | String |  | [Output Only] Server-defined URL for this resource. |
| `identity` | String |  |  |
| `machine_type` | String |  | Full or partial URL of the machine type resource to use for this instance,
in the format:zones/zone/machineTypes/machine-type. This is provided by the client
when the instance is created. For example, the following is a valid partial
url to a predefined
machine type:

zones/us-central1-f/machineTypes/n1-standard-1


To create acustom
machine type, provide a URL to a machine type in the following format,
where CPUS is 1 or an even number up to 32 (2,
4, 6, ... 24, etc), and MEMORY is the total
memory for this instance. Memory must be a multiple of 256 MB and must
be supplied in MB (e.g. 5 GB of memory is 5120 MB):

zones/zone/machineTypes/custom-CPUS-MEMORY


For example: zones/us-central1-f/machineTypes/custom-4-5120
For a full list of restrictions, read theSpecifications
for custom machine types. |
| `service_integration_specs` | HashMap<String, String> |  | Mapping of user-defined keys to specifications for service integrations.
Currently only a single key-value pair is supported. |
| `network_interfaces` | Vec<String> |  | An array of network configurations for this instance. These specify how
interfaces are configured to interact with other network services, such as
connecting to the internet. Multiple interfaces are supported
per instance. |
| `satisfies_pzi` | bool |  | [Output Only] Reserved for future use. |
| `last_start_timestamp` | String |  | [Output Only] Last start timestamp inRFC3339 text format. |
| `last_stop_timestamp` | String |  | [Output Only] Last stop timestamp inRFC3339 text format. |
| `private_ipv6_google_access` | String |  | The private IPv6 google access type for the VM.
If not specified, use  INHERIT_FROM_SUBNETWORK as default. |
| `secure_tags` | Vec<String> |  | [Input Only] Secure tags to apply to this instance. These can be later
modified by the update method.
Maximum number of secure tags allowed is 50. |
| `service_accounts` | Vec<String> |  | A list of service accounts, with their specified scopes, authorized for
this instance. Only one service account per VM instance is supported.

Service accounts generate access tokens that can be accessed
through the metadata server and used to authenticate applications on the
instance. SeeService Accounts
for more information. |
| `zone` | String | ✅ | The name of the zone for this request. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `shielded_vm_config` | String | Deprecating, please use shielded_instance_config. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339 text format. |
| `hostname` | String | Specifies the hostname of the instance. The specified hostname must be
RFC1035 compliant. If hostname is not specified, the default hostname is
[INSTANCE_NAME].c.[PROJECT_ID].internal when using the global DNS, and
[INSTANCE_NAME].[ZONE].c.[PROJECT_ID].internal when using zonal DNS. |
| `resource_policies` | Vec<String> | Resource policies applied to this instance. |
| `resource_status` | String | [Output Only] Specifies values set for instance attributes as
compared to the values requested by user in the corresponding input only
field. |
| `kind` | String | [Output Only] Type of the resource. Always compute#instance
for instances. |
| `last_suspended_timestamp` | String | [Output Only] Last suspended timestamp inRFC3339 text format. |
| `source_machine_image_encryption_key` | String | Source machine image encryption key when creating an instance from a
machine image. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `advanced_machine_features` | String | Controls for advanced machine-related behavior features. |
| `metadata` | String | The metadata key/value pairs assigned
to this instance. This includes metadata keys that were explicitly defined
for the instance. |
| `preserved_state_size_gb` | String | Total amount of preserved state for SUSPENDED instances.
Read-only in the api. |
| `disks` | Vec<String> | Array of disks associated with this instance. Persistent disks must be
created before you can assign them. |
| `upcoming_maintenance` | String | [Output Only] DEPRECATED: Please useresource_status.upcoming_maintenance instead. Specifies
upcoming maintenance for the instance. |
| `display_device` | String | Enables display device for the instance. |
| `key_revocation_action_type` | String | KeyRevocationActionType of the instance. Supported options are "STOP" and
"NONE". The default value is "NONE" if it is not specified. |
| `name` | String | The name of the resource, provided by the client when initially creating
the resource. The resource name must be 1-63 characters long, and comply
withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must be
a dash, lowercase letter, or digit, except the last character, which cannot
be a dash. |
| `can_ip_forward` | bool | Allows this instance to send and receive packets with non-matching
destination or source IPs. This is required if you plan to use this
instance to forward routes. For more information, seeEnabling IP Forwarding. |
| `shielded_vm_integrity_policy` | String | Deprecating, please use shielded_instance_integrity_policy. |
| `status_message` | String | [Output Only] An optional, human-readable explanation of the status. |
| `zone` | String | [Output Only] URL of the zone where the instance resides.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `fingerprint` | String | Specifies a fingerprint for this resource, which is essentially a hash of
the instance's contents and used for optimistic locking. The
fingerprint is initially generated by Compute Engine and changes after
every request to modify or update the instance. You must always provide an
up-to-date fingerprint hash in order to update the instance.

To see the latest fingerprint, make get() request to the
instance. |
| `labels` | HashMap<String, String> | Labels to apply to this instance. These can be later modified by
the setLabels method. |
| `network_performance_config` | String |  |
| `self_link_with_id` | String | [Output Only] Server-defined URL for this resource with the resource id. |
| `guest_accelerators` | Vec<String> | A list of the type and count of accelerator cards attached to the instance. |
| `erase_windows_vss_signature` | bool | Specifies whether the disks restored from source snapshots or source
machine image should erase Windows specific VSS signature. |
| `shielded_instance_config` | String |  |
| `reservation_affinity` | String | Specifies the reservations that this instance can consume from. |
| `satisfies_pzs` | bool | [Output Only] Reserved for future use. |
| `partner_metadata` | HashMap<String, String> | Partner Metadata assigned to the instance. A map from a subdomain
(namespace) to entries map. |
| `params` | String | Input only. [Input Only] Additional params passed with the request, but not persisted
as part of resource payload. |
| `confidential_instance_config` | String |  |
| `start_restricted` | bool | [Output Only] Whether a VM has been restricted for start because Compute
Engine has detected suspicious activity. |
| `post_key_revocation_action_type` | String | PostKeyRevocationActionType of the instance. |
| `scheduling` | String | Sets the scheduling options for this instance. |
| `identity_certificate` | bool |  |
| `min_cpu_platform` | String | Specifies aminimum CPU
platform for the VM instance. Applicable values are the friendly names
of CPU platforms, such as minCpuPlatform: "Intel
Haswell" or minCpuPlatform: "Intel Sandy
Bridge". |
| `status` | String | [Output Only] The status of the instance. One of the
following values: PROVISIONING, STAGING,RUNNING, STOPPING, SUSPENDING,SUSPENDED, REPAIRING, andTERMINATED. For more information about the status of the
instance, see 
Instance life cycle. |
| `instance_encryption_key` | String | Encrypts suspended data for an instance with acustomer-managed
encryption key.

If you are creating a new instance, this field will encrypt the local SSD
and in-memory contents of the instance during the suspend operation.

If you do not provide an encryption key when creating the instance, then
the local SSD and in-memory contents will be encrypted using
an automatically generated key during the suspend operation. |
| `cpu_platform` | String | [Output Only] The CPU platform used by this instance. |
| `workload_identity_config` | String |  |
| `tags` | String | Tags to apply to this instance. Tags are used to identify valid
sources or targets for network firewalls and are specified by the client
during instance creation. The tags can be later modified by the setTags
method. Each tag within the list must comply withRFC1035.
Multiple tags can be specified via the 'tags.items' field. |
| `deletion_protection` | bool | Whether the resource should be protected against deletion. |
| `label_fingerprint` | String | A fingerprint for this request, which is essentially a hash of
the label's contents and used for optimistic locking. The
fingerprint is initially generated by Compute Engine and changes after
every request to modify or update labels. You must always provide an
up-to-date fingerprint hash in order to update or change labels.

To see the latest fingerprint, make get() request to the
instance. |
| `source_machine_image` | String | Source machine image |
| `shielded_instance_integrity_policy` | String |  |
| `self_link` | String | [Output Only] Server-defined URL for this resource. |
| `identity` | String |  |
| `machine_type` | String | Full or partial URL of the machine type resource to use for this instance,
in the format:zones/zone/machineTypes/machine-type. This is provided by the client
when the instance is created. For example, the following is a valid partial
url to a predefined
machine type:

zones/us-central1-f/machineTypes/n1-standard-1


To create acustom
machine type, provide a URL to a machine type in the following format,
where CPUS is 1 or an even number up to 32 (2,
4, 6, ... 24, etc), and MEMORY is the total
memory for this instance. Memory must be a multiple of 256 MB and must
be supplied in MB (e.g. 5 GB of memory is 5120 MB):

zones/zone/machineTypes/custom-CPUS-MEMORY


For example: zones/us-central1-f/machineTypes/custom-4-5120
For a full list of restrictions, read theSpecifications
for custom machine types. |
| `service_integration_specs` | HashMap<String, String> | Mapping of user-defined keys to specifications for service integrations.
Currently only a single key-value pair is supported. |
| `network_interfaces` | Vec<String> | An array of network configurations for this instance. These specify how
interfaces are configured to interact with other network services, such as
connecting to the internet. Multiple interfaces are supported
per instance. |
| `satisfies_pzi` | bool | [Output Only] Reserved for future use. |
| `last_start_timestamp` | String | [Output Only] Last start timestamp inRFC3339 text format. |
| `last_stop_timestamp` | String | [Output Only] Last stop timestamp inRFC3339 text format. |
| `private_ipv6_google_access` | String | The private IPv6 google access type for the VM.
If not specified, use  INHERIT_FROM_SUBNETWORK as default. |
| `secure_tags` | Vec<String> | [Input Only] Secure tags to apply to this instance. These can be later
modified by the update method.
Maximum number of secure tags allowed is 50. |
| `service_accounts` | Vec<String> | A list of service accounts, with their specified scopes, authorized for
this instance. Only one service account per VM instance is supported.

Service accounts generate access tokens that can be accessed
through the metadata server and used to authenticate applications on the
instance. SeeService Accounts
for more information. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create instance
instance = provider.compute_api.Instance {
    zone = "value"  # The name of the zone for this request.
    project = "value"  # Project ID for this request.
}

# Access instance outputs
instance_id = instance.id
instance_shielded_vm_config = instance.shielded_vm_config
instance_creation_timestamp = instance.creation_timestamp
instance_hostname = instance.hostname
instance_resource_policies = instance.resource_policies
instance_resource_status = instance.resource_status
instance_kind = instance.kind
instance_last_suspended_timestamp = instance.last_suspended_timestamp
instance_source_machine_image_encryption_key = instance.source_machine_image_encryption_key
instance_id = instance.id
instance_advanced_machine_features = instance.advanced_machine_features
instance_metadata = instance.metadata
instance_preserved_state_size_gb = instance.preserved_state_size_gb
instance_disks = instance.disks
instance_upcoming_maintenance = instance.upcoming_maintenance
instance_display_device = instance.display_device
instance_key_revocation_action_type = instance.key_revocation_action_type
instance_name = instance.name
instance_can_ip_forward = instance.can_ip_forward
instance_shielded_vm_integrity_policy = instance.shielded_vm_integrity_policy
instance_status_message = instance.status_message
instance_zone = instance.zone
instance_description = instance.description
instance_fingerprint = instance.fingerprint
instance_labels = instance.labels
instance_network_performance_config = instance.network_performance_config
instance_self_link_with_id = instance.self_link_with_id
instance_guest_accelerators = instance.guest_accelerators
instance_erase_windows_vss_signature = instance.erase_windows_vss_signature
instance_shielded_instance_config = instance.shielded_instance_config
instance_reservation_affinity = instance.reservation_affinity
instance_satisfies_pzs = instance.satisfies_pzs
instance_partner_metadata = instance.partner_metadata
instance_params = instance.params
instance_confidential_instance_config = instance.confidential_instance_config
instance_start_restricted = instance.start_restricted
instance_post_key_revocation_action_type = instance.post_key_revocation_action_type
instance_scheduling = instance.scheduling
instance_identity_certificate = instance.identity_certificate
instance_min_cpu_platform = instance.min_cpu_platform
instance_status = instance.status
instance_instance_encryption_key = instance.instance_encryption_key
instance_cpu_platform = instance.cpu_platform
instance_workload_identity_config = instance.workload_identity_config
instance_tags = instance.tags
instance_deletion_protection = instance.deletion_protection
instance_label_fingerprint = instance.label_fingerprint
instance_source_machine_image = instance.source_machine_image
instance_shielded_instance_integrity_policy = instance.shielded_instance_integrity_policy
instance_self_link = instance.self_link
instance_identity = instance.identity
instance_machine_type = instance.machine_type
instance_service_integration_specs = instance.service_integration_specs
instance_network_interfaces = instance.network_interfaces
instance_satisfies_pzi = instance.satisfies_pzi
instance_last_start_timestamp = instance.last_start_timestamp
instance_last_stop_timestamp = instance.last_stop_timestamp
instance_private_ipv6_google_access = instance.private_ipv6_google_access
instance_secure_tags = instance.secure_tags
instance_service_accounts = instance.service_accounts
```

---


### Region_target_http_proxie

Creates a TargetHttpProxy resource in the specified project and region
using the data included in the request.

**Operations**: ✅ Create ✅ Read ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `kind` | String |  | [Output Only] Type of resource. Always compute#targetHttpProxy
for target HTTP proxies. |
| `http_keep_alive_timeout_sec` | i64 |  | Specifies how long to keep a connection open, after completing a response,
while there is no matching traffic (in seconds). If an HTTP keep-alive is
not specified, a default value (610 seconds) will be used.

For global external Application Load Balancers, the minimum allowed value
is 5 seconds and the maximum allowed value is 1200 seconds.

For classic Application Load Balancers, this option is not supported. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `fingerprint` | String |  | Fingerprint of this resource. A hash of the contents stored in this object.
This field is used in optimistic locking. This field will be ignored when
inserting a TargetHttpProxy. An up-to-date fingerprint must
be provided in order to patch/update the TargetHttpProxy; otherwise, the
request will fail with error 412 conditionNotMet. To see the
latest fingerprint, make a get() request to retrieve the
TargetHttpProxy. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `name` | String |  | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `proxy_bind` | bool |  | This field only applies when the forwarding rule that references this
target proxy has a loadBalancingScheme set toINTERNAL_SELF_MANAGED.

When this field is set to true, Envoy proxies set up inbound
traffic interception and bind to the IP address and port specified in the
forwarding rule. This is generally useful when using Traffic Director to
configure Envoy as a gateway or middle proxy (in other words, not a
sidecar proxy). The Envoy proxy listens for inbound requests and handles
requests when it receives them.

The default is false. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `http_filters` | Vec<String> |  | URLs to networkservices.HttpFilter resources
enabled for xDS clients using this configuration. For example,https://networkservices.googleapis.com/v1alpha1/projects/project/locations/locationhttpFilters/httpFilter
Only filters that handle outbound connection and
stream events may be specified. These filters work in conjunction with
a default set of HTTP filters that may already be configured by Traffic
Director. Traffic Director will determine the final location of these
filters within xDS configuration based on the name of the HTTP filter. If
Traffic Director positions multiple filters at the same location, those
filters will be in the same order as specified in this list. 

httpFilters only applies for loadbalancers withloadBalancingScheme set to INTERNAL_SELF_MANAGED.
See ForwardingRule for more details. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `self_link_with_id` | String |  | [Output Only] Server-defined URL for this resource with the resource id. |
| `url_map` | String |  | URL to the UrlMap resource that defines the mapping from URL to
the BackendService. |
| `region` | String |  | [Output Only] URL of the region where the regional Target HTTP Proxy
resides. This field is not applicable to global Target HTTP Proxies. |
| `project` | String | ✅ | Project ID for this request. |
| `region` | String | ✅ | Name of the region scoping this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `kind` | String | [Output Only] Type of resource. Always compute#targetHttpProxy
for target HTTP proxies. |
| `http_keep_alive_timeout_sec` | i64 | Specifies how long to keep a connection open, after completing a response,
while there is no matching traffic (in seconds). If an HTTP keep-alive is
not specified, a default value (610 seconds) will be used.

For global external Application Load Balancers, the minimum allowed value
is 5 seconds and the maximum allowed value is 1200 seconds.

For classic Application Load Balancers, this option is not supported. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `fingerprint` | String | Fingerprint of this resource. A hash of the contents stored in this object.
This field is used in optimistic locking. This field will be ignored when
inserting a TargetHttpProxy. An up-to-date fingerprint must
be provided in order to patch/update the TargetHttpProxy; otherwise, the
request will fail with error 412 conditionNotMet. To see the
latest fingerprint, make a get() request to retrieve the
TargetHttpProxy. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `name` | String | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `proxy_bind` | bool | This field only applies when the forwarding rule that references this
target proxy has a loadBalancingScheme set toINTERNAL_SELF_MANAGED.

When this field is set to true, Envoy proxies set up inbound
traffic interception and bind to the IP address and port specified in the
forwarding rule. This is generally useful when using Traffic Director to
configure Envoy as a gateway or middle proxy (in other words, not a
sidecar proxy). The Envoy proxy listens for inbound requests and handles
requests when it receives them.

The default is false. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `http_filters` | Vec<String> | URLs to networkservices.HttpFilter resources
enabled for xDS clients using this configuration. For example,https://networkservices.googleapis.com/v1alpha1/projects/project/locations/locationhttpFilters/httpFilter
Only filters that handle outbound connection and
stream events may be specified. These filters work in conjunction with
a default set of HTTP filters that may already be configured by Traffic
Director. Traffic Director will determine the final location of these
filters within xDS configuration based on the name of the HTTP filter. If
Traffic Director positions multiple filters at the same location, those
filters will be in the same order as specified in this list. 

httpFilters only applies for loadbalancers withloadBalancingScheme set to INTERNAL_SELF_MANAGED.
See ForwardingRule for more details. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `self_link_with_id` | String | [Output Only] Server-defined URL for this resource with the resource id. |
| `url_map` | String | URL to the UrlMap resource that defines the mapping from URL to
the BackendService. |
| `region` | String | [Output Only] URL of the region where the regional Target HTTP Proxy
resides. This field is not applicable to global Target HTTP Proxies. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create region_target_http_proxie
region_target_http_proxie = provider.compute_api.Region_target_http_proxie {
    project = "value"  # Project ID for this request.
    region = "value"  # Name of the region scoping this request.
}

# Access region_target_http_proxie outputs
region_target_http_proxie_id = region_target_http_proxie.id
region_target_http_proxie_kind = region_target_http_proxie.kind
region_target_http_proxie_http_keep_alive_timeout_sec = region_target_http_proxie.http_keep_alive_timeout_sec
region_target_http_proxie_id = region_target_http_proxie.id
region_target_http_proxie_fingerprint = region_target_http_proxie.fingerprint
region_target_http_proxie_description = region_target_http_proxie.description
region_target_http_proxie_name = region_target_http_proxie.name
region_target_http_proxie_proxy_bind = region_target_http_proxie.proxy_bind
region_target_http_proxie_self_link = region_target_http_proxie.self_link
region_target_http_proxie_http_filters = region_target_http_proxie.http_filters
region_target_http_proxie_creation_timestamp = region_target_http_proxie.creation_timestamp
region_target_http_proxie_self_link_with_id = region_target_http_proxie.self_link_with_id
region_target_http_proxie_url_map = region_target_http_proxie.url_map
region_target_http_proxie_region = region_target_http_proxie.region
```

---


### Disk_setting

Get Zonal Disk Settings.

**Operations**: ✅ Read ✅ Update

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `access_location` | String |  | Policy of which locations are allowed to create regional
snapshots. |
| `default_resource_policies` | HashMap<String, String> |  | An optional parameter for storing the default resource policies that
will be used for the Disks created in the given scope.

The Key is a string type, provided by customers to uniquely identify the
default Resource Policy entry.

The Value is a Default ResourcePolicyDetails Object used to represent the
detailed information of the Resource Policy entry. |
| `project` | String | ✅ | Project ID for this request. |
| `zone` | String | ✅ | The name of the zone for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `access_location` | String | Policy of which locations are allowed to create regional
snapshots. |
| `default_resource_policies` | HashMap<String, String> | An optional parameter for storing the default resource policies that
will be used for the Disks created in the given scope.

The Key is a string type, provided by customers to uniquely identify the
default Resource Policy entry.

The Value is a Default ResourcePolicyDetails Object used to represent the
detailed information of the Resource Policy entry. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Access disk_setting outputs
disk_setting_id = disk_setting.id
disk_setting_access_location = disk_setting.access_location
disk_setting_default_resource_policies = disk_setting.default_resource_policies
```

---


### Target_ssl_proxie

Creates a TargetSslProxy resource in the specified project using
the data included in the request.

**Operations**: ✅ Create ✅ Read ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `proxy_header` | String |  | Specifies the type of proxy header to append before sending data to the
backend, either NONE or PROXY_V1. The default
is NONE. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `service` | String |  | URL to the BackendService resource. |
| `name` | String |  | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `certificate_map` | String |  | URL of a certificate map that identifies a certificate map associated with
the given target proxy.
This field can only be set for global target proxies.
If set, sslCertificates will be ignored.

 Accepted format is//certificatemanager.googleapis.com/projects/{project}/locations/{location}/certificateMaps/{resourceName}. |
| `kind` | String |  | [Output Only] Type of the resource. Alwayscompute#targetSslProxy for target SSL proxies. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `ssl_certificates` | Vec<String> |  | URLs to SslCertificate resources that are used to
authenticate connections to Backends. At least one SSL certificate
must be specified. Currently, you may specify up to 15 SSL certificates.
sslCertificates do not apply when the load balancing scheme is set to
INTERNAL_SELF_MANAGED. |
| `ssl_policy` | String |  | URL of SslPolicy resource that will be associated with the TargetSslProxy
resource. If not set, the TargetSslProxy resource will not have any
SSL policy configured. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `proxy_header` | String | Specifies the type of proxy header to append before sending data to the
backend, either NONE or PROXY_V1. The default
is NONE. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `service` | String | URL to the BackendService resource. |
| `name` | String | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `certificate_map` | String | URL of a certificate map that identifies a certificate map associated with
the given target proxy.
This field can only be set for global target proxies.
If set, sslCertificates will be ignored.

 Accepted format is//certificatemanager.googleapis.com/projects/{project}/locations/{location}/certificateMaps/{resourceName}. |
| `kind` | String | [Output Only] Type of the resource. Alwayscompute#targetSslProxy for target SSL proxies. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `ssl_certificates` | Vec<String> | URLs to SslCertificate resources that are used to
authenticate connections to Backends. At least one SSL certificate
must be specified. Currently, you may specify up to 15 SSL certificates.
sslCertificates do not apply when the load balancing scheme is set to
INTERNAL_SELF_MANAGED. |
| `ssl_policy` | String | URL of SslPolicy resource that will be associated with the TargetSslProxy
resource. If not set, the TargetSslProxy resource will not have any
SSL policy configured. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create target_ssl_proxie
target_ssl_proxie = provider.compute_api.Target_ssl_proxie {
    project = "value"  # Project ID for this request.
}

# Access target_ssl_proxie outputs
target_ssl_proxie_id = target_ssl_proxie.id
target_ssl_proxie_proxy_header = target_ssl_proxie.proxy_header
target_ssl_proxie_creation_timestamp = target_ssl_proxie.creation_timestamp
target_ssl_proxie_self_link = target_ssl_proxie.self_link
target_ssl_proxie_service = target_ssl_proxie.service
target_ssl_proxie_name = target_ssl_proxie.name
target_ssl_proxie_certificate_map = target_ssl_proxie.certificate_map
target_ssl_proxie_kind = target_ssl_proxie.kind
target_ssl_proxie_id = target_ssl_proxie.id
target_ssl_proxie_description = target_ssl_proxie.description
target_ssl_proxie_ssl_certificates = target_ssl_proxie.ssl_certificates
target_ssl_proxie_ssl_policy = target_ssl_proxie.ssl_policy
```

---


### Reservation

Creates a new reservation. For more information, readReserving zonal
resources.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `linked_commitments` | Vec<String> |  | [Output Only] Full or partial URL to parent commitments. This field
displays for reservations that are tied to multiple commitments. |
| `advanced_deployment_control` | String |  | Advanced control for cluster management, applicable only to DENSE
deployment type reservations. |
| `protection_tier` | String |  | Protection tier for the workload which specifies the workload expectations
in the event of infrastructure failures at data center (e.g. power
and/or cooling failures). |
| `reservation_sharing_policy` | String |  | Specify the reservation sharing policy. If unspecified, the reservation
will not be shared with Google Cloud managed services. |
| `resource_policies` | HashMap<String, String> |  | Resource policies to be added to this reservation. The key is defined by
user, and the value is resource policy url. This is to define placement
policy with reservation. |
| `scheduling_type` | String |  | The type of maintenance for the reservation. |
| `share_settings` | String |  | Specify share-settings to create a shared reservation. This property is
optional. For more information about the syntax and options for this
field and its subfields, see the guide for creating
a shared reservation. |
| `self_link` | String |  | [Output Only] Server-defined fully-qualified URL for this resource. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `specific_reservation` | String |  | Reservation for instances with specific machine shapes. |
| `status` | String |  | [Output Only] The status of the reservation.
   
   
     - CREATING: Reservation resources are being
       allocated.
     - READY: Reservation resources have been allocated,
       and the reservation is ready for use.
     - DELETING: Reservation deletion is in progress.
     - UPDATING: Reservation update is in progress. |
| `delete_after_duration` | String |  | Duration time relative to reservation creation when Compute Engine will
automatically delete this resource. |
| `deployment_type` | String |  | Specifies the deployment strategy for this reservation. |
| `delete_at_time` | String |  | Absolute time in future when the reservation will be
 auto-deleted by Compute Engine. Timestamp is represented inRFC3339 text format. |
| `aggregate_reservation` | String |  | Reservation for aggregated resources, providing shape flexibility. |
| `commitment` | String |  | [Output Only] Full or partial URL to a parent commitment. This field
displays for reservations that are tied to a commitment. |
| `name` | String |  | The name of the resource, provided by the client when initially creating
the resource. The resource name must be 1-63 characters long, and comply
withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must be
a dash, lowercase letter, or digit, except the last character, which cannot
be a dash. |
| `resource_status` | String |  | [Output Only] Status information for Reservation resource. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `satisfies_pzs` | bool |  | [Output Only] Reserved for future use. |
| `self_link_with_id` | String |  | [Output Only] Server-defined URL for this resource with the resource id. |
| `enable_emergent_maintenance` | bool |  | Indicates whether Compute Engine allows unplanned maintenance for your VMs;
for example, to fix hardware errors. |
| `reservation_mode` | String |  | [Output only] Indicates the reservation mode of the reservation. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `kind` | String |  | [Output Only] Type of the resource. Alwayscompute#reservations for reservations. |
| `specific_reservation_required` | bool |  | Indicates whether the reservation can be consumed by VMs with affinity
for "any" reservation. If the field is set, then only VMs that target
the reservation by name can consume from this reservation. |
| `zone` | String |  | Zone in which the reservation resides. A zone must be provided if the
reservation is created within a commitment. |
| `project` | String | ✅ | Project ID for this request. |
| `zone` | String | ✅ | Name of the zone for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `linked_commitments` | Vec<String> | [Output Only] Full or partial URL to parent commitments. This field
displays for reservations that are tied to multiple commitments. |
| `advanced_deployment_control` | String | Advanced control for cluster management, applicable only to DENSE
deployment type reservations. |
| `protection_tier` | String | Protection tier for the workload which specifies the workload expectations
in the event of infrastructure failures at data center (e.g. power
and/or cooling failures). |
| `reservation_sharing_policy` | String | Specify the reservation sharing policy. If unspecified, the reservation
will not be shared with Google Cloud managed services. |
| `resource_policies` | HashMap<String, String> | Resource policies to be added to this reservation. The key is defined by
user, and the value is resource policy url. This is to define placement
policy with reservation. |
| `scheduling_type` | String | The type of maintenance for the reservation. |
| `share_settings` | String | Specify share-settings to create a shared reservation. This property is
optional. For more information about the syntax and options for this
field and its subfields, see the guide for creating
a shared reservation. |
| `self_link` | String | [Output Only] Server-defined fully-qualified URL for this resource. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `specific_reservation` | String | Reservation for instances with specific machine shapes. |
| `status` | String | [Output Only] The status of the reservation.
   
   
     - CREATING: Reservation resources are being
       allocated.
     - READY: Reservation resources have been allocated,
       and the reservation is ready for use.
     - DELETING: Reservation deletion is in progress.
     - UPDATING: Reservation update is in progress. |
| `delete_after_duration` | String | Duration time relative to reservation creation when Compute Engine will
automatically delete this resource. |
| `deployment_type` | String | Specifies the deployment strategy for this reservation. |
| `delete_at_time` | String | Absolute time in future when the reservation will be
 auto-deleted by Compute Engine. Timestamp is represented inRFC3339 text format. |
| `aggregate_reservation` | String | Reservation for aggregated resources, providing shape flexibility. |
| `commitment` | String | [Output Only] Full or partial URL to a parent commitment. This field
displays for reservations that are tied to a commitment. |
| `name` | String | The name of the resource, provided by the client when initially creating
the resource. The resource name must be 1-63 characters long, and comply
withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must be
a dash, lowercase letter, or digit, except the last character, which cannot
be a dash. |
| `resource_status` | String | [Output Only] Status information for Reservation resource. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `satisfies_pzs` | bool | [Output Only] Reserved for future use. |
| `self_link_with_id` | String | [Output Only] Server-defined URL for this resource with the resource id. |
| `enable_emergent_maintenance` | bool | Indicates whether Compute Engine allows unplanned maintenance for your VMs;
for example, to fix hardware errors. |
| `reservation_mode` | String | [Output only] Indicates the reservation mode of the reservation. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `kind` | String | [Output Only] Type of the resource. Alwayscompute#reservations for reservations. |
| `specific_reservation_required` | bool | Indicates whether the reservation can be consumed by VMs with affinity
for "any" reservation. If the field is set, then only VMs that target
the reservation by name can consume from this reservation. |
| `zone` | String | Zone in which the reservation resides. A zone must be provided if the
reservation is created within a commitment. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create reservation
reservation = provider.compute_api.Reservation {
    project = "value"  # Project ID for this request.
    zone = "value"  # Name of the zone for this request.
}

# Access reservation outputs
reservation_id = reservation.id
reservation_linked_commitments = reservation.linked_commitments
reservation_advanced_deployment_control = reservation.advanced_deployment_control
reservation_protection_tier = reservation.protection_tier
reservation_reservation_sharing_policy = reservation.reservation_sharing_policy
reservation_resource_policies = reservation.resource_policies
reservation_scheduling_type = reservation.scheduling_type
reservation_share_settings = reservation.share_settings
reservation_self_link = reservation.self_link
reservation_creation_timestamp = reservation.creation_timestamp
reservation_specific_reservation = reservation.specific_reservation
reservation_status = reservation.status
reservation_delete_after_duration = reservation.delete_after_duration
reservation_deployment_type = reservation.deployment_type
reservation_delete_at_time = reservation.delete_at_time
reservation_aggregate_reservation = reservation.aggregate_reservation
reservation_commitment = reservation.commitment
reservation_name = reservation.name
reservation_resource_status = reservation.resource_status
reservation_description = reservation.description
reservation_satisfies_pzs = reservation.satisfies_pzs
reservation_self_link_with_id = reservation.self_link_with_id
reservation_enable_emergent_maintenance = reservation.enable_emergent_maintenance
reservation_reservation_mode = reservation.reservation_mode
reservation_id = reservation.id
reservation_kind = reservation.kind
reservation_specific_reservation_required = reservation.specific_reservation_required
reservation_zone = reservation.zone
```

---


### Addresse

Creates an address resource in the specified project by using the data
included in the request.

**Operations**: ✅ Create ✅ Read ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `labels` | HashMap<String, String> |  | Labels for this resource. These can only be added or modified by thesetLabels method. Each label key/value pair must comply withRFC1035.
Label values may be empty. |
| `address` | String |  | The static IP address represented by this resource. |
| `ipv6_endpoint_type` | String |  | The endpoint type of this address, which should be VM
or NETLB. This is used for deciding which type of endpoint
this address can be used after the external IPv6 address reservation. |
| `network` | String |  | The URL of the network in which to reserve the address. This field can
only be used with INTERNAL type with theVPC_PEERING purpose. |
| `users` | Vec<String> |  | [Output Only] The URLs of the resources that are using this address. |
| `label_fingerprint` | String |  | A fingerprint for the labels being applied to this Address, which is
essentially a hash of the labels set used for optimistic locking. The
fingerprint is initially generated by Compute Engine and changes after
every request to modify or update labels. You must always provide an
up-to-date fingerprint hash in order to update or change labels,
otherwise the request will fail with error412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve an Address. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `address_type` | String |  | The type of address to reserve, either INTERNAL orEXTERNAL. If unspecified, defaults to EXTERNAL. |
| `region` | String |  | [Output Only] The URL of the region where a regional address resides.
For regional addresses, you must specify the region as a path parameter in
the HTTP request URL. *This field is not applicable to global
addresses.* |
| `ip_version` | String |  | The IP version that will be used by this address. Valid options areIPV4 or IPV6. |
| `self_link_with_id` | String |  | [Output Only] Server-defined URL for this resource with the resource id. |
| `name` | String |  | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?`. The first character
must be a lowercase letter, and all following characters (except for the
last character) must be a dash, lowercase letter, or digit. The last
character must be a lowercase letter or digit. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `network_tier` | String |  | This signifies the networking tier used for configuring this address and
can only take the following values: PREMIUM orSTANDARD. Internal IP addresses are always Premium Tier;
global external IP addresses are always Premium Tier; regional external IP
addresses can be either Standard or Premium Tier.

If this field is not specified, it is assumed to be PREMIUM. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `kind` | String |  | [Output Only] Type of the resource. Always compute#address for
addresses. |
| `prefix_length` | i64 |  | The prefix length if the resource represents an IP range. |
| `status` | String |  | [Output Only] The status of the address, which can be one ofRESERVING, RESERVED, or IN_USE.
An address that is RESERVING is currently in the process of
being reserved. A RESERVED address is currently reserved and
available to use. An IN_USE address is currently being used
by another resource and is not available. |
| `subnetwork` | String |  | The URL of the subnetwork in which to reserve the address. If an IP address
is specified, it must be within the subnetwork's IP range. This field can
only be used with INTERNAL type with aGCE_ENDPOINT or DNS_RESOLVER purpose. |
| `ip_collection` | String |  | Reference to the source of external IPv4 addresses,
like a PublicDelegatedPrefix (PDP) for BYOIP.
The PDP must support enhanced IPv4 allocations.

Use one of the following formats to specify a PDP when reserving an
external IPv4 address using BYOIP.
   
   - 
   Full resource URL, as inhttps://www.googleapis.com/compute/v1/projects/projectId/regions/region/publicDelegatedPrefixes/pdp-name
   - 
   Partial URL, as in
     
      
          - projects/projectId/regions/region/publicDelegatedPrefixes/pdp-name
          - regions/region/publicDelegatedPrefixes/pdp-name |
| `description` | String |  | An optional description of this resource. Provide this field when you
create the resource. |
| `purpose` | String |  | The purpose of this resource, which can be one of the following values:
   
   
     - GCE_ENDPOINT for addresses that are used by VM
     instances, alias IP ranges, load balancers, and similar resources.
     - DNS_RESOLVER for a DNS resolver address in a subnetwork
       for a Cloud DNS  inbound
       forwarder IP addresses (regional internal IP address in a subnet of
       a VPC network)
     - VPC_PEERING for global internal IP addresses used for
      
          private services access allocated ranges.
     - NAT_AUTO for the regional external IP addresses used by
          Cloud NAT when allocating addresses using
          
          automatic NAT IP address allocation.
     - IPSEC_INTERCONNECT for addresses created from a private
     IP range that are reserved for a VLAN attachment in an
     *HA VPN over Cloud Interconnect* configuration. These addresses
     are regional resources.
     - `SHARED_LOADBALANCER_VIP` for an internal IP address that is assigned
     to multiple internal forwarding rules.
     - `PRIVATE_SERVICE_CONNECT` for a private network address that is
     used to configure Private Service Connect. Only global internal addresses
     can use this purpose. |
| `region` | String | ✅ | Name of the region for this request. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `labels` | HashMap<String, String> | Labels for this resource. These can only be added or modified by thesetLabels method. Each label key/value pair must comply withRFC1035.
Label values may be empty. |
| `address` | String | The static IP address represented by this resource. |
| `ipv6_endpoint_type` | String | The endpoint type of this address, which should be VM
or NETLB. This is used for deciding which type of endpoint
this address can be used after the external IPv6 address reservation. |
| `network` | String | The URL of the network in which to reserve the address. This field can
only be used with INTERNAL type with theVPC_PEERING purpose. |
| `users` | Vec<String> | [Output Only] The URLs of the resources that are using this address. |
| `label_fingerprint` | String | A fingerprint for the labels being applied to this Address, which is
essentially a hash of the labels set used for optimistic locking. The
fingerprint is initially generated by Compute Engine and changes after
every request to modify or update labels. You must always provide an
up-to-date fingerprint hash in order to update or change labels,
otherwise the request will fail with error412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve an Address. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `address_type` | String | The type of address to reserve, either INTERNAL orEXTERNAL. If unspecified, defaults to EXTERNAL. |
| `region` | String | [Output Only] The URL of the region where a regional address resides.
For regional addresses, you must specify the region as a path parameter in
the HTTP request URL. *This field is not applicable to global
addresses.* |
| `ip_version` | String | The IP version that will be used by this address. Valid options areIPV4 or IPV6. |
| `self_link_with_id` | String | [Output Only] Server-defined URL for this resource with the resource id. |
| `name` | String | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?`. The first character
must be a lowercase letter, and all following characters (except for the
last character) must be a dash, lowercase letter, or digit. The last
character must be a lowercase letter or digit. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `network_tier` | String | This signifies the networking tier used for configuring this address and
can only take the following values: PREMIUM orSTANDARD. Internal IP addresses are always Premium Tier;
global external IP addresses are always Premium Tier; regional external IP
addresses can be either Standard or Premium Tier.

If this field is not specified, it is assumed to be PREMIUM. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `kind` | String | [Output Only] Type of the resource. Always compute#address for
addresses. |
| `prefix_length` | i64 | The prefix length if the resource represents an IP range. |
| `status` | String | [Output Only] The status of the address, which can be one ofRESERVING, RESERVED, or IN_USE.
An address that is RESERVING is currently in the process of
being reserved. A RESERVED address is currently reserved and
available to use. An IN_USE address is currently being used
by another resource and is not available. |
| `subnetwork` | String | The URL of the subnetwork in which to reserve the address. If an IP address
is specified, it must be within the subnetwork's IP range. This field can
only be used with INTERNAL type with aGCE_ENDPOINT or DNS_RESOLVER purpose. |
| `ip_collection` | String | Reference to the source of external IPv4 addresses,
like a PublicDelegatedPrefix (PDP) for BYOIP.
The PDP must support enhanced IPv4 allocations.

Use one of the following formats to specify a PDP when reserving an
external IPv4 address using BYOIP.
   
   - 
   Full resource URL, as inhttps://www.googleapis.com/compute/v1/projects/projectId/regions/region/publicDelegatedPrefixes/pdp-name
   - 
   Partial URL, as in
     
      
          - projects/projectId/regions/region/publicDelegatedPrefixes/pdp-name
          - regions/region/publicDelegatedPrefixes/pdp-name |
| `description` | String | An optional description of this resource. Provide this field when you
create the resource. |
| `purpose` | String | The purpose of this resource, which can be one of the following values:
   
   
     - GCE_ENDPOINT for addresses that are used by VM
     instances, alias IP ranges, load balancers, and similar resources.
     - DNS_RESOLVER for a DNS resolver address in a subnetwork
       for a Cloud DNS  inbound
       forwarder IP addresses (regional internal IP address in a subnet of
       a VPC network)
     - VPC_PEERING for global internal IP addresses used for
      
          private services access allocated ranges.
     - NAT_AUTO for the regional external IP addresses used by
          Cloud NAT when allocating addresses using
          
          automatic NAT IP address allocation.
     - IPSEC_INTERCONNECT for addresses created from a private
     IP range that are reserved for a VLAN attachment in an
     *HA VPN over Cloud Interconnect* configuration. These addresses
     are regional resources.
     - `SHARED_LOADBALANCER_VIP` for an internal IP address that is assigned
     to multiple internal forwarding rules.
     - `PRIVATE_SERVICE_CONNECT` for a private network address that is
     used to configure Private Service Connect. Only global internal addresses
     can use this purpose. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create addresse
addresse = provider.compute_api.Addresse {
    region = "value"  # Name of the region for this request.
    project = "value"  # Project ID for this request.
}

# Access addresse outputs
addresse_id = addresse.id
addresse_labels = addresse.labels
addresse_address = addresse.address
addresse_ipv6_endpoint_type = addresse.ipv6_endpoint_type
addresse_network = addresse.network
addresse_users = addresse.users
addresse_label_fingerprint = addresse.label_fingerprint
addresse_self_link = addresse.self_link
addresse_address_type = addresse.address_type
addresse_region = addresse.region
addresse_ip_version = addresse.ip_version
addresse_self_link_with_id = addresse.self_link_with_id
addresse_name = addresse.name
addresse_id = addresse.id
addresse_network_tier = addresse.network_tier
addresse_creation_timestamp = addresse.creation_timestamp
addresse_kind = addresse.kind
addresse_prefix_length = addresse.prefix_length
addresse_status = addresse.status
addresse_subnetwork = addresse.subnetwork
addresse_ip_collection = addresse.ip_collection
addresse_description = addresse.description
addresse_purpose = addresse.purpose
```

---


### Region

Returns the specified Region resource.

To decrease latency for this method, you can optionally omit any unneeded
information from the response by using a field mask. This practice is
especially recommended for unused quota information (the `quotas` field).
To exclude one or more fields, set your request's `fields` query parameter
to only include the fields you need. For example, to only include the `id`
and `selfLink` fields, add the query parameter `?fields=id,selfLink` to
your request.

This method fails if the quota information is unavailable for the region
and if the organization policy constraint
compute.requireBasicQuotaInResponse is enforced. This
constraint, when enforced, disables the fail-open behaviour when quota
information (the `items.quotas` field) is unavailable for the region.
It is recommended to use the default setting
for the constraint unless your application requires the fail-closed
behaviour for this method.

**Operations**: ✅ Read

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `deprecated` | String | [Output Only] The deprecation status associated with this region. |
| `name` | String | [Output Only] Name of the resource. |
| `description` | String | [Output Only] Textual description of the resource. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `self_link_with_id` | String | [Output Only] Server-defined URL for this resource with the resource id. |
| `status` | String | [Output Only] Status of the region, either UP orDOWN. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `quotas` | Vec<String> | [Output Only] Quotas assigned to this region. |
| `zones` | Vec<String> | [Output Only] A list of zones available in this region, in the form of
resource URLs. |
| `quota_status_warning` | String | [Output Only] Warning of fetching the `quotas` field for this region. This
field is populated only if fetching of the `quotas` field fails. |
| `supports_pzs` | bool | [Output Only] Reserved for future use. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `kind` | String | [Output Only] Type of the resource. Always compute#region for
regions. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Access region outputs
region_id = region.id
region_deprecated = region.deprecated
region_name = region.name
region_description = region.description
region_creation_timestamp = region.creation_timestamp
region_self_link_with_id = region.self_link_with_id
region_status = region.status
region_id = region.id
region_quotas = region.quotas
region_zones = region.zones
region_quota_status_warning = region.quota_status_warning
region_supports_pzs = region.supports_pzs
region_self_link = region.self_link
region_kind = region.kind
```

---


### Network

Creates a network in the specified project using the data included
in the request.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `i_pv4_range` | String |  | Deprecated in favor of subnet mode networks.
The range of internal addresses that are legal on this network. This
range is aCIDR specification, for example:192.168.0.0/16. Provided by the client when the network is
created. |
| `routing_config` | String |  | The network-level routing configuration for this network.  Used by Cloud
Router to determine what type of network-wide routing behavior to enforce. |
| `auto_create_subnetworks` | bool |  | Must be set to create a VPC network. If not set, a legacy network is
created.

When set to true, the VPC network is created in auto mode.
When set to false, the VPC network is created in custom mode.

An auto mode VPC network starts with one subnet per region. Each subnet
has a predetermined range as described inAuto mode VPC network IP ranges.

For custom mode VPC networks, you can add subnets using the subnetworksinsert
method. |
| `mtu` | i64 |  | Maximum Transmission Unit in bytes.
The minimum value for this field is 1300 and the maximum value is 8896.
The suggested value is 1500, which is the default MTU used on the
Internet, or 8896 if you want to use Jumbo frames. If unspecified, the
value defaults to 1460. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `region` | String |  | [Output Only] URL of the region where the regional network resides.
This field is not applicable to global network.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `description` | String |  | An optional description of this resource. Provide this field when you
create the resource. |
| `firewall_policy` | String |  | [Output Only] URL of the firewall policy the network is associated with. |
| `peerings` | Vec<String> |  | [Output Only] A list of network peerings for the resource. |
| `kind` | String |  | [Output Only] Type of the resource. Always compute#network for
networks. |
| `gateway_i_pv4` | String |  | [Output Only] The gateway address for default routing out of the network,
selected by Google Cloud. |
| `subnetworks` | Vec<String> |  | [Output Only] Server-defined fully-qualified URLs for all subnetworks
in this VPC network. |
| `params` | String |  | Input only. [Input Only] Additional params passed with the request, but not persisted
as part of resource payload. |
| `self_link_with_id` | String |  | [Output Only] Server-defined URL for this resource with the resource id. |
| `enable_ula_internal_ipv6` | bool |  | Enable ULA internal ipv6 on this network. Enabling this feature will assign
a /48 from google defined ULA prefix fd20::/20.
. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `name` | String |  | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?`. The first character must be a
lowercase letter, and all following characters (except for the last
character) must be a dash, lowercase letter, or digit. The last character
must be a lowercase letter or digit. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `internal_ipv6_range` | String |  | When enabling ula internal ipv6, caller optionally can specify the /48
range they want from the google defined ULA prefix fd20::/20. The input
must be a valid /48 ULA IPv6 address and must be within the fd20::/20.
Operation will fail if the speficied /48 is already in used by another
resource. If the field is not speficied, then a /48 range will be randomly
allocated from fd20::/20 and returned via this field.
. |
| `network_profile` | String |  | A full or partial URL of the network profile to apply to this network.
This field can be set only at resource creation time. For example, the
following are valid URLs: 
   
   - https://www.googleapis.com/compute/{api_version}/projects/{project_id}/global/networkProfiles/{network_profile_name}
   - projects/{project_id}/global/networkProfiles/{network_profile_name} |
| `network_firewall_policy_enforcement_order` | String |  | The network firewall policy enforcement order. Can be either
AFTER_CLASSIC_FIREWALL or BEFORE_CLASSIC_FIREWALL. Defaults to
AFTER_CLASSIC_FIREWALL if the field is not specified. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `i_pv4_range` | String | Deprecated in favor of subnet mode networks.
The range of internal addresses that are legal on this network. This
range is aCIDR specification, for example:192.168.0.0/16. Provided by the client when the network is
created. |
| `routing_config` | String | The network-level routing configuration for this network.  Used by Cloud
Router to determine what type of network-wide routing behavior to enforce. |
| `auto_create_subnetworks` | bool | Must be set to create a VPC network. If not set, a legacy network is
created.

When set to true, the VPC network is created in auto mode.
When set to false, the VPC network is created in custom mode.

An auto mode VPC network starts with one subnet per region. Each subnet
has a predetermined range as described inAuto mode VPC network IP ranges.

For custom mode VPC networks, you can add subnets using the subnetworksinsert
method. |
| `mtu` | i64 | Maximum Transmission Unit in bytes.
The minimum value for this field is 1300 and the maximum value is 8896.
The suggested value is 1500, which is the default MTU used on the
Internet, or 8896 if you want to use Jumbo frames. If unspecified, the
value defaults to 1460. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `region` | String | [Output Only] URL of the region where the regional network resides.
This field is not applicable to global network.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `description` | String | An optional description of this resource. Provide this field when you
create the resource. |
| `firewall_policy` | String | [Output Only] URL of the firewall policy the network is associated with. |
| `peerings` | Vec<String> | [Output Only] A list of network peerings for the resource. |
| `kind` | String | [Output Only] Type of the resource. Always compute#network for
networks. |
| `gateway_i_pv4` | String | [Output Only] The gateway address for default routing out of the network,
selected by Google Cloud. |
| `subnetworks` | Vec<String> | [Output Only] Server-defined fully-qualified URLs for all subnetworks
in this VPC network. |
| `params` | String | Input only. [Input Only] Additional params passed with the request, but not persisted
as part of resource payload. |
| `self_link_with_id` | String | [Output Only] Server-defined URL for this resource with the resource id. |
| `enable_ula_internal_ipv6` | bool | Enable ULA internal ipv6 on this network. Enabling this feature will assign
a /48 from google defined ULA prefix fd20::/20.
. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `name` | String | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?`. The first character must be a
lowercase letter, and all following characters (except for the last
character) must be a dash, lowercase letter, or digit. The last character
must be a lowercase letter or digit. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `internal_ipv6_range` | String | When enabling ula internal ipv6, caller optionally can specify the /48
range they want from the google defined ULA prefix fd20::/20. The input
must be a valid /48 ULA IPv6 address and must be within the fd20::/20.
Operation will fail if the speficied /48 is already in used by another
resource. If the field is not speficied, then a /48 range will be randomly
allocated from fd20::/20 and returned via this field.
. |
| `network_profile` | String | A full or partial URL of the network profile to apply to this network.
This field can be set only at resource creation time. For example, the
following are valid URLs: 
   
   - https://www.googleapis.com/compute/{api_version}/projects/{project_id}/global/networkProfiles/{network_profile_name}
   - projects/{project_id}/global/networkProfiles/{network_profile_name} |
| `network_firewall_policy_enforcement_order` | String | The network firewall policy enforcement order. Can be either
AFTER_CLASSIC_FIREWALL or BEFORE_CLASSIC_FIREWALL. Defaults to
AFTER_CLASSIC_FIREWALL if the field is not specified. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create network
network = provider.compute_api.Network {
    project = "value"  # Project ID for this request.
}

# Access network outputs
network_id = network.id
network_i_pv4_range = network.i_pv4_range
network_routing_config = network.routing_config
network_auto_create_subnetworks = network.auto_create_subnetworks
network_mtu = network.mtu
network_creation_timestamp = network.creation_timestamp
network_region = network.region
network_description = network.description
network_firewall_policy = network.firewall_policy
network_peerings = network.peerings
network_kind = network.kind
network_gateway_i_pv4 = network.gateway_i_pv4
network_subnetworks = network.subnetworks
network_params = network.params
network_self_link_with_id = network.self_link_with_id
network_enable_ula_internal_ipv6 = network.enable_ula_internal_ipv6
network_id = network.id
network_name = network.name
network_self_link = network.self_link
network_internal_ipv6_range = network.internal_ipv6_range
network_network_profile = network.network_profile
network_network_firewall_policy_enforcement_order = network.network_firewall_policy_enforcement_order
```

---


### Instance_group_manager

Creates a managed instance group using the information that you specify
in the request. After the group is created, instances in the group are
created using the specified instance template.
This operation is marked as DONE when the group is created
even if the instances in the group have not yet been created. You
must separately verify the status of the individual instances with thelistmanagedinstances
method.

A managed instance group can have up to 1000 VM instances per group. Please
contact Cloud Support if you need an increase in
this limit.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `target_suspended_size` | i64 |  | The target number of suspended instances for this managed instance group.
This number changes when you: 
   
   - Suspend instance using the suspendInstances
   method or resume instances using the resumeInstances
   method.
   - Manually change the targetSuspendedSize using the update
   method. |
| `distribution_policy` | String |  | Policy specifying the intended distribution of managed instances across
zones in a regional managed instance group. |
| `fingerprint` | String |  | Fingerprint of this resource. This field may be used in optimistic locking.
It will be ignored when inserting an InstanceGroupManager. An up-to-date
fingerprint must be provided in order to update the InstanceGroupManager,
otherwise the request will fail with error412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve an InstanceGroupManager. |
| `target_size` | i64 |  | The target number of running instances for this managed instance group.
You can reduce this number by using the instanceGroupManager
deleteInstances or abandonInstances methods. Resizing the group also
changes this number. |
| `update_policy` | String |  | The update policy for this managed instance group. |
| `id` | String |  | [Output Only] A unique identifier for this resource type. The server
generates this identifier. |
| `self_link_with_id` | String |  | [Output Only] Server-defined URL for this resource with the resource id. |
| `params` | String |  | Input only. Additional params passed with the request, but not persisted
as part of resource payload. |
| `versions` | Vec<String> |  | Specifies the instance templates used by this managed instance group to
create instances.

Each version is defined by an instanceTemplate and aname. Every version can appear at most once per instance
group. This field overrides the top-level instanceTemplate
field. Read more about therelationships
between these fields. Exactly one version must leave thetargetSize field unset. That version will be applied to all
remaining instances. For more information, read aboutcanary
updates. |
| `creation_timestamp` | String |  | [Output Only] The creation timestamp for this managed instance group inRFC3339
text format. |
| `instance_template` | String |  | The URL of the instance template that is specified for this managed
instance group. The group uses this template to create all new instances
in the managed instance group. The templates for existing instances in the
group do not change unless you run recreateInstances, runapplyUpdatesToInstances, or set the group'supdatePolicy.type to PROACTIVE. |
| `standby_policy` | String |  | Standby policy for stopped and suspended instances. |
| `all_instances_config` | String |  | Specifies configuration that overrides the instance template configuration
for the group. |
| `multi_mig` | String |  | URL to the multi-MIG that this Managed Instance Group belongs to. |
| `target_size_unit` | String |  | The unit of measure for the target size. |
| `self_link` | String |  | [Output Only] The URL for this managed instance group. The server defines
this URL. |
| `base_instance_name` | String |  | The base instance name is a prefix that you want to attach to the names of
all VMs in a MIG. The maximum character length is 58 and the name must
comply with RFC1035 format.

When a VM is created in the group, the MIG appends a hyphen and a random
four-character string to the base instance name. If you want the MIG to
assign sequential numbers instead of a random string, then end the base
instance name with a hyphen followed by one or more hash symbols. The hash
symbols indicate the number of digits. For example, a base instance name of
"vm-###" results in "vm-001" as a VM name.
@pattern
[a-z](([-a-z0-9]{0,57})|([-a-z0-9]{0,51}-#{1,10}(\\[[0-9]{1,10}\\])?)) |
| `instance_group` | String |  | [Output Only] The URL of the Instance Group resource. |
| `target_pools` | Vec<String> |  | The URLs for all TargetPool resources to which instances in theinstanceGroup field are added. The target pools automatically
apply to all of the instances in the managed instance group. |
| `satisfies_pzs` | bool |  | [Output Only] Reserved for future use. |
| `current_actions` | String |  | [Output Only] The list of instance actions and the number of instances
in this managed instance group that are scheduled for each of those
actions. |
| `zone` | String |  | [Output Only] The URL of azone
where the managed instance group is located (for zonal resources). |
| `instance_lifecycle_policy` | String |  | The repair policy for this managed instance group. |
| `failover_action` | String |  | The action to perform in case of zone failure. Only one value is supported,NO_FAILOVER. The default is NO_FAILOVER. |
| `target_stopped_size` | i64 |  | The target number of stopped instances for this managed instance group.
This number changes when you: 
   
   - Stop instance using the stopInstances
   method or start instances using the startInstances
   method.
   - Manually change the targetStoppedSize using the update
   method. |
| `description` | String |  | An optional description of this resource. |
| `kind` | String |  | [Output Only] The resource type, which is alwayscompute#instanceGroupManager for managed instance groups. |
| `status` | String |  | [Output Only] The status of this managed instance group. |
| `named_ports` | Vec<String> |  | [Output Only] Named ports configured on the Instance Groups complementary
to this Instance Group Manager. |
| `stateful_policy` | String |  | Stateful configuration for this Instanced Group Manager |
| `name` | String |  | The name of the managed instance group. The name must be 1-63 characters
long, and comply withRFC1035. |
| `instance_flexibility_policy` | String |  | Instance flexibility allowing MIG to create VMs from multiple
types of machines.
Instance flexibility configuration on MIG overrides instance
template configuration. |
| `service_account` | String |  | The service account to be used as credentials for all operations performed
by the managed instance group on instances. The service accounts needs all
permissions required to create and delete instances.
By default, the service account
{projectNumber}@cloudservices.gserviceaccount.com is used. |
| `resource_policies` | String |  | Resource policies for this managed instance group. |
| `auto_healing_policies` | Vec<String> |  | The autohealing policy for this managed instance group. You can specify
only one value. |
| `region` | String |  | [Output Only] The URL of theregion
where the managed instance group resides (for regional resources). |
| `satisfies_pzi` | bool |  | [Output Only] Reserved for future use. |
| `list_managed_instances_results` | String |  | Pagination behavior of the listManagedInstances API method for
this managed instance group. |
| `target_size_policy` | String |  | The policy that specifies how the MIG creates its VMs to achieve the target
size. |
| `zone` | String | ✅ | The name of the zone
where you want to create the managed instance group. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `target_suspended_size` | i64 | The target number of suspended instances for this managed instance group.
This number changes when you: 
   
   - Suspend instance using the suspendInstances
   method or resume instances using the resumeInstances
   method.
   - Manually change the targetSuspendedSize using the update
   method. |
| `distribution_policy` | String | Policy specifying the intended distribution of managed instances across
zones in a regional managed instance group. |
| `fingerprint` | String | Fingerprint of this resource. This field may be used in optimistic locking.
It will be ignored when inserting an InstanceGroupManager. An up-to-date
fingerprint must be provided in order to update the InstanceGroupManager,
otherwise the request will fail with error412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve an InstanceGroupManager. |
| `target_size` | i64 | The target number of running instances for this managed instance group.
You can reduce this number by using the instanceGroupManager
deleteInstances or abandonInstances methods. Resizing the group also
changes this number. |
| `update_policy` | String | The update policy for this managed instance group. |
| `id` | String | [Output Only] A unique identifier for this resource type. The server
generates this identifier. |
| `self_link_with_id` | String | [Output Only] Server-defined URL for this resource with the resource id. |
| `params` | String | Input only. Additional params passed with the request, but not persisted
as part of resource payload. |
| `versions` | Vec<String> | Specifies the instance templates used by this managed instance group to
create instances.

Each version is defined by an instanceTemplate and aname. Every version can appear at most once per instance
group. This field overrides the top-level instanceTemplate
field. Read more about therelationships
between these fields. Exactly one version must leave thetargetSize field unset. That version will be applied to all
remaining instances. For more information, read aboutcanary
updates. |
| `creation_timestamp` | String | [Output Only] The creation timestamp for this managed instance group inRFC3339
text format. |
| `instance_template` | String | The URL of the instance template that is specified for this managed
instance group. The group uses this template to create all new instances
in the managed instance group. The templates for existing instances in the
group do not change unless you run recreateInstances, runapplyUpdatesToInstances, or set the group'supdatePolicy.type to PROACTIVE. |
| `standby_policy` | String | Standby policy for stopped and suspended instances. |
| `all_instances_config` | String | Specifies configuration that overrides the instance template configuration
for the group. |
| `multi_mig` | String | URL to the multi-MIG that this Managed Instance Group belongs to. |
| `target_size_unit` | String | The unit of measure for the target size. |
| `self_link` | String | [Output Only] The URL for this managed instance group. The server defines
this URL. |
| `base_instance_name` | String | The base instance name is a prefix that you want to attach to the names of
all VMs in a MIG. The maximum character length is 58 and the name must
comply with RFC1035 format.

When a VM is created in the group, the MIG appends a hyphen and a random
four-character string to the base instance name. If you want the MIG to
assign sequential numbers instead of a random string, then end the base
instance name with a hyphen followed by one or more hash symbols. The hash
symbols indicate the number of digits. For example, a base instance name of
"vm-###" results in "vm-001" as a VM name.
@pattern
[a-z](([-a-z0-9]{0,57})|([-a-z0-9]{0,51}-#{1,10}(\\[[0-9]{1,10}\\])?)) |
| `instance_group` | String | [Output Only] The URL of the Instance Group resource. |
| `target_pools` | Vec<String> | The URLs for all TargetPool resources to which instances in theinstanceGroup field are added. The target pools automatically
apply to all of the instances in the managed instance group. |
| `satisfies_pzs` | bool | [Output Only] Reserved for future use. |
| `current_actions` | String | [Output Only] The list of instance actions and the number of instances
in this managed instance group that are scheduled for each of those
actions. |
| `zone` | String | [Output Only] The URL of azone
where the managed instance group is located (for zonal resources). |
| `instance_lifecycle_policy` | String | The repair policy for this managed instance group. |
| `failover_action` | String | The action to perform in case of zone failure. Only one value is supported,NO_FAILOVER. The default is NO_FAILOVER. |
| `target_stopped_size` | i64 | The target number of stopped instances for this managed instance group.
This number changes when you: 
   
   - Stop instance using the stopInstances
   method or start instances using the startInstances
   method.
   - Manually change the targetStoppedSize using the update
   method. |
| `description` | String | An optional description of this resource. |
| `kind` | String | [Output Only] The resource type, which is alwayscompute#instanceGroupManager for managed instance groups. |
| `status` | String | [Output Only] The status of this managed instance group. |
| `named_ports` | Vec<String> | [Output Only] Named ports configured on the Instance Groups complementary
to this Instance Group Manager. |
| `stateful_policy` | String | Stateful configuration for this Instanced Group Manager |
| `name` | String | The name of the managed instance group. The name must be 1-63 characters
long, and comply withRFC1035. |
| `instance_flexibility_policy` | String | Instance flexibility allowing MIG to create VMs from multiple
types of machines.
Instance flexibility configuration on MIG overrides instance
template configuration. |
| `service_account` | String | The service account to be used as credentials for all operations performed
by the managed instance group on instances. The service accounts needs all
permissions required to create and delete instances.
By default, the service account
{projectNumber}@cloudservices.gserviceaccount.com is used. |
| `resource_policies` | String | Resource policies for this managed instance group. |
| `auto_healing_policies` | Vec<String> | The autohealing policy for this managed instance group. You can specify
only one value. |
| `region` | String | [Output Only] The URL of theregion
where the managed instance group resides (for regional resources). |
| `satisfies_pzi` | bool | [Output Only] Reserved for future use. |
| `list_managed_instances_results` | String | Pagination behavior of the listManagedInstances API method for
this managed instance group. |
| `target_size_policy` | String | The policy that specifies how the MIG creates its VMs to achieve the target
size. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create instance_group_manager
instance_group_manager = provider.compute_api.Instance_group_manager {
    zone = "value"  # The name of the zone
where you want to create the managed instance group.
    project = "value"  # Project ID for this request.
}

# Access instance_group_manager outputs
instance_group_manager_id = instance_group_manager.id
instance_group_manager_target_suspended_size = instance_group_manager.target_suspended_size
instance_group_manager_distribution_policy = instance_group_manager.distribution_policy
instance_group_manager_fingerprint = instance_group_manager.fingerprint
instance_group_manager_target_size = instance_group_manager.target_size
instance_group_manager_update_policy = instance_group_manager.update_policy
instance_group_manager_id = instance_group_manager.id
instance_group_manager_self_link_with_id = instance_group_manager.self_link_with_id
instance_group_manager_params = instance_group_manager.params
instance_group_manager_versions = instance_group_manager.versions
instance_group_manager_creation_timestamp = instance_group_manager.creation_timestamp
instance_group_manager_instance_template = instance_group_manager.instance_template
instance_group_manager_standby_policy = instance_group_manager.standby_policy
instance_group_manager_all_instances_config = instance_group_manager.all_instances_config
instance_group_manager_multi_mig = instance_group_manager.multi_mig
instance_group_manager_target_size_unit = instance_group_manager.target_size_unit
instance_group_manager_self_link = instance_group_manager.self_link
instance_group_manager_base_instance_name = instance_group_manager.base_instance_name
instance_group_manager_instance_group = instance_group_manager.instance_group
instance_group_manager_target_pools = instance_group_manager.target_pools
instance_group_manager_satisfies_pzs = instance_group_manager.satisfies_pzs
instance_group_manager_current_actions = instance_group_manager.current_actions
instance_group_manager_zone = instance_group_manager.zone
instance_group_manager_instance_lifecycle_policy = instance_group_manager.instance_lifecycle_policy
instance_group_manager_failover_action = instance_group_manager.failover_action
instance_group_manager_target_stopped_size = instance_group_manager.target_stopped_size
instance_group_manager_description = instance_group_manager.description
instance_group_manager_kind = instance_group_manager.kind
instance_group_manager_status = instance_group_manager.status
instance_group_manager_named_ports = instance_group_manager.named_ports
instance_group_manager_stateful_policy = instance_group_manager.stateful_policy
instance_group_manager_name = instance_group_manager.name
instance_group_manager_instance_flexibility_policy = instance_group_manager.instance_flexibility_policy
instance_group_manager_service_account = instance_group_manager.service_account
instance_group_manager_resource_policies = instance_group_manager.resource_policies
instance_group_manager_auto_healing_policies = instance_group_manager.auto_healing_policies
instance_group_manager_region = instance_group_manager.region
instance_group_manager_satisfies_pzi = instance_group_manager.satisfies_pzi
instance_group_manager_list_managed_instances_results = instance_group_manager.list_managed_instances_results
instance_group_manager_target_size_policy = instance_group_manager.target_size_policy
```

---


### Snapshot

Creates a snapshot in the specified project using the data included
in the request. For regular snapshot creation, consider using this method
instead of disks.createSnapshot,
as this method supports more features, such as creating snapshots in a
project different from the source disk project.

**Operations**: ✅ Create ✅ Read ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `source_instant_snapshot_encryption_key` | String |  | Customer provided encryption key when creating Snapshot from Instant
Snapshot. |
| `params` | String |  | Input only. [Input Only] Additional params passed with the request, but not persisted
as part of resource payload. |
| `status` | String |  | [Output Only] The status of the snapshot. This can beCREATING, DELETING, FAILED,READY, or UPLOADING. |
| `snapshot_type` | String |  | Indicates the type of the snapshot. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `chain_name` | String |  | Creates the new snapshot in the snapshot chain labeled with the
specified name. The chain name must be 1-63 characters long and comply
with RFC1035. This is an uncommon option only for advanced service
owners who needs to create separate snapshot chains, for example,
for chargeback tracking. When you describe your snapshot resource, this
field is visible only if it has a non-empty value. |
| `user_licenses` | Vec<String> |  | [Output Only] A list of user provided licenses represented by a list of
URLs to the license resource. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `auto_created` | bool |  | [Output Only] Set to true if snapshots are automatically created by
applying resource policy on the target disk. |
| `kind` | String |  | [Output Only] Type of the resource. Always compute#snapshot
for Snapshot resources. |
| `max_retention_days` | i64 |  | Number of days the snapshot should be retained before being deleted
automatically. |
| `guest_os_features` | Vec<String> |  | [Output Only] A list of features to enable on the guest operating system.
Applicable only for bootable images. Read 
Enabling guest operating system features to see a list of available
options. |
| `source_snapshot_schedule_policy` | String |  | [Output Only] URL of the resource policy which created this
scheduled snapshot. |
| `storage_bytes_status` | String |  | [Output Only] An indicator whether storageBytes is in a
stable state or it is being adjusted as a result of shared storage
reallocation. This status can either be UPDATING, meaning
the size of the snapshot is being updated, or UP_TO_DATE,
meaning the size of the snapshot is up-to-date. |
| `labels` | HashMap<String, String> |  | Labels to apply to this snapshot. These can be later modified by
the setLabels method.
Label values may be empty. |
| `creation_size_bytes` | String |  | [Output Only] Size in bytes of the snapshot at creation time. |
| `satisfies_pzs` | bool |  | [Output Only] Reserved for future use. |
| `location_hint` | String |  | An opaque location hint used to place the snapshot close to other
resources.
This field is for use by internal tools that use the public API. |
| `self_link_with_id` | String |  | [Output Only] Server-defined URL for this resource's resource id. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `guest_flush` | bool |  | [Input Only] Whether to attempt an application consistent snapshot by
informing the OS to prepare for the snapshot process. |
| `download_bytes` | String |  | [Output Only] Number of bytes downloaded to restore a snapshot to a disk. |
| `snapshot_group_name` | String |  | [Output only] The snapshot group that this snapshot belongs to. |
| `source_disk` | String |  | The source disk used to create this snapshot. |
| `source_disk_id` | String |  | [Output Only] The ID value of the disk used to create this snapshot. This
value may be used to determine whether the snapshot was taken from the
current or a previous instance of a given disk name. |
| `resource_status` | String |  | [Output only] ResourceStatus of the Snapshot resource |
| `disk_size_gb` | String |  | [Output Only] Size of the source disk, specified in GB. |
| `source_disk_encryption_key` | String |  | The customer-supplied
encryption key of the source disk. Required if the source disk is
protected by a customer-supplied encryption key. |
| `storage_bytes` | String |  | [Output Only] A size of the storage used by the snapshot. As snapshots
share storage, this number is expected to change with snapshot
creation/deletion. |
| `licenses` | Vec<String> |  | [Output Only] A list of public visible licenses that apply to this
snapshot. This can be because the original image had licenses attached
(such as a Windows image). |
| `source_instant_snapshot_id` | String |  | [Output Only] The unique ID of the instant snapshot used to create this
snapshot. This value identifies the exact instant snapshot that was used to
create this snapshot. For example, if you created the snapshot from an
instant snapshot that was later deleted and recreated under the same name,
the source instant snapshot ID would identify the exact instant snapshot
that was used. |
| `source_snapshot_schedule_policy_id` | String |  | [Output Only] ID of the resource policy which created this
scheduled snapshot. |
| `storage_locations` | Vec<String> |  | Cloud Storage bucket storage location of the snapshot (regional or
multi-regional). |
| `license_codes` | Vec<String> |  | [Output Only] Integer license codes indicating which licenses are attached
to this snapshot. |
| `snapshot_encryption_key` | String |  | Encrypts the snapshot using acustomer-supplied
encryption key.

After you encrypt a snapshot using a customer-supplied key, you must
provide the same key if you use the snapshot later. For example, you must
provide the encryption key when you create a disk from the encrypted
snapshot in a future request.

Customer-supplied encryption keys do not protect access to metadata
of the snapshot.

If you do not provide an encryption key when creating the snapshot, then
the snapshot will be encrypted using an automatically generated key and you
do not need to provide a key to use the snapshot later. |
| `enable_confidential_compute` | bool |  | Whether this snapshot is created from a confidential compute mode disk.
[Output Only]: This field is not set by user, but from source disk. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `source_instant_snapshot` | String |  | The source instant snapshot used to create this snapshot. You can provide
this as a partial or full URL to the resource. For example, the following
are valid values:
   
   
     - https://www.googleapis.com/compute/v1/projects/project/zones/zone/instantSnapshots/instantSnapshot
     - projects/project/zones/zone/instantSnapshots/instantSnapshot 
   - zones/zone/instantSnapshots/instantSnapshot |
| `region` | String |  | [Output Only] URL of the region where the snapshot resides. Only applicable
for regional snapshots. |
| `architecture` | String |  | [Output Only] The architecture of the snapshot. Valid values are
ARM64 or X86_64. |
| `satisfies_pzi` | bool |  | Output only. Reserved for future use. |
| `name` | String |  | Name of the resource; provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must be
a dash, lowercase letter, or digit, except the last character, which cannot
be a dash. |
| `source_disk_for_recovery_checkpoint` | String |  | The source disk whose recovery checkpoint will be used to create this
snapshot. |
| `snapshot_group_id` | String |  | [Output Only] The unique ID of the snapshot group that this snapshot
belongs to. |
| `label_fingerprint` | String |  | A fingerprint for the labels being applied to this snapshot, which is
essentially a hash of the labels set used for optimistic locking. The
fingerprint is initially generated by Compute Engine and changes after
every request to modify or update labels. You must always provide an
up-to-date fingerprint hash in order to update or change labels,
otherwise the request will fail with error412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve a snapshot. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `source_instant_snapshot_encryption_key` | String | Customer provided encryption key when creating Snapshot from Instant
Snapshot. |
| `params` | String | Input only. [Input Only] Additional params passed with the request, but not persisted
as part of resource payload. |
| `status` | String | [Output Only] The status of the snapshot. This can beCREATING, DELETING, FAILED,READY, or UPLOADING. |
| `snapshot_type` | String | Indicates the type of the snapshot. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `chain_name` | String | Creates the new snapshot in the snapshot chain labeled with the
specified name. The chain name must be 1-63 characters long and comply
with RFC1035. This is an uncommon option only for advanced service
owners who needs to create separate snapshot chains, for example,
for chargeback tracking. When you describe your snapshot resource, this
field is visible only if it has a non-empty value. |
| `user_licenses` | Vec<String> | [Output Only] A list of user provided licenses represented by a list of
URLs to the license resource. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `auto_created` | bool | [Output Only] Set to true if snapshots are automatically created by
applying resource policy on the target disk. |
| `kind` | String | [Output Only] Type of the resource. Always compute#snapshot
for Snapshot resources. |
| `max_retention_days` | i64 | Number of days the snapshot should be retained before being deleted
automatically. |
| `guest_os_features` | Vec<String> | [Output Only] A list of features to enable on the guest operating system.
Applicable only for bootable images. Read 
Enabling guest operating system features to see a list of available
options. |
| `source_snapshot_schedule_policy` | String | [Output Only] URL of the resource policy which created this
scheduled snapshot. |
| `storage_bytes_status` | String | [Output Only] An indicator whether storageBytes is in a
stable state or it is being adjusted as a result of shared storage
reallocation. This status can either be UPDATING, meaning
the size of the snapshot is being updated, or UP_TO_DATE,
meaning the size of the snapshot is up-to-date. |
| `labels` | HashMap<String, String> | Labels to apply to this snapshot. These can be later modified by
the setLabels method.
Label values may be empty. |
| `creation_size_bytes` | String | [Output Only] Size in bytes of the snapshot at creation time. |
| `satisfies_pzs` | bool | [Output Only] Reserved for future use. |
| `location_hint` | String | An opaque location hint used to place the snapshot close to other
resources.
This field is for use by internal tools that use the public API. |
| `self_link_with_id` | String | [Output Only] Server-defined URL for this resource's resource id. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `guest_flush` | bool | [Input Only] Whether to attempt an application consistent snapshot by
informing the OS to prepare for the snapshot process. |
| `download_bytes` | String | [Output Only] Number of bytes downloaded to restore a snapshot to a disk. |
| `snapshot_group_name` | String | [Output only] The snapshot group that this snapshot belongs to. |
| `source_disk` | String | The source disk used to create this snapshot. |
| `source_disk_id` | String | [Output Only] The ID value of the disk used to create this snapshot. This
value may be used to determine whether the snapshot was taken from the
current or a previous instance of a given disk name. |
| `resource_status` | String | [Output only] ResourceStatus of the Snapshot resource |
| `disk_size_gb` | String | [Output Only] Size of the source disk, specified in GB. |
| `source_disk_encryption_key` | String | The customer-supplied
encryption key of the source disk. Required if the source disk is
protected by a customer-supplied encryption key. |
| `storage_bytes` | String | [Output Only] A size of the storage used by the snapshot. As snapshots
share storage, this number is expected to change with snapshot
creation/deletion. |
| `licenses` | Vec<String> | [Output Only] A list of public visible licenses that apply to this
snapshot. This can be because the original image had licenses attached
(such as a Windows image). |
| `source_instant_snapshot_id` | String | [Output Only] The unique ID of the instant snapshot used to create this
snapshot. This value identifies the exact instant snapshot that was used to
create this snapshot. For example, if you created the snapshot from an
instant snapshot that was later deleted and recreated under the same name,
the source instant snapshot ID would identify the exact instant snapshot
that was used. |
| `source_snapshot_schedule_policy_id` | String | [Output Only] ID of the resource policy which created this
scheduled snapshot. |
| `storage_locations` | Vec<String> | Cloud Storage bucket storage location of the snapshot (regional or
multi-regional). |
| `license_codes` | Vec<String> | [Output Only] Integer license codes indicating which licenses are attached
to this snapshot. |
| `snapshot_encryption_key` | String | Encrypts the snapshot using acustomer-supplied
encryption key.

After you encrypt a snapshot using a customer-supplied key, you must
provide the same key if you use the snapshot later. For example, you must
provide the encryption key when you create a disk from the encrypted
snapshot in a future request.

Customer-supplied encryption keys do not protect access to metadata
of the snapshot.

If you do not provide an encryption key when creating the snapshot, then
the snapshot will be encrypted using an automatically generated key and you
do not need to provide a key to use the snapshot later. |
| `enable_confidential_compute` | bool | Whether this snapshot is created from a confidential compute mode disk.
[Output Only]: This field is not set by user, but from source disk. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `source_instant_snapshot` | String | The source instant snapshot used to create this snapshot. You can provide
this as a partial or full URL to the resource. For example, the following
are valid values:
   
   
     - https://www.googleapis.com/compute/v1/projects/project/zones/zone/instantSnapshots/instantSnapshot
     - projects/project/zones/zone/instantSnapshots/instantSnapshot 
   - zones/zone/instantSnapshots/instantSnapshot |
| `region` | String | [Output Only] URL of the region where the snapshot resides. Only applicable
for regional snapshots. |
| `architecture` | String | [Output Only] The architecture of the snapshot. Valid values are
ARM64 or X86_64. |
| `satisfies_pzi` | bool | Output only. Reserved for future use. |
| `name` | String | Name of the resource; provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must be
a dash, lowercase letter, or digit, except the last character, which cannot
be a dash. |
| `source_disk_for_recovery_checkpoint` | String | The source disk whose recovery checkpoint will be used to create this
snapshot. |
| `snapshot_group_id` | String | [Output Only] The unique ID of the snapshot group that this snapshot
belongs to. |
| `label_fingerprint` | String | A fingerprint for the labels being applied to this snapshot, which is
essentially a hash of the labels set used for optimistic locking. The
fingerprint is initially generated by Compute Engine and changes after
every request to modify or update labels. You must always provide an
up-to-date fingerprint hash in order to update or change labels,
otherwise the request will fail with error412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve a snapshot. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create snapshot
snapshot = provider.compute_api.Snapshot {
    project = "value"  # Project ID for this request.
}

# Access snapshot outputs
snapshot_id = snapshot.id
snapshot_source_instant_snapshot_encryption_key = snapshot.source_instant_snapshot_encryption_key
snapshot_params = snapshot.params
snapshot_status = snapshot.status
snapshot_snapshot_type = snapshot.snapshot_type
snapshot_description = snapshot.description
snapshot_chain_name = snapshot.chain_name
snapshot_user_licenses = snapshot.user_licenses
snapshot_id = snapshot.id
snapshot_auto_created = snapshot.auto_created
snapshot_kind = snapshot.kind
snapshot_max_retention_days = snapshot.max_retention_days
snapshot_guest_os_features = snapshot.guest_os_features
snapshot_source_snapshot_schedule_policy = snapshot.source_snapshot_schedule_policy
snapshot_storage_bytes_status = snapshot.storage_bytes_status
snapshot_labels = snapshot.labels
snapshot_creation_size_bytes = snapshot.creation_size_bytes
snapshot_satisfies_pzs = snapshot.satisfies_pzs
snapshot_location_hint = snapshot.location_hint
snapshot_self_link_with_id = snapshot.self_link_with_id
snapshot_creation_timestamp = snapshot.creation_timestamp
snapshot_guest_flush = snapshot.guest_flush
snapshot_download_bytes = snapshot.download_bytes
snapshot_snapshot_group_name = snapshot.snapshot_group_name
snapshot_source_disk = snapshot.source_disk
snapshot_source_disk_id = snapshot.source_disk_id
snapshot_resource_status = snapshot.resource_status
snapshot_disk_size_gb = snapshot.disk_size_gb
snapshot_source_disk_encryption_key = snapshot.source_disk_encryption_key
snapshot_storage_bytes = snapshot.storage_bytes
snapshot_licenses = snapshot.licenses
snapshot_source_instant_snapshot_id = snapshot.source_instant_snapshot_id
snapshot_source_snapshot_schedule_policy_id = snapshot.source_snapshot_schedule_policy_id
snapshot_storage_locations = snapshot.storage_locations
snapshot_license_codes = snapshot.license_codes
snapshot_snapshot_encryption_key = snapshot.snapshot_encryption_key
snapshot_enable_confidential_compute = snapshot.enable_confidential_compute
snapshot_self_link = snapshot.self_link
snapshot_source_instant_snapshot = snapshot.source_instant_snapshot
snapshot_region = snapshot.region
snapshot_architecture = snapshot.architecture
snapshot_satisfies_pzi = snapshot.satisfies_pzi
snapshot_name = snapshot.name
snapshot_source_disk_for_recovery_checkpoint = snapshot.source_disk_for_recovery_checkpoint
snapshot_snapshot_group_id = snapshot.snapshot_group_id
snapshot_label_fingerprint = snapshot.label_fingerprint
```

---


### Region_security_policie

Creates a new policy in the specified project using the data included in
the request.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `adaptive_protection_config` | String |  |  |
| `advanced_options_config` | String |  |  |
| `associations` | Vec<String> |  | A list of associations that belong to this policy. |
| `name` | String |  | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `ddos_protection_config` | String |  |  |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `region` | String |  | [Output Only] URL of the region where the regional security policy
resides. This field is not applicable to global security policies. |
| `short_name` | String |  | User-provided name of the organization security policy. The name should be
unique in the organization in which the security policy is created. This
should only be used when SecurityPolicyType is CLOUD_ARMOR.
The name must be 1-63 characters long, and comply with
https://www.ietf.org/rfc/rfc1035.txt. Specifically, the name must be 1-63
characters long and match the regular expression
`[a-z]([-a-z0-9]*[a-z0-9])?` which means the first character must be a
lowercase letter, and all following characters must be a dash, lowercase
letter, or digit, except the last character, which cannot be a dash. |
| `type` | String |  | The type indicates the intended use of the security policy.
   
   - CLOUD_ARMOR: Cloud Armor backend security policies can
   be configured to filter incoming HTTP requests targeting backend services.
   They filter requests before they hit the origin servers.
   - CLOUD_ARMOR_EDGE: Cloud Armor edge security policies can
   be configured to filter incoming HTTP requests targeting backend services
   (including Cloud CDN-enabled) as well as backend buckets (Cloud Storage).
   They filter requests before the request is served from Google's cache.
   - CLOUD_ARMOR_INTERNAL_SERVICE (preview only): Cloud Armor
   internal service policies can be configured to filter HTTP requests
   targeting services managed by Traffic Director in a service mesh. They
   filter requests before the request is served from the application.

- CLOUD_ARMOR_NETWORK: Cloud Armor network policies
can be configured to filter packets targeting network load balancing
resources such as backend services, target pools, target instances, and
instances with external IPs. They filter requests before the request is
served from the application.


This field can be set only at resource creation time. |
| `fingerprint` | String |  | Specifies a fingerprint for this resource, which is essentially a hash of
the metadata's contents and used for optimistic locking. The
fingerprint is initially generated by Compute Engine and changes after
every request to modify or update metadata. You must always provide an
up-to-date fingerprint hash in order to update or change metadata,
otherwise the request will fail with error412 conditionNotMet.

To see the latest fingerprint, make get() request to the
security policy. |
| `user_defined_fields` | Vec<String> |  | Definitions of user-defined fields for CLOUD_ARMOR_NETWORK policies. A
user-defined field consists of up to 4 bytes extracted from a fixed offset
in the packet, relative to the IPv4, IPv6, TCP, or UDP header, with an
optional mask to select certain bits. Rules may then specify matching
values for these fields.

Example:

 userDefinedFields:
 - name: "ipv4_fragment_offset"
   base: IPV4
   offset: 6
   size: 2
   mask: "0x1fff" |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `labels` | HashMap<String, String> |  | Labels for this resource. These can only be added or modified by thesetLabels method. Each label key/value pair must comply withRFC1035.
Label values may be empty. |
| `label_fingerprint` | String |  | A fingerprint for the labels being applied to this security policy, which
is essentially a hash of the labels set used for optimistic locking. The
fingerprint is initially generated by Compute Engine and changes after
every request to modify or update labels. You must always provide an
up-to-date fingerprint hash in order to update or change labels.

To see the latest fingerprint, make get() request to the
security policy. |
| `kind` | String |  | [Output only] Type of the resource. Alwayscompute#securityPolicyfor security policies |
| `parent` | String |  | [Output Only] The parent of the security policy. |
| `rule_tuple_count` | i64 |  | [Output Only] Total count of all security policy rule tuples. A security
policy can not exceed a set number of tuples. |
| `cloud_armor_config` | String |  |  |
| `rules` | Vec<String> |  | A list of rules that belong to this policy.
There must always be a default rule which is a rule with priority
2147483647 and match all condition (for the match condition this means
match  "*" for srcIpRanges and for the networkMatch condition every field
must be either match "*" or not set). If no rules are provided when
creating a security policy, a default rule with action "allow" will be
added. |
| `self_link_with_id` | String |  | [Output Only] Server-defined URL for this resource with the resource id. |
| `display_name` | String |  | User-provided name of the organization security policy. The name should be
unique in the organization in which the security policy is created. This
should only be used when SecurityPolicyType is FIREWALL.
The name must be 1-63 characters long, and comply with
https://www.ietf.org/rfc/rfc1035.txt. Specifically, the name must be 1-63
characters long and match the regular expression
`[a-z]([-a-z0-9]*[a-z0-9])?` which means the first character must be a
lowercase letter, and all following characters must be a dash, lowercase
letter, or digit, except the last character, which cannot be a dash. |
| `recaptcha_options_config` | String |  |  |
| `region` | String | ✅ | Name of the region scoping this request. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `adaptive_protection_config` | String |  |
| `advanced_options_config` | String |  |
| `associations` | Vec<String> | A list of associations that belong to this policy. |
| `name` | String | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `ddos_protection_config` | String |  |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `region` | String | [Output Only] URL of the region where the regional security policy
resides. This field is not applicable to global security policies. |
| `short_name` | String | User-provided name of the organization security policy. The name should be
unique in the organization in which the security policy is created. This
should only be used when SecurityPolicyType is CLOUD_ARMOR.
The name must be 1-63 characters long, and comply with
https://www.ietf.org/rfc/rfc1035.txt. Specifically, the name must be 1-63
characters long and match the regular expression
`[a-z]([-a-z0-9]*[a-z0-9])?` which means the first character must be a
lowercase letter, and all following characters must be a dash, lowercase
letter, or digit, except the last character, which cannot be a dash. |
| `type` | String | The type indicates the intended use of the security policy.
   
   - CLOUD_ARMOR: Cloud Armor backend security policies can
   be configured to filter incoming HTTP requests targeting backend services.
   They filter requests before they hit the origin servers.
   - CLOUD_ARMOR_EDGE: Cloud Armor edge security policies can
   be configured to filter incoming HTTP requests targeting backend services
   (including Cloud CDN-enabled) as well as backend buckets (Cloud Storage).
   They filter requests before the request is served from Google's cache.
   - CLOUD_ARMOR_INTERNAL_SERVICE (preview only): Cloud Armor
   internal service policies can be configured to filter HTTP requests
   targeting services managed by Traffic Director in a service mesh. They
   filter requests before the request is served from the application.

- CLOUD_ARMOR_NETWORK: Cloud Armor network policies
can be configured to filter packets targeting network load balancing
resources such as backend services, target pools, target instances, and
instances with external IPs. They filter requests before the request is
served from the application.


This field can be set only at resource creation time. |
| `fingerprint` | String | Specifies a fingerprint for this resource, which is essentially a hash of
the metadata's contents and used for optimistic locking. The
fingerprint is initially generated by Compute Engine and changes after
every request to modify or update metadata. You must always provide an
up-to-date fingerprint hash in order to update or change metadata,
otherwise the request will fail with error412 conditionNotMet.

To see the latest fingerprint, make get() request to the
security policy. |
| `user_defined_fields` | Vec<String> | Definitions of user-defined fields for CLOUD_ARMOR_NETWORK policies. A
user-defined field consists of up to 4 bytes extracted from a fixed offset
in the packet, relative to the IPv4, IPv6, TCP, or UDP header, with an
optional mask to select certain bits. Rules may then specify matching
values for these fields.

Example:

 userDefinedFields:
 - name: "ipv4_fragment_offset"
   base: IPV4
   offset: 6
   size: 2
   mask: "0x1fff" |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `labels` | HashMap<String, String> | Labels for this resource. These can only be added or modified by thesetLabels method. Each label key/value pair must comply withRFC1035.
Label values may be empty. |
| `label_fingerprint` | String | A fingerprint for the labels being applied to this security policy, which
is essentially a hash of the labels set used for optimistic locking. The
fingerprint is initially generated by Compute Engine and changes after
every request to modify or update labels. You must always provide an
up-to-date fingerprint hash in order to update or change labels.

To see the latest fingerprint, make get() request to the
security policy. |
| `kind` | String | [Output only] Type of the resource. Alwayscompute#securityPolicyfor security policies |
| `parent` | String | [Output Only] The parent of the security policy. |
| `rule_tuple_count` | i64 | [Output Only] Total count of all security policy rule tuples. A security
policy can not exceed a set number of tuples. |
| `cloud_armor_config` | String |  |
| `rules` | Vec<String> | A list of rules that belong to this policy.
There must always be a default rule which is a rule with priority
2147483647 and match all condition (for the match condition this means
match  "*" for srcIpRanges and for the networkMatch condition every field
must be either match "*" or not set). If no rules are provided when
creating a security policy, a default rule with action "allow" will be
added. |
| `self_link_with_id` | String | [Output Only] Server-defined URL for this resource with the resource id. |
| `display_name` | String | User-provided name of the organization security policy. The name should be
unique in the organization in which the security policy is created. This
should only be used when SecurityPolicyType is FIREWALL.
The name must be 1-63 characters long, and comply with
https://www.ietf.org/rfc/rfc1035.txt. Specifically, the name must be 1-63
characters long and match the regular expression
`[a-z]([-a-z0-9]*[a-z0-9])?` which means the first character must be a
lowercase letter, and all following characters must be a dash, lowercase
letter, or digit, except the last character, which cannot be a dash. |
| `recaptcha_options_config` | String |  |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create region_security_policie
region_security_policie = provider.compute_api.Region_security_policie {
    region = "value"  # Name of the region scoping this request.
    project = "value"  # Project ID for this request.
}

# Access region_security_policie outputs
region_security_policie_id = region_security_policie.id
region_security_policie_adaptive_protection_config = region_security_policie.adaptive_protection_config
region_security_policie_advanced_options_config = region_security_policie.advanced_options_config
region_security_policie_associations = region_security_policie.associations
region_security_policie_name = region_security_policie.name
region_security_policie_id = region_security_policie.id
region_security_policie_ddos_protection_config = region_security_policie.ddos_protection_config
region_security_policie_description = region_security_policie.description
region_security_policie_self_link = region_security_policie.self_link
region_security_policie_region = region_security_policie.region
region_security_policie_short_name = region_security_policie.short_name
region_security_policie_type = region_security_policie.type
region_security_policie_fingerprint = region_security_policie.fingerprint
region_security_policie_user_defined_fields = region_security_policie.user_defined_fields
region_security_policie_creation_timestamp = region_security_policie.creation_timestamp
region_security_policie_labels = region_security_policie.labels
region_security_policie_label_fingerprint = region_security_policie.label_fingerprint
region_security_policie_kind = region_security_policie.kind
region_security_policie_parent = region_security_policie.parent
region_security_policie_rule_tuple_count = region_security_policie.rule_tuple_count
region_security_policie_cloud_armor_config = region_security_policie.cloud_armor_config
region_security_policie_rules = region_security_policie.rules
region_security_policie_self_link_with_id = region_security_policie.self_link_with_id
region_security_policie_display_name = region_security_policie.display_name
region_security_policie_recaptcha_options_config = region_security_policie.recaptcha_options_config
```

---


### Http_health_check

Creates a HttpHealthCheck resource in the specified project using the data
included in the request.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `timeout_sec` | i64 |  | How long (in seconds) to wait before claiming failure. The default value is5 seconds. It is invalid for timeoutSec to have
greater value than checkIntervalSec. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `host` | String |  | The value of the host header in the HTTP health check request. If left
empty (default value), the public IP on behalf of which this health check
is performed will be used. |
| `self_link_with_id` | String |  | [Output Only] Server-defined URL for this resource with the resource id. |
| `unhealthy_threshold` | i64 |  | A so-far healthy instance will be marked unhealthy after this
many consecutive failures. The default value is 2. |
| `check_interval_sec` | i64 |  | How often (in seconds) to send a health check. The default value is5 seconds. |
| `kind` | String |  | [Output Only] Type of the resource. Alwayscompute#httpHealthCheck for HTTP health checks. |
| `name` | String |  | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `port` | i64 |  | The TCP port number for the HTTP health check request. The default value is80. |
| `request_path` | String |  | The request path of the HTTP health check request. The default value is/. This field does not support query
parameters. Must comply withRFC3986. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `healthy_threshold` | i64 |  | A so-far unhealthy instance will be marked healthy after this
many consecutive successes. The default value is 2. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `timeout_sec` | i64 | How long (in seconds) to wait before claiming failure. The default value is5 seconds. It is invalid for timeoutSec to have
greater value than checkIntervalSec. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `host` | String | The value of the host header in the HTTP health check request. If left
empty (default value), the public IP on behalf of which this health check
is performed will be used. |
| `self_link_with_id` | String | [Output Only] Server-defined URL for this resource with the resource id. |
| `unhealthy_threshold` | i64 | A so-far healthy instance will be marked unhealthy after this
many consecutive failures. The default value is 2. |
| `check_interval_sec` | i64 | How often (in seconds) to send a health check. The default value is5 seconds. |
| `kind` | String | [Output Only] Type of the resource. Alwayscompute#httpHealthCheck for HTTP health checks. |
| `name` | String | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `port` | i64 | The TCP port number for the HTTP health check request. The default value is80. |
| `request_path` | String | The request path of the HTTP health check request. The default value is/. This field does not support query
parameters. Must comply withRFC3986. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `healthy_threshold` | i64 | A so-far unhealthy instance will be marked healthy after this
many consecutive successes. The default value is 2. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create http_health_check
http_health_check = provider.compute_api.Http_health_check {
    project = "value"  # Project ID for this request.
}

# Access http_health_check outputs
http_health_check_id = http_health_check.id
http_health_check_timeout_sec = http_health_check.timeout_sec
http_health_check_description = http_health_check.description
http_health_check_self_link = http_health_check.self_link
http_health_check_id = http_health_check.id
http_health_check_host = http_health_check.host
http_health_check_self_link_with_id = http_health_check.self_link_with_id
http_health_check_unhealthy_threshold = http_health_check.unhealthy_threshold
http_health_check_check_interval_sec = http_health_check.check_interval_sec
http_health_check_kind = http_health_check.kind
http_health_check_name = http_health_check.name
http_health_check_port = http_health_check.port
http_health_check_request_path = http_health_check.request_path
http_health_check_creation_timestamp = http_health_check.creation_timestamp
http_health_check_healthy_threshold = http_health_check.healthy_threshold
```

---


### License

Create a License resource in the specified project.
 *Caution* This resource is intended
for use only by third-party partners who are creatingCloud Marketplace
images.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `charges_use_fee` | bool |  | [Output Only] Deprecated. This field no longer reflects whether a license
charges a usage fee. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `sole_tenant_only` | bool |  | If true, this license can only be used on VMs on sole tenant nodes. |
| `resource_requirements` | String |  | [Input Only] Deprecated. |
| `multi_tenant_only` | bool |  | If true, this license can only be used on VMs on multi tenant nodes. |
| `allowed_replacement_licenses` | Vec<String> |  | Specifies licenseCodes of licenses that can replace this license. Note:
such replacements are allowed even if removable_from_disk is false. |
| `required_coattached_licenses` | Vec<String> |  | Specifies the set of permissible coattached licenseCodes of licenses that
satisfy the coattachment requirement of this license. At least one license
from the set must be attached to the same disk or image as this license. |
| `os_license` | bool |  | If true, indicates this is an OS license. Only one OS license can be
attached to a disk or image at a time. |
| `removable_from_disk` | bool |  | If true, this license can be removed from a disk's set of licenses, with no
replacement license needed. |
| `appendable_to_disk` | bool |  | If true, this license can be appended to an existing disk's set of
licenses. |
| `self_link_with_id` | String |  | [Output Only] Server-defined URL for this resource with the resource id. |
| `incompatible_licenses` | Vec<String> |  | Specifies licenseCodes of licenses that are incompatible with this license.
If a license is incompatible with this license, it cannot be attached to
the same disk or image. |
| `license_code` | String |  | [Output Only] The unique code used to attach this license to images,
snapshots, and disks. |
| `name` | String |  | Name of the resource. The name must be 1-63 characters long and
comply withRFC1035. |
| `transferable` | bool |  | If false, licenses will not be copied from the source resource when
creating an image from a disk, disk from snapshot, or snapshot from disk. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `description` | String |  | An optional textual description of the resource; provided by the client
when the resource is created. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `kind` | String |  | [Output Only] Type of resource. Always compute#license for
licenses. |
| `minimum_retention` | String |  | If set, this license will be unable to be removed or replaced once attached
to a disk until the minimum_retention period has passed. |
| `update_timestamp` | String |  | [Output Only] Last update timestamp inRFC3339
text format. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `charges_use_fee` | bool | [Output Only] Deprecated. This field no longer reflects whether a license
charges a usage fee. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `sole_tenant_only` | bool | If true, this license can only be used on VMs on sole tenant nodes. |
| `resource_requirements` | String | [Input Only] Deprecated. |
| `multi_tenant_only` | bool | If true, this license can only be used on VMs on multi tenant nodes. |
| `allowed_replacement_licenses` | Vec<String> | Specifies licenseCodes of licenses that can replace this license. Note:
such replacements are allowed even if removable_from_disk is false. |
| `required_coattached_licenses` | Vec<String> | Specifies the set of permissible coattached licenseCodes of licenses that
satisfy the coattachment requirement of this license. At least one license
from the set must be attached to the same disk or image as this license. |
| `os_license` | bool | If true, indicates this is an OS license. Only one OS license can be
attached to a disk or image at a time. |
| `removable_from_disk` | bool | If true, this license can be removed from a disk's set of licenses, with no
replacement license needed. |
| `appendable_to_disk` | bool | If true, this license can be appended to an existing disk's set of
licenses. |
| `self_link_with_id` | String | [Output Only] Server-defined URL for this resource with the resource id. |
| `incompatible_licenses` | Vec<String> | Specifies licenseCodes of licenses that are incompatible with this license.
If a license is incompatible with this license, it cannot be attached to
the same disk or image. |
| `license_code` | String | [Output Only] The unique code used to attach this license to images,
snapshots, and disks. |
| `name` | String | Name of the resource. The name must be 1-63 characters long and
comply withRFC1035. |
| `transferable` | bool | If false, licenses will not be copied from the source resource when
creating an image from a disk, disk from snapshot, or snapshot from disk. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `description` | String | An optional textual description of the resource; provided by the client
when the resource is created. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `kind` | String | [Output Only] Type of resource. Always compute#license for
licenses. |
| `minimum_retention` | String | If set, this license will be unable to be removed or replaced once attached
to a disk until the minimum_retention period has passed. |
| `update_timestamp` | String | [Output Only] Last update timestamp inRFC3339
text format. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create license
license = provider.compute_api.License {
    project = "value"  # Project ID for this request.
}

# Access license outputs
license_id = license.id
license_charges_use_fee = license.charges_use_fee
license_self_link = license.self_link
license_sole_tenant_only = license.sole_tenant_only
license_resource_requirements = license.resource_requirements
license_multi_tenant_only = license.multi_tenant_only
license_allowed_replacement_licenses = license.allowed_replacement_licenses
license_required_coattached_licenses = license.required_coattached_licenses
license_os_license = license.os_license
license_removable_from_disk = license.removable_from_disk
license_appendable_to_disk = license.appendable_to_disk
license_self_link_with_id = license.self_link_with_id
license_incompatible_licenses = license.incompatible_licenses
license_license_code = license.license_code
license_name = license.name
license_transferable = license.transferable
license_creation_timestamp = license.creation_timestamp
license_description = license.description
license_id = license.id
license_kind = license.kind
license_minimum_retention = license.minimum_retention
license_update_timestamp = license.update_timestamp
```

---


### Region_instance_group_manager_resize_request

Creates a new Resize Request that starts provisioning VMs immediately
or queues VM creation.

**Operations**: ✅ Create ✅ Read ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `description` | String |  | An optional description of this resource. |
| `region` | String |  | [Output Only] The URL of aregion
where the resize request is located. Populated only for regional resize
requests. |
| `kind` | String |  | [Output Only] The resource type, which is alwayscompute#instanceGroupManagerResizeRequest for
resize requests. |
| `requested_run_duration` | String |  | Requested run duration for instances that will be created by this request.
At the end of the run duration instance will be deleted. |
| `creation_timestamp` | String |  | [Output Only] The creation timestamp for this resize request inRFC3339
text format. |
| `resize_by` | i64 |  | The number of instances to be created by this resize request. The group's
target size will be increased by this number. This field cannot be used
together with 'instances'. |
| `state` | String |  | [Output only] Current state of the request. |
| `name` | String |  | The name of this resize request. The name must be 1-63 characters
long, and comply withRFC1035. |
| `id` | String |  | [Output Only] A unique identifier for this resource type. The server
generates this identifier. |
| `self_link_with_id` | String |  | [Output Only] Server-defined URL for this resource with the resource id. |
| `status` | String |  | [Output only] Status of the request. |
| `zone` | String |  | [Output Only] The URL of azone
where the resize request is located. Populated only for zonal resize
requests. |
| `queuing_policy` | String |  | This field is deprecated, ResizeRequests would not be provisioned
immediately and would stay in the queue until explicitly cancelled. When
set, defines queing parameters for the requested deferred capacity. When
unset, the request starts provisioning immediately, or fails if immediate
provisioning is not possible. |
| `count` | i64 |  | This field is deprecated, please use resize_by instead.
The count of instances to create as part of this resize request. |
| `instances` | Vec<String> |  | The names of instances to be created by this resize request. The number of
names specified determines the number of instances to create. The group's
target size will be increased by this number. This field cannot be used
together with 'resize_by'. |
| `self_link` | String |  | [Output Only] The URL for this resize request. The server defines
this URL. |
| `project` | String | ✅ | Project ID for this request. |
| `region` | String | ✅ | Name of the region
scoping this request. Name should conform to RFC1035. |
| `instance_group_manager` | String | ✅ | Name of the managed instance group to which the resize request is scoped.
Name should conform to RFC1035 or be a resource ID. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `description` | String | An optional description of this resource. |
| `region` | String | [Output Only] The URL of aregion
where the resize request is located. Populated only for regional resize
requests. |
| `kind` | String | [Output Only] The resource type, which is alwayscompute#instanceGroupManagerResizeRequest for
resize requests. |
| `requested_run_duration` | String | Requested run duration for instances that will be created by this request.
At the end of the run duration instance will be deleted. |
| `creation_timestamp` | String | [Output Only] The creation timestamp for this resize request inRFC3339
text format. |
| `resize_by` | i64 | The number of instances to be created by this resize request. The group's
target size will be increased by this number. This field cannot be used
together with 'instances'. |
| `state` | String | [Output only] Current state of the request. |
| `name` | String | The name of this resize request. The name must be 1-63 characters
long, and comply withRFC1035. |
| `id` | String | [Output Only] A unique identifier for this resource type. The server
generates this identifier. |
| `self_link_with_id` | String | [Output Only] Server-defined URL for this resource with the resource id. |
| `status` | String | [Output only] Status of the request. |
| `zone` | String | [Output Only] The URL of azone
where the resize request is located. Populated only for zonal resize
requests. |
| `queuing_policy` | String | This field is deprecated, ResizeRequests would not be provisioned
immediately and would stay in the queue until explicitly cancelled. When
set, defines queing parameters for the requested deferred capacity. When
unset, the request starts provisioning immediately, or fails if immediate
provisioning is not possible. |
| `count` | i64 | This field is deprecated, please use resize_by instead.
The count of instances to create as part of this resize request. |
| `instances` | Vec<String> | The names of instances to be created by this resize request. The number of
names specified determines the number of instances to create. The group's
target size will be increased by this number. This field cannot be used
together with 'resize_by'. |
| `self_link` | String | [Output Only] The URL for this resize request. The server defines
this URL. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create region_instance_group_manager_resize_request
region_instance_group_manager_resize_request = provider.compute_api.Region_instance_group_manager_resize_request {
    project = "value"  # Project ID for this request.
    region = "value"  # Name of the region
scoping this request. Name should conform to RFC1035.
    instance_group_manager = "value"  # Name of the managed instance group to which the resize request is scoped.
Name should conform to RFC1035 or be a resource ID.
}

# Access region_instance_group_manager_resize_request outputs
region_instance_group_manager_resize_request_id = region_instance_group_manager_resize_request.id
region_instance_group_manager_resize_request_description = region_instance_group_manager_resize_request.description
region_instance_group_manager_resize_request_region = region_instance_group_manager_resize_request.region
region_instance_group_manager_resize_request_kind = region_instance_group_manager_resize_request.kind
region_instance_group_manager_resize_request_requested_run_duration = region_instance_group_manager_resize_request.requested_run_duration
region_instance_group_manager_resize_request_creation_timestamp = region_instance_group_manager_resize_request.creation_timestamp
region_instance_group_manager_resize_request_resize_by = region_instance_group_manager_resize_request.resize_by
region_instance_group_manager_resize_request_state = region_instance_group_manager_resize_request.state
region_instance_group_manager_resize_request_name = region_instance_group_manager_resize_request.name
region_instance_group_manager_resize_request_id = region_instance_group_manager_resize_request.id
region_instance_group_manager_resize_request_self_link_with_id = region_instance_group_manager_resize_request.self_link_with_id
region_instance_group_manager_resize_request_status = region_instance_group_manager_resize_request.status
region_instance_group_manager_resize_request_zone = region_instance_group_manager_resize_request.zone
region_instance_group_manager_resize_request_queuing_policy = region_instance_group_manager_resize_request.queuing_policy
region_instance_group_manager_resize_request_count = region_instance_group_manager_resize_request.count
region_instance_group_manager_resize_request_instances = region_instance_group_manager_resize_request.instances
region_instance_group_manager_resize_request_self_link = region_instance_group_manager_resize_request.self_link
```

---


### Zone_folder_operation

Retrieves the specified Operations resource.

**Operations**: ✅ Read

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `http_error_status_code` | i64 | [Output Only] If the operation fails, this field contains the HTTP error
status code that was returned. For example, a `404` means the
resource was not found. |
| `description` | String | [Output Only] A textual description of the operation, which is
set when the operation is created. |
| `progress` | i64 | [Output Only] An optional progress indicator that ranges from 0 to 100.
There is no requirement that this be linear or support any granularity of
operations. This should not be used to guess when the operation will be
complete. This number should monotonically increase as the operation
progresses. |
| `start_time` | String | [Output Only] The time that this operation was started by the server.
This value is inRFC3339
text format. |
| `status` | String | [Output Only] The status of the operation, which can be one of the
following:
`PENDING`, `RUNNING`, or `DONE`. |
| `end_time` | String | [Output Only] The time that this operation was completed. This value is inRFC3339
text format. |
| `operation_type` | String | [Output Only] The type of operation, such as `insert`,
`update`, or `delete`, and so on. |
| `region` | String | [Output Only] The URL of the region where the operation resides. Only
applicable when performing regional operations. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `http_error_message` | String | [Output Only] If the operation fails, this field contains the HTTP error
message that was returned, such as `NOT FOUND`. |
| `insert_time` | String | [Output Only] The time that this operation was requested.
This value is inRFC3339
text format. |
| `instances_bulk_insert_operation_metadata` | String |  |
| `self_link_with_id` | String | [Output Only] Server-defined URL for this resource with the resource id. |
| `warnings` | Vec<String> | [Output Only] If warning messages are generated during processing of the
operation, this field will be populated. |
| `zone` | String | [Output Only] The URL of the zone where the operation resides. Only
applicable when performing per-zone operations. |
| `id` | String | [Output Only] The unique identifier for the operation. This identifier is
defined by the server. |
| `set_common_instance_metadata_operation_metadata` | String | [Output Only] If the operation is for projects.setCommonInstanceMetadata,
this field will contain information on all underlying zonal actions and
their state. |
| `client_operation_id` | String | [Output Only] The value of `requestId` if you provided it in the request.
Not present otherwise. |
| `name` | String | [Output Only] Name of the operation. |
| `kind` | String | [Output Only] Type of the resource. Always `compute#operation` for
Operation resources. |
| `operation_group_id` | String | [Output Only] An ID that represents a group of operations, such as when a
group of operations results from a `bulkInsert` API request. |
| `user` | String | [Output Only] User who requested the operation, for example:
`user@example.com` or
`alice_smith_identifier (global/workforcePools/example-com-us-employees)`. |
| `error` | String | [Output Only] If errors are generated during processing of the operation,
this field will be populated. |
| `creation_timestamp` | String | [Deprecated] This field is deprecated. |
| `status_message` | String | [Output Only] An optional textual description of the current status of the
operation. |
| `target_id` | String | [Output Only] The unique target ID, which identifies a specific incarnation
of the target resource. |
| `target_link` | String | [Output Only] The URL of the resource that the operation modifies. For
operations related to creating a snapshot, this points to the disk
that the snapshot was created from. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Access zone_folder_operation outputs
zone_folder_operation_id = zone_folder_operation.id
zone_folder_operation_http_error_status_code = zone_folder_operation.http_error_status_code
zone_folder_operation_description = zone_folder_operation.description
zone_folder_operation_progress = zone_folder_operation.progress
zone_folder_operation_start_time = zone_folder_operation.start_time
zone_folder_operation_status = zone_folder_operation.status
zone_folder_operation_end_time = zone_folder_operation.end_time
zone_folder_operation_operation_type = zone_folder_operation.operation_type
zone_folder_operation_region = zone_folder_operation.region
zone_folder_operation_self_link = zone_folder_operation.self_link
zone_folder_operation_http_error_message = zone_folder_operation.http_error_message
zone_folder_operation_insert_time = zone_folder_operation.insert_time
zone_folder_operation_instances_bulk_insert_operation_metadata = zone_folder_operation.instances_bulk_insert_operation_metadata
zone_folder_operation_self_link_with_id = zone_folder_operation.self_link_with_id
zone_folder_operation_warnings = zone_folder_operation.warnings
zone_folder_operation_zone = zone_folder_operation.zone
zone_folder_operation_id = zone_folder_operation.id
zone_folder_operation_set_common_instance_metadata_operation_metadata = zone_folder_operation.set_common_instance_metadata_operation_metadata
zone_folder_operation_client_operation_id = zone_folder_operation.client_operation_id
zone_folder_operation_name = zone_folder_operation.name
zone_folder_operation_kind = zone_folder_operation.kind
zone_folder_operation_operation_group_id = zone_folder_operation.operation_group_id
zone_folder_operation_user = zone_folder_operation.user
zone_folder_operation_error = zone_folder_operation.error
zone_folder_operation_creation_timestamp = zone_folder_operation.creation_timestamp
zone_folder_operation_status_message = zone_folder_operation.status_message
zone_folder_operation_target_id = zone_folder_operation.target_id
zone_folder_operation_target_link = zone_folder_operation.target_link
```

---


### Global_operation

Waits for the specified Operation resource to return as `DONE`
or for the request to approach the 2 minute deadline, and retrieves the
specified Operation resource. This method differs from the
`GET` method in that it waits for no more than the default
deadline (2 minutes) and then returns the current state of the operation,
which might be `DONE` or still in progress.

This method is called on a best-effort basis. Specifically:
   
   
    - In uncommon cases, when the server is overloaded, the request might
    return before the default deadline is reached, or might return after zero
    seconds.
   - If the default deadline is reached, there is no guarantee that the
    operation is actually done when the method returns. Be prepared to retry
    if the operation is not `DONE`.

**Operations**: ✅ Create ✅ Read ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `operation` | String | ✅ | Name of the Operations resource to return, or its unique numeric
identifier. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `http_error_status_code` | i64 | [Output Only] If the operation fails, this field contains the HTTP error
status code that was returned. For example, a `404` means the
resource was not found. |
| `description` | String | [Output Only] A textual description of the operation, which is
set when the operation is created. |
| `progress` | i64 | [Output Only] An optional progress indicator that ranges from 0 to 100.
There is no requirement that this be linear or support any granularity of
operations. This should not be used to guess when the operation will be
complete. This number should monotonically increase as the operation
progresses. |
| `start_time` | String | [Output Only] The time that this operation was started by the server.
This value is inRFC3339
text format. |
| `status` | String | [Output Only] The status of the operation, which can be one of the
following:
`PENDING`, `RUNNING`, or `DONE`. |
| `end_time` | String | [Output Only] The time that this operation was completed. This value is inRFC3339
text format. |
| `operation_type` | String | [Output Only] The type of operation, such as `insert`,
`update`, or `delete`, and so on. |
| `region` | String | [Output Only] The URL of the region where the operation resides. Only
applicable when performing regional operations. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `http_error_message` | String | [Output Only] If the operation fails, this field contains the HTTP error
message that was returned, such as `NOT FOUND`. |
| `insert_time` | String | [Output Only] The time that this operation was requested.
This value is inRFC3339
text format. |
| `instances_bulk_insert_operation_metadata` | String |  |
| `self_link_with_id` | String | [Output Only] Server-defined URL for this resource with the resource id. |
| `warnings` | Vec<String> | [Output Only] If warning messages are generated during processing of the
operation, this field will be populated. |
| `zone` | String | [Output Only] The URL of the zone where the operation resides. Only
applicable when performing per-zone operations. |
| `id` | String | [Output Only] The unique identifier for the operation. This identifier is
defined by the server. |
| `set_common_instance_metadata_operation_metadata` | String | [Output Only] If the operation is for projects.setCommonInstanceMetadata,
this field will contain information on all underlying zonal actions and
their state. |
| `client_operation_id` | String | [Output Only] The value of `requestId` if you provided it in the request.
Not present otherwise. |
| `name` | String | [Output Only] Name of the operation. |
| `kind` | String | [Output Only] Type of the resource. Always `compute#operation` for
Operation resources. |
| `operation_group_id` | String | [Output Only] An ID that represents a group of operations, such as when a
group of operations results from a `bulkInsert` API request. |
| `user` | String | [Output Only] User who requested the operation, for example:
`user@example.com` or
`alice_smith_identifier (global/workforcePools/example-com-us-employees)`. |
| `error` | String | [Output Only] If errors are generated during processing of the operation,
this field will be populated. |
| `creation_timestamp` | String | [Deprecated] This field is deprecated. |
| `status_message` | String | [Output Only] An optional textual description of the current status of the
operation. |
| `target_id` | String | [Output Only] The unique target ID, which identifies a specific incarnation
of the target resource. |
| `target_link` | String | [Output Only] The URL of the resource that the operation modifies. For
operations related to creating a snapshot, this points to the disk
that the snapshot was created from. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create global_operation
global_operation = provider.compute_api.Global_operation {
    operation = "value"  # Name of the Operations resource to return, or its unique numeric
identifier.
    project = "value"  # Project ID for this request.
}

# Access global_operation outputs
global_operation_id = global_operation.id
global_operation_http_error_status_code = global_operation.http_error_status_code
global_operation_description = global_operation.description
global_operation_progress = global_operation.progress
global_operation_start_time = global_operation.start_time
global_operation_status = global_operation.status
global_operation_end_time = global_operation.end_time
global_operation_operation_type = global_operation.operation_type
global_operation_region = global_operation.region
global_operation_self_link = global_operation.self_link
global_operation_http_error_message = global_operation.http_error_message
global_operation_insert_time = global_operation.insert_time
global_operation_instances_bulk_insert_operation_metadata = global_operation.instances_bulk_insert_operation_metadata
global_operation_self_link_with_id = global_operation.self_link_with_id
global_operation_warnings = global_operation.warnings
global_operation_zone = global_operation.zone
global_operation_id = global_operation.id
global_operation_set_common_instance_metadata_operation_metadata = global_operation.set_common_instance_metadata_operation_metadata
global_operation_client_operation_id = global_operation.client_operation_id
global_operation_name = global_operation.name
global_operation_kind = global_operation.kind
global_operation_operation_group_id = global_operation.operation_group_id
global_operation_user = global_operation.user
global_operation_error = global_operation.error
global_operation_creation_timestamp = global_operation.creation_timestamp
global_operation_status_message = global_operation.status_message
global_operation_target_id = global_operation.target_id
global_operation_target_link = global_operation.target_link
```

---


### Route

Creates a Route resource in the specified project using the data included
in the request.

**Operations**: ✅ Create ✅ Read ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `next_hop_origin` | String |  | [Output Only] Indicates the origin of the route. Can be IGP
(Interior Gateway Protocol), EGP (Exterior Gateway Protocol),
or INCOMPLETE. |
| `as_paths` | Vec<String> |  | [Output Only] AS path. |
| `next_hop_ilb` | String |  | The URL to a forwarding rule of typeloadBalancingScheme=INTERNAL that should handle matching
packets or the IP address of the forwarding Rule.
For example, the following are all valid URLs:
   
   
      - https://www.googleapis.com/compute/v1/projects/project/regions/region/forwardingRules/forwardingRule 
   - regions/region/forwardingRules/forwardingRule


If an IP address is provided, must specify an IPv4 address in dot-decimal
notation or an IPv6 address in RFC 4291 format. For example, the following
are all valid IP addresses:
   
   
      - 10.128.0.56
      - 2001:db8::2d9:51:0:0
      - 2001:db8:0:0:2d9:51:0:0


IPv6 addresses will be displayed using RFC 5952 compressed format (e.g.
2001:db8::2d9:51:0:0). Should never be an IPv4-mapped IPv6 address. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `next_hop_ip` | String |  | The network IP address of an instance that should handle matching packets.
Both IPv6 address and IPv4 addresses are supported.
Must specify an IPv4 address in dot-decimal notation (e.g. 192.0.2.99) or
an IPv6 address in RFC 4291 format (e.g. 2001:db8::2d9:51:0:0 or
2001:db8:0:0:2d9:51:0:0). IPv6 addresses will be displayed using RFC 5952
compressed format (e.g. 2001:db8::2d9:51:0:0). Should never be an
IPv4-mapped IPv6 address. |
| `next_hop_vpn_tunnel` | String |  | The URL to a VpnTunnel that should handle matching packets. |
| `route_status` | String |  | [Output only] The status of the route. This status only applies to
dynamic routes learned by Cloud Routers. This status is not applicable
to static routes. |
| `next_hop_inter_region_cost` | i64 |  | [Output only] Internal fixed region-to-region cost that Google Cloud
calculates based on factors such as network performance, distance, and
available bandwidth between regions. |
| `self_link` | String |  | [Output Only] Server-defined fully-qualified URL for this resource. |
| `kind` | String |  | [Output Only] Type of this resource. Always compute#routes for
Route resources. |
| `tags` | Vec<String> |  | A list of instance tags to which this route applies. |
| `next_hop_interconnect_attachment` | String |  | [Output Only] The URL to an InterconnectAttachment which is the next hop
for the route.
This field will only be populated for dynamic routes generated by
Cloud Router with a linked interconnectAttachment or the static route
generated by each L2 Interconnect Attachment. |
| `description` | String |  | An optional description of this resource. Provide this field when you
create the resource. |
| `name` | String |  | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?`. The first character must be a
lowercase letter, and all following characters (except for the last
character) must be a dash, lowercase letter, or digit. The last character
must be a lowercase letter or digit. |
| `next_hop_peering` | String |  | [Output Only] The network peering name that should handle matching packets,
which should conform to RFC1035. |
| `next_hop_med` | i64 |  | [Output Only] Multi-Exit Discriminator, a BGP route metric that indicates
the desirability of a particular route in a network. |
| `self_link_with_id` | String |  | [Output Only] Server-defined URL for this resource with the resource id. |
| `network` | String |  | Fully-qualified URL of the network that this route applies to. |
| `next_hop_hub` | String |  | [Output Only] The full resource name of the Network Connectivity Center hub
that will handle matching packets. |
| `params` | String |  | Input only. [Input Only] Additional params passed with the request, but not persisted
as part of resource payload. |
| `allow_conflicting_subnetworks` | bool |  | Whether this route can conflict with existing subnetworks. Setting this to
true allows this route to conflict with subnetworks that have already been
configured on the corresponding network. |
| `next_hop_network` | String |  | The URL of the local network if it should handle matching packets. |
| `next_hop_instance` | String |  | The URL to an instance that should handle matching packets. You can specify
this as a full or partial URL.
For example: 
https://www.googleapis.com/compute/v1/projects/project/zones/zone/instances/ |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `warnings` | Vec<String> |  | [Output Only] If potential misconfigurations are detected for this
route, this field will be populated with warning messages. |
| `route_type` | String |  | [Output Only] The type of this route, which can be one of the following
values:
- 'TRANSIT' for a transit route that this router learned from
another Cloud Router and will readvertise to one of its BGP peers 
- 'SUBNET' for a route from a subnet of the VPC 
- 'BGP' for a route learned from a BGP peer of this router 
- 'STATIC' for a static route |
| `priority` | i64 |  | The priority of this route. Priority is used to break ties in cases
where there is more than one matching route of equal prefix length. In
cases where multiple routes have equal prefix length, the one with the
lowest-numbered priority value wins. The default value is `1000`. The
priority value must be from `0` to `65535`, inclusive. |
| `next_hop_gateway` | String |  | The URL to a gateway that should handle matching packets.
You can only specify the internet gateway using a full or
partial valid URL: projects/project/global/gateways/default-internet-gateway |
| `dest_range` | String |  | The destination range of outgoing packets that this route applies to. Both
IPv4 and IPv6 are supported.
Must specify an IPv4 range (e.g. 192.0.2.0/24) or an IPv6 range in RFC 4291
format (e.g. 2001:db8::/32). IPv6 range will be displayed using RFC 5952
compressed format. |
| `ilb_route_behavior_on_unhealthy` | String |  | ILB route behavior when ILB is deemed unhealthy based on user specified
threshold on the Backend Service of the internal load balancing. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `next_hop_origin` | String | [Output Only] Indicates the origin of the route. Can be IGP
(Interior Gateway Protocol), EGP (Exterior Gateway Protocol),
or INCOMPLETE. |
| `as_paths` | Vec<String> | [Output Only] AS path. |
| `next_hop_ilb` | String | The URL to a forwarding rule of typeloadBalancingScheme=INTERNAL that should handle matching
packets or the IP address of the forwarding Rule.
For example, the following are all valid URLs:
   
   
      - https://www.googleapis.com/compute/v1/projects/project/regions/region/forwardingRules/forwardingRule 
   - regions/region/forwardingRules/forwardingRule


If an IP address is provided, must specify an IPv4 address in dot-decimal
notation or an IPv6 address in RFC 4291 format. For example, the following
are all valid IP addresses:
   
   
      - 10.128.0.56
      - 2001:db8::2d9:51:0:0
      - 2001:db8:0:0:2d9:51:0:0


IPv6 addresses will be displayed using RFC 5952 compressed format (e.g.
2001:db8::2d9:51:0:0). Should never be an IPv4-mapped IPv6 address. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `next_hop_ip` | String | The network IP address of an instance that should handle matching packets.
Both IPv6 address and IPv4 addresses are supported.
Must specify an IPv4 address in dot-decimal notation (e.g. 192.0.2.99) or
an IPv6 address in RFC 4291 format (e.g. 2001:db8::2d9:51:0:0 or
2001:db8:0:0:2d9:51:0:0). IPv6 addresses will be displayed using RFC 5952
compressed format (e.g. 2001:db8::2d9:51:0:0). Should never be an
IPv4-mapped IPv6 address. |
| `next_hop_vpn_tunnel` | String | The URL to a VpnTunnel that should handle matching packets. |
| `route_status` | String | [Output only] The status of the route. This status only applies to
dynamic routes learned by Cloud Routers. This status is not applicable
to static routes. |
| `next_hop_inter_region_cost` | i64 | [Output only] Internal fixed region-to-region cost that Google Cloud
calculates based on factors such as network performance, distance, and
available bandwidth between regions. |
| `self_link` | String | [Output Only] Server-defined fully-qualified URL for this resource. |
| `kind` | String | [Output Only] Type of this resource. Always compute#routes for
Route resources. |
| `tags` | Vec<String> | A list of instance tags to which this route applies. |
| `next_hop_interconnect_attachment` | String | [Output Only] The URL to an InterconnectAttachment which is the next hop
for the route.
This field will only be populated for dynamic routes generated by
Cloud Router with a linked interconnectAttachment or the static route
generated by each L2 Interconnect Attachment. |
| `description` | String | An optional description of this resource. Provide this field when you
create the resource. |
| `name` | String | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?`. The first character must be a
lowercase letter, and all following characters (except for the last
character) must be a dash, lowercase letter, or digit. The last character
must be a lowercase letter or digit. |
| `next_hop_peering` | String | [Output Only] The network peering name that should handle matching packets,
which should conform to RFC1035. |
| `next_hop_med` | i64 | [Output Only] Multi-Exit Discriminator, a BGP route metric that indicates
the desirability of a particular route in a network. |
| `self_link_with_id` | String | [Output Only] Server-defined URL for this resource with the resource id. |
| `network` | String | Fully-qualified URL of the network that this route applies to. |
| `next_hop_hub` | String | [Output Only] The full resource name of the Network Connectivity Center hub
that will handle matching packets. |
| `params` | String | Input only. [Input Only] Additional params passed with the request, but not persisted
as part of resource payload. |
| `allow_conflicting_subnetworks` | bool | Whether this route can conflict with existing subnetworks. Setting this to
true allows this route to conflict with subnetworks that have already been
configured on the corresponding network. |
| `next_hop_network` | String | The URL of the local network if it should handle matching packets. |
| `next_hop_instance` | String | The URL to an instance that should handle matching packets. You can specify
this as a full or partial URL.
For example: 
https://www.googleapis.com/compute/v1/projects/project/zones/zone/instances/ |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `warnings` | Vec<String> | [Output Only] If potential misconfigurations are detected for this
route, this field will be populated with warning messages. |
| `route_type` | String | [Output Only] The type of this route, which can be one of the following
values:
- 'TRANSIT' for a transit route that this router learned from
another Cloud Router and will readvertise to one of its BGP peers 
- 'SUBNET' for a route from a subnet of the VPC 
- 'BGP' for a route learned from a BGP peer of this router 
- 'STATIC' for a static route |
| `priority` | i64 | The priority of this route. Priority is used to break ties in cases
where there is more than one matching route of equal prefix length. In
cases where multiple routes have equal prefix length, the one with the
lowest-numbered priority value wins. The default value is `1000`. The
priority value must be from `0` to `65535`, inclusive. |
| `next_hop_gateway` | String | The URL to a gateway that should handle matching packets.
You can only specify the internet gateway using a full or
partial valid URL: projects/project/global/gateways/default-internet-gateway |
| `dest_range` | String | The destination range of outgoing packets that this route applies to. Both
IPv4 and IPv6 are supported.
Must specify an IPv4 range (e.g. 192.0.2.0/24) or an IPv6 range in RFC 4291
format (e.g. 2001:db8::/32). IPv6 range will be displayed using RFC 5952
compressed format. |
| `ilb_route_behavior_on_unhealthy` | String | ILB route behavior when ILB is deemed unhealthy based on user specified
threshold on the Backend Service of the internal load balancing. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create route
route = provider.compute_api.Route {
    project = "value"  # Project ID for this request.
}

# Access route outputs
route_id = route.id
route_next_hop_origin = route.next_hop_origin
route_as_paths = route.as_paths
route_next_hop_ilb = route.next_hop_ilb
route_id = route.id
route_next_hop_ip = route.next_hop_ip
route_next_hop_vpn_tunnel = route.next_hop_vpn_tunnel
route_route_status = route.route_status
route_next_hop_inter_region_cost = route.next_hop_inter_region_cost
route_self_link = route.self_link
route_kind = route.kind
route_tags = route.tags
route_next_hop_interconnect_attachment = route.next_hop_interconnect_attachment
route_description = route.description
route_name = route.name
route_next_hop_peering = route.next_hop_peering
route_next_hop_med = route.next_hop_med
route_self_link_with_id = route.self_link_with_id
route_network = route.network
route_next_hop_hub = route.next_hop_hub
route_params = route.params
route_allow_conflicting_subnetworks = route.allow_conflicting_subnetworks
route_next_hop_network = route.next_hop_network
route_next_hop_instance = route.next_hop_instance
route_creation_timestamp = route.creation_timestamp
route_warnings = route.warnings
route_route_type = route.route_type
route_priority = route.priority
route_next_hop_gateway = route.next_hop_gateway
route_dest_range = route.dest_range
route_ilb_route_behavior_on_unhealthy = route.ilb_route_behavior_on_unhealthy
```

---


### Global_folder_operation

Retrieves the specified Operations resource.

**Operations**: ✅ Read

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `http_error_status_code` | i64 | [Output Only] If the operation fails, this field contains the HTTP error
status code that was returned. For example, a `404` means the
resource was not found. |
| `description` | String | [Output Only] A textual description of the operation, which is
set when the operation is created. |
| `progress` | i64 | [Output Only] An optional progress indicator that ranges from 0 to 100.
There is no requirement that this be linear or support any granularity of
operations. This should not be used to guess when the operation will be
complete. This number should monotonically increase as the operation
progresses. |
| `start_time` | String | [Output Only] The time that this operation was started by the server.
This value is inRFC3339
text format. |
| `status` | String | [Output Only] The status of the operation, which can be one of the
following:
`PENDING`, `RUNNING`, or `DONE`. |
| `end_time` | String | [Output Only] The time that this operation was completed. This value is inRFC3339
text format. |
| `operation_type` | String | [Output Only] The type of operation, such as `insert`,
`update`, or `delete`, and so on. |
| `region` | String | [Output Only] The URL of the region where the operation resides. Only
applicable when performing regional operations. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `http_error_message` | String | [Output Only] If the operation fails, this field contains the HTTP error
message that was returned, such as `NOT FOUND`. |
| `insert_time` | String | [Output Only] The time that this operation was requested.
This value is inRFC3339
text format. |
| `instances_bulk_insert_operation_metadata` | String |  |
| `self_link_with_id` | String | [Output Only] Server-defined URL for this resource with the resource id. |
| `warnings` | Vec<String> | [Output Only] If warning messages are generated during processing of the
operation, this field will be populated. |
| `zone` | String | [Output Only] The URL of the zone where the operation resides. Only
applicable when performing per-zone operations. |
| `id` | String | [Output Only] The unique identifier for the operation. This identifier is
defined by the server. |
| `set_common_instance_metadata_operation_metadata` | String | [Output Only] If the operation is for projects.setCommonInstanceMetadata,
this field will contain information on all underlying zonal actions and
their state. |
| `client_operation_id` | String | [Output Only] The value of `requestId` if you provided it in the request.
Not present otherwise. |
| `name` | String | [Output Only] Name of the operation. |
| `kind` | String | [Output Only] Type of the resource. Always `compute#operation` for
Operation resources. |
| `operation_group_id` | String | [Output Only] An ID that represents a group of operations, such as when a
group of operations results from a `bulkInsert` API request. |
| `user` | String | [Output Only] User who requested the operation, for example:
`user@example.com` or
`alice_smith_identifier (global/workforcePools/example-com-us-employees)`. |
| `error` | String | [Output Only] If errors are generated during processing of the operation,
this field will be populated. |
| `creation_timestamp` | String | [Deprecated] This field is deprecated. |
| `status_message` | String | [Output Only] An optional textual description of the current status of the
operation. |
| `target_id` | String | [Output Only] The unique target ID, which identifies a specific incarnation
of the target resource. |
| `target_link` | String | [Output Only] The URL of the resource that the operation modifies. For
operations related to creating a snapshot, this points to the disk
that the snapshot was created from. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Access global_folder_operation outputs
global_folder_operation_id = global_folder_operation.id
global_folder_operation_http_error_status_code = global_folder_operation.http_error_status_code
global_folder_operation_description = global_folder_operation.description
global_folder_operation_progress = global_folder_operation.progress
global_folder_operation_start_time = global_folder_operation.start_time
global_folder_operation_status = global_folder_operation.status
global_folder_operation_end_time = global_folder_operation.end_time
global_folder_operation_operation_type = global_folder_operation.operation_type
global_folder_operation_region = global_folder_operation.region
global_folder_operation_self_link = global_folder_operation.self_link
global_folder_operation_http_error_message = global_folder_operation.http_error_message
global_folder_operation_insert_time = global_folder_operation.insert_time
global_folder_operation_instances_bulk_insert_operation_metadata = global_folder_operation.instances_bulk_insert_operation_metadata
global_folder_operation_self_link_with_id = global_folder_operation.self_link_with_id
global_folder_operation_warnings = global_folder_operation.warnings
global_folder_operation_zone = global_folder_operation.zone
global_folder_operation_id = global_folder_operation.id
global_folder_operation_set_common_instance_metadata_operation_metadata = global_folder_operation.set_common_instance_metadata_operation_metadata
global_folder_operation_client_operation_id = global_folder_operation.client_operation_id
global_folder_operation_name = global_folder_operation.name
global_folder_operation_kind = global_folder_operation.kind
global_folder_operation_operation_group_id = global_folder_operation.operation_group_id
global_folder_operation_user = global_folder_operation.user
global_folder_operation_error = global_folder_operation.error
global_folder_operation_creation_timestamp = global_folder_operation.creation_timestamp
global_folder_operation_status_message = global_folder_operation.status_message
global_folder_operation_target_id = global_folder_operation.target_id
global_folder_operation_target_link = global_folder_operation.target_link
```

---


### Machine_image

Creates a machine image in the specified project using the
data that is included in the request. If you are creating a new machine
image to update an existing instance, your new machine image should use the
same network or, if applicable, the same subnetwork as the original
instance.

**Operations**: ✅ Create ✅ Read ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `machine_image_encryption_key` | String |  | Encrypts the machine image using acustomer-supplied
encryption key.

After you encrypt a machine image using a customer-supplied key, you must
provide the same key if you use the machine image later. For example, you
must provide the encryption key when you create an instance from the
encrypted machine image in a future request.

Customer-supplied encryption keys do not protect access to metadata of the
machine image.

If you do not provide an encryption key when creating the machine image,
then the machine image will be encrypted using an automatically generated
key and you do not need to provide a key to use the machine image later. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `saved_disks` | Vec<String> |  | An array of Machine Image specific properties for disks attached to the
source instance |
| `storage_locations` | Vec<String> |  | The regional or multi-regional Cloud Storage bucket location where themachine image is
stored. |
| `id` | String |  | [Output Only] A unique identifier for this machine image. The server
defines this identifier. |
| `satisfies_pzi` | bool |  | Output only. Reserved for future use. |
| `label_fingerprint` | String |  | A fingerprint for the labels being applied to this machine image, which is
essentially a hash of the labels set used for optimistic locking. The
fingerprint is initially generated by Compute Engine and changes after
every request to modify or update labels. You must always provide an
up-to-date fingerprint hash in order to update or change labels.

To see the latest fingerprint, make get() request to the
machine image. |
| `source_instance` | String |  | The source instance used to create the machine image. You can provide this
as a partial or full URL to the resource. For example, the following are
valid values: 
   
   
     - https://www.googleapis.com/compute/v1/projects/project/zones/zone/instances/instance 
   - projects/project/zones/zone/instances/instance |
| `source_disk_encryption_keys` | Vec<String> |  | [Input Only] Thecustomer-supplied
encryption key of the disks attached to the source instance. Required
if the source disk is protected by a customer-supplied encryption key. |
| `total_storage_bytes` | String |  | [Output Only] Total size of the storage used by the machine image. |
| `satisfies_pzs` | bool |  | [Output Only] Reserved for future use. |
| `creation_timestamp` | String |  | [Output Only] The creation timestamp for this machine image inRFC3339
text format. |
| `guest_flush` | bool |  | [Input Only] Whether to attempt an application consistent machine image by
informing the OS to prepare for the snapshot process. |
| `instance_properties` | String |  | [Output Only] Properties of source instance |
| `self_link_with_id` | String |  | [Output Only] Server-defined URL for this resource with the resource id. |
| `status` | String |  | [Output Only] The status of the machine image. One of the following values:INVALID, CREATING, READY,DELETING, and UPLOADING. |
| `name` | String |  | Name of the resource; provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must be
a dash, lowercase letter, or digit, except the last character, which cannot
be a dash. |
| `kind` | String |  | [Output Only] The resource type, which is alwayscompute#machineImage for machine image. |
| `self_link` | String |  | [Output Only] The URL for this machine image. The server defines this URL. |
| `source_instance_properties` | String |  | [Output Only] DEPRECATED: Please use instance_properties
instead for source instance related properties. New properties will not be
added to this field. |
| `labels` | HashMap<String, String> |  | Labels to apply to this machine image. These can be later modified by
the setLabels method. |
| `params` | String |  | Input only. [Input Only] Additional parameters that are passed in the request, but are
not persisted in the resource. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `machine_image_encryption_key` | String | Encrypts the machine image using acustomer-supplied
encryption key.

After you encrypt a machine image using a customer-supplied key, you must
provide the same key if you use the machine image later. For example, you
must provide the encryption key when you create an instance from the
encrypted machine image in a future request.

Customer-supplied encryption keys do not protect access to metadata of the
machine image.

If you do not provide an encryption key when creating the machine image,
then the machine image will be encrypted using an automatically generated
key and you do not need to provide a key to use the machine image later. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `saved_disks` | Vec<String> | An array of Machine Image specific properties for disks attached to the
source instance |
| `storage_locations` | Vec<String> | The regional or multi-regional Cloud Storage bucket location where themachine image is
stored. |
| `id` | String | [Output Only] A unique identifier for this machine image. The server
defines this identifier. |
| `satisfies_pzi` | bool | Output only. Reserved for future use. |
| `label_fingerprint` | String | A fingerprint for the labels being applied to this machine image, which is
essentially a hash of the labels set used for optimistic locking. The
fingerprint is initially generated by Compute Engine and changes after
every request to modify or update labels. You must always provide an
up-to-date fingerprint hash in order to update or change labels.

To see the latest fingerprint, make get() request to the
machine image. |
| `source_instance` | String | The source instance used to create the machine image. You can provide this
as a partial or full URL to the resource. For example, the following are
valid values: 
   
   
     - https://www.googleapis.com/compute/v1/projects/project/zones/zone/instances/instance 
   - projects/project/zones/zone/instances/instance |
| `source_disk_encryption_keys` | Vec<String> | [Input Only] Thecustomer-supplied
encryption key of the disks attached to the source instance. Required
if the source disk is protected by a customer-supplied encryption key. |
| `total_storage_bytes` | String | [Output Only] Total size of the storage used by the machine image. |
| `satisfies_pzs` | bool | [Output Only] Reserved for future use. |
| `creation_timestamp` | String | [Output Only] The creation timestamp for this machine image inRFC3339
text format. |
| `guest_flush` | bool | [Input Only] Whether to attempt an application consistent machine image by
informing the OS to prepare for the snapshot process. |
| `instance_properties` | String | [Output Only] Properties of source instance |
| `self_link_with_id` | String | [Output Only] Server-defined URL for this resource with the resource id. |
| `status` | String | [Output Only] The status of the machine image. One of the following values:INVALID, CREATING, READY,DELETING, and UPLOADING. |
| `name` | String | Name of the resource; provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must be
a dash, lowercase letter, or digit, except the last character, which cannot
be a dash. |
| `kind` | String | [Output Only] The resource type, which is alwayscompute#machineImage for machine image. |
| `self_link` | String | [Output Only] The URL for this machine image. The server defines this URL. |
| `source_instance_properties` | String | [Output Only] DEPRECATED: Please use instance_properties
instead for source instance related properties. New properties will not be
added to this field. |
| `labels` | HashMap<String, String> | Labels to apply to this machine image. These can be later modified by
the setLabels method. |
| `params` | String | Input only. [Input Only] Additional parameters that are passed in the request, but are
not persisted in the resource. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create machine_image
machine_image = provider.compute_api.Machine_image {
    project = "value"  # Project ID for this request.
}

# Access machine_image outputs
machine_image_id = machine_image.id
machine_image_machine_image_encryption_key = machine_image.machine_image_encryption_key
machine_image_description = machine_image.description
machine_image_saved_disks = machine_image.saved_disks
machine_image_storage_locations = machine_image.storage_locations
machine_image_id = machine_image.id
machine_image_satisfies_pzi = machine_image.satisfies_pzi
machine_image_label_fingerprint = machine_image.label_fingerprint
machine_image_source_instance = machine_image.source_instance
machine_image_source_disk_encryption_keys = machine_image.source_disk_encryption_keys
machine_image_total_storage_bytes = machine_image.total_storage_bytes
machine_image_satisfies_pzs = machine_image.satisfies_pzs
machine_image_creation_timestamp = machine_image.creation_timestamp
machine_image_guest_flush = machine_image.guest_flush
machine_image_instance_properties = machine_image.instance_properties
machine_image_self_link_with_id = machine_image.self_link_with_id
machine_image_status = machine_image.status
machine_image_name = machine_image.name
machine_image_kind = machine_image.kind
machine_image_self_link = machine_image.self_link
machine_image_source_instance_properties = machine_image.source_instance_properties
machine_image_labels = machine_image.labels
machine_image_params = machine_image.params
```

---


### Zone_vm_extension_policie

Creates a new zone-level VM extension policy within a project.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `extension_policies` | HashMap<String, String> |  | Required. A map of extension names (e.g., "cloudops") to their corresponding policy
configurations. |
| `instance_selectors` | Vec<String> |  | Optional. Selectors to target VMs for this policy. VMs are selected if they match
*any* of the provided selectors (logical OR). If this list is empty, the
policy applies to all VMs. |
| `priority` | i64 |  | Optional. Priority of this policy. Used to resolve conflicts when multiple policies
apply to the same extension.
The policy priority is an integer from 0 to 65535, inclusive. Lower
integers indicate higher priorities. If you do not specify a priority when
creating a rule, it is assigned a priority of 1000. If priorities are
equal, the policy with the more recent creation timestamp takes precedence. |
| `kind` | String |  | [Output Only] Type of the resource. Alwayscompute#vmExtensionPolicy. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `global_resource_link` | String |  | Optional. [Output Only] Link to the global policy that manages this zone policy, if
applicable. |
| `self_link_with_id` | String |  | [Output Only] Server-defined URL for this resource's resource id. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `self_link` | String |  | [Output Only] Server-defined fully-qualified URL for this resource. |
| `state` | String |  | Optional. [Output Only] Current state of the policy: ACTIVE or DELETING. |
| `name` | String |  | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?`
which means the first character must be a lowercase letter, and all
following characters must be a dash, lowercase letter, or digit, except
the last character, which cannot be a dash. |
| `managed_by_global` | bool |  | Optional. [Output Only] Indicates if this policy is managed by a global policy. |
| `update_timestamp` | String |  | [Output Only] Update timestamp inRFC3339
text format. |
| `description` | String |  | An optional description of this resource. |
| `zone` | String | ✅ | Name of the zone for this request. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `extension_policies` | HashMap<String, String> | Required. A map of extension names (e.g., "cloudops") to their corresponding policy
configurations. |
| `instance_selectors` | Vec<String> | Optional. Selectors to target VMs for this policy. VMs are selected if they match
*any* of the provided selectors (logical OR). If this list is empty, the
policy applies to all VMs. |
| `priority` | i64 | Optional. Priority of this policy. Used to resolve conflicts when multiple policies
apply to the same extension.
The policy priority is an integer from 0 to 65535, inclusive. Lower
integers indicate higher priorities. If you do not specify a priority when
creating a rule, it is assigned a priority of 1000. If priorities are
equal, the policy with the more recent creation timestamp takes precedence. |
| `kind` | String | [Output Only] Type of the resource. Alwayscompute#vmExtensionPolicy. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `global_resource_link` | String | Optional. [Output Only] Link to the global policy that manages this zone policy, if
applicable. |
| `self_link_with_id` | String | [Output Only] Server-defined URL for this resource's resource id. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `self_link` | String | [Output Only] Server-defined fully-qualified URL for this resource. |
| `state` | String | Optional. [Output Only] Current state of the policy: ACTIVE or DELETING. |
| `name` | String | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?`
which means the first character must be a lowercase letter, and all
following characters must be a dash, lowercase letter, or digit, except
the last character, which cannot be a dash. |
| `managed_by_global` | bool | Optional. [Output Only] Indicates if this policy is managed by a global policy. |
| `update_timestamp` | String | [Output Only] Update timestamp inRFC3339
text format. |
| `description` | String | An optional description of this resource. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create zone_vm_extension_policie
zone_vm_extension_policie = provider.compute_api.Zone_vm_extension_policie {
    zone = "value"  # Name of the zone for this request.
    project = "value"  # Project ID for this request.
}

# Access zone_vm_extension_policie outputs
zone_vm_extension_policie_id = zone_vm_extension_policie.id
zone_vm_extension_policie_extension_policies = zone_vm_extension_policie.extension_policies
zone_vm_extension_policie_instance_selectors = zone_vm_extension_policie.instance_selectors
zone_vm_extension_policie_priority = zone_vm_extension_policie.priority
zone_vm_extension_policie_kind = zone_vm_extension_policie.kind
zone_vm_extension_policie_creation_timestamp = zone_vm_extension_policie.creation_timestamp
zone_vm_extension_policie_global_resource_link = zone_vm_extension_policie.global_resource_link
zone_vm_extension_policie_self_link_with_id = zone_vm_extension_policie.self_link_with_id
zone_vm_extension_policie_id = zone_vm_extension_policie.id
zone_vm_extension_policie_self_link = zone_vm_extension_policie.self_link
zone_vm_extension_policie_state = zone_vm_extension_policie.state
zone_vm_extension_policie_name = zone_vm_extension_policie.name
zone_vm_extension_policie_managed_by_global = zone_vm_extension_policie.managed_by_global
zone_vm_extension_policie_update_timestamp = zone_vm_extension_policie.update_timestamp
zone_vm_extension_policie_description = zone_vm_extension_policie.description
```

---


### Instant_snapshot_group

inserts a Zonal InstantSnapshotGroup resource

**Operations**: ✅ Create ✅ Read ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `kind` | String |  | [Output Only] Type of the resource. Alwayscompute#instantSnapshotGroup for InstantSnapshotGroup
resources. |
| `description` | String |  | Optional. An optional description of this resource. Provide this property when you
create the resource. |
| `region` | String |  | [Output Only] URL of the region where the instant snapshot group resides.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `resource_status` | String |  |  |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `self_link_with_id` | String |  | [Output Only] Server-defined URL for this resource's resource id. |
| `source_consistency_group` | String |  |  |
| `name` | String |  | Identifier. Name of the resource; provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must be
a dash, lowercase letter, or digit, except the last character, which cannot
be a dash. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `status` | String |  | [Output Only] |
| `zone` | String |  | [Output Only] URL of the zone where the instant snapshot group resides.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `project` | String | ✅ | Project ID for this request. |
| `zone` | String | ✅ | Name of the zone for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `kind` | String | [Output Only] Type of the resource. Alwayscompute#instantSnapshotGroup for InstantSnapshotGroup
resources. |
| `description` | String | Optional. An optional description of this resource. Provide this property when you
create the resource. |
| `region` | String | [Output Only] URL of the region where the instant snapshot group resides.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `resource_status` | String |  |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `self_link_with_id` | String | [Output Only] Server-defined URL for this resource's resource id. |
| `source_consistency_group` | String |  |
| `name` | String | Identifier. Name of the resource; provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must be
a dash, lowercase letter, or digit, except the last character, which cannot
be a dash. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `status` | String | [Output Only] |
| `zone` | String | [Output Only] URL of the zone where the instant snapshot group resides.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create instant_snapshot_group
instant_snapshot_group = provider.compute_api.Instant_snapshot_group {
    project = "value"  # Project ID for this request.
    zone = "value"  # Name of the zone for this request.
}

# Access instant_snapshot_group outputs
instant_snapshot_group_id = instant_snapshot_group.id
instant_snapshot_group_kind = instant_snapshot_group.kind
instant_snapshot_group_description = instant_snapshot_group.description
instant_snapshot_group_region = instant_snapshot_group.region
instant_snapshot_group_resource_status = instant_snapshot_group.resource_status
instant_snapshot_group_self_link = instant_snapshot_group.self_link
instant_snapshot_group_self_link_with_id = instant_snapshot_group.self_link_with_id
instant_snapshot_group_source_consistency_group = instant_snapshot_group.source_consistency_group
instant_snapshot_group_name = instant_snapshot_group.name
instant_snapshot_group_creation_timestamp = instant_snapshot_group.creation_timestamp
instant_snapshot_group_status = instant_snapshot_group.status
instant_snapshot_group_zone = instant_snapshot_group.zone
instant_snapshot_group_id = instant_snapshot_group.id
```

---


### Target_grpc_proxie

Creates a TargetGrpcProxy in the specified project in the given scope
using the parameters that are included in the request.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `self_link_with_id` | String |  | [Output Only] Server-defined URL with id for the resource. |
| `validate_for_proxyless` | bool |  | If true, indicates that the BackendServices referenced by the urlMap may be
accessed by gRPC applications without using a sidecar proxy. This will
enable configuration checks on urlMap and its referenced BackendServices to
not allow unsupported features. A gRPC application must use
"xds:///" scheme in the target URI of the service it is
connecting to.
If false, indicates that the BackendServices referenced by the urlMap will
be accessed by gRPC applications via a sidecar proxy. In this case, a gRPC
application must not use "xds:///" scheme in the target URI of
the service it is connecting to |
| `id` | String |  | [Output Only] The unique identifier for the resource type. The server
generates this identifier. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `fingerprint` | String |  | Fingerprint of this resource. A hash of the contents stored in this object.
This field is used in optimistic locking. This field will be ignored when
inserting a TargetGrpcProxy. An up-to-date fingerprint must
be provided in order to patch/update the TargetGrpcProxy; otherwise, the
request will fail with error 412 conditionNotMet. To see the
latest fingerprint, make a get() request to retrieve the
TargetGrpcProxy. |
| `url_map` | String |  | URL to the UrlMap resource that defines the mapping from URL to the
BackendService. The protocol field in the BackendService must be set to
GRPC. |
| `kind` | String |  | [Output Only] Type of the resource. Alwayscompute#targetGrpcProxy for target grpc proxies. |
| `name` | String |  | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `self_link_with_id` | String | [Output Only] Server-defined URL with id for the resource. |
| `validate_for_proxyless` | bool | If true, indicates that the BackendServices referenced by the urlMap may be
accessed by gRPC applications without using a sidecar proxy. This will
enable configuration checks on urlMap and its referenced BackendServices to
not allow unsupported features. A gRPC application must use
"xds:///" scheme in the target URI of the service it is
connecting to.
If false, indicates that the BackendServices referenced by the urlMap will
be accessed by gRPC applications via a sidecar proxy. In this case, a gRPC
application must not use "xds:///" scheme in the target URI of
the service it is connecting to |
| `id` | String | [Output Only] The unique identifier for the resource type. The server
generates this identifier. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `fingerprint` | String | Fingerprint of this resource. A hash of the contents stored in this object.
This field is used in optimistic locking. This field will be ignored when
inserting a TargetGrpcProxy. An up-to-date fingerprint must
be provided in order to patch/update the TargetGrpcProxy; otherwise, the
request will fail with error 412 conditionNotMet. To see the
latest fingerprint, make a get() request to retrieve the
TargetGrpcProxy. |
| `url_map` | String | URL to the UrlMap resource that defines the mapping from URL to the
BackendService. The protocol field in the BackendService must be set to
GRPC. |
| `kind` | String | [Output Only] Type of the resource. Alwayscompute#targetGrpcProxy for target grpc proxies. |
| `name` | String | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create target_grpc_proxie
target_grpc_proxie = provider.compute_api.Target_grpc_proxie {
    project = "value"  # Project ID for this request.
}

# Access target_grpc_proxie outputs
target_grpc_proxie_id = target_grpc_proxie.id
target_grpc_proxie_self_link = target_grpc_proxie.self_link
target_grpc_proxie_self_link_with_id = target_grpc_proxie.self_link_with_id
target_grpc_proxie_validate_for_proxyless = target_grpc_proxie.validate_for_proxyless
target_grpc_proxie_id = target_grpc_proxie.id
target_grpc_proxie_description = target_grpc_proxie.description
target_grpc_proxie_creation_timestamp = target_grpc_proxie.creation_timestamp
target_grpc_proxie_fingerprint = target_grpc_proxie.fingerprint
target_grpc_proxie_url_map = target_grpc_proxie.url_map
target_grpc_proxie_kind = target_grpc_proxie.kind
target_grpc_proxie_name = target_grpc_proxie.name
```

---


### Zone_queued_resource

Creates a QueuedResource.

**Operations**: ✅ Create ✅ Read ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `bulk_insert_instance_resource` | String |  | Specification of VM instances to create. |
| `queuing_policy` | String |  | Queuing parameters for the requested capacity. |
| `self_link` | String |  | [Output only] Server-defined URL for the resource. |
| `state` | String |  | [Output only] High-level status of the request. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp in RFC3339
text format. |
| `name` | String |  | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first character
must be a lowercase letter, and all following characters must be a dash,
lowercase letter, or digit, except the last character, which cannot be a
dash. |
| `kind` | String |  | [Output Only] Type of the resource. Alwayscompute#queuedResource for QueuedResources. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `self_link_with_id` | String |  | [Output Only] Server-defined URL for this resource with the resource id. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `status` | String |  | [Output only] Result of queuing and provisioning based on deferred
capacity. |
| `zone` | String |  | [Output Only] URL of the zone where the resource resides. Only applicable
for zonal resources. You must specify this field as part of the HTTP
request URL. It is not settable as a field in the request body. |
| `project` | String | ✅ | Project ID for this request. |
| `zone` | String | ✅ | Name of the zone for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `bulk_insert_instance_resource` | String | Specification of VM instances to create. |
| `queuing_policy` | String | Queuing parameters for the requested capacity. |
| `self_link` | String | [Output only] Server-defined URL for the resource. |
| `state` | String | [Output only] High-level status of the request. |
| `creation_timestamp` | String | [Output Only] Creation timestamp in RFC3339
text format. |
| `name` | String | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first character
must be a lowercase letter, and all following characters must be a dash,
lowercase letter, or digit, except the last character, which cannot be a
dash. |
| `kind` | String | [Output Only] Type of the resource. Alwayscompute#queuedResource for QueuedResources. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `self_link_with_id` | String | [Output Only] Server-defined URL for this resource with the resource id. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `status` | String | [Output only] Result of queuing and provisioning based on deferred
capacity. |
| `zone` | String | [Output Only] URL of the zone where the resource resides. Only applicable
for zonal resources. You must specify this field as part of the HTTP
request URL. It is not settable as a field in the request body. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create zone_queued_resource
zone_queued_resource = provider.compute_api.Zone_queued_resource {
    project = "value"  # Project ID for this request.
    zone = "value"  # Name of the zone for this request.
}

# Access zone_queued_resource outputs
zone_queued_resource_id = zone_queued_resource.id
zone_queued_resource_bulk_insert_instance_resource = zone_queued_resource.bulk_insert_instance_resource
zone_queued_resource_queuing_policy = zone_queued_resource.queuing_policy
zone_queued_resource_self_link = zone_queued_resource.self_link
zone_queued_resource_state = zone_queued_resource.state
zone_queued_resource_creation_timestamp = zone_queued_resource.creation_timestamp
zone_queued_resource_name = zone_queued_resource.name
zone_queued_resource_kind = zone_queued_resource.kind
zone_queued_resource_id = zone_queued_resource.id
zone_queued_resource_self_link_with_id = zone_queued_resource.self_link_with_id
zone_queued_resource_description = zone_queued_resource.description
zone_queued_resource_status = zone_queued_resource.status
zone_queued_resource_zone = zone_queued_resource.zone
```

---


### Region_commitment

Creates a commitment in the specified project using the data
included in the request.

**Operations**: ✅ Create ✅ Read ✅ Update

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `auto_renew` | bool |  | Specifies whether to automatically renew the commitment at the end of its
current term. The default value is false. If you set the field
to true, each time your commitment reaches the end of its
term, Compute Engine automatically renews it for another term. You can
update this field anytime before the commitment expires. For example, if
the commitment is set to expire at 12 AM UTC-8 on January 3, 2027, you can
update this field until 11:59 PM UTC-8 on January 2, 2027. |
| `kind` | String |  | [Output Only] Type of the resource. Always compute#commitment
for commitments. |
| `region` | String |  | [Output Only] URL of the region where the commitment and committed
resources are located. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `existing_reservations` | Vec<String> |  |  |
| `license_resource` | String |  | The license specification required as part of a license commitment. |
| `type` | String |  | The type of commitment; specifies the
machine series for which you want to commit to purchasing resources.
The choice of machine series affects the discount rate and the eligible
resource types.

 The type must be one of the following:ACCELERATOR_OPTIMIZED, ACCELERATOR_OPTIMIZED_A3,ACCELERATOR_OPTIMIZED_A3_MEGA,COMPUTE_OPTIMIZED, COMPUTE_OPTIMIZED_C2D,
 COMPUTE_OPTIMIZED_C3, COMPUTE_OPTIMIZED_C3D,COMPUTE_OPTIMIZED_H3, GENERAL_PURPOSE,GENERAL_PURPOSE_C4, GENERAL_PURPOSE_E2,GENERAL_PURPOSE_N2, GENERAL_PURPOSE_N2D,GENERAL_PURPOSE_N4, GENERAL_PURPOSE_T2D,GRAPHICS_OPTIMIZED, MEMORY_OPTIMIZED,MEMORY_OPTIMIZED_M3, MEMORY_OPTIMIZED_X4,STORAGE_OPTIMIZED_Z3.
For example, type MEMORY_OPTIMIZED specifies a commitment
that applies only to eligible resources of memory optimized M1 and M2
machine series. Type GENERAL_PURPOSE specifies a commitment
that applies only to eligible resources of general purpose N1 machine
series. |
| `name` | String |  | Name of the commitment. You must specify a name when you purchase the
commitment. The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `plan` | String |  | The minimum time duration that you commit to purchasing resources.
The plan that you choose determines the preset term length of the
commitment (which is 1 year or 3 years) and affects the discount rate that
you receive for your resources. Committing to a longer time duration
typically gives you a higher discount rate. The supported values for this
field are TWELVE_MONTH (1 year), andTHIRTY_SIX_MONTH (3 years). |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `split_source_commitment` | String |  | The source commitment from which you are transferring resources to create
the new split commitment. For more information, see 
Split commitments. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `category` | String |  | The category of the commitment; specifies whether the commitment is for
hardware or software resources. Category MACHINE specifies
that you are committing to hardware machine resources such asVCPU or MEMORY, listed in resources.
Category LICENSE specifies that you are committing to software
licenses, listed in licenseResources.
Note that if you specify MACHINE commitments, then you must
also specify a type to indicate the machine series of the
hardware resource that you are committing to. |
| `status_message` | String |  | [Output Only] An optional, human-readable explanation of the status. |
| `status` | String |  | [Output Only] Status of the commitment with regards to eventual expiration
(each commitment has an end date defined). Status can be one of the
following values: NOT_YET_ACTIVE, ACTIVE, orEXPIRED. |
| `start_timestamp` | String |  | [Output Only] Commitment start time inRFC3339
text format. |
| `resource_status` | String |  | [Output Only] Status information for Commitment resource. |
| `resources` | Vec<String> |  | The list of all the hardware resources, with their types and amounts, that
you want to commit to. Specify as a separate entry in the list for each
individual resource type. |
| `merge_source_commitments` | Vec<String> |  | The list of source commitments that you are merging to create the new
merged commitment. For more information, see
Merging commitments. |
| `description` | String |  | An optional description of the commitment. You can provide this property
when you create the resource. |
| `reservations` | Vec<String> |  | The list of new reservations that you want to create and attach to this
commitment.

You must attach reservations to your commitment if your commitment
specifies any GPUs or Local SSD disks. For more information, see 
Attach reservations to resource-based commitments.

Specify this property only if you want to create new
reservations to attach. To attach existing reservations, specify theexistingReservations property instead. |
| `custom_end_timestamp` | String |  | [Input Only] Optional, specifies the requested commitment end time inRFC3339 text format. Use this option when the desired
commitment's end date is later than the start date + term duration. |
| `self_link_with_id` | String |  | [Output Only] Server-defined URL for this resource with the resource id. |
| `end_timestamp` | String |  | [Output Only] Commitment end time inRFC3339
text format. |
| `region` | String | ✅ | Name of the region for this request. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `auto_renew` | bool | Specifies whether to automatically renew the commitment at the end of its
current term. The default value is false. If you set the field
to true, each time your commitment reaches the end of its
term, Compute Engine automatically renews it for another term. You can
update this field anytime before the commitment expires. For example, if
the commitment is set to expire at 12 AM UTC-8 on January 3, 2027, you can
update this field until 11:59 PM UTC-8 on January 2, 2027. |
| `kind` | String | [Output Only] Type of the resource. Always compute#commitment
for commitments. |
| `region` | String | [Output Only] URL of the region where the commitment and committed
resources are located. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `existing_reservations` | Vec<String> |  |
| `license_resource` | String | The license specification required as part of a license commitment. |
| `type` | String | The type of commitment; specifies the
machine series for which you want to commit to purchasing resources.
The choice of machine series affects the discount rate and the eligible
resource types.

 The type must be one of the following:ACCELERATOR_OPTIMIZED, ACCELERATOR_OPTIMIZED_A3,ACCELERATOR_OPTIMIZED_A3_MEGA,COMPUTE_OPTIMIZED, COMPUTE_OPTIMIZED_C2D,
 COMPUTE_OPTIMIZED_C3, COMPUTE_OPTIMIZED_C3D,COMPUTE_OPTIMIZED_H3, GENERAL_PURPOSE,GENERAL_PURPOSE_C4, GENERAL_PURPOSE_E2,GENERAL_PURPOSE_N2, GENERAL_PURPOSE_N2D,GENERAL_PURPOSE_N4, GENERAL_PURPOSE_T2D,GRAPHICS_OPTIMIZED, MEMORY_OPTIMIZED,MEMORY_OPTIMIZED_M3, MEMORY_OPTIMIZED_X4,STORAGE_OPTIMIZED_Z3.
For example, type MEMORY_OPTIMIZED specifies a commitment
that applies only to eligible resources of memory optimized M1 and M2
machine series. Type GENERAL_PURPOSE specifies a commitment
that applies only to eligible resources of general purpose N1 machine
series. |
| `name` | String | Name of the commitment. You must specify a name when you purchase the
commitment. The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `plan` | String | The minimum time duration that you commit to purchasing resources.
The plan that you choose determines the preset term length of the
commitment (which is 1 year or 3 years) and affects the discount rate that
you receive for your resources. Committing to a longer time duration
typically gives you a higher discount rate. The supported values for this
field are TWELVE_MONTH (1 year), andTHIRTY_SIX_MONTH (3 years). |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `split_source_commitment` | String | The source commitment from which you are transferring resources to create
the new split commitment. For more information, see 
Split commitments. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `category` | String | The category of the commitment; specifies whether the commitment is for
hardware or software resources. Category MACHINE specifies
that you are committing to hardware machine resources such asVCPU or MEMORY, listed in resources.
Category LICENSE specifies that you are committing to software
licenses, listed in licenseResources.
Note that if you specify MACHINE commitments, then you must
also specify a type to indicate the machine series of the
hardware resource that you are committing to. |
| `status_message` | String | [Output Only] An optional, human-readable explanation of the status. |
| `status` | String | [Output Only] Status of the commitment with regards to eventual expiration
(each commitment has an end date defined). Status can be one of the
following values: NOT_YET_ACTIVE, ACTIVE, orEXPIRED. |
| `start_timestamp` | String | [Output Only] Commitment start time inRFC3339
text format. |
| `resource_status` | String | [Output Only] Status information for Commitment resource. |
| `resources` | Vec<String> | The list of all the hardware resources, with their types and amounts, that
you want to commit to. Specify as a separate entry in the list for each
individual resource type. |
| `merge_source_commitments` | Vec<String> | The list of source commitments that you are merging to create the new
merged commitment. For more information, see
Merging commitments. |
| `description` | String | An optional description of the commitment. You can provide this property
when you create the resource. |
| `reservations` | Vec<String> | The list of new reservations that you want to create and attach to this
commitment.

You must attach reservations to your commitment if your commitment
specifies any GPUs or Local SSD disks. For more information, see 
Attach reservations to resource-based commitments.

Specify this property only if you want to create new
reservations to attach. To attach existing reservations, specify theexistingReservations property instead. |
| `custom_end_timestamp` | String | [Input Only] Optional, specifies the requested commitment end time inRFC3339 text format. Use this option when the desired
commitment's end date is later than the start date + term duration. |
| `self_link_with_id` | String | [Output Only] Server-defined URL for this resource with the resource id. |
| `end_timestamp` | String | [Output Only] Commitment end time inRFC3339
text format. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create region_commitment
region_commitment = provider.compute_api.Region_commitment {
    region = "value"  # Name of the region for this request.
    project = "value"  # Project ID for this request.
}

# Access region_commitment outputs
region_commitment_id = region_commitment.id
region_commitment_auto_renew = region_commitment.auto_renew
region_commitment_kind = region_commitment.kind
region_commitment_region = region_commitment.region
region_commitment_self_link = region_commitment.self_link
region_commitment_existing_reservations = region_commitment.existing_reservations
region_commitment_license_resource = region_commitment.license_resource
region_commitment_type = region_commitment.type
region_commitment_name = region_commitment.name
region_commitment_plan = region_commitment.plan
region_commitment_id = region_commitment.id
region_commitment_split_source_commitment = region_commitment.split_source_commitment
region_commitment_creation_timestamp = region_commitment.creation_timestamp
region_commitment_category = region_commitment.category
region_commitment_status_message = region_commitment.status_message
region_commitment_status = region_commitment.status
region_commitment_start_timestamp = region_commitment.start_timestamp
region_commitment_resource_status = region_commitment.resource_status
region_commitment_resources = region_commitment.resources
region_commitment_merge_source_commitments = region_commitment.merge_source_commitments
region_commitment_description = region_commitment.description
region_commitment_reservations = region_commitment.reservations
region_commitment_custom_end_timestamp = region_commitment.custom_end_timestamp
region_commitment_self_link_with_id = region_commitment.self_link_with_id
region_commitment_end_timestamp = region_commitment.end_timestamp
```

---


### Reservation_block

Allows customers to perform maintenance on a reservation block

**Operations**: ✅ Create ✅ Read

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `maintenance_scope` | String |  | Specifies if all, running or unused hosts are in scope for this request. |
| `reservation` | String | ✅ | The name of the reservation.
Name should conform to RFC1035 or be a resource ID. |
| `reservation_block` | String | ✅ | The name of the reservation block.
Name should conform to RFC1035 or be a resource ID. |
| `zone` | String | ✅ | Name of the zone for this request. Zone name should conform to RFC1035. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `resource` | String |  |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create reservation_block
reservation_block = provider.compute_api.Reservation_block {
    reservation = "value"  # The name of the reservation.
Name should conform to RFC1035 or be a resource ID.
    reservation_block = "value"  # The name of the reservation block.
Name should conform to RFC1035 or be a resource ID.
    zone = "value"  # Name of the zone for this request. Zone name should conform to RFC1035.
    project = "value"  # Project ID for this request.
}

# Access reservation_block outputs
reservation_block_id = reservation_block.id
reservation_block_resource = reservation_block.resource
```

---


### Region_multi_mig_member

Retrieves information about the specified multi-MIG member.

**Operations**: ✅ Read

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `id` | String | [Output only] The unique identifier for this resource type. The server
generates this identifier. |
| `status` | String | [Output Only] The status of this multi-MIG member |
| `name` | String | [Output Only] The name of this multi-MIG member generated by
Google Compute Engine. |
| `region` | String | [Output Only] The URL of the region where the multi-MIG resides. |
| `self_link` | String | [Output Only] Server-defined fully-qualified URL for this resource. |
| `kind` | String | Type of the resource. Alwayscompute#multiMigMember for a list of multi-MIG members. |
| `creation_timestamp` | String | [Output Only] Creation timestamp of this multi-MIG member inRFC3339 text format. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Access region_multi_mig_member outputs
region_multi_mig_member_id = region_multi_mig_member.id
region_multi_mig_member_id = region_multi_mig_member.id
region_multi_mig_member_status = region_multi_mig_member.status
region_multi_mig_member_name = region_multi_mig_member.name
region_multi_mig_member_region = region_multi_mig_member.region
region_multi_mig_member_self_link = region_multi_mig_member.self_link
region_multi_mig_member_kind = region_multi_mig_member.kind
region_multi_mig_member_creation_timestamp = region_multi_mig_member.creation_timestamp
```

---


### Vpn_tunnel

Creates a VpnTunnel resource in the specified project and region using
the data included in the request.

**Operations**: ✅ Create ✅ Read ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `remote_traffic_selector` | Vec<String> |  | Remote traffic selectors to use when establishing the VPN tunnel with
the peer VPN gateway. The value should be a CIDR formatted string,
for example: 192.168.0.0/16. The ranges should be disjoint. Only IPv4 is
supported for Classic VPN tunnels. This field is output only for HA VPN
tunnels. |
| `peer_ip` | String |  | IP address of the peer VPN gateway. Only IPv4 is supported. This field can
be set only for Classic VPN tunnels. |
| `kind` | String |  | [Output Only] Type of resource. Always compute#vpnTunnel for
VPN tunnels. |
| `params` | String |  | Input only. [Input Only] Additional params passed with the request, but not persisted
as part of resource payload. |
| `region` | String |  | [Output Only] URL of the region where the VPN tunnel resides.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `router` | String |  | URL of the router resource to be used for dynamic routing. |
| `label_fingerprint` | String |  | A fingerprint for the labels being applied to this VpnTunnel, which is
essentially a hash of the labels set used for optimistic locking. The
fingerprint is initially generated by Compute Engine and changes after
every request to modify or update labels. You must always provide an
up-to-date fingerprint hash in order to update or change labels,
otherwise the request will fail with error412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve a VpnTunnel. |
| `detailed_status` | String |  | [Output Only] Detailed status message for the VPN tunnel. |
| `shared_secret` | String |  | Shared secret used to set the secure session between the Cloud VPN gateway
and the peer VPN gateway. |
| `status` | String |  | [Output Only] The status of the VPN tunnel, which can be one of the
following:
   
   - PROVISIONING: Resource is being allocated for the VPN tunnel.
   - WAITING_FOR_FULL_CONFIG: Waiting to receive all VPN-related configs
   from
     the user. Network, TargetVpnGateway, VpnTunnel, ForwardingRule, and Route
     resources are needed to setup the VPN tunnel.
   - FIRST_HANDSHAKE: Successful first handshake with the peer VPN.
   - ESTABLISHED: Secure session is successfully established with the peer
   VPN. 
   - NETWORK_ERROR: Deprecated, replaced by
   NO_INCOMING_PACKETS 
   - AUTHORIZATION_ERROR: Auth error (for example,
   bad shared secret). 
   - NEGOTIATION_FAILURE: Handshake failed.
   - DEPROVISIONING: Resources are being deallocated for the VPN
   tunnel. 
   - FAILED: Tunnel creation has failed and the tunnel is not
   ready to be used. 
   - NO_INCOMING_PACKETS: No incoming packets from
   peer. 
   - REJECTED: Tunnel configuration was rejected, can be result
   of being denied access. 
   - ALLOCATING_RESOURCES: Cloud VPN is in the
   process of allocating all required resources.
   - STOPPED: Tunnel is stopped due to its Forwarding Rules being deleted
   for Classic VPN tunnels or the project is in frozen state.
   - PEER_IDENTITY_MISMATCH: Peer identity does not match peer IP,
   probably behind NAT. 
   - TS_NARROWING_NOT_ALLOWED: Traffic selector
   narrowing not allowed for an HA-VPN tunnel. |
| `target_vpn_gateway` | String |  | URL of the Target VPN gateway with which this VPN tunnel is associated.
Provided by the client when the VPN tunnel is created.
This field can be set only for Classic VPN tunnels. |
| `vpn_gateway` | String |  | URL of the VPN gateway with which this VPN tunnel is associated.
Provided by the client when the VPN tunnel is created. This must be
used (instead of target_vpn_gateway) if a High Availability VPN gateway
resource is created. |
| `local_traffic_selector` | Vec<String> |  | Local traffic selector to use when establishing the VPN tunnel with the
peer VPN gateway. The value should be a CIDR formatted string, for
example: 192.168.0.0/16. The ranges must be disjoint.
Only IPv4 is supported for Classic VPN tunnels. This field is output only
for HA VPN tunnels. |
| `cipher_suite` | String |  | User specified list of ciphers to use for the phase 1 and phase 2 of the
IKE protocol. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `labels` | HashMap<String, String> |  | Labels for this resource. These can only be added or modified by thesetLabels method. Each label key/value pair must comply withRFC1035.
Label values may be empty. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `name` | String |  | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `peer_external_gateway_interface` | i64 |  | The interface ID of the external VPN gateway to which this VPN tunnel is
connected. Provided by the client when the VPN tunnel is created.
Possible values are: `0`, `1`, `2`, `3`. The number of IDs in use
depends on the external VPN gateway redundancy type. |
| `peer_gcp_gateway` | String |  | URL of the peer side HA VPN gateway to which this VPN tunnel
is connected. Provided by the client when the VPN tunnel is created.
This field can be used when creating highly available VPN from VPC network
to VPC network, the field is exclusive with the field peerExternalGateway.
If provided, the VPN tunnel will automatically use the same
vpnGatewayInterface ID in the peer Google Cloud VPN gateway. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `shared_secret_hash` | String |  | Hash of the shared secret. |
| `ike_version` | i64 |  | IKE protocol version to use when establishing the VPN tunnel with the peer
VPN gateway. Acceptable IKE versions are 1 or 2.
The default version is 2. |
| `vpn_gateway_interface` | i64 |  | The interface ID of the VPN gateway with which this VPN tunnel is
associated.
Possible values are: `0`, `1`. |
| `peer_external_gateway` | String |  | URL of the peer side external VPN gateway to which this VPN tunnel is
connected.
Provided by the client when the VPN tunnel is created.
This field is exclusive with the field peerGcpGateway. |
| `region` | String | ✅ | Name of the region for this request. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `remote_traffic_selector` | Vec<String> | Remote traffic selectors to use when establishing the VPN tunnel with
the peer VPN gateway. The value should be a CIDR formatted string,
for example: 192.168.0.0/16. The ranges should be disjoint. Only IPv4 is
supported for Classic VPN tunnels. This field is output only for HA VPN
tunnels. |
| `peer_ip` | String | IP address of the peer VPN gateway. Only IPv4 is supported. This field can
be set only for Classic VPN tunnels. |
| `kind` | String | [Output Only] Type of resource. Always compute#vpnTunnel for
VPN tunnels. |
| `params` | String | Input only. [Input Only] Additional params passed with the request, but not persisted
as part of resource payload. |
| `region` | String | [Output Only] URL of the region where the VPN tunnel resides.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `router` | String | URL of the router resource to be used for dynamic routing. |
| `label_fingerprint` | String | A fingerprint for the labels being applied to this VpnTunnel, which is
essentially a hash of the labels set used for optimistic locking. The
fingerprint is initially generated by Compute Engine and changes after
every request to modify or update labels. You must always provide an
up-to-date fingerprint hash in order to update or change labels,
otherwise the request will fail with error412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve a VpnTunnel. |
| `detailed_status` | String | [Output Only] Detailed status message for the VPN tunnel. |
| `shared_secret` | String | Shared secret used to set the secure session between the Cloud VPN gateway
and the peer VPN gateway. |
| `status` | String | [Output Only] The status of the VPN tunnel, which can be one of the
following:
   
   - PROVISIONING: Resource is being allocated for the VPN tunnel.
   - WAITING_FOR_FULL_CONFIG: Waiting to receive all VPN-related configs
   from
     the user. Network, TargetVpnGateway, VpnTunnel, ForwardingRule, and Route
     resources are needed to setup the VPN tunnel.
   - FIRST_HANDSHAKE: Successful first handshake with the peer VPN.
   - ESTABLISHED: Secure session is successfully established with the peer
   VPN. 
   - NETWORK_ERROR: Deprecated, replaced by
   NO_INCOMING_PACKETS 
   - AUTHORIZATION_ERROR: Auth error (for example,
   bad shared secret). 
   - NEGOTIATION_FAILURE: Handshake failed.
   - DEPROVISIONING: Resources are being deallocated for the VPN
   tunnel. 
   - FAILED: Tunnel creation has failed and the tunnel is not
   ready to be used. 
   - NO_INCOMING_PACKETS: No incoming packets from
   peer. 
   - REJECTED: Tunnel configuration was rejected, can be result
   of being denied access. 
   - ALLOCATING_RESOURCES: Cloud VPN is in the
   process of allocating all required resources.
   - STOPPED: Tunnel is stopped due to its Forwarding Rules being deleted
   for Classic VPN tunnels or the project is in frozen state.
   - PEER_IDENTITY_MISMATCH: Peer identity does not match peer IP,
   probably behind NAT. 
   - TS_NARROWING_NOT_ALLOWED: Traffic selector
   narrowing not allowed for an HA-VPN tunnel. |
| `target_vpn_gateway` | String | URL of the Target VPN gateway with which this VPN tunnel is associated.
Provided by the client when the VPN tunnel is created.
This field can be set only for Classic VPN tunnels. |
| `vpn_gateway` | String | URL of the VPN gateway with which this VPN tunnel is associated.
Provided by the client when the VPN tunnel is created. This must be
used (instead of target_vpn_gateway) if a High Availability VPN gateway
resource is created. |
| `local_traffic_selector` | Vec<String> | Local traffic selector to use when establishing the VPN tunnel with the
peer VPN gateway. The value should be a CIDR formatted string, for
example: 192.168.0.0/16. The ranges must be disjoint.
Only IPv4 is supported for Classic VPN tunnels. This field is output only
for HA VPN tunnels. |
| `cipher_suite` | String | User specified list of ciphers to use for the phase 1 and phase 2 of the
IKE protocol. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `labels` | HashMap<String, String> | Labels for this resource. These can only be added or modified by thesetLabels method. Each label key/value pair must comply withRFC1035.
Label values may be empty. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `name` | String | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `peer_external_gateway_interface` | i64 | The interface ID of the external VPN gateway to which this VPN tunnel is
connected. Provided by the client when the VPN tunnel is created.
Possible values are: `0`, `1`, `2`, `3`. The number of IDs in use
depends on the external VPN gateway redundancy type. |
| `peer_gcp_gateway` | String | URL of the peer side HA VPN gateway to which this VPN tunnel
is connected. Provided by the client when the VPN tunnel is created.
This field can be used when creating highly available VPN from VPC network
to VPC network, the field is exclusive with the field peerExternalGateway.
If provided, the VPN tunnel will automatically use the same
vpnGatewayInterface ID in the peer Google Cloud VPN gateway. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `shared_secret_hash` | String | Hash of the shared secret. |
| `ike_version` | i64 | IKE protocol version to use when establishing the VPN tunnel with the peer
VPN gateway. Acceptable IKE versions are 1 or 2.
The default version is 2. |
| `vpn_gateway_interface` | i64 | The interface ID of the VPN gateway with which this VPN tunnel is
associated.
Possible values are: `0`, `1`. |
| `peer_external_gateway` | String | URL of the peer side external VPN gateway to which this VPN tunnel is
connected.
Provided by the client when the VPN tunnel is created.
This field is exclusive with the field peerGcpGateway. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create vpn_tunnel
vpn_tunnel = provider.compute_api.Vpn_tunnel {
    region = "value"  # Name of the region for this request.
    project = "value"  # Project ID for this request.
}

# Access vpn_tunnel outputs
vpn_tunnel_id = vpn_tunnel.id
vpn_tunnel_description = vpn_tunnel.description
vpn_tunnel_remote_traffic_selector = vpn_tunnel.remote_traffic_selector
vpn_tunnel_peer_ip = vpn_tunnel.peer_ip
vpn_tunnel_kind = vpn_tunnel.kind
vpn_tunnel_params = vpn_tunnel.params
vpn_tunnel_region = vpn_tunnel.region
vpn_tunnel_router = vpn_tunnel.router
vpn_tunnel_label_fingerprint = vpn_tunnel.label_fingerprint
vpn_tunnel_detailed_status = vpn_tunnel.detailed_status
vpn_tunnel_shared_secret = vpn_tunnel.shared_secret
vpn_tunnel_status = vpn_tunnel.status
vpn_tunnel_target_vpn_gateway = vpn_tunnel.target_vpn_gateway
vpn_tunnel_vpn_gateway = vpn_tunnel.vpn_gateway
vpn_tunnel_local_traffic_selector = vpn_tunnel.local_traffic_selector
vpn_tunnel_cipher_suite = vpn_tunnel.cipher_suite
vpn_tunnel_id = vpn_tunnel.id
vpn_tunnel_labels = vpn_tunnel.labels
vpn_tunnel_creation_timestamp = vpn_tunnel.creation_timestamp
vpn_tunnel_name = vpn_tunnel.name
vpn_tunnel_peer_external_gateway_interface = vpn_tunnel.peer_external_gateway_interface
vpn_tunnel_peer_gcp_gateway = vpn_tunnel.peer_gcp_gateway
vpn_tunnel_self_link = vpn_tunnel.self_link
vpn_tunnel_shared_secret_hash = vpn_tunnel.shared_secret_hash
vpn_tunnel_ike_version = vpn_tunnel.ike_version
vpn_tunnel_vpn_gateway_interface = vpn_tunnel.vpn_gateway_interface
vpn_tunnel_peer_external_gateway = vpn_tunnel.peer_external_gateway
```

---


### Network_edge_security_service

Creates a new service in the specified project using the data included in
the request.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `kind` | String |  | [Output only] Type of the resource. Alwayscompute#networkEdgeSecurityService for
NetworkEdgeSecurityServices |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `fingerprint` | String |  | Fingerprint of this resource. A hash of the contents stored in this object.
This field is used in optimistic locking. This field will be ignored when
inserting a NetworkEdgeSecurityService. An up-to-date fingerprint must be
provided in order to update the NetworkEdgeSecurityService,
otherwise the request will fail with error412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve a NetworkEdgeSecurityService. |
| `region` | String |  | [Output Only] URL of the region where the resource resides. You must
specify this field as part of the HTTP request URL. It is not settable as a
field in the request body. |
| `self_link_with_id` | String |  | [Output Only] Server-defined URL for this resource with the resource id. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `security_policy` | String |  | The resource URL for the network edge security service associated with this
network edge security service. |
| `name` | String |  | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `project` | String | ✅ | Project ID for this request. |
| `region` | String | ✅ | Name of the region scoping this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `kind` | String | [Output only] Type of the resource. Alwayscompute#networkEdgeSecurityService for
NetworkEdgeSecurityServices |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `fingerprint` | String | Fingerprint of this resource. A hash of the contents stored in this object.
This field is used in optimistic locking. This field will be ignored when
inserting a NetworkEdgeSecurityService. An up-to-date fingerprint must be
provided in order to update the NetworkEdgeSecurityService,
otherwise the request will fail with error412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve a NetworkEdgeSecurityService. |
| `region` | String | [Output Only] URL of the region where the resource resides. You must
specify this field as part of the HTTP request URL. It is not settable as a
field in the request body. |
| `self_link_with_id` | String | [Output Only] Server-defined URL for this resource with the resource id. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `security_policy` | String | The resource URL for the network edge security service associated with this
network edge security service. |
| `name` | String | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create network_edge_security_service
network_edge_security_service = provider.compute_api.Network_edge_security_service {
    project = "value"  # Project ID for this request.
    region = "value"  # Name of the region scoping this request.
}

# Access network_edge_security_service outputs
network_edge_security_service_id = network_edge_security_service.id
network_edge_security_service_id = network_edge_security_service.id
network_edge_security_service_kind = network_edge_security_service.kind
network_edge_security_service_description = network_edge_security_service.description
network_edge_security_service_fingerprint = network_edge_security_service.fingerprint
network_edge_security_service_region = network_edge_security_service.region
network_edge_security_service_self_link_with_id = network_edge_security_service.self_link_with_id
network_edge_security_service_creation_timestamp = network_edge_security_service.creation_timestamp
network_edge_security_service_security_policy = network_edge_security_service.security_policy
network_edge_security_service_name = network_edge_security_service.name
network_edge_security_service_self_link = network_edge_security_service.self_link
```

---


### Region_instance_group_manager

Creates a managed instance group using the information that you specify
in the request. After the group is created, instances in the group are
created using the specified instance template.
This operation is marked as DONE when the group is created
even if the instances in the group have not yet been created. You must
separately verify the status of the individual instances with thelistmanagedinstances
method.

A regional managed instance group can contain up to 2000 instances.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `target_suspended_size` | i64 |  | The target number of suspended instances for this managed instance group.
This number changes when you: 
   
   - Suspend instance using the suspendInstances
   method or resume instances using the resumeInstances
   method.
   - Manually change the targetSuspendedSize using the update
   method. |
| `distribution_policy` | String |  | Policy specifying the intended distribution of managed instances across
zones in a regional managed instance group. |
| `fingerprint` | String |  | Fingerprint of this resource. This field may be used in optimistic locking.
It will be ignored when inserting an InstanceGroupManager. An up-to-date
fingerprint must be provided in order to update the InstanceGroupManager,
otherwise the request will fail with error412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve an InstanceGroupManager. |
| `target_size` | i64 |  | The target number of running instances for this managed instance group.
You can reduce this number by using the instanceGroupManager
deleteInstances or abandonInstances methods. Resizing the group also
changes this number. |
| `update_policy` | String |  | The update policy for this managed instance group. |
| `id` | String |  | [Output Only] A unique identifier for this resource type. The server
generates this identifier. |
| `self_link_with_id` | String |  | [Output Only] Server-defined URL for this resource with the resource id. |
| `params` | String |  | Input only. Additional params passed with the request, but not persisted
as part of resource payload. |
| `versions` | Vec<String> |  | Specifies the instance templates used by this managed instance group to
create instances.

Each version is defined by an instanceTemplate and aname. Every version can appear at most once per instance
group. This field overrides the top-level instanceTemplate
field. Read more about therelationships
between these fields. Exactly one version must leave thetargetSize field unset. That version will be applied to all
remaining instances. For more information, read aboutcanary
updates. |
| `creation_timestamp` | String |  | [Output Only] The creation timestamp for this managed instance group inRFC3339
text format. |
| `instance_template` | String |  | The URL of the instance template that is specified for this managed
instance group. The group uses this template to create all new instances
in the managed instance group. The templates for existing instances in the
group do not change unless you run recreateInstances, runapplyUpdatesToInstances, or set the group'supdatePolicy.type to PROACTIVE. |
| `standby_policy` | String |  | Standby policy for stopped and suspended instances. |
| `all_instances_config` | String |  | Specifies configuration that overrides the instance template configuration
for the group. |
| `multi_mig` | String |  | URL to the multi-MIG that this Managed Instance Group belongs to. |
| `target_size_unit` | String |  | The unit of measure for the target size. |
| `self_link` | String |  | [Output Only] The URL for this managed instance group. The server defines
this URL. |
| `base_instance_name` | String |  | The base instance name is a prefix that you want to attach to the names of
all VMs in a MIG. The maximum character length is 58 and the name must
comply with RFC1035 format.

When a VM is created in the group, the MIG appends a hyphen and a random
four-character string to the base instance name. If you want the MIG to
assign sequential numbers instead of a random string, then end the base
instance name with a hyphen followed by one or more hash symbols. The hash
symbols indicate the number of digits. For example, a base instance name of
"vm-###" results in "vm-001" as a VM name.
@pattern
[a-z](([-a-z0-9]{0,57})|([-a-z0-9]{0,51}-#{1,10}(\\[[0-9]{1,10}\\])?)) |
| `instance_group` | String |  | [Output Only] The URL of the Instance Group resource. |
| `target_pools` | Vec<String> |  | The URLs for all TargetPool resources to which instances in theinstanceGroup field are added. The target pools automatically
apply to all of the instances in the managed instance group. |
| `satisfies_pzs` | bool |  | [Output Only] Reserved for future use. |
| `current_actions` | String |  | [Output Only] The list of instance actions and the number of instances
in this managed instance group that are scheduled for each of those
actions. |
| `zone` | String |  | [Output Only] The URL of azone
where the managed instance group is located (for zonal resources). |
| `instance_lifecycle_policy` | String |  | The repair policy for this managed instance group. |
| `failover_action` | String |  | The action to perform in case of zone failure. Only one value is supported,NO_FAILOVER. The default is NO_FAILOVER. |
| `target_stopped_size` | i64 |  | The target number of stopped instances for this managed instance group.
This number changes when you: 
   
   - Stop instance using the stopInstances
   method or start instances using the startInstances
   method.
   - Manually change the targetStoppedSize using the update
   method. |
| `description` | String |  | An optional description of this resource. |
| `kind` | String |  | [Output Only] The resource type, which is alwayscompute#instanceGroupManager for managed instance groups. |
| `status` | String |  | [Output Only] The status of this managed instance group. |
| `named_ports` | Vec<String> |  | [Output Only] Named ports configured on the Instance Groups complementary
to this Instance Group Manager. |
| `stateful_policy` | String |  | Stateful configuration for this Instanced Group Manager |
| `name` | String |  | The name of the managed instance group. The name must be 1-63 characters
long, and comply withRFC1035. |
| `instance_flexibility_policy` | String |  | Instance flexibility allowing MIG to create VMs from multiple
types of machines.
Instance flexibility configuration on MIG overrides instance
template configuration. |
| `service_account` | String |  | The service account to be used as credentials for all operations performed
by the managed instance group on instances. The service accounts needs all
permissions required to create and delete instances.
By default, the service account
{projectNumber}@cloudservices.gserviceaccount.com is used. |
| `resource_policies` | String |  | Resource policies for this managed instance group. |
| `auto_healing_policies` | Vec<String> |  | The autohealing policy for this managed instance group. You can specify
only one value. |
| `region` | String |  | [Output Only] The URL of theregion
where the managed instance group resides (for regional resources). |
| `satisfies_pzi` | bool |  | [Output Only] Reserved for future use. |
| `list_managed_instances_results` | String |  | Pagination behavior of the listManagedInstances API method for
this managed instance group. |
| `target_size_policy` | String |  | The policy that specifies how the MIG creates its VMs to achieve the target
size. |
| `project` | String | ✅ | Project ID for this request. |
| `region` | String | ✅ | Name of the region scoping this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `target_suspended_size` | i64 | The target number of suspended instances for this managed instance group.
This number changes when you: 
   
   - Suspend instance using the suspendInstances
   method or resume instances using the resumeInstances
   method.
   - Manually change the targetSuspendedSize using the update
   method. |
| `distribution_policy` | String | Policy specifying the intended distribution of managed instances across
zones in a regional managed instance group. |
| `fingerprint` | String | Fingerprint of this resource. This field may be used in optimistic locking.
It will be ignored when inserting an InstanceGroupManager. An up-to-date
fingerprint must be provided in order to update the InstanceGroupManager,
otherwise the request will fail with error412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve an InstanceGroupManager. |
| `target_size` | i64 | The target number of running instances for this managed instance group.
You can reduce this number by using the instanceGroupManager
deleteInstances or abandonInstances methods. Resizing the group also
changes this number. |
| `update_policy` | String | The update policy for this managed instance group. |
| `id` | String | [Output Only] A unique identifier for this resource type. The server
generates this identifier. |
| `self_link_with_id` | String | [Output Only] Server-defined URL for this resource with the resource id. |
| `params` | String | Input only. Additional params passed with the request, but not persisted
as part of resource payload. |
| `versions` | Vec<String> | Specifies the instance templates used by this managed instance group to
create instances.

Each version is defined by an instanceTemplate and aname. Every version can appear at most once per instance
group. This field overrides the top-level instanceTemplate
field. Read more about therelationships
between these fields. Exactly one version must leave thetargetSize field unset. That version will be applied to all
remaining instances. For more information, read aboutcanary
updates. |
| `creation_timestamp` | String | [Output Only] The creation timestamp for this managed instance group inRFC3339
text format. |
| `instance_template` | String | The URL of the instance template that is specified for this managed
instance group. The group uses this template to create all new instances
in the managed instance group. The templates for existing instances in the
group do not change unless you run recreateInstances, runapplyUpdatesToInstances, or set the group'supdatePolicy.type to PROACTIVE. |
| `standby_policy` | String | Standby policy for stopped and suspended instances. |
| `all_instances_config` | String | Specifies configuration that overrides the instance template configuration
for the group. |
| `multi_mig` | String | URL to the multi-MIG that this Managed Instance Group belongs to. |
| `target_size_unit` | String | The unit of measure for the target size. |
| `self_link` | String | [Output Only] The URL for this managed instance group. The server defines
this URL. |
| `base_instance_name` | String | The base instance name is a prefix that you want to attach to the names of
all VMs in a MIG. The maximum character length is 58 and the name must
comply with RFC1035 format.

When a VM is created in the group, the MIG appends a hyphen and a random
four-character string to the base instance name. If you want the MIG to
assign sequential numbers instead of a random string, then end the base
instance name with a hyphen followed by one or more hash symbols. The hash
symbols indicate the number of digits. For example, a base instance name of
"vm-###" results in "vm-001" as a VM name.
@pattern
[a-z](([-a-z0-9]{0,57})|([-a-z0-9]{0,51}-#{1,10}(\\[[0-9]{1,10}\\])?)) |
| `instance_group` | String | [Output Only] The URL of the Instance Group resource. |
| `target_pools` | Vec<String> | The URLs for all TargetPool resources to which instances in theinstanceGroup field are added. The target pools automatically
apply to all of the instances in the managed instance group. |
| `satisfies_pzs` | bool | [Output Only] Reserved for future use. |
| `current_actions` | String | [Output Only] The list of instance actions and the number of instances
in this managed instance group that are scheduled for each of those
actions. |
| `zone` | String | [Output Only] The URL of azone
where the managed instance group is located (for zonal resources). |
| `instance_lifecycle_policy` | String | The repair policy for this managed instance group. |
| `failover_action` | String | The action to perform in case of zone failure. Only one value is supported,NO_FAILOVER. The default is NO_FAILOVER. |
| `target_stopped_size` | i64 | The target number of stopped instances for this managed instance group.
This number changes when you: 
   
   - Stop instance using the stopInstances
   method or start instances using the startInstances
   method.
   - Manually change the targetStoppedSize using the update
   method. |
| `description` | String | An optional description of this resource. |
| `kind` | String | [Output Only] The resource type, which is alwayscompute#instanceGroupManager for managed instance groups. |
| `status` | String | [Output Only] The status of this managed instance group. |
| `named_ports` | Vec<String> | [Output Only] Named ports configured on the Instance Groups complementary
to this Instance Group Manager. |
| `stateful_policy` | String | Stateful configuration for this Instanced Group Manager |
| `name` | String | The name of the managed instance group. The name must be 1-63 characters
long, and comply withRFC1035. |
| `instance_flexibility_policy` | String | Instance flexibility allowing MIG to create VMs from multiple
types of machines.
Instance flexibility configuration on MIG overrides instance
template configuration. |
| `service_account` | String | The service account to be used as credentials for all operations performed
by the managed instance group on instances. The service accounts needs all
permissions required to create and delete instances.
By default, the service account
{projectNumber}@cloudservices.gserviceaccount.com is used. |
| `resource_policies` | String | Resource policies for this managed instance group. |
| `auto_healing_policies` | Vec<String> | The autohealing policy for this managed instance group. You can specify
only one value. |
| `region` | String | [Output Only] The URL of theregion
where the managed instance group resides (for regional resources). |
| `satisfies_pzi` | bool | [Output Only] Reserved for future use. |
| `list_managed_instances_results` | String | Pagination behavior of the listManagedInstances API method for
this managed instance group. |
| `target_size_policy` | String | The policy that specifies how the MIG creates its VMs to achieve the target
size. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create region_instance_group_manager
region_instance_group_manager = provider.compute_api.Region_instance_group_manager {
    project = "value"  # Project ID for this request.
    region = "value"  # Name of the region scoping this request.
}

# Access region_instance_group_manager outputs
region_instance_group_manager_id = region_instance_group_manager.id
region_instance_group_manager_target_suspended_size = region_instance_group_manager.target_suspended_size
region_instance_group_manager_distribution_policy = region_instance_group_manager.distribution_policy
region_instance_group_manager_fingerprint = region_instance_group_manager.fingerprint
region_instance_group_manager_target_size = region_instance_group_manager.target_size
region_instance_group_manager_update_policy = region_instance_group_manager.update_policy
region_instance_group_manager_id = region_instance_group_manager.id
region_instance_group_manager_self_link_with_id = region_instance_group_manager.self_link_with_id
region_instance_group_manager_params = region_instance_group_manager.params
region_instance_group_manager_versions = region_instance_group_manager.versions
region_instance_group_manager_creation_timestamp = region_instance_group_manager.creation_timestamp
region_instance_group_manager_instance_template = region_instance_group_manager.instance_template
region_instance_group_manager_standby_policy = region_instance_group_manager.standby_policy
region_instance_group_manager_all_instances_config = region_instance_group_manager.all_instances_config
region_instance_group_manager_multi_mig = region_instance_group_manager.multi_mig
region_instance_group_manager_target_size_unit = region_instance_group_manager.target_size_unit
region_instance_group_manager_self_link = region_instance_group_manager.self_link
region_instance_group_manager_base_instance_name = region_instance_group_manager.base_instance_name
region_instance_group_manager_instance_group = region_instance_group_manager.instance_group
region_instance_group_manager_target_pools = region_instance_group_manager.target_pools
region_instance_group_manager_satisfies_pzs = region_instance_group_manager.satisfies_pzs
region_instance_group_manager_current_actions = region_instance_group_manager.current_actions
region_instance_group_manager_zone = region_instance_group_manager.zone
region_instance_group_manager_instance_lifecycle_policy = region_instance_group_manager.instance_lifecycle_policy
region_instance_group_manager_failover_action = region_instance_group_manager.failover_action
region_instance_group_manager_target_stopped_size = region_instance_group_manager.target_stopped_size
region_instance_group_manager_description = region_instance_group_manager.description
region_instance_group_manager_kind = region_instance_group_manager.kind
region_instance_group_manager_status = region_instance_group_manager.status
region_instance_group_manager_named_ports = region_instance_group_manager.named_ports
region_instance_group_manager_stateful_policy = region_instance_group_manager.stateful_policy
region_instance_group_manager_name = region_instance_group_manager.name
region_instance_group_manager_instance_flexibility_policy = region_instance_group_manager.instance_flexibility_policy
region_instance_group_manager_service_account = region_instance_group_manager.service_account
region_instance_group_manager_resource_policies = region_instance_group_manager.resource_policies
region_instance_group_manager_auto_healing_policies = region_instance_group_manager.auto_healing_policies
region_instance_group_manager_region = region_instance_group_manager.region
region_instance_group_manager_satisfies_pzi = region_instance_group_manager.satisfies_pzi
region_instance_group_manager_list_managed_instances_results = region_instance_group_manager.list_managed_instances_results
region_instance_group_manager_target_size_policy = region_instance_group_manager.target_size_policy
```

---


### Snapshot_group

Creates a snapshot group in the specified project using the data included
in the request.

**Operations**: ✅ Create ✅ Read ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `description` | String |  | Optional. An optional description of this resource. Provide this property when you
create the resource. |
| `kind` | String |  | [Output Only] Type of the resource. Alwayscompute#snapshotGroup for SnapshotGroup
resources. |
| `source_instant_snapshot_group` | String |  | Input field for the source instant snapshot group. |
| `source_info` | String |  | [Output Only] |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `name` | String |  | Identifier. Name of the resource; provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must be
a dash, lowercase letter, or digit, except the last character, which cannot
be a dash. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `self_link_with_id` | String |  | [Output Only] Server-defined URL for this resource's resource id. |
| `source_instant_snapshot_group_info` | String |  | [Output Only] |
| `status` | String |  | [Output Only] |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `description` | String | Optional. An optional description of this resource. Provide this property when you
create the resource. |
| `kind` | String | [Output Only] Type of the resource. Alwayscompute#snapshotGroup for SnapshotGroup
resources. |
| `source_instant_snapshot_group` | String | Input field for the source instant snapshot group. |
| `source_info` | String | [Output Only] |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `name` | String | Identifier. Name of the resource; provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must be
a dash, lowercase letter, or digit, except the last character, which cannot
be a dash. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `self_link_with_id` | String | [Output Only] Server-defined URL for this resource's resource id. |
| `source_instant_snapshot_group_info` | String | [Output Only] |
| `status` | String | [Output Only] |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create snapshot_group
snapshot_group = provider.compute_api.Snapshot_group {
    project = "value"  # Project ID for this request.
}

# Access snapshot_group outputs
snapshot_group_id = snapshot_group.id
snapshot_group_description = snapshot_group.description
snapshot_group_kind = snapshot_group.kind
snapshot_group_source_instant_snapshot_group = snapshot_group.source_instant_snapshot_group
snapshot_group_source_info = snapshot_group.source_info
snapshot_group_creation_timestamp = snapshot_group.creation_timestamp
snapshot_group_name = snapshot_group.name
snapshot_group_id = snapshot_group.id
snapshot_group_self_link = snapshot_group.self_link
snapshot_group_self_link_with_id = snapshot_group.self_link_with_id
snapshot_group_source_instant_snapshot_group_info = snapshot_group.source_instant_snapshot_group_info
snapshot_group_status = snapshot_group.status
```

---


### Node_template

Creates a NodeTemplate resource in the specified project using the data
included in the request.

**Operations**: ✅ Create ✅ Read ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `cpu_overcommit_type` | String |  | CPU overcommit. |
| `region` | String |  | [Output Only] The name of the region where the node template resides,
such as us-central1. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `kind` | String |  | [Output Only] The type of the resource. Alwayscompute#nodeTemplate for node templates. |
| `node_type` | String |  | The node type to use for nodes group that are created from this template. |
| `accelerators` | Vec<String> |  |  |
| `server_binding` | String |  | Sets the binding properties for the physical server. Valid values include:
   
   - *[Default]* RESTART_NODE_ON_ANY_SERVER:
   Restarts VMs on any available
   physical server
   - RESTART_NODE_ON_MINIMAL_SERVER: Restarts VMs on the same
   physical server whenever possible



See Sole-tenant
node options for more information. |
| `status` | String |  | [Output Only] The status of the node template. One of the following values:CREATING, READY, and DELETING. |
| `node_affinity_labels` | HashMap<String, String> |  | Labels to use for node affinity, which will be used in instance scheduling. |
| `status_message` | String |  | [Output Only] An optional, human-readable explanation of the status. |
| `name` | String |  | The name of the resource, provided by the client when initially creating
the resource. The resource name must be 1-63 characters long, and comply
withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must be
a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `self_link_with_id` | String |  | [Output Only] Server-defined URL for this resource with the resource id. |
| `disks` | Vec<String> |  |  |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `node_type_flexibility` | String |  | Do not use. Instead, use the node_type property. |
| `project` | String | ✅ | Project ID for this request. |
| `region` | String | ✅ | The name of the region for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `cpu_overcommit_type` | String | CPU overcommit. |
| `region` | String | [Output Only] The name of the region where the node template resides,
such as us-central1. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `kind` | String | [Output Only] The type of the resource. Alwayscompute#nodeTemplate for node templates. |
| `node_type` | String | The node type to use for nodes group that are created from this template. |
| `accelerators` | Vec<String> |  |
| `server_binding` | String | Sets the binding properties for the physical server. Valid values include:
   
   - *[Default]* RESTART_NODE_ON_ANY_SERVER:
   Restarts VMs on any available
   physical server
   - RESTART_NODE_ON_MINIMAL_SERVER: Restarts VMs on the same
   physical server whenever possible



See Sole-tenant
node options for more information. |
| `status` | String | [Output Only] The status of the node template. One of the following values:CREATING, READY, and DELETING. |
| `node_affinity_labels` | HashMap<String, String> | Labels to use for node affinity, which will be used in instance scheduling. |
| `status_message` | String | [Output Only] An optional, human-readable explanation of the status. |
| `name` | String | The name of the resource, provided by the client when initially creating
the resource. The resource name must be 1-63 characters long, and comply
withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must be
a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `self_link_with_id` | String | [Output Only] Server-defined URL for this resource with the resource id. |
| `disks` | Vec<String> |  |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `node_type_flexibility` | String | Do not use. Instead, use the node_type property. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create node_template
node_template = provider.compute_api.Node_template {
    project = "value"  # Project ID for this request.
    region = "value"  # The name of the region for this request.
}

# Access node_template outputs
node_template_id = node_template.id
node_template_id = node_template.id
node_template_cpu_overcommit_type = node_template.cpu_overcommit_type
node_template_region = node_template.region
node_template_creation_timestamp = node_template.creation_timestamp
node_template_kind = node_template.kind
node_template_node_type = node_template.node_type
node_template_accelerators = node_template.accelerators
node_template_server_binding = node_template.server_binding
node_template_status = node_template.status
node_template_node_affinity_labels = node_template.node_affinity_labels
node_template_status_message = node_template.status_message
node_template_name = node_template.name
node_template_self_link = node_template.self_link
node_template_self_link_with_id = node_template.self_link_with_id
node_template_disks = node_template.disks
node_template_description = node_template.description
node_template_node_type_flexibility = node_template.node_type_flexibility
```

---


### Zone_organization_operation

Retrieves the specified Operations resource.

**Operations**: ✅ Read

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `http_error_status_code` | i64 | [Output Only] If the operation fails, this field contains the HTTP error
status code that was returned. For example, a `404` means the
resource was not found. |
| `description` | String | [Output Only] A textual description of the operation, which is
set when the operation is created. |
| `progress` | i64 | [Output Only] An optional progress indicator that ranges from 0 to 100.
There is no requirement that this be linear or support any granularity of
operations. This should not be used to guess when the operation will be
complete. This number should monotonically increase as the operation
progresses. |
| `start_time` | String | [Output Only] The time that this operation was started by the server.
This value is inRFC3339
text format. |
| `status` | String | [Output Only] The status of the operation, which can be one of the
following:
`PENDING`, `RUNNING`, or `DONE`. |
| `end_time` | String | [Output Only] The time that this operation was completed. This value is inRFC3339
text format. |
| `operation_type` | String | [Output Only] The type of operation, such as `insert`,
`update`, or `delete`, and so on. |
| `region` | String | [Output Only] The URL of the region where the operation resides. Only
applicable when performing regional operations. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `http_error_message` | String | [Output Only] If the operation fails, this field contains the HTTP error
message that was returned, such as `NOT FOUND`. |
| `insert_time` | String | [Output Only] The time that this operation was requested.
This value is inRFC3339
text format. |
| `instances_bulk_insert_operation_metadata` | String |  |
| `self_link_with_id` | String | [Output Only] Server-defined URL for this resource with the resource id. |
| `warnings` | Vec<String> | [Output Only] If warning messages are generated during processing of the
operation, this field will be populated. |
| `zone` | String | [Output Only] The URL of the zone where the operation resides. Only
applicable when performing per-zone operations. |
| `id` | String | [Output Only] The unique identifier for the operation. This identifier is
defined by the server. |
| `set_common_instance_metadata_operation_metadata` | String | [Output Only] If the operation is for projects.setCommonInstanceMetadata,
this field will contain information on all underlying zonal actions and
their state. |
| `client_operation_id` | String | [Output Only] The value of `requestId` if you provided it in the request.
Not present otherwise. |
| `name` | String | [Output Only] Name of the operation. |
| `kind` | String | [Output Only] Type of the resource. Always `compute#operation` for
Operation resources. |
| `operation_group_id` | String | [Output Only] An ID that represents a group of operations, such as when a
group of operations results from a `bulkInsert` API request. |
| `user` | String | [Output Only] User who requested the operation, for example:
`user@example.com` or
`alice_smith_identifier (global/workforcePools/example-com-us-employees)`. |
| `error` | String | [Output Only] If errors are generated during processing of the operation,
this field will be populated. |
| `creation_timestamp` | String | [Deprecated] This field is deprecated. |
| `status_message` | String | [Output Only] An optional textual description of the current status of the
operation. |
| `target_id` | String | [Output Only] The unique target ID, which identifies a specific incarnation
of the target resource. |
| `target_link` | String | [Output Only] The URL of the resource that the operation modifies. For
operations related to creating a snapshot, this points to the disk
that the snapshot was created from. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Access zone_organization_operation outputs
zone_organization_operation_id = zone_organization_operation.id
zone_organization_operation_http_error_status_code = zone_organization_operation.http_error_status_code
zone_organization_operation_description = zone_organization_operation.description
zone_organization_operation_progress = zone_organization_operation.progress
zone_organization_operation_start_time = zone_organization_operation.start_time
zone_organization_operation_status = zone_organization_operation.status
zone_organization_operation_end_time = zone_organization_operation.end_time
zone_organization_operation_operation_type = zone_organization_operation.operation_type
zone_organization_operation_region = zone_organization_operation.region
zone_organization_operation_self_link = zone_organization_operation.self_link
zone_organization_operation_http_error_message = zone_organization_operation.http_error_message
zone_organization_operation_insert_time = zone_organization_operation.insert_time
zone_organization_operation_instances_bulk_insert_operation_metadata = zone_organization_operation.instances_bulk_insert_operation_metadata
zone_organization_operation_self_link_with_id = zone_organization_operation.self_link_with_id
zone_organization_operation_warnings = zone_organization_operation.warnings
zone_organization_operation_zone = zone_organization_operation.zone
zone_organization_operation_id = zone_organization_operation.id
zone_organization_operation_set_common_instance_metadata_operation_metadata = zone_organization_operation.set_common_instance_metadata_operation_metadata
zone_organization_operation_client_operation_id = zone_organization_operation.client_operation_id
zone_organization_operation_name = zone_organization_operation.name
zone_organization_operation_kind = zone_organization_operation.kind
zone_organization_operation_operation_group_id = zone_organization_operation.operation_group_id
zone_organization_operation_user = zone_organization_operation.user
zone_organization_operation_error = zone_organization_operation.error
zone_organization_operation_creation_timestamp = zone_organization_operation.creation_timestamp
zone_organization_operation_status_message = zone_organization_operation.status_message
zone_organization_operation_target_id = zone_organization_operation.target_id
zone_organization_operation_target_link = zone_organization_operation.target_link
```

---


### Interconnect_attachment_group

Creates a InterconnectAttachmentGroup in the specified project in the given
scope using the parameters that are included in the request.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `id` | String |  | [Output Only] The unique identifier for the resource type. The server
generates this identifier. |
| `attachments` | HashMap<String, String> |  | Attachments in the AttachmentGroup. Keys are arbitrary user-specified
strings. Users are encouraged, but not required, to use their preferred
format for resource links as keys.
Note that there are add-members and remove-members methods in gcloud.
The size of this map is limited by an "Attachments per group" quota. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `configured` | String |  |  |
| `interconnect_group` | String |  | The URL of an InterconnectGroup that groups these Attachments'
Interconnects. Customers do not need to set this unless directed by
Google Support. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `etag` | String |  | Opaque system-generated token that uniquely identifies the configuration.
If provided when patching a configuration in update mode, the provided
token must match the current token or the update is rejected. This provides
a reliable means of doing read-modify-write (optimistic locking) as
described byAIP 154. |
| `kind` | String |  | [Output Only] Type of the resource. Always
compute#interconnectAttachmentGroup. |
| `intent` | String |  |  |
| `logical_structure` | String |  |  |
| `name` | String |  | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `id` | String | [Output Only] The unique identifier for the resource type. The server
generates this identifier. |
| `attachments` | HashMap<String, String> | Attachments in the AttachmentGroup. Keys are arbitrary user-specified
strings. Users are encouraged, but not required, to use their preferred
format for resource links as keys.
Note that there are add-members and remove-members methods in gcloud.
The size of this map is limited by an "Attachments per group" quota. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `configured` | String |  |
| `interconnect_group` | String | The URL of an InterconnectGroup that groups these Attachments'
Interconnects. Customers do not need to set this unless directed by
Google Support. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `etag` | String | Opaque system-generated token that uniquely identifies the configuration.
If provided when patching a configuration in update mode, the provided
token must match the current token or the update is rejected. This provides
a reliable means of doing read-modify-write (optimistic locking) as
described byAIP 154. |
| `kind` | String | [Output Only] Type of the resource. Always
compute#interconnectAttachmentGroup. |
| `intent` | String |  |
| `logical_structure` | String |  |
| `name` | String | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create interconnect_attachment_group
interconnect_attachment_group = provider.compute_api.Interconnect_attachment_group {
    project = "value"  # Project ID for this request.
}

# Access interconnect_attachment_group outputs
interconnect_attachment_group_id = interconnect_attachment_group.id
interconnect_attachment_group_id = interconnect_attachment_group.id
interconnect_attachment_group_attachments = interconnect_attachment_group.attachments
interconnect_attachment_group_self_link = interconnect_attachment_group.self_link
interconnect_attachment_group_configured = interconnect_attachment_group.configured
interconnect_attachment_group_interconnect_group = interconnect_attachment_group.interconnect_group
interconnect_attachment_group_creation_timestamp = interconnect_attachment_group.creation_timestamp
interconnect_attachment_group_description = interconnect_attachment_group.description
interconnect_attachment_group_etag = interconnect_attachment_group.etag
interconnect_attachment_group_kind = interconnect_attachment_group.kind
interconnect_attachment_group_intent = interconnect_attachment_group.intent
interconnect_attachment_group_logical_structure = interconnect_attachment_group.logical_structure
interconnect_attachment_group_name = interconnect_attachment_group.name
```

---


### Advice

Advice on making real-time decisions (such as choosing zone or
machine types) during deployment to maximize your chances of obtaining
capacity.

**Operations**: ✅ Create

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `instance_properties` | String |  | Instance properties for this request. |
| `distribution_policy` | String |  | Policy specifying the distribution of instances across
zones within the requested region. |
| `size` | i64 |  | Size of requested capacity. |
| `instance_flexibility_policy` | String |  | Policy for instance selectors. |
| `project` | String | ✅ | Project ID for this request. |
| `region` | String | ✅ | Name of the region for this request. |



#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create advice
advice = provider.compute_api.Advice {
    project = "value"  # Project ID for this request.
    region = "value"  # Name of the region for this request.
}

```

---


### Region_composite_health_check

Create a CompositeHealthCheck in the specified project in the given region
using the parameters that are included in the request.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `fingerprint` | String |  | Fingerprint of this resource. A hash of the contents stored in this object.
This field is used in optimistic locking. This field will be ignored when
inserting a CompositeHealthCheck. An up-to-date fingerprint
must be provided in order to patch the CompositeHealthCheck; Otherwise,
the request will fail with error 412 conditionNotMet. To see
the latest fingerprint, make a get() request to retrieve the
CompositeHealthCheck. |
| `health_destination` | String |  | URL to the destination resource. Must be set. Must be aForwardingRule. The ForwardingRule must have
load balancing scheme INTERNAL orINTERNAL_MANAGED and must be regional and in the same region
as the CompositeHealthCheck (cross-region deployment forINTERNAL_MANAGED is not supported). Can be mutated. |
| `region` | String |  | [Output Only] URL of the region where the composite health check resides.
This field applies only to the regional resource. You must specify this
field as part of the HTTP request URL. It is not settable as a field in
the request body. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `self_link_with_id` | String |  | [Output Only] Server-defined URL with id for the resource. |
| `health_sources` | Vec<String> |  | URLs to the HealthSource resources whose results are AND'ed.
I.e. he aggregated result is is HEALTHY only if all sources
are HEALTHY. Must have at least 1. Must not have more than 10.
Must be regional and in the same region as theCompositeHealthCheck. Can be mutated. |
| `name` | String |  | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `id` | String |  | [Output Only] A unique identifier for this resource type. The server
generates this identifier. |
| `kind` | String |  | [Output Only] Type of the resource. Alwayscompute#compositeHealthCheck for composite health checks. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `project` | String | ✅ | Project ID for this request. |
| `region` | String | ✅ | Name of the region scoping this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `fingerprint` | String | Fingerprint of this resource. A hash of the contents stored in this object.
This field is used in optimistic locking. This field will be ignored when
inserting a CompositeHealthCheck. An up-to-date fingerprint
must be provided in order to patch the CompositeHealthCheck; Otherwise,
the request will fail with error 412 conditionNotMet. To see
the latest fingerprint, make a get() request to retrieve the
CompositeHealthCheck. |
| `health_destination` | String | URL to the destination resource. Must be set. Must be aForwardingRule. The ForwardingRule must have
load balancing scheme INTERNAL orINTERNAL_MANAGED and must be regional and in the same region
as the CompositeHealthCheck (cross-region deployment forINTERNAL_MANAGED is not supported). Can be mutated. |
| `region` | String | [Output Only] URL of the region where the composite health check resides.
This field applies only to the regional resource. You must specify this
field as part of the HTTP request URL. It is not settable as a field in
the request body. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `self_link_with_id` | String | [Output Only] Server-defined URL with id for the resource. |
| `health_sources` | Vec<String> | URLs to the HealthSource resources whose results are AND'ed.
I.e. he aggregated result is is HEALTHY only if all sources
are HEALTHY. Must have at least 1. Must not have more than 10.
Must be regional and in the same region as theCompositeHealthCheck. Can be mutated. |
| `name` | String | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `id` | String | [Output Only] A unique identifier for this resource type. The server
generates this identifier. |
| `kind` | String | [Output Only] Type of the resource. Alwayscompute#compositeHealthCheck for composite health checks. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create region_composite_health_check
region_composite_health_check = provider.compute_api.Region_composite_health_check {
    project = "value"  # Project ID for this request.
    region = "value"  # Name of the region scoping this request.
}

# Access region_composite_health_check outputs
region_composite_health_check_id = region_composite_health_check.id
region_composite_health_check_fingerprint = region_composite_health_check.fingerprint
region_composite_health_check_health_destination = region_composite_health_check.health_destination
region_composite_health_check_region = region_composite_health_check.region
region_composite_health_check_self_link = region_composite_health_check.self_link
region_composite_health_check_self_link_with_id = region_composite_health_check.self_link_with_id
region_composite_health_check_health_sources = region_composite_health_check.health_sources
region_composite_health_check_name = region_composite_health_check.name
region_composite_health_check_creation_timestamp = region_composite_health_check.creation_timestamp
region_composite_health_check_id = region_composite_health_check.id
region_composite_health_check_kind = region_composite_health_check.kind
region_composite_health_check_description = region_composite_health_check.description
```

---


### Target_https_proxie

Creates a TargetHttpsProxy resource in the specified
project using the data included in the request.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `certificate_map` | String |  | URL of a certificate map that identifies a certificate map associated with
the given target proxy.
This field can only be set for Global external Application Load Balancer or
Classic Application Load Balancer. For other products use Certificate
Manager Certificates instead.

If set, sslCertificates will be ignored.

 Accepted format is//certificatemanager.googleapis.com/projects/{project}/locations/{location}/certificateMaps/{resourceName}. |
| `server_tls_policy` | String |  | Optional. A URL referring to a networksecurity.ServerTlsPolicy resource
that describes how the proxy should authenticate inbound traffic.

 serverTlsPolicy only applies to a globalTargetHttpsProxy attached toglobalForwardingRules with theloadBalancingScheme set to INTERNAL_SELF_MANAGED
or EXTERNAL orEXTERNAL_MANAGED or INTERNAL_MANAGED.
It also applies to a regional TargetHttpsProxy attached to
regional forwardingRules with theloadBalancingScheme set to EXTERNAL_MANAGED orINTERNAL_MANAGED. For details whichServerTlsPolicy resources are accepted withINTERNAL_SELF_MANAGED and which with EXTERNAL,INTERNAL_MANAGED, EXTERNAL_MANAGEDloadBalancingScheme consult ServerTlsPolicy
documentation.

  If left blank, communications are not encrypted. |
| `ssl_certificates` | Vec<String> |  | URLs to SslCertificate resources that are used to authenticate
connections between users and the load balancer. At least one SSL
certificate must be specified. SslCertificates do not apply when the load
balancing scheme is set to INTERNAL_SELF_MANAGED.

The URLs should refer to a SSL Certificate resource or Certificate Manager
Certificate resource. Mixing Classic Certificates and Certificate Manager
Certificates is not allowed. Certificate Manager Certificates must include
the certificatemanager API namespace. Using Certificate Manager
Certificates in this field is not supported by Global external Application
Load Balancer or Classic Application Load Balancer, use certificate_map
instead.

Currently, you may specify up to 15 Classic SSL Certificates or up to 100
Certificate Manager Certificates.

Certificate Manager Certificates accepted formats are:
   
   - //certificatemanager.googleapis.com/projects/{project}/locations/{location}/certificates/{resourceName}.
   - https://certificatemanager.googleapis.com/v1alpha1/projects/{project}/locations/{location}/certificates/{resourceName}. |
| `authentication` | String |  | [Deprecated] Use serverTlsPolicy instead. |
| `authorization` | String |  | [Deprecated] Use authorizationPolicy instead. |
| `http_filters` | Vec<String> |  | URLs to networkservices.HttpFilter resources
enabled for xDS clients using this configuration. For example,https://networkservices.googleapis.com/beta/projects/project/locations/location/httpFilters/httpFilter
Only filters that handle outbound connection and
stream events may be specified. These filters work in conjunction with
a default set of HTTP filters that may already be configured by Traffic
Director. Traffic Director will determine the final location of these
filters within xDS configuration based on the name of the HTTP filter. If
Traffic Director positions multiple filters at the same location, those
filters will be in the same order as specified in this list. 

httpFilters only applies for loadbalancers withloadBalancingScheme set to INTERNAL_SELF_MANAGED.
See ForwardingRule for more details. |
| `http_keep_alive_timeout_sec` | i64 |  | Specifies how long to keep a connection open, after completing a response,
while there is no matching traffic (in seconds). If an HTTP keep-alive is
not specified, a default value (610 seconds) will be used.

For global external Application Load Balancers, the minimum allowed value
is 5 seconds and the maximum allowed value is 1200 seconds.

For classic Application Load Balancers, this option is not supported. |
| `kind` | String |  | [Output Only] Type of resource. Alwayscompute#targetHttpsProxy for target HTTPS proxies. |
| `ssl_policy` | String |  | URL of SslPolicy resource that will be associated with the TargetHttpsProxy
resource. If not set, the TargetHttpsProxy resource has no
SSL policy configured. |
| `fingerprint` | String |  | Fingerprint of this resource. A hash of the contents stored in this object.
This field is used in optimistic locking. This field will be ignored when
inserting a TargetHttpsProxy. An up-to-date fingerprint must
be provided in order to patch the TargetHttpsProxy; otherwise, the request
will fail with error 412 conditionNotMet. To see the latest
fingerprint, make a get() request to retrieve the
TargetHttpsProxy. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `authorization_policy` | String |  | Optional. A URL referring to a networksecurity.AuthorizationPolicy resource
that describes how the proxy should authorize inbound traffic. If left
blank, access will not be restricted by an authorization policy.

 Refer to the AuthorizationPolicy resource for additional
details.

 authorizationPolicy only applies to a globalTargetHttpsProxy attached toglobalForwardingRules with theloadBalancingScheme set to INTERNAL_SELF_MANAGED.

 Note: This field currently has no impact. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `self_link_with_id` | String |  | [Output Only] Server-defined URL for this resource with the resource id. |
| `name` | String |  | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `proxy_bind` | bool |  | This field only applies when the forwarding rule that references this
target proxy has a loadBalancingScheme set toINTERNAL_SELF_MANAGED.

When this field is set to true, Envoy proxies set up inbound
traffic interception and bind to the IP address and port specified in the
forwarding rule. This is generally useful when using Traffic Director to
configure Envoy as a gateway or middle proxy (in other words, not a
sidecar proxy). The Envoy proxy listens for inbound requests and handles
requests when it receives them.

The default is false. |
| `tls_early_data` | String |  | Specifies whether TLS 1.3 0-RTT Data ("Early Data") should be accepted
for this service. Early Data allows a TLS resumption handshake to include
the initial application payload (a HTTP request) alongside the handshake,
reducing the effective round trips to "zero". This applies to TLS 1.3
connections over TCP (HTTP/2) as well as over UDP (QUIC/h3).


This can improve application performance, especially on networks where
interruptions may be common, such as on mobile.


Requests with Early Data will have the "Early-Data" HTTP header set on
the request, with a value of "1", to allow the backend to determine whether
Early Data was included.


Note: TLS Early Data may allow requests to be replayed, as the data is
sent to the backend before the handshake has fully completed. Applications
that allow idempotent HTTP methods to make non-idempotent changes, such as
a GET request updating a database, should not accept Early Data on those
requests, and reject requests with the "Early-Data: 1" HTTP header by
returning a HTTP 425 (Too Early) status code, in order to remain RFC
compliant.


The default value is DISABLED. |
| `url_map` | String |  | A fully-qualified or valid partial URL to the UrlMap resource that defines
the mapping from URL to the BackendService. For example, the following are
all valid URLs for specifying a URL map:
   
   - https://www.googleapis.compute/v1/projects/project/global/urlMaps/url-map 
   - projects/project/global/urlMaps/url-map 
   - global/urlMaps/url-map |
| `region` | String |  | [Output Only] URL of the region where the regional TargetHttpsProxy
resides. This field is not applicable to global TargetHttpsProxies. |
| `quic_override` | String |  | Specifies the QUIC override policy for this TargetHttpsProxy resource. This
setting determines whether the load balancer attempts to negotiate QUIC
with clients.
You can specify NONE, ENABLE, orDISABLE.
   
   - When quic-override is set to NONE,
   Google manages whether QUIC is used.
   - When quic-override is set to ENABLE, the
   load balancer uses QUIC when possible.
   - When quic-override is set to DISABLE, the
   load balancer doesn't use QUIC.
   - If the quic-override flag is not specified,NONE is implied. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `certificate_map` | String | URL of a certificate map that identifies a certificate map associated with
the given target proxy.
This field can only be set for Global external Application Load Balancer or
Classic Application Load Balancer. For other products use Certificate
Manager Certificates instead.

If set, sslCertificates will be ignored.

 Accepted format is//certificatemanager.googleapis.com/projects/{project}/locations/{location}/certificateMaps/{resourceName}. |
| `server_tls_policy` | String | Optional. A URL referring to a networksecurity.ServerTlsPolicy resource
that describes how the proxy should authenticate inbound traffic.

 serverTlsPolicy only applies to a globalTargetHttpsProxy attached toglobalForwardingRules with theloadBalancingScheme set to INTERNAL_SELF_MANAGED
or EXTERNAL orEXTERNAL_MANAGED or INTERNAL_MANAGED.
It also applies to a regional TargetHttpsProxy attached to
regional forwardingRules with theloadBalancingScheme set to EXTERNAL_MANAGED orINTERNAL_MANAGED. For details whichServerTlsPolicy resources are accepted withINTERNAL_SELF_MANAGED and which with EXTERNAL,INTERNAL_MANAGED, EXTERNAL_MANAGEDloadBalancingScheme consult ServerTlsPolicy
documentation.

  If left blank, communications are not encrypted. |
| `ssl_certificates` | Vec<String> | URLs to SslCertificate resources that are used to authenticate
connections between users and the load balancer. At least one SSL
certificate must be specified. SslCertificates do not apply when the load
balancing scheme is set to INTERNAL_SELF_MANAGED.

The URLs should refer to a SSL Certificate resource or Certificate Manager
Certificate resource. Mixing Classic Certificates and Certificate Manager
Certificates is not allowed. Certificate Manager Certificates must include
the certificatemanager API namespace. Using Certificate Manager
Certificates in this field is not supported by Global external Application
Load Balancer or Classic Application Load Balancer, use certificate_map
instead.

Currently, you may specify up to 15 Classic SSL Certificates or up to 100
Certificate Manager Certificates.

Certificate Manager Certificates accepted formats are:
   
   - //certificatemanager.googleapis.com/projects/{project}/locations/{location}/certificates/{resourceName}.
   - https://certificatemanager.googleapis.com/v1alpha1/projects/{project}/locations/{location}/certificates/{resourceName}. |
| `authentication` | String | [Deprecated] Use serverTlsPolicy instead. |
| `authorization` | String | [Deprecated] Use authorizationPolicy instead. |
| `http_filters` | Vec<String> | URLs to networkservices.HttpFilter resources
enabled for xDS clients using this configuration. For example,https://networkservices.googleapis.com/beta/projects/project/locations/location/httpFilters/httpFilter
Only filters that handle outbound connection and
stream events may be specified. These filters work in conjunction with
a default set of HTTP filters that may already be configured by Traffic
Director. Traffic Director will determine the final location of these
filters within xDS configuration based on the name of the HTTP filter. If
Traffic Director positions multiple filters at the same location, those
filters will be in the same order as specified in this list. 

httpFilters only applies for loadbalancers withloadBalancingScheme set to INTERNAL_SELF_MANAGED.
See ForwardingRule for more details. |
| `http_keep_alive_timeout_sec` | i64 | Specifies how long to keep a connection open, after completing a response,
while there is no matching traffic (in seconds). If an HTTP keep-alive is
not specified, a default value (610 seconds) will be used.

For global external Application Load Balancers, the minimum allowed value
is 5 seconds and the maximum allowed value is 1200 seconds.

For classic Application Load Balancers, this option is not supported. |
| `kind` | String | [Output Only] Type of resource. Alwayscompute#targetHttpsProxy for target HTTPS proxies. |
| `ssl_policy` | String | URL of SslPolicy resource that will be associated with the TargetHttpsProxy
resource. If not set, the TargetHttpsProxy resource has no
SSL policy configured. |
| `fingerprint` | String | Fingerprint of this resource. A hash of the contents stored in this object.
This field is used in optimistic locking. This field will be ignored when
inserting a TargetHttpsProxy. An up-to-date fingerprint must
be provided in order to patch the TargetHttpsProxy; otherwise, the request
will fail with error 412 conditionNotMet. To see the latest
fingerprint, make a get() request to retrieve the
TargetHttpsProxy. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `authorization_policy` | String | Optional. A URL referring to a networksecurity.AuthorizationPolicy resource
that describes how the proxy should authorize inbound traffic. If left
blank, access will not be restricted by an authorization policy.

 Refer to the AuthorizationPolicy resource for additional
details.

 authorizationPolicy only applies to a globalTargetHttpsProxy attached toglobalForwardingRules with theloadBalancingScheme set to INTERNAL_SELF_MANAGED.

 Note: This field currently has no impact. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `self_link_with_id` | String | [Output Only] Server-defined URL for this resource with the resource id. |
| `name` | String | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `proxy_bind` | bool | This field only applies when the forwarding rule that references this
target proxy has a loadBalancingScheme set toINTERNAL_SELF_MANAGED.

When this field is set to true, Envoy proxies set up inbound
traffic interception and bind to the IP address and port specified in the
forwarding rule. This is generally useful when using Traffic Director to
configure Envoy as a gateway or middle proxy (in other words, not a
sidecar proxy). The Envoy proxy listens for inbound requests and handles
requests when it receives them.

The default is false. |
| `tls_early_data` | String | Specifies whether TLS 1.3 0-RTT Data ("Early Data") should be accepted
for this service. Early Data allows a TLS resumption handshake to include
the initial application payload (a HTTP request) alongside the handshake,
reducing the effective round trips to "zero". This applies to TLS 1.3
connections over TCP (HTTP/2) as well as over UDP (QUIC/h3).


This can improve application performance, especially on networks where
interruptions may be common, such as on mobile.


Requests with Early Data will have the "Early-Data" HTTP header set on
the request, with a value of "1", to allow the backend to determine whether
Early Data was included.


Note: TLS Early Data may allow requests to be replayed, as the data is
sent to the backend before the handshake has fully completed. Applications
that allow idempotent HTTP methods to make non-idempotent changes, such as
a GET request updating a database, should not accept Early Data on those
requests, and reject requests with the "Early-Data: 1" HTTP header by
returning a HTTP 425 (Too Early) status code, in order to remain RFC
compliant.


The default value is DISABLED. |
| `url_map` | String | A fully-qualified or valid partial URL to the UrlMap resource that defines
the mapping from URL to the BackendService. For example, the following are
all valid URLs for specifying a URL map:
   
   - https://www.googleapis.compute/v1/projects/project/global/urlMaps/url-map 
   - projects/project/global/urlMaps/url-map 
   - global/urlMaps/url-map |
| `region` | String | [Output Only] URL of the region where the regional TargetHttpsProxy
resides. This field is not applicable to global TargetHttpsProxies. |
| `quic_override` | String | Specifies the QUIC override policy for this TargetHttpsProxy resource. This
setting determines whether the load balancer attempts to negotiate QUIC
with clients.
You can specify NONE, ENABLE, orDISABLE.
   
   - When quic-override is set to NONE,
   Google manages whether QUIC is used.
   - When quic-override is set to ENABLE, the
   load balancer uses QUIC when possible.
   - When quic-override is set to DISABLE, the
   load balancer doesn't use QUIC.
   - If the quic-override flag is not specified,NONE is implied. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create target_https_proxie
target_https_proxie = provider.compute_api.Target_https_proxie {
    project = "value"  # Project ID for this request.
}

# Access target_https_proxie outputs
target_https_proxie_id = target_https_proxie.id
target_https_proxie_certificate_map = target_https_proxie.certificate_map
target_https_proxie_server_tls_policy = target_https_proxie.server_tls_policy
target_https_proxie_ssl_certificates = target_https_proxie.ssl_certificates
target_https_proxie_authentication = target_https_proxie.authentication
target_https_proxie_authorization = target_https_proxie.authorization
target_https_proxie_http_filters = target_https_proxie.http_filters
target_https_proxie_http_keep_alive_timeout_sec = target_https_proxie.http_keep_alive_timeout_sec
target_https_proxie_kind = target_https_proxie.kind
target_https_proxie_ssl_policy = target_https_proxie.ssl_policy
target_https_proxie_fingerprint = target_https_proxie.fingerprint
target_https_proxie_self_link = target_https_proxie.self_link
target_https_proxie_creation_timestamp = target_https_proxie.creation_timestamp
target_https_proxie_authorization_policy = target_https_proxie.authorization_policy
target_https_proxie_description = target_https_proxie.description
target_https_proxie_self_link_with_id = target_https_proxie.self_link_with_id
target_https_proxie_name = target_https_proxie.name
target_https_proxie_proxy_bind = target_https_proxie.proxy_bind
target_https_proxie_tls_early_data = target_https_proxie.tls_early_data
target_https_proxie_url_map = target_https_proxie.url_map
target_https_proxie_region = target_https_proxie.region
target_https_proxie_quic_override = target_https_proxie.quic_override
target_https_proxie_id = target_https_proxie.id
```

---


### Region_instant_snapshot

Creates an instant snapshot in the specified region.

**Operations**: ✅ Create ✅ Read ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `region` | String |  | [Output Only] URL of the region where the instant snapshot resides.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `label_fingerprint` | String |  | A fingerprint for the labels being applied to this InstantSnapshot, which
is essentially a hash of the labels set used for optimistic locking. The
fingerprint is initially generated by Compute Engine and changes after
every request to modify or update labels. You must always provide an
up-to-date fingerprint hash in order to update or change labels,
otherwise the request will fail with error412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve a InstantSnapshot. |
| `source_instant_snapshot_group_id` | String |  | [Output Only] The ID value of the source instant snapshot group this
InstantSnapshot is part of. This value may be used to determine whether the
InstantSnapshot was created as part of an InstantSnapshotGroup creation. |
| `zone` | String |  | [Output Only] URL of the zone where the instant snapshot resides.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `name` | String |  | Name of the resource; provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must be
a dash, lowercase letter, or digit, except the last character, which cannot
be a dash. |
| `resource_status` | String |  | [Output Only] Status information for the instant snapshot resource. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `kind` | String |  | [Output Only] Type of the resource. Alwayscompute#instantSnapshot for InstantSnapshot resources. |
| `source_disk_id` | String |  | [Output Only] The ID value of the disk used to create this InstantSnapshot.
This value may be used to determine whether the InstantSnapshot
was taken from the current or a previous instance of a given disk name. |
| `disk_size_gb` | String |  | [Output Only] Size of the source disk, specified in GB. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `self_link_with_id` | String |  | [Output Only] Server-defined URL for this resource's resource id. |
| `labels` | HashMap<String, String> |  | Labels to apply to this InstantSnapshot. These can be later modified by
the setLabels method.
Label values may be empty. |
| `status` | String |  | [Output Only] The status of the instantSnapshot. This can beCREATING, DELETING, FAILED, orREADY. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `source_disk` | String |  | URL of the source disk used to create this instant snapshot.
Note that the source disk must be in the same zone/region as the
instant snapshot to be created. This can be a full or valid partial URL.
For example, the following are valid values:
   
   
     - 
       https://www.googleapis.com/compute/v1/projects/project/zones/zone/disks/disk
     
     - 
       https://www.googleapis.com/compute/v1/projects/project/regions/region/disks/disk
     
     - 
       projects/project/zones/zone/disks/disk
     
     - 
       projects/project/regions/region/disks/disk
     
     - 
       zones/zone/disks/disk
     
     - 
       regions/region/disks/disk |
| `guest_flush` | bool |  | Whether to attempt an application consistent instant snapshot by
informing the OS to prepare for the snapshot process. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `satisfies_pzi` | bool |  | Output only. Reserved for future use. |
| `architecture` | String |  | [Output Only] The architecture of the instant snapshot. Valid values are
ARM64 or X86_64. |
| `satisfies_pzs` | bool |  | [Output Only] Reserved for future use. |
| `source_instant_snapshot_group` | String |  | [Output Only] URL of the source instant snapshot this instant snapshot is
part of. Note that the source instant snapshot group must be in the same
zone/region as the instant snapshot to be created. This can be a full or
valid partial URL. |
| `region` | String | ✅ | Name of the region for this request. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `region` | String | [Output Only] URL of the region where the instant snapshot resides.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `label_fingerprint` | String | A fingerprint for the labels being applied to this InstantSnapshot, which
is essentially a hash of the labels set used for optimistic locking. The
fingerprint is initially generated by Compute Engine and changes after
every request to modify or update labels. You must always provide an
up-to-date fingerprint hash in order to update or change labels,
otherwise the request will fail with error412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve a InstantSnapshot. |
| `source_instant_snapshot_group_id` | String | [Output Only] The ID value of the source instant snapshot group this
InstantSnapshot is part of. This value may be used to determine whether the
InstantSnapshot was created as part of an InstantSnapshotGroup creation. |
| `zone` | String | [Output Only] URL of the zone where the instant snapshot resides.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `name` | String | Name of the resource; provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must be
a dash, lowercase letter, or digit, except the last character, which cannot
be a dash. |
| `resource_status` | String | [Output Only] Status information for the instant snapshot resource. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `kind` | String | [Output Only] Type of the resource. Alwayscompute#instantSnapshot for InstantSnapshot resources. |
| `source_disk_id` | String | [Output Only] The ID value of the disk used to create this InstantSnapshot.
This value may be used to determine whether the InstantSnapshot
was taken from the current or a previous instance of a given disk name. |
| `disk_size_gb` | String | [Output Only] Size of the source disk, specified in GB. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `self_link_with_id` | String | [Output Only] Server-defined URL for this resource's resource id. |
| `labels` | HashMap<String, String> | Labels to apply to this InstantSnapshot. These can be later modified by
the setLabels method.
Label values may be empty. |
| `status` | String | [Output Only] The status of the instantSnapshot. This can beCREATING, DELETING, FAILED, orREADY. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `source_disk` | String | URL of the source disk used to create this instant snapshot.
Note that the source disk must be in the same zone/region as the
instant snapshot to be created. This can be a full or valid partial URL.
For example, the following are valid values:
   
   
     - 
       https://www.googleapis.com/compute/v1/projects/project/zones/zone/disks/disk
     
     - 
       https://www.googleapis.com/compute/v1/projects/project/regions/region/disks/disk
     
     - 
       projects/project/zones/zone/disks/disk
     
     - 
       projects/project/regions/region/disks/disk
     
     - 
       zones/zone/disks/disk
     
     - 
       regions/region/disks/disk |
| `guest_flush` | bool | Whether to attempt an application consistent instant snapshot by
informing the OS to prepare for the snapshot process. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `satisfies_pzi` | bool | Output only. Reserved for future use. |
| `architecture` | String | [Output Only] The architecture of the instant snapshot. Valid values are
ARM64 or X86_64. |
| `satisfies_pzs` | bool | [Output Only] Reserved for future use. |
| `source_instant_snapshot_group` | String | [Output Only] URL of the source instant snapshot this instant snapshot is
part of. Note that the source instant snapshot group must be in the same
zone/region as the instant snapshot to be created. This can be a full or
valid partial URL. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create region_instant_snapshot
region_instant_snapshot = provider.compute_api.Region_instant_snapshot {
    region = "value"  # Name of the region for this request.
    project = "value"  # Project ID for this request.
}

# Access region_instant_snapshot outputs
region_instant_snapshot_id = region_instant_snapshot.id
region_instant_snapshot_region = region_instant_snapshot.region
region_instant_snapshot_label_fingerprint = region_instant_snapshot.label_fingerprint
region_instant_snapshot_source_instant_snapshot_group_id = region_instant_snapshot.source_instant_snapshot_group_id
region_instant_snapshot_zone = region_instant_snapshot.zone
region_instant_snapshot_name = region_instant_snapshot.name
region_instant_snapshot_resource_status = region_instant_snapshot.resource_status
region_instant_snapshot_self_link = region_instant_snapshot.self_link
region_instant_snapshot_kind = region_instant_snapshot.kind
region_instant_snapshot_source_disk_id = region_instant_snapshot.source_disk_id
region_instant_snapshot_disk_size_gb = region_instant_snapshot.disk_size_gb
region_instant_snapshot_creation_timestamp = region_instant_snapshot.creation_timestamp
region_instant_snapshot_self_link_with_id = region_instant_snapshot.self_link_with_id
region_instant_snapshot_labels = region_instant_snapshot.labels
region_instant_snapshot_status = region_instant_snapshot.status
region_instant_snapshot_id = region_instant_snapshot.id
region_instant_snapshot_source_disk = region_instant_snapshot.source_disk
region_instant_snapshot_guest_flush = region_instant_snapshot.guest_flush
region_instant_snapshot_description = region_instant_snapshot.description
region_instant_snapshot_satisfies_pzi = region_instant_snapshot.satisfies_pzi
region_instant_snapshot_architecture = region_instant_snapshot.architecture
region_instant_snapshot_satisfies_pzs = region_instant_snapshot.satisfies_pzs
region_instant_snapshot_source_instant_snapshot_group = region_instant_snapshot.source_instant_snapshot_group
```

---


### Region_snapshot_setting

Get region snapshot settings.

**Operations**: ✅ Read ✅ Update

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `access_location` | String |  | (Regional snapshots use only)Policy of which location is allowed to access
snapshot. |
| `storage_location` | String |  | Policy of which storage location is going to be resolved, and additional
data that particularizes how the policy is going to be carried out. |
| `region` | String | ✅ | Name of the region for this request. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `access_location` | String | (Regional snapshots use only)Policy of which location is allowed to access
snapshot. |
| `storage_location` | String | Policy of which storage location is going to be resolved, and additional
data that particularizes how the policy is going to be carried out. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Access region_snapshot_setting outputs
region_snapshot_setting_id = region_snapshot_setting.id
region_snapshot_setting_access_location = region_snapshot_setting.access_location
region_snapshot_setting_storage_location = region_snapshot_setting.storage_location
```

---


### Instance_template

Creates an instance template in the specified project using the
data that is included in the request. If you are creating a new template to
update an existing instance group, your new instance template must use the
same network or, if applicable, the same subnetwork as the original
template.

**Operations**: ✅ Create ✅ Read ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `properties` | String |  | The instance properties for this instance template. |
| `name` | String |  | Name of the resource; provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must be
a dash, lowercase letter, or digit, except the last character, which cannot
be a dash. |
| `id` | String |  | [Output Only] A unique identifier for this instance template. The server
defines this identifier. |
| `self_link_with_id` | String |  | [Output Only] Server-defined URL for this resource with the resource id. |
| `kind` | String |  | [Output Only] The resource type, which is alwayscompute#instanceTemplate for instance templates. |
| `region` | String |  | [Output Only] URL of the region where the instance template resides. Only
applicable for regional resources. |
| `self_link` | String |  | [Output Only] The URL for this instance template. The server defines this
URL. |
| `source_instance_params` | String |  | The source instance params to use to create this instance template. |
| `creation_timestamp` | String |  | [Output Only] The creation timestamp for this instance template inRFC3339
text format. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `source_instance` | String |  | The source instance used to create the template. You can provide this as a
partial or full URL to the resource. For example, the following are valid
values:
   
   
     - https://www.googleapis.com/compute/v1/projects/project/zones/zone/instances/instance 
   - projects/project/zones/zone/instances/instance |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `properties` | String | The instance properties for this instance template. |
| `name` | String | Name of the resource; provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must be
a dash, lowercase letter, or digit, except the last character, which cannot
be a dash. |
| `id` | String | [Output Only] A unique identifier for this instance template. The server
defines this identifier. |
| `self_link_with_id` | String | [Output Only] Server-defined URL for this resource with the resource id. |
| `kind` | String | [Output Only] The resource type, which is alwayscompute#instanceTemplate for instance templates. |
| `region` | String | [Output Only] URL of the region where the instance template resides. Only
applicable for regional resources. |
| `self_link` | String | [Output Only] The URL for this instance template. The server defines this
URL. |
| `source_instance_params` | String | The source instance params to use to create this instance template. |
| `creation_timestamp` | String | [Output Only] The creation timestamp for this instance template inRFC3339
text format. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `source_instance` | String | The source instance used to create the template. You can provide this as a
partial or full URL to the resource. For example, the following are valid
values:
   
   
     - https://www.googleapis.com/compute/v1/projects/project/zones/zone/instances/instance 
   - projects/project/zones/zone/instances/instance |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create instance_template
instance_template = provider.compute_api.Instance_template {
    project = "value"  # Project ID for this request.
}

# Access instance_template outputs
instance_template_id = instance_template.id
instance_template_properties = instance_template.properties
instance_template_name = instance_template.name
instance_template_id = instance_template.id
instance_template_self_link_with_id = instance_template.self_link_with_id
instance_template_kind = instance_template.kind
instance_template_region = instance_template.region
instance_template_self_link = instance_template.self_link
instance_template_source_instance_params = instance_template.source_instance_params
instance_template_creation_timestamp = instance_template.creation_timestamp
instance_template_description = instance_template.description
instance_template_source_instance = instance_template.source_instance
```

---


### Instant_snapshot

Creates an instant snapshot in the specified zone.

**Operations**: ✅ Create ✅ Read ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `region` | String |  | [Output Only] URL of the region where the instant snapshot resides.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `label_fingerprint` | String |  | A fingerprint for the labels being applied to this InstantSnapshot, which
is essentially a hash of the labels set used for optimistic locking. The
fingerprint is initially generated by Compute Engine and changes after
every request to modify or update labels. You must always provide an
up-to-date fingerprint hash in order to update or change labels,
otherwise the request will fail with error412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve a InstantSnapshot. |
| `source_instant_snapshot_group_id` | String |  | [Output Only] The ID value of the source instant snapshot group this
InstantSnapshot is part of. This value may be used to determine whether the
InstantSnapshot was created as part of an InstantSnapshotGroup creation. |
| `zone` | String |  | [Output Only] URL of the zone where the instant snapshot resides.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `name` | String |  | Name of the resource; provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must be
a dash, lowercase letter, or digit, except the last character, which cannot
be a dash. |
| `resource_status` | String |  | [Output Only] Status information for the instant snapshot resource. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `kind` | String |  | [Output Only] Type of the resource. Alwayscompute#instantSnapshot for InstantSnapshot resources. |
| `source_disk_id` | String |  | [Output Only] The ID value of the disk used to create this InstantSnapshot.
This value may be used to determine whether the InstantSnapshot
was taken from the current or a previous instance of a given disk name. |
| `disk_size_gb` | String |  | [Output Only] Size of the source disk, specified in GB. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `self_link_with_id` | String |  | [Output Only] Server-defined URL for this resource's resource id. |
| `labels` | HashMap<String, String> |  | Labels to apply to this InstantSnapshot. These can be later modified by
the setLabels method.
Label values may be empty. |
| `status` | String |  | [Output Only] The status of the instantSnapshot. This can beCREATING, DELETING, FAILED, orREADY. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `source_disk` | String |  | URL of the source disk used to create this instant snapshot.
Note that the source disk must be in the same zone/region as the
instant snapshot to be created. This can be a full or valid partial URL.
For example, the following are valid values:
   
   
     - 
       https://www.googleapis.com/compute/v1/projects/project/zones/zone/disks/disk
     
     - 
       https://www.googleapis.com/compute/v1/projects/project/regions/region/disks/disk
     
     - 
       projects/project/zones/zone/disks/disk
     
     - 
       projects/project/regions/region/disks/disk
     
     - 
       zones/zone/disks/disk
     
     - 
       regions/region/disks/disk |
| `guest_flush` | bool |  | Whether to attempt an application consistent instant snapshot by
informing the OS to prepare for the snapshot process. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `satisfies_pzi` | bool |  | Output only. Reserved for future use. |
| `architecture` | String |  | [Output Only] The architecture of the instant snapshot. Valid values are
ARM64 or X86_64. |
| `satisfies_pzs` | bool |  | [Output Only] Reserved for future use. |
| `source_instant_snapshot_group` | String |  | [Output Only] URL of the source instant snapshot this instant snapshot is
part of. Note that the source instant snapshot group must be in the same
zone/region as the instant snapshot to be created. This can be a full or
valid partial URL. |
| `zone` | String | ✅ | Name of the zone for this request. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `region` | String | [Output Only] URL of the region where the instant snapshot resides.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `label_fingerprint` | String | A fingerprint for the labels being applied to this InstantSnapshot, which
is essentially a hash of the labels set used for optimistic locking. The
fingerprint is initially generated by Compute Engine and changes after
every request to modify or update labels. You must always provide an
up-to-date fingerprint hash in order to update or change labels,
otherwise the request will fail with error412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve a InstantSnapshot. |
| `source_instant_snapshot_group_id` | String | [Output Only] The ID value of the source instant snapshot group this
InstantSnapshot is part of. This value may be used to determine whether the
InstantSnapshot was created as part of an InstantSnapshotGroup creation. |
| `zone` | String | [Output Only] URL of the zone where the instant snapshot resides.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `name` | String | Name of the resource; provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must be
a dash, lowercase letter, or digit, except the last character, which cannot
be a dash. |
| `resource_status` | String | [Output Only] Status information for the instant snapshot resource. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `kind` | String | [Output Only] Type of the resource. Alwayscompute#instantSnapshot for InstantSnapshot resources. |
| `source_disk_id` | String | [Output Only] The ID value of the disk used to create this InstantSnapshot.
This value may be used to determine whether the InstantSnapshot
was taken from the current or a previous instance of a given disk name. |
| `disk_size_gb` | String | [Output Only] Size of the source disk, specified in GB. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `self_link_with_id` | String | [Output Only] Server-defined URL for this resource's resource id. |
| `labels` | HashMap<String, String> | Labels to apply to this InstantSnapshot. These can be later modified by
the setLabels method.
Label values may be empty. |
| `status` | String | [Output Only] The status of the instantSnapshot. This can beCREATING, DELETING, FAILED, orREADY. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `source_disk` | String | URL of the source disk used to create this instant snapshot.
Note that the source disk must be in the same zone/region as the
instant snapshot to be created. This can be a full or valid partial URL.
For example, the following are valid values:
   
   
     - 
       https://www.googleapis.com/compute/v1/projects/project/zones/zone/disks/disk
     
     - 
       https://www.googleapis.com/compute/v1/projects/project/regions/region/disks/disk
     
     - 
       projects/project/zones/zone/disks/disk
     
     - 
       projects/project/regions/region/disks/disk
     
     - 
       zones/zone/disks/disk
     
     - 
       regions/region/disks/disk |
| `guest_flush` | bool | Whether to attempt an application consistent instant snapshot by
informing the OS to prepare for the snapshot process. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `satisfies_pzi` | bool | Output only. Reserved for future use. |
| `architecture` | String | [Output Only] The architecture of the instant snapshot. Valid values are
ARM64 or X86_64. |
| `satisfies_pzs` | bool | [Output Only] Reserved for future use. |
| `source_instant_snapshot_group` | String | [Output Only] URL of the source instant snapshot this instant snapshot is
part of. Note that the source instant snapshot group must be in the same
zone/region as the instant snapshot to be created. This can be a full or
valid partial URL. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create instant_snapshot
instant_snapshot = provider.compute_api.Instant_snapshot {
    zone = "value"  # Name of the zone for this request.
    project = "value"  # Project ID for this request.
}

# Access instant_snapshot outputs
instant_snapshot_id = instant_snapshot.id
instant_snapshot_region = instant_snapshot.region
instant_snapshot_label_fingerprint = instant_snapshot.label_fingerprint
instant_snapshot_source_instant_snapshot_group_id = instant_snapshot.source_instant_snapshot_group_id
instant_snapshot_zone = instant_snapshot.zone
instant_snapshot_name = instant_snapshot.name
instant_snapshot_resource_status = instant_snapshot.resource_status
instant_snapshot_self_link = instant_snapshot.self_link
instant_snapshot_kind = instant_snapshot.kind
instant_snapshot_source_disk_id = instant_snapshot.source_disk_id
instant_snapshot_disk_size_gb = instant_snapshot.disk_size_gb
instant_snapshot_creation_timestamp = instant_snapshot.creation_timestamp
instant_snapshot_self_link_with_id = instant_snapshot.self_link_with_id
instant_snapshot_labels = instant_snapshot.labels
instant_snapshot_status = instant_snapshot.status
instant_snapshot_id = instant_snapshot.id
instant_snapshot_source_disk = instant_snapshot.source_disk
instant_snapshot_guest_flush = instant_snapshot.guest_flush
instant_snapshot_description = instant_snapshot.description
instant_snapshot_satisfies_pzi = instant_snapshot.satisfies_pzi
instant_snapshot_architecture = instant_snapshot.architecture
instant_snapshot_satisfies_pzs = instant_snapshot.satisfies_pzs
instant_snapshot_source_instant_snapshot_group = instant_snapshot.source_instant_snapshot_group
```

---


### Storage_pool_type

Returns the specified storage pool type.

**Operations**: ✅ Read

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `supported_disk_types` | Vec<String> | [Output Only] The list of disk types supported in this storage pool type. |
| `name` | String | [Output Only] Name of the resource. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `deprecated` | String | [Output Only] The deprecation status associated with this storage pool
type. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `description` | String | [Output Only] An optional description of this resource. |
| `max_pool_provisioned_throughput` | String | [Output Only] Maximum provisioned throughput. |
| `min_pool_provisioned_throughput` | String | [Output Only] Minimum provisioned throughput. |
| `min_pool_provisioned_iops` | String | [Output Only] Minimum provisioned IOPS. |
| `max_pool_provisioned_iops` | String | [Output Only] Maximum provisioned IOPS. |
| `max_pool_provisioned_capacity_gb` | String | [Output Only] Maximum storage pool size in GB. |
| `zone` | String | [Output Only] URL of the zone where the storage pool type resides.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `min_size_gb` | String | [Deprecated] This field is deprecated. Use minPoolProvisionedCapacityGb
instead. |
| `self_link_with_id` | String | [Output Only] Server-defined URL for this resource with the resource id. |
| `min_pool_provisioned_capacity_gb` | String | [Output Only] Minimum storage pool size in GB. |
| `kind` | String | [Output Only] Type of the resource. Alwayscompute#storagePoolType for storage pool types. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Access storage_pool_type outputs
storage_pool_type_id = storage_pool_type.id
storage_pool_type_supported_disk_types = storage_pool_type.supported_disk_types
storage_pool_type_name = storage_pool_type.name
storage_pool_type_creation_timestamp = storage_pool_type.creation_timestamp
storage_pool_type_deprecated = storage_pool_type.deprecated
storage_pool_type_self_link = storage_pool_type.self_link
storage_pool_type_description = storage_pool_type.description
storage_pool_type_max_pool_provisioned_throughput = storage_pool_type.max_pool_provisioned_throughput
storage_pool_type_min_pool_provisioned_throughput = storage_pool_type.min_pool_provisioned_throughput
storage_pool_type_min_pool_provisioned_iops = storage_pool_type.min_pool_provisioned_iops
storage_pool_type_max_pool_provisioned_iops = storage_pool_type.max_pool_provisioned_iops
storage_pool_type_max_pool_provisioned_capacity_gb = storage_pool_type.max_pool_provisioned_capacity_gb
storage_pool_type_zone = storage_pool_type.zone
storage_pool_type_id = storage_pool_type.id
storage_pool_type_min_size_gb = storage_pool_type.min_size_gb
storage_pool_type_self_link_with_id = storage_pool_type.self_link_with_id
storage_pool_type_min_pool_provisioned_capacity_gb = storage_pool_type.min_pool_provisioned_capacity_gb
storage_pool_type_kind = storage_pool_type.kind
```

---


### Global_organization_operation

Retrieves the specified Operations resource. Gets a list of operations
by making a `list()` request.

**Operations**: ✅ Read ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `http_error_status_code` | i64 | [Output Only] If the operation fails, this field contains the HTTP error
status code that was returned. For example, a `404` means the
resource was not found. |
| `description` | String | [Output Only] A textual description of the operation, which is
set when the operation is created. |
| `progress` | i64 | [Output Only] An optional progress indicator that ranges from 0 to 100.
There is no requirement that this be linear or support any granularity of
operations. This should not be used to guess when the operation will be
complete. This number should monotonically increase as the operation
progresses. |
| `start_time` | String | [Output Only] The time that this operation was started by the server.
This value is inRFC3339
text format. |
| `status` | String | [Output Only] The status of the operation, which can be one of the
following:
`PENDING`, `RUNNING`, or `DONE`. |
| `end_time` | String | [Output Only] The time that this operation was completed. This value is inRFC3339
text format. |
| `operation_type` | String | [Output Only] The type of operation, such as `insert`,
`update`, or `delete`, and so on. |
| `region` | String | [Output Only] The URL of the region where the operation resides. Only
applicable when performing regional operations. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `http_error_message` | String | [Output Only] If the operation fails, this field contains the HTTP error
message that was returned, such as `NOT FOUND`. |
| `insert_time` | String | [Output Only] The time that this operation was requested.
This value is inRFC3339
text format. |
| `instances_bulk_insert_operation_metadata` | String |  |
| `self_link_with_id` | String | [Output Only] Server-defined URL for this resource with the resource id. |
| `warnings` | Vec<String> | [Output Only] If warning messages are generated during processing of the
operation, this field will be populated. |
| `zone` | String | [Output Only] The URL of the zone where the operation resides. Only
applicable when performing per-zone operations. |
| `id` | String | [Output Only] The unique identifier for the operation. This identifier is
defined by the server. |
| `set_common_instance_metadata_operation_metadata` | String | [Output Only] If the operation is for projects.setCommonInstanceMetadata,
this field will contain information on all underlying zonal actions and
their state. |
| `client_operation_id` | String | [Output Only] The value of `requestId` if you provided it in the request.
Not present otherwise. |
| `name` | String | [Output Only] Name of the operation. |
| `kind` | String | [Output Only] Type of the resource. Always `compute#operation` for
Operation resources. |
| `operation_group_id` | String | [Output Only] An ID that represents a group of operations, such as when a
group of operations results from a `bulkInsert` API request. |
| `user` | String | [Output Only] User who requested the operation, for example:
`user@example.com` or
`alice_smith_identifier (global/workforcePools/example-com-us-employees)`. |
| `error` | String | [Output Only] If errors are generated during processing of the operation,
this field will be populated. |
| `creation_timestamp` | String | [Deprecated] This field is deprecated. |
| `status_message` | String | [Output Only] An optional textual description of the current status of the
operation. |
| `target_id` | String | [Output Only] The unique target ID, which identifies a specific incarnation
of the target resource. |
| `target_link` | String | [Output Only] The URL of the resource that the operation modifies. For
operations related to creating a snapshot, this points to the disk
that the snapshot was created from. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Access global_organization_operation outputs
global_organization_operation_id = global_organization_operation.id
global_organization_operation_http_error_status_code = global_organization_operation.http_error_status_code
global_organization_operation_description = global_organization_operation.description
global_organization_operation_progress = global_organization_operation.progress
global_organization_operation_start_time = global_organization_operation.start_time
global_organization_operation_status = global_organization_operation.status
global_organization_operation_end_time = global_organization_operation.end_time
global_organization_operation_operation_type = global_organization_operation.operation_type
global_organization_operation_region = global_organization_operation.region
global_organization_operation_self_link = global_organization_operation.self_link
global_organization_operation_http_error_message = global_organization_operation.http_error_message
global_organization_operation_insert_time = global_organization_operation.insert_time
global_organization_operation_instances_bulk_insert_operation_metadata = global_organization_operation.instances_bulk_insert_operation_metadata
global_organization_operation_self_link_with_id = global_organization_operation.self_link_with_id
global_organization_operation_warnings = global_organization_operation.warnings
global_organization_operation_zone = global_organization_operation.zone
global_organization_operation_id = global_organization_operation.id
global_organization_operation_set_common_instance_metadata_operation_metadata = global_organization_operation.set_common_instance_metadata_operation_metadata
global_organization_operation_client_operation_id = global_organization_operation.client_operation_id
global_organization_operation_name = global_organization_operation.name
global_organization_operation_kind = global_organization_operation.kind
global_organization_operation_operation_group_id = global_organization_operation.operation_group_id
global_organization_operation_user = global_organization_operation.user
global_organization_operation_error = global_organization_operation.error
global_organization_operation_creation_timestamp = global_organization_operation.creation_timestamp
global_organization_operation_status_message = global_organization_operation.status_message
global_organization_operation_target_id = global_organization_operation.target_id
global_organization_operation_target_link = global_organization_operation.target_link
```

---


### Zone_operation

Waits for the specified Operation resource to return as `DONE`
or for the request to approach the 2 minute deadline, and retrieves the
specified Operation resource. This method waits for no more than the
2 minutes and then returns the current state of the
operation, which might be `DONE` or still in progress.

This method is called on a best-effort basis. Specifically:
   
   
    - In uncommon cases, when the server is overloaded, the request might
    return before the default deadline is reached, or might return after zero
    seconds.
   - If the default deadline is reached, there is no guarantee that the
    operation is actually done when the method returns. Be prepared to retry
    if the operation is not `DONE`.

**Operations**: ✅ Create ✅ Read ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `project` | String | ✅ | Project ID for this request. |
| `zone` | String | ✅ | Name of the zone for this request. |
| `operation` | String | ✅ | Name of the Operations resource to return, or its unique numeric
identifier. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `http_error_status_code` | i64 | [Output Only] If the operation fails, this field contains the HTTP error
status code that was returned. For example, a `404` means the
resource was not found. |
| `description` | String | [Output Only] A textual description of the operation, which is
set when the operation is created. |
| `progress` | i64 | [Output Only] An optional progress indicator that ranges from 0 to 100.
There is no requirement that this be linear or support any granularity of
operations. This should not be used to guess when the operation will be
complete. This number should monotonically increase as the operation
progresses. |
| `start_time` | String | [Output Only] The time that this operation was started by the server.
This value is inRFC3339
text format. |
| `status` | String | [Output Only] The status of the operation, which can be one of the
following:
`PENDING`, `RUNNING`, or `DONE`. |
| `end_time` | String | [Output Only] The time that this operation was completed. This value is inRFC3339
text format. |
| `operation_type` | String | [Output Only] The type of operation, such as `insert`,
`update`, or `delete`, and so on. |
| `region` | String | [Output Only] The URL of the region where the operation resides. Only
applicable when performing regional operations. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `http_error_message` | String | [Output Only] If the operation fails, this field contains the HTTP error
message that was returned, such as `NOT FOUND`. |
| `insert_time` | String | [Output Only] The time that this operation was requested.
This value is inRFC3339
text format. |
| `instances_bulk_insert_operation_metadata` | String |  |
| `self_link_with_id` | String | [Output Only] Server-defined URL for this resource with the resource id. |
| `warnings` | Vec<String> | [Output Only] If warning messages are generated during processing of the
operation, this field will be populated. |
| `zone` | String | [Output Only] The URL of the zone where the operation resides. Only
applicable when performing per-zone operations. |
| `id` | String | [Output Only] The unique identifier for the operation. This identifier is
defined by the server. |
| `set_common_instance_metadata_operation_metadata` | String | [Output Only] If the operation is for projects.setCommonInstanceMetadata,
this field will contain information on all underlying zonal actions and
their state. |
| `client_operation_id` | String | [Output Only] The value of `requestId` if you provided it in the request.
Not present otherwise. |
| `name` | String | [Output Only] Name of the operation. |
| `kind` | String | [Output Only] Type of the resource. Always `compute#operation` for
Operation resources. |
| `operation_group_id` | String | [Output Only] An ID that represents a group of operations, such as when a
group of operations results from a `bulkInsert` API request. |
| `user` | String | [Output Only] User who requested the operation, for example:
`user@example.com` or
`alice_smith_identifier (global/workforcePools/example-com-us-employees)`. |
| `error` | String | [Output Only] If errors are generated during processing of the operation,
this field will be populated. |
| `creation_timestamp` | String | [Deprecated] This field is deprecated. |
| `status_message` | String | [Output Only] An optional textual description of the current status of the
operation. |
| `target_id` | String | [Output Only] The unique target ID, which identifies a specific incarnation
of the target resource. |
| `target_link` | String | [Output Only] The URL of the resource that the operation modifies. For
operations related to creating a snapshot, this points to the disk
that the snapshot was created from. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create zone_operation
zone_operation = provider.compute_api.Zone_operation {
    project = "value"  # Project ID for this request.
    zone = "value"  # Name of the zone for this request.
    operation = "value"  # Name of the Operations resource to return, or its unique numeric
identifier.
}

# Access zone_operation outputs
zone_operation_id = zone_operation.id
zone_operation_http_error_status_code = zone_operation.http_error_status_code
zone_operation_description = zone_operation.description
zone_operation_progress = zone_operation.progress
zone_operation_start_time = zone_operation.start_time
zone_operation_status = zone_operation.status
zone_operation_end_time = zone_operation.end_time
zone_operation_operation_type = zone_operation.operation_type
zone_operation_region = zone_operation.region
zone_operation_self_link = zone_operation.self_link
zone_operation_http_error_message = zone_operation.http_error_message
zone_operation_insert_time = zone_operation.insert_time
zone_operation_instances_bulk_insert_operation_metadata = zone_operation.instances_bulk_insert_operation_metadata
zone_operation_self_link_with_id = zone_operation.self_link_with_id
zone_operation_warnings = zone_operation.warnings
zone_operation_zone = zone_operation.zone
zone_operation_id = zone_operation.id
zone_operation_set_common_instance_metadata_operation_metadata = zone_operation.set_common_instance_metadata_operation_metadata
zone_operation_client_operation_id = zone_operation.client_operation_id
zone_operation_name = zone_operation.name
zone_operation_kind = zone_operation.kind
zone_operation_operation_group_id = zone_operation.operation_group_id
zone_operation_user = zone_operation.user
zone_operation_error = zone_operation.error
zone_operation_creation_timestamp = zone_operation.creation_timestamp
zone_operation_status_message = zone_operation.status_message
zone_operation_target_id = zone_operation.target_id
zone_operation_target_link = zone_operation.target_link
```

---


### Target_instance

Creates a TargetInstance resource in the specified project and zone using
the data included in the request.

**Operations**: ✅ Create ✅ Read ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `name` | String |  | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `zone` | String |  | [Output Only] URL of the zone where the target instance resides.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `self_link_with_id` | String |  | [Output Only] Server-defined URL for this resource with the resource id. |
| `instance` | String |  | A URL to the virtual machine instance that handles traffic for this target
instance. When creating a target instance, you can provide the
fully-qualified URL or a valid partial URL to the desired virtual machine.
For example, the following are all valid URLs:
   
   - https://www.googleapis.com/compute/v1/projects/project/zones/zone/instances/instance 
   - projects/project/zones/zone/instances/instance 
   - zones/zone/instances/instance |
| `nat_policy` | String |  | Must have a value of NO_NAT.
Protocol forwarding delivers packets while preserving the destination IP
address of the forwarding rule referencing the target instance. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `network` | String |  | The URL of the network this target instance uses to forward traffic.
If not specified, the traffic will be forwarded to the network that
the default network interface belongs to. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `security_policy` | String |  | [Output Only] The resource URL for the security policy associated with this
target instance. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `kind` | String |  | [Output Only] The type of the resource. Alwayscompute#targetInstance for target instances. |
| `zone` | String | ✅ | Name of the zone scoping this request. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `name` | String | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `zone` | String | [Output Only] URL of the zone where the target instance resides.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `self_link_with_id` | String | [Output Only] Server-defined URL for this resource with the resource id. |
| `instance` | String | A URL to the virtual machine instance that handles traffic for this target
instance. When creating a target instance, you can provide the
fully-qualified URL or a valid partial URL to the desired virtual machine.
For example, the following are all valid URLs:
   
   - https://www.googleapis.com/compute/v1/projects/project/zones/zone/instances/instance 
   - projects/project/zones/zone/instances/instance 
   - zones/zone/instances/instance |
| `nat_policy` | String | Must have a value of NO_NAT.
Protocol forwarding delivers packets while preserving the destination IP
address of the forwarding rule referencing the target instance. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `network` | String | The URL of the network this target instance uses to forward traffic.
If not specified, the traffic will be forwarded to the network that
the default network interface belongs to. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `security_policy` | String | [Output Only] The resource URL for the security policy associated with this
target instance. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `kind` | String | [Output Only] The type of the resource. Alwayscompute#targetInstance for target instances. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create target_instance
target_instance = provider.compute_api.Target_instance {
    zone = "value"  # Name of the zone scoping this request.
    project = "value"  # Project ID for this request.
}

# Access target_instance outputs
target_instance_id = target_instance.id
target_instance_description = target_instance.description
target_instance_name = target_instance.name
target_instance_zone = target_instance.zone
target_instance_self_link_with_id = target_instance.self_link_with_id
target_instance_instance = target_instance.instance
target_instance_nat_policy = target_instance.nat_policy
target_instance_id = target_instance.id
target_instance_network = target_instance.network
target_instance_self_link = target_instance.self_link
target_instance_security_policy = target_instance.security_policy
target_instance_creation_timestamp = target_instance.creation_timestamp
target_instance_kind = target_instance.kind
```

---


### Network_profile

Returns the specified network profile.

**Operations**: ✅ Read

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `self_link_with_id` | String | [Output Only] Server-defined URL for this resource with the resource id. |
| `kind` | String | [Output Only] Type of the resource. Alwayscompute#networkProfile for network profiles. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `features` | String | [Output Only] Features supported by the network. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `description` | String | [Output Only] An optional description of this resource. |
| `profile_type` | String | [Output Only] Type of the network profile. |
| `name` | String | [Output Only] Name of the resource. |
| `location` | String | [Output Only] Location to which the network is restricted. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Access network_profile outputs
network_profile_id = network_profile.id
network_profile_self_link_with_id = network_profile.self_link_with_id
network_profile_kind = network_profile.kind
network_profile_creation_timestamp = network_profile.creation_timestamp
network_profile_features = network_profile.features
network_profile_id = network_profile.id
network_profile_self_link = network_profile.self_link
network_profile_description = network_profile.description
network_profile_profile_type = network_profile.profile_type
network_profile_name = network_profile.name
network_profile_location = network_profile.location
```

---


### Service_attachment

Creates a ServiceAttachment in the specified project in the given scope
using the parameters that are included in the request.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `domain_names` | Vec<String> |  | If specified, the domain name will be used during the integration between
the PSC connected endpoints and the Cloud DNS. For example, this is a valid
domain name: "p.mycompany.com.". Current max number of domain names
supported is 1. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `name` | String |  | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `propagated_connection_limit` | i64 |  | The number of consumer spokes that connected Private Service Connect
endpoints can be propagated to through Network Connectivity Center. This
limit lets the service producer limit how many propagated Private Service
Connect connections can be established to this service attachment from a
single consumer.

If the connection preference of the service attachment is ACCEPT_MANUAL,
the limit applies to each project or network that is listed in the consumer
accept list. If the connection preference of the service attachment is
ACCEPT_AUTOMATIC, the limit applies to each project that contains a
connected endpoint.

If unspecified, the default propagated connection limit is 250. |
| `connection_preference` | String |  | The connection preference of service attachment. The value can be set
to ACCEPT_AUTOMATIC. An ACCEPT_AUTOMATIC
service attachment is one that always accepts the connection from consumer
forwarding rules. |
| `id` | String |  | [Output Only] The unique identifier for the resource type. The server
generates this identifier. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `kind` | String |  | [Output Only] Type of the resource. Alwayscompute#serviceAttachment for service attachments. |
| `producer_forwarding_rule` | String |  | The URL of a forwarding rule with loadBalancingScheme INTERNAL* that is
serving the endpoint identified by this service attachment. |
| `region` | String |  | [Output Only] URL of the region where the service attachment resides.
This field applies only to the region resource. You must specify this
field as part of the HTTP request URL. It is not settable as a field in
the request body. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `connected_endpoints` | Vec<String> |  | [Output Only] An array of connections for all the consumers connected to
this service attachment. |
| `consumer_reject_lists` | Vec<String> |  | Specifies a list of projects or networks that are not allowed to connect to
this service attachment. The project can be specified using its project ID
or project number and the network can be specified using its URL. A given
service attachment can manage connections at either the project or network
level. Therefore, both the reject and accept lists for a given service
attachment must contain either only projects or only networks. |
| `tunneling_config` | String |  | When a tunneling config is set on this service attachment it will
encapsulate traffic between consumer and producer.

When tunneling is enabled:
   
   
    -  nat_subnets must be unset 
    -  enable_proxy_protocol must be false 
    -  producer_forwarding_rule must be a L4 ILB. 
   - |
| `nat_subnets` | Vec<String> |  | An array of URLs where each entry is the URL of a subnet provided
by the service producer to use for NAT in this service attachment. |
| `reconcile_connections` | bool |  | This flag determines whether a consumer accept/reject list change can
reconcile the statuses of existing ACCEPTED or REJECTED PSC endpoints.
   
   
    -  If false, connection policy update will only affect existing PENDING
    PSC endpoints. Existing ACCEPTED/REJECTED endpoints will remain untouched
    regardless how the connection policy is modified . 
   -  If true,
    update will affect both PENDING and ACCEPTED/REJECTED PSC endpoints. For
    example, an ACCEPTED PSC endpoint will be moved to REJECTED if its project
    is added to the reject list.


For newly created service attachment, this boolean defaults to false. |
| `psc_service_attachment_id` | String |  | [Output Only] An 128-bit global unique ID of the PSC service attachment. |
| `consumer_accept_lists` | Vec<String> |  | Specifies which consumer projects or networks are allowed to connect to the
service attachment. Each project or network has a connection limit. A given
service attachment can manage connections at either the project or network
level. Therefore, both the accept and reject lists for a given service
attachment must contain either only projects or only networks or only
endpoints. |
| `fingerprint` | String |  | Fingerprint of this resource. A hash of the contents stored in this object.
This field is used in optimistic locking. This field will be ignored when
inserting a ServiceAttachment. An up-to-date fingerprint must
be provided in order to patch/update the ServiceAttachment; otherwise, the
request will fail with error 412 conditionNotMet. To see the
latest fingerprint, make a get() request to retrieve the
ServiceAttachment. |
| `target_service` | String |  | The URL of a service serving the endpoint identified by this service
attachment. |
| `metadata` | HashMap<String, String> |  | Metadata of the service attachment. |
| `enable_proxy_protocol` | bool |  | If true, enable the proxy protocol which is for supplying client TCP/IP
address data in TCP connections that traverse proxies on their way to
destination servers. |
| `project` | String | ✅ | Project ID for this request. |
| `region` | String | ✅ | Name of the region of this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `domain_names` | Vec<String> | If specified, the domain name will be used during the integration between
the PSC connected endpoints and the Cloud DNS. For example, this is a valid
domain name: "p.mycompany.com.". Current max number of domain names
supported is 1. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `name` | String | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `propagated_connection_limit` | i64 | The number of consumer spokes that connected Private Service Connect
endpoints can be propagated to through Network Connectivity Center. This
limit lets the service producer limit how many propagated Private Service
Connect connections can be established to this service attachment from a
single consumer.

If the connection preference of the service attachment is ACCEPT_MANUAL,
the limit applies to each project or network that is listed in the consumer
accept list. If the connection preference of the service attachment is
ACCEPT_AUTOMATIC, the limit applies to each project that contains a
connected endpoint.

If unspecified, the default propagated connection limit is 250. |
| `connection_preference` | String | The connection preference of service attachment. The value can be set
to ACCEPT_AUTOMATIC. An ACCEPT_AUTOMATIC
service attachment is one that always accepts the connection from consumer
forwarding rules. |
| `id` | String | [Output Only] The unique identifier for the resource type. The server
generates this identifier. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `kind` | String | [Output Only] Type of the resource. Alwayscompute#serviceAttachment for service attachments. |
| `producer_forwarding_rule` | String | The URL of a forwarding rule with loadBalancingScheme INTERNAL* that is
serving the endpoint identified by this service attachment. |
| `region` | String | [Output Only] URL of the region where the service attachment resides.
This field applies only to the region resource. You must specify this
field as part of the HTTP request URL. It is not settable as a field in
the request body. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `connected_endpoints` | Vec<String> | [Output Only] An array of connections for all the consumers connected to
this service attachment. |
| `consumer_reject_lists` | Vec<String> | Specifies a list of projects or networks that are not allowed to connect to
this service attachment. The project can be specified using its project ID
or project number and the network can be specified using its URL. A given
service attachment can manage connections at either the project or network
level. Therefore, both the reject and accept lists for a given service
attachment must contain either only projects or only networks. |
| `tunneling_config` | String | When a tunneling config is set on this service attachment it will
encapsulate traffic between consumer and producer.

When tunneling is enabled:
   
   
    -  nat_subnets must be unset 
    -  enable_proxy_protocol must be false 
    -  producer_forwarding_rule must be a L4 ILB. 
   - |
| `nat_subnets` | Vec<String> | An array of URLs where each entry is the URL of a subnet provided
by the service producer to use for NAT in this service attachment. |
| `reconcile_connections` | bool | This flag determines whether a consumer accept/reject list change can
reconcile the statuses of existing ACCEPTED or REJECTED PSC endpoints.
   
   
    -  If false, connection policy update will only affect existing PENDING
    PSC endpoints. Existing ACCEPTED/REJECTED endpoints will remain untouched
    regardless how the connection policy is modified . 
   -  If true,
    update will affect both PENDING and ACCEPTED/REJECTED PSC endpoints. For
    example, an ACCEPTED PSC endpoint will be moved to REJECTED if its project
    is added to the reject list.


For newly created service attachment, this boolean defaults to false. |
| `psc_service_attachment_id` | String | [Output Only] An 128-bit global unique ID of the PSC service attachment. |
| `consumer_accept_lists` | Vec<String> | Specifies which consumer projects or networks are allowed to connect to the
service attachment. Each project or network has a connection limit. A given
service attachment can manage connections at either the project or network
level. Therefore, both the accept and reject lists for a given service
attachment must contain either only projects or only networks or only
endpoints. |
| `fingerprint` | String | Fingerprint of this resource. A hash of the contents stored in this object.
This field is used in optimistic locking. This field will be ignored when
inserting a ServiceAttachment. An up-to-date fingerprint must
be provided in order to patch/update the ServiceAttachment; otherwise, the
request will fail with error 412 conditionNotMet. To see the
latest fingerprint, make a get() request to retrieve the
ServiceAttachment. |
| `target_service` | String | The URL of a service serving the endpoint identified by this service
attachment. |
| `metadata` | HashMap<String, String> | Metadata of the service attachment. |
| `enable_proxy_protocol` | bool | If true, enable the proxy protocol which is for supplying client TCP/IP
address data in TCP connections that traverse proxies on their way to
destination servers. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create service_attachment
service_attachment = provider.compute_api.Service_attachment {
    project = "value"  # Project ID for this request.
    region = "value"  # Name of the region of this request.
}

# Access service_attachment outputs
service_attachment_id = service_attachment.id
service_attachment_domain_names = service_attachment.domain_names
service_attachment_creation_timestamp = service_attachment.creation_timestamp
service_attachment_name = service_attachment.name
service_attachment_propagated_connection_limit = service_attachment.propagated_connection_limit
service_attachment_connection_preference = service_attachment.connection_preference
service_attachment_id = service_attachment.id
service_attachment_description = service_attachment.description
service_attachment_kind = service_attachment.kind
service_attachment_producer_forwarding_rule = service_attachment.producer_forwarding_rule
service_attachment_region = service_attachment.region
service_attachment_self_link = service_attachment.self_link
service_attachment_connected_endpoints = service_attachment.connected_endpoints
service_attachment_consumer_reject_lists = service_attachment.consumer_reject_lists
service_attachment_tunneling_config = service_attachment.tunneling_config
service_attachment_nat_subnets = service_attachment.nat_subnets
service_attachment_reconcile_connections = service_attachment.reconcile_connections
service_attachment_psc_service_attachment_id = service_attachment.psc_service_attachment_id
service_attachment_consumer_accept_lists = service_attachment.consumer_accept_lists
service_attachment_fingerprint = service_attachment.fingerprint
service_attachment_target_service = service_attachment.target_service
service_attachment_metadata = service_attachment.metadata
service_attachment_enable_proxy_protocol = service_attachment.enable_proxy_protocol
```

---


### Region_ssl_certificate

Creates a SslCertificate resource in the specified project and region using
the data included in the request

**Operations**: ✅ Create ✅ Read ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `certificate` | String |  | A value read into memory from a certificate file. The certificate file must
be in PEM format. The certificate chain must be no greater than 5 certs
long. The chain must include at least one intermediate cert. |
| `self_link` | String |  | [Output only] Server-defined URL for the resource. |
| `kind` | String |  | [Output Only] Type of the resource. Alwayscompute#sslCertificate for SSL certificates. |
| `private_key` | String |  | A value read into memory from a write-only private key file. The private
key file must be in PEM format. For security, only insert
requests include this field. |
| `name` | String |  | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `expire_time` | String |  | [Output Only] Expire time of the certificate. RFC3339 |
| `self_link_with_id` | String |  | [Output Only] Server-defined URL for this resource with the resource id. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `region` | String |  | [Output Only] URL of the region where the regional SSL Certificate
resides. This field is not applicable to global SSL Certificate. |
| `self_managed` | String |  | Configuration and status of a self-managed SSL certificate. |
| `managed` | String |  | Configuration and status of a managed SSL certificate. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `subject_alternative_names` | Vec<String> |  | [Output Only] Domains associated with the certificate via Subject
Alternative Name. |
| `type` | String |  | (Optional) Specifies the type of SSL certificate, either "SELF_MANAGED" or
"MANAGED". If not specified, the certificate is self-managed and the fieldscertificate and private_key are used. |
| `project` | String | ✅ | Project ID for this request. |
| `region` | String | ✅ | Name of the region scoping this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `certificate` | String | A value read into memory from a certificate file. The certificate file must
be in PEM format. The certificate chain must be no greater than 5 certs
long. The chain must include at least one intermediate cert. |
| `self_link` | String | [Output only] Server-defined URL for the resource. |
| `kind` | String | [Output Only] Type of the resource. Alwayscompute#sslCertificate for SSL certificates. |
| `private_key` | String | A value read into memory from a write-only private key file. The private
key file must be in PEM format. For security, only insert
requests include this field. |
| `name` | String | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `expire_time` | String | [Output Only] Expire time of the certificate. RFC3339 |
| `self_link_with_id` | String | [Output Only] Server-defined URL for this resource with the resource id. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `region` | String | [Output Only] URL of the region where the regional SSL Certificate
resides. This field is not applicable to global SSL Certificate. |
| `self_managed` | String | Configuration and status of a self-managed SSL certificate. |
| `managed` | String | Configuration and status of a managed SSL certificate. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `subject_alternative_names` | Vec<String> | [Output Only] Domains associated with the certificate via Subject
Alternative Name. |
| `type` | String | (Optional) Specifies the type of SSL certificate, either "SELF_MANAGED" or
"MANAGED". If not specified, the certificate is self-managed and the fieldscertificate and private_key are used. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create region_ssl_certificate
region_ssl_certificate = provider.compute_api.Region_ssl_certificate {
    project = "value"  # Project ID for this request.
    region = "value"  # Name of the region scoping this request.
}

# Access region_ssl_certificate outputs
region_ssl_certificate_id = region_ssl_certificate.id
region_ssl_certificate_certificate = region_ssl_certificate.certificate
region_ssl_certificate_self_link = region_ssl_certificate.self_link
region_ssl_certificate_kind = region_ssl_certificate.kind
region_ssl_certificate_private_key = region_ssl_certificate.private_key
region_ssl_certificate_name = region_ssl_certificate.name
region_ssl_certificate_expire_time = region_ssl_certificate.expire_time
region_ssl_certificate_self_link_with_id = region_ssl_certificate.self_link_with_id
region_ssl_certificate_description = region_ssl_certificate.description
region_ssl_certificate_region = region_ssl_certificate.region
region_ssl_certificate_self_managed = region_ssl_certificate.self_managed
region_ssl_certificate_managed = region_ssl_certificate.managed
region_ssl_certificate_id = region_ssl_certificate.id
region_ssl_certificate_creation_timestamp = region_ssl_certificate.creation_timestamp
region_ssl_certificate_subject_alternative_names = region_ssl_certificate.subject_alternative_names
region_ssl_certificate_type = region_ssl_certificate.type
```

---


### Global_addresse

Creates an address resource in the specified project by using the data
included in the request.

**Operations**: ✅ Create ✅ Read ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `labels` | HashMap<String, String> |  | Labels for this resource. These can only be added or modified by thesetLabels method. Each label key/value pair must comply withRFC1035.
Label values may be empty. |
| `address` | String |  | The static IP address represented by this resource. |
| `ipv6_endpoint_type` | String |  | The endpoint type of this address, which should be VM
or NETLB. This is used for deciding which type of endpoint
this address can be used after the external IPv6 address reservation. |
| `network` | String |  | The URL of the network in which to reserve the address. This field can
only be used with INTERNAL type with theVPC_PEERING purpose. |
| `users` | Vec<String> |  | [Output Only] The URLs of the resources that are using this address. |
| `label_fingerprint` | String |  | A fingerprint for the labels being applied to this Address, which is
essentially a hash of the labels set used for optimistic locking. The
fingerprint is initially generated by Compute Engine and changes after
every request to modify or update labels. You must always provide an
up-to-date fingerprint hash in order to update or change labels,
otherwise the request will fail with error412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve an Address. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `address_type` | String |  | The type of address to reserve, either INTERNAL orEXTERNAL. If unspecified, defaults to EXTERNAL. |
| `region` | String |  | [Output Only] The URL of the region where a regional address resides.
For regional addresses, you must specify the region as a path parameter in
the HTTP request URL. *This field is not applicable to global
addresses.* |
| `ip_version` | String |  | The IP version that will be used by this address. Valid options areIPV4 or IPV6. |
| `self_link_with_id` | String |  | [Output Only] Server-defined URL for this resource with the resource id. |
| `name` | String |  | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?`. The first character
must be a lowercase letter, and all following characters (except for the
last character) must be a dash, lowercase letter, or digit. The last
character must be a lowercase letter or digit. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `network_tier` | String |  | This signifies the networking tier used for configuring this address and
can only take the following values: PREMIUM orSTANDARD. Internal IP addresses are always Premium Tier;
global external IP addresses are always Premium Tier; regional external IP
addresses can be either Standard or Premium Tier.

If this field is not specified, it is assumed to be PREMIUM. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `kind` | String |  | [Output Only] Type of the resource. Always compute#address for
addresses. |
| `prefix_length` | i64 |  | The prefix length if the resource represents an IP range. |
| `status` | String |  | [Output Only] The status of the address, which can be one ofRESERVING, RESERVED, or IN_USE.
An address that is RESERVING is currently in the process of
being reserved. A RESERVED address is currently reserved and
available to use. An IN_USE address is currently being used
by another resource and is not available. |
| `subnetwork` | String |  | The URL of the subnetwork in which to reserve the address. If an IP address
is specified, it must be within the subnetwork's IP range. This field can
only be used with INTERNAL type with aGCE_ENDPOINT or DNS_RESOLVER purpose. |
| `ip_collection` | String |  | Reference to the source of external IPv4 addresses,
like a PublicDelegatedPrefix (PDP) for BYOIP.
The PDP must support enhanced IPv4 allocations.

Use one of the following formats to specify a PDP when reserving an
external IPv4 address using BYOIP.
   
   - 
   Full resource URL, as inhttps://www.googleapis.com/compute/v1/projects/projectId/regions/region/publicDelegatedPrefixes/pdp-name
   - 
   Partial URL, as in
     
      
          - projects/projectId/regions/region/publicDelegatedPrefixes/pdp-name
          - regions/region/publicDelegatedPrefixes/pdp-name |
| `description` | String |  | An optional description of this resource. Provide this field when you
create the resource. |
| `purpose` | String |  | The purpose of this resource, which can be one of the following values:
   
   
     - GCE_ENDPOINT for addresses that are used by VM
     instances, alias IP ranges, load balancers, and similar resources.
     - DNS_RESOLVER for a DNS resolver address in a subnetwork
       for a Cloud DNS  inbound
       forwarder IP addresses (regional internal IP address in a subnet of
       a VPC network)
     - VPC_PEERING for global internal IP addresses used for
      
          private services access allocated ranges.
     - NAT_AUTO for the regional external IP addresses used by
          Cloud NAT when allocating addresses using
          
          automatic NAT IP address allocation.
     - IPSEC_INTERCONNECT for addresses created from a private
     IP range that are reserved for a VLAN attachment in an
     *HA VPN over Cloud Interconnect* configuration. These addresses
     are regional resources.
     - `SHARED_LOADBALANCER_VIP` for an internal IP address that is assigned
     to multiple internal forwarding rules.
     - `PRIVATE_SERVICE_CONNECT` for a private network address that is
     used to configure Private Service Connect. Only global internal addresses
     can use this purpose. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `labels` | HashMap<String, String> | Labels for this resource. These can only be added or modified by thesetLabels method. Each label key/value pair must comply withRFC1035.
Label values may be empty. |
| `address` | String | The static IP address represented by this resource. |
| `ipv6_endpoint_type` | String | The endpoint type of this address, which should be VM
or NETLB. This is used for deciding which type of endpoint
this address can be used after the external IPv6 address reservation. |
| `network` | String | The URL of the network in which to reserve the address. This field can
only be used with INTERNAL type with theVPC_PEERING purpose. |
| `users` | Vec<String> | [Output Only] The URLs of the resources that are using this address. |
| `label_fingerprint` | String | A fingerprint for the labels being applied to this Address, which is
essentially a hash of the labels set used for optimistic locking. The
fingerprint is initially generated by Compute Engine and changes after
every request to modify or update labels. You must always provide an
up-to-date fingerprint hash in order to update or change labels,
otherwise the request will fail with error412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve an Address. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `address_type` | String | The type of address to reserve, either INTERNAL orEXTERNAL. If unspecified, defaults to EXTERNAL. |
| `region` | String | [Output Only] The URL of the region where a regional address resides.
For regional addresses, you must specify the region as a path parameter in
the HTTP request URL. *This field is not applicable to global
addresses.* |
| `ip_version` | String | The IP version that will be used by this address. Valid options areIPV4 or IPV6. |
| `self_link_with_id` | String | [Output Only] Server-defined URL for this resource with the resource id. |
| `name` | String | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?`. The first character
must be a lowercase letter, and all following characters (except for the
last character) must be a dash, lowercase letter, or digit. The last
character must be a lowercase letter or digit. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `network_tier` | String | This signifies the networking tier used for configuring this address and
can only take the following values: PREMIUM orSTANDARD. Internal IP addresses are always Premium Tier;
global external IP addresses are always Premium Tier; regional external IP
addresses can be either Standard or Premium Tier.

If this field is not specified, it is assumed to be PREMIUM. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `kind` | String | [Output Only] Type of the resource. Always compute#address for
addresses. |
| `prefix_length` | i64 | The prefix length if the resource represents an IP range. |
| `status` | String | [Output Only] The status of the address, which can be one ofRESERVING, RESERVED, or IN_USE.
An address that is RESERVING is currently in the process of
being reserved. A RESERVED address is currently reserved and
available to use. An IN_USE address is currently being used
by another resource and is not available. |
| `subnetwork` | String | The URL of the subnetwork in which to reserve the address. If an IP address
is specified, it must be within the subnetwork's IP range. This field can
only be used with INTERNAL type with aGCE_ENDPOINT or DNS_RESOLVER purpose. |
| `ip_collection` | String | Reference to the source of external IPv4 addresses,
like a PublicDelegatedPrefix (PDP) for BYOIP.
The PDP must support enhanced IPv4 allocations.

Use one of the following formats to specify a PDP when reserving an
external IPv4 address using BYOIP.
   
   - 
   Full resource URL, as inhttps://www.googleapis.com/compute/v1/projects/projectId/regions/region/publicDelegatedPrefixes/pdp-name
   - 
   Partial URL, as in
     
      
          - projects/projectId/regions/region/publicDelegatedPrefixes/pdp-name
          - regions/region/publicDelegatedPrefixes/pdp-name |
| `description` | String | An optional description of this resource. Provide this field when you
create the resource. |
| `purpose` | String | The purpose of this resource, which can be one of the following values:
   
   
     - GCE_ENDPOINT for addresses that are used by VM
     instances, alias IP ranges, load balancers, and similar resources.
     - DNS_RESOLVER for a DNS resolver address in a subnetwork
       for a Cloud DNS  inbound
       forwarder IP addresses (regional internal IP address in a subnet of
       a VPC network)
     - VPC_PEERING for global internal IP addresses used for
      
          private services access allocated ranges.
     - NAT_AUTO for the regional external IP addresses used by
          Cloud NAT when allocating addresses using
          
          automatic NAT IP address allocation.
     - IPSEC_INTERCONNECT for addresses created from a private
     IP range that are reserved for a VLAN attachment in an
     *HA VPN over Cloud Interconnect* configuration. These addresses
     are regional resources.
     - `SHARED_LOADBALANCER_VIP` for an internal IP address that is assigned
     to multiple internal forwarding rules.
     - `PRIVATE_SERVICE_CONNECT` for a private network address that is
     used to configure Private Service Connect. Only global internal addresses
     can use this purpose. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create global_addresse
global_addresse = provider.compute_api.Global_addresse {
    project = "value"  # Project ID for this request.
}

# Access global_addresse outputs
global_addresse_id = global_addresse.id
global_addresse_labels = global_addresse.labels
global_addresse_address = global_addresse.address
global_addresse_ipv6_endpoint_type = global_addresse.ipv6_endpoint_type
global_addresse_network = global_addresse.network
global_addresse_users = global_addresse.users
global_addresse_label_fingerprint = global_addresse.label_fingerprint
global_addresse_self_link = global_addresse.self_link
global_addresse_address_type = global_addresse.address_type
global_addresse_region = global_addresse.region
global_addresse_ip_version = global_addresse.ip_version
global_addresse_self_link_with_id = global_addresse.self_link_with_id
global_addresse_name = global_addresse.name
global_addresse_id = global_addresse.id
global_addresse_network_tier = global_addresse.network_tier
global_addresse_creation_timestamp = global_addresse.creation_timestamp
global_addresse_kind = global_addresse.kind
global_addresse_prefix_length = global_addresse.prefix_length
global_addresse_status = global_addresse.status
global_addresse_subnetwork = global_addresse.subnetwork
global_addresse_ip_collection = global_addresse.ip_collection
global_addresse_description = global_addresse.description
global_addresse_purpose = global_addresse.purpose
```

---


### Global_network_endpoint_group

Creates a network endpoint group in the specified project using the
parameters that are included in the request.

**Operations**: ✅ Create ✅ Read ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `load_balancer` | String |  | This field is only valid when the network endpoint group is used for load
balancing.
[Deprecated] This field is deprecated. |
| `psc_target_service` | String |  | The target service url used to set up private service connection to
a Google API or a PSC Producer Service Attachment.
An example value is: asia-northeast3-cloudkms.googleapis.com.

Optional. Only valid when networkEndpointType isPRIVATE_SERVICE_CONNECT. |
| `cloud_function` | String |  | Optional. Only valid when networkEndpointType isSERVERLESS. Only one of cloudRun,appEngine or cloudFunction may be set. |
| `region` | String |  | [Output Only] The URL of theregion
where the network endpoint group is located. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `size` | i64 |  | [Output only] Number of network endpoints in the network endpoint group. |
| `subnetwork` | String |  | Optional URL of the subnetwork to which all network endpoints in the NEG
belong. |
| `app_engine` | String |  | Optional. Only valid when networkEndpointType isSERVERLESS. Only one of cloudRun,appEngine or cloudFunction may be set. |
| `annotations` | HashMap<String, String> |  | Optional. Metadata defined as annotations on the network endpoint group. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `kind` | String |  | [Output Only] Type of the resource. Alwayscompute#networkEndpointGroup for network endpoint group. |
| `default_port` | i64 |  | The default port used if the port number is not specified in the network
endpoint.

Optional. If the network endpoint type is either GCE_VM_IP,SERVERLESS or PRIVATE_SERVICE_CONNECT, this
field must not be specified. |
| `cloud_run` | String |  | Optional. Only valid when networkEndpointType isSERVERLESS. Only one of cloudRun,appEngine or cloudFunction may be set. |
| `network` | String |  | The URL of the network to which all network endpoints in the NEG belong.
Uses default project network if unspecified. |
| `psc_data` | String |  | Optional. Only valid when networkEndpointType isPRIVATE_SERVICE_CONNECT. |
| `name` | String |  | Name of the resource; provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must be
a dash, lowercase letter, or digit, except the last character, which cannot
be a dash. |
| `network_endpoint_type` | String |  | Type of network endpoints in this network endpoint group. Can be one ofGCE_VM_IP, GCE_VM_IP_PORT,NON_GCP_PRIVATE_IP_PORT, INTERNET_FQDN_PORT,INTERNET_IP_PORT, SERVERLESS,PRIVATE_SERVICE_CONNECT, GCE_VM_IP_PORTMAP. |
| `serverless_deployment` | String |  | Only valid when networkEndpointType isSERVERLESS. Only one of cloudRun,appEngine, cloudFunction orserverlessDeployment may be set. |
| `type` | String |  | Specify the type of this network endpoint group. OnlyLOAD_BALANCING is valid for now. |
| `zone` | String |  | [Output Only] The URL of thezone
where the network endpoint group is located. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `self_link_with_id` | String |  | [Output Only] Server-defined URL for this resource with the resource id. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `load_balancer` | String | This field is only valid when the network endpoint group is used for load
balancing.
[Deprecated] This field is deprecated. |
| `psc_target_service` | String | The target service url used to set up private service connection to
a Google API or a PSC Producer Service Attachment.
An example value is: asia-northeast3-cloudkms.googleapis.com.

Optional. Only valid when networkEndpointType isPRIVATE_SERVICE_CONNECT. |
| `cloud_function` | String | Optional. Only valid when networkEndpointType isSERVERLESS. Only one of cloudRun,appEngine or cloudFunction may be set. |
| `region` | String | [Output Only] The URL of theregion
where the network endpoint group is located. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `size` | i64 | [Output only] Number of network endpoints in the network endpoint group. |
| `subnetwork` | String | Optional URL of the subnetwork to which all network endpoints in the NEG
belong. |
| `app_engine` | String | Optional. Only valid when networkEndpointType isSERVERLESS. Only one of cloudRun,appEngine or cloudFunction may be set. |
| `annotations` | HashMap<String, String> | Optional. Metadata defined as annotations on the network endpoint group. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `kind` | String | [Output Only] Type of the resource. Alwayscompute#networkEndpointGroup for network endpoint group. |
| `default_port` | i64 | The default port used if the port number is not specified in the network
endpoint.

Optional. If the network endpoint type is either GCE_VM_IP,SERVERLESS or PRIVATE_SERVICE_CONNECT, this
field must not be specified. |
| `cloud_run` | String | Optional. Only valid when networkEndpointType isSERVERLESS. Only one of cloudRun,appEngine or cloudFunction may be set. |
| `network` | String | The URL of the network to which all network endpoints in the NEG belong.
Uses default project network if unspecified. |
| `psc_data` | String | Optional. Only valid when networkEndpointType isPRIVATE_SERVICE_CONNECT. |
| `name` | String | Name of the resource; provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must be
a dash, lowercase letter, or digit, except the last character, which cannot
be a dash. |
| `network_endpoint_type` | String | Type of network endpoints in this network endpoint group. Can be one ofGCE_VM_IP, GCE_VM_IP_PORT,NON_GCP_PRIVATE_IP_PORT, INTERNET_FQDN_PORT,INTERNET_IP_PORT, SERVERLESS,PRIVATE_SERVICE_CONNECT, GCE_VM_IP_PORTMAP. |
| `serverless_deployment` | String | Only valid when networkEndpointType isSERVERLESS. Only one of cloudRun,appEngine, cloudFunction orserverlessDeployment may be set. |
| `type` | String | Specify the type of this network endpoint group. OnlyLOAD_BALANCING is valid for now. |
| `zone` | String | [Output Only] The URL of thezone
where the network endpoint group is located. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `self_link_with_id` | String | [Output Only] Server-defined URL for this resource with the resource id. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create global_network_endpoint_group
global_network_endpoint_group = provider.compute_api.Global_network_endpoint_group {
    project = "value"  # Project ID for this request.
}

# Access global_network_endpoint_group outputs
global_network_endpoint_group_id = global_network_endpoint_group.id
global_network_endpoint_group_self_link = global_network_endpoint_group.self_link
global_network_endpoint_group_load_balancer = global_network_endpoint_group.load_balancer
global_network_endpoint_group_psc_target_service = global_network_endpoint_group.psc_target_service
global_network_endpoint_group_cloud_function = global_network_endpoint_group.cloud_function
global_network_endpoint_group_region = global_network_endpoint_group.region
global_network_endpoint_group_creation_timestamp = global_network_endpoint_group.creation_timestamp
global_network_endpoint_group_size = global_network_endpoint_group.size
global_network_endpoint_group_subnetwork = global_network_endpoint_group.subnetwork
global_network_endpoint_group_app_engine = global_network_endpoint_group.app_engine
global_network_endpoint_group_annotations = global_network_endpoint_group.annotations
global_network_endpoint_group_id = global_network_endpoint_group.id
global_network_endpoint_group_kind = global_network_endpoint_group.kind
global_network_endpoint_group_default_port = global_network_endpoint_group.default_port
global_network_endpoint_group_cloud_run = global_network_endpoint_group.cloud_run
global_network_endpoint_group_network = global_network_endpoint_group.network
global_network_endpoint_group_psc_data = global_network_endpoint_group.psc_data
global_network_endpoint_group_name = global_network_endpoint_group.name
global_network_endpoint_group_network_endpoint_type = global_network_endpoint_group.network_endpoint_type
global_network_endpoint_group_serverless_deployment = global_network_endpoint_group.serverless_deployment
global_network_endpoint_group_type = global_network_endpoint_group.type
global_network_endpoint_group_zone = global_network_endpoint_group.zone
global_network_endpoint_group_description = global_network_endpoint_group.description
global_network_endpoint_group_self_link_with_id = global_network_endpoint_group.self_link_with_id
```

---


### Interconnect_attachment

Creates an InterconnectAttachment in the specified project using the data
included in the request.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `pairing_key` | String |  | [Output only for type PARTNER. Input only for PARTNER_PROVIDER. Not
present for DEDICATED].
The opaque identifier of a PARTNER attachment used to initiate
provisioning with a selected partner.
Of the form "XXXXX/region/domain" |
| `partner_asn` | String |  | Optional BGP ASN for the router supplied by a Layer 3 Partner if they
configured BGP on behalf of the customer.
Output only for PARTNER type, input only for PARTNER_PROVIDER, not
available for DEDICATED. |
| `configuration_constraints` | String |  | [Output Only] Constraints for this attachment, if any. The attachment does
not work if these constraints are not met. |
| `kind` | String |  | [Output Only] Type of the resource. Alwayscompute#interconnectAttachment for interconnect attachments. |
| `l2_forwarding` | String |  | L2 Interconnect Attachment related config. This field is required if the
type is L2_DEDICATED.

The configuration specifies how VLAN tags (like dot1q, qinq, or dot1ad)
within L2 packets are mapped to the destination appliances IP addresses.
The packet is then encapsulated with the appliance IP address and sent to
the edge appliance. |
| `bandwidth` | String |  | Provisioned bandwidth capacity for the interconnect attachment. For
attachments of type DEDICATED, the user can set the bandwidth.
For attachments of type PARTNER, the Google Partner that is operating
the interconnect must set the bandwidth.
Output only for PARTNER type, mutable for PARTNER_PROVIDER and DEDICATED,
and can take one of the following values:
   
   - BPS_50M: 50 Mbit/s
   - BPS_100M: 100 Mbit/s
   - BPS_200M: 200 Mbit/s
   - BPS_300M: 300 Mbit/s
   - BPS_400M: 400 Mbit/s
   - BPS_500M: 500 Mbit/s
   - BPS_1G: 1 Gbit/s
   - BPS_2G: 2 Gbit/s
   - BPS_5G: 5 Gbit/s
   - BPS_10G: 10 Gbit/s
   - BPS_20G: 20 Gbit/s
   - BPS_50G: 50 Gbit/s
   - BPS_100G: 100 Gbit/s |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `labels` | HashMap<String, String> |  | Labels for this resource. These can only be added or modified by thesetLabels method. Each label key/value pair must comply withRFC1035.
Label values may be empty. |
| `operational_status` | String |  | [Output Only] The current status of whether or not this interconnect
attachment is functional, which can take one of the following values:
   
   - OS_ACTIVE: The attachment has been turned up and is ready to
   use. 
   - OS_UNPROVISIONED: The attachment is not ready to use yet,
   because turnup is not complete. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `self_link_with_id` | String |  | [Output Only] Server-defined URL for this resource with the resource id. |
| `cloud_router_ipv6_interface_id` | String |  | This field is not available. |
| `type` | String |  | The type of interconnect attachment this is, which can take one of the
following values:
   
   - DEDICATED: an attachment to a Dedicated Interconnect.
   - PARTNER: an attachment to a Partner Interconnect, created by the
   customer.
   - PARTNER_PROVIDER: an attachment to a Partner Interconnect, created by
   the partner.

- L2_DEDICATED: a L2 attachment to a Dedicated Interconnect. |
| `admin_enabled` | bool |  | Determines whether this Attachment will carry packets.
Not present for PARTNER_PROVIDER. |
| `description` | String |  | An optional description of this resource. |
| `params` | String |  | Input only. [Input Only] Additional params passed with the request, but not persisted
as part of resource payload. |
| `remote_service` | String |  | [Output Only]
If the attachment is on a Cross-Cloud Interconnect connection, this field
contains the interconnect's remote location service provider. Example
values: "Amazon Web Services" "Microsoft Azure".

The field is set only for attachments on Cross-Cloud Interconnect
connections. Its value is copied from the InterconnectRemoteLocation
remoteService field. |
| `cloud_router_ip_address` | String |  | [Output Only] IPv4 address + prefix length to be configured on Cloud Router
Interface for this interconnect attachment. |
| `multicast_enabled` | bool |  | Whether or not to permit multicast traffic for this attachment. Multicast
packets will be dropped if this is not enabled. |
| `stack_type` | String |  | The stack type for this interconnect attachment to identify whether the
IPv6 feature is enabled or not. If not specified, IPV4_ONLY
will be used.

This field can be both set at interconnect attachments creation and
update interconnect attachment operations. |
| `region` | String |  | [Output Only] URL of the region where the regional interconnect attachment
resides.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `partner_metadata` | String |  | Informational metadata about Partner attachments from Partners to display
to customers.
Output only for PARTNER type, mutable for PARTNER_PROVIDER, not
available for DEDICATED. |
| `candidate_customer_router_ip_address` | String |  | Single IPv4 address + prefix length to be configured on the customer router
interface for this interconnect attachment. |
| `attachment_group` | String |  | [Output Only] URL of the AttachmentGroup that includes this Attachment. |
| `customer_router_ipv6_interface_id` | String |  | This field is not available. |
| `edge_availability_domain` | String |  | Input only. Desired availability domain for the attachment. Only available for type
PARTNER, at creation time, and can take one of the following values:
   
   - AVAILABILITY_DOMAIN_ANY
   - AVAILABILITY_DOMAIN_1
   - AVAILABILITY_DOMAIN_2


For improved reliability, customers should configure a pair of attachments,
one per availability domain. The selected availability domain will be
provided to the Partner via the pairing key, so that the provisioned
circuit will lie in the specified domain. If not specified, the value will
default to AVAILABILITY_DOMAIN_ANY. |
| `satisfies_pzs` | bool |  | [Output Only] Reserved for future use. |
| `state` | String |  | [Output Only] The current state of this attachment's functionality.
Enum values ACTIVE and UNPROVISIONED are shared by DEDICATED/PRIVATE,
PARTNER, and PARTNER_PROVIDER interconnect attachments, while enum values
PENDING_PARTNER, PARTNER_REQUEST_RECEIVED, and PENDING_CUSTOMER are used
for only PARTNER and PARTNER_PROVIDER interconnect attachments.
This state can take one of the following values:
   
   - ACTIVE: The attachment has been turned up and is ready to use.
   - UNPROVISIONED: The attachment is not ready to use yet, because turnup
   is not complete.
   - PENDING_PARTNER: A newly-created PARTNER attachment that has not yet
   been configured on the Partner side.
   - PARTNER_REQUEST_RECEIVED: A PARTNER attachment is in the process of
   provisioning after a PARTNER_PROVIDER attachment was created that
   references it. 
   - PENDING_CUSTOMER: A PARTNER or PARTNER_PROVIDER
   attachment that is waiting for a customer to activate it. 
   - DEFUNCT:
   The attachment was deleted externally and is no longer functional. This
   could be because the associated Interconnect was removed, or because the
   other side of a Partner attachment was deleted. |
| `label_fingerprint` | String |  | A fingerprint for the labels being applied to this InterconnectAttachment,
which is essentially a hash of the labels set used for optimistic locking.
The fingerprint is initially generated by Compute Engine and changes after
every request to modify or update labels. You must always provide an
up-to-date fingerprint hash in order to update or change labels,
otherwise the request will fail with error412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve an InterconnectAttachment. |
| `interconnect` | String |  | URL of the underlying Interconnect object that this attachment's traffic
will traverse through. |
| `candidate_cloud_router_ip_address` | String |  | Single IPv4 address + prefix length to be configured on the cloud router
interface for this interconnect attachment.
   
   - Both candidate_cloud_router_ip_address and
   candidate_customer_router_ip_address fields must be set or both must be
   unset.
   - Prefix length of both candidate_cloud_router_ip_address and
   candidate_customer_router_ip_address must be the same.
   - Max prefix length is 31. |
| `candidate_cloud_router_ipv6_address` | String |  | Single IPv6 address + prefix length to be configured on the cloud router
interface for this interconnect attachment.
   
   - Both candidate_cloud_router_ipv6_address and
   candidate_customer_router_ipv6_address fields must be set or both must be
   unset.
   - Prefix length of both candidate_cloud_router_ipv6_address and
   candidate_customer_router_ipv6_address must be the same.
   - Max prefix length is 126. |
| `customer_router_ipv6_address` | String |  | [Output Only] IPv6 address + prefix length to be configured on the
customer router subinterface for this interconnect attachment. |
| `encryption` | String |  | Indicates the user-supplied encryption option of this VLAN attachment
(interconnectAttachment). Can only be specified at attachment creation
for PARTNER or DEDICATED attachments.
Possible values are:
   
   - NONE - This is the default value, which means that the
   VLAN attachment carries unencrypted traffic. VMs are able to send
   traffic to, or receive traffic from, such a VLAN attachment.
   - IPSEC - The VLAN attachment carries only encrypted
   traffic that is encrypted by an IPsec device, such as an HA VPN gateway or
   third-party IPsec VPN. VMs cannot directly send traffic to, or receive
   traffic from, such a VLAN attachment. To use *HA VPN over Cloud
   Interconnect*, the VLAN attachment must be created with this
   option. |
| `candidate_customer_router_ipv6_address` | String |  | Single IPv6 address + prefix length to be configured on the customer router
interface for this interconnect attachment. |
| `customer_router_ip_address` | String |  | [Output Only] IPv4 address + prefix length to be configured on the customer
router subinterface for this interconnect attachment. |
| `ipsec_internal_addresses` | Vec<String> |  | A list of URLs of addresses that have been reserved for the VLAN
attachment. Used only for the VLAN attachment that has the encryption
option as IPSEC. The addresses must be regional internal IP address ranges.
When creating an HA VPN gateway over the VLAN attachment, if the attachment
is configured to use a regional internal IP address, then the VPN gateway's
IP address is allocated from the IP address range specified here. For
example, if the HA VPN gateway's interface 0 is paired to this VLAN
attachment, then a regional internal IP address for the VPN gateway
interface 0 will be allocated from the IP address specified for this
VLAN attachment.
If this field is not specified when creating the VLAN attachment, then
later on when creating an HA VPN gateway on this VLAN attachment, the HA
VPN gateway's IP address is allocated from the regional external IP address
pool. |
| `candidate_subnets` | Vec<String> |  | Input only. Up to 16 candidate prefixes that can be used to restrict the allocation
of cloudRouterIpAddress and customerRouterIpAddress for this attachment.
All prefixes must be within link-local address space (169.254.0.0/16) and
must be /29 or shorter (/28, /27, etc). Google will attempt to select an
unused /29 from the supplied candidate prefix(es). The request will fail if
all possible /29s are in use on Google's edge. If not supplied, Google will
randomly select an unused /29 from all of link-local space. |
| `mtu` | i64 |  | Maximum Transmission Unit (MTU), in bytes, of packets passing through this
interconnect attachment.
Valid values are 1440, 1460, 1500, and 8896. If not specified,
the value will default to 1440. |
| `cloud_router_ipv6_address` | String |  | [Output Only] IPv6 address + prefix length to be configured on Cloud
Router Interface for this interconnect attachment. |
| `google_reference_id` | String |  | [Output Only] Google reference ID, to be used when raising support tickets
with Google or otherwise to debug backend connectivity issues.
[Deprecated] This field is not used. |
| `name` | String |  | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `router` | String |  | URL of the Cloud Router to be used for dynamic routing. This router must be
in the same region as this InterconnectAttachment. The
InterconnectAttachment will automatically connect the Interconnect to the
network & region within which the Cloud Router is configured. |
| `private_interconnect_info` | String |  | [Output Only] Information specific to an InterconnectAttachment.
This property is populated if the interconnect that
this is attached to is of type DEDICATED. |
| `subnet_length` | i64 |  | Input only. Length of the IPv4 subnet mask.
Allowed values:
   
   
    - 29 (default)
    - 30

The default value is 29, except for Cross-Cloud Interconnect
connections that use an InterconnectRemoteLocation with a
constraints.subnetLengthRange.min equal to 30. For example,
connections that use an Azure remote location fall into this
category. In these cases, the default value is 30, and requesting
29 returns an error.

Where both 29 and 30 are allowed, 29 is preferred, because it gives
Google Cloud Support more debugging visibility. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `dataplane_version` | i64 |  | [Output Only] Dataplane version for this InterconnectAttachment. This
field is only present for Dataplane version 2 and higher. Absence of this
field in the API output indicates that the Dataplane is version 1. |
| `vlan_tag8021q` | i64 |  | The IEEE 802.1Q VLAN tag for this attachment, in the range 2-4093.
Only specified at creation time. |
| `candidate_ipv6_subnets` | Vec<String> |  | This field is not available. |
| `region` | String | ✅ | Name of the region for this request. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `pairing_key` | String | [Output only for type PARTNER. Input only for PARTNER_PROVIDER. Not
present for DEDICATED].
The opaque identifier of a PARTNER attachment used to initiate
provisioning with a selected partner.
Of the form "XXXXX/region/domain" |
| `partner_asn` | String | Optional BGP ASN for the router supplied by a Layer 3 Partner if they
configured BGP on behalf of the customer.
Output only for PARTNER type, input only for PARTNER_PROVIDER, not
available for DEDICATED. |
| `configuration_constraints` | String | [Output Only] Constraints for this attachment, if any. The attachment does
not work if these constraints are not met. |
| `kind` | String | [Output Only] Type of the resource. Alwayscompute#interconnectAttachment for interconnect attachments. |
| `l2_forwarding` | String | L2 Interconnect Attachment related config. This field is required if the
type is L2_DEDICATED.

The configuration specifies how VLAN tags (like dot1q, qinq, or dot1ad)
within L2 packets are mapped to the destination appliances IP addresses.
The packet is then encapsulated with the appliance IP address and sent to
the edge appliance. |
| `bandwidth` | String | Provisioned bandwidth capacity for the interconnect attachment. For
attachments of type DEDICATED, the user can set the bandwidth.
For attachments of type PARTNER, the Google Partner that is operating
the interconnect must set the bandwidth.
Output only for PARTNER type, mutable for PARTNER_PROVIDER and DEDICATED,
and can take one of the following values:
   
   - BPS_50M: 50 Mbit/s
   - BPS_100M: 100 Mbit/s
   - BPS_200M: 200 Mbit/s
   - BPS_300M: 300 Mbit/s
   - BPS_400M: 400 Mbit/s
   - BPS_500M: 500 Mbit/s
   - BPS_1G: 1 Gbit/s
   - BPS_2G: 2 Gbit/s
   - BPS_5G: 5 Gbit/s
   - BPS_10G: 10 Gbit/s
   - BPS_20G: 20 Gbit/s
   - BPS_50G: 50 Gbit/s
   - BPS_100G: 100 Gbit/s |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `labels` | HashMap<String, String> | Labels for this resource. These can only be added or modified by thesetLabels method. Each label key/value pair must comply withRFC1035.
Label values may be empty. |
| `operational_status` | String | [Output Only] The current status of whether or not this interconnect
attachment is functional, which can take one of the following values:
   
   - OS_ACTIVE: The attachment has been turned up and is ready to
   use. 
   - OS_UNPROVISIONED: The attachment is not ready to use yet,
   because turnup is not complete. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `self_link_with_id` | String | [Output Only] Server-defined URL for this resource with the resource id. |
| `cloud_router_ipv6_interface_id` | String | This field is not available. |
| `type` | String | The type of interconnect attachment this is, which can take one of the
following values:
   
   - DEDICATED: an attachment to a Dedicated Interconnect.
   - PARTNER: an attachment to a Partner Interconnect, created by the
   customer.
   - PARTNER_PROVIDER: an attachment to a Partner Interconnect, created by
   the partner.

- L2_DEDICATED: a L2 attachment to a Dedicated Interconnect. |
| `admin_enabled` | bool | Determines whether this Attachment will carry packets.
Not present for PARTNER_PROVIDER. |
| `description` | String | An optional description of this resource. |
| `params` | String | Input only. [Input Only] Additional params passed with the request, but not persisted
as part of resource payload. |
| `remote_service` | String | [Output Only]
If the attachment is on a Cross-Cloud Interconnect connection, this field
contains the interconnect's remote location service provider. Example
values: "Amazon Web Services" "Microsoft Azure".

The field is set only for attachments on Cross-Cloud Interconnect
connections. Its value is copied from the InterconnectRemoteLocation
remoteService field. |
| `cloud_router_ip_address` | String | [Output Only] IPv4 address + prefix length to be configured on Cloud Router
Interface for this interconnect attachment. |
| `multicast_enabled` | bool | Whether or not to permit multicast traffic for this attachment. Multicast
packets will be dropped if this is not enabled. |
| `stack_type` | String | The stack type for this interconnect attachment to identify whether the
IPv6 feature is enabled or not. If not specified, IPV4_ONLY
will be used.

This field can be both set at interconnect attachments creation and
update interconnect attachment operations. |
| `region` | String | [Output Only] URL of the region where the regional interconnect attachment
resides.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `partner_metadata` | String | Informational metadata about Partner attachments from Partners to display
to customers.
Output only for PARTNER type, mutable for PARTNER_PROVIDER, not
available for DEDICATED. |
| `candidate_customer_router_ip_address` | String | Single IPv4 address + prefix length to be configured on the customer router
interface for this interconnect attachment. |
| `attachment_group` | String | [Output Only] URL of the AttachmentGroup that includes this Attachment. |
| `customer_router_ipv6_interface_id` | String | This field is not available. |
| `edge_availability_domain` | String | Input only. Desired availability domain for the attachment. Only available for type
PARTNER, at creation time, and can take one of the following values:
   
   - AVAILABILITY_DOMAIN_ANY
   - AVAILABILITY_DOMAIN_1
   - AVAILABILITY_DOMAIN_2


For improved reliability, customers should configure a pair of attachments,
one per availability domain. The selected availability domain will be
provided to the Partner via the pairing key, so that the provisioned
circuit will lie in the specified domain. If not specified, the value will
default to AVAILABILITY_DOMAIN_ANY. |
| `satisfies_pzs` | bool | [Output Only] Reserved for future use. |
| `state` | String | [Output Only] The current state of this attachment's functionality.
Enum values ACTIVE and UNPROVISIONED are shared by DEDICATED/PRIVATE,
PARTNER, and PARTNER_PROVIDER interconnect attachments, while enum values
PENDING_PARTNER, PARTNER_REQUEST_RECEIVED, and PENDING_CUSTOMER are used
for only PARTNER and PARTNER_PROVIDER interconnect attachments.
This state can take one of the following values:
   
   - ACTIVE: The attachment has been turned up and is ready to use.
   - UNPROVISIONED: The attachment is not ready to use yet, because turnup
   is not complete.
   - PENDING_PARTNER: A newly-created PARTNER attachment that has not yet
   been configured on the Partner side.
   - PARTNER_REQUEST_RECEIVED: A PARTNER attachment is in the process of
   provisioning after a PARTNER_PROVIDER attachment was created that
   references it. 
   - PENDING_CUSTOMER: A PARTNER or PARTNER_PROVIDER
   attachment that is waiting for a customer to activate it. 
   - DEFUNCT:
   The attachment was deleted externally and is no longer functional. This
   could be because the associated Interconnect was removed, or because the
   other side of a Partner attachment was deleted. |
| `label_fingerprint` | String | A fingerprint for the labels being applied to this InterconnectAttachment,
which is essentially a hash of the labels set used for optimistic locking.
The fingerprint is initially generated by Compute Engine and changes after
every request to modify or update labels. You must always provide an
up-to-date fingerprint hash in order to update or change labels,
otherwise the request will fail with error412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve an InterconnectAttachment. |
| `interconnect` | String | URL of the underlying Interconnect object that this attachment's traffic
will traverse through. |
| `candidate_cloud_router_ip_address` | String | Single IPv4 address + prefix length to be configured on the cloud router
interface for this interconnect attachment.
   
   - Both candidate_cloud_router_ip_address and
   candidate_customer_router_ip_address fields must be set or both must be
   unset.
   - Prefix length of both candidate_cloud_router_ip_address and
   candidate_customer_router_ip_address must be the same.
   - Max prefix length is 31. |
| `candidate_cloud_router_ipv6_address` | String | Single IPv6 address + prefix length to be configured on the cloud router
interface for this interconnect attachment.
   
   - Both candidate_cloud_router_ipv6_address and
   candidate_customer_router_ipv6_address fields must be set or both must be
   unset.
   - Prefix length of both candidate_cloud_router_ipv6_address and
   candidate_customer_router_ipv6_address must be the same.
   - Max prefix length is 126. |
| `customer_router_ipv6_address` | String | [Output Only] IPv6 address + prefix length to be configured on the
customer router subinterface for this interconnect attachment. |
| `encryption` | String | Indicates the user-supplied encryption option of this VLAN attachment
(interconnectAttachment). Can only be specified at attachment creation
for PARTNER or DEDICATED attachments.
Possible values are:
   
   - NONE - This is the default value, which means that the
   VLAN attachment carries unencrypted traffic. VMs are able to send
   traffic to, or receive traffic from, such a VLAN attachment.
   - IPSEC - The VLAN attachment carries only encrypted
   traffic that is encrypted by an IPsec device, such as an HA VPN gateway or
   third-party IPsec VPN. VMs cannot directly send traffic to, or receive
   traffic from, such a VLAN attachment. To use *HA VPN over Cloud
   Interconnect*, the VLAN attachment must be created with this
   option. |
| `candidate_customer_router_ipv6_address` | String | Single IPv6 address + prefix length to be configured on the customer router
interface for this interconnect attachment. |
| `customer_router_ip_address` | String | [Output Only] IPv4 address + prefix length to be configured on the customer
router subinterface for this interconnect attachment. |
| `ipsec_internal_addresses` | Vec<String> | A list of URLs of addresses that have been reserved for the VLAN
attachment. Used only for the VLAN attachment that has the encryption
option as IPSEC. The addresses must be regional internal IP address ranges.
When creating an HA VPN gateway over the VLAN attachment, if the attachment
is configured to use a regional internal IP address, then the VPN gateway's
IP address is allocated from the IP address range specified here. For
example, if the HA VPN gateway's interface 0 is paired to this VLAN
attachment, then a regional internal IP address for the VPN gateway
interface 0 will be allocated from the IP address specified for this
VLAN attachment.
If this field is not specified when creating the VLAN attachment, then
later on when creating an HA VPN gateway on this VLAN attachment, the HA
VPN gateway's IP address is allocated from the regional external IP address
pool. |
| `candidate_subnets` | Vec<String> | Input only. Up to 16 candidate prefixes that can be used to restrict the allocation
of cloudRouterIpAddress and customerRouterIpAddress for this attachment.
All prefixes must be within link-local address space (169.254.0.0/16) and
must be /29 or shorter (/28, /27, etc). Google will attempt to select an
unused /29 from the supplied candidate prefix(es). The request will fail if
all possible /29s are in use on Google's edge. If not supplied, Google will
randomly select an unused /29 from all of link-local space. |
| `mtu` | i64 | Maximum Transmission Unit (MTU), in bytes, of packets passing through this
interconnect attachment.
Valid values are 1440, 1460, 1500, and 8896. If not specified,
the value will default to 1440. |
| `cloud_router_ipv6_address` | String | [Output Only] IPv6 address + prefix length to be configured on Cloud
Router Interface for this interconnect attachment. |
| `google_reference_id` | String | [Output Only] Google reference ID, to be used when raising support tickets
with Google or otherwise to debug backend connectivity issues.
[Deprecated] This field is not used. |
| `name` | String | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `router` | String | URL of the Cloud Router to be used for dynamic routing. This router must be
in the same region as this InterconnectAttachment. The
InterconnectAttachment will automatically connect the Interconnect to the
network & region within which the Cloud Router is configured. |
| `private_interconnect_info` | String | [Output Only] Information specific to an InterconnectAttachment.
This property is populated if the interconnect that
this is attached to is of type DEDICATED. |
| `subnet_length` | i64 | Input only. Length of the IPv4 subnet mask.
Allowed values:
   
   
    - 29 (default)
    - 30

The default value is 29, except for Cross-Cloud Interconnect
connections that use an InterconnectRemoteLocation with a
constraints.subnetLengthRange.min equal to 30. For example,
connections that use an Azure remote location fall into this
category. In these cases, the default value is 30, and requesting
29 returns an error.

Where both 29 and 30 are allowed, 29 is preferred, because it gives
Google Cloud Support more debugging visibility. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `dataplane_version` | i64 | [Output Only] Dataplane version for this InterconnectAttachment. This
field is only present for Dataplane version 2 and higher. Absence of this
field in the API output indicates that the Dataplane is version 1. |
| `vlan_tag8021q` | i64 | The IEEE 802.1Q VLAN tag for this attachment, in the range 2-4093.
Only specified at creation time. |
| `candidate_ipv6_subnets` | Vec<String> | This field is not available. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create interconnect_attachment
interconnect_attachment = provider.compute_api.Interconnect_attachment {
    region = "value"  # Name of the region for this request.
    project = "value"  # Project ID for this request.
}

# Access interconnect_attachment outputs
interconnect_attachment_id = interconnect_attachment.id
interconnect_attachment_pairing_key = interconnect_attachment.pairing_key
interconnect_attachment_partner_asn = interconnect_attachment.partner_asn
interconnect_attachment_configuration_constraints = interconnect_attachment.configuration_constraints
interconnect_attachment_kind = interconnect_attachment.kind
interconnect_attachment_l2_forwarding = interconnect_attachment.l2_forwarding
interconnect_attachment_bandwidth = interconnect_attachment.bandwidth
interconnect_attachment_id = interconnect_attachment.id
interconnect_attachment_labels = interconnect_attachment.labels
interconnect_attachment_operational_status = interconnect_attachment.operational_status
interconnect_attachment_self_link = interconnect_attachment.self_link
interconnect_attachment_self_link_with_id = interconnect_attachment.self_link_with_id
interconnect_attachment_cloud_router_ipv6_interface_id = interconnect_attachment.cloud_router_ipv6_interface_id
interconnect_attachment_type = interconnect_attachment.type
interconnect_attachment_admin_enabled = interconnect_attachment.admin_enabled
interconnect_attachment_description = interconnect_attachment.description
interconnect_attachment_params = interconnect_attachment.params
interconnect_attachment_remote_service = interconnect_attachment.remote_service
interconnect_attachment_cloud_router_ip_address = interconnect_attachment.cloud_router_ip_address
interconnect_attachment_multicast_enabled = interconnect_attachment.multicast_enabled
interconnect_attachment_stack_type = interconnect_attachment.stack_type
interconnect_attachment_region = interconnect_attachment.region
interconnect_attachment_partner_metadata = interconnect_attachment.partner_metadata
interconnect_attachment_candidate_customer_router_ip_address = interconnect_attachment.candidate_customer_router_ip_address
interconnect_attachment_attachment_group = interconnect_attachment.attachment_group
interconnect_attachment_customer_router_ipv6_interface_id = interconnect_attachment.customer_router_ipv6_interface_id
interconnect_attachment_edge_availability_domain = interconnect_attachment.edge_availability_domain
interconnect_attachment_satisfies_pzs = interconnect_attachment.satisfies_pzs
interconnect_attachment_state = interconnect_attachment.state
interconnect_attachment_label_fingerprint = interconnect_attachment.label_fingerprint
interconnect_attachment_interconnect = interconnect_attachment.interconnect
interconnect_attachment_candidate_cloud_router_ip_address = interconnect_attachment.candidate_cloud_router_ip_address
interconnect_attachment_candidate_cloud_router_ipv6_address = interconnect_attachment.candidate_cloud_router_ipv6_address
interconnect_attachment_customer_router_ipv6_address = interconnect_attachment.customer_router_ipv6_address
interconnect_attachment_encryption = interconnect_attachment.encryption
interconnect_attachment_candidate_customer_router_ipv6_address = interconnect_attachment.candidate_customer_router_ipv6_address
interconnect_attachment_customer_router_ip_address = interconnect_attachment.customer_router_ip_address
interconnect_attachment_ipsec_internal_addresses = interconnect_attachment.ipsec_internal_addresses
interconnect_attachment_candidate_subnets = interconnect_attachment.candidate_subnets
interconnect_attachment_mtu = interconnect_attachment.mtu
interconnect_attachment_cloud_router_ipv6_address = interconnect_attachment.cloud_router_ipv6_address
interconnect_attachment_google_reference_id = interconnect_attachment.google_reference_id
interconnect_attachment_name = interconnect_attachment.name
interconnect_attachment_router = interconnect_attachment.router
interconnect_attachment_private_interconnect_info = interconnect_attachment.private_interconnect_info
interconnect_attachment_subnet_length = interconnect_attachment.subnet_length
interconnect_attachment_creation_timestamp = interconnect_attachment.creation_timestamp
interconnect_attachment_dataplane_version = interconnect_attachment.dataplane_version
interconnect_attachment_vlan_tag8021q = interconnect_attachment.vlan_tag8021q
interconnect_attachment_candidate_ipv6_subnets = interconnect_attachment.candidate_ipv6_subnets
```

---


### Project

Enable service resource (a.k.a service project) for a host project, so that
subnets in the host project can be used by instances in the service
project.

**Operations**: ✅ Create ✅ Read

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `xpn_resource` | String |  | Service resource (a.k.a service project) ID. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `vm_dns_setting` | String | [Output Only] Default internal DNS setting used by VMs running in
this project. |
| `common_instance_metadata` | String | Metadata key/value pairs available to all instances contained in this
project. See Custom
metadata for more information. |
| `enabled_features` | Vec<String> | An optional list of restricted features enabled for use on this project. |
| `xpn_project_status` | String | [Output Only] The role this project has in a shared VPC configuration.
Currently, only projects with the host role, which is specified by the
value HOST, are differentiated. |
| `default_network_tier` | String | This signifies the default network tier used for configuring resources of
the project and can only take the following values:PREMIUM, STANDARD. Initially the default network
tier is PREMIUM. |
| `kind` | String | [Output Only] Type of the resource. Always compute#project for
projects. |
| `quotas` | Vec<String> | [Output Only] Quotas assigned to this project. |
| `cloud_armor_tier` | String | [Output Only] The Cloud Armor tier for this project. It can be one of the
following values: CA_STANDARD,CA_ENTERPRISE_PAYGO.

If this field is not specified, it is assumed to beCA_STANDARD. |
| `managed_protection_tier` | String | [Output Only] The Cloud Armor Managed Protection (CAMP) tier for
this project. It can be one of the following values:CA_STANDARD, CAMP_PLUS_PAYGO.

If this field is not specified, it is assumed to beCA_STANDARD. |
| `default_service_account` | String | [Output Only] Default service account used by VMs running in this project. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. This is *not* the project ID, and is
just a unique ID used by Compute Engine to identify resources. |
| `usage_export_location` | String | An optional naming prefix for daily usage reports and the Google Cloud
Storage bucket where they are stored. |
| `description` | String | An optional textual description of the resource. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `name` | String | The project ID. For example: my-example-project. Use the
project ID to make requests to Compute Engine. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create project
project = provider.compute_api.Project {
    project = "value"  # Project ID for this request.
}

# Access project outputs
project_id = project.id
project_vm_dns_setting = project.vm_dns_setting
project_common_instance_metadata = project.common_instance_metadata
project_enabled_features = project.enabled_features
project_xpn_project_status = project.xpn_project_status
project_default_network_tier = project.default_network_tier
project_kind = project.kind
project_quotas = project.quotas
project_cloud_armor_tier = project.cloud_armor_tier
project_managed_protection_tier = project.managed_protection_tier
project_default_service_account = project.default_service_account
project_id = project.id
project_usage_export_location = project.usage_export_location
project_description = project.description
project_self_link = project.self_link
project_creation_timestamp = project.creation_timestamp
project_name = project.name
```

---


### Region_url_map

Creates a UrlMap resource in the specified project using
the data included in the request.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `default_route_action` | String |  | defaultRouteAction takes effect when none of the 
hostRules match. The load balancer performs advanced routing
actions, such as URL rewrites and header transformations, before forwarding
the request to the selected backend.


Only one of defaultUrlRedirect, defaultService
or defaultRouteAction.weightedBackendService can be set.


 URL maps for classic Application Load Balancers only support
the urlRewrite action within defaultRouteAction.


defaultRouteAction has no effect when the URL map is bound
to a target gRPC proxy that has the validateForProxyless field
set to true. |
| `default_custom_error_response_policy` | String |  | defaultCustomErrorResponsePolicy specifies how the Load
Balancer returns error responses when BackendServiceorBackendBucket responds with an error. 

This policy takes
effect at the load balancer level and applies only when no policy has been
defined for the error code at lower levels like PathMatcher, RouteRule and
PathRule within this UrlMap. 

For example, consider a UrlMap with the
following configuration:
   
   
     - defaultCustomErrorResponsePolicy containing policies for
     responding to 5xx and 4xx errors
     - A PathMatcher configured for *.example.com has
     defaultCustomErrorResponsePolicy for 4xx.

If a request for http://www.example.com/ encounters a404, the policy inpathMatcher.defaultCustomErrorResponsePolicy will be enforced.
When the request for http://www.example.com/ encounters a502, the policy inUrlMap.defaultCustomErrorResponsePolicy will be enforced. When
a request that does not match any host in *.example.com such
as http://www.myotherexample.com/, encounters a404, UrlMap.defaultCustomErrorResponsePolicy
takes effect. 

When used in conjunction withdefaultRouteAction.retryPolicy, retries take precedence. Only
once all retries are exhausted, thedefaultCustomErrorResponsePolicy is applied. While attempting
a retry, if load balancer is successful in reaching the
service, the defaultCustomErrorResponsePolicy is ignored and
the response from the service is returned to the client.

defaultCustomErrorResponsePolicy is supported only for
global external Application Load Balancers. |
| `default_url_redirect` | String |  | When none of the specified hostRules match, the request
is redirected to a URL specified by defaultUrlRedirect.


Only one of defaultUrlRedirect, defaultService
or defaultRouteAction.weightedBackendService can be set.


Not supported when the URL map is bound to a target gRPC proxy. |
| `region` | String |  | [Output Only] URL of the region where the regional URL map resides.
This field is not applicable to global URL maps.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `header_action` | String |  | Specifies changes to request and response headers that need to take effect
for the selected backendService.

The headerAction specified here take effect afterheaderAction specified under pathMatcher.

headerAction is not supported for load balancers
that have
their loadBalancingScheme set to EXTERNAL.

Not supported when the URL map is bound to a target gRPC proxy that
has validateForProxyless field set to true. |
| `path_matchers` | Vec<String> |  | The list of named PathMatchers to use against the URL. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `name` | String |  | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `tests` | Vec<String> |  | The list of expected URL mapping tests. Request to update theUrlMap succeeds only if all test cases pass. You can specify a
maximum of 100 tests per UrlMap.

Not supported when the URL map is bound to a target gRPC proxy that
has validateForProxyless field set to true. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `default_service` | String |  | The full or partial URL of the defaultService resource to
which traffic is directed if none of the hostRules match.
If defaultRouteAction is also specified, advanced
routing actions, such as URL rewrites, take effect before sending the
request to the backend.


Only one of defaultUrlRedirect, defaultService
or defaultRouteAction.weightedBackendService can be set.


defaultService has no effect when the URL map is bound
to a target gRPC proxy that has the validateForProxyless field
set to true. |
| `kind` | String |  | [Output Only] Type of the resource. Always compute#urlMaps for
url maps. |
| `fingerprint` | String |  | Fingerprint of this resource. A hash of the contents stored in this object.
This field is used in optimistic locking. This field is ignored when
inserting a UrlMap. An up-to-date fingerprint must be provided
in order to update the UrlMap, otherwise the request will
fail with error 412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve a UrlMap. |
| `host_rules` | Vec<String> |  | The list of host rules to use against the URL. |
| `project` | String | ✅ | Project ID for this request. |
| `region` | String | ✅ | Name of the region scoping this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `default_route_action` | String | defaultRouteAction takes effect when none of the 
hostRules match. The load balancer performs advanced routing
actions, such as URL rewrites and header transformations, before forwarding
the request to the selected backend.


Only one of defaultUrlRedirect, defaultService
or defaultRouteAction.weightedBackendService can be set.


 URL maps for classic Application Load Balancers only support
the urlRewrite action within defaultRouteAction.


defaultRouteAction has no effect when the URL map is bound
to a target gRPC proxy that has the validateForProxyless field
set to true. |
| `default_custom_error_response_policy` | String | defaultCustomErrorResponsePolicy specifies how the Load
Balancer returns error responses when BackendServiceorBackendBucket responds with an error. 

This policy takes
effect at the load balancer level and applies only when no policy has been
defined for the error code at lower levels like PathMatcher, RouteRule and
PathRule within this UrlMap. 

For example, consider a UrlMap with the
following configuration:
   
   
     - defaultCustomErrorResponsePolicy containing policies for
     responding to 5xx and 4xx errors
     - A PathMatcher configured for *.example.com has
     defaultCustomErrorResponsePolicy for 4xx.

If a request for http://www.example.com/ encounters a404, the policy inpathMatcher.defaultCustomErrorResponsePolicy will be enforced.
When the request for http://www.example.com/ encounters a502, the policy inUrlMap.defaultCustomErrorResponsePolicy will be enforced. When
a request that does not match any host in *.example.com such
as http://www.myotherexample.com/, encounters a404, UrlMap.defaultCustomErrorResponsePolicy
takes effect. 

When used in conjunction withdefaultRouteAction.retryPolicy, retries take precedence. Only
once all retries are exhausted, thedefaultCustomErrorResponsePolicy is applied. While attempting
a retry, if load balancer is successful in reaching the
service, the defaultCustomErrorResponsePolicy is ignored and
the response from the service is returned to the client.

defaultCustomErrorResponsePolicy is supported only for
global external Application Load Balancers. |
| `default_url_redirect` | String | When none of the specified hostRules match, the request
is redirected to a URL specified by defaultUrlRedirect.


Only one of defaultUrlRedirect, defaultService
or defaultRouteAction.weightedBackendService can be set.


Not supported when the URL map is bound to a target gRPC proxy. |
| `region` | String | [Output Only] URL of the region where the regional URL map resides.
This field is not applicable to global URL maps.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `header_action` | String | Specifies changes to request and response headers that need to take effect
for the selected backendService.

The headerAction specified here take effect afterheaderAction specified under pathMatcher.

headerAction is not supported for load balancers
that have
their loadBalancingScheme set to EXTERNAL.

Not supported when the URL map is bound to a target gRPC proxy that
has validateForProxyless field set to true. |
| `path_matchers` | Vec<String> | The list of named PathMatchers to use against the URL. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `name` | String | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `tests` | Vec<String> | The list of expected URL mapping tests. Request to update theUrlMap succeeds only if all test cases pass. You can specify a
maximum of 100 tests per UrlMap.

Not supported when the URL map is bound to a target gRPC proxy that
has validateForProxyless field set to true. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `default_service` | String | The full or partial URL of the defaultService resource to
which traffic is directed if none of the hostRules match.
If defaultRouteAction is also specified, advanced
routing actions, such as URL rewrites, take effect before sending the
request to the backend.


Only one of defaultUrlRedirect, defaultService
or defaultRouteAction.weightedBackendService can be set.


defaultService has no effect when the URL map is bound
to a target gRPC proxy that has the validateForProxyless field
set to true. |
| `kind` | String | [Output Only] Type of the resource. Always compute#urlMaps for
url maps. |
| `fingerprint` | String | Fingerprint of this resource. A hash of the contents stored in this object.
This field is used in optimistic locking. This field is ignored when
inserting a UrlMap. An up-to-date fingerprint must be provided
in order to update the UrlMap, otherwise the request will
fail with error 412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve a UrlMap. |
| `host_rules` | Vec<String> | The list of host rules to use against the URL. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create region_url_map
region_url_map = provider.compute_api.Region_url_map {
    project = "value"  # Project ID for this request.
    region = "value"  # Name of the region scoping this request.
}

# Access region_url_map outputs
region_url_map_id = region_url_map.id
region_url_map_default_route_action = region_url_map.default_route_action
region_url_map_default_custom_error_response_policy = region_url_map.default_custom_error_response_policy
region_url_map_default_url_redirect = region_url_map.default_url_redirect
region_url_map_region = region_url_map.region
region_url_map_self_link = region_url_map.self_link
region_url_map_header_action = region_url_map.header_action
region_url_map_path_matchers = region_url_map.path_matchers
region_url_map_creation_timestamp = region_url_map.creation_timestamp
region_url_map_name = region_url_map.name
region_url_map_tests = region_url_map.tests
region_url_map_id = region_url_map.id
region_url_map_description = region_url_map.description
region_url_map_default_service = region_url_map.default_service
region_url_map_kind = region_url_map.kind
region_url_map_fingerprint = region_url_map.fingerprint
region_url_map_host_rules = region_url_map.host_rules
```

---


### Region_target_tcp_proxie

Creates a TargetTcpProxy resource in the specified project and region using
the data included in the request.

**Operations**: ✅ Create ✅ Read ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `service` | String |  | URL to the BackendService resource. |
| `name` | String |  | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `proxy_header` | String |  | Specifies the type of proxy header to append before sending data to the
backend, either NONE or PROXY_V1. The default
is NONE. |
| `kind` | String |  | [Output Only] Type of the resource. Alwayscompute#targetTcpProxy for target TCP proxies. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `proxy_bind` | bool |  | This field only applies when the forwarding rule that references this
target proxy has a loadBalancingScheme set toINTERNAL_SELF_MANAGED.

When this field is set to true, Envoy proxies set up inbound
traffic interception and bind to the IP address and port specified in the
forwarding rule. This is generally useful when using Traffic Director to
configure Envoy as a gateway or middle proxy (in other words, not a
sidecar proxy). The Envoy proxy listens for inbound requests and handles
requests when it receives them.

The default is false. |
| `region` | String |  | [Output Only] URL of the region where the regional TCP proxy resides.
This field is not applicable to global TCP proxy. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `region` | String | ✅ | Name of the region scoping this request. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `service` | String | URL to the BackendService resource. |
| `name` | String | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `proxy_header` | String | Specifies the type of proxy header to append before sending data to the
backend, either NONE or PROXY_V1. The default
is NONE. |
| `kind` | String | [Output Only] Type of the resource. Alwayscompute#targetTcpProxy for target TCP proxies. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `proxy_bind` | bool | This field only applies when the forwarding rule that references this
target proxy has a loadBalancingScheme set toINTERNAL_SELF_MANAGED.

When this field is set to true, Envoy proxies set up inbound
traffic interception and bind to the IP address and port specified in the
forwarding rule. This is generally useful when using Traffic Director to
configure Envoy as a gateway or middle proxy (in other words, not a
sidecar proxy). The Envoy proxy listens for inbound requests and handles
requests when it receives them.

The default is false. |
| `region` | String | [Output Only] URL of the region where the regional TCP proxy resides.
This field is not applicable to global TCP proxy. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create region_target_tcp_proxie
region_target_tcp_proxie = provider.compute_api.Region_target_tcp_proxie {
    region = "value"  # Name of the region scoping this request.
    project = "value"  # Project ID for this request.
}

# Access region_target_tcp_proxie outputs
region_target_tcp_proxie_id = region_target_tcp_proxie.id
region_target_tcp_proxie_self_link = region_target_tcp_proxie.self_link
region_target_tcp_proxie_creation_timestamp = region_target_tcp_proxie.creation_timestamp
region_target_tcp_proxie_service = region_target_tcp_proxie.service
region_target_tcp_proxie_name = region_target_tcp_proxie.name
region_target_tcp_proxie_proxy_header = region_target_tcp_proxie.proxy_header
region_target_tcp_proxie_kind = region_target_tcp_proxie.kind
region_target_tcp_proxie_description = region_target_tcp_proxie.description
region_target_tcp_proxie_proxy_bind = region_target_tcp_proxie.proxy_bind
region_target_tcp_proxie_region = region_target_tcp_proxie.region
region_target_tcp_proxie_id = region_target_tcp_proxie.id
```

---


### Https_health_check

Creates a HttpsHealthCheck resource in the specified project using the data
included in the request.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `self_link_with_id` | String |  | [Output Only] Server-defined URL for this resource with the resource id. |
| `unhealthy_threshold` | i64 |  | A so-far healthy instance will be marked unhealthy after this
many consecutive failures. The default value is 2. |
| `port` | i64 |  | The TCP port number for the HTTPS health check request. The default value
is 443. |
| `healthy_threshold` | i64 |  | A so-far unhealthy instance will be marked healthy after this
many consecutive successes. The default value is 2. |
| `check_interval_sec` | i64 |  | How often (in seconds) to send a health check. The default value is 5
seconds. |
| `name` | String |  | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `request_path` | String |  | The request path of the HTTPS health check request. The default value is
"/". Must comply withRFC3986. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `timeout_sec` | i64 |  | How long (in seconds) to wait before claiming failure. The default value is
5 seconds. It is invalid for timeoutSec to have a greater value than
checkIntervalSec. |
| `host` | String |  | The value of the host header in the HTTPS health check request. If left
empty (default value), the public IP on behalf of which this health check
is performed will be used. |
| `kind` | String |  | Type of the resource. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `self_link_with_id` | String | [Output Only] Server-defined URL for this resource with the resource id. |
| `unhealthy_threshold` | i64 | A so-far healthy instance will be marked unhealthy after this
many consecutive failures. The default value is 2. |
| `port` | i64 | The TCP port number for the HTTPS health check request. The default value
is 443. |
| `healthy_threshold` | i64 | A so-far unhealthy instance will be marked healthy after this
many consecutive successes. The default value is 2. |
| `check_interval_sec` | i64 | How often (in seconds) to send a health check. The default value is 5
seconds. |
| `name` | String | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `request_path` | String | The request path of the HTTPS health check request. The default value is
"/". Must comply withRFC3986. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `timeout_sec` | i64 | How long (in seconds) to wait before claiming failure. The default value is
5 seconds. It is invalid for timeoutSec to have a greater value than
checkIntervalSec. |
| `host` | String | The value of the host header in the HTTPS health check request. If left
empty (default value), the public IP on behalf of which this health check
is performed will be used. |
| `kind` | String | Type of the resource. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create https_health_check
https_health_check = provider.compute_api.Https_health_check {
    project = "value"  # Project ID for this request.
}

# Access https_health_check outputs
https_health_check_id = https_health_check.id
https_health_check_id = https_health_check.id
https_health_check_creation_timestamp = https_health_check.creation_timestamp
https_health_check_self_link_with_id = https_health_check.self_link_with_id
https_health_check_unhealthy_threshold = https_health_check.unhealthy_threshold
https_health_check_port = https_health_check.port
https_health_check_healthy_threshold = https_health_check.healthy_threshold
https_health_check_check_interval_sec = https_health_check.check_interval_sec
https_health_check_name = https_health_check.name
https_health_check_request_path = https_health_check.request_path
https_health_check_description = https_health_check.description
https_health_check_self_link = https_health_check.self_link
https_health_check_timeout_sec = https_health_check.timeout_sec
https_health_check_host = https_health_check.host
https_health_check_kind = https_health_check.kind
```

---


### Cross_site_network

Creates a cross-site network in the specified project in the given scope
using the parameters that are included in the request.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `kind` | String |  | [Output Only] Type of the resource. Alwayscompute#crossSiteNetwork for cross-site networks. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `self_link_with_id` | String |  | [Output Only] Server-defined URL for this resource with the resource id. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `id` | String |  | [Output Only] The unique identifier for the resource type. The server
generates this identifier. |
| `name` | String |  | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `description` | String |  | An optional description of the cross-site network. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `kind` | String | [Output Only] Type of the resource. Alwayscompute#crossSiteNetwork for cross-site networks. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `self_link_with_id` | String | [Output Only] Server-defined URL for this resource with the resource id. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `id` | String | [Output Only] The unique identifier for the resource type. The server
generates this identifier. |
| `name` | String | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `description` | String | An optional description of the cross-site network. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create cross_site_network
cross_site_network = provider.compute_api.Cross_site_network {
    project = "value"  # Project ID for this request.
}

# Access cross_site_network outputs
cross_site_network_id = cross_site_network.id
cross_site_network_kind = cross_site_network.kind
cross_site_network_self_link = cross_site_network.self_link
cross_site_network_self_link_with_id = cross_site_network.self_link_with_id
cross_site_network_creation_timestamp = cross_site_network.creation_timestamp
cross_site_network_id = cross_site_network.id
cross_site_network_name = cross_site_network.name
cross_site_network_description = cross_site_network.description
```

---


### Resource_policie

Creates a new resource policy.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `instance_schedule_policy` | String |  | Resource policy for scheduling instance operations. |
| `workload_policy` | String |  | Resource policy for defining instance placement for MIGs. |
| `status` | String |  | [Output Only] The status of resource policy creation. |
| `self_link` | String |  | [Output Only] Server-defined fully-qualified URL for this resource. |
| `disk_consistency_group_policy` | String |  | Resource policy for disk consistency groups. |
| `description` | String |  |  |
| `group_placement_policy` | String |  | Resource policy for instances for placement configuration. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `region` | String |  |  |
| `self_link_with_id` | String |  | [Output Only] Server-defined URL for this resource with the resource id. |
| `name` | String |  | The name of the resource, provided by the client when initially creating
the resource. The resource name must be 1-63 characters long, and comply
withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must be
a dash, lowercase letter, or digit, except the last character, which cannot
be a dash. |
| `vm_maintenance_policy` | String |  | Resource policy applicable to VMs for infrastructure maintenance. |
| `resource_status` | String |  | [Output Only] The system status of the resource policy. |
| `kind` | String |  | [Output Only] Type of the resource. Alwayscompute#resource_policies for resource policies. |
| `snapshot_schedule_policy` | String |  | Resource policy for persistent disks for creating snapshots. |
| `region` | String | ✅ | Name of the region for this request. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `instance_schedule_policy` | String | Resource policy for scheduling instance operations. |
| `workload_policy` | String | Resource policy for defining instance placement for MIGs. |
| `status` | String | [Output Only] The status of resource policy creation. |
| `self_link` | String | [Output Only] Server-defined fully-qualified URL for this resource. |
| `disk_consistency_group_policy` | String | Resource policy for disk consistency groups. |
| `description` | String |  |
| `group_placement_policy` | String | Resource policy for instances for placement configuration. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `region` | String |  |
| `self_link_with_id` | String | [Output Only] Server-defined URL for this resource with the resource id. |
| `name` | String | The name of the resource, provided by the client when initially creating
the resource. The resource name must be 1-63 characters long, and comply
withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must be
a dash, lowercase letter, or digit, except the last character, which cannot
be a dash. |
| `vm_maintenance_policy` | String | Resource policy applicable to VMs for infrastructure maintenance. |
| `resource_status` | String | [Output Only] The system status of the resource policy. |
| `kind` | String | [Output Only] Type of the resource. Alwayscompute#resource_policies for resource policies. |
| `snapshot_schedule_policy` | String | Resource policy for persistent disks for creating snapshots. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create resource_policie
resource_policie = provider.compute_api.Resource_policie {
    region = "value"  # Name of the region for this request.
    project = "value"  # Project ID for this request.
}

# Access resource_policie outputs
resource_policie_id = resource_policie.id
resource_policie_instance_schedule_policy = resource_policie.instance_schedule_policy
resource_policie_workload_policy = resource_policie.workload_policy
resource_policie_status = resource_policie.status
resource_policie_self_link = resource_policie.self_link
resource_policie_disk_consistency_group_policy = resource_policie.disk_consistency_group_policy
resource_policie_description = resource_policie.description
resource_policie_group_placement_policy = resource_policie.group_placement_policy
resource_policie_id = resource_policie.id
resource_policie_creation_timestamp = resource_policie.creation_timestamp
resource_policie_region = resource_policie.region
resource_policie_self_link_with_id = resource_policie.self_link_with_id
resource_policie_name = resource_policie.name
resource_policie_vm_maintenance_policy = resource_policie.vm_maintenance_policy
resource_policie_resource_status = resource_policie.resource_status
resource_policie_kind = resource_policie.kind
resource_policie_snapshot_schedule_policy = resource_policie.snapshot_schedule_policy
```

---


### Image

Creates an image in the specified project using the data included
in the request.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `source_snapshot` | String |  | URL of the source snapshot used to create this image.
The following are valid formats for the URL:
   
   - https://www.googleapis.com/compute/v1/projects/project_id/global/
   snapshots/snapshot_name
   - projects/project_id/global/snapshots/snapshot_name



In order to create an image, you must provide the full or partial URL of
one of the following:
   
   - The rawDisk.source URL 
   - The sourceDisk URL 
   - The sourceImage URL 
   - The sourceSnapshot URL |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `architecture` | String |  | The architecture of the image. Valid values are
ARM64 or X86_64. |
| `rollout_override` | String |  | A rollout policy to apply to this image. When specified, the rollout policy
overrides per-zone references to the image via the associated image family.
The rollout policy restricts the zones where this image is accessible when
using a zonal image family reference. When the rollout policy does not
include the user specified zone, or if the zone is rolled out, this image
is accessible.

The rollout policy for this image is read-only, except for allowlisted
users. This field might not be configured. To view the latest
non-deprecated image in a specific zone, use theimageFamilyViews.get method. |
| `licenses` | Vec<String> |  | Any applicable license URI. |
| `deprecated` | String |  | The deprecation status associated with this image. |
| `satisfies_pzi` | bool |  | Output only. Reserved for future use. |
| `source_disk_encryption_key` | String |  | Thecustomer-supplied
encryption key of the source disk. Required if the source disk is
protected by a customer-supplied encryption key. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `source_image_id` | String |  | [Output Only]
The ID value of the image used to create this image. This value may be used
to determine whether the image was taken from the current or a previous
instance of a given image name. |
| `source_snapshot_encryption_key` | String |  | The customer-supplied encryption key of the source snapshot. Required if
the source snapshot is protected by a customer-supplied encryption key. |
| `guest_os_features` | Vec<String> |  | A list of features to enable on the guest operating system. Applicable
only for bootable images. To see a list of available options, see theguestOSfeatures[].type parameter. |
| `user_licenses` | Vec<String> |  | A list of publicly visible user-licenses. Unlike regular licenses, user
provided licenses can be modified after the disk is created. This includes
a list of URLs to the license resource. For example, to provide a debian
license:

https://www.googleapis.com/compute/v1/projects/debian-cloud/global/licenses/debian-9-stretch |
| `labels` | HashMap<String, String> |  | Labels to apply to this image. These can be later modified by
the setLabels method. |
| `enable_confidential_compute` | bool |  | Whether this image is created from a confidential compute mode disk.
[Output Only]: This field is not set by user, but from source disk. |
| `status` | String |  | [Output Only] The status of the image. An image can be used to create other
resources, such as instances, only after the image has been successfully
created and the status is set to READY. Possible
values are FAILED, PENDING, orREADY. |
| `family` | String |  | The name of the image family to which this image belongs. The image
family name can be from a publicly managed image family provided by
Compute Engine, or from a custom image family you create. For example,centos-stream-9 is a publicly available image family.
For more information, see Image
family best practices.

When creating disks, you can specify an image family instead of a specific
image name. The image family always returns its latest image that is not
deprecated. The name of the image family must comply with RFC1035. |
| `kind` | String |  | [Output Only] Type of the resource. Always compute#image for
images. |
| `name` | String |  | Name of the resource; provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must be
a dash, lowercase letter, or digit, except the last character, which cannot
be a dash. |
| `source_disk` | String |  | URL of the source disk used to create this image.
For example, the following are valid values:
   
   - https://www.googleapis.com/compute/v1/projects/project/zones/zone/disks/disk 
   - projects/project/zones/zone/disks/disk 
   - zones/zone/disks/disk



In order to create an image, you must provide the full or partial URL of
one of the following:
   
   - The rawDisk.source URL 
   - The sourceDisk URL 
   - The sourceImage URL 
   - The sourceSnapshot URL |
| `license_codes` | Vec<String> |  | Integer license codes indicating which licenses are attached to this image. |
| `params` | String |  | Input only. [Input Only] Additional params passed with the request, but not persisted
as part of resource payload. |
| `label_fingerprint` | String |  | A fingerprint for the labels being applied to this image, which is
essentially a hash of the labels used for optimistic locking. The
fingerprint is initially generated by Compute Engine and changes after
every request to modify or update labels. You must always provide an
up-to-date fingerprint hash in order to update or change labels,
otherwise the request will fail with error412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve an image. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `locked` | bool |  | A flag for marketplace VM disk created from the image, which is designed
for marketplace VM disk to prevent the proprietary data on the disk from
being accessed unwantedly. The flag will be inherited by the disk created
from the image.

The disk with locked flag set to true will be prohibited from performing
the operations below:
   
   - R/W or R/O disk attach
   - Disk detach, if disk is created via create-on-create
   - Create images
   - Create snapshots
   - Create disk clone (create disk from the current disk)



The image with the locked field set to true will be prohibited from
performing the operations below:
   
   - Create images from the current image
   - Update the locked field for the current image



The instance with at least one disk with locked flag set to true will be
prohibited from performing the operations below:
   
   - Secondary disk attach
   - Create instant snapshot
   - Create machine images
   - Create instance template
   - Delete the instance with --keep-disk parameter set to true |
| `satisfies_pzs` | bool |  | [Output Only] Reserved for future use. |
| `storage_locations` | Vec<String> |  | Cloud Storage bucket storage location of the image (regional or
multi-regional). |
| `archive_size_bytes` | String |  | Size of the image tar.gz archive stored in Google Cloud
Storage (in bytes). |
| `source_disk_id` | String |  | [Output Only]
The ID value of the disk used to create this image. This value may be used
to determine whether the image was taken from the current or a previous
instance of a given disk name. |
| `self_link_with_id` | String |  | [Output Only] Server-defined URL for this resource's resource id. |
| `source_snapshot_id` | String |  | [Output Only]
The ID value of the snapshot used to create this image. This value may be
used to determine whether the snapshot was taken from the current or a
previous instance of a given snapshot name. |
| `source_image_encryption_key` | String |  | The customer-supplied encryption key of the source image. Required if the
source image is protected by a customer-supplied encryption key. |
| `source_type` | String |  | The type of the image used to create this disk. The
default and only valid value is RAW. |
| `disk_size_gb` | String |  | Size of the image when restored onto a persistent disk (in GB). |
| `raw_disk` | String |  | The parameters of the raw disk image. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `image_encryption_key` | String |  | Encrypts the image using acustomer-supplied
encryption key.

After you encrypt an image with a customer-supplied key, you must provide
the same key if you use the image later (e.g. to create a disk from
the image).

Customer-supplied encryption keys do not protect access to metadata
of the disk.

If you do not provide an encryption key when creating the image, then the
disk will be encrypted using an automatically generated key and you do not
need to provide a key to use the image later. |
| `shielded_instance_initial_state` | String |  | Set the secure boot keys of shielded instance. |
| `source_image` | String |  | URL of the source image used to create this image.
The following are valid formats for the URL:
   
   - https://www.googleapis.com/compute/v1/projects/project_id/global/
   images/image_name
   - projects/project_id/global/images/image_name



In order to create an image, you must provide the full or partial URL of
one of the following:
   
   - The rawDisk.source URL 
   - The sourceDisk URL 
   - The sourceImage URL 
   - The sourceSnapshot URL |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `source_snapshot` | String | URL of the source snapshot used to create this image.
The following are valid formats for the URL:
   
   - https://www.googleapis.com/compute/v1/projects/project_id/global/
   snapshots/snapshot_name
   - projects/project_id/global/snapshots/snapshot_name



In order to create an image, you must provide the full or partial URL of
one of the following:
   
   - The rawDisk.source URL 
   - The sourceDisk URL 
   - The sourceImage URL 
   - The sourceSnapshot URL |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `architecture` | String | The architecture of the image. Valid values are
ARM64 or X86_64. |
| `rollout_override` | String | A rollout policy to apply to this image. When specified, the rollout policy
overrides per-zone references to the image via the associated image family.
The rollout policy restricts the zones where this image is accessible when
using a zonal image family reference. When the rollout policy does not
include the user specified zone, or if the zone is rolled out, this image
is accessible.

The rollout policy for this image is read-only, except for allowlisted
users. This field might not be configured. To view the latest
non-deprecated image in a specific zone, use theimageFamilyViews.get method. |
| `licenses` | Vec<String> | Any applicable license URI. |
| `deprecated` | String | The deprecation status associated with this image. |
| `satisfies_pzi` | bool | Output only. Reserved for future use. |
| `source_disk_encryption_key` | String | Thecustomer-supplied
encryption key of the source disk. Required if the source disk is
protected by a customer-supplied encryption key. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `source_image_id` | String | [Output Only]
The ID value of the image used to create this image. This value may be used
to determine whether the image was taken from the current or a previous
instance of a given image name. |
| `source_snapshot_encryption_key` | String | The customer-supplied encryption key of the source snapshot. Required if
the source snapshot is protected by a customer-supplied encryption key. |
| `guest_os_features` | Vec<String> | A list of features to enable on the guest operating system. Applicable
only for bootable images. To see a list of available options, see theguestOSfeatures[].type parameter. |
| `user_licenses` | Vec<String> | A list of publicly visible user-licenses. Unlike regular licenses, user
provided licenses can be modified after the disk is created. This includes
a list of URLs to the license resource. For example, to provide a debian
license:

https://www.googleapis.com/compute/v1/projects/debian-cloud/global/licenses/debian-9-stretch |
| `labels` | HashMap<String, String> | Labels to apply to this image. These can be later modified by
the setLabels method. |
| `enable_confidential_compute` | bool | Whether this image is created from a confidential compute mode disk.
[Output Only]: This field is not set by user, but from source disk. |
| `status` | String | [Output Only] The status of the image. An image can be used to create other
resources, such as instances, only after the image has been successfully
created and the status is set to READY. Possible
values are FAILED, PENDING, orREADY. |
| `family` | String | The name of the image family to which this image belongs. The image
family name can be from a publicly managed image family provided by
Compute Engine, or from a custom image family you create. For example,centos-stream-9 is a publicly available image family.
For more information, see Image
family best practices.

When creating disks, you can specify an image family instead of a specific
image name. The image family always returns its latest image that is not
deprecated. The name of the image family must comply with RFC1035. |
| `kind` | String | [Output Only] Type of the resource. Always compute#image for
images. |
| `name` | String | Name of the resource; provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must be
a dash, lowercase letter, or digit, except the last character, which cannot
be a dash. |
| `source_disk` | String | URL of the source disk used to create this image.
For example, the following are valid values:
   
   - https://www.googleapis.com/compute/v1/projects/project/zones/zone/disks/disk 
   - projects/project/zones/zone/disks/disk 
   - zones/zone/disks/disk



In order to create an image, you must provide the full or partial URL of
one of the following:
   
   - The rawDisk.source URL 
   - The sourceDisk URL 
   - The sourceImage URL 
   - The sourceSnapshot URL |
| `license_codes` | Vec<String> | Integer license codes indicating which licenses are attached to this image. |
| `params` | String | Input only. [Input Only] Additional params passed with the request, but not persisted
as part of resource payload. |
| `label_fingerprint` | String | A fingerprint for the labels being applied to this image, which is
essentially a hash of the labels used for optimistic locking. The
fingerprint is initially generated by Compute Engine and changes after
every request to modify or update labels. You must always provide an
up-to-date fingerprint hash in order to update or change labels,
otherwise the request will fail with error412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve an image. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `locked` | bool | A flag for marketplace VM disk created from the image, which is designed
for marketplace VM disk to prevent the proprietary data on the disk from
being accessed unwantedly. The flag will be inherited by the disk created
from the image.

The disk with locked flag set to true will be prohibited from performing
the operations below:
   
   - R/W or R/O disk attach
   - Disk detach, if disk is created via create-on-create
   - Create images
   - Create snapshots
   - Create disk clone (create disk from the current disk)



The image with the locked field set to true will be prohibited from
performing the operations below:
   
   - Create images from the current image
   - Update the locked field for the current image



The instance with at least one disk with locked flag set to true will be
prohibited from performing the operations below:
   
   - Secondary disk attach
   - Create instant snapshot
   - Create machine images
   - Create instance template
   - Delete the instance with --keep-disk parameter set to true |
| `satisfies_pzs` | bool | [Output Only] Reserved for future use. |
| `storage_locations` | Vec<String> | Cloud Storage bucket storage location of the image (regional or
multi-regional). |
| `archive_size_bytes` | String | Size of the image tar.gz archive stored in Google Cloud
Storage (in bytes). |
| `source_disk_id` | String | [Output Only]
The ID value of the disk used to create this image. This value may be used
to determine whether the image was taken from the current or a previous
instance of a given disk name. |
| `self_link_with_id` | String | [Output Only] Server-defined URL for this resource's resource id. |
| `source_snapshot_id` | String | [Output Only]
The ID value of the snapshot used to create this image. This value may be
used to determine whether the snapshot was taken from the current or a
previous instance of a given snapshot name. |
| `source_image_encryption_key` | String | The customer-supplied encryption key of the source image. Required if the
source image is protected by a customer-supplied encryption key. |
| `source_type` | String | The type of the image used to create this disk. The
default and only valid value is RAW. |
| `disk_size_gb` | String | Size of the image when restored onto a persistent disk (in GB). |
| `raw_disk` | String | The parameters of the raw disk image. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `image_encryption_key` | String | Encrypts the image using acustomer-supplied
encryption key.

After you encrypt an image with a customer-supplied key, you must provide
the same key if you use the image later (e.g. to create a disk from
the image).

Customer-supplied encryption keys do not protect access to metadata
of the disk.

If you do not provide an encryption key when creating the image, then the
disk will be encrypted using an automatically generated key and you do not
need to provide a key to use the image later. |
| `shielded_instance_initial_state` | String | Set the secure boot keys of shielded instance. |
| `source_image` | String | URL of the source image used to create this image.
The following are valid formats for the URL:
   
   - https://www.googleapis.com/compute/v1/projects/project_id/global/
   images/image_name
   - projects/project_id/global/images/image_name



In order to create an image, you must provide the full or partial URL of
one of the following:
   
   - The rawDisk.source URL 
   - The sourceDisk URL 
   - The sourceImage URL 
   - The sourceSnapshot URL |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create image
image = provider.compute_api.Image {
    project = "value"  # Project ID for this request.
}

# Access image outputs
image_id = image.id
image_source_snapshot = image.source_snapshot
image_creation_timestamp = image.creation_timestamp
image_architecture = image.architecture
image_rollout_override = image.rollout_override
image_licenses = image.licenses
image_deprecated = image.deprecated
image_satisfies_pzi = image.satisfies_pzi
image_source_disk_encryption_key = image.source_disk_encryption_key
image_id = image.id
image_source_image_id = image.source_image_id
image_source_snapshot_encryption_key = image.source_snapshot_encryption_key
image_guest_os_features = image.guest_os_features
image_user_licenses = image.user_licenses
image_labels = image.labels
image_enable_confidential_compute = image.enable_confidential_compute
image_status = image.status
image_family = image.family
image_kind = image.kind
image_name = image.name
image_source_disk = image.source_disk
image_license_codes = image.license_codes
image_params = image.params
image_label_fingerprint = image.label_fingerprint
image_description = image.description
image_locked = image.locked
image_satisfies_pzs = image.satisfies_pzs
image_storage_locations = image.storage_locations
image_archive_size_bytes = image.archive_size_bytes
image_source_disk_id = image.source_disk_id
image_self_link_with_id = image.self_link_with_id
image_source_snapshot_id = image.source_snapshot_id
image_source_image_encryption_key = image.source_image_encryption_key
image_source_type = image.source_type
image_disk_size_gb = image.disk_size_gb
image_raw_disk = image.raw_disk
image_self_link = image.self_link
image_image_encryption_key = image.image_encryption_key
image_shielded_instance_initial_state = image.shielded_instance_initial_state
image_source_image = image.source_image
```

---


### Backend_bucket

Creates a BackendBucket resource in the specified project using
the data included in the request.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `cdn_policy` | String |  | Cloud CDN configuration for this BackendBucket. |
| `enable_cdn` | bool |  | If true, enable Cloud CDN for this BackendBucket. |
| `name` | String |  | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `region` | String |  | [Output Only] URL of the region where the regional backend bucket
resides. This field is not applicable to global backend buckets.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `params` | String |  | Input only. [Input Only] Additional params passed with the request, but not persisted
as part of resource payload. |
| `edge_security_policy` | String |  | [Output Only] The resource URL for the edge security policy associated with
this backend bucket. |
| `description` | String |  | An optional textual description of the resource; provided by the client
when the resource is created. |
| `kind` | String |  | Type of the resource. |
| `load_balancing_scheme` | String |  | The value can only be INTERNAL_MANAGED for cross-region internal layer 7
load balancer.

If loadBalancingScheme is not specified, the backend bucket can be used by
classic global external load balancers, or global application external load
balancers, or both. |
| `self_link_with_id` | String |  | [Output Only] Server-defined URL for this resource with the resource id. |
| `bucket_name` | String |  | Cloud Storage bucket name. |
| `used_by` | Vec<String> |  | [Output Only] List of resources referencing that backend bucket. |
| `id` | String |  | [Output Only] Unique identifier for the resource; defined by the server. |
| `compression_mode` | String |  | Compress text responses using Brotli or gzip compression, based on
the client's Accept-Encoding header. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `custom_response_headers` | Vec<String> |  | Headers that the Application Load Balancer should add to proxied responses. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `cdn_policy` | String | Cloud CDN configuration for this BackendBucket. |
| `enable_cdn` | bool | If true, enable Cloud CDN for this BackendBucket. |
| `name` | String | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `region` | String | [Output Only] URL of the region where the regional backend bucket
resides. This field is not applicable to global backend buckets.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `params` | String | Input only. [Input Only] Additional params passed with the request, but not persisted
as part of resource payload. |
| `edge_security_policy` | String | [Output Only] The resource URL for the edge security policy associated with
this backend bucket. |
| `description` | String | An optional textual description of the resource; provided by the client
when the resource is created. |
| `kind` | String | Type of the resource. |
| `load_balancing_scheme` | String | The value can only be INTERNAL_MANAGED for cross-region internal layer 7
load balancer.

If loadBalancingScheme is not specified, the backend bucket can be used by
classic global external load balancers, or global application external load
balancers, or both. |
| `self_link_with_id` | String | [Output Only] Server-defined URL for this resource with the resource id. |
| `bucket_name` | String | Cloud Storage bucket name. |
| `used_by` | Vec<String> | [Output Only] List of resources referencing that backend bucket. |
| `id` | String | [Output Only] Unique identifier for the resource; defined by the server. |
| `compression_mode` | String | Compress text responses using Brotli or gzip compression, based on
the client's Accept-Encoding header. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `custom_response_headers` | Vec<String> | Headers that the Application Load Balancer should add to proxied responses. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create backend_bucket
backend_bucket = provider.compute_api.Backend_bucket {
    project = "value"  # Project ID for this request.
}

# Access backend_bucket outputs
backend_bucket_id = backend_bucket.id
backend_bucket_cdn_policy = backend_bucket.cdn_policy
backend_bucket_enable_cdn = backend_bucket.enable_cdn
backend_bucket_name = backend_bucket.name
backend_bucket_region = backend_bucket.region
backend_bucket_params = backend_bucket.params
backend_bucket_edge_security_policy = backend_bucket.edge_security_policy
backend_bucket_description = backend_bucket.description
backend_bucket_kind = backend_bucket.kind
backend_bucket_load_balancing_scheme = backend_bucket.load_balancing_scheme
backend_bucket_self_link_with_id = backend_bucket.self_link_with_id
backend_bucket_bucket_name = backend_bucket.bucket_name
backend_bucket_used_by = backend_bucket.used_by
backend_bucket_id = backend_bucket.id
backend_bucket_compression_mode = backend_bucket.compression_mode
backend_bucket_creation_timestamp = backend_bucket.creation_timestamp
backend_bucket_custom_response_headers = backend_bucket.custom_response_headers
backend_bucket_self_link = backend_bucket.self_link
```

---


### Autoscaler

Creates an autoscaler in the specified project using the data
included in the request.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `kind` | String |  | [Output Only] Type of the resource. Always compute#autoscaler
for autoscalers. |
| `region` | String |  | [Output Only] URL of theregion
where the instance group resides (for autoscalers living in regional
scope). |
| `scaling_schedule_status` | HashMap<String, String> |  | [Output Only] Status information of existing scaling schedules. |
| `status_details` | Vec<String> |  | [Output Only] Human-readable details about the current state of the
autoscaler. Read the documentation forCommonly
returned status messages for examples of status messages you might
encounter. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `zone` | String |  | [Output Only] URL of thezone
where the instance group resides (for autoscalers living in zonal scope). |
| `autoscaling_policy` | String |  | The configuration parameters for the autoscaling algorithm. You can define
one or more signals for an autoscaler: cpuUtilization,customMetricUtilizations, andloadBalancingUtilization.

If none of these are specified, the default will be to autoscale based oncpuUtilization to 0.6 or 60%. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `target` | String |  | URL of the managed instance group that this autoscaler will scale. This
field is required when creating an autoscaler. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `self_link_with_id` | String |  | [Output Only] Server-defined URL for this resource with the resource id. |
| `name` | String |  | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `status` | String |  | [Output Only] The status of the autoscaler configuration. Current set of
possible values:
   
   - PENDING:
     Autoscaler backend hasn't read new/updated configuration.
   - DELETING:
     Configuration is being deleted.
   - ACTIVE:
     Configuration is acknowledged to be effective. Some warnings might
     be present in the statusDetails field.
   - ERROR:
     Configuration has errors. Actionable for users. Details are present in
     the statusDetails field.


New values might be added in the future. |
| `recommended_size` | i64 |  | [Output Only] Target recommended MIG size (number of instances) computed by
autoscaler. Autoscaler calculates the recommended MIG size even when the
autoscaling policy mode is different from ON. This field is empty when
autoscaler is not connected to an existing managed instance group or
autoscaler did not generate its prediction. |
| `zone` | String | ✅ | Name of the zone for this request. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `kind` | String | [Output Only] Type of the resource. Always compute#autoscaler
for autoscalers. |
| `region` | String | [Output Only] URL of theregion
where the instance group resides (for autoscalers living in regional
scope). |
| `scaling_schedule_status` | HashMap<String, String> | [Output Only] Status information of existing scaling schedules. |
| `status_details` | Vec<String> | [Output Only] Human-readable details about the current state of the
autoscaler. Read the documentation forCommonly
returned status messages for examples of status messages you might
encounter. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `zone` | String | [Output Only] URL of thezone
where the instance group resides (for autoscalers living in zonal scope). |
| `autoscaling_policy` | String | The configuration parameters for the autoscaling algorithm. You can define
one or more signals for an autoscaler: cpuUtilization,customMetricUtilizations, andloadBalancingUtilization.

If none of these are specified, the default will be to autoscale based oncpuUtilization to 0.6 or 60%. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `target` | String | URL of the managed instance group that this autoscaler will scale. This
field is required when creating an autoscaler. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `self_link_with_id` | String | [Output Only] Server-defined URL for this resource with the resource id. |
| `name` | String | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `status` | String | [Output Only] The status of the autoscaler configuration. Current set of
possible values:
   
   - PENDING:
     Autoscaler backend hasn't read new/updated configuration.
   - DELETING:
     Configuration is being deleted.
   - ACTIVE:
     Configuration is acknowledged to be effective. Some warnings might
     be present in the statusDetails field.
   - ERROR:
     Configuration has errors. Actionable for users. Details are present in
     the statusDetails field.


New values might be added in the future. |
| `recommended_size` | i64 | [Output Only] Target recommended MIG size (number of instances) computed by
autoscaler. Autoscaler calculates the recommended MIG size even when the
autoscaling policy mode is different from ON. This field is empty when
autoscaler is not connected to an existing managed instance group or
autoscaler did not generate its prediction. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create autoscaler
autoscaler = provider.compute_api.Autoscaler {
    zone = "value"  # Name of the zone for this request.
    project = "value"  # Project ID for this request.
}

# Access autoscaler outputs
autoscaler_id = autoscaler.id
autoscaler_kind = autoscaler.kind
autoscaler_region = autoscaler.region
autoscaler_scaling_schedule_status = autoscaler.scaling_schedule_status
autoscaler_status_details = autoscaler.status_details
autoscaler_self_link = autoscaler.self_link
autoscaler_zone = autoscaler.zone
autoscaler_autoscaling_policy = autoscaler.autoscaling_policy
autoscaler_id = autoscaler.id
autoscaler_target = autoscaler.target
autoscaler_description = autoscaler.description
autoscaler_self_link_with_id = autoscaler.self_link_with_id
autoscaler_name = autoscaler.name
autoscaler_creation_timestamp = autoscaler.creation_timestamp
autoscaler_status = autoscaler.status
autoscaler_recommended_size = autoscaler.recommended_size
```

---


### Region_disk_setting

Get Regional Disk Settings.

**Operations**: ✅ Read ✅ Update

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `access_location` | String |  | Policy of which locations are allowed to create regional
snapshots. |
| `default_resource_policies` | HashMap<String, String> |  | An optional parameter for storing the default resource policies that
will be used for the Disks created in the given scope.

The Key is a string type, provided by customers to uniquely identify the
default Resource Policy entry.

The Value is a Default ResourcePolicyDetails Object used to represent the
detailed information of the Resource Policy entry. |
| `region` | String | ✅ | Name of the region for this request. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `access_location` | String | Policy of which locations are allowed to create regional
snapshots. |
| `default_resource_policies` | HashMap<String, String> | An optional parameter for storing the default resource policies that
will be used for the Disks created in the given scope.

The Key is a string type, provided by customers to uniquely identify the
default Resource Policy entry.

The Value is a Default ResourcePolicyDetails Object used to represent the
detailed information of the Resource Policy entry. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Access region_disk_setting outputs
region_disk_setting_id = region_disk_setting.id
region_disk_setting_access_location = region_disk_setting.access_location
region_disk_setting_default_resource_policies = region_disk_setting.default_resource_policies
```

---


### Rollout_plan

Creates a new RolloutPlan in a given project and location.

**Operations**: ✅ Create ✅ Read ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `kind` | String |  | [Output Only] Type of the resource. Always compute#rolloutPlan
for rolloutPlans. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `waves` | Vec<String> |  | Required. The waves included in this rollout plan. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `name` | String |  | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?`
which means the first character must be a lowercase letter, and all
following characters must be a dash, lowercase letter, or digit, except
the last character, which cannot be a dash. |
| `self_link` | String |  | [Output Only] Server-defined fully-qualified URL for this resource. |
| `location_scope` | String |  | The location scope of the rollout plan. If not specified, the location
scope is considered as ZONAL. |
| `self_link_with_id` | String |  | [Output Only] Server-defined URL for this resource's resource id. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `kind` | String | [Output Only] Type of the resource. Always compute#rolloutPlan
for rolloutPlans. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `waves` | Vec<String> | Required. The waves included in this rollout plan. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `name` | String | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?`
which means the first character must be a lowercase letter, and all
following characters must be a dash, lowercase letter, or digit, except
the last character, which cannot be a dash. |
| `self_link` | String | [Output Only] Server-defined fully-qualified URL for this resource. |
| `location_scope` | String | The location scope of the rollout plan. If not specified, the location
scope is considered as ZONAL. |
| `self_link_with_id` | String | [Output Only] Server-defined URL for this resource's resource id. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create rollout_plan
rollout_plan = provider.compute_api.Rollout_plan {
    project = "value"  # Project ID for this request.
}

# Access rollout_plan outputs
rollout_plan_id = rollout_plan.id
rollout_plan_kind = rollout_plan.kind
rollout_plan_id = rollout_plan.id
rollout_plan_creation_timestamp = rollout_plan.creation_timestamp
rollout_plan_waves = rollout_plan.waves
rollout_plan_description = rollout_plan.description
rollout_plan_name = rollout_plan.name
rollout_plan_self_link = rollout_plan.self_link
rollout_plan_location_scope = rollout_plan.location_scope
rollout_plan_self_link_with_id = rollout_plan.self_link_with_id
```

---


### Region_instance_group

Returns permissions that a caller has on the specified resource.

**Operations**: ✅ Create ✅ Read

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `permissions` | Vec<String> |  | The set of permissions to check for the 'resource'. Permissions with
wildcards (such as '*' or 'storage.*') are not allowed. |
| `region` | String | ✅ | The name of the region for this request. |
| `project` | String | ✅ | Project ID for this request. |
| `resource` | String | ✅ | Name or id of the resource for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `creation_timestamp` | String | [Output Only] The creation timestamp for this instance group inRFC3339
text format. |
| `id` | String | [Output Only] A unique identifier for this instance group, generated
by the server. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `self_link_with_id` | String | [Output Only] Server-defined URL for this resource with the resource id. |
| `subnetwork` | String | [Output Only] The URL of the subnetwork to which all instances in the
instance group belong. If your instance has multiple network interfaces,
then the network and subnetwork fields only refer to the
network and subnet used by your primary interface (nic0). |
| `self_link` | String | [Output Only] The URL for this instance group. The server generates
this URL. |
| `size` | i64 | [Output Only] The total number of instances in the instance group. |
| `zone` | String | [Output Only] The URL of thezone
where the instance group is located (for zonal resources). |
| `name` | String | The name of the instance group. The name must be 1-63 characters
long, and comply withRFC1035. |
| `network` | String | [Output Only] The URL of the network to which all instances in the
instance group belong. If your instance has multiple network interfaces,
then the network and subnetwork fields only refer to the
network and subnet used by your primary interface (nic0). |
| `fingerprint` | String | [Output Only] The fingerprint of the named ports. The system
uses this fingerprint to detect conflicts when multiple users change the
named ports concurrently. |
| `kind` | String | [Output Only] The resource type, which is alwayscompute#instanceGroup for instance groups. |
| `named_ports` | Vec<String> | Optional. Assigns a name to a port number. For example:{name: "http", port: 80}

This
allows the system to reference ports by the assigned name instead of a
port number. Named ports can also contain multiple ports. For example:[{name: "app1", port: 8080}, {name:
"app1", port: 8081}, {name: "app2", port:
8082}]

Named ports apply to all instances in this instance group. |
| `region` | String | [Output Only] The URL of theregion
where the instance group is located (for regional resources). |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create region_instance_group
region_instance_group = provider.compute_api.Region_instance_group {
    region = "value"  # The name of the region for this request.
    project = "value"  # Project ID for this request.
    resource = "value"  # Name or id of the resource for this request.
}

# Access region_instance_group outputs
region_instance_group_id = region_instance_group.id
region_instance_group_creation_timestamp = region_instance_group.creation_timestamp
region_instance_group_id = region_instance_group.id
region_instance_group_description = region_instance_group.description
region_instance_group_self_link_with_id = region_instance_group.self_link_with_id
region_instance_group_subnetwork = region_instance_group.subnetwork
region_instance_group_self_link = region_instance_group.self_link
region_instance_group_size = region_instance_group.size
region_instance_group_zone = region_instance_group.zone
region_instance_group_name = region_instance_group.name
region_instance_group_network = region_instance_group.network
region_instance_group_fingerprint = region_instance_group.fingerprint
region_instance_group_kind = region_instance_group.kind
region_instance_group_named_ports = region_instance_group.named_ports
region_instance_group_region = region_instance_group.region
```

---


### Rollout

Gets details of a single project-scoped Rollout.

**Operations**: ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `rollout` | String | ✅ | Name of the Rollout resource to cancel. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `self_link` | String | [Output Only] Server-defined fully-qualified URL for this resource. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `cancellation_time` | String | Output only. The timestamp at which the Rollout was cancelled. |
| `kind` | String | [Output Only] Type of the resource. Always compute#rollout
for rollouts. |
| `rollout_entity` | String | Required. The resource being rolled out. |
| `current_wave_number` | String | Output only. The number of the currently running wave.
Ex. 1 |
| `wave_details` | Vec<String> | Output only. Details about each wave of the rollout. |
| `completion_time` | String | Output only. The timestamp at which the Rollout was completed. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `name` | String | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?`
which means the first character must be a lowercase letter, and all
following characters must be a dash, lowercase letter, or digit, except
the last character, which cannot be a dash. |
| `state` | String | Output only. The current state of the Rollout. |
| `rollout_plan` | String | Required. Rollout Plan used to model the Rollout.
Ex. progressiverollout.googleapis.com/v1/organizations/1/rolloutPlans
Ex. progressiverollout.googleapis.com/v1/folders/1/rolloutPlans |
| `self_link_with_id` | String | [Output Only] Server-defined URL for this resource's resource id. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `etag` | String | Output only. etag of the Rollout
Ex. abc1234 |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Access rollout outputs
rollout_id = rollout.id
rollout_self_link = rollout.self_link
rollout_id = rollout.id
rollout_cancellation_time = rollout.cancellation_time
rollout_kind = rollout.kind
rollout_rollout_entity = rollout.rollout_entity
rollout_current_wave_number = rollout.current_wave_number
rollout_wave_details = rollout.wave_details
rollout_completion_time = rollout.completion_time
rollout_creation_timestamp = rollout.creation_timestamp
rollout_name = rollout.name
rollout_state = rollout.state
rollout_rollout_plan = rollout.rollout_plan
rollout_self_link_with_id = rollout.self_link_with_id
rollout_description = rollout.description
rollout_etag = rollout.etag
```

---


### Router

Creates a Router resource in the specified project and region using
the data included in the request.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `encrypted_interconnect_router` | bool |  | Indicates if a router is dedicated for use with encrypted VLAN
attachments (interconnectAttachments). |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `kind` | String |  | [Output Only] Type of resource. Always compute#router for
routers. |
| `ncc_gateway` | String |  | URI of the ncc_gateway to which this router associated. |
| `nats` | Vec<String> |  | A list of NAT services created in this router. |
| `interfaces` | Vec<String> |  | Router interfaces.
To create a BGP peer that uses a router interface,
the interface must have one of the following fields specified:
   
   - linkedVpnTunnel
   - linkedInterconnectAttachment
   - subnetwork


You can create a router interface without any of these fields specified.
However, you cannot create a BGP peer that uses that interface. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `self_link_with_id` | String |  | [Output Only] Server-defined URL for this resource with the resource id. |
| `network` | String |  | URI of the network to which this router belongs. |
| `md5_authentication_keys` | Vec<String> |  | Keys used for MD5 authentication. |
| `params` | String |  | Input only. [Input Only] Additional params passed with the request, but not persisted
as part of resource payload. |
| `bgp_peers` | Vec<String> |  | BGP information that must be configured into the routing stack to
establish BGP peering. This information must specify the peer ASN and
either the interface name, IP address, or peer IP address. Please refer toRFC4273. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `region` | String |  | [Output Only] URI of the region where the router resides.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `bgp` | String |  | BGP information specific to this router. |
| `name` | String |  | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `project` | String | ✅ | Project ID for this request. |
| `region` | String | ✅ | Name of the region for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `encrypted_interconnect_router` | bool | Indicates if a router is dedicated for use with encrypted VLAN
attachments (interconnectAttachments). |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `kind` | String | [Output Only] Type of resource. Always compute#router for
routers. |
| `ncc_gateway` | String | URI of the ncc_gateway to which this router associated. |
| `nats` | Vec<String> | A list of NAT services created in this router. |
| `interfaces` | Vec<String> | Router interfaces.
To create a BGP peer that uses a router interface,
the interface must have one of the following fields specified:
   
   - linkedVpnTunnel
   - linkedInterconnectAttachment
   - subnetwork


You can create a router interface without any of these fields specified.
However, you cannot create a BGP peer that uses that interface. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `self_link_with_id` | String | [Output Only] Server-defined URL for this resource with the resource id. |
| `network` | String | URI of the network to which this router belongs. |
| `md5_authentication_keys` | Vec<String> | Keys used for MD5 authentication. |
| `params` | String | Input only. [Input Only] Additional params passed with the request, but not persisted
as part of resource payload. |
| `bgp_peers` | Vec<String> | BGP information that must be configured into the routing stack to
establish BGP peering. This information must specify the peer ASN and
either the interface name, IP address, or peer IP address. Please refer toRFC4273. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `region` | String | [Output Only] URI of the region where the router resides.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `bgp` | String | BGP information specific to this router. |
| `name` | String | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create router
router = provider.compute_api.Router {
    project = "value"  # Project ID for this request.
    region = "value"  # Name of the region for this request.
}

# Access router outputs
router_id = router.id
router_encrypted_interconnect_router = router.encrypted_interconnect_router
router_creation_timestamp = router.creation_timestamp
router_kind = router.kind
router_ncc_gateway = router.ncc_gateway
router_nats = router.nats
router_interfaces = router.interfaces
router_id = router.id
router_self_link_with_id = router.self_link_with_id
router_network = router.network
router_md5_authentication_keys = router.md5_authentication_keys
router_params = router.params
router_bgp_peers = router.bgp_peers
router_description = router.description
router_region = router.region
router_bgp = router.bgp
router_name = router.name
router_self_link = router.self_link
```

---


### Recoverable_snapshot

Sets the access control policy on the specified resource.
Replaces any existing policy.

**Operations**: ✅ Create ✅ Read ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `etag` | String |  | Flatten Policy to create a backward compatible wire-format.
Deprecated. Use 'policy' to specify the etag. |
| `policy` | String |  | REQUIRED: The complete policy to be applied to the 'resource'. The size of
the policy is limited to a few 10s of KB. An empty policy is in general a
valid policy but certain services (like Projects) might reject them. |
| `bindings` | Vec<String> |  | Flatten Policy to create a backward compatible wire-format.
Deprecated. Use 'policy' to specify bindings. |
| `resource` | String | ✅ | Name or id of the resource for this request. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `purge_timestamp` | String | [Output Only] Purge timestamp of recoverable snapshot inRFC3339
text format. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `self_link_with_id` | String | [Output Only] Server-defined URL for this resource's resource id. |
| `status` | String | [Output Only] Status of the recoverable snapshot. |
| `kind` | String | [Output Only] Type of the resource. Alwayscompute#recoverableSnapshot for RecoverableSnapshot
resources. |
| `satisfies_pzs` | bool | [Output Only] Reserved for future use. |
| `satisfies_pzi` | bool | Output only. [Output Only] Reserved for future use. |
| `original_resource` | String | Output Only] The original snapshot resource. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `description` | String | Optional. An optional description of this resource. |
| `name` | String | Identifier. Name of the recoverable snapshot generated on the deletion of the snapshot.
The name will be 1-63 characters long, and comply withRFC1035.
Specifically, the name will be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character will be a lowercase letter, and all following characters can be
a dash, lowercase letter, or digit, except the last character, which cannot
be a dash. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create recoverable_snapshot
recoverable_snapshot = provider.compute_api.Recoverable_snapshot {
    resource = "value"  # Name or id of the resource for this request.
    project = "value"  # Project ID for this request.
}

# Access recoverable_snapshot outputs
recoverable_snapshot_id = recoverable_snapshot.id
recoverable_snapshot_purge_timestamp = recoverable_snapshot.purge_timestamp
recoverable_snapshot_id = recoverable_snapshot.id
recoverable_snapshot_self_link_with_id = recoverable_snapshot.self_link_with_id
recoverable_snapshot_status = recoverable_snapshot.status
recoverable_snapshot_kind = recoverable_snapshot.kind
recoverable_snapshot_satisfies_pzs = recoverable_snapshot.satisfies_pzs
recoverable_snapshot_satisfies_pzi = recoverable_snapshot.satisfies_pzi
recoverable_snapshot_original_resource = recoverable_snapshot.original_resource
recoverable_snapshot_self_link = recoverable_snapshot.self_link
recoverable_snapshot_description = recoverable_snapshot.description
recoverable_snapshot_name = recoverable_snapshot.name
recoverable_snapshot_creation_timestamp = recoverable_snapshot.creation_timestamp
```

---


### Firewall_policie

Creates a new policy in the specified project using the data included in
the request.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `fingerprint` | String |  | Specifies a fingerprint for this resource, which is essentially a hash of
the metadata's contents and used for optimistic locking. The
fingerprint is initially generated by Compute Engine and changes after
every request to modify or update metadata. You must always provide an
up-to-date fingerprint hash in order to update or change metadata,
otherwise the request will fail with error412 conditionNotMet.

To see the latest fingerprint, make get() request to the
firewall policy. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `packet_mirroring_rules` | Vec<String> |  | A list of packet mirroring rules that belong to this policy. |
| `parent` | String |  | [Output Only] The parent of the firewall policy.
This field is not applicable to network firewall policies. |
| `region` | String |  | [Output Only] URL of the region where the regional firewall policy resides.
This field is not applicable to global firewall policies.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `rules` | Vec<String> |  | A list of rules that belong to this policy.
There must always be a default rule (rule with priority 2147483647 and
match "*"). If no rules are provided when creating a firewall policy, a
default rule with action "allow" will be added. |
| `policy_type` | String |  | The type of the firewall policy. This field can be eitherVPC_POLICY or RDMA_ROCE_POLICY.

Note: if not specified then VPC_POLICY will be used. |
| `self_link_with_id` | String |  | [Output Only] Server-defined URL for this resource with the resource id. |
| `short_name` | String |  | User-provided name of the Organization firewall policy. The name should be
unique in the organization in which the firewall policy is created.
This field is not applicable to network firewall policies.
This name must be set on creation and cannot be changed. The name must be
1-63 characters long, and comply with RFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `kind` | String |  | [Output only] Type of the resource. Alwayscompute#firewallPolicyfor firewall policies |
| `vpc_network_scope` | String |  | The scope of networks allowed to be associated with the firewall
policy. This field can be either GLOBAL_VPC_NETWORK orREGIONAL_VPC_NETWORK.

A firewall policy with the VPC scope set to GLOBAL_VPC_NETWORK
is allowed to be attached only to global networks.

When the VPC scope is set to REGIONAL_VPC_NETWORK the firewall
policy is allowed to be attached only to regional networks in the same
scope as the firewall policy.

Note: if not specified then GLOBAL_VPC_NETWORK will be used. |
| `associations` | Vec<String> |  | A list of associations that belong to this firewall policy. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `display_name` | String |  | Deprecated, please use short name instead. User-provided name of the
Organization firewall policy. The name should be unique in the organization
in which the firewall policy is created.
This field is not applicable to network firewall policies.
This name must be set on creation and cannot be changed.
The name must be 1-63 characters long, and comply
with RFC1035. Specifically, the name must be 1-63 characters
long and match the regular expression `[a-z]([-a-z0-9]*[a-z0-9])?` which
means the first character must be a lowercase letter, and all following
characters must be a dash, lowercase letter, or digit, except the last
character, which cannot be a dash. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `name` | String |  | Name of the resource. For Organization Firewall Policies it's a
[Output Only] numeric ID allocated by Google Cloud which uniquely
identifies the Organization Firewall Policy. |
| `policy_source` | String |  | [Output Only] Source of this Firewall Policy. USER_DEFINED if
created by a Cloud user, or SYSTEM if created by managed
services like GKE. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `rule_tuple_count` | i64 |  | [Output Only] Total count of all firewall policy rule tuples. A firewall
policy can not exceed a set number of tuples. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `fingerprint` | String | Specifies a fingerprint for this resource, which is essentially a hash of
the metadata's contents and used for optimistic locking. The
fingerprint is initially generated by Compute Engine and changes after
every request to modify or update metadata. You must always provide an
up-to-date fingerprint hash in order to update or change metadata,
otherwise the request will fail with error412 conditionNotMet.

To see the latest fingerprint, make get() request to the
firewall policy. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `packet_mirroring_rules` | Vec<String> | A list of packet mirroring rules that belong to this policy. |
| `parent` | String | [Output Only] The parent of the firewall policy.
This field is not applicable to network firewall policies. |
| `region` | String | [Output Only] URL of the region where the regional firewall policy resides.
This field is not applicable to global firewall policies.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `rules` | Vec<String> | A list of rules that belong to this policy.
There must always be a default rule (rule with priority 2147483647 and
match "*"). If no rules are provided when creating a firewall policy, a
default rule with action "allow" will be added. |
| `policy_type` | String | The type of the firewall policy. This field can be eitherVPC_POLICY or RDMA_ROCE_POLICY.

Note: if not specified then VPC_POLICY will be used. |
| `self_link_with_id` | String | [Output Only] Server-defined URL for this resource with the resource id. |
| `short_name` | String | User-provided name of the Organization firewall policy. The name should be
unique in the organization in which the firewall policy is created.
This field is not applicable to network firewall policies.
This name must be set on creation and cannot be changed. The name must be
1-63 characters long, and comply with RFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `kind` | String | [Output only] Type of the resource. Alwayscompute#firewallPolicyfor firewall policies |
| `vpc_network_scope` | String | The scope of networks allowed to be associated with the firewall
policy. This field can be either GLOBAL_VPC_NETWORK orREGIONAL_VPC_NETWORK.

A firewall policy with the VPC scope set to GLOBAL_VPC_NETWORK
is allowed to be attached only to global networks.

When the VPC scope is set to REGIONAL_VPC_NETWORK the firewall
policy is allowed to be attached only to regional networks in the same
scope as the firewall policy.

Note: if not specified then GLOBAL_VPC_NETWORK will be used. |
| `associations` | Vec<String> | A list of associations that belong to this firewall policy. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `display_name` | String | Deprecated, please use short name instead. User-provided name of the
Organization firewall policy. The name should be unique in the organization
in which the firewall policy is created.
This field is not applicable to network firewall policies.
This name must be set on creation and cannot be changed.
The name must be 1-63 characters long, and comply
with RFC1035. Specifically, the name must be 1-63 characters
long and match the regular expression `[a-z]([-a-z0-9]*[a-z0-9])?` which
means the first character must be a lowercase letter, and all following
characters must be a dash, lowercase letter, or digit, except the last
character, which cannot be a dash. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `name` | String | Name of the resource. For Organization Firewall Policies it's a
[Output Only] numeric ID allocated by Google Cloud which uniquely
identifies the Organization Firewall Policy. |
| `policy_source` | String | [Output Only] Source of this Firewall Policy. USER_DEFINED if
created by a Cloud user, or SYSTEM if created by managed
services like GKE. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `rule_tuple_count` | i64 | [Output Only] Total count of all firewall policy rule tuples. A firewall
policy can not exceed a set number of tuples. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create firewall_policie
firewall_policie = provider.compute_api.Firewall_policie {
}

# Access firewall_policie outputs
firewall_policie_id = firewall_policie.id
firewall_policie_fingerprint = firewall_policie.fingerprint
firewall_policie_self_link = firewall_policie.self_link
firewall_policie_packet_mirroring_rules = firewall_policie.packet_mirroring_rules
firewall_policie_parent = firewall_policie.parent
firewall_policie_region = firewall_policie.region
firewall_policie_rules = firewall_policie.rules
firewall_policie_policy_type = firewall_policie.policy_type
firewall_policie_self_link_with_id = firewall_policie.self_link_with_id
firewall_policie_short_name = firewall_policie.short_name
firewall_policie_kind = firewall_policie.kind
firewall_policie_vpc_network_scope = firewall_policie.vpc_network_scope
firewall_policie_associations = firewall_policie.associations
firewall_policie_creation_timestamp = firewall_policie.creation_timestamp
firewall_policie_display_name = firewall_policie.display_name
firewall_policie_id = firewall_policie.id
firewall_policie_name = firewall_policie.name
firewall_policie_policy_source = firewall_policie.policy_source
firewall_policie_description = firewall_policie.description
firewall_policie_rule_tuple_count = firewall_policie.rule_tuple_count
```

---


### Region_disk

Creates a persistent regional disk in the specified project using the data
included in the request.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `source_consistency_group_policy_id` | String |  | [Output Only] ID of the DiskConsistencyGroupPolicy for a secondary disk
that was created using a consistency group. |
| `physical_block_size_bytes` | String |  | Physical block size of the persistent disk, in bytes.
If not present in a request, a default value is used.
The currently supported size is 4096, other sizes may be added in
the future.
If an unsupported value is requested, the error message will list
the supported values for the caller's project. |
| `provisioned_iops` | String |  | Indicates how many IOPS to provision for the disk. This sets the number
of I/O operations per second that the disk can handle. Values must be
between 10,000 and 120,000. For more details, see theExtreme persistent
disk documentation. |
| `source_snapshot_encryption_key` | String |  | Thecustomer-supplied
encryption key of the source snapshot. Required if the source snapshot
is protected by a customer-supplied encryption key. |
| `async_secondary_disks` | HashMap<String, String> |  | [Output Only] A list of disks this disk is asynchronously replicated to. |
| `source_image` | String |  | The source image used to create this disk. If the source image is
deleted, this field will not be set.

To create a disk with one of the public operating system images, specify
the image by its family name. For example, specifyfamily/debian-9 to use the latest Debian 9 image:

projects/debian-cloud/global/images/family/debian-9


Alternatively, use a specific version of a public operating system image:

projects/debian-cloud/global/images/debian-9-stretch-vYYYYMMDD


To create a disk with a custom image that you created, specify the
image name in the following format:

global/images/my-custom-image


You can also specify a custom image by its image family, which returns
the latest version of the image in that family. Replace the image name
with family/family-name:

global/images/family/my-image-family |
| `source_disk_id` | String |  | [Output Only] The unique ID of the disk used to create this disk. This
value identifies the exact disk that was used to create this persistent
disk. For example, if you created the persistent disk from a disk that
was later deleted and recreated under the same name, the source disk ID
would identify the exact version of the disk that was used. |
| `source_instant_snapshot_id` | String |  | [Output Only] The unique ID of the instant snapshot used to create this
disk. This value identifies the exact instant snapshot that was used to
create this persistent disk. For example, if you created the persistent
disk from an instant snapshot that was later deleted and recreated under
the same name, the source instant snapshot ID would identify the exact
version of the instant snapshot that was used. |
| `params` | String |  | Input only. [Input Only] Additional params passed with the request, but not persisted
as part of resource payload. |
| `architecture` | String |  | The architecture of the disk. Valid values are
ARM64 or X86_64. |
| `guest_os_features` | Vec<String> |  | A list of features to enable on the guest operating system. Applicable
only for bootable images. Read
Enabling guest operating system features to see a list of available
options. |
| `locked` | bool |  | [Output Only] The field indicates if the disk is created from a locked
source image. Attachment of a disk created from a locked source image will
cause the following operations to become irreversibly prohibited:
   
   - R/W or R/O disk attachment to any other instance
   - Disk detachment. And the disk can only be deleted when the instance
   is deleted
   - Creation of images or snapshots
   - Disk cloning



Furthermore, the instance with at least one disk with locked flag set to
true will be prohibited from performing the operations below:
   
   - Further attachment of secondary disks.
   - Detachment of any disks
   - Create machine images
   - Create instance template
   - Delete the instance with --keep-disk parameter set to true for locked
   disks
   - Attach a locked disk with --auto-delete parameter set to false |
| `kind` | String |  | [Output Only] Type of the resource. Always compute#disk for
disks. |
| `storage_pool` | String |  | The storage pool in which the new disk is created. You can provide
this as a partial or full URL to the resource. For example, the following
are valid values:
   
   
     - https://www.googleapis.com/compute/v1/projects/project/zones/zone/storagePools/storagePool
     - projects/project/zones/zone/storagePools/storagePool 
   - zones/zone/storagePools/storagePool |
| `options` | String |  | Internal use only. |
| `disk_encryption_key` | String |  | Encrypts the disk using a
customer-supplied encryption key or a 
customer-managed encryption key.

Encryption keys do not protect access to metadata of the disk.

After you encrypt a disk with a customer-supplied key, you must provide the
same key if you use the disk later. For example, to create a disk snapshot,
to create a disk image, to create a machine image, or to attach the disk to
a virtual machine.

After you encrypt a disk with a customer-managed key, thediskEncryptionKey.kmsKeyName is set to a key *version*
name once the disk is created. The disk is encrypted with this version of
the key. In the response, diskEncryptionKey.kmsKeyName appears
in the following format:

"diskEncryptionKey.kmsKeyName":
"projects/kms_project_id/locations/region/keyRings/
key_region/cryptoKeys/key
/cryptoKeysVersions/version

If you do not provide an encryption key when creating the disk, then the 
disk is encrypted using an automatically generated key and you don't need 
to provide a key to use the disk later. |
| `labels` | HashMap<String, String> |  | Labels to apply to this disk. These can be later modified by
the setLabels method. |
| `location_hint` | String |  | An opaque location hint used to place the disk close to other resources.
This field is for use by internal tools that use the public API. |
| `self_link_with_id` | String |  | [Output Only] Server-defined URL for this resource's resource id. |
| `license_codes` | Vec<String> |  | Integer license codes indicating which licenses are attached to this disk. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `interface` | String |  | [Deprecated] Specifies the disk interface to use for attaching this disk,
which is either SCSI or NVME. The default isSCSI. |
| `source_instant_snapshot` | String |  | The source instant snapshot used to create this disk. You can provide
this as a partial or full URL to the resource. For example, the following
are valid values:
   
   
     - https://www.googleapis.com/compute/v1/projects/project/zones/zone/instantSnapshots/instantSnapshot
     - projects/project/zones/zone/instantSnapshots/instantSnapshot 
   - zones/zone/instantSnapshots/instantSnapshot |
| `resource_policies` | Vec<String> |  | Resource policies applied to this disk for automatic snapshot creations. |
| `erase_windows_vss_signature` | bool |  | Specifies whether the disk restored from a source snapshot should erase
Windows specific VSS signature. |
| `status` | String |  | [Output Only] The status of disk creation.
   
   
     - CREATING: Disk is provisioning.
     - RESTORING: Source data is being copied into the
     disk.
     - FAILED: Disk creation failed.
     - READY: Disk is ready for use.
     - DELETING: Disk is deleting. |
| `multi_writer` | bool |  | Indicates whether or not the disk can be read/write attached to
more than one instance. |
| `region` | String |  | [Output Only] URL of the region where the disk resides. Only applicable for
regional resources.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `source_storage_object` | String |  | The full Google Cloud Storage URI where the disk image is stored. This file
must be a gzip-compressed tarball whose name ends in .tar.gz or virtual
machine disk whose name ends in vmdk. Valid URIs may start with gs:// or
https://storage.googleapis.com/. This flag is not optimized for creating
multiple disks from a source storage object. To create many disks from a
source storage object, use gcloud compute images
import instead. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `satisfies_pzi` | bool |  | Output only. Reserved for future use. |
| `label_fingerprint` | String |  | A fingerprint for the labels being applied to this disk, which is
essentially a hash of the labels set used for optimistic locking. The
fingerprint is initially generated by Compute Engine and changes after
every request to modify or update labels. You must always provide an
up-to-date fingerprint hash in order to update or change labels,
otherwise the request will fail with error412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve a disk. |
| `source_image_id` | String |  | [Output Only] The ID value of the image used to create this disk. This
value identifies the exact image that was used to create this persistent
disk. For example, if you created the persistent disk from an image that
was later deleted and recreated under the same name, the source image ID
would identify the exact version of the image that was used. |
| `source_snapshot_id` | String |  | [Output Only] The unique ID of the snapshot used to create this disk. This
value identifies the exact snapshot that was used to create this persistent
disk. For example, if you created the persistent disk from a snapshot that
was later deleted and recreated under the same name, the source snapshot ID
would identify the exact version of the snapshot that was used. |
| `type` | String |  | URL of the disk type resource describing which disk type to use to create
the disk. Provide this when creating the disk. For example:projects/project/zones/zone/diskTypes/pd-ssd. See Persistent disk
types. |
| `zone` | String |  | [Output Only] URL of the zone where the disk resides.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `user_licenses` | Vec<String> |  | A list of publicly visible user-licenses. Unlike regular licenses, user
provided licenses can be modified after the disk is created. This includes
a list of URLs to the license resource. For example, to provide a debian
license:

https://www.googleapis.com/compute/v1/projects/debian-cloud/global/licenses/debian-9-stretch |
| `users` | Vec<String> |  | [Output Only] Links to the users of the disk (attached instances)
in form:projects/project/zones/zone/instances/instance |
| `provisioned_throughput` | String |  | Indicates how much throughput to provision for the disk. This sets the
number of throughput mb per second that the disk can handle. Values must be
greater than or equal to 1. |
| `enable_confidential_compute` | bool |  | Whether this disk is using confidential compute mode. |
| `storage_type` | String |  | [Deprecated] Storage type of the persistent disk. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `source_image_encryption_key` | String |  | Thecustomer-supplied
encryption key of the source image. Required if the source image is
protected by a customer-supplied encryption key. |
| `resource_status` | String |  | [Output Only] Status information for the disk resource. |
| `satisfies_pzs` | bool |  | [Output Only] Reserved for future use. |
| `last_detach_timestamp` | String |  | [Output Only] Last detach timestamp inRFC3339
text format. |
| `source_machine_image_disk_device_name` | String |  | Input only. The device name of a disk within a given machine image. The
source_machine_image must be specified. |
| `async_primary_disk` | String |  | Disk asynchronously replicated into this disk. |
| `size_gb` | String |  | Size, in GB, of the persistent disk. You can specify
this field when creating a persistent disk using thesourceImage, sourceSnapshot, orsourceDisk parameter, or specify it alone to create an empty
persistent disk.

If you specify this field along with a source, the value ofsizeGb must not be less than the size of the
source.
Acceptable values are greater than 0. |
| `last_attach_timestamp` | String |  | [Output Only] Last attach timestamp inRFC3339
text format. |
| `source_machine_image_encryption_key` | String |  | Thecustomer-supplied
encryption key of the source machine image. Required if the source
machine image is protected by a customer-supplied encryption key. |
| `source_machine_image_id` | String |  | [Output Only] The unique ID of the machine image used to create this disk.
This value identifies the exact machine image that was used to create this
persistent disk. For example, if you created the persistent disk from a
machine image that was later deleted and recreated under the same name, the
source machine image ID would identify the exact version of the machine
image that was used. |
| `source_disk` | String |  | The source disk used to create this disk. You can provide this as a
partial or full URL to the resource. For example, the following are valid
values:
   
   
     - 
       https://www.googleapis.com/compute/v1/projects/project/zones/zone/disks/disk
     
     - 
       https://www.googleapis.com/compute/v1/projects/project/regions/region/disks/disk
     
     - 
       projects/project/zones/zone/disks/disk
     
     - 
       projects/project/regions/region/disks/disk
     
     - 
       zones/zone/disks/disk
     
     - 
       regions/region/disks/disk |
| `self_link` | String |  | [Output Only] Server-defined fully-qualified URL for this resource. |
| `name` | String |  | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?`
which means the first character must be a lowercase letter, and all
following characters must be a dash, lowercase letter, or digit, except
the last character, which cannot be a dash. |
| `source_consistency_group_policy` | String |  | [Output Only] URL of the DiskConsistencyGroupPolicy for a secondary disk
that was created using a consistency group. |
| `source_machine_image` | String |  | The machine image to create the disk from. You can provide this as a
partial or full URL to the resource. For example, the following are valid
values:
   
   
     - https://www.googleapis.com/compute/v1/projects/project/global/machineImages/machineImage 
   - projects/project/global/machineImages/machineImage
     - global/machineImages/machineImage |
| `licenses` | Vec<String> |  | A list of publicly visible licenses. Reserved for Google's use. |
| `source_snapshot` | String |  | The source snapshot used to create this disk. You can provide this as a
partial or full URL to the resource. For example, the following are valid
values:
   
   
     - https://www.googleapis.com/compute/v1/projects/project/global/snapshots/snapshot 
   - projects/project/global/snapshots/snapshot
     - global/snapshots/snapshot |
| `access_mode` | String |  | The access mode of the disk.
   
   
     - READ_WRITE_SINGLE: The default AccessMode, means the
     disk can be attached to single instance in RW mode.
     - READ_WRITE_MANY: The AccessMode means the disk can be
     attached to multiple instances in RW mode.
     - READ_ONLY_MANY: The AccessMode means the disk can be
     attached to multiple instances in RO mode.


The AccessMode is only valid for Hyperdisk disk types. |
| `replica_zones` | Vec<String> |  | URLs of the zones where the disk should be replicated to. Only applicable
for regional resources. |
| `region` | String | ✅ | Name of the region for this request. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `source_consistency_group_policy_id` | String | [Output Only] ID of the DiskConsistencyGroupPolicy for a secondary disk
that was created using a consistency group. |
| `physical_block_size_bytes` | String | Physical block size of the persistent disk, in bytes.
If not present in a request, a default value is used.
The currently supported size is 4096, other sizes may be added in
the future.
If an unsupported value is requested, the error message will list
the supported values for the caller's project. |
| `provisioned_iops` | String | Indicates how many IOPS to provision for the disk. This sets the number
of I/O operations per second that the disk can handle. Values must be
between 10,000 and 120,000. For more details, see theExtreme persistent
disk documentation. |
| `source_snapshot_encryption_key` | String | Thecustomer-supplied
encryption key of the source snapshot. Required if the source snapshot
is protected by a customer-supplied encryption key. |
| `async_secondary_disks` | HashMap<String, String> | [Output Only] A list of disks this disk is asynchronously replicated to. |
| `source_image` | String | The source image used to create this disk. If the source image is
deleted, this field will not be set.

To create a disk with one of the public operating system images, specify
the image by its family name. For example, specifyfamily/debian-9 to use the latest Debian 9 image:

projects/debian-cloud/global/images/family/debian-9


Alternatively, use a specific version of a public operating system image:

projects/debian-cloud/global/images/debian-9-stretch-vYYYYMMDD


To create a disk with a custom image that you created, specify the
image name in the following format:

global/images/my-custom-image


You can also specify a custom image by its image family, which returns
the latest version of the image in that family. Replace the image name
with family/family-name:

global/images/family/my-image-family |
| `source_disk_id` | String | [Output Only] The unique ID of the disk used to create this disk. This
value identifies the exact disk that was used to create this persistent
disk. For example, if you created the persistent disk from a disk that
was later deleted and recreated under the same name, the source disk ID
would identify the exact version of the disk that was used. |
| `source_instant_snapshot_id` | String | [Output Only] The unique ID of the instant snapshot used to create this
disk. This value identifies the exact instant snapshot that was used to
create this persistent disk. For example, if you created the persistent
disk from an instant snapshot that was later deleted and recreated under
the same name, the source instant snapshot ID would identify the exact
version of the instant snapshot that was used. |
| `params` | String | Input only. [Input Only] Additional params passed with the request, but not persisted
as part of resource payload. |
| `architecture` | String | The architecture of the disk. Valid values are
ARM64 or X86_64. |
| `guest_os_features` | Vec<String> | A list of features to enable on the guest operating system. Applicable
only for bootable images. Read
Enabling guest operating system features to see a list of available
options. |
| `locked` | bool | [Output Only] The field indicates if the disk is created from a locked
source image. Attachment of a disk created from a locked source image will
cause the following operations to become irreversibly prohibited:
   
   - R/W or R/O disk attachment to any other instance
   - Disk detachment. And the disk can only be deleted when the instance
   is deleted
   - Creation of images or snapshots
   - Disk cloning



Furthermore, the instance with at least one disk with locked flag set to
true will be prohibited from performing the operations below:
   
   - Further attachment of secondary disks.
   - Detachment of any disks
   - Create machine images
   - Create instance template
   - Delete the instance with --keep-disk parameter set to true for locked
   disks
   - Attach a locked disk with --auto-delete parameter set to false |
| `kind` | String | [Output Only] Type of the resource. Always compute#disk for
disks. |
| `storage_pool` | String | The storage pool in which the new disk is created. You can provide
this as a partial or full URL to the resource. For example, the following
are valid values:
   
   
     - https://www.googleapis.com/compute/v1/projects/project/zones/zone/storagePools/storagePool
     - projects/project/zones/zone/storagePools/storagePool 
   - zones/zone/storagePools/storagePool |
| `options` | String | Internal use only. |
| `disk_encryption_key` | String | Encrypts the disk using a
customer-supplied encryption key or a 
customer-managed encryption key.

Encryption keys do not protect access to metadata of the disk.

After you encrypt a disk with a customer-supplied key, you must provide the
same key if you use the disk later. For example, to create a disk snapshot,
to create a disk image, to create a machine image, or to attach the disk to
a virtual machine.

After you encrypt a disk with a customer-managed key, thediskEncryptionKey.kmsKeyName is set to a key *version*
name once the disk is created. The disk is encrypted with this version of
the key. In the response, diskEncryptionKey.kmsKeyName appears
in the following format:

"diskEncryptionKey.kmsKeyName":
"projects/kms_project_id/locations/region/keyRings/
key_region/cryptoKeys/key
/cryptoKeysVersions/version

If you do not provide an encryption key when creating the disk, then the 
disk is encrypted using an automatically generated key and you don't need 
to provide a key to use the disk later. |
| `labels` | HashMap<String, String> | Labels to apply to this disk. These can be later modified by
the setLabels method. |
| `location_hint` | String | An opaque location hint used to place the disk close to other resources.
This field is for use by internal tools that use the public API. |
| `self_link_with_id` | String | [Output Only] Server-defined URL for this resource's resource id. |
| `license_codes` | Vec<String> | Integer license codes indicating which licenses are attached to this disk. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `interface` | String | [Deprecated] Specifies the disk interface to use for attaching this disk,
which is either SCSI or NVME. The default isSCSI. |
| `source_instant_snapshot` | String | The source instant snapshot used to create this disk. You can provide
this as a partial or full URL to the resource. For example, the following
are valid values:
   
   
     - https://www.googleapis.com/compute/v1/projects/project/zones/zone/instantSnapshots/instantSnapshot
     - projects/project/zones/zone/instantSnapshots/instantSnapshot 
   - zones/zone/instantSnapshots/instantSnapshot |
| `resource_policies` | Vec<String> | Resource policies applied to this disk for automatic snapshot creations. |
| `erase_windows_vss_signature` | bool | Specifies whether the disk restored from a source snapshot should erase
Windows specific VSS signature. |
| `status` | String | [Output Only] The status of disk creation.
   
   
     - CREATING: Disk is provisioning.
     - RESTORING: Source data is being copied into the
     disk.
     - FAILED: Disk creation failed.
     - READY: Disk is ready for use.
     - DELETING: Disk is deleting. |
| `multi_writer` | bool | Indicates whether or not the disk can be read/write attached to
more than one instance. |
| `region` | String | [Output Only] URL of the region where the disk resides. Only applicable for
regional resources.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `source_storage_object` | String | The full Google Cloud Storage URI where the disk image is stored. This file
must be a gzip-compressed tarball whose name ends in .tar.gz or virtual
machine disk whose name ends in vmdk. Valid URIs may start with gs:// or
https://storage.googleapis.com/. This flag is not optimized for creating
multiple disks from a source storage object. To create many disks from a
source storage object, use gcloud compute images
import instead. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `satisfies_pzi` | bool | Output only. Reserved for future use. |
| `label_fingerprint` | String | A fingerprint for the labels being applied to this disk, which is
essentially a hash of the labels set used for optimistic locking. The
fingerprint is initially generated by Compute Engine and changes after
every request to modify or update labels. You must always provide an
up-to-date fingerprint hash in order to update or change labels,
otherwise the request will fail with error412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve a disk. |
| `source_image_id` | String | [Output Only] The ID value of the image used to create this disk. This
value identifies the exact image that was used to create this persistent
disk. For example, if you created the persistent disk from an image that
was later deleted and recreated under the same name, the source image ID
would identify the exact version of the image that was used. |
| `source_snapshot_id` | String | [Output Only] The unique ID of the snapshot used to create this disk. This
value identifies the exact snapshot that was used to create this persistent
disk. For example, if you created the persistent disk from a snapshot that
was later deleted and recreated under the same name, the source snapshot ID
would identify the exact version of the snapshot that was used. |
| `type` | String | URL of the disk type resource describing which disk type to use to create
the disk. Provide this when creating the disk. For example:projects/project/zones/zone/diskTypes/pd-ssd. See Persistent disk
types. |
| `zone` | String | [Output Only] URL of the zone where the disk resides.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `user_licenses` | Vec<String> | A list of publicly visible user-licenses. Unlike regular licenses, user
provided licenses can be modified after the disk is created. This includes
a list of URLs to the license resource. For example, to provide a debian
license:

https://www.googleapis.com/compute/v1/projects/debian-cloud/global/licenses/debian-9-stretch |
| `users` | Vec<String> | [Output Only] Links to the users of the disk (attached instances)
in form:projects/project/zones/zone/instances/instance |
| `provisioned_throughput` | String | Indicates how much throughput to provision for the disk. This sets the
number of throughput mb per second that the disk can handle. Values must be
greater than or equal to 1. |
| `enable_confidential_compute` | bool | Whether this disk is using confidential compute mode. |
| `storage_type` | String | [Deprecated] Storage type of the persistent disk. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `source_image_encryption_key` | String | Thecustomer-supplied
encryption key of the source image. Required if the source image is
protected by a customer-supplied encryption key. |
| `resource_status` | String | [Output Only] Status information for the disk resource. |
| `satisfies_pzs` | bool | [Output Only] Reserved for future use. |
| `last_detach_timestamp` | String | [Output Only] Last detach timestamp inRFC3339
text format. |
| `source_machine_image_disk_device_name` | String | Input only. The device name of a disk within a given machine image. The
source_machine_image must be specified. |
| `async_primary_disk` | String | Disk asynchronously replicated into this disk. |
| `size_gb` | String | Size, in GB, of the persistent disk. You can specify
this field when creating a persistent disk using thesourceImage, sourceSnapshot, orsourceDisk parameter, or specify it alone to create an empty
persistent disk.

If you specify this field along with a source, the value ofsizeGb must not be less than the size of the
source.
Acceptable values are greater than 0. |
| `last_attach_timestamp` | String | [Output Only] Last attach timestamp inRFC3339
text format. |
| `source_machine_image_encryption_key` | String | Thecustomer-supplied
encryption key of the source machine image. Required if the source
machine image is protected by a customer-supplied encryption key. |
| `source_machine_image_id` | String | [Output Only] The unique ID of the machine image used to create this disk.
This value identifies the exact machine image that was used to create this
persistent disk. For example, if you created the persistent disk from a
machine image that was later deleted and recreated under the same name, the
source machine image ID would identify the exact version of the machine
image that was used. |
| `source_disk` | String | The source disk used to create this disk. You can provide this as a
partial or full URL to the resource. For example, the following are valid
values:
   
   
     - 
       https://www.googleapis.com/compute/v1/projects/project/zones/zone/disks/disk
     
     - 
       https://www.googleapis.com/compute/v1/projects/project/regions/region/disks/disk
     
     - 
       projects/project/zones/zone/disks/disk
     
     - 
       projects/project/regions/region/disks/disk
     
     - 
       zones/zone/disks/disk
     
     - 
       regions/region/disks/disk |
| `self_link` | String | [Output Only] Server-defined fully-qualified URL for this resource. |
| `name` | String | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?`
which means the first character must be a lowercase letter, and all
following characters must be a dash, lowercase letter, or digit, except
the last character, which cannot be a dash. |
| `source_consistency_group_policy` | String | [Output Only] URL of the DiskConsistencyGroupPolicy for a secondary disk
that was created using a consistency group. |
| `source_machine_image` | String | The machine image to create the disk from. You can provide this as a
partial or full URL to the resource. For example, the following are valid
values:
   
   
     - https://www.googleapis.com/compute/v1/projects/project/global/machineImages/machineImage 
   - projects/project/global/machineImages/machineImage
     - global/machineImages/machineImage |
| `licenses` | Vec<String> | A list of publicly visible licenses. Reserved for Google's use. |
| `source_snapshot` | String | The source snapshot used to create this disk. You can provide this as a
partial or full URL to the resource. For example, the following are valid
values:
   
   
     - https://www.googleapis.com/compute/v1/projects/project/global/snapshots/snapshot 
   - projects/project/global/snapshots/snapshot
     - global/snapshots/snapshot |
| `access_mode` | String | The access mode of the disk.
   
   
     - READ_WRITE_SINGLE: The default AccessMode, means the
     disk can be attached to single instance in RW mode.
     - READ_WRITE_MANY: The AccessMode means the disk can be
     attached to multiple instances in RW mode.
     - READ_ONLY_MANY: The AccessMode means the disk can be
     attached to multiple instances in RO mode.


The AccessMode is only valid for Hyperdisk disk types. |
| `replica_zones` | Vec<String> | URLs of the zones where the disk should be replicated to. Only applicable
for regional resources. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create region_disk
region_disk = provider.compute_api.Region_disk {
    region = "value"  # Name of the region for this request.
    project = "value"  # Project ID for this request.
}

# Access region_disk outputs
region_disk_id = region_disk.id
region_disk_source_consistency_group_policy_id = region_disk.source_consistency_group_policy_id
region_disk_physical_block_size_bytes = region_disk.physical_block_size_bytes
region_disk_provisioned_iops = region_disk.provisioned_iops
region_disk_source_snapshot_encryption_key = region_disk.source_snapshot_encryption_key
region_disk_async_secondary_disks = region_disk.async_secondary_disks
region_disk_source_image = region_disk.source_image
region_disk_source_disk_id = region_disk.source_disk_id
region_disk_source_instant_snapshot_id = region_disk.source_instant_snapshot_id
region_disk_params = region_disk.params
region_disk_architecture = region_disk.architecture
region_disk_guest_os_features = region_disk.guest_os_features
region_disk_locked = region_disk.locked
region_disk_kind = region_disk.kind
region_disk_storage_pool = region_disk.storage_pool
region_disk_options = region_disk.options
region_disk_disk_encryption_key = region_disk.disk_encryption_key
region_disk_labels = region_disk.labels
region_disk_location_hint = region_disk.location_hint
region_disk_self_link_with_id = region_disk.self_link_with_id
region_disk_license_codes = region_disk.license_codes
region_disk_creation_timestamp = region_disk.creation_timestamp
region_disk_interface = region_disk.interface
region_disk_source_instant_snapshot = region_disk.source_instant_snapshot
region_disk_resource_policies = region_disk.resource_policies
region_disk_erase_windows_vss_signature = region_disk.erase_windows_vss_signature
region_disk_status = region_disk.status
region_disk_multi_writer = region_disk.multi_writer
region_disk_region = region_disk.region
region_disk_source_storage_object = region_disk.source_storage_object
region_disk_description = region_disk.description
region_disk_satisfies_pzi = region_disk.satisfies_pzi
region_disk_label_fingerprint = region_disk.label_fingerprint
region_disk_source_image_id = region_disk.source_image_id
region_disk_source_snapshot_id = region_disk.source_snapshot_id
region_disk_type = region_disk.type
region_disk_zone = region_disk.zone
region_disk_user_licenses = region_disk.user_licenses
region_disk_users = region_disk.users
region_disk_provisioned_throughput = region_disk.provisioned_throughput
region_disk_enable_confidential_compute = region_disk.enable_confidential_compute
region_disk_storage_type = region_disk.storage_type
region_disk_id = region_disk.id
region_disk_source_image_encryption_key = region_disk.source_image_encryption_key
region_disk_resource_status = region_disk.resource_status
region_disk_satisfies_pzs = region_disk.satisfies_pzs
region_disk_last_detach_timestamp = region_disk.last_detach_timestamp
region_disk_source_machine_image_disk_device_name = region_disk.source_machine_image_disk_device_name
region_disk_async_primary_disk = region_disk.async_primary_disk
region_disk_size_gb = region_disk.size_gb
region_disk_last_attach_timestamp = region_disk.last_attach_timestamp
region_disk_source_machine_image_encryption_key = region_disk.source_machine_image_encryption_key
region_disk_source_machine_image_id = region_disk.source_machine_image_id
region_disk_source_disk = region_disk.source_disk
region_disk_self_link = region_disk.self_link
region_disk_name = region_disk.name
region_disk_source_consistency_group_policy = region_disk.source_consistency_group_policy
region_disk_source_machine_image = region_disk.source_machine_image
region_disk_licenses = region_disk.licenses
region_disk_source_snapshot = region_disk.source_snapshot
region_disk_access_mode = region_disk.access_mode
region_disk_replica_zones = region_disk.replica_zones
```

---


### Target_tcp_proxie

Creates a TargetTcpProxy resource in the specified project using
the data included in the request.

**Operations**: ✅ Create ✅ Read ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `service` | String |  | URL to the BackendService resource. |
| `name` | String |  | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `proxy_header` | String |  | Specifies the type of proxy header to append before sending data to the
backend, either NONE or PROXY_V1. The default
is NONE. |
| `kind` | String |  | [Output Only] Type of the resource. Alwayscompute#targetTcpProxy for target TCP proxies. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `proxy_bind` | bool |  | This field only applies when the forwarding rule that references this
target proxy has a loadBalancingScheme set toINTERNAL_SELF_MANAGED.

When this field is set to true, Envoy proxies set up inbound
traffic interception and bind to the IP address and port specified in the
forwarding rule. This is generally useful when using Traffic Director to
configure Envoy as a gateway or middle proxy (in other words, not a
sidecar proxy). The Envoy proxy listens for inbound requests and handles
requests when it receives them.

The default is false. |
| `region` | String |  | [Output Only] URL of the region where the regional TCP proxy resides.
This field is not applicable to global TCP proxy. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `service` | String | URL to the BackendService resource. |
| `name` | String | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `proxy_header` | String | Specifies the type of proxy header to append before sending data to the
backend, either NONE or PROXY_V1. The default
is NONE. |
| `kind` | String | [Output Only] Type of the resource. Alwayscompute#targetTcpProxy for target TCP proxies. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `proxy_bind` | bool | This field only applies when the forwarding rule that references this
target proxy has a loadBalancingScheme set toINTERNAL_SELF_MANAGED.

When this field is set to true, Envoy proxies set up inbound
traffic interception and bind to the IP address and port specified in the
forwarding rule. This is generally useful when using Traffic Director to
configure Envoy as a gateway or middle proxy (in other words, not a
sidecar proxy). The Envoy proxy listens for inbound requests and handles
requests when it receives them.

The default is false. |
| `region` | String | [Output Only] URL of the region where the regional TCP proxy resides.
This field is not applicable to global TCP proxy. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create target_tcp_proxie
target_tcp_proxie = provider.compute_api.Target_tcp_proxie {
    project = "value"  # Project ID for this request.
}

# Access target_tcp_proxie outputs
target_tcp_proxie_id = target_tcp_proxie.id
target_tcp_proxie_self_link = target_tcp_proxie.self_link
target_tcp_proxie_creation_timestamp = target_tcp_proxie.creation_timestamp
target_tcp_proxie_service = target_tcp_proxie.service
target_tcp_proxie_name = target_tcp_proxie.name
target_tcp_proxie_proxy_header = target_tcp_proxie.proxy_header
target_tcp_proxie_kind = target_tcp_proxie.kind
target_tcp_proxie_description = target_tcp_proxie.description
target_tcp_proxie_proxy_bind = target_tcp_proxie.proxy_bind
target_tcp_proxie_region = target_tcp_proxie.region
target_tcp_proxie_id = target_tcp_proxie.id
```

---


### Region_multi_mig

Creates a multi-MIG in the specified project.

**Operations**: ✅ Create ✅ Read ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `status` | String |  |  |
| `creation_timestamp` | String |  | [Output only] The creation timestamp of this multi-MIG in RFC3339
text format. |
| `id` | String |  | [Output only] The unique identifier for this resource type. The server
generates this identifier. |
| `region` | String |  | [Output only] The URL of the region where the resource resides. You must
specify this field as part of the HTTP request URL. You cannot set the
region as a field in the request body. |
| `kind` | String |  | [Output only] Type of the resource. Alwayscompute#multiMig for multi-MIGs. |
| `description` | String |  | An optional description of this resource. |
| `name` | String |  | The name of the multi-MIG.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first character
must be a lowercase letter, and all following characters must be a dash,
lowercase letter, or digit, except the last character, which cannot be a
dash. |
| `self_link` | String |  | [Output only] Server-defined URL for the resource. |
| `resource_policies` | String |  | Resource policies for this multi-MIG. |
| `region` | String | ✅ | Name of the region for this request. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `status` | String |  |
| `creation_timestamp` | String | [Output only] The creation timestamp of this multi-MIG in RFC3339
text format. |
| `id` | String | [Output only] The unique identifier for this resource type. The server
generates this identifier. |
| `region` | String | [Output only] The URL of the region where the resource resides. You must
specify this field as part of the HTTP request URL. You cannot set the
region as a field in the request body. |
| `kind` | String | [Output only] Type of the resource. Alwayscompute#multiMig for multi-MIGs. |
| `description` | String | An optional description of this resource. |
| `name` | String | The name of the multi-MIG.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first character
must be a lowercase letter, and all following characters must be a dash,
lowercase letter, or digit, except the last character, which cannot be a
dash. |
| `self_link` | String | [Output only] Server-defined URL for the resource. |
| `resource_policies` | String | Resource policies for this multi-MIG. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create region_multi_mig
region_multi_mig = provider.compute_api.Region_multi_mig {
    region = "value"  # Name of the region for this request.
    project = "value"  # Project ID for this request.
}

# Access region_multi_mig outputs
region_multi_mig_id = region_multi_mig.id
region_multi_mig_status = region_multi_mig.status
region_multi_mig_creation_timestamp = region_multi_mig.creation_timestamp
region_multi_mig_id = region_multi_mig.id
region_multi_mig_region = region_multi_mig.region
region_multi_mig_kind = region_multi_mig.kind
region_multi_mig_description = region_multi_mig.description
region_multi_mig_name = region_multi_mig.name
region_multi_mig_self_link = region_multi_mig.self_link
region_multi_mig_resource_policies = region_multi_mig.resource_policies
```

---


### Instance_setting

Get Instance settings.

**Operations**: ✅ Read ✅ Update

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `zone` | String |  | [Output Only] URL of the zone where the resource resides
You must specify this field as part of the HTTP request URL. It is not
settable as a field in the request body. |
| `email` | String |  | Email address of the service account. |
| `fingerprint` | String |  | Specifies a fingerprint for instance settings, which is essentially a hash
of the instance settings resource's contents and used for optimistic
locking. The fingerprint is initially generated by Compute Engine and
changes after every request to modify or update the instance settings
resource. You must always provide an up-to-date fingerprint hash in order
to update or change the resource, otherwise the request will fail with
error 412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve the resource. |
| `kind` | String |  | [Output Only] Type of the resource. Alwayscompute#instance_settings for instance settings. |
| `metadata` | String |  | The metadata key/value pairs assigned to all the instances in the
corresponding scope. |
| `project` | String | ✅ | Project ID for this request. |
| `zone` | String | ✅ | The zone scoping this request. It should conform to RFC1035. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `zone` | String | [Output Only] URL of the zone where the resource resides
You must specify this field as part of the HTTP request URL. It is not
settable as a field in the request body. |
| `email` | String | Email address of the service account. |
| `fingerprint` | String | Specifies a fingerprint for instance settings, which is essentially a hash
of the instance settings resource's contents and used for optimistic
locking. The fingerprint is initially generated by Compute Engine and
changes after every request to modify or update the instance settings
resource. You must always provide an up-to-date fingerprint hash in order
to update or change the resource, otherwise the request will fail with
error 412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve the resource. |
| `kind` | String | [Output Only] Type of the resource. Alwayscompute#instance_settings for instance settings. |
| `metadata` | String | The metadata key/value pairs assigned to all the instances in the
corresponding scope. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Access instance_setting outputs
instance_setting_id = instance_setting.id
instance_setting_zone = instance_setting.zone
instance_setting_email = instance_setting.email
instance_setting_fingerprint = instance_setting.fingerprint
instance_setting_kind = instance_setting.kind
instance_setting_metadata = instance_setting.metadata
```

---


### Url_map

Creates a UrlMap resource in the specified project using
the data included in the request.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `default_route_action` | String |  | defaultRouteAction takes effect when none of the 
hostRules match. The load balancer performs advanced routing
actions, such as URL rewrites and header transformations, before forwarding
the request to the selected backend.


Only one of defaultUrlRedirect, defaultService
or defaultRouteAction.weightedBackendService can be set.


 URL maps for classic Application Load Balancers only support
the urlRewrite action within defaultRouteAction.


defaultRouteAction has no effect when the URL map is bound
to a target gRPC proxy that has the validateForProxyless field
set to true. |
| `default_custom_error_response_policy` | String |  | defaultCustomErrorResponsePolicy specifies how the Load
Balancer returns error responses when BackendServiceorBackendBucket responds with an error. 

This policy takes
effect at the load balancer level and applies only when no policy has been
defined for the error code at lower levels like PathMatcher, RouteRule and
PathRule within this UrlMap. 

For example, consider a UrlMap with the
following configuration:
   
   
     - defaultCustomErrorResponsePolicy containing policies for
     responding to 5xx and 4xx errors
     - A PathMatcher configured for *.example.com has
     defaultCustomErrorResponsePolicy for 4xx.

If a request for http://www.example.com/ encounters a404, the policy inpathMatcher.defaultCustomErrorResponsePolicy will be enforced.
When the request for http://www.example.com/ encounters a502, the policy inUrlMap.defaultCustomErrorResponsePolicy will be enforced. When
a request that does not match any host in *.example.com such
as http://www.myotherexample.com/, encounters a404, UrlMap.defaultCustomErrorResponsePolicy
takes effect. 

When used in conjunction withdefaultRouteAction.retryPolicy, retries take precedence. Only
once all retries are exhausted, thedefaultCustomErrorResponsePolicy is applied. While attempting
a retry, if load balancer is successful in reaching the
service, the defaultCustomErrorResponsePolicy is ignored and
the response from the service is returned to the client.

defaultCustomErrorResponsePolicy is supported only for
global external Application Load Balancers. |
| `default_url_redirect` | String |  | When none of the specified hostRules match, the request
is redirected to a URL specified by defaultUrlRedirect.


Only one of defaultUrlRedirect, defaultService
or defaultRouteAction.weightedBackendService can be set.


Not supported when the URL map is bound to a target gRPC proxy. |
| `region` | String |  | [Output Only] URL of the region where the regional URL map resides.
This field is not applicable to global URL maps.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `header_action` | String |  | Specifies changes to request and response headers that need to take effect
for the selected backendService.

The headerAction specified here take effect afterheaderAction specified under pathMatcher.

headerAction is not supported for load balancers
that have
their loadBalancingScheme set to EXTERNAL.

Not supported when the URL map is bound to a target gRPC proxy that
has validateForProxyless field set to true. |
| `path_matchers` | Vec<String> |  | The list of named PathMatchers to use against the URL. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `name` | String |  | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `tests` | Vec<String> |  | The list of expected URL mapping tests. Request to update theUrlMap succeeds only if all test cases pass. You can specify a
maximum of 100 tests per UrlMap.

Not supported when the URL map is bound to a target gRPC proxy that
has validateForProxyless field set to true. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `default_service` | String |  | The full or partial URL of the defaultService resource to
which traffic is directed if none of the hostRules match.
If defaultRouteAction is also specified, advanced
routing actions, such as URL rewrites, take effect before sending the
request to the backend.


Only one of defaultUrlRedirect, defaultService
or defaultRouteAction.weightedBackendService can be set.


defaultService has no effect when the URL map is bound
to a target gRPC proxy that has the validateForProxyless field
set to true. |
| `kind` | String |  | [Output Only] Type of the resource. Always compute#urlMaps for
url maps. |
| `fingerprint` | String |  | Fingerprint of this resource. A hash of the contents stored in this object.
This field is used in optimistic locking. This field is ignored when
inserting a UrlMap. An up-to-date fingerprint must be provided
in order to update the UrlMap, otherwise the request will
fail with error 412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve a UrlMap. |
| `host_rules` | Vec<String> |  | The list of host rules to use against the URL. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `default_route_action` | String | defaultRouteAction takes effect when none of the 
hostRules match. The load balancer performs advanced routing
actions, such as URL rewrites and header transformations, before forwarding
the request to the selected backend.


Only one of defaultUrlRedirect, defaultService
or defaultRouteAction.weightedBackendService can be set.


 URL maps for classic Application Load Balancers only support
the urlRewrite action within defaultRouteAction.


defaultRouteAction has no effect when the URL map is bound
to a target gRPC proxy that has the validateForProxyless field
set to true. |
| `default_custom_error_response_policy` | String | defaultCustomErrorResponsePolicy specifies how the Load
Balancer returns error responses when BackendServiceorBackendBucket responds with an error. 

This policy takes
effect at the load balancer level and applies only when no policy has been
defined for the error code at lower levels like PathMatcher, RouteRule and
PathRule within this UrlMap. 

For example, consider a UrlMap with the
following configuration:
   
   
     - defaultCustomErrorResponsePolicy containing policies for
     responding to 5xx and 4xx errors
     - A PathMatcher configured for *.example.com has
     defaultCustomErrorResponsePolicy for 4xx.

If a request for http://www.example.com/ encounters a404, the policy inpathMatcher.defaultCustomErrorResponsePolicy will be enforced.
When the request for http://www.example.com/ encounters a502, the policy inUrlMap.defaultCustomErrorResponsePolicy will be enforced. When
a request that does not match any host in *.example.com such
as http://www.myotherexample.com/, encounters a404, UrlMap.defaultCustomErrorResponsePolicy
takes effect. 

When used in conjunction withdefaultRouteAction.retryPolicy, retries take precedence. Only
once all retries are exhausted, thedefaultCustomErrorResponsePolicy is applied. While attempting
a retry, if load balancer is successful in reaching the
service, the defaultCustomErrorResponsePolicy is ignored and
the response from the service is returned to the client.

defaultCustomErrorResponsePolicy is supported only for
global external Application Load Balancers. |
| `default_url_redirect` | String | When none of the specified hostRules match, the request
is redirected to a URL specified by defaultUrlRedirect.


Only one of defaultUrlRedirect, defaultService
or defaultRouteAction.weightedBackendService can be set.


Not supported when the URL map is bound to a target gRPC proxy. |
| `region` | String | [Output Only] URL of the region where the regional URL map resides.
This field is not applicable to global URL maps.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `header_action` | String | Specifies changes to request and response headers that need to take effect
for the selected backendService.

The headerAction specified here take effect afterheaderAction specified under pathMatcher.

headerAction is not supported for load balancers
that have
their loadBalancingScheme set to EXTERNAL.

Not supported when the URL map is bound to a target gRPC proxy that
has validateForProxyless field set to true. |
| `path_matchers` | Vec<String> | The list of named PathMatchers to use against the URL. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `name` | String | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `tests` | Vec<String> | The list of expected URL mapping tests. Request to update theUrlMap succeeds only if all test cases pass. You can specify a
maximum of 100 tests per UrlMap.

Not supported when the URL map is bound to a target gRPC proxy that
has validateForProxyless field set to true. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `default_service` | String | The full or partial URL of the defaultService resource to
which traffic is directed if none of the hostRules match.
If defaultRouteAction is also specified, advanced
routing actions, such as URL rewrites, take effect before sending the
request to the backend.


Only one of defaultUrlRedirect, defaultService
or defaultRouteAction.weightedBackendService can be set.


defaultService has no effect when the URL map is bound
to a target gRPC proxy that has the validateForProxyless field
set to true. |
| `kind` | String | [Output Only] Type of the resource. Always compute#urlMaps for
url maps. |
| `fingerprint` | String | Fingerprint of this resource. A hash of the contents stored in this object.
This field is used in optimistic locking. This field is ignored when
inserting a UrlMap. An up-to-date fingerprint must be provided
in order to update the UrlMap, otherwise the request will
fail with error 412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve a UrlMap. |
| `host_rules` | Vec<String> | The list of host rules to use against the URL. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create url_map
url_map = provider.compute_api.Url_map {
    project = "value"  # Project ID for this request.
}

# Access url_map outputs
url_map_id = url_map.id
url_map_default_route_action = url_map.default_route_action
url_map_default_custom_error_response_policy = url_map.default_custom_error_response_policy
url_map_default_url_redirect = url_map.default_url_redirect
url_map_region = url_map.region
url_map_self_link = url_map.self_link
url_map_header_action = url_map.header_action
url_map_path_matchers = url_map.path_matchers
url_map_creation_timestamp = url_map.creation_timestamp
url_map_name = url_map.name
url_map_tests = url_map.tests
url_map_id = url_map.id
url_map_description = url_map.description
url_map_default_service = url_map.default_service
url_map_kind = url_map.kind
url_map_fingerprint = url_map.fingerprint
url_map_host_rules = url_map.host_rules
```

---


### Node_type

Returns the specified node type.

**Operations**: ✅ Read

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `zone` | String | [Output Only] The name of the zone where the node type resides,
such as us-central1-a. |
| `name` | String | [Output Only] Name of the resource. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `description` | String | [Output Only] An optional textual description of the resource. |
| `kind` | String | [Output Only] The type of the resource. Alwayscompute#nodeType for node types. |
| `guest_cpus` | i64 | [Output Only] The number of virtual CPUs that are available to the node
type. |
| `local_ssd_gb` | i64 | [Output Only] Local SSD available to the node type, defined in GB. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `cpu_platform` | String | [Output Only] The CPU platform used by this node type. |
| `max_vms` | i64 | [Output Only] Maximum number of VMs that can be created for this node type. |
| `memory_mb` | i64 | [Output Only] The amount of physical memory available to the node type,
defined in MB. |
| `self_link_with_id` | String | [Output Only] Server-defined URL for this resource with the resource id. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `deprecated` | String | [Output Only] The deprecation status associated with this node type. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Access node_type outputs
node_type_id = node_type.id
node_type_zone = node_type.zone
node_type_name = node_type.name
node_type_id = node_type.id
node_type_description = node_type.description
node_type_kind = node_type.kind
node_type_guest_cpus = node_type.guest_cpus
node_type_local_ssd_gb = node_type.local_ssd_gb
node_type_creation_timestamp = node_type.creation_timestamp
node_type_cpu_platform = node_type.cpu_platform
node_type_max_vms = node_type.max_vms
node_type_memory_mb = node_type.memory_mb
node_type_self_link_with_id = node_type.self_link_with_id
node_type_self_link = node_type.self_link
node_type_deprecated = node_type.deprecated
```

---


### Interconnect_remote_location

Returns the details for the specified interconnect remote location. Gets a
list of available interconnect remote locations by making alist() request.

**Operations**: ✅ Read

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `self_link_with_id` | String | [Output Only] Server-defined URL for this resource with the resource id. |
| `address` | String | [Output Only] The postal address of the Point of Presence, each line in
the address is separated by a newline character. |
| `facility_provider` | String | [Output Only] The name of the provider for this facility (e.g., EQUINIX). |
| `max_lag_size100_gbps` | i64 | [Output Only]
The maximum number of 100 Gbps ports supported in a link aggregation group
(LAG). When linkType is 100 Gbps, requestedLinkCount cannot exceed
max_lag_size_100_gbps. |
| `max_lag_size400_gbps` | i64 | [Output Only]
The maximum number of 400 Gbps ports supported in a link aggregation group
(LAG). When linkType is 400 Gbps, requestedLinkCount cannot exceed
max_lag_size_400_gbps. |
| `peeringdb_facility_id` | String | [Output Only] The peeringdb identifier for this facility (corresponding
with a netfac type in peeringdb). |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `remote_service` | String | [Output Only] Indicates the service provider present at the remote
location. Example values: "Amazon Web Services", "Microsoft Azure". |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `facility_provider_facility_id` | String | [Output Only] A provider-assigned Identifier for this facility (e.g.,
Ashburn-DC1). |
| `permitted_connections` | Vec<String> | [Output Only] Permitted connections. |
| `city` | String | [Output Only] Metropolitan area designator that indicates which city an
interconnect is located.
For example: "Chicago, IL", "Amsterdam, Netherlands". |
| `constraints` | String | [Output Only] Constraints on the parameters for creating Cross-Cloud
Interconnect and associated InterconnectAttachments. |
| `attachment_configuration_constraints` | String | [Output Only] Subset of fields from InterconnectAttachment's
|configurationConstraints| field that apply to all attachments for this
remote location. |
| `description` | String | [Output Only] An optional description of the resource. |
| `kind` | String | [Output Only] Type of the resource. Alwayscompute#interconnectRemoteLocation for interconnect remote
locations. |
| `name` | String | [Output Only] Name of the resource. |
| `max_lag_size10_gbps` | i64 | [Output Only]
The maximum number of 10 Gbps ports supported in a link aggregation group
(LAG). When linkType is 10 Gbps, requestedLinkCount cannot exceed
max_lag_size_10_gbps. |
| `status` | String | [Output Only] The status of this InterconnectRemoteLocation, which can take
one of the following values:
   
   - CLOSED: The InterconnectRemoteLocation is closed and is unavailable
   for provisioning new Cross-Cloud Interconnects. 
   - AVAILABLE: The
   InterconnectRemoteLocation is available for provisioning new
   Cross-Cloud Interconnects. |
| `lacp` | String | [Output Only] Link Aggregation Control Protocol (LACP) constraints, which
can take one of the following values: LACP_SUPPORTED, LACP_UNSUPPORTED |
| `continent` | String | [Output Only] Continent for this location, which can take one of the
following values:
   
   - AFRICA
   - ASIA_PAC
   - EUROPE
   - NORTH_AMERICA
   - SOUTH_AMERICA |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Access interconnect_remote_location outputs
interconnect_remote_location_id = interconnect_remote_location.id
interconnect_remote_location_self_link_with_id = interconnect_remote_location.self_link_with_id
interconnect_remote_location_address = interconnect_remote_location.address
interconnect_remote_location_facility_provider = interconnect_remote_location.facility_provider
interconnect_remote_location_max_lag_size100_gbps = interconnect_remote_location.max_lag_size100_gbps
interconnect_remote_location_max_lag_size400_gbps = interconnect_remote_location.max_lag_size400_gbps
interconnect_remote_location_peeringdb_facility_id = interconnect_remote_location.peeringdb_facility_id
interconnect_remote_location_id = interconnect_remote_location.id
interconnect_remote_location_remote_service = interconnect_remote_location.remote_service
interconnect_remote_location_self_link = interconnect_remote_location.self_link
interconnect_remote_location_facility_provider_facility_id = interconnect_remote_location.facility_provider_facility_id
interconnect_remote_location_permitted_connections = interconnect_remote_location.permitted_connections
interconnect_remote_location_city = interconnect_remote_location.city
interconnect_remote_location_constraints = interconnect_remote_location.constraints
interconnect_remote_location_attachment_configuration_constraints = interconnect_remote_location.attachment_configuration_constraints
interconnect_remote_location_description = interconnect_remote_location.description
interconnect_remote_location_kind = interconnect_remote_location.kind
interconnect_remote_location_name = interconnect_remote_location.name
interconnect_remote_location_max_lag_size10_gbps = interconnect_remote_location.max_lag_size10_gbps
interconnect_remote_location_status = interconnect_remote_location.status
interconnect_remote_location_lacp = interconnect_remote_location.lacp
interconnect_remote_location_continent = interconnect_remote_location.continent
interconnect_remote_location_creation_timestamp = interconnect_remote_location.creation_timestamp
```

---


### Ssl_policie

Returns the specified SSL policy resource.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `enabled_features` | Vec<String> |  | [Output Only] The list of features enabled in the SSL policy. |
| `tls_settings` | String |  | Security settings for the proxy. This field is only applicable to a
global backend service with the loadBalancingScheme set toINTERNAL_SELF_MANAGED. |
| `kind` | String |  | [Output only] Type of the resource. Alwayscompute#sslPolicyfor SSL policies. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `name` | String |  | Name of the resource. The name must be 1-63 characters long, and comply
with RFC1035. Specifically, the name must be 1-63 characters
long and match the regular expression
`[a-z]([-a-z0-9]*[a-z0-9])?` which means the first character
must be a lowercase letter, and all following characters must be a dash,
lowercase letter, or digit, except the last character, which cannot be a
dash. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `warnings` | Vec<String> |  | [Output Only] If potential misconfigurations are detected for this
SSL policy, this field will be populated with warning messages. |
| `fingerprint` | String |  | Fingerprint of this resource. A hash of the contents stored in this object.
This field is used in optimistic locking. This field will be ignored when
inserting a SslPolicy. An up-to-date fingerprint must be
provided in order to update the SslPolicy, otherwise the
request will fail with error 412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve an SslPolicy. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `profile` | String |  | Profile specifies the set of SSL features that can be used by the load
balancer when negotiating SSL with clients. This can be one ofCOMPATIBLE, MODERN, RESTRICTED, orCUSTOM. If using CUSTOM, the set of SSL features
to enable must be specified in the customFeatures field. |
| `self_link_with_id` | String |  | [Output Only] Server-defined URL for this resource with the resource id. |
| `min_tls_version` | String |  | The minimum version of SSL protocol that can be used by the clients to
establish a connection with the load balancer. This can be one ofTLS_1_0, TLS_1_1, TLS_1_2,TLS_1_3. When set to TLS_1_3, the profile field
must be set to RESTRICTED. |
| `custom_features` | Vec<String> |  | A list of features enabled when the selected profile is CUSTOM. The
 method returns the set of features that can be
specified in this list. This field must be empty if the profile is notCUSTOM. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `region` | String |  | [Output Only] URL of the region where the regional SSL policy
resides. This field is not applicable to global SSL policies. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `enabled_features` | Vec<String> | [Output Only] The list of features enabled in the SSL policy. |
| `tls_settings` | String | Security settings for the proxy. This field is only applicable to a
global backend service with the loadBalancingScheme set toINTERNAL_SELF_MANAGED. |
| `kind` | String | [Output only] Type of the resource. Alwayscompute#sslPolicyfor SSL policies. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `name` | String | Name of the resource. The name must be 1-63 characters long, and comply
with RFC1035. Specifically, the name must be 1-63 characters
long and match the regular expression
`[a-z]([-a-z0-9]*[a-z0-9])?` which means the first character
must be a lowercase letter, and all following characters must be a dash,
lowercase letter, or digit, except the last character, which cannot be a
dash. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `warnings` | Vec<String> | [Output Only] If potential misconfigurations are detected for this
SSL policy, this field will be populated with warning messages. |
| `fingerprint` | String | Fingerprint of this resource. A hash of the contents stored in this object.
This field is used in optimistic locking. This field will be ignored when
inserting a SslPolicy. An up-to-date fingerprint must be
provided in order to update the SslPolicy, otherwise the
request will fail with error 412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve an SslPolicy. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `profile` | String | Profile specifies the set of SSL features that can be used by the load
balancer when negotiating SSL with clients. This can be one ofCOMPATIBLE, MODERN, RESTRICTED, orCUSTOM. If using CUSTOM, the set of SSL features
to enable must be specified in the customFeatures field. |
| `self_link_with_id` | String | [Output Only] Server-defined URL for this resource with the resource id. |
| `min_tls_version` | String | The minimum version of SSL protocol that can be used by the clients to
establish a connection with the load balancer. This can be one ofTLS_1_0, TLS_1_1, TLS_1_2,TLS_1_3. When set to TLS_1_3, the profile field
must be set to RESTRICTED. |
| `custom_features` | Vec<String> | A list of features enabled when the selected profile is CUSTOM. The
 method returns the set of features that can be
specified in this list. This field must be empty if the profile is notCUSTOM. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `region` | String | [Output Only] URL of the region where the regional SSL policy
resides. This field is not applicable to global SSL policies. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create ssl_policie
ssl_policie = provider.compute_api.Ssl_policie {
    project = "value"  # Project ID for this request.
}

# Access ssl_policie outputs
ssl_policie_id = ssl_policie.id
ssl_policie_enabled_features = ssl_policie.enabled_features
ssl_policie_tls_settings = ssl_policie.tls_settings
ssl_policie_kind = ssl_policie.kind
ssl_policie_id = ssl_policie.id
ssl_policie_name = ssl_policie.name
ssl_policie_self_link = ssl_policie.self_link
ssl_policie_warnings = ssl_policie.warnings
ssl_policie_fingerprint = ssl_policie.fingerprint
ssl_policie_creation_timestamp = ssl_policie.creation_timestamp
ssl_policie_profile = ssl_policie.profile
ssl_policie_self_link_with_id = ssl_policie.self_link_with_id
ssl_policie_min_tls_version = ssl_policie.min_tls_version
ssl_policie_custom_features = ssl_policie.custom_features
ssl_policie_description = ssl_policie.description
ssl_policie_region = ssl_policie.region
```

---


### Region_health_check_service

Creates a regional HealthCheckService resource in the
specified project and region using the data included in the request.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `health_checks` | Vec<String> |  | A list of URLs to the HealthCheck resources. Must have
at least one HealthCheck, and not more than 10 for regionalHealthCheckService, and not more than 1 for globalHealthCheckService.HealthCheck resources must haveportSpecification=USE_SERVING_PORT orportSpecification=USE_FIXED_PORT. For
regional HealthCheckService, theHealthCheck must be regional and in the same
region. For global HealthCheckService,HealthCheck must be global. Mix of regional and globalHealthChecks is not supported. Multiple regionalHealthChecks must belong to the same region. RegionalHealthChecks must belong to the same region as zones ofNetworkEndpointGroups. For globalHealthCheckService using globalINTERNET_IP_PORT NetworkEndpointGroups, the
global HealthChecks must specify sourceRegions,
and HealthChecks that specify sourceRegions can
only be used with global INTERNET_IP_PORTNetworkEndpointGroups. |
| `name` | String |  | Name of the resource. The name must be 1-63 characters long, and comply
with RFC1035. Specifically, the name must be 1-63 characters
long and match the regular expression
`[a-z]([-a-z0-9]*[a-z0-9])?` which means the first character
must be a lowercase letter, and all following characters must be a dash,
lowercase letter, or digit, except the last character, which cannot be a
dash. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `health_status_aggregation_strategy` | String |  | This field is deprecated. Use health_status_aggregation_policy
instead.

Policy for how the results from multiple health checks for the
same endpoint are aggregated.
   
   - NO_AGGREGATION. An EndpointHealth
   message is returned for each backend in the health check
   service.
   - AND.  If any backend's health check reportsUNHEALTHY, then UNHEALTHY is theHealthState of the entire health check service. If
   all backend's are healthy, the HealthState of the
   health check service is HEALTHY.

. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `self_link_with_id` | String |  | [Output Only] Server-defined URL with id for the resource. |
| `kind` | String |  | [Output only] Type of the resource. Alwayscompute#healthCheckServicefor health check services. |
| `notification_endpoints` | Vec<String> |  | A list of URLs to the NotificationEndpoint
resources. Must not have more than 10.  A list of endpoints for
receiving notifications of change in health status. For regionalHealthCheckService,NotificationEndpoint must be regional and in the
same region. For global HealthCheckService,NotificationEndpoint must be global. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `fingerprint` | String |  | Fingerprint of this resource. A hash of the contents stored in this object.
This field is used in optimistic locking. This field will be ignored when
inserting a HealthCheckService. An up-to-date fingerprint must
be provided in order to patch/update the HealthCheckService; Otherwise, the
request will fail with error 412 conditionNotMet. To see the
latest fingerprint, make a get() request to retrieve the
HealthCheckService. |
| `network_endpoint_groups` | Vec<String> |  | A list of URLs to the NetworkEndpointGroup
resources. Must not have more than 100.  For regionalHealthCheckService, NEGs must be in
zones in the region of the HealthCheckService. For globalHealthCheckServices, the NetworkEndpointGroups
must be global INTERNET_IP_PORT. |
| `region` | String |  | [Output Only] URL of the region where the health check service
resides. This field is not applicable to global health check services.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `health_status_aggregation_policy` | String |  | Optional. Policy for how the results from multiple health checks
for the same endpoint are aggregated. Defaults to NO_AGGREGATION
if unspecified.
   
   - NO_AGGREGATION. An EndpointHealth message is
   returned for each  pair in the health check
   service.
   - AND. If any health check of an endpoint reportsUNHEALTHY, then UNHEALTHY is theHealthState of the endpoint. If all health checks reportHEALTHY, the HealthState of the endpoint isHEALTHY.

.
This is only allowed with regional HealthCheckService. |
| `region` | String | ✅ | Name of the region scoping this request. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `health_checks` | Vec<String> | A list of URLs to the HealthCheck resources. Must have
at least one HealthCheck, and not more than 10 for regionalHealthCheckService, and not more than 1 for globalHealthCheckService.HealthCheck resources must haveportSpecification=USE_SERVING_PORT orportSpecification=USE_FIXED_PORT. For
regional HealthCheckService, theHealthCheck must be regional and in the same
region. For global HealthCheckService,HealthCheck must be global. Mix of regional and globalHealthChecks is not supported. Multiple regionalHealthChecks must belong to the same region. RegionalHealthChecks must belong to the same region as zones ofNetworkEndpointGroups. For globalHealthCheckService using globalINTERNET_IP_PORT NetworkEndpointGroups, the
global HealthChecks must specify sourceRegions,
and HealthChecks that specify sourceRegions can
only be used with global INTERNET_IP_PORTNetworkEndpointGroups. |
| `name` | String | Name of the resource. The name must be 1-63 characters long, and comply
with RFC1035. Specifically, the name must be 1-63 characters
long and match the regular expression
`[a-z]([-a-z0-9]*[a-z0-9])?` which means the first character
must be a lowercase letter, and all following characters must be a dash,
lowercase letter, or digit, except the last character, which cannot be a
dash. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `health_status_aggregation_strategy` | String | This field is deprecated. Use health_status_aggregation_policy
instead.

Policy for how the results from multiple health checks for the
same endpoint are aggregated.
   
   - NO_AGGREGATION. An EndpointHealth
   message is returned for each backend in the health check
   service.
   - AND.  If any backend's health check reportsUNHEALTHY, then UNHEALTHY is theHealthState of the entire health check service. If
   all backend's are healthy, the HealthState of the
   health check service is HEALTHY.

. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `self_link_with_id` | String | [Output Only] Server-defined URL with id for the resource. |
| `kind` | String | [Output only] Type of the resource. Alwayscompute#healthCheckServicefor health check services. |
| `notification_endpoints` | Vec<String> | A list of URLs to the NotificationEndpoint
resources. Must not have more than 10.  A list of endpoints for
receiving notifications of change in health status. For regionalHealthCheckService,NotificationEndpoint must be regional and in the
same region. For global HealthCheckService,NotificationEndpoint must be global. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `fingerprint` | String | Fingerprint of this resource. A hash of the contents stored in this object.
This field is used in optimistic locking. This field will be ignored when
inserting a HealthCheckService. An up-to-date fingerprint must
be provided in order to patch/update the HealthCheckService; Otherwise, the
request will fail with error 412 conditionNotMet. To see the
latest fingerprint, make a get() request to retrieve the
HealthCheckService. |
| `network_endpoint_groups` | Vec<String> | A list of URLs to the NetworkEndpointGroup
resources. Must not have more than 100.  For regionalHealthCheckService, NEGs must be in
zones in the region of the HealthCheckService. For globalHealthCheckServices, the NetworkEndpointGroups
must be global INTERNET_IP_PORT. |
| `region` | String | [Output Only] URL of the region where the health check service
resides. This field is not applicable to global health check services.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `health_status_aggregation_policy` | String | Optional. Policy for how the results from multiple health checks
for the same endpoint are aggregated. Defaults to NO_AGGREGATION
if unspecified.
   
   - NO_AGGREGATION. An EndpointHealth message is
   returned for each  pair in the health check
   service.
   - AND. If any health check of an endpoint reportsUNHEALTHY, then UNHEALTHY is theHealthState of the endpoint. If all health checks reportHEALTHY, the HealthState of the endpoint isHEALTHY.

.
This is only allowed with regional HealthCheckService. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create region_health_check_service
region_health_check_service = provider.compute_api.Region_health_check_service {
    region = "value"  # Name of the region scoping this request.
    project = "value"  # Project ID for this request.
}

# Access region_health_check_service outputs
region_health_check_service_id = region_health_check_service.id
region_health_check_service_health_checks = region_health_check_service.health_checks
region_health_check_service_name = region_health_check_service.name
region_health_check_service_creation_timestamp = region_health_check_service.creation_timestamp
region_health_check_service_health_status_aggregation_strategy = region_health_check_service.health_status_aggregation_strategy
region_health_check_service_self_link = region_health_check_service.self_link
region_health_check_service_self_link_with_id = region_health_check_service.self_link_with_id
region_health_check_service_kind = region_health_check_service.kind
region_health_check_service_notification_endpoints = region_health_check_service.notification_endpoints
region_health_check_service_id = region_health_check_service.id
region_health_check_service_description = region_health_check_service.description
region_health_check_service_fingerprint = region_health_check_service.fingerprint
region_health_check_service_network_endpoint_groups = region_health_check_service.network_endpoint_groups
region_health_check_service_region = region_health_check_service.region
region_health_check_service_health_status_aggregation_policy = region_health_check_service.health_status_aggregation_policy
```

---


### Region_instant_snapshot_group

creates a Regional InstantSnapshotGroup resource

**Operations**: ✅ Create ✅ Read ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `kind` | String |  | [Output Only] Type of the resource. Alwayscompute#instantSnapshotGroup for InstantSnapshotGroup
resources. |
| `description` | String |  | Optional. An optional description of this resource. Provide this property when you
create the resource. |
| `region` | String |  | [Output Only] URL of the region where the instant snapshot group resides.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `resource_status` | String |  |  |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `self_link_with_id` | String |  | [Output Only] Server-defined URL for this resource's resource id. |
| `source_consistency_group` | String |  |  |
| `name` | String |  | Identifier. Name of the resource; provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must be
a dash, lowercase letter, or digit, except the last character, which cannot
be a dash. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `status` | String |  | [Output Only] |
| `zone` | String |  | [Output Only] URL of the zone where the instant snapshot group resides.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `region` | String | ✅ | Name of the region for this request. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `kind` | String | [Output Only] Type of the resource. Alwayscompute#instantSnapshotGroup for InstantSnapshotGroup
resources. |
| `description` | String | Optional. An optional description of this resource. Provide this property when you
create the resource. |
| `region` | String | [Output Only] URL of the region where the instant snapshot group resides.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `resource_status` | String |  |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `self_link_with_id` | String | [Output Only] Server-defined URL for this resource's resource id. |
| `source_consistency_group` | String |  |
| `name` | String | Identifier. Name of the resource; provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must be
a dash, lowercase letter, or digit, except the last character, which cannot
be a dash. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `status` | String | [Output Only] |
| `zone` | String | [Output Only] URL of the zone where the instant snapshot group resides.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create region_instant_snapshot_group
region_instant_snapshot_group = provider.compute_api.Region_instant_snapshot_group {
    region = "value"  # Name of the region for this request.
    project = "value"  # Project ID for this request.
}

# Access region_instant_snapshot_group outputs
region_instant_snapshot_group_id = region_instant_snapshot_group.id
region_instant_snapshot_group_kind = region_instant_snapshot_group.kind
region_instant_snapshot_group_description = region_instant_snapshot_group.description
region_instant_snapshot_group_region = region_instant_snapshot_group.region
region_instant_snapshot_group_resource_status = region_instant_snapshot_group.resource_status
region_instant_snapshot_group_self_link = region_instant_snapshot_group.self_link
region_instant_snapshot_group_self_link_with_id = region_instant_snapshot_group.self_link_with_id
region_instant_snapshot_group_source_consistency_group = region_instant_snapshot_group.source_consistency_group
region_instant_snapshot_group_name = region_instant_snapshot_group.name
region_instant_snapshot_group_creation_timestamp = region_instant_snapshot_group.creation_timestamp
region_instant_snapshot_group_status = region_instant_snapshot_group.status
region_instant_snapshot_group_zone = region_instant_snapshot_group.zone
region_instant_snapshot_group_id = region_instant_snapshot_group.id
```

---


### Region_disk_type

Returns the specified regional disk type.

**Operations**: ✅ Read

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `deprecated` | String | [Output Only] The deprecation status associated with this disk type. |
| `name` | String | [Output Only] Name of the resource. |
| `self_link_with_id` | String | [Output Only] Server-defined URL for this resource with the resource id. |
| `region` | String | [Output Only] URL of the region where the disk type resides. Only
applicable for regional resources.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `description` | String | [Output Only] An optional description of this resource. |
| `kind` | String | [Output Only] Type of the resource. Always compute#diskType
for disk types. |
| `valid_disk_size` | String | [Output Only] An optional textual description of the valid disk size,
such as "10GB-10TB". |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `default_disk_size_gb` | String | [Output Only] Server-defined default disk size in GB. |
| `zone` | String | [Output Only] URL of the zone where the disk type resides.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Access region_disk_type outputs
region_disk_type_id = region_disk_type.id
region_disk_type_creation_timestamp = region_disk_type.creation_timestamp
region_disk_type_deprecated = region_disk_type.deprecated
region_disk_type_name = region_disk_type.name
region_disk_type_self_link_with_id = region_disk_type.self_link_with_id
region_disk_type_region = region_disk_type.region
region_disk_type_self_link = region_disk_type.self_link
region_disk_type_description = region_disk_type.description
region_disk_type_kind = region_disk_type.kind
region_disk_type_valid_disk_size = region_disk_type.valid_disk_size
region_disk_type_id = region_disk_type.id
region_disk_type_default_disk_size_gb = region_disk_type.default_disk_size_gb
region_disk_type_zone = region_disk_type.zone
```

---


### Region_health_check

Creates a HealthCheck resource in the specified project using the data
included in the request.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `http2_health_check` | String |  |  |
| `kind` | String |  | Type of the resource. |
| `self_link_with_id` | String |  | [Output Only] Server-defined URL for this resource with the resource id. |
| `grpc_tls_health_check` | String |  |  |
| `source_regions` | Vec<String> |  | The list of cloud regions from which health checks are performed. If any
regions are specified, then exactly 3 regions should be specified. The
region names must be valid names of Google Cloud regions.
This can only be set for global health check.
If this list is non-empty, then there are restrictions
on what other health check fields are supported and what other resources
can use this health check:
   
   - SSL, HTTP2, and GRPC protocols are not supported.
   - The TCP request field is not supported.
   - The proxyHeader field for HTTP, HTTPS, and TCP is not
   supported.
   - The checkIntervalSec field must be at least 30.
   - The health check cannot be used with BackendService nor with managed
   instance group auto-healing. |
| `ssl_health_check` | String |  |  |
| `region` | String |  | [Output Only] Region where the health check resides.  Not applicable to
global health checks. |
| `healthy_threshold` | i64 |  | A so-far unhealthy instance will be marked healthy after this
many consecutive successes. The default value is 2. |
| `http_health_check` | String |  |  |
| `tcp_health_check` | String |  |  |
| `udp_health_check` | String |  |  |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp in3339
text format. |
| `grpc_health_check` | String |  |  |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `log_config` | String |  | Configure logging on this health check. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `name` | String |  | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
For example, a name that is 1-63 characters long, matches the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?`, and otherwise complies with
RFC1035. This regular expression describes a name where the first
character is a lowercase letter, and all following characters are a dash,
lowercase letter, or digit, except the last character, which isn't a dash. |
| `check_interval_sec` | i64 |  | How often (in seconds) to send a health check. The default value is 5
seconds. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `type` | String |  | Specifies the type of the healthCheck, either TCP,SSL, HTTP, HTTPS,HTTP2 or GRPC. Exactly one of the
protocol-specific health check fields must be specified, which must matchtype field. |
| `unhealthy_threshold` | i64 |  | A so-far healthy instance will be marked unhealthy after this many
consecutive failures. The default value is 2. |
| `https_health_check` | String |  |  |
| `timeout_sec` | i64 |  | How long (in seconds) to wait before claiming failure. The default value is
5 seconds. It is invalid for timeoutSec to have greater
value than checkIntervalSec. |
| `region` | String | ✅ | Name of the region scoping this request. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `http2_health_check` | String |  |
| `kind` | String | Type of the resource. |
| `self_link_with_id` | String | [Output Only] Server-defined URL for this resource with the resource id. |
| `grpc_tls_health_check` | String |  |
| `source_regions` | Vec<String> | The list of cloud regions from which health checks are performed. If any
regions are specified, then exactly 3 regions should be specified. The
region names must be valid names of Google Cloud regions.
This can only be set for global health check.
If this list is non-empty, then there are restrictions
on what other health check fields are supported and what other resources
can use this health check:
   
   - SSL, HTTP2, and GRPC protocols are not supported.
   - The TCP request field is not supported.
   - The proxyHeader field for HTTP, HTTPS, and TCP is not
   supported.
   - The checkIntervalSec field must be at least 30.
   - The health check cannot be used with BackendService nor with managed
   instance group auto-healing. |
| `ssl_health_check` | String |  |
| `region` | String | [Output Only] Region where the health check resides.  Not applicable to
global health checks. |
| `healthy_threshold` | i64 | A so-far unhealthy instance will be marked healthy after this
many consecutive successes. The default value is 2. |
| `http_health_check` | String |  |
| `tcp_health_check` | String |  |
| `udp_health_check` | String |  |
| `creation_timestamp` | String | [Output Only] Creation timestamp in3339
text format. |
| `grpc_health_check` | String |  |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `log_config` | String | Configure logging on this health check. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `name` | String | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
For example, a name that is 1-63 characters long, matches the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?`, and otherwise complies with
RFC1035. This regular expression describes a name where the first
character is a lowercase letter, and all following characters are a dash,
lowercase letter, or digit, except the last character, which isn't a dash. |
| `check_interval_sec` | i64 | How often (in seconds) to send a health check. The default value is 5
seconds. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `type` | String | Specifies the type of the healthCheck, either TCP,SSL, HTTP, HTTPS,HTTP2 or GRPC. Exactly one of the
protocol-specific health check fields must be specified, which must matchtype field. |
| `unhealthy_threshold` | i64 | A so-far healthy instance will be marked unhealthy after this many
consecutive failures. The default value is 2. |
| `https_health_check` | String |  |
| `timeout_sec` | i64 | How long (in seconds) to wait before claiming failure. The default value is
5 seconds. It is invalid for timeoutSec to have greater
value than checkIntervalSec. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create region_health_check
region_health_check = provider.compute_api.Region_health_check {
    region = "value"  # Name of the region scoping this request.
    project = "value"  # Project ID for this request.
}

# Access region_health_check outputs
region_health_check_id = region_health_check.id
region_health_check_http2_health_check = region_health_check.http2_health_check
region_health_check_kind = region_health_check.kind
region_health_check_self_link_with_id = region_health_check.self_link_with_id
region_health_check_grpc_tls_health_check = region_health_check.grpc_tls_health_check
region_health_check_source_regions = region_health_check.source_regions
region_health_check_ssl_health_check = region_health_check.ssl_health_check
region_health_check_region = region_health_check.region
region_health_check_healthy_threshold = region_health_check.healthy_threshold
region_health_check_http_health_check = region_health_check.http_health_check
region_health_check_tcp_health_check = region_health_check.tcp_health_check
region_health_check_udp_health_check = region_health_check.udp_health_check
region_health_check_creation_timestamp = region_health_check.creation_timestamp
region_health_check_grpc_health_check = region_health_check.grpc_health_check
region_health_check_id = region_health_check.id
region_health_check_log_config = region_health_check.log_config
region_health_check_self_link = region_health_check.self_link
region_health_check_name = region_health_check.name
region_health_check_check_interval_sec = region_health_check.check_interval_sec
region_health_check_description = region_health_check.description
region_health_check_type = region_health_check.type
region_health_check_unhealthy_threshold = region_health_check.unhealthy_threshold
region_health_check_https_health_check = region_health_check.https_health_check
region_health_check_timeout_sec = region_health_check.timeout_sec
```

---


### Wire_group

Creates a wire group in the specified project in the given scope
using the parameters that are included in the request.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `admin_enabled` | bool |  | Indicates whether the wires in the wire group are enabled. When false, the
wires in the wire group are disabled. When true and when
there is simultaneously no wire-specific override of `adminEnabled` to
false, a given wire is enabled. Defaults to true. |
| `name` | String |  | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `kind` | String |  | [Output Only] Type of the resource. Alwayscompute#wireGroups for wire groups. |
| `self_link_with_id` | String |  |  |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `wire_inputs` | HashMap<String, String> |  | A map that contains optional settings for individual wires.
Specify key-value pairs for the map as follows:
   
   - Key: the label of an existing wire. To view a wire label and its
   format, see the wires[].label field.
   - Value: a WireInputs object. |
| `description` | String |  | An optional description of the wire group. |
| `wire_group_properties` | String |  | Properties of the wire group. |
| `endpoints` | HashMap<String, String> |  | A map that contains the logical endpoints of the wire group. Specify
key-value pairs for the map as follows:
   
   - Key: an RFC1035 user-specified label.
   - Value: an Endpoint object. |
| `service_level` | String |  | Service level details determined for the wire group configuration. |
| `topology` | String |  | Topology details for the wire group configuration. |
| `wires` | Vec<String> |  | The single/redundant wire(s) managed by the wire group. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `id` | String |  | [Output Only] The unique identifier for the resource type. The server
generates this identifier. |
| `reconciling` | bool |  | [Output Only] Indicates whether there are wire changes yet to be processed. |
| `wire_properties` | String |  | Properties for all wires in the wire group. |
| `project` | String | ✅ | Project ID for this request. |
| `cross_site_network` | String | ✅ |  |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `admin_enabled` | bool | Indicates whether the wires in the wire group are enabled. When false, the
wires in the wire group are disabled. When true and when
there is simultaneously no wire-specific override of `adminEnabled` to
false, a given wire is enabled. Defaults to true. |
| `name` | String | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `kind` | String | [Output Only] Type of the resource. Alwayscompute#wireGroups for wire groups. |
| `self_link_with_id` | String |  |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `wire_inputs` | HashMap<String, String> | A map that contains optional settings for individual wires.
Specify key-value pairs for the map as follows:
   
   - Key: the label of an existing wire. To view a wire label and its
   format, see the wires[].label field.
   - Value: a WireInputs object. |
| `description` | String | An optional description of the wire group. |
| `wire_group_properties` | String | Properties of the wire group. |
| `endpoints` | HashMap<String, String> | A map that contains the logical endpoints of the wire group. Specify
key-value pairs for the map as follows:
   
   - Key: an RFC1035 user-specified label.
   - Value: an Endpoint object. |
| `service_level` | String | Service level details determined for the wire group configuration. |
| `topology` | String | Topology details for the wire group configuration. |
| `wires` | Vec<String> | The single/redundant wire(s) managed by the wire group. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `id` | String | [Output Only] The unique identifier for the resource type. The server
generates this identifier. |
| `reconciling` | bool | [Output Only] Indicates whether there are wire changes yet to be processed. |
| `wire_properties` | String | Properties for all wires in the wire group. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create wire_group
wire_group = provider.compute_api.Wire_group {
    project = "value"  # Project ID for this request.
    cross_site_network = "value"  # Required field
}

# Access wire_group outputs
wire_group_id = wire_group.id
wire_group_admin_enabled = wire_group.admin_enabled
wire_group_name = wire_group.name
wire_group_kind = wire_group.kind
wire_group_self_link_with_id = wire_group.self_link_with_id
wire_group_self_link = wire_group.self_link
wire_group_wire_inputs = wire_group.wire_inputs
wire_group_description = wire_group.description
wire_group_wire_group_properties = wire_group.wire_group_properties
wire_group_endpoints = wire_group.endpoints
wire_group_service_level = wire_group.service_level
wire_group_topology = wire_group.topology
wire_group_wires = wire_group.wires
wire_group_creation_timestamp = wire_group.creation_timestamp
wire_group_id = wire_group.id
wire_group_reconciling = wire_group.reconciling
wire_group_wire_properties = wire_group.wire_properties
```

---


### Target_http_proxie

Creates a TargetHttpProxy resource in the specified
project using the data included in the request.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `kind` | String |  | [Output Only] Type of resource. Always compute#targetHttpProxy
for target HTTP proxies. |
| `http_keep_alive_timeout_sec` | i64 |  | Specifies how long to keep a connection open, after completing a response,
while there is no matching traffic (in seconds). If an HTTP keep-alive is
not specified, a default value (610 seconds) will be used.

For global external Application Load Balancers, the minimum allowed value
is 5 seconds and the maximum allowed value is 1200 seconds.

For classic Application Load Balancers, this option is not supported. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `fingerprint` | String |  | Fingerprint of this resource. A hash of the contents stored in this object.
This field is used in optimistic locking. This field will be ignored when
inserting a TargetHttpProxy. An up-to-date fingerprint must
be provided in order to patch/update the TargetHttpProxy; otherwise, the
request will fail with error 412 conditionNotMet. To see the
latest fingerprint, make a get() request to retrieve the
TargetHttpProxy. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `name` | String |  | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `proxy_bind` | bool |  | This field only applies when the forwarding rule that references this
target proxy has a loadBalancingScheme set toINTERNAL_SELF_MANAGED.

When this field is set to true, Envoy proxies set up inbound
traffic interception and bind to the IP address and port specified in the
forwarding rule. This is generally useful when using Traffic Director to
configure Envoy as a gateway or middle proxy (in other words, not a
sidecar proxy). The Envoy proxy listens for inbound requests and handles
requests when it receives them.

The default is false. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `http_filters` | Vec<String> |  | URLs to networkservices.HttpFilter resources
enabled for xDS clients using this configuration. For example,https://networkservices.googleapis.com/v1alpha1/projects/project/locations/locationhttpFilters/httpFilter
Only filters that handle outbound connection and
stream events may be specified. These filters work in conjunction with
a default set of HTTP filters that may already be configured by Traffic
Director. Traffic Director will determine the final location of these
filters within xDS configuration based on the name of the HTTP filter. If
Traffic Director positions multiple filters at the same location, those
filters will be in the same order as specified in this list. 

httpFilters only applies for loadbalancers withloadBalancingScheme set to INTERNAL_SELF_MANAGED.
See ForwardingRule for more details. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `self_link_with_id` | String |  | [Output Only] Server-defined URL for this resource with the resource id. |
| `url_map` | String |  | URL to the UrlMap resource that defines the mapping from URL to
the BackendService. |
| `region` | String |  | [Output Only] URL of the region where the regional Target HTTP Proxy
resides. This field is not applicable to global Target HTTP Proxies. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `kind` | String | [Output Only] Type of resource. Always compute#targetHttpProxy
for target HTTP proxies. |
| `http_keep_alive_timeout_sec` | i64 | Specifies how long to keep a connection open, after completing a response,
while there is no matching traffic (in seconds). If an HTTP keep-alive is
not specified, a default value (610 seconds) will be used.

For global external Application Load Balancers, the minimum allowed value
is 5 seconds and the maximum allowed value is 1200 seconds.

For classic Application Load Balancers, this option is not supported. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `fingerprint` | String | Fingerprint of this resource. A hash of the contents stored in this object.
This field is used in optimistic locking. This field will be ignored when
inserting a TargetHttpProxy. An up-to-date fingerprint must
be provided in order to patch/update the TargetHttpProxy; otherwise, the
request will fail with error 412 conditionNotMet. To see the
latest fingerprint, make a get() request to retrieve the
TargetHttpProxy. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `name` | String | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `proxy_bind` | bool | This field only applies when the forwarding rule that references this
target proxy has a loadBalancingScheme set toINTERNAL_SELF_MANAGED.

When this field is set to true, Envoy proxies set up inbound
traffic interception and bind to the IP address and port specified in the
forwarding rule. This is generally useful when using Traffic Director to
configure Envoy as a gateway or middle proxy (in other words, not a
sidecar proxy). The Envoy proxy listens for inbound requests and handles
requests when it receives them.

The default is false. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `http_filters` | Vec<String> | URLs to networkservices.HttpFilter resources
enabled for xDS clients using this configuration. For example,https://networkservices.googleapis.com/v1alpha1/projects/project/locations/locationhttpFilters/httpFilter
Only filters that handle outbound connection and
stream events may be specified. These filters work in conjunction with
a default set of HTTP filters that may already be configured by Traffic
Director. Traffic Director will determine the final location of these
filters within xDS configuration based on the name of the HTTP filter. If
Traffic Director positions multiple filters at the same location, those
filters will be in the same order as specified in this list. 

httpFilters only applies for loadbalancers withloadBalancingScheme set to INTERNAL_SELF_MANAGED.
See ForwardingRule for more details. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `self_link_with_id` | String | [Output Only] Server-defined URL for this resource with the resource id. |
| `url_map` | String | URL to the UrlMap resource that defines the mapping from URL to
the BackendService. |
| `region` | String | [Output Only] URL of the region where the regional Target HTTP Proxy
resides. This field is not applicable to global Target HTTP Proxies. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create target_http_proxie
target_http_proxie = provider.compute_api.Target_http_proxie {
    project = "value"  # Project ID for this request.
}

# Access target_http_proxie outputs
target_http_proxie_id = target_http_proxie.id
target_http_proxie_kind = target_http_proxie.kind
target_http_proxie_http_keep_alive_timeout_sec = target_http_proxie.http_keep_alive_timeout_sec
target_http_proxie_id = target_http_proxie.id
target_http_proxie_fingerprint = target_http_proxie.fingerprint
target_http_proxie_description = target_http_proxie.description
target_http_proxie_name = target_http_proxie.name
target_http_proxie_proxy_bind = target_http_proxie.proxy_bind
target_http_proxie_self_link = target_http_proxie.self_link
target_http_proxie_http_filters = target_http_proxie.http_filters
target_http_proxie_creation_timestamp = target_http_proxie.creation_timestamp
target_http_proxie_self_link_with_id = target_http_proxie.self_link_with_id
target_http_proxie_url_map = target_http_proxie.url_map
target_http_proxie_region = target_http_proxie.region
```

---


### Forwarding_rule

Creates a ForwardingRule resource in the specified project and region using
the data included in the request.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `fingerprint` | String |  | Fingerprint of this resource. A hash of the contents stored in this object.
This field is used in optimistic locking. This field will be ignored when
inserting a ForwardingRule. Include the fingerprint in patch request to
ensure that you do not overwrite changes that were applied from another
concurrent request.

To see the latest fingerprint, make a get() request to
retrieve a ForwardingRule. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `region` | String |  | [Output Only] URL of the region where the regional forwarding rule resides.
This field is not applicable to global forwarding rules.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `allow_psc_packet_injection` | bool |  | This is used in PSC consumer ForwardingRule to control whether the producer
is allowed to inject packets into the consumer's network. If set to true,
the target service attachment must have tunneling enabled and
TunnelingConfig.RoutingMode set to PACKET_INJECTION
Non-PSC forwarding rules should not use this field.

This field was never released to any customers and is deprecated and
will be removed in the future. |
| `name` | String |  | Name of the resource; provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash.

For Private Service Connect forwarding rules that forward traffic to Google
APIs, the forwarding rule name must be a 1-20 characters string with
lowercase letters and numbers and must start with a letter. |
| `network_tier` | String |  | This signifies the networking tier used for configuring
this load balancer and can only take the following values:PREMIUM, STANDARD.

For regional ForwardingRule, the valid values are PREMIUM andSTANDARD. For GlobalForwardingRule, the valid value isPREMIUM.

If this field is not specified, it is assumed to be PREMIUM.
If IPAddress is specified, this value must be equal to the
networkTier of the Address. |
| `psc_connection_status` | String |  |  |
| `subnetwork` | String |  | This field identifies the subnetwork that the load balanced IP should
belong to for this forwarding rule, used with internal load balancers and
external passthrough Network Load Balancers with IPv6.

If the network specified is in auto subnet mode, this field is optional.
However, a subnetwork must be specified if the network is in custom subnet
mode or when creating external forwarding rule with IPv6. |
| `availability_group` | String |  | [Output Only] Specifies the availability group of the forwarding rule. This
field is for use by global external passthrough load balancers (load
balancing scheme EXTERNAL_PASSTHROUGH) and is set for the child forwarding
rules only. |
| `is_mirroring_collector` | bool |  | Indicates whether or not this load balancer can be used as a collector for
packet mirroring. To prevent mirroring loops, instances behind this
load balancer will not have their traffic mirrored even if aPacketMirroring rule applies to them.
This can only be set to true for load balancers that have theirloadBalancingScheme set to INTERNAL. |
| `ip_address` | String |  | IP address for which this forwarding rule accepts traffic. When a client
sends traffic to this IP address, the forwarding rule directs the traffic
to the referenced target or backendService.
While creating a forwarding rule, specifying an IPAddress is
required under the following circumstances:

   
   - When the target is set to targetGrpcProxy andvalidateForProxyless is set to true, theIPAddress should be set to 0.0.0.0.
   - When the target is a Private Service Connect Google APIs
   bundle, you must specify an IPAddress.


Otherwise, you can optionally specify an IP address that references an
existing static (reserved) IP address resource. When omitted, Google Cloud
assigns an ephemeral IP address.

Use one of the following formats to specify an IP address while creating a
forwarding rule:

* IP address number, as in `100.1.2.3`
* IPv6 address range, as in `2600:1234::/96`
* Full resource URL, as inhttps://www.googleapis.com/compute/v1/projects/project_id/regions/region/addresses/address-name
* Partial URL or by name, as in:
   
   - projects/project_id/regions/region/addresses/address-name
   - regions/region/addresses/address-name
   - global/addresses/address-name
   - address-name



The forwarding rule's target or backendService,
and in most cases, also the loadBalancingScheme, determine the
type of IP address that you can use. For detailed information, see
[IP address
specifications](https://cloud.google.com/load-balancing/docs/forwarding-rule-concepts#ip_address_specifications).

When reading an IPAddress, the API always returns the IP
address number. |
| `external_managed_backend_bucket_migration_testing_percentage` | f64 |  | Determines the fraction of requests to backend buckets that should be
processed by the global external Application Load Balancer.

The value of this field must be in the range [0, 100].

This value can only be set if the loadBalancingScheme in the BackendService
is set to EXTERNAL (when using the classic Application Load Balancer) and
the migration state is TEST_BY_PERCENTAGE. |
| `service_directory_registrations` | Vec<String> |  | Service Directory resources to register this forwarding rule with.
Currently, only supports a single Service Directory resource. |
| `ports` | Vec<String> |  | The ports, portRange, and allPorts
fields are mutually exclusive. Only packets addressed to ports in the
specified range will be forwarded to the backends configured with this
forwarding rule.

The ports field has the following limitations:
   
   - It requires that the forwarding rule IPProtocol be TCP,
   UDP, or SCTP, and
   - It's applicable only to the following products: internal passthrough
   Network Load Balancers, backend service-based external passthrough Network
   Load Balancers, and internal protocol forwarding.
   - You can specify a list of up to five ports by number, separated by
   commas. The ports can be contiguous or discontiguous.



For external forwarding rules, two or more forwarding rules cannot use the
same [IPAddress, IPProtocol] pair if they share at least one
port number.

For internal forwarding rules within the same VPC network, two or more
forwarding rules cannot use the same [IPAddress, IPProtocol]
pair if they share at least one port number.

@pattern: \\d+(?:-\\d+)? |
| `label_fingerprint` | String |  | A fingerprint for the labels being applied to this resource, which is
essentially a hash of the labels set used for optimistic locking. The
fingerprint is initially generated by Compute Engine and changes after
every request to modify or update labels. You must always provide an
up-to-date fingerprint hash in order to update or change labels,
otherwise the request will fail with error412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve a ForwardingRule. |
| `child_forwarding_rules` | Vec<String> |  | [Output Only] Applicable only to the parent forwarding rule of global
external passthrough load balancers. This field contains the list of child
forwarding rule URLs associated with the parent forwarding rule: one for
each availability group. AVAILABILITY_GROUP0 will be the first element, and
AVAILABILITY_GROUP1 will be the second element. |
| `external_managed_backend_bucket_migration_state` | String |  | Specifies the canary migration state for the backend buckets attached to
this forwarding rule. Possible values are PREPARE, TEST_BY_PERCENTAGE, and
TEST_ALL_TRAFFIC.

To begin the migration from EXTERNAL to EXTERNAL_MANAGED, the state must be
changed to PREPARE. The state must be changed to TEST_ALL_TRAFFIC before
the loadBalancingScheme can be changed to EXTERNAL_MANAGED. Optionally, the
TEST_BY_PERCENTAGE state can be used to migrate traffic to backend buckets
attached to this forwarding rule by percentage using
externalManagedBackendBucketMigrationTestingPercentage.

Rolling back a migration requires the states to be set in reverse order. So
changing the scheme from EXTERNAL_MANAGED to EXTERNAL requires the state to
be set to TEST_ALL_TRAFFIC at the same time. Optionally, the
TEST_BY_PERCENTAGE state can be used to migrate some traffic back to
EXTERNAL or PREPARE can be used to migrate all traffic back to EXTERNAL. |
| `parent_forwarding_rule` | String |  | [Output Only] Applicable only to the child forwarding rules of global external
passthrough load balancers. This field contains the URL of the parent
forwarding rule. |
| `metadata_filters` | Vec<String> |  | Opaque filter criteria used by load balancer to restrict routing
configuration to a limited set of xDS
compliant clients. In their xDS requests to load balancer, xDS clients
present node
metadata. When there is a match, the relevant configuration
is made available to those proxies. Otherwise, all the resources (e.g.TargetHttpProxy, UrlMap)
referenced by the ForwardingRule are not visible to
those proxies.

For each metadataFilter in this list, if itsfilterMatchCriteria is set to MATCH_ANY, at least one of thefilterLabels must match the corresponding label provided in
the metadata. If its filterMatchCriteria is set to
MATCH_ALL, then all of its filterLabels must match with
corresponding labels provided in the metadata. If multiplemetadataFilters are specified, all of them need to be satisfied
in order to be considered a match.

metadataFilters specified here will be applifed before
those specified in the UrlMap that thisForwardingRule references.

metadataFilters only applies to Loadbalancers that have
their loadBalancingScheme set toINTERNAL_SELF_MANAGED. |
| `source_ip_ranges` | Vec<String> |  | If not empty, this forwarding rule will only forward the traffic when the
source IP address matches one of the IP addresses or CIDR ranges set here.
Note that a forwarding rule can only have up to 64 source IP ranges, and
this field can only be used with a regional forwarding rule whose scheme isEXTERNAL.
Each source_ip_range entry should be either an IP address (for
example, 1.2.3.4) or a CIDR range (for example, 1.2.3.0/24). |
| `kind` | String |  | [Output Only] Type of the resource. Alwayscompute#forwardingRule for forwarding rule resources. |
| `all_ports` | bool |  | The ports, portRange, and allPorts
fields are mutually exclusive. Only packets addressed to ports in the
specified range will be forwarded to the backends configured with this
forwarding rule.

The allPorts field has the following limitations:
   
   - It requires that the forwarding rule IPProtocol be TCP,
   UDP, SCTP, or L3_DEFAULT.
   - It's applicable only to the following products: internal passthrough
   Network Load Balancers, backend service-based external passthrough Network
   Load Balancers, and internal and external protocol forwarding.
   - Set this field to true to allow packets addressed to any port or
   packets lacking destination port information (for example, UDP fragments
   after the first fragment) to be forwarded to the backends configured with
   this forwarding rule. The L3_DEFAULT protocol requiresallPorts be set to true. |
| `ip_version` | String |  | The IP Version that will be used by this forwarding rule.  Valid options
are IPV4 or IPV6. |
| `port_range` | String |  | The ports, portRange, and allPorts
fields are mutually exclusive. Only packets addressed to ports in the
specified range will be forwarded to the backends configured with this
forwarding rule.

The portRange field has the following limitations:
   
   - It requires that the forwarding rule IPProtocol be TCP,
   UDP, or SCTP, and
   - It's applicable only to the following products: external passthrough
   Network Load Balancers, internal and external proxy Network Load Balancers,
   internal and external Application Load Balancers, external protocol
   forwarding, and Classic VPN.
   - Some products have restrictions on what ports can be used. See 
   port specifications for details.



For external forwarding rules, two or more forwarding rules cannot use the
same [IPAddress, IPProtocol] pair, and cannot have overlappingportRanges.

For internal forwarding rules within the same VPC network, two or more
forwarding rules cannot use the same [IPAddress, IPProtocol]
pair, and cannot have overlapping portRanges.

@pattern: \\d+(?:-\\d+)? |
| `allow_psc_global_access` | bool |  | This is used in PSC consumer ForwardingRule to control whether the PSC
endpoint can be accessed from another region. |
| `service_label` | String |  | An optional prefix to the service name for this forwarding rule.
If specified, the prefix is the first label of the fully qualified service
name.

The label must be 1-63 characters long, and comply withRFC1035.
Specifically, the label must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash.

This field is only used for internal load balancing. |
| `allow_global_access` | bool |  | If set to true, clients can access the internal passthrough Network Load
Balancers, the regional internal Application Load Balancer, and the
regional internal proxy Network Load Balancer from all regions.
If false, only allows access from the local region the load balancer is
located at. Note that for INTERNAL_MANAGED forwarding rules, this field
cannot be changed after the forwarding rule is created. |
| `backend_service` | String |  | Identifies the backend service to which the forwarding rule sends traffic.
Required for internal and external passthrough Network Load Balancers;
must be omitted for all other load balancer types. |
| `base_forwarding_rule` | String |  | [Output Only] The URL for the corresponding base forwarding rule. By base
forwarding rule, we mean the forwarding rule that has the same IP address,
protocol, and port settings with the current forwarding rule, but without
sourceIPRanges specified.
Always empty if the current forwarding rule does not have sourceIPRanges
specified. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `psc_connection_id` | String |  | [Output Only] The PSC connection id of the PSC forwarding rule. |
| `load_balancing_scheme` | String |  | Specifies the forwarding rule type.

For more information about forwarding rules, refer to
Forwarding rule concepts. |
| `labels` | HashMap<String, String> |  | Labels for this resource. These can only be added or modified by thesetLabels method. Each label key/value pair must comply withRFC1035.
Label values may be empty. |
| `network` | String |  | This field is not used for global external load balancing.

For internal passthrough Network Load Balancers, this field identifies the
network that the load balanced IP should belong to for this forwarding
rule.
If the subnetwork is specified, the network of the subnetwork will be used.
If neither subnetwork nor this field is specified, the default network will
be used.

For Private Service Connect forwarding rules that forward traffic to Google
APIs, a network must be provided. |
| `self_link_with_id` | String |  | [Output Only] Server-defined URL for this resource with the resource id. |
| `ip_protocol` | String |  | The IP protocol to which this rule applies.

For protocol forwarding, valid
options are TCP, UDP, ESP,AH, SCTP, ICMP andL3_DEFAULT.

The valid IP protocols are different for different load balancing products
as described in [Load balancing
features](https://cloud.google.com/load-balancing/docs/features#protocols_from_the_load_balancer_to_the_backends). |
| `no_automate_dns_zone` | bool |  | This is used in PSC consumer ForwardingRule to control whether it should
try to auto-generate a DNS zone or not. Non-PSC forwarding rules do not use
this field. Once set, this field is not mutable. |
| `service_name` | String |  | [Output Only]
The internal fully qualified service name for this forwarding rule.

This field is only used for internal load balancing. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `target` | String |  | The URL of the target resource to receive the matched traffic.  For
regional forwarding rules, this target must be in the same region as the
forwarding rule. For global forwarding rules, this target must be a global
load balancing resource.

The forwarded traffic must be of a type appropriate to the target object.
   
   
     -  For load balancers, see the "Target" column in [Port specifications](https://cloud.google.com/load-balancing/docs/forwarding-rule-concepts#ip_address_specifications).
     -  For Private Service Connect forwarding rules that forward traffic to Google APIs, provide the name of a supported Google API bundle:
       
      
            -  vpc-sc -  APIs that support VPC Service Controls. 
            -  all-apis - All supported Google APIs. 
          
     
     -  For Private Service Connect forwarding rules that forward traffic to managed services, the target must be a service attachment. The target is not mutable once set as a service attachment. |
| `ip_collection` | String |  | Resource reference of a PublicDelegatedPrefix. The PDP must
be a sub-PDP in EXTERNAL_IPV6_FORWARDING_RULE_CREATION mode.

Use one of the following formats to specify a sub-PDP when creating an IPv6
NetLB forwarding rule using BYOIP:
Full resource URL, as inhttps://www.googleapis.com/compute/v1/projects/project_id/regions/region/publicDelegatedPrefixes/sub-pdp-name
Partial URL, as in:
   
   - projects/project_id/regions/region/publicDelegatedPrefixes/sub-pdp-name
   - regions/region/publicDelegatedPrefixes/sub-pdp-name |
| `project` | String | ✅ | Project ID for this request. |
| `region` | String | ✅ | Name of the region scoping this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `fingerprint` | String | Fingerprint of this resource. A hash of the contents stored in this object.
This field is used in optimistic locking. This field will be ignored when
inserting a ForwardingRule. Include the fingerprint in patch request to
ensure that you do not overwrite changes that were applied from another
concurrent request.

To see the latest fingerprint, make a get() request to
retrieve a ForwardingRule. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `region` | String | [Output Only] URL of the region where the regional forwarding rule resides.
This field is not applicable to global forwarding rules.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `allow_psc_packet_injection` | bool | This is used in PSC consumer ForwardingRule to control whether the producer
is allowed to inject packets into the consumer's network. If set to true,
the target service attachment must have tunneling enabled and
TunnelingConfig.RoutingMode set to PACKET_INJECTION
Non-PSC forwarding rules should not use this field.

This field was never released to any customers and is deprecated and
will be removed in the future. |
| `name` | String | Name of the resource; provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash.

For Private Service Connect forwarding rules that forward traffic to Google
APIs, the forwarding rule name must be a 1-20 characters string with
lowercase letters and numbers and must start with a letter. |
| `network_tier` | String | This signifies the networking tier used for configuring
this load balancer and can only take the following values:PREMIUM, STANDARD.

For regional ForwardingRule, the valid values are PREMIUM andSTANDARD. For GlobalForwardingRule, the valid value isPREMIUM.

If this field is not specified, it is assumed to be PREMIUM.
If IPAddress is specified, this value must be equal to the
networkTier of the Address. |
| `psc_connection_status` | String |  |
| `subnetwork` | String | This field identifies the subnetwork that the load balanced IP should
belong to for this forwarding rule, used with internal load balancers and
external passthrough Network Load Balancers with IPv6.

If the network specified is in auto subnet mode, this field is optional.
However, a subnetwork must be specified if the network is in custom subnet
mode or when creating external forwarding rule with IPv6. |
| `availability_group` | String | [Output Only] Specifies the availability group of the forwarding rule. This
field is for use by global external passthrough load balancers (load
balancing scheme EXTERNAL_PASSTHROUGH) and is set for the child forwarding
rules only. |
| `is_mirroring_collector` | bool | Indicates whether or not this load balancer can be used as a collector for
packet mirroring. To prevent mirroring loops, instances behind this
load balancer will not have their traffic mirrored even if aPacketMirroring rule applies to them.
This can only be set to true for load balancers that have theirloadBalancingScheme set to INTERNAL. |
| `ip_address` | String | IP address for which this forwarding rule accepts traffic. When a client
sends traffic to this IP address, the forwarding rule directs the traffic
to the referenced target or backendService.
While creating a forwarding rule, specifying an IPAddress is
required under the following circumstances:

   
   - When the target is set to targetGrpcProxy andvalidateForProxyless is set to true, theIPAddress should be set to 0.0.0.0.
   - When the target is a Private Service Connect Google APIs
   bundle, you must specify an IPAddress.


Otherwise, you can optionally specify an IP address that references an
existing static (reserved) IP address resource. When omitted, Google Cloud
assigns an ephemeral IP address.

Use one of the following formats to specify an IP address while creating a
forwarding rule:

* IP address number, as in `100.1.2.3`
* IPv6 address range, as in `2600:1234::/96`
* Full resource URL, as inhttps://www.googleapis.com/compute/v1/projects/project_id/regions/region/addresses/address-name
* Partial URL or by name, as in:
   
   - projects/project_id/regions/region/addresses/address-name
   - regions/region/addresses/address-name
   - global/addresses/address-name
   - address-name



The forwarding rule's target or backendService,
and in most cases, also the loadBalancingScheme, determine the
type of IP address that you can use. For detailed information, see
[IP address
specifications](https://cloud.google.com/load-balancing/docs/forwarding-rule-concepts#ip_address_specifications).

When reading an IPAddress, the API always returns the IP
address number. |
| `external_managed_backend_bucket_migration_testing_percentage` | f64 | Determines the fraction of requests to backend buckets that should be
processed by the global external Application Load Balancer.

The value of this field must be in the range [0, 100].

This value can only be set if the loadBalancingScheme in the BackendService
is set to EXTERNAL (when using the classic Application Load Balancer) and
the migration state is TEST_BY_PERCENTAGE. |
| `service_directory_registrations` | Vec<String> | Service Directory resources to register this forwarding rule with.
Currently, only supports a single Service Directory resource. |
| `ports` | Vec<String> | The ports, portRange, and allPorts
fields are mutually exclusive. Only packets addressed to ports in the
specified range will be forwarded to the backends configured with this
forwarding rule.

The ports field has the following limitations:
   
   - It requires that the forwarding rule IPProtocol be TCP,
   UDP, or SCTP, and
   - It's applicable only to the following products: internal passthrough
   Network Load Balancers, backend service-based external passthrough Network
   Load Balancers, and internal protocol forwarding.
   - You can specify a list of up to five ports by number, separated by
   commas. The ports can be contiguous or discontiguous.



For external forwarding rules, two or more forwarding rules cannot use the
same [IPAddress, IPProtocol] pair if they share at least one
port number.

For internal forwarding rules within the same VPC network, two or more
forwarding rules cannot use the same [IPAddress, IPProtocol]
pair if they share at least one port number.

@pattern: \\d+(?:-\\d+)? |
| `label_fingerprint` | String | A fingerprint for the labels being applied to this resource, which is
essentially a hash of the labels set used for optimistic locking. The
fingerprint is initially generated by Compute Engine and changes after
every request to modify or update labels. You must always provide an
up-to-date fingerprint hash in order to update or change labels,
otherwise the request will fail with error412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve a ForwardingRule. |
| `child_forwarding_rules` | Vec<String> | [Output Only] Applicable only to the parent forwarding rule of global
external passthrough load balancers. This field contains the list of child
forwarding rule URLs associated with the parent forwarding rule: one for
each availability group. AVAILABILITY_GROUP0 will be the first element, and
AVAILABILITY_GROUP1 will be the second element. |
| `external_managed_backend_bucket_migration_state` | String | Specifies the canary migration state for the backend buckets attached to
this forwarding rule. Possible values are PREPARE, TEST_BY_PERCENTAGE, and
TEST_ALL_TRAFFIC.

To begin the migration from EXTERNAL to EXTERNAL_MANAGED, the state must be
changed to PREPARE. The state must be changed to TEST_ALL_TRAFFIC before
the loadBalancingScheme can be changed to EXTERNAL_MANAGED. Optionally, the
TEST_BY_PERCENTAGE state can be used to migrate traffic to backend buckets
attached to this forwarding rule by percentage using
externalManagedBackendBucketMigrationTestingPercentage.

Rolling back a migration requires the states to be set in reverse order. So
changing the scheme from EXTERNAL_MANAGED to EXTERNAL requires the state to
be set to TEST_ALL_TRAFFIC at the same time. Optionally, the
TEST_BY_PERCENTAGE state can be used to migrate some traffic back to
EXTERNAL or PREPARE can be used to migrate all traffic back to EXTERNAL. |
| `parent_forwarding_rule` | String | [Output Only] Applicable only to the child forwarding rules of global external
passthrough load balancers. This field contains the URL of the parent
forwarding rule. |
| `metadata_filters` | Vec<String> | Opaque filter criteria used by load balancer to restrict routing
configuration to a limited set of xDS
compliant clients. In their xDS requests to load balancer, xDS clients
present node
metadata. When there is a match, the relevant configuration
is made available to those proxies. Otherwise, all the resources (e.g.TargetHttpProxy, UrlMap)
referenced by the ForwardingRule are not visible to
those proxies.

For each metadataFilter in this list, if itsfilterMatchCriteria is set to MATCH_ANY, at least one of thefilterLabels must match the corresponding label provided in
the metadata. If its filterMatchCriteria is set to
MATCH_ALL, then all of its filterLabels must match with
corresponding labels provided in the metadata. If multiplemetadataFilters are specified, all of them need to be satisfied
in order to be considered a match.

metadataFilters specified here will be applifed before
those specified in the UrlMap that thisForwardingRule references.

metadataFilters only applies to Loadbalancers that have
their loadBalancingScheme set toINTERNAL_SELF_MANAGED. |
| `source_ip_ranges` | Vec<String> | If not empty, this forwarding rule will only forward the traffic when the
source IP address matches one of the IP addresses or CIDR ranges set here.
Note that a forwarding rule can only have up to 64 source IP ranges, and
this field can only be used with a regional forwarding rule whose scheme isEXTERNAL.
Each source_ip_range entry should be either an IP address (for
example, 1.2.3.4) or a CIDR range (for example, 1.2.3.0/24). |
| `kind` | String | [Output Only] Type of the resource. Alwayscompute#forwardingRule for forwarding rule resources. |
| `all_ports` | bool | The ports, portRange, and allPorts
fields are mutually exclusive. Only packets addressed to ports in the
specified range will be forwarded to the backends configured with this
forwarding rule.

The allPorts field has the following limitations:
   
   - It requires that the forwarding rule IPProtocol be TCP,
   UDP, SCTP, or L3_DEFAULT.
   - It's applicable only to the following products: internal passthrough
   Network Load Balancers, backend service-based external passthrough Network
   Load Balancers, and internal and external protocol forwarding.
   - Set this field to true to allow packets addressed to any port or
   packets lacking destination port information (for example, UDP fragments
   after the first fragment) to be forwarded to the backends configured with
   this forwarding rule. The L3_DEFAULT protocol requiresallPorts be set to true. |
| `ip_version` | String | The IP Version that will be used by this forwarding rule.  Valid options
are IPV4 or IPV6. |
| `port_range` | String | The ports, portRange, and allPorts
fields are mutually exclusive. Only packets addressed to ports in the
specified range will be forwarded to the backends configured with this
forwarding rule.

The portRange field has the following limitations:
   
   - It requires that the forwarding rule IPProtocol be TCP,
   UDP, or SCTP, and
   - It's applicable only to the following products: external passthrough
   Network Load Balancers, internal and external proxy Network Load Balancers,
   internal and external Application Load Balancers, external protocol
   forwarding, and Classic VPN.
   - Some products have restrictions on what ports can be used. See 
   port specifications for details.



For external forwarding rules, two or more forwarding rules cannot use the
same [IPAddress, IPProtocol] pair, and cannot have overlappingportRanges.

For internal forwarding rules within the same VPC network, two or more
forwarding rules cannot use the same [IPAddress, IPProtocol]
pair, and cannot have overlapping portRanges.

@pattern: \\d+(?:-\\d+)? |
| `allow_psc_global_access` | bool | This is used in PSC consumer ForwardingRule to control whether the PSC
endpoint can be accessed from another region. |
| `service_label` | String | An optional prefix to the service name for this forwarding rule.
If specified, the prefix is the first label of the fully qualified service
name.

The label must be 1-63 characters long, and comply withRFC1035.
Specifically, the label must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash.

This field is only used for internal load balancing. |
| `allow_global_access` | bool | If set to true, clients can access the internal passthrough Network Load
Balancers, the regional internal Application Load Balancer, and the
regional internal proxy Network Load Balancer from all regions.
If false, only allows access from the local region the load balancer is
located at. Note that for INTERNAL_MANAGED forwarding rules, this field
cannot be changed after the forwarding rule is created. |
| `backend_service` | String | Identifies the backend service to which the forwarding rule sends traffic.
Required for internal and external passthrough Network Load Balancers;
must be omitted for all other load balancer types. |
| `base_forwarding_rule` | String | [Output Only] The URL for the corresponding base forwarding rule. By base
forwarding rule, we mean the forwarding rule that has the same IP address,
protocol, and port settings with the current forwarding rule, but without
sourceIPRanges specified.
Always empty if the current forwarding rule does not have sourceIPRanges
specified. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `psc_connection_id` | String | [Output Only] The PSC connection id of the PSC forwarding rule. |
| `load_balancing_scheme` | String | Specifies the forwarding rule type.

For more information about forwarding rules, refer to
Forwarding rule concepts. |
| `labels` | HashMap<String, String> | Labels for this resource. These can only be added or modified by thesetLabels method. Each label key/value pair must comply withRFC1035.
Label values may be empty. |
| `network` | String | This field is not used for global external load balancing.

For internal passthrough Network Load Balancers, this field identifies the
network that the load balanced IP should belong to for this forwarding
rule.
If the subnetwork is specified, the network of the subnetwork will be used.
If neither subnetwork nor this field is specified, the default network will
be used.

For Private Service Connect forwarding rules that forward traffic to Google
APIs, a network must be provided. |
| `self_link_with_id` | String | [Output Only] Server-defined URL for this resource with the resource id. |
| `ip_protocol` | String | The IP protocol to which this rule applies.

For protocol forwarding, valid
options are TCP, UDP, ESP,AH, SCTP, ICMP andL3_DEFAULT.

The valid IP protocols are different for different load balancing products
as described in [Load balancing
features](https://cloud.google.com/load-balancing/docs/features#protocols_from_the_load_balancer_to_the_backends). |
| `no_automate_dns_zone` | bool | This is used in PSC consumer ForwardingRule to control whether it should
try to auto-generate a DNS zone or not. Non-PSC forwarding rules do not use
this field. Once set, this field is not mutable. |
| `service_name` | String | [Output Only]
The internal fully qualified service name for this forwarding rule.

This field is only used for internal load balancing. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `target` | String | The URL of the target resource to receive the matched traffic.  For
regional forwarding rules, this target must be in the same region as the
forwarding rule. For global forwarding rules, this target must be a global
load balancing resource.

The forwarded traffic must be of a type appropriate to the target object.
   
   
     -  For load balancers, see the "Target" column in [Port specifications](https://cloud.google.com/load-balancing/docs/forwarding-rule-concepts#ip_address_specifications).
     -  For Private Service Connect forwarding rules that forward traffic to Google APIs, provide the name of a supported Google API bundle:
       
      
            -  vpc-sc -  APIs that support VPC Service Controls. 
            -  all-apis - All supported Google APIs. 
          
     
     -  For Private Service Connect forwarding rules that forward traffic to managed services, the target must be a service attachment. The target is not mutable once set as a service attachment. |
| `ip_collection` | String | Resource reference of a PublicDelegatedPrefix. The PDP must
be a sub-PDP in EXTERNAL_IPV6_FORWARDING_RULE_CREATION mode.

Use one of the following formats to specify a sub-PDP when creating an IPv6
NetLB forwarding rule using BYOIP:
Full resource URL, as inhttps://www.googleapis.com/compute/v1/projects/project_id/regions/region/publicDelegatedPrefixes/sub-pdp-name
Partial URL, as in:
   
   - projects/project_id/regions/region/publicDelegatedPrefixes/sub-pdp-name
   - regions/region/publicDelegatedPrefixes/sub-pdp-name |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create forwarding_rule
forwarding_rule = provider.compute_api.Forwarding_rule {
    project = "value"  # Project ID for this request.
    region = "value"  # Name of the region scoping this request.
}

# Access forwarding_rule outputs
forwarding_rule_id = forwarding_rule.id
forwarding_rule_fingerprint = forwarding_rule.fingerprint
forwarding_rule_id = forwarding_rule.id
forwarding_rule_region = forwarding_rule.region
forwarding_rule_self_link = forwarding_rule.self_link
forwarding_rule_allow_psc_packet_injection = forwarding_rule.allow_psc_packet_injection
forwarding_rule_name = forwarding_rule.name
forwarding_rule_network_tier = forwarding_rule.network_tier
forwarding_rule_psc_connection_status = forwarding_rule.psc_connection_status
forwarding_rule_subnetwork = forwarding_rule.subnetwork
forwarding_rule_availability_group = forwarding_rule.availability_group
forwarding_rule_is_mirroring_collector = forwarding_rule.is_mirroring_collector
forwarding_rule_ip_address = forwarding_rule.ip_address
forwarding_rule_external_managed_backend_bucket_migration_testing_percentage = forwarding_rule.external_managed_backend_bucket_migration_testing_percentage
forwarding_rule_service_directory_registrations = forwarding_rule.service_directory_registrations
forwarding_rule_ports = forwarding_rule.ports
forwarding_rule_label_fingerprint = forwarding_rule.label_fingerprint
forwarding_rule_child_forwarding_rules = forwarding_rule.child_forwarding_rules
forwarding_rule_external_managed_backend_bucket_migration_state = forwarding_rule.external_managed_backend_bucket_migration_state
forwarding_rule_parent_forwarding_rule = forwarding_rule.parent_forwarding_rule
forwarding_rule_metadata_filters = forwarding_rule.metadata_filters
forwarding_rule_source_ip_ranges = forwarding_rule.source_ip_ranges
forwarding_rule_kind = forwarding_rule.kind
forwarding_rule_all_ports = forwarding_rule.all_ports
forwarding_rule_ip_version = forwarding_rule.ip_version
forwarding_rule_port_range = forwarding_rule.port_range
forwarding_rule_allow_psc_global_access = forwarding_rule.allow_psc_global_access
forwarding_rule_service_label = forwarding_rule.service_label
forwarding_rule_allow_global_access = forwarding_rule.allow_global_access
forwarding_rule_backend_service = forwarding_rule.backend_service
forwarding_rule_base_forwarding_rule = forwarding_rule.base_forwarding_rule
forwarding_rule_description = forwarding_rule.description
forwarding_rule_psc_connection_id = forwarding_rule.psc_connection_id
forwarding_rule_load_balancing_scheme = forwarding_rule.load_balancing_scheme
forwarding_rule_labels = forwarding_rule.labels
forwarding_rule_network = forwarding_rule.network
forwarding_rule_self_link_with_id = forwarding_rule.self_link_with_id
forwarding_rule_ip_protocol = forwarding_rule.ip_protocol
forwarding_rule_no_automate_dns_zone = forwarding_rule.no_automate_dns_zone
forwarding_rule_service_name = forwarding_rule.service_name
forwarding_rule_creation_timestamp = forwarding_rule.creation_timestamp
forwarding_rule_target = forwarding_rule.target
forwarding_rule_ip_collection = forwarding_rule.ip_collection
```

---


### Public_advertised_prefixe

Creates a PublicAdvertisedPrefix in the specified project
using the parameters that are included in the request.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `fingerprint` | String |  | Fingerprint of this resource. A hash of the contents stored in this object.
This field is used in optimistic locking. This field will be ignored when
inserting a new PublicAdvertisedPrefix. An up-to-date
fingerprint must be provided in order to update thePublicAdvertisedPrefix, otherwise the request will fail with
error 412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve a PublicAdvertisedPrefix. |
| `ipv6_access_type` | String |  | The internet access type for IPv6 Public Advertised Prefixes. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `self_link_with_id` | String |  | [Output Only] Server-defined URL with id for the resource. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `public_delegated_prefixs` | Vec<String> |  | [Output Only] The list of public delegated prefixes that exist for this
public advertised prefix. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `kind` | String |  | [Output Only] Type of the resource. Alwayscompute#publicAdvertisedPrefix for public advertised prefixes. |
| `shared_secret` | String |  | [Output Only] The shared secret to be used for reverse DNS verification. |
| `ip_cidr_range` | String |  | The address range, in CIDR format, represented by this public advertised
prefix. |
| `status` | String |  | The status of the public advertised prefix. Possible values include:
   
   
     - `INITIAL`: RPKI validation is complete.
     - `PTR_CONFIGURED`: User has configured the PTR.
     - `VALIDATED`: Reverse DNS lookup is successful.
     - `REVERSE_DNS_LOOKUP_FAILED`: Reverse DNS lookup failed.
     - `PREFIX_CONFIGURATION_IN_PROGRESS`: The prefix is being
     configured.
     - `PREFIX_CONFIGURATION_COMPLETE`: The prefix is fully configured.
     - `PREFIX_REMOVAL_IN_PROGRESS`: The prefix is being removed. |
| `byoip_api_version` | String |  | [Output Only] The version of BYOIP API. |
| `pdp_scope` | String |  | Specifies how child public delegated prefix will be scoped. It could
be one of following values:
   
   
     - `REGIONAL`: The public delegated prefix is regional only. The
     provisioning will take a few minutes.
     - `GLOBAL`: The public delegated prefix is global only. The
     provisioning will take ~4 weeks.
     - `GLOBAL_AND_REGIONAL` [output only]: The public delegated prefixes is
      BYOIP V1 legacy prefix. This is output only value and no longer
      supported in BYOIP V2. |
| `dns_verification_ip` | String |  | The address to be used for reverse DNS verification. |
| `id` | String |  | [Output Only] The unique identifier for the resource type. The server
generates this identifier. |
| `name` | String |  | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `fingerprint` | String | Fingerprint of this resource. A hash of the contents stored in this object.
This field is used in optimistic locking. This field will be ignored when
inserting a new PublicAdvertisedPrefix. An up-to-date
fingerprint must be provided in order to update thePublicAdvertisedPrefix, otherwise the request will fail with
error 412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve a PublicAdvertisedPrefix. |
| `ipv6_access_type` | String | The internet access type for IPv6 Public Advertised Prefixes. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `self_link_with_id` | String | [Output Only] Server-defined URL with id for the resource. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `public_delegated_prefixs` | Vec<String> | [Output Only] The list of public delegated prefixes that exist for this
public advertised prefix. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `kind` | String | [Output Only] Type of the resource. Alwayscompute#publicAdvertisedPrefix for public advertised prefixes. |
| `shared_secret` | String | [Output Only] The shared secret to be used for reverse DNS verification. |
| `ip_cidr_range` | String | The address range, in CIDR format, represented by this public advertised
prefix. |
| `status` | String | The status of the public advertised prefix. Possible values include:
   
   
     - `INITIAL`: RPKI validation is complete.
     - `PTR_CONFIGURED`: User has configured the PTR.
     - `VALIDATED`: Reverse DNS lookup is successful.
     - `REVERSE_DNS_LOOKUP_FAILED`: Reverse DNS lookup failed.
     - `PREFIX_CONFIGURATION_IN_PROGRESS`: The prefix is being
     configured.
     - `PREFIX_CONFIGURATION_COMPLETE`: The prefix is fully configured.
     - `PREFIX_REMOVAL_IN_PROGRESS`: The prefix is being removed. |
| `byoip_api_version` | String | [Output Only] The version of BYOIP API. |
| `pdp_scope` | String | Specifies how child public delegated prefix will be scoped. It could
be one of following values:
   
   
     - `REGIONAL`: The public delegated prefix is regional only. The
     provisioning will take a few minutes.
     - `GLOBAL`: The public delegated prefix is global only. The
     provisioning will take ~4 weeks.
     - `GLOBAL_AND_REGIONAL` [output only]: The public delegated prefixes is
      BYOIP V1 legacy prefix. This is output only value and no longer
      supported in BYOIP V2. |
| `dns_verification_ip` | String | The address to be used for reverse DNS verification. |
| `id` | String | [Output Only] The unique identifier for the resource type. The server
generates this identifier. |
| `name` | String | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create public_advertised_prefixe
public_advertised_prefixe = provider.compute_api.Public_advertised_prefixe {
    project = "value"  # Project ID for this request.
}

# Access public_advertised_prefixe outputs
public_advertised_prefixe_id = public_advertised_prefixe.id
public_advertised_prefixe_fingerprint = public_advertised_prefixe.fingerprint
public_advertised_prefixe_ipv6_access_type = public_advertised_prefixe.ipv6_access_type
public_advertised_prefixe_creation_timestamp = public_advertised_prefixe.creation_timestamp
public_advertised_prefixe_self_link_with_id = public_advertised_prefixe.self_link_with_id
public_advertised_prefixe_description = public_advertised_prefixe.description
public_advertised_prefixe_public_delegated_prefixs = public_advertised_prefixe.public_delegated_prefixs
public_advertised_prefixe_self_link = public_advertised_prefixe.self_link
public_advertised_prefixe_kind = public_advertised_prefixe.kind
public_advertised_prefixe_shared_secret = public_advertised_prefixe.shared_secret
public_advertised_prefixe_ip_cidr_range = public_advertised_prefixe.ip_cidr_range
public_advertised_prefixe_status = public_advertised_prefixe.status
public_advertised_prefixe_byoip_api_version = public_advertised_prefixe.byoip_api_version
public_advertised_prefixe_pdp_scope = public_advertised_prefixe.pdp_scope
public_advertised_prefixe_dns_verification_ip = public_advertised_prefixe.dns_verification_ip
public_advertised_prefixe_id = public_advertised_prefixe.id
public_advertised_prefixe_name = public_advertised_prefixe.name
```

---


### Ha_controller

Creates HaController in the specified project.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `secondary_zone_capacity` | String |  | Indicates the capacity guarantees in the secondary zone. |
| `backend_services` | Vec<String> |  | Advanced configuration option. If specified, these Backend Services need to
be pre-created and configured as managed. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp in RFC3339
text format. |
| `name` | String |  | Name of the resource.
Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first character
must be a lowercase letter, and all following characters must be a dash,
lowercase letter, or digit, except the last character, which cannot be a
dash. |
| `instance_name` | String |  | Name of the instance that HaController is in charge of.
If not specified the HaController's resource name will be used instead.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first character
must be a lowercase letter, and all following characters must be a dash,
lowercase letter, or digit, except the last character, which cannot be a
dash. |
| `region` | String |  | [Output Only] URL of the region where the resource resides.
You must specify this field as part of the HTTP request URL.
It is not settable as a field in the request body. |
| `kind` | String |  | [Output Only] Type of the resource. Alwayscompute#haController for HaControllers. |
| `self_link` | String |  | [Output only] Server-defined URL for the resource. |
| `failover_initiation` | String |  | Indicates how failover should be initiated. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `self_link_with_id` | String |  | [Output Only] Server-defined URL for this resource with the resource id. |
| `status` | String |  | [Output Only] Status information for the HaController resource. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `networking_auto_configuration` | String |  | Basic networking configuration. Required backend services and forwarding
rules will be automatically created with default parameters. |
| `zone_configurations` | HashMap<String, String> |  | Map of zone configurations
Key: name of the zone
Value: ZoneConfiguration |
| `project` | String | ✅ | Project ID for this request. |
| `region` | String | ✅ | Name of the region for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `secondary_zone_capacity` | String | Indicates the capacity guarantees in the secondary zone. |
| `backend_services` | Vec<String> | Advanced configuration option. If specified, these Backend Services need to
be pre-created and configured as managed. |
| `creation_timestamp` | String | [Output Only] Creation timestamp in RFC3339
text format. |
| `name` | String | Name of the resource.
Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first character
must be a lowercase letter, and all following characters must be a dash,
lowercase letter, or digit, except the last character, which cannot be a
dash. |
| `instance_name` | String | Name of the instance that HaController is in charge of.
If not specified the HaController's resource name will be used instead.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first character
must be a lowercase letter, and all following characters must be a dash,
lowercase letter, or digit, except the last character, which cannot be a
dash. |
| `region` | String | [Output Only] URL of the region where the resource resides.
You must specify this field as part of the HTTP request URL.
It is not settable as a field in the request body. |
| `kind` | String | [Output Only] Type of the resource. Alwayscompute#haController for HaControllers. |
| `self_link` | String | [Output only] Server-defined URL for the resource. |
| `failover_initiation` | String | Indicates how failover should be initiated. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `self_link_with_id` | String | [Output Only] Server-defined URL for this resource with the resource id. |
| `status` | String | [Output Only] Status information for the HaController resource. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `networking_auto_configuration` | String | Basic networking configuration. Required backend services and forwarding
rules will be automatically created with default parameters. |
| `zone_configurations` | HashMap<String, String> | Map of zone configurations
Key: name of the zone
Value: ZoneConfiguration |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create ha_controller
ha_controller = provider.compute_api.Ha_controller {
    project = "value"  # Project ID for this request.
    region = "value"  # Name of the region for this request.
}

# Access ha_controller outputs
ha_controller_id = ha_controller.id
ha_controller_secondary_zone_capacity = ha_controller.secondary_zone_capacity
ha_controller_backend_services = ha_controller.backend_services
ha_controller_creation_timestamp = ha_controller.creation_timestamp
ha_controller_name = ha_controller.name
ha_controller_instance_name = ha_controller.instance_name
ha_controller_region = ha_controller.region
ha_controller_kind = ha_controller.kind
ha_controller_self_link = ha_controller.self_link
ha_controller_failover_initiation = ha_controller.failover_initiation
ha_controller_description = ha_controller.description
ha_controller_self_link_with_id = ha_controller.self_link_with_id
ha_controller_status = ha_controller.status
ha_controller_id = ha_controller.id
ha_controller_networking_auto_configuration = ha_controller.networking_auto_configuration
ha_controller_zone_configurations = ha_controller.zone_configurations
```

---


### Region_target_https_proxie

Creates a TargetHttpsProxy resource in the specified project and region
using the data included in the request.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `certificate_map` | String |  | URL of a certificate map that identifies a certificate map associated with
the given target proxy.
This field can only be set for Global external Application Load Balancer or
Classic Application Load Balancer. For other products use Certificate
Manager Certificates instead.

If set, sslCertificates will be ignored.

 Accepted format is//certificatemanager.googleapis.com/projects/{project}/locations/{location}/certificateMaps/{resourceName}. |
| `server_tls_policy` | String |  | Optional. A URL referring to a networksecurity.ServerTlsPolicy resource
that describes how the proxy should authenticate inbound traffic.

 serverTlsPolicy only applies to a globalTargetHttpsProxy attached toglobalForwardingRules with theloadBalancingScheme set to INTERNAL_SELF_MANAGED
or EXTERNAL orEXTERNAL_MANAGED or INTERNAL_MANAGED.
It also applies to a regional TargetHttpsProxy attached to
regional forwardingRules with theloadBalancingScheme set to EXTERNAL_MANAGED orINTERNAL_MANAGED. For details whichServerTlsPolicy resources are accepted withINTERNAL_SELF_MANAGED and which with EXTERNAL,INTERNAL_MANAGED, EXTERNAL_MANAGEDloadBalancingScheme consult ServerTlsPolicy
documentation.

  If left blank, communications are not encrypted. |
| `ssl_certificates` | Vec<String> |  | URLs to SslCertificate resources that are used to authenticate
connections between users and the load balancer. At least one SSL
certificate must be specified. SslCertificates do not apply when the load
balancing scheme is set to INTERNAL_SELF_MANAGED.

The URLs should refer to a SSL Certificate resource or Certificate Manager
Certificate resource. Mixing Classic Certificates and Certificate Manager
Certificates is not allowed. Certificate Manager Certificates must include
the certificatemanager API namespace. Using Certificate Manager
Certificates in this field is not supported by Global external Application
Load Balancer or Classic Application Load Balancer, use certificate_map
instead.

Currently, you may specify up to 15 Classic SSL Certificates or up to 100
Certificate Manager Certificates.

Certificate Manager Certificates accepted formats are:
   
   - //certificatemanager.googleapis.com/projects/{project}/locations/{location}/certificates/{resourceName}.
   - https://certificatemanager.googleapis.com/v1alpha1/projects/{project}/locations/{location}/certificates/{resourceName}. |
| `authentication` | String |  | [Deprecated] Use serverTlsPolicy instead. |
| `authorization` | String |  | [Deprecated] Use authorizationPolicy instead. |
| `http_filters` | Vec<String> |  | URLs to networkservices.HttpFilter resources
enabled for xDS clients using this configuration. For example,https://networkservices.googleapis.com/beta/projects/project/locations/location/httpFilters/httpFilter
Only filters that handle outbound connection and
stream events may be specified. These filters work in conjunction with
a default set of HTTP filters that may already be configured by Traffic
Director. Traffic Director will determine the final location of these
filters within xDS configuration based on the name of the HTTP filter. If
Traffic Director positions multiple filters at the same location, those
filters will be in the same order as specified in this list. 

httpFilters only applies for loadbalancers withloadBalancingScheme set to INTERNAL_SELF_MANAGED.
See ForwardingRule for more details. |
| `http_keep_alive_timeout_sec` | i64 |  | Specifies how long to keep a connection open, after completing a response,
while there is no matching traffic (in seconds). If an HTTP keep-alive is
not specified, a default value (610 seconds) will be used.

For global external Application Load Balancers, the minimum allowed value
is 5 seconds and the maximum allowed value is 1200 seconds.

For classic Application Load Balancers, this option is not supported. |
| `kind` | String |  | [Output Only] Type of resource. Alwayscompute#targetHttpsProxy for target HTTPS proxies. |
| `ssl_policy` | String |  | URL of SslPolicy resource that will be associated with the TargetHttpsProxy
resource. If not set, the TargetHttpsProxy resource has no
SSL policy configured. |
| `fingerprint` | String |  | Fingerprint of this resource. A hash of the contents stored in this object.
This field is used in optimistic locking. This field will be ignored when
inserting a TargetHttpsProxy. An up-to-date fingerprint must
be provided in order to patch the TargetHttpsProxy; otherwise, the request
will fail with error 412 conditionNotMet. To see the latest
fingerprint, make a get() request to retrieve the
TargetHttpsProxy. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `authorization_policy` | String |  | Optional. A URL referring to a networksecurity.AuthorizationPolicy resource
that describes how the proxy should authorize inbound traffic. If left
blank, access will not be restricted by an authorization policy.

 Refer to the AuthorizationPolicy resource for additional
details.

 authorizationPolicy only applies to a globalTargetHttpsProxy attached toglobalForwardingRules with theloadBalancingScheme set to INTERNAL_SELF_MANAGED.

 Note: This field currently has no impact. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `self_link_with_id` | String |  | [Output Only] Server-defined URL for this resource with the resource id. |
| `name` | String |  | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `proxy_bind` | bool |  | This field only applies when the forwarding rule that references this
target proxy has a loadBalancingScheme set toINTERNAL_SELF_MANAGED.

When this field is set to true, Envoy proxies set up inbound
traffic interception and bind to the IP address and port specified in the
forwarding rule. This is generally useful when using Traffic Director to
configure Envoy as a gateway or middle proxy (in other words, not a
sidecar proxy). The Envoy proxy listens for inbound requests and handles
requests when it receives them.

The default is false. |
| `tls_early_data` | String |  | Specifies whether TLS 1.3 0-RTT Data ("Early Data") should be accepted
for this service. Early Data allows a TLS resumption handshake to include
the initial application payload (a HTTP request) alongside the handshake,
reducing the effective round trips to "zero". This applies to TLS 1.3
connections over TCP (HTTP/2) as well as over UDP (QUIC/h3).


This can improve application performance, especially on networks where
interruptions may be common, such as on mobile.


Requests with Early Data will have the "Early-Data" HTTP header set on
the request, with a value of "1", to allow the backend to determine whether
Early Data was included.


Note: TLS Early Data may allow requests to be replayed, as the data is
sent to the backend before the handshake has fully completed. Applications
that allow idempotent HTTP methods to make non-idempotent changes, such as
a GET request updating a database, should not accept Early Data on those
requests, and reject requests with the "Early-Data: 1" HTTP header by
returning a HTTP 425 (Too Early) status code, in order to remain RFC
compliant.


The default value is DISABLED. |
| `url_map` | String |  | A fully-qualified or valid partial URL to the UrlMap resource that defines
the mapping from URL to the BackendService. For example, the following are
all valid URLs for specifying a URL map:
   
   - https://www.googleapis.compute/v1/projects/project/global/urlMaps/url-map 
   - projects/project/global/urlMaps/url-map 
   - global/urlMaps/url-map |
| `region` | String |  | [Output Only] URL of the region where the regional TargetHttpsProxy
resides. This field is not applicable to global TargetHttpsProxies. |
| `quic_override` | String |  | Specifies the QUIC override policy for this TargetHttpsProxy resource. This
setting determines whether the load balancer attempts to negotiate QUIC
with clients.
You can specify NONE, ENABLE, orDISABLE.
   
   - When quic-override is set to NONE,
   Google manages whether QUIC is used.
   - When quic-override is set to ENABLE, the
   load balancer uses QUIC when possible.
   - When quic-override is set to DISABLE, the
   load balancer doesn't use QUIC.
   - If the quic-override flag is not specified,NONE is implied. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `project` | String | ✅ | Project ID for this request. |
| `region` | String | ✅ | Name of the region scoping this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `certificate_map` | String | URL of a certificate map that identifies a certificate map associated with
the given target proxy.
This field can only be set for Global external Application Load Balancer or
Classic Application Load Balancer. For other products use Certificate
Manager Certificates instead.

If set, sslCertificates will be ignored.

 Accepted format is//certificatemanager.googleapis.com/projects/{project}/locations/{location}/certificateMaps/{resourceName}. |
| `server_tls_policy` | String | Optional. A URL referring to a networksecurity.ServerTlsPolicy resource
that describes how the proxy should authenticate inbound traffic.

 serverTlsPolicy only applies to a globalTargetHttpsProxy attached toglobalForwardingRules with theloadBalancingScheme set to INTERNAL_SELF_MANAGED
or EXTERNAL orEXTERNAL_MANAGED or INTERNAL_MANAGED.
It also applies to a regional TargetHttpsProxy attached to
regional forwardingRules with theloadBalancingScheme set to EXTERNAL_MANAGED orINTERNAL_MANAGED. For details whichServerTlsPolicy resources are accepted withINTERNAL_SELF_MANAGED and which with EXTERNAL,INTERNAL_MANAGED, EXTERNAL_MANAGEDloadBalancingScheme consult ServerTlsPolicy
documentation.

  If left blank, communications are not encrypted. |
| `ssl_certificates` | Vec<String> | URLs to SslCertificate resources that are used to authenticate
connections between users and the load balancer. At least one SSL
certificate must be specified. SslCertificates do not apply when the load
balancing scheme is set to INTERNAL_SELF_MANAGED.

The URLs should refer to a SSL Certificate resource or Certificate Manager
Certificate resource. Mixing Classic Certificates and Certificate Manager
Certificates is not allowed. Certificate Manager Certificates must include
the certificatemanager API namespace. Using Certificate Manager
Certificates in this field is not supported by Global external Application
Load Balancer or Classic Application Load Balancer, use certificate_map
instead.

Currently, you may specify up to 15 Classic SSL Certificates or up to 100
Certificate Manager Certificates.

Certificate Manager Certificates accepted formats are:
   
   - //certificatemanager.googleapis.com/projects/{project}/locations/{location}/certificates/{resourceName}.
   - https://certificatemanager.googleapis.com/v1alpha1/projects/{project}/locations/{location}/certificates/{resourceName}. |
| `authentication` | String | [Deprecated] Use serverTlsPolicy instead. |
| `authorization` | String | [Deprecated] Use authorizationPolicy instead. |
| `http_filters` | Vec<String> | URLs to networkservices.HttpFilter resources
enabled for xDS clients using this configuration. For example,https://networkservices.googleapis.com/beta/projects/project/locations/location/httpFilters/httpFilter
Only filters that handle outbound connection and
stream events may be specified. These filters work in conjunction with
a default set of HTTP filters that may already be configured by Traffic
Director. Traffic Director will determine the final location of these
filters within xDS configuration based on the name of the HTTP filter. If
Traffic Director positions multiple filters at the same location, those
filters will be in the same order as specified in this list. 

httpFilters only applies for loadbalancers withloadBalancingScheme set to INTERNAL_SELF_MANAGED.
See ForwardingRule for more details. |
| `http_keep_alive_timeout_sec` | i64 | Specifies how long to keep a connection open, after completing a response,
while there is no matching traffic (in seconds). If an HTTP keep-alive is
not specified, a default value (610 seconds) will be used.

For global external Application Load Balancers, the minimum allowed value
is 5 seconds and the maximum allowed value is 1200 seconds.

For classic Application Load Balancers, this option is not supported. |
| `kind` | String | [Output Only] Type of resource. Alwayscompute#targetHttpsProxy for target HTTPS proxies. |
| `ssl_policy` | String | URL of SslPolicy resource that will be associated with the TargetHttpsProxy
resource. If not set, the TargetHttpsProxy resource has no
SSL policy configured. |
| `fingerprint` | String | Fingerprint of this resource. A hash of the contents stored in this object.
This field is used in optimistic locking. This field will be ignored when
inserting a TargetHttpsProxy. An up-to-date fingerprint must
be provided in order to patch the TargetHttpsProxy; otherwise, the request
will fail with error 412 conditionNotMet. To see the latest
fingerprint, make a get() request to retrieve the
TargetHttpsProxy. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `authorization_policy` | String | Optional. A URL referring to a networksecurity.AuthorizationPolicy resource
that describes how the proxy should authorize inbound traffic. If left
blank, access will not be restricted by an authorization policy.

 Refer to the AuthorizationPolicy resource for additional
details.

 authorizationPolicy only applies to a globalTargetHttpsProxy attached toglobalForwardingRules with theloadBalancingScheme set to INTERNAL_SELF_MANAGED.

 Note: This field currently has no impact. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `self_link_with_id` | String | [Output Only] Server-defined URL for this resource with the resource id. |
| `name` | String | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `proxy_bind` | bool | This field only applies when the forwarding rule that references this
target proxy has a loadBalancingScheme set toINTERNAL_SELF_MANAGED.

When this field is set to true, Envoy proxies set up inbound
traffic interception and bind to the IP address and port specified in the
forwarding rule. This is generally useful when using Traffic Director to
configure Envoy as a gateway or middle proxy (in other words, not a
sidecar proxy). The Envoy proxy listens for inbound requests and handles
requests when it receives them.

The default is false. |
| `tls_early_data` | String | Specifies whether TLS 1.3 0-RTT Data ("Early Data") should be accepted
for this service. Early Data allows a TLS resumption handshake to include
the initial application payload (a HTTP request) alongside the handshake,
reducing the effective round trips to "zero". This applies to TLS 1.3
connections over TCP (HTTP/2) as well as over UDP (QUIC/h3).


This can improve application performance, especially on networks where
interruptions may be common, such as on mobile.


Requests with Early Data will have the "Early-Data" HTTP header set on
the request, with a value of "1", to allow the backend to determine whether
Early Data was included.


Note: TLS Early Data may allow requests to be replayed, as the data is
sent to the backend before the handshake has fully completed. Applications
that allow idempotent HTTP methods to make non-idempotent changes, such as
a GET request updating a database, should not accept Early Data on those
requests, and reject requests with the "Early-Data: 1" HTTP header by
returning a HTTP 425 (Too Early) status code, in order to remain RFC
compliant.


The default value is DISABLED. |
| `url_map` | String | A fully-qualified or valid partial URL to the UrlMap resource that defines
the mapping from URL to the BackendService. For example, the following are
all valid URLs for specifying a URL map:
   
   - https://www.googleapis.compute/v1/projects/project/global/urlMaps/url-map 
   - projects/project/global/urlMaps/url-map 
   - global/urlMaps/url-map |
| `region` | String | [Output Only] URL of the region where the regional TargetHttpsProxy
resides. This field is not applicable to global TargetHttpsProxies. |
| `quic_override` | String | Specifies the QUIC override policy for this TargetHttpsProxy resource. This
setting determines whether the load balancer attempts to negotiate QUIC
with clients.
You can specify NONE, ENABLE, orDISABLE.
   
   - When quic-override is set to NONE,
   Google manages whether QUIC is used.
   - When quic-override is set to ENABLE, the
   load balancer uses QUIC when possible.
   - When quic-override is set to DISABLE, the
   load balancer doesn't use QUIC.
   - If the quic-override flag is not specified,NONE is implied. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create region_target_https_proxie
region_target_https_proxie = provider.compute_api.Region_target_https_proxie {
    project = "value"  # Project ID for this request.
    region = "value"  # Name of the region scoping this request.
}

# Access region_target_https_proxie outputs
region_target_https_proxie_id = region_target_https_proxie.id
region_target_https_proxie_certificate_map = region_target_https_proxie.certificate_map
region_target_https_proxie_server_tls_policy = region_target_https_proxie.server_tls_policy
region_target_https_proxie_ssl_certificates = region_target_https_proxie.ssl_certificates
region_target_https_proxie_authentication = region_target_https_proxie.authentication
region_target_https_proxie_authorization = region_target_https_proxie.authorization
region_target_https_proxie_http_filters = region_target_https_proxie.http_filters
region_target_https_proxie_http_keep_alive_timeout_sec = region_target_https_proxie.http_keep_alive_timeout_sec
region_target_https_proxie_kind = region_target_https_proxie.kind
region_target_https_proxie_ssl_policy = region_target_https_proxie.ssl_policy
region_target_https_proxie_fingerprint = region_target_https_proxie.fingerprint
region_target_https_proxie_self_link = region_target_https_proxie.self_link
region_target_https_proxie_creation_timestamp = region_target_https_proxie.creation_timestamp
region_target_https_proxie_authorization_policy = region_target_https_proxie.authorization_policy
region_target_https_proxie_description = region_target_https_proxie.description
region_target_https_proxie_self_link_with_id = region_target_https_proxie.self_link_with_id
region_target_https_proxie_name = region_target_https_proxie.name
region_target_https_proxie_proxy_bind = region_target_https_proxie.proxy_bind
region_target_https_proxie_tls_early_data = region_target_https_proxie.tls_early_data
region_target_https_proxie_url_map = region_target_https_proxie.url_map
region_target_https_proxie_region = region_target_https_proxie.region
region_target_https_proxie_quic_override = region_target_https_proxie.quic_override
region_target_https_proxie_id = region_target_https_proxie.id
```

---


### Backend_service

Creates a BackendService resource in the specified project using
the data included in the request. For more information, see
Backend services overview.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `backends` | Vec<String> |  | The list of backends that serve this BackendService. |
| `log_config` | String |  | This field denotes the logging options for the load balancer traffic served
by this backend service. If logging is enabled, logs will be exported to
Stackdriver. |
| `compression_mode` | String |  | Compress text responses using Brotli or gzip compression, based on
the client's Accept-Encoding header. |
| `name` | String |  | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `params` | String |  | Input only. [Input Only] Additional params passed with the request, but not persisted
as part of resource payload. |
| `security_policy` | String |  | [Output Only] The resource URL for the security policy associated with this
backend service. |
| `kind` | String |  | [Output Only] Type of resource. Always compute#backendService
for backend services. |
| `outlier_detection` | String |  | Settings controlling the ejection of unhealthy backend endpoints from the
load balancing pool of each individual proxy instance that processes the
traffic for the given backend service. If not set, this feature is
considered disabled.

Results of the outlier detection algorithm (ejection of endpoints from the
load balancing pool and returning them back to the pool) are executed
independently by each proxy instance of the load balancer. In most cases,
more than one proxy instance handles the traffic received by a backend
service. Thus, it is possible that an unhealthy endpoint is detected and
ejected by only some of the proxies, and while this happens, other proxies
may continue to send requests to the same unhealthy endpoint until they
detect and eject the unhealthy endpoint.

Applicable backend endpoints can be:
   
   - VM instances in an Instance Group
   - Endpoints in a Zonal NEG (GCE_VM_IP, GCE_VM_IP_PORT)
   - Endpoints in a Hybrid Connectivity NEG (NON_GCP_PRIVATE_IP_PORT)
   - Serverless NEGs, that resolve to Cloud Run, App Engine, or Cloud
   Functions Services 
   - Private Service Connect NEGs, that resolve to
   Google-managed regional API endpoints or managed services published using
   Private Service Connect



Applicable backend service types can be:
   
   - A global backend service with the loadBalancingScheme set to
   INTERNAL_SELF_MANAGED or EXTERNAL_MANAGED. 
   - A regional backend
   service with the serviceProtocol set to HTTP, HTTPS, HTTP2 or H2C, and
   loadBalancingScheme set to INTERNAL_MANAGED or EXTERNAL_MANAGED. Not
   supported for Serverless NEGs.



Not supported when the backend service is referenced by a URL map that is
bound to target gRPC proxy that has validateForProxyless field set to true. |
| `service_bindings` | Vec<String> |  | URLs of networkservices.ServiceBinding resources.

Can only be set if load balancing scheme is INTERNAL_SELF_MANAGED.
If set, lists of backends and health checks must be both empty. |
| `custom_request_headers` | Vec<String> |  | Headers that the load balancer adds to proxied requests. See [Creating
custom
headers](https://cloud.google.com/load-balancing/docs/custom-headers). |
| `health_checks` | Vec<String> |  | The list of URLs to the healthChecks, httpHealthChecks (legacy), or
httpsHealthChecks (legacy) resource for health checking this backend
service. Not all backend services support legacy health checks. See
Load balancer guide. Currently, at most one health check can be
specified for each backend service. Backend services with
instance group or zonal NEG backends must have a health check unless
haPolicy is specified. Backend services with internet or serverless NEG
backends must not have a health check.

healthChecks[] cannot be specified with haPolicy. |
| `load_balancing_scheme` | String |  | Specifies the load balancer type. A backend service
created for one type of load balancer cannot be used with another.
For more information, refer toChoosing
a load balancer. |
| `max_stream_duration` | String |  | Specifies the default maximum duration (timeout) for streams to this
service. Duration is computed from the beginning of the stream until the
response has been completely processed, including all retries. A stream
that does not complete in this duration is closed.

If not specified, there will be no timeout limit, i.e. the maximum
duration is infinite.

This value can be overridden in the PathMatcher configuration of the
UrlMap that references this backend service.

This field is only allowed when the loadBalancingScheme of
the backend service is INTERNAL_SELF_MANAGED. |
| `network` | String |  | The URL of the network to which this backend service belongs.

This field must be set for Internal Passthrough Network Load Balancers when
the haPolicy is enabled, and for External Passthrough Network Load
Balancers when the haPolicy fastIpMove is enabled.

This field can only be specified when the load balancing scheme is set toINTERNAL, or when the load balancing scheme is set toEXTERNAL and haPolicy fastIpMove is enabled. |
| `custom_response_headers` | Vec<String> |  | Headers that the load balancer adds to proxied responses. See [Creating
custom
headers](https://cloud.google.com/load-balancing/docs/custom-headers). |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `subsetting` | String |  | subsetting cannot be specified with haPolicy. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `timeout_sec` | i64 |  | The backend service timeout has a different meaning depending on the
type of load balancer. For more information see,
Backend service settings.
The default is 30 seconds.
The full range of timeout values allowed goes from 1
through 2,147,483,647 seconds.

This value can be overridden in the PathMatcher configuration of the
UrlMap that references this backend service.

Not supported when the backend service is referenced by a URL map that is
bound to target gRPC proxy that has validateForProxyless field set to true.
Instead, use maxStreamDuration. |
| `connection_tracking_policy` | String |  | Connection Tracking configuration for this BackendService. Connection
tracking policy settings are only available for external passthrough
Network Load Balancers and internal passthrough Network Load Balancers.

connectionTrackingPolicy cannot be specified with haPolicy. |
| `session_affinity` | String |  | Type of session affinity to use. The default is NONE.

Only NONE and HEADER_FIELD are supported
when the backend service is referenced by a URL map that is bound to
target gRPC proxy that has validateForProxyless field set to true.

For more details, see:
[Session
Affinity](https://cloud.google.com/load-balancing/docs/backend-service#session_affinity).

sessionAffinity cannot be specified with haPolicy. |
| `port` | i64 |  | Deprecated in favor of portName. The TCP port to connect on
the backend. The default value is 80.
For internal passthrough Network Load Balancers and external passthrough
Network Load Balancers, omit port. |
| `iap` | String |  | The configurations for Identity-Aware Proxy on this resource.
Not available for internal passthrough Network Load Balancers and external
passthrough Network Load Balancers. |
| `self_link_with_id` | String |  | [Output Only] Server-defined URL for this resource with the resource id. |
| `ip_address_selection_policy` | String |  | Specifies a preference for traffic sent from the proxy to the backend (or
from the client to the backend for proxyless gRPC).
The possible values are:
   
   - IPV4_ONLY: Only send IPv4 traffic to the backends of the
   backend service (Instance Group, Managed Instance Group, Network Endpoint
   Group), regardless of traffic from the client to the proxy. Only IPv4
   health checks are used to check the health of the backends. This is the
   default setting.
   - PREFER_IPV6: Prioritize the connection to the endpoint's
   IPv6 address over its IPv4 address (provided there is a healthy IPv6
   address).
   - IPV6_ONLY: Only send IPv6 traffic to the backends of the
   backend service (Instance Group, Managed Instance Group, Network Endpoint
   Group), regardless of traffic from the client to the proxy. Only IPv6
   health checks are used to check the health of the backends.



This field is applicable to either:
   
   -  Advanced global external Application Load Balancer (load balancing
   scheme EXTERNAL_MANAGED), 
   -  Regional external Application Load
   Balancer, 
   -  Internal proxy Network Load Balancer (load balancing
   scheme INTERNAL_MANAGED), 
   -  Regional internal Application Load
   Balancer (load balancing scheme INTERNAL_MANAGED), 
   -  Traffic
   Director with Envoy proxies and proxyless gRPC (load balancing scheme
   INTERNAL_SELF_MANAGED). |
| `circuit_breakers` | String |  |  |
| `metadatas` | HashMap<String, String> |  | Deployment metadata associated with the resource to be set by a GKE hub
controller and read by the backend RCTH |
| `external_managed_migration_state` | String |  | Specifies the canary migration state. Possible values are PREPARE,
TEST_BY_PERCENTAGE, and TEST_ALL_TRAFFIC.

To begin the migration from EXTERNAL to EXTERNAL_MANAGED, the state must be
changed to PREPARE. The state must be changed to TEST_ALL_TRAFFIC before
the loadBalancingScheme can be changed to EXTERNAL_MANAGED. Optionally, the
TEST_BY_PERCENTAGE state can be used to migrate traffic by percentage using
externalManagedMigrationTestingPercentage.

Rolling back a migration requires the states to be set in reverse order. So
changing the scheme from EXTERNAL_MANAGED to EXTERNAL requires the state to
be set to TEST_ALL_TRAFFIC at the same time. Optionally, the
TEST_BY_PERCENTAGE state can be used to migrate some traffic back to
EXTERNAL or PREPARE can be used to migrate all traffic back to EXTERNAL. |
| `tls_settings` | String |  | Configuration for Backend Authenticated TLS and mTLS. May only be specified
when the backend protocol is SSL, HTTPS or HTTP2. |
| `dynamic_forwarding` | String |  | Dynamic forwarding configuration. This field is used to configure the
backend service with dynamic forwarding feature which together with Service
Extension allows customized and complex routing logic. |
| `used_by` | Vec<String> |  | [Output Only] List of resources referencing given backend service. |
| `connection_draining` | String |  | connectionDraining cannot be specified with haPolicy. |
| `external_managed_migration_testing_percentage` | f64 |  | Determines the fraction of requests that should be processed by the Global
external Application Load Balancer.

The value of this field must be in the range [0, 100].

Session affinity options will slightly affect this routing behavior, for
more details, see:Session
Affinity.

This value can only be set if the loadBalancingScheme in the BackendService
is set to EXTERNAL (when using the classic Application Load Balancer) and
the migration state is TEST_BY_PERCENTAGE. |
| `region` | String |  | [Output Only] URL of the region where the regional backend service
resides. This field is not applicable to global backend services.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `strong_session_affinity_cookie` | String |  | Describes the HTTP cookie used for stateful session affinity. This field is
applicable and required if the sessionAffinity is set toSTRONG_COOKIE_AFFINITY. |
| `locality_lb_policy` | String |  | The load balancing algorithm used within the scope of the locality. The
possible values are:
   
   - ROUND_ROBIN: This is a simple policy in which each healthy
   backend is selected in round robin order. This is the default.
   - LEAST_REQUEST: An O(1) algorithm which
   selects two random healthy hosts and picks the host which has fewer active
   requests.
   - RING_HASH: The ring/modulo hash load balancer implements
   consistent hashing to backends. The algorithm has the property that the
   addition/removal of a host from a set of N hosts only affects 1/N of the
   requests.
   - RANDOM: The load balancer selects a random healthy
   host.
   - ORIGINAL_DESTINATION: Backend host is selected
   based on the client connection metadata, i.e., connections are opened to
   the same address as the destination address of the incoming connection
   before the connection was redirected to the load balancer.
   - MAGLEV: used as a drop in replacement for the ring hash
   load balancer. Maglev is not as stable as ring hash but has faster table
   lookup build times and host selection times. For more information about
   Maglev, see Maglev:
   A Fast and Reliable Software Network Load Balancer.
   - WEIGHTED_ROUND_ROBIN: Per-endpoint Weighted Round Robin
   Load Balancing using weights computed from Backend reported Custom Metrics.
   If set, the Backend Service responses are expected to contain non-standard
   HTTP response header field Endpoint-Load-Metrics. The reported
   metrics to use for computing the weights are specified via thecustomMetrics field.
   
   This field is applicable to either:
      - A regional backend service with the service_protocol set to HTTP,
      HTTPS, HTTP2 or H2C, and load_balancing_scheme set to
      INTERNAL_MANAGED. 
      - A global backend service with the
      load_balancing_scheme set to INTERNAL_SELF_MANAGED, INTERNAL_MANAGED, or
      EXTERNAL_MANAGED.
   
   
   If sessionAffinity is not configured—that is, if session
   affinity remains at the default value of NONE—then the
   default value for localityLbPolicy
   is ROUND_ROBIN. If session affinity is set to a value other
   than NONE,
   then the default value for localityLbPolicy isMAGLEV.
   
   Only ROUND_ROBIN and RING_HASH are supported
   when the backend service is referenced by a URL map that is bound to
   target gRPC proxy that has validateForProxyless field set to true.
   
   localityLbPolicy cannot be specified with haPolicy. |
| `vpc_network_scope` | String |  | The network scope of the backends that can be added to the backend
service. This field can be either GLOBAL_VPC_NETWORK orREGIONAL_VPC_NETWORK.

A backend service with the VPC scope set to GLOBAL_VPC_NETWORK
is only allowed to have backends in global VPC networks.

When the VPC scope is set to REGIONAL_VPC_NETWORK the backend
service is only allowed to have backends in regional networks in the same
scope as the backend service.
Note: if not specified then GLOBAL_VPC_NETWORK will be used. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `edge_security_policy` | String |  | [Output Only] The resource URL for the edge security policy associated with
this backend service. |
| `network_pass_through_lb_traffic_policy` | String |  | Configures traffic steering properties of internal passthrough Network
Load Balancers.

networkPassThroughLbTrafficPolicy cannot be specified with haPolicy. |
| `allow_multinetwork` | bool |  | A boolean flag enabling multi-network mesh. This field is only allowed with
load balancing scheme set to INTERNAL_SELF_MANAGED. |
| `enable_cdn` | bool |  | If true, enables Cloud CDN for the backend service of a
global external Application Load Balancer. |
| `affinity_cookie_ttl_sec` | i64 |  | Lifetime of cookies in seconds. This setting is applicable to Application
Load Balancers and Traffic Director and requires
GENERATED_COOKIE or HTTP_COOKIE session affinity.

If set to 0, the cookie is non-persistent and lasts only until
the end of the browser session (or equivalent). The maximum allowed value
is two weeks (1,209,600).

Not supported when the backend service is referenced by a URL map that is
bound to target gRPC proxy that has validateForProxyless field set to true. |
| `protocol` | String |  | The protocol this BackendService uses to communicate
with backends.

Possible values are HTTP, HTTPS, HTTP2, H2C, TCP, SSL, UDP or GRPC.
depending on the chosen load balancer or Traffic Director configuration.
Refer to the documentation for the load balancers or for Traffic Director
for more information.

Must be set to GRPC when the backend service is referenced by a URL map
that is bound to target gRPC proxy. |
| `service_lb_policy` | String |  | URL to networkservices.ServiceLbPolicy resource.

Can only be set if load balancing scheme is EXTERNAL_MANAGED,
INTERNAL_MANAGED or INTERNAL_SELF_MANAGED and the scope is global. |
| `consistent_hash` | String |  | Consistent Hash-based load balancing can be used to provide soft session
affinity based on HTTP headers, cookies or other properties. This load
balancing policy is applicable only for HTTP connections. The affinity to a
particular destination host will be lost when one or more hosts are
added/removed from the destination service. This field specifies parameters
that control consistent hashing. This field is only applicable whenlocalityLbPolicy is set to MAGLEV orRING_HASH.

This field is applicable to either:
   
   - A regional backend service with the service_protocol set to HTTP,
   HTTPS, HTTP2 or H2C, and load_balancing_scheme set to
   INTERNAL_MANAGED. 
   - A global backend service with the
   load_balancing_scheme set to INTERNAL_SELF_MANAGED. |
| `locality_lb_policies` | Vec<String> |  | A list of locality load-balancing policies to be used in order of
preference. When you use localityLbPolicies, you must set at least one
value for either the localityLbPolicies[].policy or the
localityLbPolicies[].customPolicy field. localityLbPolicies overrides any
value set in the localityLbPolicy field.

For an example of how to use this field, seeDefine
a list of preferred policies.

Caution: This field and its children are intended for use in a service mesh
that includes gRPC clients only. Envoy proxies can't use backend services
that have this configuration. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `port_name` | String |  | A named port on a backend instance group representing the port for
communication to the backend VMs in that group. The
named port must be [defined on each backend instance
group](https://cloud.google.com/load-balancing/docs/backend-service#named_ports).
This parameter has no meaning if the backends are NEGs. For internal
passthrough Network Load Balancers and external passthrough Network Load
Balancers, omit port_name. |
| `ha_policy` | String |  | Configures self-managed High Availability (HA) for External and Internal
Protocol Forwarding.

The backends of this regional backend service must only specify zonal
network endpoint groups (NEGs) of type GCE_VM_IP.

When haPolicy is set for an Internal Passthrough Network Load Balancer, the
regional backend service must set the network field. All zonal NEGs must
belong to the same network. However, individual NEGs can
belong to different subnetworks of that network.

When haPolicy is specified, the set of attached network endpoints across
all backends comprise an High Availability domain from which one endpoint
is selected as the active endpoint (the leader) that receives all
traffic.

haPolicy can be added only at backend service creation time. Once set up,
it cannot be deleted.

Note that haPolicy is not for load balancing, and therefore cannot be
specified with sessionAffinity, connectionTrackingPolicy, and
failoverPolicy.

haPolicy requires customers to be responsible for tracking backend
endpoint health and electing a leader among the healthy endpoints.
Therefore, haPolicy cannot be specified with healthChecks.

haPolicy can only be specified for External Passthrough Network Load
Balancers and Internal Passthrough Network Load Balancers. |
| `cdn_policy` | String |  | Cloud CDN configuration for this BackendService. Only available for
specified load balancer types. |
| `fingerprint` | String |  | Fingerprint of this resource. A hash of the contents stored in this object.
This field is used in optimistic locking. This field will be ignored when
inserting a BackendService. An up-to-date fingerprint must be provided in
order to update the BackendService, otherwise the request will
fail with error 412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve a BackendService. |
| `security_settings` | String |  | This field specifies the security settings that apply to this backend
service. This field is applicable to a global backend service with the
load_balancing_scheme set to INTERNAL_SELF_MANAGED. |
| `custom_metrics` | Vec<String> |  | List of custom metrics that are used for theWEIGHTED_ROUND_ROBIN locality_lb_policy. |
| `failover_policy` | String |  | Requires at least one backend instance group to be defined
as a backup (failover) backend.
For load balancers that have configurable failover:
[Internal passthrough Network Load
Balancers](https://cloud.google.com/load-balancing/docs/internal/failover-overview)
and [external passthrough Network Load
Balancers](https://cloud.google.com/load-balancing/docs/network/networklb-failover-overview).

failoverPolicy cannot be specified with haPolicy. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `backends` | Vec<String> | The list of backends that serve this BackendService. |
| `log_config` | String | This field denotes the logging options for the load balancer traffic served
by this backend service. If logging is enabled, logs will be exported to
Stackdriver. |
| `compression_mode` | String | Compress text responses using Brotli or gzip compression, based on
the client's Accept-Encoding header. |
| `name` | String | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `params` | String | Input only. [Input Only] Additional params passed with the request, but not persisted
as part of resource payload. |
| `security_policy` | String | [Output Only] The resource URL for the security policy associated with this
backend service. |
| `kind` | String | [Output Only] Type of resource. Always compute#backendService
for backend services. |
| `outlier_detection` | String | Settings controlling the ejection of unhealthy backend endpoints from the
load balancing pool of each individual proxy instance that processes the
traffic for the given backend service. If not set, this feature is
considered disabled.

Results of the outlier detection algorithm (ejection of endpoints from the
load balancing pool and returning them back to the pool) are executed
independently by each proxy instance of the load balancer. In most cases,
more than one proxy instance handles the traffic received by a backend
service. Thus, it is possible that an unhealthy endpoint is detected and
ejected by only some of the proxies, and while this happens, other proxies
may continue to send requests to the same unhealthy endpoint until they
detect and eject the unhealthy endpoint.

Applicable backend endpoints can be:
   
   - VM instances in an Instance Group
   - Endpoints in a Zonal NEG (GCE_VM_IP, GCE_VM_IP_PORT)
   - Endpoints in a Hybrid Connectivity NEG (NON_GCP_PRIVATE_IP_PORT)
   - Serverless NEGs, that resolve to Cloud Run, App Engine, or Cloud
   Functions Services 
   - Private Service Connect NEGs, that resolve to
   Google-managed regional API endpoints or managed services published using
   Private Service Connect



Applicable backend service types can be:
   
   - A global backend service with the loadBalancingScheme set to
   INTERNAL_SELF_MANAGED or EXTERNAL_MANAGED. 
   - A regional backend
   service with the serviceProtocol set to HTTP, HTTPS, HTTP2 or H2C, and
   loadBalancingScheme set to INTERNAL_MANAGED or EXTERNAL_MANAGED. Not
   supported for Serverless NEGs.



Not supported when the backend service is referenced by a URL map that is
bound to target gRPC proxy that has validateForProxyless field set to true. |
| `service_bindings` | Vec<String> | URLs of networkservices.ServiceBinding resources.

Can only be set if load balancing scheme is INTERNAL_SELF_MANAGED.
If set, lists of backends and health checks must be both empty. |
| `custom_request_headers` | Vec<String> | Headers that the load balancer adds to proxied requests. See [Creating
custom
headers](https://cloud.google.com/load-balancing/docs/custom-headers). |
| `health_checks` | Vec<String> | The list of URLs to the healthChecks, httpHealthChecks (legacy), or
httpsHealthChecks (legacy) resource for health checking this backend
service. Not all backend services support legacy health checks. See
Load balancer guide. Currently, at most one health check can be
specified for each backend service. Backend services with
instance group or zonal NEG backends must have a health check unless
haPolicy is specified. Backend services with internet or serverless NEG
backends must not have a health check.

healthChecks[] cannot be specified with haPolicy. |
| `load_balancing_scheme` | String | Specifies the load balancer type. A backend service
created for one type of load balancer cannot be used with another.
For more information, refer toChoosing
a load balancer. |
| `max_stream_duration` | String | Specifies the default maximum duration (timeout) for streams to this
service. Duration is computed from the beginning of the stream until the
response has been completely processed, including all retries. A stream
that does not complete in this duration is closed.

If not specified, there will be no timeout limit, i.e. the maximum
duration is infinite.

This value can be overridden in the PathMatcher configuration of the
UrlMap that references this backend service.

This field is only allowed when the loadBalancingScheme of
the backend service is INTERNAL_SELF_MANAGED. |
| `network` | String | The URL of the network to which this backend service belongs.

This field must be set for Internal Passthrough Network Load Balancers when
the haPolicy is enabled, and for External Passthrough Network Load
Balancers when the haPolicy fastIpMove is enabled.

This field can only be specified when the load balancing scheme is set toINTERNAL, or when the load balancing scheme is set toEXTERNAL and haPolicy fastIpMove is enabled. |
| `custom_response_headers` | Vec<String> | Headers that the load balancer adds to proxied responses. See [Creating
custom
headers](https://cloud.google.com/load-balancing/docs/custom-headers). |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `subsetting` | String | subsetting cannot be specified with haPolicy. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `timeout_sec` | i64 | The backend service timeout has a different meaning depending on the
type of load balancer. For more information see,
Backend service settings.
The default is 30 seconds.
The full range of timeout values allowed goes from 1
through 2,147,483,647 seconds.

This value can be overridden in the PathMatcher configuration of the
UrlMap that references this backend service.

Not supported when the backend service is referenced by a URL map that is
bound to target gRPC proxy that has validateForProxyless field set to true.
Instead, use maxStreamDuration. |
| `connection_tracking_policy` | String | Connection Tracking configuration for this BackendService. Connection
tracking policy settings are only available for external passthrough
Network Load Balancers and internal passthrough Network Load Balancers.

connectionTrackingPolicy cannot be specified with haPolicy. |
| `session_affinity` | String | Type of session affinity to use. The default is NONE.

Only NONE and HEADER_FIELD are supported
when the backend service is referenced by a URL map that is bound to
target gRPC proxy that has validateForProxyless field set to true.

For more details, see:
[Session
Affinity](https://cloud.google.com/load-balancing/docs/backend-service#session_affinity).

sessionAffinity cannot be specified with haPolicy. |
| `port` | i64 | Deprecated in favor of portName. The TCP port to connect on
the backend. The default value is 80.
For internal passthrough Network Load Balancers and external passthrough
Network Load Balancers, omit port. |
| `iap` | String | The configurations for Identity-Aware Proxy on this resource.
Not available for internal passthrough Network Load Balancers and external
passthrough Network Load Balancers. |
| `self_link_with_id` | String | [Output Only] Server-defined URL for this resource with the resource id. |
| `ip_address_selection_policy` | String | Specifies a preference for traffic sent from the proxy to the backend (or
from the client to the backend for proxyless gRPC).
The possible values are:
   
   - IPV4_ONLY: Only send IPv4 traffic to the backends of the
   backend service (Instance Group, Managed Instance Group, Network Endpoint
   Group), regardless of traffic from the client to the proxy. Only IPv4
   health checks are used to check the health of the backends. This is the
   default setting.
   - PREFER_IPV6: Prioritize the connection to the endpoint's
   IPv6 address over its IPv4 address (provided there is a healthy IPv6
   address).
   - IPV6_ONLY: Only send IPv6 traffic to the backends of the
   backend service (Instance Group, Managed Instance Group, Network Endpoint
   Group), regardless of traffic from the client to the proxy. Only IPv6
   health checks are used to check the health of the backends.



This field is applicable to either:
   
   -  Advanced global external Application Load Balancer (load balancing
   scheme EXTERNAL_MANAGED), 
   -  Regional external Application Load
   Balancer, 
   -  Internal proxy Network Load Balancer (load balancing
   scheme INTERNAL_MANAGED), 
   -  Regional internal Application Load
   Balancer (load balancing scheme INTERNAL_MANAGED), 
   -  Traffic
   Director with Envoy proxies and proxyless gRPC (load balancing scheme
   INTERNAL_SELF_MANAGED). |
| `circuit_breakers` | String |  |
| `metadatas` | HashMap<String, String> | Deployment metadata associated with the resource to be set by a GKE hub
controller and read by the backend RCTH |
| `external_managed_migration_state` | String | Specifies the canary migration state. Possible values are PREPARE,
TEST_BY_PERCENTAGE, and TEST_ALL_TRAFFIC.

To begin the migration from EXTERNAL to EXTERNAL_MANAGED, the state must be
changed to PREPARE. The state must be changed to TEST_ALL_TRAFFIC before
the loadBalancingScheme can be changed to EXTERNAL_MANAGED. Optionally, the
TEST_BY_PERCENTAGE state can be used to migrate traffic by percentage using
externalManagedMigrationTestingPercentage.

Rolling back a migration requires the states to be set in reverse order. So
changing the scheme from EXTERNAL_MANAGED to EXTERNAL requires the state to
be set to TEST_ALL_TRAFFIC at the same time. Optionally, the
TEST_BY_PERCENTAGE state can be used to migrate some traffic back to
EXTERNAL or PREPARE can be used to migrate all traffic back to EXTERNAL. |
| `tls_settings` | String | Configuration for Backend Authenticated TLS and mTLS. May only be specified
when the backend protocol is SSL, HTTPS or HTTP2. |
| `dynamic_forwarding` | String | Dynamic forwarding configuration. This field is used to configure the
backend service with dynamic forwarding feature which together with Service
Extension allows customized and complex routing logic. |
| `used_by` | Vec<String> | [Output Only] List of resources referencing given backend service. |
| `connection_draining` | String | connectionDraining cannot be specified with haPolicy. |
| `external_managed_migration_testing_percentage` | f64 | Determines the fraction of requests that should be processed by the Global
external Application Load Balancer.

The value of this field must be in the range [0, 100].

Session affinity options will slightly affect this routing behavior, for
more details, see:Session
Affinity.

This value can only be set if the loadBalancingScheme in the BackendService
is set to EXTERNAL (when using the classic Application Load Balancer) and
the migration state is TEST_BY_PERCENTAGE. |
| `region` | String | [Output Only] URL of the region where the regional backend service
resides. This field is not applicable to global backend services.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `strong_session_affinity_cookie` | String | Describes the HTTP cookie used for stateful session affinity. This field is
applicable and required if the sessionAffinity is set toSTRONG_COOKIE_AFFINITY. |
| `locality_lb_policy` | String | The load balancing algorithm used within the scope of the locality. The
possible values are:
   
   - ROUND_ROBIN: This is a simple policy in which each healthy
   backend is selected in round robin order. This is the default.
   - LEAST_REQUEST: An O(1) algorithm which
   selects two random healthy hosts and picks the host which has fewer active
   requests.
   - RING_HASH: The ring/modulo hash load balancer implements
   consistent hashing to backends. The algorithm has the property that the
   addition/removal of a host from a set of N hosts only affects 1/N of the
   requests.
   - RANDOM: The load balancer selects a random healthy
   host.
   - ORIGINAL_DESTINATION: Backend host is selected
   based on the client connection metadata, i.e., connections are opened to
   the same address as the destination address of the incoming connection
   before the connection was redirected to the load balancer.
   - MAGLEV: used as a drop in replacement for the ring hash
   load balancer. Maglev is not as stable as ring hash but has faster table
   lookup build times and host selection times. For more information about
   Maglev, see Maglev:
   A Fast and Reliable Software Network Load Balancer.
   - WEIGHTED_ROUND_ROBIN: Per-endpoint Weighted Round Robin
   Load Balancing using weights computed from Backend reported Custom Metrics.
   If set, the Backend Service responses are expected to contain non-standard
   HTTP response header field Endpoint-Load-Metrics. The reported
   metrics to use for computing the weights are specified via thecustomMetrics field.
   
   This field is applicable to either:
      - A regional backend service with the service_protocol set to HTTP,
      HTTPS, HTTP2 or H2C, and load_balancing_scheme set to
      INTERNAL_MANAGED. 
      - A global backend service with the
      load_balancing_scheme set to INTERNAL_SELF_MANAGED, INTERNAL_MANAGED, or
      EXTERNAL_MANAGED.
   
   
   If sessionAffinity is not configured—that is, if session
   affinity remains at the default value of NONE—then the
   default value for localityLbPolicy
   is ROUND_ROBIN. If session affinity is set to a value other
   than NONE,
   then the default value for localityLbPolicy isMAGLEV.
   
   Only ROUND_ROBIN and RING_HASH are supported
   when the backend service is referenced by a URL map that is bound to
   target gRPC proxy that has validateForProxyless field set to true.
   
   localityLbPolicy cannot be specified with haPolicy. |
| `vpc_network_scope` | String | The network scope of the backends that can be added to the backend
service. This field can be either GLOBAL_VPC_NETWORK orREGIONAL_VPC_NETWORK.

A backend service with the VPC scope set to GLOBAL_VPC_NETWORK
is only allowed to have backends in global VPC networks.

When the VPC scope is set to REGIONAL_VPC_NETWORK the backend
service is only allowed to have backends in regional networks in the same
scope as the backend service.
Note: if not specified then GLOBAL_VPC_NETWORK will be used. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `edge_security_policy` | String | [Output Only] The resource URL for the edge security policy associated with
this backend service. |
| `network_pass_through_lb_traffic_policy` | String | Configures traffic steering properties of internal passthrough Network
Load Balancers.

networkPassThroughLbTrafficPolicy cannot be specified with haPolicy. |
| `allow_multinetwork` | bool | A boolean flag enabling multi-network mesh. This field is only allowed with
load balancing scheme set to INTERNAL_SELF_MANAGED. |
| `enable_cdn` | bool | If true, enables Cloud CDN for the backend service of a
global external Application Load Balancer. |
| `affinity_cookie_ttl_sec` | i64 | Lifetime of cookies in seconds. This setting is applicable to Application
Load Balancers and Traffic Director and requires
GENERATED_COOKIE or HTTP_COOKIE session affinity.

If set to 0, the cookie is non-persistent and lasts only until
the end of the browser session (or equivalent). The maximum allowed value
is two weeks (1,209,600).

Not supported when the backend service is referenced by a URL map that is
bound to target gRPC proxy that has validateForProxyless field set to true. |
| `protocol` | String | The protocol this BackendService uses to communicate
with backends.

Possible values are HTTP, HTTPS, HTTP2, H2C, TCP, SSL, UDP or GRPC.
depending on the chosen load balancer or Traffic Director configuration.
Refer to the documentation for the load balancers or for Traffic Director
for more information.

Must be set to GRPC when the backend service is referenced by a URL map
that is bound to target gRPC proxy. |
| `service_lb_policy` | String | URL to networkservices.ServiceLbPolicy resource.

Can only be set if load balancing scheme is EXTERNAL_MANAGED,
INTERNAL_MANAGED or INTERNAL_SELF_MANAGED and the scope is global. |
| `consistent_hash` | String | Consistent Hash-based load balancing can be used to provide soft session
affinity based on HTTP headers, cookies or other properties. This load
balancing policy is applicable only for HTTP connections. The affinity to a
particular destination host will be lost when one or more hosts are
added/removed from the destination service. This field specifies parameters
that control consistent hashing. This field is only applicable whenlocalityLbPolicy is set to MAGLEV orRING_HASH.

This field is applicable to either:
   
   - A regional backend service with the service_protocol set to HTTP,
   HTTPS, HTTP2 or H2C, and load_balancing_scheme set to
   INTERNAL_MANAGED. 
   - A global backend service with the
   load_balancing_scheme set to INTERNAL_SELF_MANAGED. |
| `locality_lb_policies` | Vec<String> | A list of locality load-balancing policies to be used in order of
preference. When you use localityLbPolicies, you must set at least one
value for either the localityLbPolicies[].policy or the
localityLbPolicies[].customPolicy field. localityLbPolicies overrides any
value set in the localityLbPolicy field.

For an example of how to use this field, seeDefine
a list of preferred policies.

Caution: This field and its children are intended for use in a service mesh
that includes gRPC clients only. Envoy proxies can't use backend services
that have this configuration. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `port_name` | String | A named port on a backend instance group representing the port for
communication to the backend VMs in that group. The
named port must be [defined on each backend instance
group](https://cloud.google.com/load-balancing/docs/backend-service#named_ports).
This parameter has no meaning if the backends are NEGs. For internal
passthrough Network Load Balancers and external passthrough Network Load
Balancers, omit port_name. |
| `ha_policy` | String | Configures self-managed High Availability (HA) for External and Internal
Protocol Forwarding.

The backends of this regional backend service must only specify zonal
network endpoint groups (NEGs) of type GCE_VM_IP.

When haPolicy is set for an Internal Passthrough Network Load Balancer, the
regional backend service must set the network field. All zonal NEGs must
belong to the same network. However, individual NEGs can
belong to different subnetworks of that network.

When haPolicy is specified, the set of attached network endpoints across
all backends comprise an High Availability domain from which one endpoint
is selected as the active endpoint (the leader) that receives all
traffic.

haPolicy can be added only at backend service creation time. Once set up,
it cannot be deleted.

Note that haPolicy is not for load balancing, and therefore cannot be
specified with sessionAffinity, connectionTrackingPolicy, and
failoverPolicy.

haPolicy requires customers to be responsible for tracking backend
endpoint health and electing a leader among the healthy endpoints.
Therefore, haPolicy cannot be specified with healthChecks.

haPolicy can only be specified for External Passthrough Network Load
Balancers and Internal Passthrough Network Load Balancers. |
| `cdn_policy` | String | Cloud CDN configuration for this BackendService. Only available for
specified load balancer types. |
| `fingerprint` | String | Fingerprint of this resource. A hash of the contents stored in this object.
This field is used in optimistic locking. This field will be ignored when
inserting a BackendService. An up-to-date fingerprint must be provided in
order to update the BackendService, otherwise the request will
fail with error 412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve a BackendService. |
| `security_settings` | String | This field specifies the security settings that apply to this backend
service. This field is applicable to a global backend service with the
load_balancing_scheme set to INTERNAL_SELF_MANAGED. |
| `custom_metrics` | Vec<String> | List of custom metrics that are used for theWEIGHTED_ROUND_ROBIN locality_lb_policy. |
| `failover_policy` | String | Requires at least one backend instance group to be defined
as a backup (failover) backend.
For load balancers that have configurable failover:
[Internal passthrough Network Load
Balancers](https://cloud.google.com/load-balancing/docs/internal/failover-overview)
and [external passthrough Network Load
Balancers](https://cloud.google.com/load-balancing/docs/network/networklb-failover-overview).

failoverPolicy cannot be specified with haPolicy. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create backend_service
backend_service = provider.compute_api.Backend_service {
    project = "value"  # Project ID for this request.
}

# Access backend_service outputs
backend_service_id = backend_service.id
backend_service_backends = backend_service.backends
backend_service_log_config = backend_service.log_config
backend_service_compression_mode = backend_service.compression_mode
backend_service_name = backend_service.name
backend_service_params = backend_service.params
backend_service_security_policy = backend_service.security_policy
backend_service_kind = backend_service.kind
backend_service_outlier_detection = backend_service.outlier_detection
backend_service_service_bindings = backend_service.service_bindings
backend_service_custom_request_headers = backend_service.custom_request_headers
backend_service_health_checks = backend_service.health_checks
backend_service_load_balancing_scheme = backend_service.load_balancing_scheme
backend_service_max_stream_duration = backend_service.max_stream_duration
backend_service_network = backend_service.network
backend_service_custom_response_headers = backend_service.custom_response_headers
backend_service_self_link = backend_service.self_link
backend_service_subsetting = backend_service.subsetting
backend_service_creation_timestamp = backend_service.creation_timestamp
backend_service_timeout_sec = backend_service.timeout_sec
backend_service_connection_tracking_policy = backend_service.connection_tracking_policy
backend_service_session_affinity = backend_service.session_affinity
backend_service_port = backend_service.port
backend_service_iap = backend_service.iap
backend_service_self_link_with_id = backend_service.self_link_with_id
backend_service_ip_address_selection_policy = backend_service.ip_address_selection_policy
backend_service_circuit_breakers = backend_service.circuit_breakers
backend_service_metadatas = backend_service.metadatas
backend_service_external_managed_migration_state = backend_service.external_managed_migration_state
backend_service_tls_settings = backend_service.tls_settings
backend_service_dynamic_forwarding = backend_service.dynamic_forwarding
backend_service_used_by = backend_service.used_by
backend_service_connection_draining = backend_service.connection_draining
backend_service_external_managed_migration_testing_percentage = backend_service.external_managed_migration_testing_percentage
backend_service_region = backend_service.region
backend_service_strong_session_affinity_cookie = backend_service.strong_session_affinity_cookie
backend_service_locality_lb_policy = backend_service.locality_lb_policy
backend_service_vpc_network_scope = backend_service.vpc_network_scope
backend_service_description = backend_service.description
backend_service_edge_security_policy = backend_service.edge_security_policy
backend_service_network_pass_through_lb_traffic_policy = backend_service.network_pass_through_lb_traffic_policy
backend_service_allow_multinetwork = backend_service.allow_multinetwork
backend_service_enable_cdn = backend_service.enable_cdn
backend_service_affinity_cookie_ttl_sec = backend_service.affinity_cookie_ttl_sec
backend_service_protocol = backend_service.protocol
backend_service_service_lb_policy = backend_service.service_lb_policy
backend_service_consistent_hash = backend_service.consistent_hash
backend_service_locality_lb_policies = backend_service.locality_lb_policies
backend_service_id = backend_service.id
backend_service_port_name = backend_service.port_name
backend_service_ha_policy = backend_service.ha_policy
backend_service_cdn_policy = backend_service.cdn_policy
backend_service_fingerprint = backend_service.fingerprint
backend_service_security_settings = backend_service.security_settings
backend_service_custom_metrics = backend_service.custom_metrics
backend_service_failover_policy = backend_service.failover_policy
```

---


### Disk

Creates a persistent disk in the specified project using the data
in the request. You can create a disk from a source
(sourceImage, sourceSnapshot, orsourceDisk) or create an empty 500 GB data disk by
omitting all properties. You can also create a disk that is larger than
the default size by specifying the sizeGb property.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `source_consistency_group_policy_id` | String |  | [Output Only] ID of the DiskConsistencyGroupPolicy for a secondary disk
that was created using a consistency group. |
| `physical_block_size_bytes` | String |  | Physical block size of the persistent disk, in bytes.
If not present in a request, a default value is used.
The currently supported size is 4096, other sizes may be added in
the future.
If an unsupported value is requested, the error message will list
the supported values for the caller's project. |
| `provisioned_iops` | String |  | Indicates how many IOPS to provision for the disk. This sets the number
of I/O operations per second that the disk can handle. Values must be
between 10,000 and 120,000. For more details, see theExtreme persistent
disk documentation. |
| `source_snapshot_encryption_key` | String |  | Thecustomer-supplied
encryption key of the source snapshot. Required if the source snapshot
is protected by a customer-supplied encryption key. |
| `async_secondary_disks` | HashMap<String, String> |  | [Output Only] A list of disks this disk is asynchronously replicated to. |
| `source_image` | String |  | The source image used to create this disk. If the source image is
deleted, this field will not be set.

To create a disk with one of the public operating system images, specify
the image by its family name. For example, specifyfamily/debian-9 to use the latest Debian 9 image:

projects/debian-cloud/global/images/family/debian-9


Alternatively, use a specific version of a public operating system image:

projects/debian-cloud/global/images/debian-9-stretch-vYYYYMMDD


To create a disk with a custom image that you created, specify the
image name in the following format:

global/images/my-custom-image


You can also specify a custom image by its image family, which returns
the latest version of the image in that family. Replace the image name
with family/family-name:

global/images/family/my-image-family |
| `source_disk_id` | String |  | [Output Only] The unique ID of the disk used to create this disk. This
value identifies the exact disk that was used to create this persistent
disk. For example, if you created the persistent disk from a disk that
was later deleted and recreated under the same name, the source disk ID
would identify the exact version of the disk that was used. |
| `source_instant_snapshot_id` | String |  | [Output Only] The unique ID of the instant snapshot used to create this
disk. This value identifies the exact instant snapshot that was used to
create this persistent disk. For example, if you created the persistent
disk from an instant snapshot that was later deleted and recreated under
the same name, the source instant snapshot ID would identify the exact
version of the instant snapshot that was used. |
| `params` | String |  | Input only. [Input Only] Additional params passed with the request, but not persisted
as part of resource payload. |
| `architecture` | String |  | The architecture of the disk. Valid values are
ARM64 or X86_64. |
| `guest_os_features` | Vec<String> |  | A list of features to enable on the guest operating system. Applicable
only for bootable images. Read
Enabling guest operating system features to see a list of available
options. |
| `locked` | bool |  | [Output Only] The field indicates if the disk is created from a locked
source image. Attachment of a disk created from a locked source image will
cause the following operations to become irreversibly prohibited:
   
   - R/W or R/O disk attachment to any other instance
   - Disk detachment. And the disk can only be deleted when the instance
   is deleted
   - Creation of images or snapshots
   - Disk cloning



Furthermore, the instance with at least one disk with locked flag set to
true will be prohibited from performing the operations below:
   
   - Further attachment of secondary disks.
   - Detachment of any disks
   - Create machine images
   - Create instance template
   - Delete the instance with --keep-disk parameter set to true for locked
   disks
   - Attach a locked disk with --auto-delete parameter set to false |
| `kind` | String |  | [Output Only] Type of the resource. Always compute#disk for
disks. |
| `storage_pool` | String |  | The storage pool in which the new disk is created. You can provide
this as a partial or full URL to the resource. For example, the following
are valid values:
   
   
     - https://www.googleapis.com/compute/v1/projects/project/zones/zone/storagePools/storagePool
     - projects/project/zones/zone/storagePools/storagePool 
   - zones/zone/storagePools/storagePool |
| `options` | String |  | Internal use only. |
| `disk_encryption_key` | String |  | Encrypts the disk using a
customer-supplied encryption key or a 
customer-managed encryption key.

Encryption keys do not protect access to metadata of the disk.

After you encrypt a disk with a customer-supplied key, you must provide the
same key if you use the disk later. For example, to create a disk snapshot,
to create a disk image, to create a machine image, or to attach the disk to
a virtual machine.

After you encrypt a disk with a customer-managed key, thediskEncryptionKey.kmsKeyName is set to a key *version*
name once the disk is created. The disk is encrypted with this version of
the key. In the response, diskEncryptionKey.kmsKeyName appears
in the following format:

"diskEncryptionKey.kmsKeyName":
"projects/kms_project_id/locations/region/keyRings/
key_region/cryptoKeys/key
/cryptoKeysVersions/version

If you do not provide an encryption key when creating the disk, then the 
disk is encrypted using an automatically generated key and you don't need 
to provide a key to use the disk later. |
| `labels` | HashMap<String, String> |  | Labels to apply to this disk. These can be later modified by
the setLabels method. |
| `location_hint` | String |  | An opaque location hint used to place the disk close to other resources.
This field is for use by internal tools that use the public API. |
| `self_link_with_id` | String |  | [Output Only] Server-defined URL for this resource's resource id. |
| `license_codes` | Vec<String> |  | Integer license codes indicating which licenses are attached to this disk. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `interface` | String |  | [Deprecated] Specifies the disk interface to use for attaching this disk,
which is either SCSI or NVME. The default isSCSI. |
| `source_instant_snapshot` | String |  | The source instant snapshot used to create this disk. You can provide
this as a partial or full URL to the resource. For example, the following
are valid values:
   
   
     - https://www.googleapis.com/compute/v1/projects/project/zones/zone/instantSnapshots/instantSnapshot
     - projects/project/zones/zone/instantSnapshots/instantSnapshot 
   - zones/zone/instantSnapshots/instantSnapshot |
| `resource_policies` | Vec<String> |  | Resource policies applied to this disk for automatic snapshot creations. |
| `erase_windows_vss_signature` | bool |  | Specifies whether the disk restored from a source snapshot should erase
Windows specific VSS signature. |
| `status` | String |  | [Output Only] The status of disk creation.
   
   
     - CREATING: Disk is provisioning.
     - RESTORING: Source data is being copied into the
     disk.
     - FAILED: Disk creation failed.
     - READY: Disk is ready for use.
     - DELETING: Disk is deleting. |
| `multi_writer` | bool |  | Indicates whether or not the disk can be read/write attached to
more than one instance. |
| `region` | String |  | [Output Only] URL of the region where the disk resides. Only applicable for
regional resources.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `source_storage_object` | String |  | The full Google Cloud Storage URI where the disk image is stored. This file
must be a gzip-compressed tarball whose name ends in .tar.gz or virtual
machine disk whose name ends in vmdk. Valid URIs may start with gs:// or
https://storage.googleapis.com/. This flag is not optimized for creating
multiple disks from a source storage object. To create many disks from a
source storage object, use gcloud compute images
import instead. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `satisfies_pzi` | bool |  | Output only. Reserved for future use. |
| `label_fingerprint` | String |  | A fingerprint for the labels being applied to this disk, which is
essentially a hash of the labels set used for optimistic locking. The
fingerprint is initially generated by Compute Engine and changes after
every request to modify or update labels. You must always provide an
up-to-date fingerprint hash in order to update or change labels,
otherwise the request will fail with error412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve a disk. |
| `source_image_id` | String |  | [Output Only] The ID value of the image used to create this disk. This
value identifies the exact image that was used to create this persistent
disk. For example, if you created the persistent disk from an image that
was later deleted and recreated under the same name, the source image ID
would identify the exact version of the image that was used. |
| `source_snapshot_id` | String |  | [Output Only] The unique ID of the snapshot used to create this disk. This
value identifies the exact snapshot that was used to create this persistent
disk. For example, if you created the persistent disk from a snapshot that
was later deleted and recreated under the same name, the source snapshot ID
would identify the exact version of the snapshot that was used. |
| `type` | String |  | URL of the disk type resource describing which disk type to use to create
the disk. Provide this when creating the disk. For example:projects/project/zones/zone/diskTypes/pd-ssd. See Persistent disk
types. |
| `zone` | String |  | [Output Only] URL of the zone where the disk resides.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `user_licenses` | Vec<String> |  | A list of publicly visible user-licenses. Unlike regular licenses, user
provided licenses can be modified after the disk is created. This includes
a list of URLs to the license resource. For example, to provide a debian
license:

https://www.googleapis.com/compute/v1/projects/debian-cloud/global/licenses/debian-9-stretch |
| `users` | Vec<String> |  | [Output Only] Links to the users of the disk (attached instances)
in form:projects/project/zones/zone/instances/instance |
| `provisioned_throughput` | String |  | Indicates how much throughput to provision for the disk. This sets the
number of throughput mb per second that the disk can handle. Values must be
greater than or equal to 1. |
| `enable_confidential_compute` | bool |  | Whether this disk is using confidential compute mode. |
| `storage_type` | String |  | [Deprecated] Storage type of the persistent disk. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `source_image_encryption_key` | String |  | Thecustomer-supplied
encryption key of the source image. Required if the source image is
protected by a customer-supplied encryption key. |
| `resource_status` | String |  | [Output Only] Status information for the disk resource. |
| `satisfies_pzs` | bool |  | [Output Only] Reserved for future use. |
| `last_detach_timestamp` | String |  | [Output Only] Last detach timestamp inRFC3339
text format. |
| `source_machine_image_disk_device_name` | String |  | Input only. The device name of a disk within a given machine image. The
source_machine_image must be specified. |
| `async_primary_disk` | String |  | Disk asynchronously replicated into this disk. |
| `size_gb` | String |  | Size, in GB, of the persistent disk. You can specify
this field when creating a persistent disk using thesourceImage, sourceSnapshot, orsourceDisk parameter, or specify it alone to create an empty
persistent disk.

If you specify this field along with a source, the value ofsizeGb must not be less than the size of the
source.
Acceptable values are greater than 0. |
| `last_attach_timestamp` | String |  | [Output Only] Last attach timestamp inRFC3339
text format. |
| `source_machine_image_encryption_key` | String |  | Thecustomer-supplied
encryption key of the source machine image. Required if the source
machine image is protected by a customer-supplied encryption key. |
| `source_machine_image_id` | String |  | [Output Only] The unique ID of the machine image used to create this disk.
This value identifies the exact machine image that was used to create this
persistent disk. For example, if you created the persistent disk from a
machine image that was later deleted and recreated under the same name, the
source machine image ID would identify the exact version of the machine
image that was used. |
| `source_disk` | String |  | The source disk used to create this disk. You can provide this as a
partial or full URL to the resource. For example, the following are valid
values:
   
   
     - 
       https://www.googleapis.com/compute/v1/projects/project/zones/zone/disks/disk
     
     - 
       https://www.googleapis.com/compute/v1/projects/project/regions/region/disks/disk
     
     - 
       projects/project/zones/zone/disks/disk
     
     - 
       projects/project/regions/region/disks/disk
     
     - 
       zones/zone/disks/disk
     
     - 
       regions/region/disks/disk |
| `self_link` | String |  | [Output Only] Server-defined fully-qualified URL for this resource. |
| `name` | String |  | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?`
which means the first character must be a lowercase letter, and all
following characters must be a dash, lowercase letter, or digit, except
the last character, which cannot be a dash. |
| `source_consistency_group_policy` | String |  | [Output Only] URL of the DiskConsistencyGroupPolicy for a secondary disk
that was created using a consistency group. |
| `source_machine_image` | String |  | The machine image to create the disk from. You can provide this as a
partial or full URL to the resource. For example, the following are valid
values:
   
   
     - https://www.googleapis.com/compute/v1/projects/project/global/machineImages/machineImage 
   - projects/project/global/machineImages/machineImage
     - global/machineImages/machineImage |
| `licenses` | Vec<String> |  | A list of publicly visible licenses. Reserved for Google's use. |
| `source_snapshot` | String |  | The source snapshot used to create this disk. You can provide this as a
partial or full URL to the resource. For example, the following are valid
values:
   
   
     - https://www.googleapis.com/compute/v1/projects/project/global/snapshots/snapshot 
   - projects/project/global/snapshots/snapshot
     - global/snapshots/snapshot |
| `access_mode` | String |  | The access mode of the disk.
   
   
     - READ_WRITE_SINGLE: The default AccessMode, means the
     disk can be attached to single instance in RW mode.
     - READ_WRITE_MANY: The AccessMode means the disk can be
     attached to multiple instances in RW mode.
     - READ_ONLY_MANY: The AccessMode means the disk can be
     attached to multiple instances in RO mode.


The AccessMode is only valid for Hyperdisk disk types. |
| `replica_zones` | Vec<String> |  | URLs of the zones where the disk should be replicated to. Only applicable
for regional resources. |
| `zone` | String | ✅ | The name of the zone for this request. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `source_consistency_group_policy_id` | String | [Output Only] ID of the DiskConsistencyGroupPolicy for a secondary disk
that was created using a consistency group. |
| `physical_block_size_bytes` | String | Physical block size of the persistent disk, in bytes.
If not present in a request, a default value is used.
The currently supported size is 4096, other sizes may be added in
the future.
If an unsupported value is requested, the error message will list
the supported values for the caller's project. |
| `provisioned_iops` | String | Indicates how many IOPS to provision for the disk. This sets the number
of I/O operations per second that the disk can handle. Values must be
between 10,000 and 120,000. For more details, see theExtreme persistent
disk documentation. |
| `source_snapshot_encryption_key` | String | Thecustomer-supplied
encryption key of the source snapshot. Required if the source snapshot
is protected by a customer-supplied encryption key. |
| `async_secondary_disks` | HashMap<String, String> | [Output Only] A list of disks this disk is asynchronously replicated to. |
| `source_image` | String | The source image used to create this disk. If the source image is
deleted, this field will not be set.

To create a disk with one of the public operating system images, specify
the image by its family name. For example, specifyfamily/debian-9 to use the latest Debian 9 image:

projects/debian-cloud/global/images/family/debian-9


Alternatively, use a specific version of a public operating system image:

projects/debian-cloud/global/images/debian-9-stretch-vYYYYMMDD


To create a disk with a custom image that you created, specify the
image name in the following format:

global/images/my-custom-image


You can also specify a custom image by its image family, which returns
the latest version of the image in that family. Replace the image name
with family/family-name:

global/images/family/my-image-family |
| `source_disk_id` | String | [Output Only] The unique ID of the disk used to create this disk. This
value identifies the exact disk that was used to create this persistent
disk. For example, if you created the persistent disk from a disk that
was later deleted and recreated under the same name, the source disk ID
would identify the exact version of the disk that was used. |
| `source_instant_snapshot_id` | String | [Output Only] The unique ID of the instant snapshot used to create this
disk. This value identifies the exact instant snapshot that was used to
create this persistent disk. For example, if you created the persistent
disk from an instant snapshot that was later deleted and recreated under
the same name, the source instant snapshot ID would identify the exact
version of the instant snapshot that was used. |
| `params` | String | Input only. [Input Only] Additional params passed with the request, but not persisted
as part of resource payload. |
| `architecture` | String | The architecture of the disk. Valid values are
ARM64 or X86_64. |
| `guest_os_features` | Vec<String> | A list of features to enable on the guest operating system. Applicable
only for bootable images. Read
Enabling guest operating system features to see a list of available
options. |
| `locked` | bool | [Output Only] The field indicates if the disk is created from a locked
source image. Attachment of a disk created from a locked source image will
cause the following operations to become irreversibly prohibited:
   
   - R/W or R/O disk attachment to any other instance
   - Disk detachment. And the disk can only be deleted when the instance
   is deleted
   - Creation of images or snapshots
   - Disk cloning



Furthermore, the instance with at least one disk with locked flag set to
true will be prohibited from performing the operations below:
   
   - Further attachment of secondary disks.
   - Detachment of any disks
   - Create machine images
   - Create instance template
   - Delete the instance with --keep-disk parameter set to true for locked
   disks
   - Attach a locked disk with --auto-delete parameter set to false |
| `kind` | String | [Output Only] Type of the resource. Always compute#disk for
disks. |
| `storage_pool` | String | The storage pool in which the new disk is created. You can provide
this as a partial or full URL to the resource. For example, the following
are valid values:
   
   
     - https://www.googleapis.com/compute/v1/projects/project/zones/zone/storagePools/storagePool
     - projects/project/zones/zone/storagePools/storagePool 
   - zones/zone/storagePools/storagePool |
| `options` | String | Internal use only. |
| `disk_encryption_key` | String | Encrypts the disk using a
customer-supplied encryption key or a 
customer-managed encryption key.

Encryption keys do not protect access to metadata of the disk.

After you encrypt a disk with a customer-supplied key, you must provide the
same key if you use the disk later. For example, to create a disk snapshot,
to create a disk image, to create a machine image, or to attach the disk to
a virtual machine.

After you encrypt a disk with a customer-managed key, thediskEncryptionKey.kmsKeyName is set to a key *version*
name once the disk is created. The disk is encrypted with this version of
the key. In the response, diskEncryptionKey.kmsKeyName appears
in the following format:

"diskEncryptionKey.kmsKeyName":
"projects/kms_project_id/locations/region/keyRings/
key_region/cryptoKeys/key
/cryptoKeysVersions/version

If you do not provide an encryption key when creating the disk, then the 
disk is encrypted using an automatically generated key and you don't need 
to provide a key to use the disk later. |
| `labels` | HashMap<String, String> | Labels to apply to this disk. These can be later modified by
the setLabels method. |
| `location_hint` | String | An opaque location hint used to place the disk close to other resources.
This field is for use by internal tools that use the public API. |
| `self_link_with_id` | String | [Output Only] Server-defined URL for this resource's resource id. |
| `license_codes` | Vec<String> | Integer license codes indicating which licenses are attached to this disk. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `interface` | String | [Deprecated] Specifies the disk interface to use for attaching this disk,
which is either SCSI or NVME. The default isSCSI. |
| `source_instant_snapshot` | String | The source instant snapshot used to create this disk. You can provide
this as a partial or full URL to the resource. For example, the following
are valid values:
   
   
     - https://www.googleapis.com/compute/v1/projects/project/zones/zone/instantSnapshots/instantSnapshot
     - projects/project/zones/zone/instantSnapshots/instantSnapshot 
   - zones/zone/instantSnapshots/instantSnapshot |
| `resource_policies` | Vec<String> | Resource policies applied to this disk for automatic snapshot creations. |
| `erase_windows_vss_signature` | bool | Specifies whether the disk restored from a source snapshot should erase
Windows specific VSS signature. |
| `status` | String | [Output Only] The status of disk creation.
   
   
     - CREATING: Disk is provisioning.
     - RESTORING: Source data is being copied into the
     disk.
     - FAILED: Disk creation failed.
     - READY: Disk is ready for use.
     - DELETING: Disk is deleting. |
| `multi_writer` | bool | Indicates whether or not the disk can be read/write attached to
more than one instance. |
| `region` | String | [Output Only] URL of the region where the disk resides. Only applicable for
regional resources.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `source_storage_object` | String | The full Google Cloud Storage URI where the disk image is stored. This file
must be a gzip-compressed tarball whose name ends in .tar.gz or virtual
machine disk whose name ends in vmdk. Valid URIs may start with gs:// or
https://storage.googleapis.com/. This flag is not optimized for creating
multiple disks from a source storage object. To create many disks from a
source storage object, use gcloud compute images
import instead. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `satisfies_pzi` | bool | Output only. Reserved for future use. |
| `label_fingerprint` | String | A fingerprint for the labels being applied to this disk, which is
essentially a hash of the labels set used for optimistic locking. The
fingerprint is initially generated by Compute Engine and changes after
every request to modify or update labels. You must always provide an
up-to-date fingerprint hash in order to update or change labels,
otherwise the request will fail with error412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve a disk. |
| `source_image_id` | String | [Output Only] The ID value of the image used to create this disk. This
value identifies the exact image that was used to create this persistent
disk. For example, if you created the persistent disk from an image that
was later deleted and recreated under the same name, the source image ID
would identify the exact version of the image that was used. |
| `source_snapshot_id` | String | [Output Only] The unique ID of the snapshot used to create this disk. This
value identifies the exact snapshot that was used to create this persistent
disk. For example, if you created the persistent disk from a snapshot that
was later deleted and recreated under the same name, the source snapshot ID
would identify the exact version of the snapshot that was used. |
| `type` | String | URL of the disk type resource describing which disk type to use to create
the disk. Provide this when creating the disk. For example:projects/project/zones/zone/diskTypes/pd-ssd. See Persistent disk
types. |
| `zone` | String | [Output Only] URL of the zone where the disk resides.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `user_licenses` | Vec<String> | A list of publicly visible user-licenses. Unlike regular licenses, user
provided licenses can be modified after the disk is created. This includes
a list of URLs to the license resource. For example, to provide a debian
license:

https://www.googleapis.com/compute/v1/projects/debian-cloud/global/licenses/debian-9-stretch |
| `users` | Vec<String> | [Output Only] Links to the users of the disk (attached instances)
in form:projects/project/zones/zone/instances/instance |
| `provisioned_throughput` | String | Indicates how much throughput to provision for the disk. This sets the
number of throughput mb per second that the disk can handle. Values must be
greater than or equal to 1. |
| `enable_confidential_compute` | bool | Whether this disk is using confidential compute mode. |
| `storage_type` | String | [Deprecated] Storage type of the persistent disk. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `source_image_encryption_key` | String | Thecustomer-supplied
encryption key of the source image. Required if the source image is
protected by a customer-supplied encryption key. |
| `resource_status` | String | [Output Only] Status information for the disk resource. |
| `satisfies_pzs` | bool | [Output Only] Reserved for future use. |
| `last_detach_timestamp` | String | [Output Only] Last detach timestamp inRFC3339
text format. |
| `source_machine_image_disk_device_name` | String | Input only. The device name of a disk within a given machine image. The
source_machine_image must be specified. |
| `async_primary_disk` | String | Disk asynchronously replicated into this disk. |
| `size_gb` | String | Size, in GB, of the persistent disk. You can specify
this field when creating a persistent disk using thesourceImage, sourceSnapshot, orsourceDisk parameter, or specify it alone to create an empty
persistent disk.

If you specify this field along with a source, the value ofsizeGb must not be less than the size of the
source.
Acceptable values are greater than 0. |
| `last_attach_timestamp` | String | [Output Only] Last attach timestamp inRFC3339
text format. |
| `source_machine_image_encryption_key` | String | Thecustomer-supplied
encryption key of the source machine image. Required if the source
machine image is protected by a customer-supplied encryption key. |
| `source_machine_image_id` | String | [Output Only] The unique ID of the machine image used to create this disk.
This value identifies the exact machine image that was used to create this
persistent disk. For example, if you created the persistent disk from a
machine image that was later deleted and recreated under the same name, the
source machine image ID would identify the exact version of the machine
image that was used. |
| `source_disk` | String | The source disk used to create this disk. You can provide this as a
partial or full URL to the resource. For example, the following are valid
values:
   
   
     - 
       https://www.googleapis.com/compute/v1/projects/project/zones/zone/disks/disk
     
     - 
       https://www.googleapis.com/compute/v1/projects/project/regions/region/disks/disk
     
     - 
       projects/project/zones/zone/disks/disk
     
     - 
       projects/project/regions/region/disks/disk
     
     - 
       zones/zone/disks/disk
     
     - 
       regions/region/disks/disk |
| `self_link` | String | [Output Only] Server-defined fully-qualified URL for this resource. |
| `name` | String | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?`
which means the first character must be a lowercase letter, and all
following characters must be a dash, lowercase letter, or digit, except
the last character, which cannot be a dash. |
| `source_consistency_group_policy` | String | [Output Only] URL of the DiskConsistencyGroupPolicy for a secondary disk
that was created using a consistency group. |
| `source_machine_image` | String | The machine image to create the disk from. You can provide this as a
partial or full URL to the resource. For example, the following are valid
values:
   
   
     - https://www.googleapis.com/compute/v1/projects/project/global/machineImages/machineImage 
   - projects/project/global/machineImages/machineImage
     - global/machineImages/machineImage |
| `licenses` | Vec<String> | A list of publicly visible licenses. Reserved for Google's use. |
| `source_snapshot` | String | The source snapshot used to create this disk. You can provide this as a
partial or full URL to the resource. For example, the following are valid
values:
   
   
     - https://www.googleapis.com/compute/v1/projects/project/global/snapshots/snapshot 
   - projects/project/global/snapshots/snapshot
     - global/snapshots/snapshot |
| `access_mode` | String | The access mode of the disk.
   
   
     - READ_WRITE_SINGLE: The default AccessMode, means the
     disk can be attached to single instance in RW mode.
     - READ_WRITE_MANY: The AccessMode means the disk can be
     attached to multiple instances in RW mode.
     - READ_ONLY_MANY: The AccessMode means the disk can be
     attached to multiple instances in RO mode.


The AccessMode is only valid for Hyperdisk disk types. |
| `replica_zones` | Vec<String> | URLs of the zones where the disk should be replicated to. Only applicable
for regional resources. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create disk
disk = provider.compute_api.Disk {
    zone = "value"  # The name of the zone for this request.
    project = "value"  # Project ID for this request.
}

# Access disk outputs
disk_id = disk.id
disk_source_consistency_group_policy_id = disk.source_consistency_group_policy_id
disk_physical_block_size_bytes = disk.physical_block_size_bytes
disk_provisioned_iops = disk.provisioned_iops
disk_source_snapshot_encryption_key = disk.source_snapshot_encryption_key
disk_async_secondary_disks = disk.async_secondary_disks
disk_source_image = disk.source_image
disk_source_disk_id = disk.source_disk_id
disk_source_instant_snapshot_id = disk.source_instant_snapshot_id
disk_params = disk.params
disk_architecture = disk.architecture
disk_guest_os_features = disk.guest_os_features
disk_locked = disk.locked
disk_kind = disk.kind
disk_storage_pool = disk.storage_pool
disk_options = disk.options
disk_disk_encryption_key = disk.disk_encryption_key
disk_labels = disk.labels
disk_location_hint = disk.location_hint
disk_self_link_with_id = disk.self_link_with_id
disk_license_codes = disk.license_codes
disk_creation_timestamp = disk.creation_timestamp
disk_interface = disk.interface
disk_source_instant_snapshot = disk.source_instant_snapshot
disk_resource_policies = disk.resource_policies
disk_erase_windows_vss_signature = disk.erase_windows_vss_signature
disk_status = disk.status
disk_multi_writer = disk.multi_writer
disk_region = disk.region
disk_source_storage_object = disk.source_storage_object
disk_description = disk.description
disk_satisfies_pzi = disk.satisfies_pzi
disk_label_fingerprint = disk.label_fingerprint
disk_source_image_id = disk.source_image_id
disk_source_snapshot_id = disk.source_snapshot_id
disk_type = disk.type
disk_zone = disk.zone
disk_user_licenses = disk.user_licenses
disk_users = disk.users
disk_provisioned_throughput = disk.provisioned_throughput
disk_enable_confidential_compute = disk.enable_confidential_compute
disk_storage_type = disk.storage_type
disk_id = disk.id
disk_source_image_encryption_key = disk.source_image_encryption_key
disk_resource_status = disk.resource_status
disk_satisfies_pzs = disk.satisfies_pzs
disk_last_detach_timestamp = disk.last_detach_timestamp
disk_source_machine_image_disk_device_name = disk.source_machine_image_disk_device_name
disk_async_primary_disk = disk.async_primary_disk
disk_size_gb = disk.size_gb
disk_last_attach_timestamp = disk.last_attach_timestamp
disk_source_machine_image_encryption_key = disk.source_machine_image_encryption_key
disk_source_machine_image_id = disk.source_machine_image_id
disk_source_disk = disk.source_disk
disk_self_link = disk.self_link
disk_name = disk.name
disk_source_consistency_group_policy = disk.source_consistency_group_policy
disk_source_machine_image = disk.source_machine_image
disk_licenses = disk.licenses
disk_source_snapshot = disk.source_snapshot
disk_access_mode = disk.access_mode
disk_replica_zones = disk.replica_zones
```

---


### Reliability_risk

Returns the specified ReliabilityRisk resource.

**Operations**: ✅ Read

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `self_link_with_id` | String | [Output Only] Server-defined URL for this resource with the resource id. |
| `name` | String | Name of the resource. The name must be 1-63 characters long and
comply withRFC1035. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `kind` | String | [Output Only] Type of resource. Always compute#reliabilityRisk
for reliability risks. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `recommendation` | String | The recommendations to mitigate the risk. |
| `details` | String | [Output Only] Details of the reliability risk resource |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `description` | String | An optional textual description of the resource; provided when the
resource is created. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Access reliability_risk outputs
reliability_risk_id = reliability_risk.id
reliability_risk_self_link_with_id = reliability_risk.self_link_with_id
reliability_risk_name = reliability_risk.name
reliability_risk_id = reliability_risk.id
reliability_risk_kind = reliability_risk.kind
reliability_risk_self_link = reliability_risk.self_link
reliability_risk_recommendation = reliability_risk.recommendation
reliability_risk_details = reliability_risk.details
reliability_risk_creation_timestamp = reliability_risk.creation_timestamp
reliability_risk_description = reliability_risk.description
```

---


### Instance_group

Creates an instance group in the specified project using the
parameters that are included in the request.

**Operations**: ✅ Create ✅ Read ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `creation_timestamp` | String |  | [Output Only] The creation timestamp for this instance group inRFC3339
text format. |
| `id` | String |  | [Output Only] A unique identifier for this instance group, generated
by the server. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `self_link_with_id` | String |  | [Output Only] Server-defined URL for this resource with the resource id. |
| `subnetwork` | String |  | [Output Only] The URL of the subnetwork to which all instances in the
instance group belong. If your instance has multiple network interfaces,
then the network and subnetwork fields only refer to the
network and subnet used by your primary interface (nic0). |
| `self_link` | String |  | [Output Only] The URL for this instance group. The server generates
this URL. |
| `size` | i64 |  | [Output Only] The total number of instances in the instance group. |
| `zone` | String |  | [Output Only] The URL of thezone
where the instance group is located (for zonal resources). |
| `name` | String |  | The name of the instance group. The name must be 1-63 characters
long, and comply withRFC1035. |
| `network` | String |  | [Output Only] The URL of the network to which all instances in the
instance group belong. If your instance has multiple network interfaces,
then the network and subnetwork fields only refer to the
network and subnet used by your primary interface (nic0). |
| `fingerprint` | String |  | [Output Only] The fingerprint of the named ports. The system
uses this fingerprint to detect conflicts when multiple users change the
named ports concurrently. |
| `kind` | String |  | [Output Only] The resource type, which is alwayscompute#instanceGroup for instance groups. |
| `named_ports` | Vec<String> |  | Optional. Assigns a name to a port number. For example:{name: "http", port: 80}

This
allows the system to reference ports by the assigned name instead of a
port number. Named ports can also contain multiple ports. For example:[{name: "app1", port: 8080}, {name:
"app1", port: 8081}, {name: "app2", port:
8082}]

Named ports apply to all instances in this instance group. |
| `region` | String |  | [Output Only] The URL of theregion
where the instance group is located (for regional resources). |
| `zone` | String | ✅ | The name of the zone
where you want to create the instance group. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `creation_timestamp` | String | [Output Only] The creation timestamp for this instance group inRFC3339
text format. |
| `id` | String | [Output Only] A unique identifier for this instance group, generated
by the server. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `self_link_with_id` | String | [Output Only] Server-defined URL for this resource with the resource id. |
| `subnetwork` | String | [Output Only] The URL of the subnetwork to which all instances in the
instance group belong. If your instance has multiple network interfaces,
then the network and subnetwork fields only refer to the
network and subnet used by your primary interface (nic0). |
| `self_link` | String | [Output Only] The URL for this instance group. The server generates
this URL. |
| `size` | i64 | [Output Only] The total number of instances in the instance group. |
| `zone` | String | [Output Only] The URL of thezone
where the instance group is located (for zonal resources). |
| `name` | String | The name of the instance group. The name must be 1-63 characters
long, and comply withRFC1035. |
| `network` | String | [Output Only] The URL of the network to which all instances in the
instance group belong. If your instance has multiple network interfaces,
then the network and subnetwork fields only refer to the
network and subnet used by your primary interface (nic0). |
| `fingerprint` | String | [Output Only] The fingerprint of the named ports. The system
uses this fingerprint to detect conflicts when multiple users change the
named ports concurrently. |
| `kind` | String | [Output Only] The resource type, which is alwayscompute#instanceGroup for instance groups. |
| `named_ports` | Vec<String> | Optional. Assigns a name to a port number. For example:{name: "http", port: 80}

This
allows the system to reference ports by the assigned name instead of a
port number. Named ports can also contain multiple ports. For example:[{name: "app1", port: 8080}, {name:
"app1", port: 8081}, {name: "app2", port:
8082}]

Named ports apply to all instances in this instance group. |
| `region` | String | [Output Only] The URL of theregion
where the instance group is located (for regional resources). |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create instance_group
instance_group = provider.compute_api.Instance_group {
    zone = "value"  # The name of the zone
where you want to create the instance group.
    project = "value"  # Project ID for this request.
}

# Access instance_group outputs
instance_group_id = instance_group.id
instance_group_creation_timestamp = instance_group.creation_timestamp
instance_group_id = instance_group.id
instance_group_description = instance_group.description
instance_group_self_link_with_id = instance_group.self_link_with_id
instance_group_subnetwork = instance_group.subnetwork
instance_group_self_link = instance_group.self_link
instance_group_size = instance_group.size
instance_group_zone = instance_group.zone
instance_group_name = instance_group.name
instance_group_network = instance_group.network
instance_group_fingerprint = instance_group.fingerprint
instance_group_kind = instance_group.kind
instance_group_named_ports = instance_group.named_ports
instance_group_region = instance_group.region
```

---


### Target_vpn_gateway

Creates a target VPN gateway in the specified project and region using
the data included in the request.

**Operations**: ✅ Create ✅ Read ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `labels` | HashMap<String, String> |  | Labels for this resource. These can only be added or modified by thesetLabels method. Each label key/value pair must comply withRFC1035.
Label values may be empty. |
| `params` | String |  | Input only. [Input Only] Additional params passed with the request, but not persisted
as part of resource payload. |
| `label_fingerprint` | String |  | A fingerprint for the labels being applied to this TargetVpnGateway, which
is essentially a hash of the labels set used for optimistic locking. The
fingerprint is initially generated by Compute Engine and changes after
every request to modify or update labels. You must always provide an
up-to-date fingerprint hash in order to update or change labels,
otherwise the request will fail with error412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve a TargetVpnGateway. |
| `name` | String |  | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `tunnels` | Vec<String> |  | [Output Only] A list of URLs to VpnTunnel resources. VpnTunnels are
created using the compute.vpntunnels.insert method and
associated with a VPN gateway. |
| `kind` | String |  | [Output Only] Type of resource. Alwayscompute#targetVpnGateway for target VPN gateways. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `forwarding_rules` | Vec<String> |  | [Output Only] A list of URLs to the ForwardingRule resources.
ForwardingRules are created usingcompute.forwardingRules.insert and associated with a VPN
gateway. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `network` | String |  | URL of the network to which this VPN gateway is attached. Provided by the
client when the VPN gateway is created. |
| `region` | String |  | [Output Only] URL of the region where the target VPN gateway resides.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `status` | String |  | [Output Only] The status of the VPN gateway,
which can be one of the following: CREATING, READY, FAILED, or DELETING. |
| `project` | String | ✅ | Project ID for this request. |
| `region` | String | ✅ | Name of the region for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `labels` | HashMap<String, String> | Labels for this resource. These can only be added or modified by thesetLabels method. Each label key/value pair must comply withRFC1035.
Label values may be empty. |
| `params` | String | Input only. [Input Only] Additional params passed with the request, but not persisted
as part of resource payload. |
| `label_fingerprint` | String | A fingerprint for the labels being applied to this TargetVpnGateway, which
is essentially a hash of the labels set used for optimistic locking. The
fingerprint is initially generated by Compute Engine and changes after
every request to modify or update labels. You must always provide an
up-to-date fingerprint hash in order to update or change labels,
otherwise the request will fail with error412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve a TargetVpnGateway. |
| `name` | String | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `tunnels` | Vec<String> | [Output Only] A list of URLs to VpnTunnel resources. VpnTunnels are
created using the compute.vpntunnels.insert method and
associated with a VPN gateway. |
| `kind` | String | [Output Only] Type of resource. Alwayscompute#targetVpnGateway for target VPN gateways. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `forwarding_rules` | Vec<String> | [Output Only] A list of URLs to the ForwardingRule resources.
ForwardingRules are created usingcompute.forwardingRules.insert and associated with a VPN
gateway. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `network` | String | URL of the network to which this VPN gateway is attached. Provided by the
client when the VPN gateway is created. |
| `region` | String | [Output Only] URL of the region where the target VPN gateway resides.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `status` | String | [Output Only] The status of the VPN gateway,
which can be one of the following: CREATING, READY, FAILED, or DELETING. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create target_vpn_gateway
target_vpn_gateway = provider.compute_api.Target_vpn_gateway {
    project = "value"  # Project ID for this request.
    region = "value"  # Name of the region for this request.
}

# Access target_vpn_gateway outputs
target_vpn_gateway_id = target_vpn_gateway.id
target_vpn_gateway_labels = target_vpn_gateway.labels
target_vpn_gateway_params = target_vpn_gateway.params
target_vpn_gateway_label_fingerprint = target_vpn_gateway.label_fingerprint
target_vpn_gateway_name = target_vpn_gateway.name
target_vpn_gateway_creation_timestamp = target_vpn_gateway.creation_timestamp
target_vpn_gateway_tunnels = target_vpn_gateway.tunnels
target_vpn_gateway_kind = target_vpn_gateway.kind
target_vpn_gateway_description = target_vpn_gateway.description
target_vpn_gateway_forwarding_rules = target_vpn_gateway.forwarding_rules
target_vpn_gateway_id = target_vpn_gateway.id
target_vpn_gateway_network = target_vpn_gateway.network
target_vpn_gateway_region = target_vpn_gateway.region
target_vpn_gateway_self_link = target_vpn_gateway.self_link
target_vpn_gateway_status = target_vpn_gateway.status
```

---


### Future_reservation

Creates a new Future Reservation.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `auto_delete_auto_created_reservations` | bool |  | Setting for enabling or disabling automatic deletion for auto-created
reservation. If set to true, auto-created reservations will be
deleted at Future Reservation's end time (default) or at user's defined
timestamp if any of the
[auto_created_reservations_delete_time, auto_created_reservations_duration]
values is specified.
For keeping auto-created reservation indefinitely, this value should be set
to false. |
| `auto_created_reservations_duration` | String |  | Specifies the duration of auto-created reservations.
It represents relative time to future reservation start_time when
auto-created reservations will be automatically deleted by Compute
Engine. Duration time unit is represented as a count of seconds
and fractions of seconds at nanosecond resolution. |
| `commitment_info` | String |  | If not present, then FR will not deliver a new commitment or update an
existing commitment. |
| `deployment_type` | String |  | Type of the deployment requested as part of future reservation. |
| `id` | String |  | [Output Only] A unique identifier for this future reservation. The server
defines this identifier. |
| `name` | String |  | The name of the resource, provided by the client when initially creating
the resource. The resource name must be 1-63 characters long, and comply
withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must be
a dash, lowercase letter, or digit, except the last character, which cannot
be a dash. |
| `name_prefix` | String |  | Name prefix for the reservations to be created at the time of
delivery. The name prefix must comply with RFC1035.
Maximum allowed length for name prefix is 20. Automatically created
reservations name format will be -date-####. |
| `reservation_mode` | String |  | The reservation mode which determines reservation-termination behavior and
expected pricing. |
| `reservation_name` | String |  | Name of reservations where the capacity is provisioned at the time of
delivery of  future reservations. If the reservation with the given name
does not exist already, it is created automatically at the time of Approval
with INACTIVE state till specified start-time. Either provide the
reservation_name or a name_prefix. |
| `protection_tier` | String |  | Protection tier for the workload. |
| `self_link_with_id` | String |  | [Output Only] Server-defined URL for this resource with the resource id. |
| `specific_reservation_required` | bool |  | Indicates whether the auto-created reservation can be consumed by VMs with
affinity for "any" reservation. If the field is set, then only VMs that
target the reservation by name can consume from the delivered reservation. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the future reservation. |
| `time_window` | String |  | Time window for this Future Reservation. |
| `self_link` | String |  | [Output Only] Server-defined fully-qualified URL for this resource. |
| `creation_timestamp` | String |  | [Output Only] The creation timestamp for this future reservation inRFC3339
text format. |
| `share_settings` | String |  | List of Projects/Folders to share with. |
| `aggregate_reservation` | String |  | Aggregate reservation details for the future reservation. |
| `auto_created_reservations_delete_time` | String |  | Future timestamp when the FR auto-created reservations will be deleted by
Compute Engine. Format of this field must be a valid
href="https://www.ietf.org/rfc/rfc3339.txt">RFC3339 value. |
| `planning_status` | String |  | Planning state before being submitted for evaluation |
| `kind` | String |  | [Output Only] Type of the resource. Alwayscompute#futureReservation for future reservations. |
| `status` | String |  | [Output only] Status of the Future Reservation |
| `specific_sku_properties` | String |  | Future Reservation configuration to indicate instance properties and
total count. |
| `enable_emergent_maintenance` | bool |  | Indicates if this group of VMs have emergent maintenance enabled. |
| `scheduling_type` | String |  | Maintenance information for this reservation |
| `zone` | String |  | [Output Only] URL of the Zone where this future reservation resides. |
| `zone` | String | ✅ | Name of the zone for this request. Name should conform to RFC1035. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `auto_delete_auto_created_reservations` | bool | Setting for enabling or disabling automatic deletion for auto-created
reservation. If set to true, auto-created reservations will be
deleted at Future Reservation's end time (default) or at user's defined
timestamp if any of the
[auto_created_reservations_delete_time, auto_created_reservations_duration]
values is specified.
For keeping auto-created reservation indefinitely, this value should be set
to false. |
| `auto_created_reservations_duration` | String | Specifies the duration of auto-created reservations.
It represents relative time to future reservation start_time when
auto-created reservations will be automatically deleted by Compute
Engine. Duration time unit is represented as a count of seconds
and fractions of seconds at nanosecond resolution. |
| `commitment_info` | String | If not present, then FR will not deliver a new commitment or update an
existing commitment. |
| `deployment_type` | String | Type of the deployment requested as part of future reservation. |
| `id` | String | [Output Only] A unique identifier for this future reservation. The server
defines this identifier. |
| `name` | String | The name of the resource, provided by the client when initially creating
the resource. The resource name must be 1-63 characters long, and comply
withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must be
a dash, lowercase letter, or digit, except the last character, which cannot
be a dash. |
| `name_prefix` | String | Name prefix for the reservations to be created at the time of
delivery. The name prefix must comply with RFC1035.
Maximum allowed length for name prefix is 20. Automatically created
reservations name format will be -date-####. |
| `reservation_mode` | String | The reservation mode which determines reservation-termination behavior and
expected pricing. |
| `reservation_name` | String | Name of reservations where the capacity is provisioned at the time of
delivery of  future reservations. If the reservation with the given name
does not exist already, it is created automatically at the time of Approval
with INACTIVE state till specified start-time. Either provide the
reservation_name or a name_prefix. |
| `protection_tier` | String | Protection tier for the workload. |
| `self_link_with_id` | String | [Output Only] Server-defined URL for this resource with the resource id. |
| `specific_reservation_required` | bool | Indicates whether the auto-created reservation can be consumed by VMs with
affinity for "any" reservation. If the field is set, then only VMs that
target the reservation by name can consume from the delivered reservation. |
| `description` | String | An optional description of this resource. Provide this property when you
create the future reservation. |
| `time_window` | String | Time window for this Future Reservation. |
| `self_link` | String | [Output Only] Server-defined fully-qualified URL for this resource. |
| `creation_timestamp` | String | [Output Only] The creation timestamp for this future reservation inRFC3339
text format. |
| `share_settings` | String | List of Projects/Folders to share with. |
| `aggregate_reservation` | String | Aggregate reservation details for the future reservation. |
| `auto_created_reservations_delete_time` | String | Future timestamp when the FR auto-created reservations will be deleted by
Compute Engine. Format of this field must be a valid
href="https://www.ietf.org/rfc/rfc3339.txt">RFC3339 value. |
| `planning_status` | String | Planning state before being submitted for evaluation |
| `kind` | String | [Output Only] Type of the resource. Alwayscompute#futureReservation for future reservations. |
| `status` | String | [Output only] Status of the Future Reservation |
| `specific_sku_properties` | String | Future Reservation configuration to indicate instance properties and
total count. |
| `enable_emergent_maintenance` | bool | Indicates if this group of VMs have emergent maintenance enabled. |
| `scheduling_type` | String | Maintenance information for this reservation |
| `zone` | String | [Output Only] URL of the Zone where this future reservation resides. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create future_reservation
future_reservation = provider.compute_api.Future_reservation {
    zone = "value"  # Name of the zone for this request. Name should conform to RFC1035.
    project = "value"  # Project ID for this request.
}

# Access future_reservation outputs
future_reservation_id = future_reservation.id
future_reservation_auto_delete_auto_created_reservations = future_reservation.auto_delete_auto_created_reservations
future_reservation_auto_created_reservations_duration = future_reservation.auto_created_reservations_duration
future_reservation_commitment_info = future_reservation.commitment_info
future_reservation_deployment_type = future_reservation.deployment_type
future_reservation_id = future_reservation.id
future_reservation_name = future_reservation.name
future_reservation_name_prefix = future_reservation.name_prefix
future_reservation_reservation_mode = future_reservation.reservation_mode
future_reservation_reservation_name = future_reservation.reservation_name
future_reservation_protection_tier = future_reservation.protection_tier
future_reservation_self_link_with_id = future_reservation.self_link_with_id
future_reservation_specific_reservation_required = future_reservation.specific_reservation_required
future_reservation_description = future_reservation.description
future_reservation_time_window = future_reservation.time_window
future_reservation_self_link = future_reservation.self_link
future_reservation_creation_timestamp = future_reservation.creation_timestamp
future_reservation_share_settings = future_reservation.share_settings
future_reservation_aggregate_reservation = future_reservation.aggregate_reservation
future_reservation_auto_created_reservations_delete_time = future_reservation.auto_created_reservations_delete_time
future_reservation_planning_status = future_reservation.planning_status
future_reservation_kind = future_reservation.kind
future_reservation_status = future_reservation.status
future_reservation_specific_sku_properties = future_reservation.specific_sku_properties
future_reservation_enable_emergent_maintenance = future_reservation.enable_emergent_maintenance
future_reservation_scheduling_type = future_reservation.scheduling_type
future_reservation_zone = future_reservation.zone
```

---


### Region_network_policie

Creates a new policy in the specified project using the data included in
the request.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `traffic_classification_rules` | Vec<String> |  | [Output Only] A list of traffic classification rules that belong to this
policy. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `name` | String |  | Name of the resource. |
| `region` | String |  | [Output Only] URL of the region where the regional network policy resides.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `rule_tuple_count` | i64 |  | [Output Only] Total count of all network policy rule tuples. A network
policy can not exceed a set number of tuples. |
| `self_link_with_id` | String |  | [Output Only] Server-defined URL for this resource with the resource id. |
| `associations` | Vec<String> |  | [Output Only] A list of associations that belong to this network policy. |
| `kind` | String |  | [Output only] Type of the resource. Alwayscompute#networkPolicy for network policies |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `region` | String | ✅ | Name of the region of this request. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `traffic_classification_rules` | Vec<String> | [Output Only] A list of traffic classification rules that belong to this
policy. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `name` | String | Name of the resource. |
| `region` | String | [Output Only] URL of the region where the regional network policy resides.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `rule_tuple_count` | i64 | [Output Only] Total count of all network policy rule tuples. A network
policy can not exceed a set number of tuples. |
| `self_link_with_id` | String | [Output Only] Server-defined URL for this resource with the resource id. |
| `associations` | Vec<String> | [Output Only] A list of associations that belong to this network policy. |
| `kind` | String | [Output only] Type of the resource. Alwayscompute#networkPolicy for network policies |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create region_network_policie
region_network_policie = provider.compute_api.Region_network_policie {
    region = "value"  # Name of the region of this request.
    project = "value"  # Project ID for this request.
}

# Access region_network_policie outputs
region_network_policie_id = region_network_policie.id
region_network_policie_traffic_classification_rules = region_network_policie.traffic_classification_rules
region_network_policie_description = region_network_policie.description
region_network_policie_id = region_network_policie.id
region_network_policie_name = region_network_policie.name
region_network_policie_region = region_network_policie.region
region_network_policie_creation_timestamp = region_network_policie.creation_timestamp
region_network_policie_rule_tuple_count = region_network_policie.rule_tuple_count
region_network_policie_self_link_with_id = region_network_policie.self_link_with_id
region_network_policie_associations = region_network_policie.associations
region_network_policie_kind = region_network_policie.kind
region_network_policie_self_link = region_network_policie.self_link
```

---


### Network_endpoint_group

Creates a network endpoint group in the specified project using the
parameters that are included in the request.

**Operations**: ✅ Create ✅ Read ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `load_balancer` | String |  | This field is only valid when the network endpoint group is used for load
balancing.
[Deprecated] This field is deprecated. |
| `psc_target_service` | String |  | The target service url used to set up private service connection to
a Google API or a PSC Producer Service Attachment.
An example value is: asia-northeast3-cloudkms.googleapis.com.

Optional. Only valid when networkEndpointType isPRIVATE_SERVICE_CONNECT. |
| `cloud_function` | String |  | Optional. Only valid when networkEndpointType isSERVERLESS. Only one of cloudRun,appEngine or cloudFunction may be set. |
| `region` | String |  | [Output Only] The URL of theregion
where the network endpoint group is located. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `size` | i64 |  | [Output only] Number of network endpoints in the network endpoint group. |
| `subnetwork` | String |  | Optional URL of the subnetwork to which all network endpoints in the NEG
belong. |
| `app_engine` | String |  | Optional. Only valid when networkEndpointType isSERVERLESS. Only one of cloudRun,appEngine or cloudFunction may be set. |
| `annotations` | HashMap<String, String> |  | Optional. Metadata defined as annotations on the network endpoint group. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `kind` | String |  | [Output Only] Type of the resource. Alwayscompute#networkEndpointGroup for network endpoint group. |
| `default_port` | i64 |  | The default port used if the port number is not specified in the network
endpoint.

Optional. If the network endpoint type is either GCE_VM_IP,SERVERLESS or PRIVATE_SERVICE_CONNECT, this
field must not be specified. |
| `cloud_run` | String |  | Optional. Only valid when networkEndpointType isSERVERLESS. Only one of cloudRun,appEngine or cloudFunction may be set. |
| `network` | String |  | The URL of the network to which all network endpoints in the NEG belong.
Uses default project network if unspecified. |
| `psc_data` | String |  | Optional. Only valid when networkEndpointType isPRIVATE_SERVICE_CONNECT. |
| `name` | String |  | Name of the resource; provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must be
a dash, lowercase letter, or digit, except the last character, which cannot
be a dash. |
| `network_endpoint_type` | String |  | Type of network endpoints in this network endpoint group. Can be one ofGCE_VM_IP, GCE_VM_IP_PORT,NON_GCP_PRIVATE_IP_PORT, INTERNET_FQDN_PORT,INTERNET_IP_PORT, SERVERLESS,PRIVATE_SERVICE_CONNECT, GCE_VM_IP_PORTMAP. |
| `serverless_deployment` | String |  | Only valid when networkEndpointType isSERVERLESS. Only one of cloudRun,appEngine, cloudFunction orserverlessDeployment may be set. |
| `type` | String |  | Specify the type of this network endpoint group. OnlyLOAD_BALANCING is valid for now. |
| `zone` | String |  | [Output Only] The URL of thezone
where the network endpoint group is located. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `self_link_with_id` | String |  | [Output Only] Server-defined URL for this resource with the resource id. |
| `zone` | String | ✅ | The name of the zone where
you want to create the network endpoint group. It should comply with
RFC1035. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `load_balancer` | String | This field is only valid when the network endpoint group is used for load
balancing.
[Deprecated] This field is deprecated. |
| `psc_target_service` | String | The target service url used to set up private service connection to
a Google API or a PSC Producer Service Attachment.
An example value is: asia-northeast3-cloudkms.googleapis.com.

Optional. Only valid when networkEndpointType isPRIVATE_SERVICE_CONNECT. |
| `cloud_function` | String | Optional. Only valid when networkEndpointType isSERVERLESS. Only one of cloudRun,appEngine or cloudFunction may be set. |
| `region` | String | [Output Only] The URL of theregion
where the network endpoint group is located. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `size` | i64 | [Output only] Number of network endpoints in the network endpoint group. |
| `subnetwork` | String | Optional URL of the subnetwork to which all network endpoints in the NEG
belong. |
| `app_engine` | String | Optional. Only valid when networkEndpointType isSERVERLESS. Only one of cloudRun,appEngine or cloudFunction may be set. |
| `annotations` | HashMap<String, String> | Optional. Metadata defined as annotations on the network endpoint group. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `kind` | String | [Output Only] Type of the resource. Alwayscompute#networkEndpointGroup for network endpoint group. |
| `default_port` | i64 | The default port used if the port number is not specified in the network
endpoint.

Optional. If the network endpoint type is either GCE_VM_IP,SERVERLESS or PRIVATE_SERVICE_CONNECT, this
field must not be specified. |
| `cloud_run` | String | Optional. Only valid when networkEndpointType isSERVERLESS. Only one of cloudRun,appEngine or cloudFunction may be set. |
| `network` | String | The URL of the network to which all network endpoints in the NEG belong.
Uses default project network if unspecified. |
| `psc_data` | String | Optional. Only valid when networkEndpointType isPRIVATE_SERVICE_CONNECT. |
| `name` | String | Name of the resource; provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must be
a dash, lowercase letter, or digit, except the last character, which cannot
be a dash. |
| `network_endpoint_type` | String | Type of network endpoints in this network endpoint group. Can be one ofGCE_VM_IP, GCE_VM_IP_PORT,NON_GCP_PRIVATE_IP_PORT, INTERNET_FQDN_PORT,INTERNET_IP_PORT, SERVERLESS,PRIVATE_SERVICE_CONNECT, GCE_VM_IP_PORTMAP. |
| `serverless_deployment` | String | Only valid when networkEndpointType isSERVERLESS. Only one of cloudRun,appEngine, cloudFunction orserverlessDeployment may be set. |
| `type` | String | Specify the type of this network endpoint group. OnlyLOAD_BALANCING is valid for now. |
| `zone` | String | [Output Only] The URL of thezone
where the network endpoint group is located. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `self_link_with_id` | String | [Output Only] Server-defined URL for this resource with the resource id. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create network_endpoint_group
network_endpoint_group = provider.compute_api.Network_endpoint_group {
    zone = "value"  # The name of the zone where
you want to create the network endpoint group. It should comply with
RFC1035.
    project = "value"  # Project ID for this request.
}

# Access network_endpoint_group outputs
network_endpoint_group_id = network_endpoint_group.id
network_endpoint_group_self_link = network_endpoint_group.self_link
network_endpoint_group_load_balancer = network_endpoint_group.load_balancer
network_endpoint_group_psc_target_service = network_endpoint_group.psc_target_service
network_endpoint_group_cloud_function = network_endpoint_group.cloud_function
network_endpoint_group_region = network_endpoint_group.region
network_endpoint_group_creation_timestamp = network_endpoint_group.creation_timestamp
network_endpoint_group_size = network_endpoint_group.size
network_endpoint_group_subnetwork = network_endpoint_group.subnetwork
network_endpoint_group_app_engine = network_endpoint_group.app_engine
network_endpoint_group_annotations = network_endpoint_group.annotations
network_endpoint_group_id = network_endpoint_group.id
network_endpoint_group_kind = network_endpoint_group.kind
network_endpoint_group_default_port = network_endpoint_group.default_port
network_endpoint_group_cloud_run = network_endpoint_group.cloud_run
network_endpoint_group_network = network_endpoint_group.network
network_endpoint_group_psc_data = network_endpoint_group.psc_data
network_endpoint_group_name = network_endpoint_group.name
network_endpoint_group_network_endpoint_type = network_endpoint_group.network_endpoint_type
network_endpoint_group_serverless_deployment = network_endpoint_group.serverless_deployment
network_endpoint_group_type = network_endpoint_group.type
network_endpoint_group_zone = network_endpoint_group.zone
network_endpoint_group_description = network_endpoint_group.description
network_endpoint_group_self_link_with_id = network_endpoint_group.self_link_with_id
```

---


### Region_network_firewall_policie

Creates a new network firewall policy in the specified project and region.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `fingerprint` | String |  | Specifies a fingerprint for this resource, which is essentially a hash of
the metadata's contents and used for optimistic locking. The
fingerprint is initially generated by Compute Engine and changes after
every request to modify or update metadata. You must always provide an
up-to-date fingerprint hash in order to update or change metadata,
otherwise the request will fail with error412 conditionNotMet.

To see the latest fingerprint, make get() request to the
firewall policy. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `packet_mirroring_rules` | Vec<String> |  | A list of packet mirroring rules that belong to this policy. |
| `parent` | String |  | [Output Only] The parent of the firewall policy.
This field is not applicable to network firewall policies. |
| `region` | String |  | [Output Only] URL of the region where the regional firewall policy resides.
This field is not applicable to global firewall policies.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `rules` | Vec<String> |  | A list of rules that belong to this policy.
There must always be a default rule (rule with priority 2147483647 and
match "*"). If no rules are provided when creating a firewall policy, a
default rule with action "allow" will be added. |
| `policy_type` | String |  | The type of the firewall policy. This field can be eitherVPC_POLICY or RDMA_ROCE_POLICY.

Note: if not specified then VPC_POLICY will be used. |
| `self_link_with_id` | String |  | [Output Only] Server-defined URL for this resource with the resource id. |
| `short_name` | String |  | User-provided name of the Organization firewall policy. The name should be
unique in the organization in which the firewall policy is created.
This field is not applicable to network firewall policies.
This name must be set on creation and cannot be changed. The name must be
1-63 characters long, and comply with RFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `kind` | String |  | [Output only] Type of the resource. Alwayscompute#firewallPolicyfor firewall policies |
| `vpc_network_scope` | String |  | The scope of networks allowed to be associated with the firewall
policy. This field can be either GLOBAL_VPC_NETWORK orREGIONAL_VPC_NETWORK.

A firewall policy with the VPC scope set to GLOBAL_VPC_NETWORK
is allowed to be attached only to global networks.

When the VPC scope is set to REGIONAL_VPC_NETWORK the firewall
policy is allowed to be attached only to regional networks in the same
scope as the firewall policy.

Note: if not specified then GLOBAL_VPC_NETWORK will be used. |
| `associations` | Vec<String> |  | A list of associations that belong to this firewall policy. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `display_name` | String |  | Deprecated, please use short name instead. User-provided name of the
Organization firewall policy. The name should be unique in the organization
in which the firewall policy is created.
This field is not applicable to network firewall policies.
This name must be set on creation and cannot be changed.
The name must be 1-63 characters long, and comply
with RFC1035. Specifically, the name must be 1-63 characters
long and match the regular expression `[a-z]([-a-z0-9]*[a-z0-9])?` which
means the first character must be a lowercase letter, and all following
characters must be a dash, lowercase letter, or digit, except the last
character, which cannot be a dash. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `name` | String |  | Name of the resource. For Organization Firewall Policies it's a
[Output Only] numeric ID allocated by Google Cloud which uniquely
identifies the Organization Firewall Policy. |
| `policy_source` | String |  | [Output Only] Source of this Firewall Policy. USER_DEFINED if
created by a Cloud user, or SYSTEM if created by managed
services like GKE. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `rule_tuple_count` | i64 |  | [Output Only] Total count of all firewall policy rule tuples. A firewall
policy can not exceed a set number of tuples. |
| `region` | String | ✅ | Name of the region scoping this request. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `fingerprint` | String | Specifies a fingerprint for this resource, which is essentially a hash of
the metadata's contents and used for optimistic locking. The
fingerprint is initially generated by Compute Engine and changes after
every request to modify or update metadata. You must always provide an
up-to-date fingerprint hash in order to update or change metadata,
otherwise the request will fail with error412 conditionNotMet.

To see the latest fingerprint, make get() request to the
firewall policy. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `packet_mirroring_rules` | Vec<String> | A list of packet mirroring rules that belong to this policy. |
| `parent` | String | [Output Only] The parent of the firewall policy.
This field is not applicable to network firewall policies. |
| `region` | String | [Output Only] URL of the region where the regional firewall policy resides.
This field is not applicable to global firewall policies.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `rules` | Vec<String> | A list of rules that belong to this policy.
There must always be a default rule (rule with priority 2147483647 and
match "*"). If no rules are provided when creating a firewall policy, a
default rule with action "allow" will be added. |
| `policy_type` | String | The type of the firewall policy. This field can be eitherVPC_POLICY or RDMA_ROCE_POLICY.

Note: if not specified then VPC_POLICY will be used. |
| `self_link_with_id` | String | [Output Only] Server-defined URL for this resource with the resource id. |
| `short_name` | String | User-provided name of the Organization firewall policy. The name should be
unique in the organization in which the firewall policy is created.
This field is not applicable to network firewall policies.
This name must be set on creation and cannot be changed. The name must be
1-63 characters long, and comply with RFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `kind` | String | [Output only] Type of the resource. Alwayscompute#firewallPolicyfor firewall policies |
| `vpc_network_scope` | String | The scope of networks allowed to be associated with the firewall
policy. This field can be either GLOBAL_VPC_NETWORK orREGIONAL_VPC_NETWORK.

A firewall policy with the VPC scope set to GLOBAL_VPC_NETWORK
is allowed to be attached only to global networks.

When the VPC scope is set to REGIONAL_VPC_NETWORK the firewall
policy is allowed to be attached only to regional networks in the same
scope as the firewall policy.

Note: if not specified then GLOBAL_VPC_NETWORK will be used. |
| `associations` | Vec<String> | A list of associations that belong to this firewall policy. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `display_name` | String | Deprecated, please use short name instead. User-provided name of the
Organization firewall policy. The name should be unique in the organization
in which the firewall policy is created.
This field is not applicable to network firewall policies.
This name must be set on creation and cannot be changed.
The name must be 1-63 characters long, and comply
with RFC1035. Specifically, the name must be 1-63 characters
long and match the regular expression `[a-z]([-a-z0-9]*[a-z0-9])?` which
means the first character must be a lowercase letter, and all following
characters must be a dash, lowercase letter, or digit, except the last
character, which cannot be a dash. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `name` | String | Name of the resource. For Organization Firewall Policies it's a
[Output Only] numeric ID allocated by Google Cloud which uniquely
identifies the Organization Firewall Policy. |
| `policy_source` | String | [Output Only] Source of this Firewall Policy. USER_DEFINED if
created by a Cloud user, or SYSTEM if created by managed
services like GKE. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `rule_tuple_count` | i64 | [Output Only] Total count of all firewall policy rule tuples. A firewall
policy can not exceed a set number of tuples. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create region_network_firewall_policie
region_network_firewall_policie = provider.compute_api.Region_network_firewall_policie {
    region = "value"  # Name of the region scoping this request.
    project = "value"  # Project ID for this request.
}

# Access region_network_firewall_policie outputs
region_network_firewall_policie_id = region_network_firewall_policie.id
region_network_firewall_policie_fingerprint = region_network_firewall_policie.fingerprint
region_network_firewall_policie_self_link = region_network_firewall_policie.self_link
region_network_firewall_policie_packet_mirroring_rules = region_network_firewall_policie.packet_mirroring_rules
region_network_firewall_policie_parent = region_network_firewall_policie.parent
region_network_firewall_policie_region = region_network_firewall_policie.region
region_network_firewall_policie_rules = region_network_firewall_policie.rules
region_network_firewall_policie_policy_type = region_network_firewall_policie.policy_type
region_network_firewall_policie_self_link_with_id = region_network_firewall_policie.self_link_with_id
region_network_firewall_policie_short_name = region_network_firewall_policie.short_name
region_network_firewall_policie_kind = region_network_firewall_policie.kind
region_network_firewall_policie_vpc_network_scope = region_network_firewall_policie.vpc_network_scope
region_network_firewall_policie_associations = region_network_firewall_policie.associations
region_network_firewall_policie_creation_timestamp = region_network_firewall_policie.creation_timestamp
region_network_firewall_policie_display_name = region_network_firewall_policie.display_name
region_network_firewall_policie_id = region_network_firewall_policie.id
region_network_firewall_policie_name = region_network_firewall_policie.name
region_network_firewall_policie_policy_source = region_network_firewall_policie.policy_source
region_network_firewall_policie_description = region_network_firewall_policie.description
region_network_firewall_policie_rule_tuple_count = region_network_firewall_policie.rule_tuple_count
```

---


### Interconnect

Creates an Interconnect in the specified project using
the data included in the request.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `subzone` | String |  | Specific subzone in the InterconnectLocation that represents where
this connection is to be provisioned. |
| `kind` | String |  | [Output Only] Type of the resource. Alwayscompute#interconnect for interconnects. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `circuit_infos` | Vec<String> |  | [Output Only] A list of CircuitInfo objects, that describe the individual
circuits in this LAG. |
| `link_type` | String |  | Type of link requested, which can take one of the following values:
   
   - LINK_TYPE_ETHERNET_10G_LR: A 10G Ethernet with LR optics
   - LINK_TYPE_ETHERNET_100G_LR: A 100G Ethernet with LR optics.
   - LINK_TYPE_ETHERNET_400G_LR4: A 400G Ethernet with LR4 optics.


 Note that this field indicates the speed of each of
the links in the bundle, not the speed of the entire bundle. |
| `google_ip_address` | String |  | [Output Only] IP address configured on the Google side of the Interconnect
link. This can be used only for ping tests. |
| `wire_groups` | Vec<String> |  | [Output Only] A list of the URLs of all CrossSiteNetwork WireGroups
configured to use this Interconnect. The Interconnect cannot be deleted if
this list is non-empty. |
| `interconnect_type` | String |  | Type of interconnect, which can take one of the following values:
   
   - PARTNER: A partner-managed interconnection shared between customers
   though a partner.
   - DEDICATED: A dedicated physical interconnection with the
   customer.


Note that a value IT_PRIVATE has been deprecated in favor of DEDICATED. |
| `interconnect_groups` | Vec<String> |  | [Output Only] URLs of InterconnectGroups that include this Interconnect.
Order is arbitrary and items are unique. |
| `satisfies_pzs` | bool |  | [Output Only] Reserved for future use. |
| `params` | String |  | Input only. [Input Only] Additional params passed with the request, but not persisted
as part of resource payload. |
| `provisioned_link_count` | i64 |  | [Output Only] Number of links actually provisioned in this interconnect. |
| `labels` | HashMap<String, String> |  | Labels for this resource. These can only be added or modified by thesetLabels method. Each label key/value pair must comply withRFC1035.
Label values may be empty. |
| `remote_location` | String |  | Indicates that this is a Cross-Cloud Interconnect. This field specifies the
location outside of Google's network that the interconnect is connected to. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `state` | String |  | [Output Only] The current state of Interconnect functionality, which can
take one of the following values:
   
   - ACTIVE: The Interconnect is valid, turned up and ready to use.
   Attachments may be provisioned on this Interconnect.
   - UNPROVISIONED: The Interconnect has not completed turnup. No
   attachments may be provisioned on this Interconnect.
   - UNDER_MAINTENANCE: The Interconnect is undergoing internal maintenance.
   No attachments may be provisioned or updated on this
   Interconnect. |
| `available_features` | Vec<String> |  | [Output only] List of features available for this Interconnect connection,
which can take one of the following values:
   
   - IF_MACSEC: If present, then the Interconnect connection is
   provisioned on MACsec capable hardware ports. If not present, then the
   Interconnect connection is provisioned on non-MACsec capable ports. Any
   attempt to enable MACsec will fail.
   - IF_CROSS_SITE_NETWORK: If present, then the Interconnect connection is
   provisioned exclusively for Cross-Site Networking. Any attempt to configure
   VLAN attachments will fail. If not present, then the Interconnect
   connection is not provisioned for Cross-Site Networking. Any attempt to use
   it for Cross-Site Networking will fail. |
| `operational_status` | String |  | [Output Only] The current status of this Interconnect's functionality,
which can take one of the following values:
   
   - OS_ACTIVE: A valid Interconnect, which is turned up and is ready to
   use. Attachments may be provisioned on this Interconnect.

- OS_UNPROVISIONED: An Interconnect that has not completed turnup. No
attachments may be provisioned on this Interconnect.
- OS_UNDER_MAINTENANCE: An Interconnect that is undergoing internal
maintenance. No attachments may be provisioned or updated on this
Interconnect. |
| `customer_name` | String |  | Customer name, to put in the Letter of Authorization as the party
authorized to request a crossconnect. |
| `interconnect_attachments` | Vec<String> |  | [Output Only] A list of the URLs of all InterconnectAttachments configured
to use  this Interconnect. |
| `requested_link_count` | i64 |  | Target number of physical links in the link bundle, as requested by the
customer. |
| `peer_ip_address` | String |  | [Output Only] IP address configured on the customer side of the
Interconnect link. The customer should configure this IP address during
turnup when prompted by Google NOC. This can be used only for ping tests. |
| `location` | String |  | URL of the InterconnectLocation object that represents where this
connection is to be provisioned. |
| `noc_contact_email` | String |  | Email address to contact the customer NOC for operations and maintenance
notifications regarding this Interconnect. If specified, this will be used
for notifications in addition to all other forms described, such as
Cloud Monitoring logs alerting and Cloud Notifications. This field is
required for users who sign up for Cloud Interconnect using
workforce identity federation. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `requested_features` | Vec<String> |  | Optional. This parameter can be provided only with Interconnect INSERT. It
isn't valid for Interconnect PATCH. List of features requested for this
Interconnect connection, which can take one of the following values:
   
   - IF_MACSEC: If specified, then the connection is created on MACsec
   capable hardware ports. If not specified, non-MACsec capable ports will
   also be considered.
   - IF_CROSS_SITE_NETWORK: If specified, then the connection is created
   exclusively for Cross-Site Networking. The connection can not be used for
   Cross-Site Networking unless this feature is specified. |
| `self_link_with_id` | String |  | [Output Only] Server-defined URL for this resource with the resource id. |
| `expected_outages` | Vec<String> |  | [Output Only] A list of outages expected for this Interconnect. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `aai_enabled` | bool |  | Enable or disable the application awareness feature on this Cloud
Interconnect. |
| `admin_enabled` | bool |  | Administrative status of the interconnect. When this is set to true, the
Interconnect is functional and can carry traffic.
When set to false, no packets can be carried over the interconnect and
no BGP routes are exchanged over it. By default, the status is set to true. |
| `macsec_enabled` | bool |  | Enable or disable MACsec on this Interconnect connection. MACsec enablement
fails if the MACsec object is not specified. |
| `google_reference_id` | String |  | [Output Only] Google reference ID to be used when raising support tickets
with Google or otherwise to debug backend connectivity issues. |
| `macsec` | String |  | Configuration that enables Media Access Control security (MACsec) on the
Cloud Interconnect connection between Google and your on-premises router. |
| `name` | String |  | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must be
a dash, lowercase letter, or digit, except the last character, which cannot
be a dash. |
| `label_fingerprint` | String |  | A fingerprint for the labels being applied to this Interconnect, which
is essentially a hash of the labels set used for optimistic locking. The
fingerprint is initially generated by Compute Engine and changes after
every request to modify or update labels. You must always provide an
up-to-date fingerprint hash in order to update or change labels,
otherwise the request will fail with error412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve an Interconnect. |
| `application_aware_interconnect` | String |  | Configuration information for application awareness on this Cloud
Interconnect. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `subzone` | String | Specific subzone in the InterconnectLocation that represents where
this connection is to be provisioned. |
| `kind` | String | [Output Only] Type of the resource. Alwayscompute#interconnect for interconnects. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `circuit_infos` | Vec<String> | [Output Only] A list of CircuitInfo objects, that describe the individual
circuits in this LAG. |
| `link_type` | String | Type of link requested, which can take one of the following values:
   
   - LINK_TYPE_ETHERNET_10G_LR: A 10G Ethernet with LR optics
   - LINK_TYPE_ETHERNET_100G_LR: A 100G Ethernet with LR optics.
   - LINK_TYPE_ETHERNET_400G_LR4: A 400G Ethernet with LR4 optics.


 Note that this field indicates the speed of each of
the links in the bundle, not the speed of the entire bundle. |
| `google_ip_address` | String | [Output Only] IP address configured on the Google side of the Interconnect
link. This can be used only for ping tests. |
| `wire_groups` | Vec<String> | [Output Only] A list of the URLs of all CrossSiteNetwork WireGroups
configured to use this Interconnect. The Interconnect cannot be deleted if
this list is non-empty. |
| `interconnect_type` | String | Type of interconnect, which can take one of the following values:
   
   - PARTNER: A partner-managed interconnection shared between customers
   though a partner.
   - DEDICATED: A dedicated physical interconnection with the
   customer.


Note that a value IT_PRIVATE has been deprecated in favor of DEDICATED. |
| `interconnect_groups` | Vec<String> | [Output Only] URLs of InterconnectGroups that include this Interconnect.
Order is arbitrary and items are unique. |
| `satisfies_pzs` | bool | [Output Only] Reserved for future use. |
| `params` | String | Input only. [Input Only] Additional params passed with the request, but not persisted
as part of resource payload. |
| `provisioned_link_count` | i64 | [Output Only] Number of links actually provisioned in this interconnect. |
| `labels` | HashMap<String, String> | Labels for this resource. These can only be added or modified by thesetLabels method. Each label key/value pair must comply withRFC1035.
Label values may be empty. |
| `remote_location` | String | Indicates that this is a Cross-Cloud Interconnect. This field specifies the
location outside of Google's network that the interconnect is connected to. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `state` | String | [Output Only] The current state of Interconnect functionality, which can
take one of the following values:
   
   - ACTIVE: The Interconnect is valid, turned up and ready to use.
   Attachments may be provisioned on this Interconnect.
   - UNPROVISIONED: The Interconnect has not completed turnup. No
   attachments may be provisioned on this Interconnect.
   - UNDER_MAINTENANCE: The Interconnect is undergoing internal maintenance.
   No attachments may be provisioned or updated on this
   Interconnect. |
| `available_features` | Vec<String> | [Output only] List of features available for this Interconnect connection,
which can take one of the following values:
   
   - IF_MACSEC: If present, then the Interconnect connection is
   provisioned on MACsec capable hardware ports. If not present, then the
   Interconnect connection is provisioned on non-MACsec capable ports. Any
   attempt to enable MACsec will fail.
   - IF_CROSS_SITE_NETWORK: If present, then the Interconnect connection is
   provisioned exclusively for Cross-Site Networking. Any attempt to configure
   VLAN attachments will fail. If not present, then the Interconnect
   connection is not provisioned for Cross-Site Networking. Any attempt to use
   it for Cross-Site Networking will fail. |
| `operational_status` | String | [Output Only] The current status of this Interconnect's functionality,
which can take one of the following values:
   
   - OS_ACTIVE: A valid Interconnect, which is turned up and is ready to
   use. Attachments may be provisioned on this Interconnect.

- OS_UNPROVISIONED: An Interconnect that has not completed turnup. No
attachments may be provisioned on this Interconnect.
- OS_UNDER_MAINTENANCE: An Interconnect that is undergoing internal
maintenance. No attachments may be provisioned or updated on this
Interconnect. |
| `customer_name` | String | Customer name, to put in the Letter of Authorization as the party
authorized to request a crossconnect. |
| `interconnect_attachments` | Vec<String> | [Output Only] A list of the URLs of all InterconnectAttachments configured
to use  this Interconnect. |
| `requested_link_count` | i64 | Target number of physical links in the link bundle, as requested by the
customer. |
| `peer_ip_address` | String | [Output Only] IP address configured on the customer side of the
Interconnect link. The customer should configure this IP address during
turnup when prompted by Google NOC. This can be used only for ping tests. |
| `location` | String | URL of the InterconnectLocation object that represents where this
connection is to be provisioned. |
| `noc_contact_email` | String | Email address to contact the customer NOC for operations and maintenance
notifications regarding this Interconnect. If specified, this will be used
for notifications in addition to all other forms described, such as
Cloud Monitoring logs alerting and Cloud Notifications. This field is
required for users who sign up for Cloud Interconnect using
workforce identity federation. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `requested_features` | Vec<String> | Optional. This parameter can be provided only with Interconnect INSERT. It
isn't valid for Interconnect PATCH. List of features requested for this
Interconnect connection, which can take one of the following values:
   
   - IF_MACSEC: If specified, then the connection is created on MACsec
   capable hardware ports. If not specified, non-MACsec capable ports will
   also be considered.
   - IF_CROSS_SITE_NETWORK: If specified, then the connection is created
   exclusively for Cross-Site Networking. The connection can not be used for
   Cross-Site Networking unless this feature is specified. |
| `self_link_with_id` | String | [Output Only] Server-defined URL for this resource with the resource id. |
| `expected_outages` | Vec<String> | [Output Only] A list of outages expected for this Interconnect. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `aai_enabled` | bool | Enable or disable the application awareness feature on this Cloud
Interconnect. |
| `admin_enabled` | bool | Administrative status of the interconnect. When this is set to true, the
Interconnect is functional and can carry traffic.
When set to false, no packets can be carried over the interconnect and
no BGP routes are exchanged over it. By default, the status is set to true. |
| `macsec_enabled` | bool | Enable or disable MACsec on this Interconnect connection. MACsec enablement
fails if the MACsec object is not specified. |
| `google_reference_id` | String | [Output Only] Google reference ID to be used when raising support tickets
with Google or otherwise to debug backend connectivity issues. |
| `macsec` | String | Configuration that enables Media Access Control security (MACsec) on the
Cloud Interconnect connection between Google and your on-premises router. |
| `name` | String | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must be
a dash, lowercase letter, or digit, except the last character, which cannot
be a dash. |
| `label_fingerprint` | String | A fingerprint for the labels being applied to this Interconnect, which
is essentially a hash of the labels set used for optimistic locking. The
fingerprint is initially generated by Compute Engine and changes after
every request to modify or update labels. You must always provide an
up-to-date fingerprint hash in order to update or change labels,
otherwise the request will fail with error412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve an Interconnect. |
| `application_aware_interconnect` | String | Configuration information for application awareness on this Cloud
Interconnect. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create interconnect
interconnect = provider.compute_api.Interconnect {
    project = "value"  # Project ID for this request.
}

# Access interconnect outputs
interconnect_id = interconnect.id
interconnect_subzone = interconnect.subzone
interconnect_kind = interconnect.kind
interconnect_self_link = interconnect.self_link
interconnect_circuit_infos = interconnect.circuit_infos
interconnect_link_type = interconnect.link_type
interconnect_google_ip_address = interconnect.google_ip_address
interconnect_wire_groups = interconnect.wire_groups
interconnect_interconnect_type = interconnect.interconnect_type
interconnect_interconnect_groups = interconnect.interconnect_groups
interconnect_satisfies_pzs = interconnect.satisfies_pzs
interconnect_params = interconnect.params
interconnect_provisioned_link_count = interconnect.provisioned_link_count
interconnect_labels = interconnect.labels
interconnect_remote_location = interconnect.remote_location
interconnect_creation_timestamp = interconnect.creation_timestamp
interconnect_state = interconnect.state
interconnect_available_features = interconnect.available_features
interconnect_operational_status = interconnect.operational_status
interconnect_customer_name = interconnect.customer_name
interconnect_interconnect_attachments = interconnect.interconnect_attachments
interconnect_requested_link_count = interconnect.requested_link_count
interconnect_peer_ip_address = interconnect.peer_ip_address
interconnect_location = interconnect.location
interconnect_noc_contact_email = interconnect.noc_contact_email
interconnect_id = interconnect.id
interconnect_requested_features = interconnect.requested_features
interconnect_self_link_with_id = interconnect.self_link_with_id
interconnect_expected_outages = interconnect.expected_outages
interconnect_description = interconnect.description
interconnect_aai_enabled = interconnect.aai_enabled
interconnect_admin_enabled = interconnect.admin_enabled
interconnect_macsec_enabled = interconnect.macsec_enabled
interconnect_google_reference_id = interconnect.google_reference_id
interconnect_macsec = interconnect.macsec
interconnect_name = interconnect.name
interconnect_label_fingerprint = interconnect.label_fingerprint
interconnect_application_aware_interconnect = interconnect.application_aware_interconnect
```

---


### Region_instance

Creates multiple instances in a given region. Count specifies the number of
instances to create.

**Operations**: ✅ Create

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `location_policy` | String |  | Policy for choosing target zone. For more information, seeCreate VMs in
bulk. |
| `min_count` | String |  | The minimum number of instances to create. If no min_count is
specified then count is used as the default value. Ifmin_count instances cannot be created, then no instances will
be created and instances already created will be deleted. |
| `name_pattern` | String |  | The string pattern used for the names of the VMs.
Either name_pattern or per_instance_properties
must be set. The pattern must contain one continuous sequence of
placeholder hash characters (#) with each character corresponding to one
digit of the generated instance name. Example: a name_pattern
of inst-#### generates instance names such asinst-0001 and inst-0002. If existing instances
in the same project and zone have names that match the name pattern
then the generated instance numbers start after the biggest
existing number. For example, if there exists an instance with nameinst-0050, then instance names generated using the patterninst-#### begin with inst-0051. The name pattern
placeholder #...# can contain up to 18 characters. |
| `instance_flexibility_policy` | String |  | A flexible specification of machine type of instances to create. |
| `count` | String |  | The maximum number of instances to create. |
| `instance_properties` | String |  | The instance properties defining the VM instances to be created. Required
if sourceInstanceTemplate is not provided. |
| `source_instance_template` | String |  | Specifies the instance template from which to create instances. You may
combine sourceInstanceTemplate withinstanceProperties to override specific values from an
existing instance template. Bulk API follows the semantics of JSON Merge
Patch described by RFC
7396.

It can be a full or partial URL. For example, the following are
all valid URLs to an instance template: 
   
   
      - https://www.googleapis.com/compute/v1/projects/project/global/instanceTemplates/instanceTemplate
      - projects/project/global/instanceTemplates/instanceTemplate
      - global/instanceTemplates/instanceTemplate



This field is optional. |
| `per_instance_properties` | HashMap<String, String> |  | Per-instance properties to be set on individual instances.
Keys of this map specify requested instance names.
Can be empty if name_pattern is used. |
| `project` | String | ✅ | Project ID for this request. |
| `region` | String | ✅ | The name of the region for this request. |



#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create region_instance
region_instance = provider.compute_api.Region_instance {
    project = "value"  # Project ID for this request.
    region = "value"  # The name of the region for this request.
}

```

---


### Global_public_delegated_prefixe

Creates a global PublicDelegatedPrefix in the specified project using the
parameters that are included in the request.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `kind` | String |  | [Output Only] Type of the resource. Alwayscompute#publicDelegatedPrefix for public delegated prefixes. |
| `self_link_with_id` | String |  | [Output Only] Server-defined URL with id for the resource. |
| `region` | String |  | [Output Only] URL of the region where the public delegated prefix resides.
This field applies only to the region resource. You must specify this
field as part of the HTTP request URL. It is not settable as a field in
the request body. |
| `id` | String |  | [Output Only] The unique identifier for the resource type. The server
generates this identifier. |
| `status` | String |  | [Output Only] The status of the public delegated prefix, which can be one
of following values:
   
   
     - `INITIALIZING` The public delegated prefix is being initialized and
     addresses cannot be created yet.
     - `READY_TO_ANNOUNCE` The public delegated prefix is a live migration
     prefix and is active.
     - `ANNOUNCED` The public delegated prefix is announced and ready to
     use.
     - `DELETING` The public delegated prefix is being deprovsioned.
     - `ACTIVE` The public delegated prefix is ready to use. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `allocatable_prefix_length` | i64 |  | The allocatable prefix length supported by this public delegated prefix.
This field is optional and cannot be set for prefixes in DELEGATION mode.
It cannot be set for IPv4 prefixes either, and it always defaults to 32. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `mode` | String |  | The public delegated prefix mode for IPv6 only. |
| `purpose` | String |  | The purpose of the public delegated prefix.

This field can only be set for the top-level global public delegated
prefix. It is an output-only field for the sub-delegates that inherit the
value from the top-level global public delegated prefix. Once the value is
set, it cannot be changed.

The field cannot be set for regional public delegated prefixes.

The supported values are:
   
   
     - APPLICATION_AND_PROXY_LOAD_BALANCERS: The global public
     delegated prefix can only be used by Global External Application and
     Proxy Load Balancers to allocate addresses for forwarding rules. This is
     the default value.
     - PASSTHROUGH_LOAD_BALANCER_AVAILABILITY_GROUP0: The
     global public delegated prefix can only be used by Global External
     Passthrough Network Load Balancers to allocate Availability Group 0
     addresses for forwarding rules.
     - PASSTHROUGH_LOAD_BALANCER_AVAILABILITY_GROUP1: The
     global public delegated prefix can only be used by Global External
     Passthrough Network Load Balancers to allocate Availability Group 1
     addresses for forwarding rules. |
| `enable_enhanced_ipv4_allocation` | bool |  | [Output Only] Whether this PDP supports enhanced IPv4 allocations.
Applicable for IPv4 PDPs only. |
| `ipv6_access_type` | String |  | [Output Only] The internet access type for IPv6 Public Delegated Prefixes.
Inherited from parent prefix. |
| `public_delegated_sub_prefixs` | Vec<String> |  | The list of sub public delegated prefixes that exist for this public
delegated prefix. |
| `is_live_migration` | bool |  | If true, the prefix will be live migrated. |
| `name` | String |  | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `byoip_api_version` | String |  | [Output Only] The version of BYOIP API. |
| `fingerprint` | String |  | Fingerprint of this resource. A hash of the contents stored in this object.
This field is used in optimistic locking. This field will be ignored when
inserting a new PublicDelegatedPrefix. An up-to-date
fingerprint must be provided in order to update thePublicDelegatedPrefix, otherwise the request will fail with
error 412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve a PublicDelegatedPrefix. |
| `parent_prefix` | String |  | The URL of parent prefix. Either PublicAdvertisedPrefix or
PublicDelegatedPrefix. |
| `ip_cidr_range` | String |  | The IP address range, in CIDR format, represented by this public
delegated prefix. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `kind` | String | [Output Only] Type of the resource. Alwayscompute#publicDelegatedPrefix for public delegated prefixes. |
| `self_link_with_id` | String | [Output Only] Server-defined URL with id for the resource. |
| `region` | String | [Output Only] URL of the region where the public delegated prefix resides.
This field applies only to the region resource. You must specify this
field as part of the HTTP request URL. It is not settable as a field in
the request body. |
| `id` | String | [Output Only] The unique identifier for the resource type. The server
generates this identifier. |
| `status` | String | [Output Only] The status of the public delegated prefix, which can be one
of following values:
   
   
     - `INITIALIZING` The public delegated prefix is being initialized and
     addresses cannot be created yet.
     - `READY_TO_ANNOUNCE` The public delegated prefix is a live migration
     prefix and is active.
     - `ANNOUNCED` The public delegated prefix is announced and ready to
     use.
     - `DELETING` The public delegated prefix is being deprovsioned.
     - `ACTIVE` The public delegated prefix is ready to use. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `allocatable_prefix_length` | i64 | The allocatable prefix length supported by this public delegated prefix.
This field is optional and cannot be set for prefixes in DELEGATION mode.
It cannot be set for IPv4 prefixes either, and it always defaults to 32. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `mode` | String | The public delegated prefix mode for IPv6 only. |
| `purpose` | String | The purpose of the public delegated prefix.

This field can only be set for the top-level global public delegated
prefix. It is an output-only field for the sub-delegates that inherit the
value from the top-level global public delegated prefix. Once the value is
set, it cannot be changed.

The field cannot be set for regional public delegated prefixes.

The supported values are:
   
   
     - APPLICATION_AND_PROXY_LOAD_BALANCERS: The global public
     delegated prefix can only be used by Global External Application and
     Proxy Load Balancers to allocate addresses for forwarding rules. This is
     the default value.
     - PASSTHROUGH_LOAD_BALANCER_AVAILABILITY_GROUP0: The
     global public delegated prefix can only be used by Global External
     Passthrough Network Load Balancers to allocate Availability Group 0
     addresses for forwarding rules.
     - PASSTHROUGH_LOAD_BALANCER_AVAILABILITY_GROUP1: The
     global public delegated prefix can only be used by Global External
     Passthrough Network Load Balancers to allocate Availability Group 1
     addresses for forwarding rules. |
| `enable_enhanced_ipv4_allocation` | bool | [Output Only] Whether this PDP supports enhanced IPv4 allocations.
Applicable for IPv4 PDPs only. |
| `ipv6_access_type` | String | [Output Only] The internet access type for IPv6 Public Delegated Prefixes.
Inherited from parent prefix. |
| `public_delegated_sub_prefixs` | Vec<String> | The list of sub public delegated prefixes that exist for this public
delegated prefix. |
| `is_live_migration` | bool | If true, the prefix will be live migrated. |
| `name` | String | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `byoip_api_version` | String | [Output Only] The version of BYOIP API. |
| `fingerprint` | String | Fingerprint of this resource. A hash of the contents stored in this object.
This field is used in optimistic locking. This field will be ignored when
inserting a new PublicDelegatedPrefix. An up-to-date
fingerprint must be provided in order to update thePublicDelegatedPrefix, otherwise the request will fail with
error 412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve a PublicDelegatedPrefix. |
| `parent_prefix` | String | The URL of parent prefix. Either PublicAdvertisedPrefix or
PublicDelegatedPrefix. |
| `ip_cidr_range` | String | The IP address range, in CIDR format, represented by this public
delegated prefix. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create global_public_delegated_prefixe
global_public_delegated_prefixe = provider.compute_api.Global_public_delegated_prefixe {
    project = "value"  # Project ID for this request.
}

# Access global_public_delegated_prefixe outputs
global_public_delegated_prefixe_id = global_public_delegated_prefixe.id
global_public_delegated_prefixe_kind = global_public_delegated_prefixe.kind
global_public_delegated_prefixe_self_link_with_id = global_public_delegated_prefixe.self_link_with_id
global_public_delegated_prefixe_region = global_public_delegated_prefixe.region
global_public_delegated_prefixe_id = global_public_delegated_prefixe.id
global_public_delegated_prefixe_status = global_public_delegated_prefixe.status
global_public_delegated_prefixe_self_link = global_public_delegated_prefixe.self_link
global_public_delegated_prefixe_allocatable_prefix_length = global_public_delegated_prefixe.allocatable_prefix_length
global_public_delegated_prefixe_description = global_public_delegated_prefixe.description
global_public_delegated_prefixe_mode = global_public_delegated_prefixe.mode
global_public_delegated_prefixe_purpose = global_public_delegated_prefixe.purpose
global_public_delegated_prefixe_enable_enhanced_ipv4_allocation = global_public_delegated_prefixe.enable_enhanced_ipv4_allocation
global_public_delegated_prefixe_ipv6_access_type = global_public_delegated_prefixe.ipv6_access_type
global_public_delegated_prefixe_public_delegated_sub_prefixs = global_public_delegated_prefixe.public_delegated_sub_prefixs
global_public_delegated_prefixe_is_live_migration = global_public_delegated_prefixe.is_live_migration
global_public_delegated_prefixe_name = global_public_delegated_prefixe.name
global_public_delegated_prefixe_creation_timestamp = global_public_delegated_prefixe.creation_timestamp
global_public_delegated_prefixe_byoip_api_version = global_public_delegated_prefixe.byoip_api_version
global_public_delegated_prefixe_fingerprint = global_public_delegated_prefixe.fingerprint
global_public_delegated_prefixe_parent_prefix = global_public_delegated_prefixe.parent_prefix
global_public_delegated_prefixe_ip_cidr_range = global_public_delegated_prefixe.ip_cidr_range
```

---


### Public_delegated_prefixe

Creates a PublicDelegatedPrefix in the specified project in the given
region using the parameters that are included in the request.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `kind` | String |  | [Output Only] Type of the resource. Alwayscompute#publicDelegatedPrefix for public delegated prefixes. |
| `self_link_with_id` | String |  | [Output Only] Server-defined URL with id for the resource. |
| `region` | String |  | [Output Only] URL of the region where the public delegated prefix resides.
This field applies only to the region resource. You must specify this
field as part of the HTTP request URL. It is not settable as a field in
the request body. |
| `id` | String |  | [Output Only] The unique identifier for the resource type. The server
generates this identifier. |
| `status` | String |  | [Output Only] The status of the public delegated prefix, which can be one
of following values:
   
   
     - `INITIALIZING` The public delegated prefix is being initialized and
     addresses cannot be created yet.
     - `READY_TO_ANNOUNCE` The public delegated prefix is a live migration
     prefix and is active.
     - `ANNOUNCED` The public delegated prefix is announced and ready to
     use.
     - `DELETING` The public delegated prefix is being deprovsioned.
     - `ACTIVE` The public delegated prefix is ready to use. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `allocatable_prefix_length` | i64 |  | The allocatable prefix length supported by this public delegated prefix.
This field is optional and cannot be set for prefixes in DELEGATION mode.
It cannot be set for IPv4 prefixes either, and it always defaults to 32. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `mode` | String |  | The public delegated prefix mode for IPv6 only. |
| `purpose` | String |  | The purpose of the public delegated prefix.

This field can only be set for the top-level global public delegated
prefix. It is an output-only field for the sub-delegates that inherit the
value from the top-level global public delegated prefix. Once the value is
set, it cannot be changed.

The field cannot be set for regional public delegated prefixes.

The supported values are:
   
   
     - APPLICATION_AND_PROXY_LOAD_BALANCERS: The global public
     delegated prefix can only be used by Global External Application and
     Proxy Load Balancers to allocate addresses for forwarding rules. This is
     the default value.
     - PASSTHROUGH_LOAD_BALANCER_AVAILABILITY_GROUP0: The
     global public delegated prefix can only be used by Global External
     Passthrough Network Load Balancers to allocate Availability Group 0
     addresses for forwarding rules.
     - PASSTHROUGH_LOAD_BALANCER_AVAILABILITY_GROUP1: The
     global public delegated prefix can only be used by Global External
     Passthrough Network Load Balancers to allocate Availability Group 1
     addresses for forwarding rules. |
| `enable_enhanced_ipv4_allocation` | bool |  | [Output Only] Whether this PDP supports enhanced IPv4 allocations.
Applicable for IPv4 PDPs only. |
| `ipv6_access_type` | String |  | [Output Only] The internet access type for IPv6 Public Delegated Prefixes.
Inherited from parent prefix. |
| `public_delegated_sub_prefixs` | Vec<String> |  | The list of sub public delegated prefixes that exist for this public
delegated prefix. |
| `is_live_migration` | bool |  | If true, the prefix will be live migrated. |
| `name` | String |  | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `byoip_api_version` | String |  | [Output Only] The version of BYOIP API. |
| `fingerprint` | String |  | Fingerprint of this resource. A hash of the contents stored in this object.
This field is used in optimistic locking. This field will be ignored when
inserting a new PublicDelegatedPrefix. An up-to-date
fingerprint must be provided in order to update thePublicDelegatedPrefix, otherwise the request will fail with
error 412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve a PublicDelegatedPrefix. |
| `parent_prefix` | String |  | The URL of parent prefix. Either PublicAdvertisedPrefix or
PublicDelegatedPrefix. |
| `ip_cidr_range` | String |  | The IP address range, in CIDR format, represented by this public
delegated prefix. |
| `region` | String | ✅ | Name of the region of this request. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `kind` | String | [Output Only] Type of the resource. Alwayscompute#publicDelegatedPrefix for public delegated prefixes. |
| `self_link_with_id` | String | [Output Only] Server-defined URL with id for the resource. |
| `region` | String | [Output Only] URL of the region where the public delegated prefix resides.
This field applies only to the region resource. You must specify this
field as part of the HTTP request URL. It is not settable as a field in
the request body. |
| `id` | String | [Output Only] The unique identifier for the resource type. The server
generates this identifier. |
| `status` | String | [Output Only] The status of the public delegated prefix, which can be one
of following values:
   
   
     - `INITIALIZING` The public delegated prefix is being initialized and
     addresses cannot be created yet.
     - `READY_TO_ANNOUNCE` The public delegated prefix is a live migration
     prefix and is active.
     - `ANNOUNCED` The public delegated prefix is announced and ready to
     use.
     - `DELETING` The public delegated prefix is being deprovsioned.
     - `ACTIVE` The public delegated prefix is ready to use. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `allocatable_prefix_length` | i64 | The allocatable prefix length supported by this public delegated prefix.
This field is optional and cannot be set for prefixes in DELEGATION mode.
It cannot be set for IPv4 prefixes either, and it always defaults to 32. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `mode` | String | The public delegated prefix mode for IPv6 only. |
| `purpose` | String | The purpose of the public delegated prefix.

This field can only be set for the top-level global public delegated
prefix. It is an output-only field for the sub-delegates that inherit the
value from the top-level global public delegated prefix. Once the value is
set, it cannot be changed.

The field cannot be set for regional public delegated prefixes.

The supported values are:
   
   
     - APPLICATION_AND_PROXY_LOAD_BALANCERS: The global public
     delegated prefix can only be used by Global External Application and
     Proxy Load Balancers to allocate addresses for forwarding rules. This is
     the default value.
     - PASSTHROUGH_LOAD_BALANCER_AVAILABILITY_GROUP0: The
     global public delegated prefix can only be used by Global External
     Passthrough Network Load Balancers to allocate Availability Group 0
     addresses for forwarding rules.
     - PASSTHROUGH_LOAD_BALANCER_AVAILABILITY_GROUP1: The
     global public delegated prefix can only be used by Global External
     Passthrough Network Load Balancers to allocate Availability Group 1
     addresses for forwarding rules. |
| `enable_enhanced_ipv4_allocation` | bool | [Output Only] Whether this PDP supports enhanced IPv4 allocations.
Applicable for IPv4 PDPs only. |
| `ipv6_access_type` | String | [Output Only] The internet access type for IPv6 Public Delegated Prefixes.
Inherited from parent prefix. |
| `public_delegated_sub_prefixs` | Vec<String> | The list of sub public delegated prefixes that exist for this public
delegated prefix. |
| `is_live_migration` | bool | If true, the prefix will be live migrated. |
| `name` | String | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `byoip_api_version` | String | [Output Only] The version of BYOIP API. |
| `fingerprint` | String | Fingerprint of this resource. A hash of the contents stored in this object.
This field is used in optimistic locking. This field will be ignored when
inserting a new PublicDelegatedPrefix. An up-to-date
fingerprint must be provided in order to update thePublicDelegatedPrefix, otherwise the request will fail with
error 412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve a PublicDelegatedPrefix. |
| `parent_prefix` | String | The URL of parent prefix. Either PublicAdvertisedPrefix or
PublicDelegatedPrefix. |
| `ip_cidr_range` | String | The IP address range, in CIDR format, represented by this public
delegated prefix. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create public_delegated_prefixe
public_delegated_prefixe = provider.compute_api.Public_delegated_prefixe {
    region = "value"  # Name of the region of this request.
    project = "value"  # Project ID for this request.
}

# Access public_delegated_prefixe outputs
public_delegated_prefixe_id = public_delegated_prefixe.id
public_delegated_prefixe_kind = public_delegated_prefixe.kind
public_delegated_prefixe_self_link_with_id = public_delegated_prefixe.self_link_with_id
public_delegated_prefixe_region = public_delegated_prefixe.region
public_delegated_prefixe_id = public_delegated_prefixe.id
public_delegated_prefixe_status = public_delegated_prefixe.status
public_delegated_prefixe_self_link = public_delegated_prefixe.self_link
public_delegated_prefixe_allocatable_prefix_length = public_delegated_prefixe.allocatable_prefix_length
public_delegated_prefixe_description = public_delegated_prefixe.description
public_delegated_prefixe_mode = public_delegated_prefixe.mode
public_delegated_prefixe_purpose = public_delegated_prefixe.purpose
public_delegated_prefixe_enable_enhanced_ipv4_allocation = public_delegated_prefixe.enable_enhanced_ipv4_allocation
public_delegated_prefixe_ipv6_access_type = public_delegated_prefixe.ipv6_access_type
public_delegated_prefixe_public_delegated_sub_prefixs = public_delegated_prefixe.public_delegated_sub_prefixs
public_delegated_prefixe_is_live_migration = public_delegated_prefixe.is_live_migration
public_delegated_prefixe_name = public_delegated_prefixe.name
public_delegated_prefixe_creation_timestamp = public_delegated_prefixe.creation_timestamp
public_delegated_prefixe_byoip_api_version = public_delegated_prefixe.byoip_api_version
public_delegated_prefixe_fingerprint = public_delegated_prefixe.fingerprint
public_delegated_prefixe_parent_prefix = public_delegated_prefixe.parent_prefix
public_delegated_prefixe_ip_cidr_range = public_delegated_prefixe.ip_cidr_range
```

---


### Region_snapshot

Creates a snapshot in the specified region using the data included
in the request.

**Operations**: ✅ Create ✅ Read ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `source_instant_snapshot_encryption_key` | String |  | Customer provided encryption key when creating Snapshot from Instant
Snapshot. |
| `params` | String |  | Input only. [Input Only] Additional params passed with the request, but not persisted
as part of resource payload. |
| `status` | String |  | [Output Only] The status of the snapshot. This can beCREATING, DELETING, FAILED,READY, or UPLOADING. |
| `snapshot_type` | String |  | Indicates the type of the snapshot. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `chain_name` | String |  | Creates the new snapshot in the snapshot chain labeled with the
specified name. The chain name must be 1-63 characters long and comply
with RFC1035. This is an uncommon option only for advanced service
owners who needs to create separate snapshot chains, for example,
for chargeback tracking. When you describe your snapshot resource, this
field is visible only if it has a non-empty value. |
| `user_licenses` | Vec<String> |  | [Output Only] A list of user provided licenses represented by a list of
URLs to the license resource. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `auto_created` | bool |  | [Output Only] Set to true if snapshots are automatically created by
applying resource policy on the target disk. |
| `kind` | String |  | [Output Only] Type of the resource. Always compute#snapshot
for Snapshot resources. |
| `max_retention_days` | i64 |  | Number of days the snapshot should be retained before being deleted
automatically. |
| `guest_os_features` | Vec<String> |  | [Output Only] A list of features to enable on the guest operating system.
Applicable only for bootable images. Read 
Enabling guest operating system features to see a list of available
options. |
| `source_snapshot_schedule_policy` | String |  | [Output Only] URL of the resource policy which created this
scheduled snapshot. |
| `storage_bytes_status` | String |  | [Output Only] An indicator whether storageBytes is in a
stable state or it is being adjusted as a result of shared storage
reallocation. This status can either be UPDATING, meaning
the size of the snapshot is being updated, or UP_TO_DATE,
meaning the size of the snapshot is up-to-date. |
| `labels` | HashMap<String, String> |  | Labels to apply to this snapshot. These can be later modified by
the setLabels method.
Label values may be empty. |
| `creation_size_bytes` | String |  | [Output Only] Size in bytes of the snapshot at creation time. |
| `satisfies_pzs` | bool |  | [Output Only] Reserved for future use. |
| `location_hint` | String |  | An opaque location hint used to place the snapshot close to other
resources.
This field is for use by internal tools that use the public API. |
| `self_link_with_id` | String |  | [Output Only] Server-defined URL for this resource's resource id. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `guest_flush` | bool |  | [Input Only] Whether to attempt an application consistent snapshot by
informing the OS to prepare for the snapshot process. |
| `download_bytes` | String |  | [Output Only] Number of bytes downloaded to restore a snapshot to a disk. |
| `snapshot_group_name` | String |  | [Output only] The snapshot group that this snapshot belongs to. |
| `source_disk` | String |  | The source disk used to create this snapshot. |
| `source_disk_id` | String |  | [Output Only] The ID value of the disk used to create this snapshot. This
value may be used to determine whether the snapshot was taken from the
current or a previous instance of a given disk name. |
| `resource_status` | String |  | [Output only] ResourceStatus of the Snapshot resource |
| `disk_size_gb` | String |  | [Output Only] Size of the source disk, specified in GB. |
| `source_disk_encryption_key` | String |  | The customer-supplied
encryption key of the source disk. Required if the source disk is
protected by a customer-supplied encryption key. |
| `storage_bytes` | String |  | [Output Only] A size of the storage used by the snapshot. As snapshots
share storage, this number is expected to change with snapshot
creation/deletion. |
| `licenses` | Vec<String> |  | [Output Only] A list of public visible licenses that apply to this
snapshot. This can be because the original image had licenses attached
(such as a Windows image). |
| `source_instant_snapshot_id` | String |  | [Output Only] The unique ID of the instant snapshot used to create this
snapshot. This value identifies the exact instant snapshot that was used to
create this snapshot. For example, if you created the snapshot from an
instant snapshot that was later deleted and recreated under the same name,
the source instant snapshot ID would identify the exact instant snapshot
that was used. |
| `source_snapshot_schedule_policy_id` | String |  | [Output Only] ID of the resource policy which created this
scheduled snapshot. |
| `storage_locations` | Vec<String> |  | Cloud Storage bucket storage location of the snapshot (regional or
multi-regional). |
| `license_codes` | Vec<String> |  | [Output Only] Integer license codes indicating which licenses are attached
to this snapshot. |
| `snapshot_encryption_key` | String |  | Encrypts the snapshot using acustomer-supplied
encryption key.

After you encrypt a snapshot using a customer-supplied key, you must
provide the same key if you use the snapshot later. For example, you must
provide the encryption key when you create a disk from the encrypted
snapshot in a future request.

Customer-supplied encryption keys do not protect access to metadata
of the snapshot.

If you do not provide an encryption key when creating the snapshot, then
the snapshot will be encrypted using an automatically generated key and you
do not need to provide a key to use the snapshot later. |
| `enable_confidential_compute` | bool |  | Whether this snapshot is created from a confidential compute mode disk.
[Output Only]: This field is not set by user, but from source disk. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `source_instant_snapshot` | String |  | The source instant snapshot used to create this snapshot. You can provide
this as a partial or full URL to the resource. For example, the following
are valid values:
   
   
     - https://www.googleapis.com/compute/v1/projects/project/zones/zone/instantSnapshots/instantSnapshot
     - projects/project/zones/zone/instantSnapshots/instantSnapshot 
   - zones/zone/instantSnapshots/instantSnapshot |
| `region` | String |  | [Output Only] URL of the region where the snapshot resides. Only applicable
for regional snapshots. |
| `architecture` | String |  | [Output Only] The architecture of the snapshot. Valid values are
ARM64 or X86_64. |
| `satisfies_pzi` | bool |  | Output only. Reserved for future use. |
| `name` | String |  | Name of the resource; provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must be
a dash, lowercase letter, or digit, except the last character, which cannot
be a dash. |
| `source_disk_for_recovery_checkpoint` | String |  | The source disk whose recovery checkpoint will be used to create this
snapshot. |
| `snapshot_group_id` | String |  | [Output Only] The unique ID of the snapshot group that this snapshot
belongs to. |
| `label_fingerprint` | String |  | A fingerprint for the labels being applied to this snapshot, which is
essentially a hash of the labels set used for optimistic locking. The
fingerprint is initially generated by Compute Engine and changes after
every request to modify or update labels. You must always provide an
up-to-date fingerprint hash in order to update or change labels,
otherwise the request will fail with error412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve a snapshot. |
| `project` | String | ✅ | Project ID for this request. |
| `region` | String | ✅ | Name of the region for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `source_instant_snapshot_encryption_key` | String | Customer provided encryption key when creating Snapshot from Instant
Snapshot. |
| `params` | String | Input only. [Input Only] Additional params passed with the request, but not persisted
as part of resource payload. |
| `status` | String | [Output Only] The status of the snapshot. This can beCREATING, DELETING, FAILED,READY, or UPLOADING. |
| `snapshot_type` | String | Indicates the type of the snapshot. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `chain_name` | String | Creates the new snapshot in the snapshot chain labeled with the
specified name. The chain name must be 1-63 characters long and comply
with RFC1035. This is an uncommon option only for advanced service
owners who needs to create separate snapshot chains, for example,
for chargeback tracking. When you describe your snapshot resource, this
field is visible only if it has a non-empty value. |
| `user_licenses` | Vec<String> | [Output Only] A list of user provided licenses represented by a list of
URLs to the license resource. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `auto_created` | bool | [Output Only] Set to true if snapshots are automatically created by
applying resource policy on the target disk. |
| `kind` | String | [Output Only] Type of the resource. Always compute#snapshot
for Snapshot resources. |
| `max_retention_days` | i64 | Number of days the snapshot should be retained before being deleted
automatically. |
| `guest_os_features` | Vec<String> | [Output Only] A list of features to enable on the guest operating system.
Applicable only for bootable images. Read 
Enabling guest operating system features to see a list of available
options. |
| `source_snapshot_schedule_policy` | String | [Output Only] URL of the resource policy which created this
scheduled snapshot. |
| `storage_bytes_status` | String | [Output Only] An indicator whether storageBytes is in a
stable state or it is being adjusted as a result of shared storage
reallocation. This status can either be UPDATING, meaning
the size of the snapshot is being updated, or UP_TO_DATE,
meaning the size of the snapshot is up-to-date. |
| `labels` | HashMap<String, String> | Labels to apply to this snapshot. These can be later modified by
the setLabels method.
Label values may be empty. |
| `creation_size_bytes` | String | [Output Only] Size in bytes of the snapshot at creation time. |
| `satisfies_pzs` | bool | [Output Only] Reserved for future use. |
| `location_hint` | String | An opaque location hint used to place the snapshot close to other
resources.
This field is for use by internal tools that use the public API. |
| `self_link_with_id` | String | [Output Only] Server-defined URL for this resource's resource id. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `guest_flush` | bool | [Input Only] Whether to attempt an application consistent snapshot by
informing the OS to prepare for the snapshot process. |
| `download_bytes` | String | [Output Only] Number of bytes downloaded to restore a snapshot to a disk. |
| `snapshot_group_name` | String | [Output only] The snapshot group that this snapshot belongs to. |
| `source_disk` | String | The source disk used to create this snapshot. |
| `source_disk_id` | String | [Output Only] The ID value of the disk used to create this snapshot. This
value may be used to determine whether the snapshot was taken from the
current or a previous instance of a given disk name. |
| `resource_status` | String | [Output only] ResourceStatus of the Snapshot resource |
| `disk_size_gb` | String | [Output Only] Size of the source disk, specified in GB. |
| `source_disk_encryption_key` | String | The customer-supplied
encryption key of the source disk. Required if the source disk is
protected by a customer-supplied encryption key. |
| `storage_bytes` | String | [Output Only] A size of the storage used by the snapshot. As snapshots
share storage, this number is expected to change with snapshot
creation/deletion. |
| `licenses` | Vec<String> | [Output Only] A list of public visible licenses that apply to this
snapshot. This can be because the original image had licenses attached
(such as a Windows image). |
| `source_instant_snapshot_id` | String | [Output Only] The unique ID of the instant snapshot used to create this
snapshot. This value identifies the exact instant snapshot that was used to
create this snapshot. For example, if you created the snapshot from an
instant snapshot that was later deleted and recreated under the same name,
the source instant snapshot ID would identify the exact instant snapshot
that was used. |
| `source_snapshot_schedule_policy_id` | String | [Output Only] ID of the resource policy which created this
scheduled snapshot. |
| `storage_locations` | Vec<String> | Cloud Storage bucket storage location of the snapshot (regional or
multi-regional). |
| `license_codes` | Vec<String> | [Output Only] Integer license codes indicating which licenses are attached
to this snapshot. |
| `snapshot_encryption_key` | String | Encrypts the snapshot using acustomer-supplied
encryption key.

After you encrypt a snapshot using a customer-supplied key, you must
provide the same key if you use the snapshot later. For example, you must
provide the encryption key when you create a disk from the encrypted
snapshot in a future request.

Customer-supplied encryption keys do not protect access to metadata
of the snapshot.

If you do not provide an encryption key when creating the snapshot, then
the snapshot will be encrypted using an automatically generated key and you
do not need to provide a key to use the snapshot later. |
| `enable_confidential_compute` | bool | Whether this snapshot is created from a confidential compute mode disk.
[Output Only]: This field is not set by user, but from source disk. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `source_instant_snapshot` | String | The source instant snapshot used to create this snapshot. You can provide
this as a partial or full URL to the resource. For example, the following
are valid values:
   
   
     - https://www.googleapis.com/compute/v1/projects/project/zones/zone/instantSnapshots/instantSnapshot
     - projects/project/zones/zone/instantSnapshots/instantSnapshot 
   - zones/zone/instantSnapshots/instantSnapshot |
| `region` | String | [Output Only] URL of the region where the snapshot resides. Only applicable
for regional snapshots. |
| `architecture` | String | [Output Only] The architecture of the snapshot. Valid values are
ARM64 or X86_64. |
| `satisfies_pzi` | bool | Output only. Reserved for future use. |
| `name` | String | Name of the resource; provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must be
a dash, lowercase letter, or digit, except the last character, which cannot
be a dash. |
| `source_disk_for_recovery_checkpoint` | String | The source disk whose recovery checkpoint will be used to create this
snapshot. |
| `snapshot_group_id` | String | [Output Only] The unique ID of the snapshot group that this snapshot
belongs to. |
| `label_fingerprint` | String | A fingerprint for the labels being applied to this snapshot, which is
essentially a hash of the labels set used for optimistic locking. The
fingerprint is initially generated by Compute Engine and changes after
every request to modify or update labels. You must always provide an
up-to-date fingerprint hash in order to update or change labels,
otherwise the request will fail with error412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve a snapshot. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create region_snapshot
region_snapshot = provider.compute_api.Region_snapshot {
    project = "value"  # Project ID for this request.
    region = "value"  # Name of the region for this request.
}

# Access region_snapshot outputs
region_snapshot_id = region_snapshot.id
region_snapshot_source_instant_snapshot_encryption_key = region_snapshot.source_instant_snapshot_encryption_key
region_snapshot_params = region_snapshot.params
region_snapshot_status = region_snapshot.status
region_snapshot_snapshot_type = region_snapshot.snapshot_type
region_snapshot_description = region_snapshot.description
region_snapshot_chain_name = region_snapshot.chain_name
region_snapshot_user_licenses = region_snapshot.user_licenses
region_snapshot_id = region_snapshot.id
region_snapshot_auto_created = region_snapshot.auto_created
region_snapshot_kind = region_snapshot.kind
region_snapshot_max_retention_days = region_snapshot.max_retention_days
region_snapshot_guest_os_features = region_snapshot.guest_os_features
region_snapshot_source_snapshot_schedule_policy = region_snapshot.source_snapshot_schedule_policy
region_snapshot_storage_bytes_status = region_snapshot.storage_bytes_status
region_snapshot_labels = region_snapshot.labels
region_snapshot_creation_size_bytes = region_snapshot.creation_size_bytes
region_snapshot_satisfies_pzs = region_snapshot.satisfies_pzs
region_snapshot_location_hint = region_snapshot.location_hint
region_snapshot_self_link_with_id = region_snapshot.self_link_with_id
region_snapshot_creation_timestamp = region_snapshot.creation_timestamp
region_snapshot_guest_flush = region_snapshot.guest_flush
region_snapshot_download_bytes = region_snapshot.download_bytes
region_snapshot_snapshot_group_name = region_snapshot.snapshot_group_name
region_snapshot_source_disk = region_snapshot.source_disk
region_snapshot_source_disk_id = region_snapshot.source_disk_id
region_snapshot_resource_status = region_snapshot.resource_status
region_snapshot_disk_size_gb = region_snapshot.disk_size_gb
region_snapshot_source_disk_encryption_key = region_snapshot.source_disk_encryption_key
region_snapshot_storage_bytes = region_snapshot.storage_bytes
region_snapshot_licenses = region_snapshot.licenses
region_snapshot_source_instant_snapshot_id = region_snapshot.source_instant_snapshot_id
region_snapshot_source_snapshot_schedule_policy_id = region_snapshot.source_snapshot_schedule_policy_id
region_snapshot_storage_locations = region_snapshot.storage_locations
region_snapshot_license_codes = region_snapshot.license_codes
region_snapshot_snapshot_encryption_key = region_snapshot.snapshot_encryption_key
region_snapshot_enable_confidential_compute = region_snapshot.enable_confidential_compute
region_snapshot_self_link = region_snapshot.self_link
region_snapshot_source_instant_snapshot = region_snapshot.source_instant_snapshot
region_snapshot_region = region_snapshot.region
region_snapshot_architecture = region_snapshot.architecture
region_snapshot_satisfies_pzi = region_snapshot.satisfies_pzi
region_snapshot_name = region_snapshot.name
region_snapshot_source_disk_for_recovery_checkpoint = region_snapshot.source_disk_for_recovery_checkpoint
region_snapshot_snapshot_group_id = region_snapshot.snapshot_group_id
region_snapshot_label_fingerprint = region_snapshot.label_fingerprint
```

---


### Region_network_endpoint_group

Creates a network endpoint group in the specified project using the
parameters that are included in the request.

**Operations**: ✅ Create ✅ Read ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `load_balancer` | String |  | This field is only valid when the network endpoint group is used for load
balancing.
[Deprecated] This field is deprecated. |
| `psc_target_service` | String |  | The target service url used to set up private service connection to
a Google API or a PSC Producer Service Attachment.
An example value is: asia-northeast3-cloudkms.googleapis.com.

Optional. Only valid when networkEndpointType isPRIVATE_SERVICE_CONNECT. |
| `cloud_function` | String |  | Optional. Only valid when networkEndpointType isSERVERLESS. Only one of cloudRun,appEngine or cloudFunction may be set. |
| `region` | String |  | [Output Only] The URL of theregion
where the network endpoint group is located. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `size` | i64 |  | [Output only] Number of network endpoints in the network endpoint group. |
| `subnetwork` | String |  | Optional URL of the subnetwork to which all network endpoints in the NEG
belong. |
| `app_engine` | String |  | Optional. Only valid when networkEndpointType isSERVERLESS. Only one of cloudRun,appEngine or cloudFunction may be set. |
| `annotations` | HashMap<String, String> |  | Optional. Metadata defined as annotations on the network endpoint group. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `kind` | String |  | [Output Only] Type of the resource. Alwayscompute#networkEndpointGroup for network endpoint group. |
| `default_port` | i64 |  | The default port used if the port number is not specified in the network
endpoint.

Optional. If the network endpoint type is either GCE_VM_IP,SERVERLESS or PRIVATE_SERVICE_CONNECT, this
field must not be specified. |
| `cloud_run` | String |  | Optional. Only valid when networkEndpointType isSERVERLESS. Only one of cloudRun,appEngine or cloudFunction may be set. |
| `network` | String |  | The URL of the network to which all network endpoints in the NEG belong.
Uses default project network if unspecified. |
| `psc_data` | String |  | Optional. Only valid when networkEndpointType isPRIVATE_SERVICE_CONNECT. |
| `name` | String |  | Name of the resource; provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must be
a dash, lowercase letter, or digit, except the last character, which cannot
be a dash. |
| `network_endpoint_type` | String |  | Type of network endpoints in this network endpoint group. Can be one ofGCE_VM_IP, GCE_VM_IP_PORT,NON_GCP_PRIVATE_IP_PORT, INTERNET_FQDN_PORT,INTERNET_IP_PORT, SERVERLESS,PRIVATE_SERVICE_CONNECT, GCE_VM_IP_PORTMAP. |
| `serverless_deployment` | String |  | Only valid when networkEndpointType isSERVERLESS. Only one of cloudRun,appEngine, cloudFunction orserverlessDeployment may be set. |
| `type` | String |  | Specify the type of this network endpoint group. OnlyLOAD_BALANCING is valid for now. |
| `zone` | String |  | [Output Only] The URL of thezone
where the network endpoint group is located. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `self_link_with_id` | String |  | [Output Only] Server-defined URL for this resource with the resource id. |
| `project` | String | ✅ | Project ID for this request. |
| `region` | String | ✅ | The name of the region where
you want to create the network endpoint group. It should comply with
RFC1035. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `load_balancer` | String | This field is only valid when the network endpoint group is used for load
balancing.
[Deprecated] This field is deprecated. |
| `psc_target_service` | String | The target service url used to set up private service connection to
a Google API or a PSC Producer Service Attachment.
An example value is: asia-northeast3-cloudkms.googleapis.com.

Optional. Only valid when networkEndpointType isPRIVATE_SERVICE_CONNECT. |
| `cloud_function` | String | Optional. Only valid when networkEndpointType isSERVERLESS. Only one of cloudRun,appEngine or cloudFunction may be set. |
| `region` | String | [Output Only] The URL of theregion
where the network endpoint group is located. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `size` | i64 | [Output only] Number of network endpoints in the network endpoint group. |
| `subnetwork` | String | Optional URL of the subnetwork to which all network endpoints in the NEG
belong. |
| `app_engine` | String | Optional. Only valid when networkEndpointType isSERVERLESS. Only one of cloudRun,appEngine or cloudFunction may be set. |
| `annotations` | HashMap<String, String> | Optional. Metadata defined as annotations on the network endpoint group. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `kind` | String | [Output Only] Type of the resource. Alwayscompute#networkEndpointGroup for network endpoint group. |
| `default_port` | i64 | The default port used if the port number is not specified in the network
endpoint.

Optional. If the network endpoint type is either GCE_VM_IP,SERVERLESS or PRIVATE_SERVICE_CONNECT, this
field must not be specified. |
| `cloud_run` | String | Optional. Only valid when networkEndpointType isSERVERLESS. Only one of cloudRun,appEngine or cloudFunction may be set. |
| `network` | String | The URL of the network to which all network endpoints in the NEG belong.
Uses default project network if unspecified. |
| `psc_data` | String | Optional. Only valid when networkEndpointType isPRIVATE_SERVICE_CONNECT. |
| `name` | String | Name of the resource; provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must be
a dash, lowercase letter, or digit, except the last character, which cannot
be a dash. |
| `network_endpoint_type` | String | Type of network endpoints in this network endpoint group. Can be one ofGCE_VM_IP, GCE_VM_IP_PORT,NON_GCP_PRIVATE_IP_PORT, INTERNET_FQDN_PORT,INTERNET_IP_PORT, SERVERLESS,PRIVATE_SERVICE_CONNECT, GCE_VM_IP_PORTMAP. |
| `serverless_deployment` | String | Only valid when networkEndpointType isSERVERLESS. Only one of cloudRun,appEngine, cloudFunction orserverlessDeployment may be set. |
| `type` | String | Specify the type of this network endpoint group. OnlyLOAD_BALANCING is valid for now. |
| `zone` | String | [Output Only] The URL of thezone
where the network endpoint group is located. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `self_link_with_id` | String | [Output Only] Server-defined URL for this resource with the resource id. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create region_network_endpoint_group
region_network_endpoint_group = provider.compute_api.Region_network_endpoint_group {
    project = "value"  # Project ID for this request.
    region = "value"  # The name of the region where
you want to create the network endpoint group. It should comply with
RFC1035.
}

# Access region_network_endpoint_group outputs
region_network_endpoint_group_id = region_network_endpoint_group.id
region_network_endpoint_group_self_link = region_network_endpoint_group.self_link
region_network_endpoint_group_load_balancer = region_network_endpoint_group.load_balancer
region_network_endpoint_group_psc_target_service = region_network_endpoint_group.psc_target_service
region_network_endpoint_group_cloud_function = region_network_endpoint_group.cloud_function
region_network_endpoint_group_region = region_network_endpoint_group.region
region_network_endpoint_group_creation_timestamp = region_network_endpoint_group.creation_timestamp
region_network_endpoint_group_size = region_network_endpoint_group.size
region_network_endpoint_group_subnetwork = region_network_endpoint_group.subnetwork
region_network_endpoint_group_app_engine = region_network_endpoint_group.app_engine
region_network_endpoint_group_annotations = region_network_endpoint_group.annotations
region_network_endpoint_group_id = region_network_endpoint_group.id
region_network_endpoint_group_kind = region_network_endpoint_group.kind
region_network_endpoint_group_default_port = region_network_endpoint_group.default_port
region_network_endpoint_group_cloud_run = region_network_endpoint_group.cloud_run
region_network_endpoint_group_network = region_network_endpoint_group.network
region_network_endpoint_group_psc_data = region_network_endpoint_group.psc_data
region_network_endpoint_group_name = region_network_endpoint_group.name
region_network_endpoint_group_network_endpoint_type = region_network_endpoint_group.network_endpoint_type
region_network_endpoint_group_serverless_deployment = region_network_endpoint_group.serverless_deployment
region_network_endpoint_group_type = region_network_endpoint_group.type
region_network_endpoint_group_zone = region_network_endpoint_group.zone
region_network_endpoint_group_description = region_network_endpoint_group.description
region_network_endpoint_group_self_link_with_id = region_network_endpoint_group.self_link_with_id
```

---


### Organization_security_policie

Creates a new policy in the specified organization using the data included
in the request.

Use of this API to insert firewall policies is deprecated. Use
firewallPolicies.insert instead.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `adaptive_protection_config` | String |  |  |
| `advanced_options_config` | String |  |  |
| `associations` | Vec<String> |  | A list of associations that belong to this policy. |
| `name` | String |  | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `ddos_protection_config` | String |  |  |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `region` | String |  | [Output Only] URL of the region where the regional security policy
resides. This field is not applicable to global security policies. |
| `short_name` | String |  | User-provided name of the organization security policy. The name should be
unique in the organization in which the security policy is created. This
should only be used when SecurityPolicyType is CLOUD_ARMOR.
The name must be 1-63 characters long, and comply with
https://www.ietf.org/rfc/rfc1035.txt. Specifically, the name must be 1-63
characters long and match the regular expression
`[a-z]([-a-z0-9]*[a-z0-9])?` which means the first character must be a
lowercase letter, and all following characters must be a dash, lowercase
letter, or digit, except the last character, which cannot be a dash. |
| `type` | String |  | The type indicates the intended use of the security policy.
   
   - CLOUD_ARMOR: Cloud Armor backend security policies can
   be configured to filter incoming HTTP requests targeting backend services.
   They filter requests before they hit the origin servers.
   - CLOUD_ARMOR_EDGE: Cloud Armor edge security policies can
   be configured to filter incoming HTTP requests targeting backend services
   (including Cloud CDN-enabled) as well as backend buckets (Cloud Storage).
   They filter requests before the request is served from Google's cache.
   - CLOUD_ARMOR_INTERNAL_SERVICE (preview only): Cloud Armor
   internal service policies can be configured to filter HTTP requests
   targeting services managed by Traffic Director in a service mesh. They
   filter requests before the request is served from the application.

- CLOUD_ARMOR_NETWORK: Cloud Armor network policies
can be configured to filter packets targeting network load balancing
resources such as backend services, target pools, target instances, and
instances with external IPs. They filter requests before the request is
served from the application.


This field can be set only at resource creation time. |
| `fingerprint` | String |  | Specifies a fingerprint for this resource, which is essentially a hash of
the metadata's contents and used for optimistic locking. The
fingerprint is initially generated by Compute Engine and changes after
every request to modify or update metadata. You must always provide an
up-to-date fingerprint hash in order to update or change metadata,
otherwise the request will fail with error412 conditionNotMet.

To see the latest fingerprint, make get() request to the
security policy. |
| `user_defined_fields` | Vec<String> |  | Definitions of user-defined fields for CLOUD_ARMOR_NETWORK policies. A
user-defined field consists of up to 4 bytes extracted from a fixed offset
in the packet, relative to the IPv4, IPv6, TCP, or UDP header, with an
optional mask to select certain bits. Rules may then specify matching
values for these fields.

Example:

 userDefinedFields:
 - name: "ipv4_fragment_offset"
   base: IPV4
   offset: 6
   size: 2
   mask: "0x1fff" |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `labels` | HashMap<String, String> |  | Labels for this resource. These can only be added or modified by thesetLabels method. Each label key/value pair must comply withRFC1035.
Label values may be empty. |
| `label_fingerprint` | String |  | A fingerprint for the labels being applied to this security policy, which
is essentially a hash of the labels set used for optimistic locking. The
fingerprint is initially generated by Compute Engine and changes after
every request to modify or update labels. You must always provide an
up-to-date fingerprint hash in order to update or change labels.

To see the latest fingerprint, make get() request to the
security policy. |
| `kind` | String |  | [Output only] Type of the resource. Alwayscompute#securityPolicyfor security policies |
| `parent` | String |  | [Output Only] The parent of the security policy. |
| `rule_tuple_count` | i64 |  | [Output Only] Total count of all security policy rule tuples. A security
policy can not exceed a set number of tuples. |
| `cloud_armor_config` | String |  |  |
| `rules` | Vec<String> |  | A list of rules that belong to this policy.
There must always be a default rule which is a rule with priority
2147483647 and match all condition (for the match condition this means
match  "*" for srcIpRanges and for the networkMatch condition every field
must be either match "*" or not set). If no rules are provided when
creating a security policy, a default rule with action "allow" will be
added. |
| `self_link_with_id` | String |  | [Output Only] Server-defined URL for this resource with the resource id. |
| `display_name` | String |  | User-provided name of the organization security policy. The name should be
unique in the organization in which the security policy is created. This
should only be used when SecurityPolicyType is FIREWALL.
The name must be 1-63 characters long, and comply with
https://www.ietf.org/rfc/rfc1035.txt. Specifically, the name must be 1-63
characters long and match the regular expression
`[a-z]([-a-z0-9]*[a-z0-9])?` which means the first character must be a
lowercase letter, and all following characters must be a dash, lowercase
letter, or digit, except the last character, which cannot be a dash. |
| `recaptcha_options_config` | String |  |  |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `adaptive_protection_config` | String |  |
| `advanced_options_config` | String |  |
| `associations` | Vec<String> | A list of associations that belong to this policy. |
| `name` | String | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `ddos_protection_config` | String |  |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `region` | String | [Output Only] URL of the region where the regional security policy
resides. This field is not applicable to global security policies. |
| `short_name` | String | User-provided name of the organization security policy. The name should be
unique in the organization in which the security policy is created. This
should only be used when SecurityPolicyType is CLOUD_ARMOR.
The name must be 1-63 characters long, and comply with
https://www.ietf.org/rfc/rfc1035.txt. Specifically, the name must be 1-63
characters long and match the regular expression
`[a-z]([-a-z0-9]*[a-z0-9])?` which means the first character must be a
lowercase letter, and all following characters must be a dash, lowercase
letter, or digit, except the last character, which cannot be a dash. |
| `type` | String | The type indicates the intended use of the security policy.
   
   - CLOUD_ARMOR: Cloud Armor backend security policies can
   be configured to filter incoming HTTP requests targeting backend services.
   They filter requests before they hit the origin servers.
   - CLOUD_ARMOR_EDGE: Cloud Armor edge security policies can
   be configured to filter incoming HTTP requests targeting backend services
   (including Cloud CDN-enabled) as well as backend buckets (Cloud Storage).
   They filter requests before the request is served from Google's cache.
   - CLOUD_ARMOR_INTERNAL_SERVICE (preview only): Cloud Armor
   internal service policies can be configured to filter HTTP requests
   targeting services managed by Traffic Director in a service mesh. They
   filter requests before the request is served from the application.

- CLOUD_ARMOR_NETWORK: Cloud Armor network policies
can be configured to filter packets targeting network load balancing
resources such as backend services, target pools, target instances, and
instances with external IPs. They filter requests before the request is
served from the application.


This field can be set only at resource creation time. |
| `fingerprint` | String | Specifies a fingerprint for this resource, which is essentially a hash of
the metadata's contents and used for optimistic locking. The
fingerprint is initially generated by Compute Engine and changes after
every request to modify or update metadata. You must always provide an
up-to-date fingerprint hash in order to update or change metadata,
otherwise the request will fail with error412 conditionNotMet.

To see the latest fingerprint, make get() request to the
security policy. |
| `user_defined_fields` | Vec<String> | Definitions of user-defined fields for CLOUD_ARMOR_NETWORK policies. A
user-defined field consists of up to 4 bytes extracted from a fixed offset
in the packet, relative to the IPv4, IPv6, TCP, or UDP header, with an
optional mask to select certain bits. Rules may then specify matching
values for these fields.

Example:

 userDefinedFields:
 - name: "ipv4_fragment_offset"
   base: IPV4
   offset: 6
   size: 2
   mask: "0x1fff" |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `labels` | HashMap<String, String> | Labels for this resource. These can only be added or modified by thesetLabels method. Each label key/value pair must comply withRFC1035.
Label values may be empty. |
| `label_fingerprint` | String | A fingerprint for the labels being applied to this security policy, which
is essentially a hash of the labels set used for optimistic locking. The
fingerprint is initially generated by Compute Engine and changes after
every request to modify or update labels. You must always provide an
up-to-date fingerprint hash in order to update or change labels.

To see the latest fingerprint, make get() request to the
security policy. |
| `kind` | String | [Output only] Type of the resource. Alwayscompute#securityPolicyfor security policies |
| `parent` | String | [Output Only] The parent of the security policy. |
| `rule_tuple_count` | i64 | [Output Only] Total count of all security policy rule tuples. A security
policy can not exceed a set number of tuples. |
| `cloud_armor_config` | String |  |
| `rules` | Vec<String> | A list of rules that belong to this policy.
There must always be a default rule which is a rule with priority
2147483647 and match all condition (for the match condition this means
match  "*" for srcIpRanges and for the networkMatch condition every field
must be either match "*" or not set). If no rules are provided when
creating a security policy, a default rule with action "allow" will be
added. |
| `self_link_with_id` | String | [Output Only] Server-defined URL for this resource with the resource id. |
| `display_name` | String | User-provided name of the organization security policy. The name should be
unique in the organization in which the security policy is created. This
should only be used when SecurityPolicyType is FIREWALL.
The name must be 1-63 characters long, and comply with
https://www.ietf.org/rfc/rfc1035.txt. Specifically, the name must be 1-63
characters long and match the regular expression
`[a-z]([-a-z0-9]*[a-z0-9])?` which means the first character must be a
lowercase letter, and all following characters must be a dash, lowercase
letter, or digit, except the last character, which cannot be a dash. |
| `recaptcha_options_config` | String |  |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create organization_security_policie
organization_security_policie = provider.compute_api.Organization_security_policie {
}

# Access organization_security_policie outputs
organization_security_policie_id = organization_security_policie.id
organization_security_policie_adaptive_protection_config = organization_security_policie.adaptive_protection_config
organization_security_policie_advanced_options_config = organization_security_policie.advanced_options_config
organization_security_policie_associations = organization_security_policie.associations
organization_security_policie_name = organization_security_policie.name
organization_security_policie_id = organization_security_policie.id
organization_security_policie_ddos_protection_config = organization_security_policie.ddos_protection_config
organization_security_policie_description = organization_security_policie.description
organization_security_policie_self_link = organization_security_policie.self_link
organization_security_policie_region = organization_security_policie.region
organization_security_policie_short_name = organization_security_policie.short_name
organization_security_policie_type = organization_security_policie.type
organization_security_policie_fingerprint = organization_security_policie.fingerprint
organization_security_policie_user_defined_fields = organization_security_policie.user_defined_fields
organization_security_policie_creation_timestamp = organization_security_policie.creation_timestamp
organization_security_policie_labels = organization_security_policie.labels
organization_security_policie_label_fingerprint = organization_security_policie.label_fingerprint
organization_security_policie_kind = organization_security_policie.kind
organization_security_policie_parent = organization_security_policie.parent
organization_security_policie_rule_tuple_count = organization_security_policie.rule_tuple_count
organization_security_policie_cloud_armor_config = organization_security_policie.cloud_armor_config
organization_security_policie_rules = organization_security_policie.rules
organization_security_policie_self_link_with_id = organization_security_policie.self_link_with_id
organization_security_policie_display_name = organization_security_policie.display_name
organization_security_policie_recaptcha_options_config = organization_security_policie.recaptcha_options_config
```

---


### Accelerator_type

Returns the specified accelerator type.

**Operations**: ✅ Read

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `deprecated` | String | [Output Only] The deprecation status associated with this accelerator type. |
| `maximum_cards_per_instance` | i64 | [Output Only] Maximum number of accelerator cards allowed per instance. |
| `self_link` | String | [Output Only] Server-defined, fully qualified URL for this resource. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339 text format. |
| `zone` | String | [Output Only] The name of the zone where the accelerator type resides,
such as us-central1-a.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `kind` | String | [Output Only] The type of the resource. Alwayscompute#acceleratorType for accelerator types. |
| `description` | String | [Output Only] An optional textual description of the resource. |
| `self_link_with_id` | String | [Output Only] Server-defined URL for this resource's resource id. |
| `name` | String | [Output Only] Name of the resource. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Access accelerator_type outputs
accelerator_type_id = accelerator_type.id
accelerator_type_deprecated = accelerator_type.deprecated
accelerator_type_maximum_cards_per_instance = accelerator_type.maximum_cards_per_instance
accelerator_type_self_link = accelerator_type.self_link
accelerator_type_id = accelerator_type.id
accelerator_type_creation_timestamp = accelerator_type.creation_timestamp
accelerator_type_zone = accelerator_type.zone
accelerator_type_kind = accelerator_type.kind
accelerator_type_description = accelerator_type.description
accelerator_type_self_link_with_id = accelerator_type.self_link_with_id
accelerator_type_name = accelerator_type.name
```

---


### Region_health_source

Create a HealthSource in the specified project in the given region
using the parameters that are included in the request.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `source_type` | String |  | Specifies the type of the HealthSource. The only allowed value
is BACKEND_SERVICE. Must be specified when theHealthSource is created, and cannot be mutated. |
| `kind` | String |  | [Output Only] Type of the resource. Alwayscompute#healthSource for health sources. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `id` | String |  | [Output Only] A unique identifier for this resource type. The server
generates this identifier. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `fingerprint` | String |  | Fingerprint of this resource. A hash of the contents stored in this object.
This field is used in optimistic locking. This field will be ignored when
inserting a HealthSource. An up-to-date fingerprint
must be provided in order to patch the HealthSource; Otherwise, the request
will fail with error 412 conditionNotMet. To see the latest
fingerprint, make a get() request to retrieve the
HealthSource. |
| `name` | String |  | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `sources` | Vec<String> |  | URLs to the source resources. Must be size 1. Must be aBackendService if
the sourceType is BACKEND_SERVICE. TheBackendService must have load balancing schemeINTERNAL or INTERNAL_MANAGED and must be regional
and in the same region as the HealthSource (cross-region
deployment for INTERNAL_MANAGED is not supported). TheBackendService may use only IGs, MIGs, or NEGs of typeGCE_VM_IP or GCE_VM_IP_PORT. TheBackendService may not use haPolicy. Can be
mutated. |
| `health_aggregation_policy` | String |  | URL to the HealthAggregationPolicy resource. Must be set. Must
be regional and in the same region as the HealthSource. Can be
mutated. |
| `region` | String |  | [Output Only] URL of the region where the health source resides.
This field applies only to the regional resource. You must specify this
field as part of the HTTP request URL. It is not settable as a field in
the request body. |
| `self_link_with_id` | String |  | [Output Only] Server-defined URL with id for the resource. |
| `project` | String | ✅ | Project ID for this request. |
| `region` | String | ✅ | Name of the region scoping this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `source_type` | String | Specifies the type of the HealthSource. The only allowed value
is BACKEND_SERVICE. Must be specified when theHealthSource is created, and cannot be mutated. |
| `kind` | String | [Output Only] Type of the resource. Alwayscompute#healthSource for health sources. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `id` | String | [Output Only] A unique identifier for this resource type. The server
generates this identifier. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `fingerprint` | String | Fingerprint of this resource. A hash of the contents stored in this object.
This field is used in optimistic locking. This field will be ignored when
inserting a HealthSource. An up-to-date fingerprint
must be provided in order to patch the HealthSource; Otherwise, the request
will fail with error 412 conditionNotMet. To see the latest
fingerprint, make a get() request to retrieve the
HealthSource. |
| `name` | String | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `sources` | Vec<String> | URLs to the source resources. Must be size 1. Must be aBackendService if
the sourceType is BACKEND_SERVICE. TheBackendService must have load balancing schemeINTERNAL or INTERNAL_MANAGED and must be regional
and in the same region as the HealthSource (cross-region
deployment for INTERNAL_MANAGED is not supported). TheBackendService may use only IGs, MIGs, or NEGs of typeGCE_VM_IP or GCE_VM_IP_PORT. TheBackendService may not use haPolicy. Can be
mutated. |
| `health_aggregation_policy` | String | URL to the HealthAggregationPolicy resource. Must be set. Must
be regional and in the same region as the HealthSource. Can be
mutated. |
| `region` | String | [Output Only] URL of the region where the health source resides.
This field applies only to the regional resource. You must specify this
field as part of the HTTP request URL. It is not settable as a field in
the request body. |
| `self_link_with_id` | String | [Output Only] Server-defined URL with id for the resource. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create region_health_source
region_health_source = provider.compute_api.Region_health_source {
    project = "value"  # Project ID for this request.
    region = "value"  # Name of the region scoping this request.
}

# Access region_health_source outputs
region_health_source_id = region_health_source.id
region_health_source_self_link = region_health_source.self_link
region_health_source_source_type = region_health_source.source_type
region_health_source_kind = region_health_source.kind
region_health_source_creation_timestamp = region_health_source.creation_timestamp
region_health_source_id = region_health_source.id
region_health_source_description = region_health_source.description
region_health_source_fingerprint = region_health_source.fingerprint
region_health_source_name = region_health_source.name
region_health_source_sources = region_health_source.sources
region_health_source_health_aggregation_policy = region_health_source.health_aggregation_policy
region_health_source_region = region_health_source.region
region_health_source_self_link_with_id = region_health_source.self_link_with_id
```

---


### Region_operation

Waits for the specified Operation resource to return as `DONE`
or for the request to approach the 2 minute deadline, and retrieves the
specified Operation resource. This method differs from the
`GET` method in that it waits for no more than the default
deadline (2 minutes) and then returns the current state of the operation,
which might be `DONE` or still in progress.

This method is called on a best-effort basis. Specifically:
   
   
    - In uncommon cases, when the server is overloaded, the request might
    return before the default deadline is reached, or might return after zero
    seconds.
   - If the default deadline is reached, there is no guarantee that the
    operation is actually done when the method returns. Be prepared to retry
    if the operation is not `DONE`.

**Operations**: ✅ Create ✅ Read ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `project` | String | ✅ | Project ID for this request. |
| `region` | String | ✅ | Name of the region for this request. |
| `operation` | String | ✅ | Name of the Operations resource to return, or its unique numeric
identifier. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `http_error_status_code` | i64 | [Output Only] If the operation fails, this field contains the HTTP error
status code that was returned. For example, a `404` means the
resource was not found. |
| `description` | String | [Output Only] A textual description of the operation, which is
set when the operation is created. |
| `progress` | i64 | [Output Only] An optional progress indicator that ranges from 0 to 100.
There is no requirement that this be linear or support any granularity of
operations. This should not be used to guess when the operation will be
complete. This number should monotonically increase as the operation
progresses. |
| `start_time` | String | [Output Only] The time that this operation was started by the server.
This value is inRFC3339
text format. |
| `status` | String | [Output Only] The status of the operation, which can be one of the
following:
`PENDING`, `RUNNING`, or `DONE`. |
| `end_time` | String | [Output Only] The time that this operation was completed. This value is inRFC3339
text format. |
| `operation_type` | String | [Output Only] The type of operation, such as `insert`,
`update`, or `delete`, and so on. |
| `region` | String | [Output Only] The URL of the region where the operation resides. Only
applicable when performing regional operations. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `http_error_message` | String | [Output Only] If the operation fails, this field contains the HTTP error
message that was returned, such as `NOT FOUND`. |
| `insert_time` | String | [Output Only] The time that this operation was requested.
This value is inRFC3339
text format. |
| `instances_bulk_insert_operation_metadata` | String |  |
| `self_link_with_id` | String | [Output Only] Server-defined URL for this resource with the resource id. |
| `warnings` | Vec<String> | [Output Only] If warning messages are generated during processing of the
operation, this field will be populated. |
| `zone` | String | [Output Only] The URL of the zone where the operation resides. Only
applicable when performing per-zone operations. |
| `id` | String | [Output Only] The unique identifier for the operation. This identifier is
defined by the server. |
| `set_common_instance_metadata_operation_metadata` | String | [Output Only] If the operation is for projects.setCommonInstanceMetadata,
this field will contain information on all underlying zonal actions and
their state. |
| `client_operation_id` | String | [Output Only] The value of `requestId` if you provided it in the request.
Not present otherwise. |
| `name` | String | [Output Only] Name of the operation. |
| `kind` | String | [Output Only] Type of the resource. Always `compute#operation` for
Operation resources. |
| `operation_group_id` | String | [Output Only] An ID that represents a group of operations, such as when a
group of operations results from a `bulkInsert` API request. |
| `user` | String | [Output Only] User who requested the operation, for example:
`user@example.com` or
`alice_smith_identifier (global/workforcePools/example-com-us-employees)`. |
| `error` | String | [Output Only] If errors are generated during processing of the operation,
this field will be populated. |
| `creation_timestamp` | String | [Deprecated] This field is deprecated. |
| `status_message` | String | [Output Only] An optional textual description of the current status of the
operation. |
| `target_id` | String | [Output Only] The unique target ID, which identifies a specific incarnation
of the target resource. |
| `target_link` | String | [Output Only] The URL of the resource that the operation modifies. For
operations related to creating a snapshot, this points to the disk
that the snapshot was created from. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create region_operation
region_operation = provider.compute_api.Region_operation {
    project = "value"  # Project ID for this request.
    region = "value"  # Name of the region for this request.
    operation = "value"  # Name of the Operations resource to return, or its unique numeric
identifier.
}

# Access region_operation outputs
region_operation_id = region_operation.id
region_operation_http_error_status_code = region_operation.http_error_status_code
region_operation_description = region_operation.description
region_operation_progress = region_operation.progress
region_operation_start_time = region_operation.start_time
region_operation_status = region_operation.status
region_operation_end_time = region_operation.end_time
region_operation_operation_type = region_operation.operation_type
region_operation_region = region_operation.region
region_operation_self_link = region_operation.self_link
region_operation_http_error_message = region_operation.http_error_message
region_operation_insert_time = region_operation.insert_time
region_operation_instances_bulk_insert_operation_metadata = region_operation.instances_bulk_insert_operation_metadata
region_operation_self_link_with_id = region_operation.self_link_with_id
region_operation_warnings = region_operation.warnings
region_operation_zone = region_operation.zone
region_operation_id = region_operation.id
region_operation_set_common_instance_metadata_operation_metadata = region_operation.set_common_instance_metadata_operation_metadata
region_operation_client_operation_id = region_operation.client_operation_id
region_operation_name = region_operation.name
region_operation_kind = region_operation.kind
region_operation_operation_group_id = region_operation.operation_group_id
region_operation_user = region_operation.user
region_operation_error = region_operation.error
region_operation_creation_timestamp = region_operation.creation_timestamp
region_operation_status_message = region_operation.status_message
region_operation_target_id = region_operation.target_id
region_operation_target_link = region_operation.target_link
```

---


### Storage_pool

Creates a storage pool in the specified project using the data
in the request.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `exapool_provisioned_capacity_gb` | String |  | [Output Only] Provisioned capacities for each SKU for this Exapool in GiB |
| `provisioned_iops` | String |  | DEPRECATED -- use "pool provisioned IOPS". |
| `state` | String |  | [Output Only] The status of storage pool creation.
   
   
     - CREATING: Storage pool is provisioning.
     storagePool.
     - FAILED: Storage pool creation failed.
     - READY: Storage pool is ready for use.
     - DELETING: Storage pool is deleting. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `capacity_provisioning_type` | String |  | Provisioning type of the byte capacity of the pool. |
| `pool_provisioned_iops` | String |  | Provisioned IOPS of the storage pool. Only relevant if the storage pool
type is hyperdisk-balanced. |
| `storage_pool_type` | String |  | Type of the storage pool. |
| `self_link_with_id` | String |  | [Output Only] Server-defined URL for this resource's resource id. |
| `pool_provisioned_capacity_gb` | String |  | Size of the storage pool in GiB. For more information about the size
limits, see https://cloud.google.com/compute/docs/disks/storage-pools. |
| `label_fingerprint` | String |  | A fingerprint for the labels being applied to this storage pool, which is
essentially a hash of the labels set used for optimistic locking. The
fingerprint is initially generated by Compute Engine and changes after
every request to modify or update labels. You must always provide an
up-to-date fingerprint hash in order to update or change labels,
otherwise the request will fail with error412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve a storage pool. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `size_gb` | String |  | DEPRECATED -- use "pool provisioned capacity gb". |
| `performance_provisioning_type` | String |  | Provisioning type of the performance-related parameters of the pool,
such as throughput and IOPS. |
| `labels` | HashMap<String, String> |  | Labels to apply to this storage pool. These can be later modified by
the setLabels method. |
| `zone` | String |  | [Output Only] URL of the zone where the storage pool resides.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `status` | String |  | [Output Only] Status information for the storage pool resource. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `kind` | String |  | [Output Only] Type of the resource. Always compute#storagePool
for storage pools. |
| `name` | String |  | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?`
which means the first character must be a lowercase letter, and all
following characters must be a dash, lowercase letter, or digit, except
the last character, which cannot be a dash. |
| `provisioned_throughput` | String |  | DEPRECATED -- use "pool provisioned throughput". |
| `pool_provisioned_throughput` | String |  | Provisioned throughput of the storage pool in MiB/s. Only relevant if the
storage pool type is hyperdisk-balanced or hyperdisk-throughput. |
| `resource_status` | String |  | [Output Only] Status information for the storage pool resource. |
| `self_link` | String |  | [Output Only] Server-defined fully-qualified URL for this resource. |
| `zone` | String | ✅ | The name of the zone for this request. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `exapool_provisioned_capacity_gb` | String | [Output Only] Provisioned capacities for each SKU for this Exapool in GiB |
| `provisioned_iops` | String | DEPRECATED -- use "pool provisioned IOPS". |
| `state` | String | [Output Only] The status of storage pool creation.
   
   
     - CREATING: Storage pool is provisioning.
     storagePool.
     - FAILED: Storage pool creation failed.
     - READY: Storage pool is ready for use.
     - DELETING: Storage pool is deleting. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `capacity_provisioning_type` | String | Provisioning type of the byte capacity of the pool. |
| `pool_provisioned_iops` | String | Provisioned IOPS of the storage pool. Only relevant if the storage pool
type is hyperdisk-balanced. |
| `storage_pool_type` | String | Type of the storage pool. |
| `self_link_with_id` | String | [Output Only] Server-defined URL for this resource's resource id. |
| `pool_provisioned_capacity_gb` | String | Size of the storage pool in GiB. For more information about the size
limits, see https://cloud.google.com/compute/docs/disks/storage-pools. |
| `label_fingerprint` | String | A fingerprint for the labels being applied to this storage pool, which is
essentially a hash of the labels set used for optimistic locking. The
fingerprint is initially generated by Compute Engine and changes after
every request to modify or update labels. You must always provide an
up-to-date fingerprint hash in order to update or change labels,
otherwise the request will fail with error412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve a storage pool. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `size_gb` | String | DEPRECATED -- use "pool provisioned capacity gb". |
| `performance_provisioning_type` | String | Provisioning type of the performance-related parameters of the pool,
such as throughput and IOPS. |
| `labels` | HashMap<String, String> | Labels to apply to this storage pool. These can be later modified by
the setLabels method. |
| `zone` | String | [Output Only] URL of the zone where the storage pool resides.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `status` | String | [Output Only] Status information for the storage pool resource. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `kind` | String | [Output Only] Type of the resource. Always compute#storagePool
for storage pools. |
| `name` | String | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?`
which means the first character must be a lowercase letter, and all
following characters must be a dash, lowercase letter, or digit, except
the last character, which cannot be a dash. |
| `provisioned_throughput` | String | DEPRECATED -- use "pool provisioned throughput". |
| `pool_provisioned_throughput` | String | Provisioned throughput of the storage pool in MiB/s. Only relevant if the
storage pool type is hyperdisk-balanced or hyperdisk-throughput. |
| `resource_status` | String | [Output Only] Status information for the storage pool resource. |
| `self_link` | String | [Output Only] Server-defined fully-qualified URL for this resource. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create storage_pool
storage_pool = provider.compute_api.Storage_pool {
    zone = "value"  # The name of the zone for this request.
    project = "value"  # Project ID for this request.
}

# Access storage_pool outputs
storage_pool_id = storage_pool.id
storage_pool_exapool_provisioned_capacity_gb = storage_pool.exapool_provisioned_capacity_gb
storage_pool_provisioned_iops = storage_pool.provisioned_iops
storage_pool_state = storage_pool.state
storage_pool_description = storage_pool.description
storage_pool_capacity_provisioning_type = storage_pool.capacity_provisioning_type
storage_pool_pool_provisioned_iops = storage_pool.pool_provisioned_iops
storage_pool_storage_pool_type = storage_pool.storage_pool_type
storage_pool_self_link_with_id = storage_pool.self_link_with_id
storage_pool_pool_provisioned_capacity_gb = storage_pool.pool_provisioned_capacity_gb
storage_pool_label_fingerprint = storage_pool.label_fingerprint
storage_pool_creation_timestamp = storage_pool.creation_timestamp
storage_pool_size_gb = storage_pool.size_gb
storage_pool_performance_provisioning_type = storage_pool.performance_provisioning_type
storage_pool_labels = storage_pool.labels
storage_pool_zone = storage_pool.zone
storage_pool_status = storage_pool.status
storage_pool_id = storage_pool.id
storage_pool_kind = storage_pool.kind
storage_pool_name = storage_pool.name
storage_pool_provisioned_throughput = storage_pool.provisioned_throughput
storage_pool_pool_provisioned_throughput = storage_pool.pool_provisioned_throughput
storage_pool_resource_status = storage_pool.resource_status
storage_pool_self_link = storage_pool.self_link
```

---


### Ssl_certificate

Creates a SslCertificate resource in the specified project using the data
included in the request.

**Operations**: ✅ Create ✅ Read ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `certificate` | String |  | A value read into memory from a certificate file. The certificate file must
be in PEM format. The certificate chain must be no greater than 5 certs
long. The chain must include at least one intermediate cert. |
| `self_link` | String |  | [Output only] Server-defined URL for the resource. |
| `kind` | String |  | [Output Only] Type of the resource. Alwayscompute#sslCertificate for SSL certificates. |
| `private_key` | String |  | A value read into memory from a write-only private key file. The private
key file must be in PEM format. For security, only insert
requests include this field. |
| `name` | String |  | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `expire_time` | String |  | [Output Only] Expire time of the certificate. RFC3339 |
| `self_link_with_id` | String |  | [Output Only] Server-defined URL for this resource with the resource id. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `region` | String |  | [Output Only] URL of the region where the regional SSL Certificate
resides. This field is not applicable to global SSL Certificate. |
| `self_managed` | String |  | Configuration and status of a self-managed SSL certificate. |
| `managed` | String |  | Configuration and status of a managed SSL certificate. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `subject_alternative_names` | Vec<String> |  | [Output Only] Domains associated with the certificate via Subject
Alternative Name. |
| `type` | String |  | (Optional) Specifies the type of SSL certificate, either "SELF_MANAGED" or
"MANAGED". If not specified, the certificate is self-managed and the fieldscertificate and private_key are used. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `certificate` | String | A value read into memory from a certificate file. The certificate file must
be in PEM format. The certificate chain must be no greater than 5 certs
long. The chain must include at least one intermediate cert. |
| `self_link` | String | [Output only] Server-defined URL for the resource. |
| `kind` | String | [Output Only] Type of the resource. Alwayscompute#sslCertificate for SSL certificates. |
| `private_key` | String | A value read into memory from a write-only private key file. The private
key file must be in PEM format. For security, only insert
requests include this field. |
| `name` | String | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `expire_time` | String | [Output Only] Expire time of the certificate. RFC3339 |
| `self_link_with_id` | String | [Output Only] Server-defined URL for this resource with the resource id. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `region` | String | [Output Only] URL of the region where the regional SSL Certificate
resides. This field is not applicable to global SSL Certificate. |
| `self_managed` | String | Configuration and status of a self-managed SSL certificate. |
| `managed` | String | Configuration and status of a managed SSL certificate. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `subject_alternative_names` | Vec<String> | [Output Only] Domains associated with the certificate via Subject
Alternative Name. |
| `type` | String | (Optional) Specifies the type of SSL certificate, either "SELF_MANAGED" or
"MANAGED". If not specified, the certificate is self-managed and the fieldscertificate and private_key are used. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create ssl_certificate
ssl_certificate = provider.compute_api.Ssl_certificate {
    project = "value"  # Project ID for this request.
}

# Access ssl_certificate outputs
ssl_certificate_id = ssl_certificate.id
ssl_certificate_certificate = ssl_certificate.certificate
ssl_certificate_self_link = ssl_certificate.self_link
ssl_certificate_kind = ssl_certificate.kind
ssl_certificate_private_key = ssl_certificate.private_key
ssl_certificate_name = ssl_certificate.name
ssl_certificate_expire_time = ssl_certificate.expire_time
ssl_certificate_self_link_with_id = ssl_certificate.self_link_with_id
ssl_certificate_description = ssl_certificate.description
ssl_certificate_region = ssl_certificate.region
ssl_certificate_self_managed = ssl_certificate.self_managed
ssl_certificate_managed = ssl_certificate.managed
ssl_certificate_id = ssl_certificate.id
ssl_certificate_creation_timestamp = ssl_certificate.creation_timestamp
ssl_certificate_subject_alternative_names = ssl_certificate.subject_alternative_names
ssl_certificate_type = ssl_certificate.type
```

---


### Firewall

Creates a firewall rule in the specified project using the data
included in the request.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `enable_logging` | bool |  | Deprecated in favor of enable in LogConfig.
This field denotes whether to enable logging for a particular
firewall rule. If logging is enabled, logs will be exported t
Cloud Logging. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `priority` | i64 |  | Priority for this rule.
This is an integer between `0` and `65535`, both inclusive.
The default value is `1000`.
Relative priorities determine which rule takes effect if multiple rules
apply. Lower values indicate higher priority. For example, a rule with
priority `0` has higher precedence than a rule with priority `1`.
DENY rules take precedence over ALLOW rules if they have equal priority.
Note that VPC networks have implied
rules with a priority of `65535`. To avoid conflicts with the implied
rules, use a priority number less than `65535`. |
| `self_link_with_id` | String |  | [Output Only] Server-defined URL for this resource with the resource id. |
| `source_tags` | Vec<String> |  | If source tags are specified, the firewall rule applies only to traffic
with source IPs that match the primary network interfaces of VM instances
that have the tag and are in the same VPC network.
Source tags cannot be used to control traffic to an instance's external IP
address, it only applies to traffic between instances in the same virtual
network. Because tags are associated with instances, not IP addresses.
One or both of sourceRanges and sourceTags may be
set. If both fields are set, the firewall applies to traffic that has a
source IP address within sourceRanges OR a source IP from a
resource with a matching tag listed in the sourceTags
field. The connection does not need to match both fields for the
firewall to apply. |
| `target_service_accounts` | Vec<String> |  | A list of service accounts indicating sets of instances located in the
network that may make network connections as specified inallowed[].targetServiceAccounts cannot be used at the same time astargetTags or sourceTags.
If neither targetServiceAccounts nor targetTags
are specified, the firewall rule applies to all instances on the specified
network. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `params` | String |  | Input only. [Input Only] Additional params passed with the request, but not persisted
as part of resource payload. |
| `name` | String |  | Name of the resource; provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?`. The first character
must be a lowercase letter, and all following characters (except for the
last character) must be a dash, lowercase letter, or digit. The last
character must be a lowercase letter or digit. |
| `denied` | Vec<String> |  | The list of DENY rules specified by this firewall. Each rule specifies a
protocol and port-range tuple that describes a denied connection. |
| `allowed` | Vec<String> |  | The list of ALLOW rules specified by this firewall. Each rule specifies a
protocol and port-range tuple that describes a permitted connection. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `source_service_accounts` | Vec<String> |  | If source service accounts are specified, the firewall rules apply only to
traffic originating from an instance with a service account in this list.
Source service accounts cannot be used to control traffic to an instance's
external IP address because service accounts are associated with an
instance, not an IP address.sourceRanges can be set at the same time assourceServiceAccounts.
If both are set, the firewall applies to traffic that
has a source IP address within the sourceRanges OR a source
IP that belongs to an instance with service account listed insourceServiceAccount. The connection does not need to match
both fields for the firewall to apply.sourceServiceAccounts cannot be used at the same time assourceTags or targetTags. |
| `direction` | String |  | Direction of traffic to which this firewall applies, either `INGRESS` or
`EGRESS`. The default is `INGRESS`. For `EGRESS` traffic, you cannot
specify the sourceTags fields. |
| `disabled` | bool |  | Denotes whether the firewall rule is disabled. When set to true, the
firewall rule is not enforced and the network behaves as if it did not
exist. If this is unspecified, the firewall rule will be enabled. |
| `source_ranges` | Vec<String> |  | If source ranges are specified, the firewall rule applies only to traffic
that has a source IP address in these ranges. These ranges must be
expressed inCIDR format. One or both of sourceRanges
and sourceTags may be set.
If both fields are set, the rule applies to traffic that has a
source IP address within sourceRanges OR a source IP
from a resource with a matching tag listed in thesourceTags field. The connection does not need to match
both fields for the rule to
apply. Both IPv4 and IPv6 are supported. |
| `destination_ranges` | Vec<String> |  | If destination ranges are specified, the firewall rule applies only to
traffic that has destination IP address in these ranges. These ranges must
be expressed inCIDR format. Both IPv4 and IPv6 are supported. |
| `kind` | String |  | [Output Only] Type of the resource. Always compute#firewall
for firewall rules. |
| `network` | String |  | URL of the network resource for this firewall rule. If not
specified when creating a firewall rule, the default network
is used:

global/networks/default

If you choose to specify this field, you can specify the network as a full 
or partial URL. For example, the following are all valid URLs: 
   
   - 
   https://www.googleapis.com/compute/v1/projects/myproject/global/networks/my-network 
   - projects/myproject/global/networks/my-network 
   - global/networks/default |
| `log_config` | String |  | This field denotes the logging options for a particular firewall rule. If
logging is enabled, logs will be exported to Cloud Logging. |
| `target_tags` | Vec<String> |  | A list of tags that controls which instances the firewall rule
applies to. If targetTags are specified, then the firewall
rule applies only to instances in the VPC network that have one of those
tags. If no targetTags are specified, the firewall rule
applies to all instances on the specified network. |
| `description` | String |  | An optional description of this resource. Provide this field when you
create the resource. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `enable_logging` | bool | Deprecated in favor of enable in LogConfig.
This field denotes whether to enable logging for a particular
firewall rule. If logging is enabled, logs will be exported t
Cloud Logging. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `priority` | i64 | Priority for this rule.
This is an integer between `0` and `65535`, both inclusive.
The default value is `1000`.
Relative priorities determine which rule takes effect if multiple rules
apply. Lower values indicate higher priority. For example, a rule with
priority `0` has higher precedence than a rule with priority `1`.
DENY rules take precedence over ALLOW rules if they have equal priority.
Note that VPC networks have implied
rules with a priority of `65535`. To avoid conflicts with the implied
rules, use a priority number less than `65535`. |
| `self_link_with_id` | String | [Output Only] Server-defined URL for this resource with the resource id. |
| `source_tags` | Vec<String> | If source tags are specified, the firewall rule applies only to traffic
with source IPs that match the primary network interfaces of VM instances
that have the tag and are in the same VPC network.
Source tags cannot be used to control traffic to an instance's external IP
address, it only applies to traffic between instances in the same virtual
network. Because tags are associated with instances, not IP addresses.
One or both of sourceRanges and sourceTags may be
set. If both fields are set, the firewall applies to traffic that has a
source IP address within sourceRanges OR a source IP from a
resource with a matching tag listed in the sourceTags
field. The connection does not need to match both fields for the
firewall to apply. |
| `target_service_accounts` | Vec<String> | A list of service accounts indicating sets of instances located in the
network that may make network connections as specified inallowed[].targetServiceAccounts cannot be used at the same time astargetTags or sourceTags.
If neither targetServiceAccounts nor targetTags
are specified, the firewall rule applies to all instances on the specified
network. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `params` | String | Input only. [Input Only] Additional params passed with the request, but not persisted
as part of resource payload. |
| `name` | String | Name of the resource; provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?`. The first character
must be a lowercase letter, and all following characters (except for the
last character) must be a dash, lowercase letter, or digit. The last
character must be a lowercase letter or digit. |
| `denied` | Vec<String> | The list of DENY rules specified by this firewall. Each rule specifies a
protocol and port-range tuple that describes a denied connection. |
| `allowed` | Vec<String> | The list of ALLOW rules specified by this firewall. Each rule specifies a
protocol and port-range tuple that describes a permitted connection. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `source_service_accounts` | Vec<String> | If source service accounts are specified, the firewall rules apply only to
traffic originating from an instance with a service account in this list.
Source service accounts cannot be used to control traffic to an instance's
external IP address because service accounts are associated with an
instance, not an IP address.sourceRanges can be set at the same time assourceServiceAccounts.
If both are set, the firewall applies to traffic that
has a source IP address within the sourceRanges OR a source
IP that belongs to an instance with service account listed insourceServiceAccount. The connection does not need to match
both fields for the firewall to apply.sourceServiceAccounts cannot be used at the same time assourceTags or targetTags. |
| `direction` | String | Direction of traffic to which this firewall applies, either `INGRESS` or
`EGRESS`. The default is `INGRESS`. For `EGRESS` traffic, you cannot
specify the sourceTags fields. |
| `disabled` | bool | Denotes whether the firewall rule is disabled. When set to true, the
firewall rule is not enforced and the network behaves as if it did not
exist. If this is unspecified, the firewall rule will be enabled. |
| `source_ranges` | Vec<String> | If source ranges are specified, the firewall rule applies only to traffic
that has a source IP address in these ranges. These ranges must be
expressed inCIDR format. One or both of sourceRanges
and sourceTags may be set.
If both fields are set, the rule applies to traffic that has a
source IP address within sourceRanges OR a source IP
from a resource with a matching tag listed in thesourceTags field. The connection does not need to match
both fields for the rule to
apply. Both IPv4 and IPv6 are supported. |
| `destination_ranges` | Vec<String> | If destination ranges are specified, the firewall rule applies only to
traffic that has destination IP address in these ranges. These ranges must
be expressed inCIDR format. Both IPv4 and IPv6 are supported. |
| `kind` | String | [Output Only] Type of the resource. Always compute#firewall
for firewall rules. |
| `network` | String | URL of the network resource for this firewall rule. If not
specified when creating a firewall rule, the default network
is used:

global/networks/default

If you choose to specify this field, you can specify the network as a full 
or partial URL. For example, the following are all valid URLs: 
   
   - 
   https://www.googleapis.com/compute/v1/projects/myproject/global/networks/my-network 
   - projects/myproject/global/networks/my-network 
   - global/networks/default |
| `log_config` | String | This field denotes the logging options for a particular firewall rule. If
logging is enabled, logs will be exported to Cloud Logging. |
| `target_tags` | Vec<String> | A list of tags that controls which instances the firewall rule
applies to. If targetTags are specified, then the firewall
rule applies only to instances in the VPC network that have one of those
tags. If no targetTags are specified, the firewall rule
applies to all instances on the specified network. |
| `description` | String | An optional description of this resource. Provide this field when you
create the resource. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create firewall
firewall = provider.compute_api.Firewall {
    project = "value"  # Project ID for this request.
}

# Access firewall outputs
firewall_id = firewall.id
firewall_enable_logging = firewall.enable_logging
firewall_creation_timestamp = firewall.creation_timestamp
firewall_priority = firewall.priority
firewall_self_link_with_id = firewall.self_link_with_id
firewall_source_tags = firewall.source_tags
firewall_target_service_accounts = firewall.target_service_accounts
firewall_id = firewall.id
firewall_params = firewall.params
firewall_name = firewall.name
firewall_denied = firewall.denied
firewall_allowed = firewall.allowed
firewall_self_link = firewall.self_link
firewall_source_service_accounts = firewall.source_service_accounts
firewall_direction = firewall.direction
firewall_disabled = firewall.disabled
firewall_source_ranges = firewall.source_ranges
firewall_destination_ranges = firewall.destination_ranges
firewall_kind = firewall.kind
firewall_network = firewall.network
firewall_log_config = firewall.log_config
firewall_target_tags = firewall.target_tags
firewall_description = firewall.description
```

---


### Image_family_view

Returns the latest image that is part of an image family, is not
deprecated and is rolled out in the specified zone.

**Operations**: ✅ Read

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `image` | String | The latest image that is part of the specified image family in the
requested location, and that is not deprecated. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Access image_family_view outputs
image_family_view_id = image_family_view.id
image_family_view_image = image_family_view.image
```

---


### Network_firewall_policie

Creates a new policy in the specified project using the data included in
the request.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `fingerprint` | String |  | Specifies a fingerprint for this resource, which is essentially a hash of
the metadata's contents and used for optimistic locking. The
fingerprint is initially generated by Compute Engine and changes after
every request to modify or update metadata. You must always provide an
up-to-date fingerprint hash in order to update or change metadata,
otherwise the request will fail with error412 conditionNotMet.

To see the latest fingerprint, make get() request to the
firewall policy. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `packet_mirroring_rules` | Vec<String> |  | A list of packet mirroring rules that belong to this policy. |
| `parent` | String |  | [Output Only] The parent of the firewall policy.
This field is not applicable to network firewall policies. |
| `region` | String |  | [Output Only] URL of the region where the regional firewall policy resides.
This field is not applicable to global firewall policies.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `rules` | Vec<String> |  | A list of rules that belong to this policy.
There must always be a default rule (rule with priority 2147483647 and
match "*"). If no rules are provided when creating a firewall policy, a
default rule with action "allow" will be added. |
| `policy_type` | String |  | The type of the firewall policy. This field can be eitherVPC_POLICY or RDMA_ROCE_POLICY.

Note: if not specified then VPC_POLICY will be used. |
| `self_link_with_id` | String |  | [Output Only] Server-defined URL for this resource with the resource id. |
| `short_name` | String |  | User-provided name of the Organization firewall policy. The name should be
unique in the organization in which the firewall policy is created.
This field is not applicable to network firewall policies.
This name must be set on creation and cannot be changed. The name must be
1-63 characters long, and comply with RFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `kind` | String |  | [Output only] Type of the resource. Alwayscompute#firewallPolicyfor firewall policies |
| `vpc_network_scope` | String |  | The scope of networks allowed to be associated with the firewall
policy. This field can be either GLOBAL_VPC_NETWORK orREGIONAL_VPC_NETWORK.

A firewall policy with the VPC scope set to GLOBAL_VPC_NETWORK
is allowed to be attached only to global networks.

When the VPC scope is set to REGIONAL_VPC_NETWORK the firewall
policy is allowed to be attached only to regional networks in the same
scope as the firewall policy.

Note: if not specified then GLOBAL_VPC_NETWORK will be used. |
| `associations` | Vec<String> |  | A list of associations that belong to this firewall policy. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `display_name` | String |  | Deprecated, please use short name instead. User-provided name of the
Organization firewall policy. The name should be unique in the organization
in which the firewall policy is created.
This field is not applicable to network firewall policies.
This name must be set on creation and cannot be changed.
The name must be 1-63 characters long, and comply
with RFC1035. Specifically, the name must be 1-63 characters
long and match the regular expression `[a-z]([-a-z0-9]*[a-z0-9])?` which
means the first character must be a lowercase letter, and all following
characters must be a dash, lowercase letter, or digit, except the last
character, which cannot be a dash. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `name` | String |  | Name of the resource. For Organization Firewall Policies it's a
[Output Only] numeric ID allocated by Google Cloud which uniquely
identifies the Organization Firewall Policy. |
| `policy_source` | String |  | [Output Only] Source of this Firewall Policy. USER_DEFINED if
created by a Cloud user, or SYSTEM if created by managed
services like GKE. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `rule_tuple_count` | i64 |  | [Output Only] Total count of all firewall policy rule tuples. A firewall
policy can not exceed a set number of tuples. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `fingerprint` | String | Specifies a fingerprint for this resource, which is essentially a hash of
the metadata's contents and used for optimistic locking. The
fingerprint is initially generated by Compute Engine and changes after
every request to modify or update metadata. You must always provide an
up-to-date fingerprint hash in order to update or change metadata,
otherwise the request will fail with error412 conditionNotMet.

To see the latest fingerprint, make get() request to the
firewall policy. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `packet_mirroring_rules` | Vec<String> | A list of packet mirroring rules that belong to this policy. |
| `parent` | String | [Output Only] The parent of the firewall policy.
This field is not applicable to network firewall policies. |
| `region` | String | [Output Only] URL of the region where the regional firewall policy resides.
This field is not applicable to global firewall policies.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `rules` | Vec<String> | A list of rules that belong to this policy.
There must always be a default rule (rule with priority 2147483647 and
match "*"). If no rules are provided when creating a firewall policy, a
default rule with action "allow" will be added. |
| `policy_type` | String | The type of the firewall policy. This field can be eitherVPC_POLICY or RDMA_ROCE_POLICY.

Note: if not specified then VPC_POLICY will be used. |
| `self_link_with_id` | String | [Output Only] Server-defined URL for this resource with the resource id. |
| `short_name` | String | User-provided name of the Organization firewall policy. The name should be
unique in the organization in which the firewall policy is created.
This field is not applicable to network firewall policies.
This name must be set on creation and cannot be changed. The name must be
1-63 characters long, and comply with RFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `kind` | String | [Output only] Type of the resource. Alwayscompute#firewallPolicyfor firewall policies |
| `vpc_network_scope` | String | The scope of networks allowed to be associated with the firewall
policy. This field can be either GLOBAL_VPC_NETWORK orREGIONAL_VPC_NETWORK.

A firewall policy with the VPC scope set to GLOBAL_VPC_NETWORK
is allowed to be attached only to global networks.

When the VPC scope is set to REGIONAL_VPC_NETWORK the firewall
policy is allowed to be attached only to regional networks in the same
scope as the firewall policy.

Note: if not specified then GLOBAL_VPC_NETWORK will be used. |
| `associations` | Vec<String> | A list of associations that belong to this firewall policy. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `display_name` | String | Deprecated, please use short name instead. User-provided name of the
Organization firewall policy. The name should be unique in the organization
in which the firewall policy is created.
This field is not applicable to network firewall policies.
This name must be set on creation and cannot be changed.
The name must be 1-63 characters long, and comply
with RFC1035. Specifically, the name must be 1-63 characters
long and match the regular expression `[a-z]([-a-z0-9]*[a-z0-9])?` which
means the first character must be a lowercase letter, and all following
characters must be a dash, lowercase letter, or digit, except the last
character, which cannot be a dash. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `name` | String | Name of the resource. For Organization Firewall Policies it's a
[Output Only] numeric ID allocated by Google Cloud which uniquely
identifies the Organization Firewall Policy. |
| `policy_source` | String | [Output Only] Source of this Firewall Policy. USER_DEFINED if
created by a Cloud user, or SYSTEM if created by managed
services like GKE. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `rule_tuple_count` | i64 | [Output Only] Total count of all firewall policy rule tuples. A firewall
policy can not exceed a set number of tuples. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create network_firewall_policie
network_firewall_policie = provider.compute_api.Network_firewall_policie {
    project = "value"  # Project ID for this request.
}

# Access network_firewall_policie outputs
network_firewall_policie_id = network_firewall_policie.id
network_firewall_policie_fingerprint = network_firewall_policie.fingerprint
network_firewall_policie_self_link = network_firewall_policie.self_link
network_firewall_policie_packet_mirroring_rules = network_firewall_policie.packet_mirroring_rules
network_firewall_policie_parent = network_firewall_policie.parent
network_firewall_policie_region = network_firewall_policie.region
network_firewall_policie_rules = network_firewall_policie.rules
network_firewall_policie_policy_type = network_firewall_policie.policy_type
network_firewall_policie_self_link_with_id = network_firewall_policie.self_link_with_id
network_firewall_policie_short_name = network_firewall_policie.short_name
network_firewall_policie_kind = network_firewall_policie.kind
network_firewall_policie_vpc_network_scope = network_firewall_policie.vpc_network_scope
network_firewall_policie_associations = network_firewall_policie.associations
network_firewall_policie_creation_timestamp = network_firewall_policie.creation_timestamp
network_firewall_policie_display_name = network_firewall_policie.display_name
network_firewall_policie_id = network_firewall_policie.id
network_firewall_policie_name = network_firewall_policie.name
network_firewall_policie_policy_source = network_firewall_policie.policy_source
network_firewall_policie_description = network_firewall_policie.description
network_firewall_policie_rule_tuple_count = network_firewall_policie.rule_tuple_count
```

---


### Interconnect_location

Returns the details for the specified interconnect location. Gets a list of
available interconnect locations by making a list() request.

**Operations**: ✅ Read

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `name` | String | [Output Only] Name of the resource. |
| `supports_pzs` | bool | [Output Only] Reserved for future use. |
| `region_infos` | Vec<String> | [Output Only] A list of InterconnectLocation.RegionInfo objects, that
describe parameters pertaining to the relation between this
InterconnectLocation and various Google Cloud regions. |
| `peeringdb_facility_id` | String | [Output Only] The peeringdb identifier for this facility (corresponding
with a netfac type in peeringdb). |
| `address` | String | [Output Only] The postal address of the Point of Presence, each line in
the address is separated by a newline character. |
| `available_features` | Vec<String> | [Output only] List of features available at this InterconnectLocation,
which can take one of the following values:
   
   - IF_MACSEC
   - IF_CROSS_SITE_NETWORK |
| `city` | String | [Output Only] Metropolitan area designator that indicates which city an
interconnect is located.
For example: "Chicago, IL", "Amsterdam, Netherlands". |
| `available_link_types` | Vec<String> | [Output only] List of link types available at this InterconnectLocation,
which can take one of the following values:
   
   - LINK_TYPE_ETHERNET_10G_LR
   - LINK_TYPE_ETHERNET_100G_LR
   - LINK_TYPE_ETHERNET_400G_LR4 |
| `status` | String | [Output Only] The status of this InterconnectLocation, which can take one
of the following values:
   
   - CLOSED: The InterconnectLocation is closed and is unavailable for
   provisioning new Interconnects.
   - AVAILABLE: The InterconnectLocation is available for provisioning new
   Interconnects. |
| `cross_site_interconnect_infos` | Vec<String> | [Output Only] A list of InterconnectLocation.CrossSiteInterconnectInfo
objects, that describe where Cross-Site Interconnect wires may connect to
from this location and associated connection parameters. Cross-Site
Interconnect isn't allowed to locations which are not listed. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `facility_provider` | String | [Output Only] The name of the provider for this facility (e.g., EQUINIX). |
| `description` | String | [Output Only] An optional description of the resource. |
| `kind` | String | [Output Only] Type of the resource. Alwayscompute#interconnectLocation for interconnect locations. |
| `availability_zone` | String | [Output Only] Availability zone for this InterconnectLocation. Within a
metropolitan area (metro), maintenance will not be simultaneously scheduled
in more than one availability zone.  Example: "zone1" or "zone2". |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `facility_provider_facility_id` | String | [Output Only] A provider-assigned Identifier for this facility (e.g.,
Ashburn-DC1). |
| `continent` | String | [Output Only] Continent for this location, which can take one of the
following values:
   
   - AFRICA
   - ASIA_PAC
   - EUROPE
   - NORTH_AMERICA
   - SOUTH_AMERICA |
| `single_region_production_critical_peer_locations` | Vec<String> | [Output Only] URLs of the other locations that can pair up with this
location to support Single-Region 99.99% SLA. E.g. iad-zone1-1 and
iad-zone2-5467 are Single-Region 99.99% peer locations of each other. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Access interconnect_location outputs
interconnect_location_id = interconnect_location.id
interconnect_location_creation_timestamp = interconnect_location.creation_timestamp
interconnect_location_name = interconnect_location.name
interconnect_location_supports_pzs = interconnect_location.supports_pzs
interconnect_location_region_infos = interconnect_location.region_infos
interconnect_location_peeringdb_facility_id = interconnect_location.peeringdb_facility_id
interconnect_location_address = interconnect_location.address
interconnect_location_available_features = interconnect_location.available_features
interconnect_location_city = interconnect_location.city
interconnect_location_available_link_types = interconnect_location.available_link_types
interconnect_location_status = interconnect_location.status
interconnect_location_cross_site_interconnect_infos = interconnect_location.cross_site_interconnect_infos
interconnect_location_id = interconnect_location.id
interconnect_location_facility_provider = interconnect_location.facility_provider
interconnect_location_description = interconnect_location.description
interconnect_location_kind = interconnect_location.kind
interconnect_location_availability_zone = interconnect_location.availability_zone
interconnect_location_self_link = interconnect_location.self_link
interconnect_location_facility_provider_facility_id = interconnect_location.facility_provider_facility_id
interconnect_location_continent = interconnect_location.continent
interconnect_location_single_region_production_critical_peer_locations = interconnect_location.single_region_production_critical_peer_locations
```

---


### Cross_site_network

Creates a cross-site network in the specified project in the given scope
using the parameters that are included in the request.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `name` | String |  | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `kind` | String |  | [Output Only] Type of the resource. Alwayscompute#crossSiteNetwork for cross-site networks. |
| `description` | String |  | An optional description of the cross-site network. |
| `id` | String |  | [Output Only] The unique identifier for the resource type. The server
generates this identifier. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `name` | String | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `kind` | String | [Output Only] Type of the resource. Alwayscompute#crossSiteNetwork for cross-site networks. |
| `description` | String | An optional description of the cross-site network. |
| `id` | String | [Output Only] The unique identifier for the resource type. The server
generates this identifier. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create cross_site_network
cross_site_network = provider.compute_api.Cross_site_network {
    project = "value"  # Project ID for this request.
}

# Access cross_site_network outputs
cross_site_network_id = cross_site_network.id
cross_site_network_name = cross_site_network.name
cross_site_network_creation_timestamp = cross_site_network.creation_timestamp
cross_site_network_self_link = cross_site_network.self_link
cross_site_network_kind = cross_site_network.kind
cross_site_network_description = cross_site_network.description
cross_site_network_id = cross_site_network.id
```

---


### Region_ssl_certificate

Creates a SslCertificate resource in the specified project and region using
the data included in the request

**Operations**: ✅ Create ✅ Read ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `private_key` | String |  | A value read into memory from a write-only private key file. The private
key file must be in PEM format. For security, only insert
requests include this field. |
| `subject_alternative_names` | Vec<String> |  | [Output Only] Domains associated with the certificate via Subject
Alternative Name. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `name` | String |  | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `kind` | String |  | [Output Only] Type of the resource. Alwayscompute#sslCertificate for SSL certificates. |
| `self_link` | String |  | [Output only] Server-defined URL for the resource. |
| `expire_time` | String |  | [Output Only] Expire time of the certificate. RFC3339 |
| `self_managed` | String |  | Configuration and status of a self-managed SSL certificate. |
| `type` | String |  | (Optional) Specifies the type of SSL certificate, either "SELF_MANAGED" or
"MANAGED". If not specified, the certificate is self-managed and the fieldscertificate and private_key are used. |
| `certificate` | String |  | A value read into memory from a certificate file. The certificate file must
be in PEM format. The certificate chain must be no greater than 5 certs
long. The chain must include at least one intermediate cert. |
| `region` | String |  | [Output Only] URL of the region where the regional SSL Certificate
resides. This field is not applicable to global SSL Certificate. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `managed` | String |  | Configuration and status of a managed SSL certificate. |
| `project` | String | ✅ | Project ID for this request. |
| `region` | String | ✅ | Name of the region scoping this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `private_key` | String | A value read into memory from a write-only private key file. The private
key file must be in PEM format. For security, only insert
requests include this field. |
| `subject_alternative_names` | Vec<String> | [Output Only] Domains associated with the certificate via Subject
Alternative Name. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `name` | String | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `kind` | String | [Output Only] Type of the resource. Alwayscompute#sslCertificate for SSL certificates. |
| `self_link` | String | [Output only] Server-defined URL for the resource. |
| `expire_time` | String | [Output Only] Expire time of the certificate. RFC3339 |
| `self_managed` | String | Configuration and status of a self-managed SSL certificate. |
| `type` | String | (Optional) Specifies the type of SSL certificate, either "SELF_MANAGED" or
"MANAGED". If not specified, the certificate is self-managed and the fieldscertificate and private_key are used. |
| `certificate` | String | A value read into memory from a certificate file. The certificate file must
be in PEM format. The certificate chain must be no greater than 5 certs
long. The chain must include at least one intermediate cert. |
| `region` | String | [Output Only] URL of the region where the regional SSL Certificate
resides. This field is not applicable to global SSL Certificate. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `managed` | String | Configuration and status of a managed SSL certificate. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create region_ssl_certificate
region_ssl_certificate = provider.compute_api.Region_ssl_certificate {
    project = "value"  # Project ID for this request.
    region = "value"  # Name of the region scoping this request.
}

# Access region_ssl_certificate outputs
region_ssl_certificate_id = region_ssl_certificate.id
region_ssl_certificate_private_key = region_ssl_certificate.private_key
region_ssl_certificate_subject_alternative_names = region_ssl_certificate.subject_alternative_names
region_ssl_certificate_id = region_ssl_certificate.id
region_ssl_certificate_creation_timestamp = region_ssl_certificate.creation_timestamp
region_ssl_certificate_name = region_ssl_certificate.name
region_ssl_certificate_kind = region_ssl_certificate.kind
region_ssl_certificate_self_link = region_ssl_certificate.self_link
region_ssl_certificate_expire_time = region_ssl_certificate.expire_time
region_ssl_certificate_self_managed = region_ssl_certificate.self_managed
region_ssl_certificate_type = region_ssl_certificate.type
region_ssl_certificate_certificate = region_ssl_certificate.certificate
region_ssl_certificate_region = region_ssl_certificate.region
region_ssl_certificate_description = region_ssl_certificate.description
region_ssl_certificate_managed = region_ssl_certificate.managed
```

---


### Zone_operation

Waits for the specified Operation resource to return as `DONE`
or for the request to approach the 2 minute deadline, and retrieves the
specified Operation resource. This method waits for no more than the
2 minutes and then returns the current state of the
operation, which might be `DONE` or still in progress.

This method is called on a best-effort basis. Specifically:
   
   
    - In uncommon cases, when the server is overloaded, the request might
    return before the default deadline is reached, or might return after zero
    seconds.
   - If the default deadline is reached, there is no guarantee that the
    operation is actually done when the method returns. Be prepared to retry
    if the operation is not `DONE`.

**Operations**: ✅ Create ✅ Read ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `project` | String | ✅ | Project ID for this request. |
| `operation` | String | ✅ | Name of the Operations resource to return, or its unique numeric
identifier. |
| `zone` | String | ✅ | Name of the zone for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `start_time` | String | [Output Only] The time that this operation was started by the server.
This value is inRFC3339
text format. |
| `http_error_message` | String | [Output Only] If the operation fails, this field contains the HTTP error
message that was returned, such as `NOT FOUND`. |
| `id` | String | [Output Only] The unique identifier for the operation. This identifier is
defined by the server. |
| `kind` | String | [Output Only] Type of the resource. Always `compute#operation` for
Operation resources. |
| `error` | String | [Output Only] If errors are generated during processing of the operation,
this field will be populated. |
| `description` | String | [Output Only] A textual description of the operation, which is
set when the operation is created. |
| `status_message` | String | [Output Only] An optional textual description of the current status of the
operation. |
| `warnings` | Vec<String> | [Output Only] If warning messages are generated during processing of the
operation, this field will be populated. |
| `creation_timestamp` | String | [Deprecated] This field is deprecated. |
| `zone` | String | [Output Only] The URL of the zone where the operation resides. Only
applicable when performing per-zone operations. |
| `user` | String | [Output Only] User who requested the operation, for example:
`user@example.com` or
`alice_smith_identifier (global/workforcePools/example-com-us-employees)`. |
| `name` | String | [Output Only] Name of the operation. |
| `operation_group_id` | String | [Output Only] An ID that represents a group of operations, such as when a
group of operations results from a `bulkInsert` API request. |
| `client_operation_id` | String | [Output Only] The value of `requestId` if you provided it in the request.
Not present otherwise. |
| `http_error_status_code` | i64 | [Output Only] If the operation fails, this field contains the HTTP error
status code that was returned. For example, a `404` means the
resource was not found. |
| `progress` | i64 | [Output Only] An optional progress indicator that ranges from 0 to 100.
There is no requirement that this be linear or support any granularity of
operations. This should not be used to guess when the operation will be
complete. This number should monotonically increase as the operation
progresses. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `status` | String | [Output Only] The status of the operation, which can be one of the
following:
`PENDING`, `RUNNING`, or `DONE`. |
| `target_link` | String | [Output Only] The URL of the resource that the operation modifies. For
operations related to creating a snapshot, this points to the disk
that the snapshot was created from. |
| `set_common_instance_metadata_operation_metadata` | String | [Output Only] If the operation is for projects.setCommonInstanceMetadata,
this field will contain information on all underlying zonal actions and
their state. |
| `target_id` | String | [Output Only] The unique target ID, which identifies a specific incarnation
of the target resource. |
| `instances_bulk_insert_operation_metadata` | String |  |
| `insert_time` | String | [Output Only] The time that this operation was requested.
This value is inRFC3339
text format. |
| `operation_type` | String | [Output Only] The type of operation, such as `insert`,
`update`, or `delete`, and so on. |
| `region` | String | [Output Only] The URL of the region where the operation resides. Only
applicable when performing regional operations. |
| `end_time` | String | [Output Only] The time that this operation was completed. This value is inRFC3339
text format. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create zone_operation
zone_operation = provider.compute_api.Zone_operation {
    project = "value"  # Project ID for this request.
    operation = "value"  # Name of the Operations resource to return, or its unique numeric
identifier.
    zone = "value"  # Name of the zone for this request.
}

# Access zone_operation outputs
zone_operation_id = zone_operation.id
zone_operation_start_time = zone_operation.start_time
zone_operation_http_error_message = zone_operation.http_error_message
zone_operation_id = zone_operation.id
zone_operation_kind = zone_operation.kind
zone_operation_error = zone_operation.error
zone_operation_description = zone_operation.description
zone_operation_status_message = zone_operation.status_message
zone_operation_warnings = zone_operation.warnings
zone_operation_creation_timestamp = zone_operation.creation_timestamp
zone_operation_zone = zone_operation.zone
zone_operation_user = zone_operation.user
zone_operation_name = zone_operation.name
zone_operation_operation_group_id = zone_operation.operation_group_id
zone_operation_client_operation_id = zone_operation.client_operation_id
zone_operation_http_error_status_code = zone_operation.http_error_status_code
zone_operation_progress = zone_operation.progress
zone_operation_self_link = zone_operation.self_link
zone_operation_status = zone_operation.status
zone_operation_target_link = zone_operation.target_link
zone_operation_set_common_instance_metadata_operation_metadata = zone_operation.set_common_instance_metadata_operation_metadata
zone_operation_target_id = zone_operation.target_id
zone_operation_instances_bulk_insert_operation_metadata = zone_operation.instances_bulk_insert_operation_metadata
zone_operation_insert_time = zone_operation.insert_time
zone_operation_operation_type = zone_operation.operation_type
zone_operation_region = zone_operation.region
zone_operation_end_time = zone_operation.end_time
```

---


### Region_composite_health_check

Create a CompositeHealthCheck in the specified project in the given region
using the parameters that are included in the request.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `health_sources` | Vec<String> |  | URLs to the HealthSource resources whose results are AND'ed.
I.e. he aggregated result is is HEALTHY only if all sources
are HEALTHY. Must have at least 1. Must not have more than 10.
Must be regional and in the same region as theCompositeHealthCheck. Can be mutated. |
| `name` | String |  | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `region` | String |  | [Output Only] URL of the region where the composite health check resides.
This field applies only to the regional resource. You must specify this
field as part of the HTTP request URL. It is not settable as a field in
the request body. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `self_link_with_id` | String |  | [Output Only] Server-defined URL with id for the resource. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `health_destination` | String |  | URL to the destination resource. Must be set. Must be aForwardingRule. The ForwardingRule must have
load balancing scheme INTERNAL orINTERNAL_MANAGED and must be regional and in the same region
as the CompositeHealthCheck (cross-region deployment forINTERNAL_MANAGED is not supported). Can be mutated. |
| `id` | String |  | [Output Only] A unique identifier for this resource type. The server
generates this identifier. |
| `fingerprint` | String |  | Fingerprint of this resource. A hash of the contents stored in this object.
This field is used in optimistic locking. This field will be ignored when
inserting a CompositeHealthCheck. An up-to-date fingerprint
must be provided in order to patch the CompositeHealthCheck; Otherwise,
the request will fail with error 412 conditionNotMet. To see
the latest fingerprint, make a get() request to retrieve the
CompositeHealthCheck. |
| `kind` | String |  | [Output Only] Type of the resource. Alwayscompute#compositeHealthCheck for composite health checks. |
| `region` | String | ✅ | Name of the region scoping this request. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `health_sources` | Vec<String> | URLs to the HealthSource resources whose results are AND'ed.
I.e. he aggregated result is is HEALTHY only if all sources
are HEALTHY. Must have at least 1. Must not have more than 10.
Must be regional and in the same region as theCompositeHealthCheck. Can be mutated. |
| `name` | String | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `region` | String | [Output Only] URL of the region where the composite health check resides.
This field applies only to the regional resource. You must specify this
field as part of the HTTP request URL. It is not settable as a field in
the request body. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `self_link_with_id` | String | [Output Only] Server-defined URL with id for the resource. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `health_destination` | String | URL to the destination resource. Must be set. Must be aForwardingRule. The ForwardingRule must have
load balancing scheme INTERNAL orINTERNAL_MANAGED and must be regional and in the same region
as the CompositeHealthCheck (cross-region deployment forINTERNAL_MANAGED is not supported). Can be mutated. |
| `id` | String | [Output Only] A unique identifier for this resource type. The server
generates this identifier. |
| `fingerprint` | String | Fingerprint of this resource. A hash of the contents stored in this object.
This field is used in optimistic locking. This field will be ignored when
inserting a CompositeHealthCheck. An up-to-date fingerprint
must be provided in order to patch the CompositeHealthCheck; Otherwise,
the request will fail with error 412 conditionNotMet. To see
the latest fingerprint, make a get() request to retrieve the
CompositeHealthCheck. |
| `kind` | String | [Output Only] Type of the resource. Alwayscompute#compositeHealthCheck for composite health checks. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create region_composite_health_check
region_composite_health_check = provider.compute_api.Region_composite_health_check {
    region = "value"  # Name of the region scoping this request.
    project = "value"  # Project ID for this request.
}

# Access region_composite_health_check outputs
region_composite_health_check_id = region_composite_health_check.id
region_composite_health_check_creation_timestamp = region_composite_health_check.creation_timestamp
region_composite_health_check_health_sources = region_composite_health_check.health_sources
region_composite_health_check_name = region_composite_health_check.name
region_composite_health_check_region = region_composite_health_check.region
region_composite_health_check_self_link = region_composite_health_check.self_link
region_composite_health_check_self_link_with_id = region_composite_health_check.self_link_with_id
region_composite_health_check_description = region_composite_health_check.description
region_composite_health_check_health_destination = region_composite_health_check.health_destination
region_composite_health_check_id = region_composite_health_check.id
region_composite_health_check_fingerprint = region_composite_health_check.fingerprint
region_composite_health_check_kind = region_composite_health_check.kind
```

---


### Region_disk_setting

Get Regional Disk Settings.

**Operations**: ✅ Read ✅ Update

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `access_location` | String |  | Policy of which locations are allowed to create regional
snapshots. |
| `region` | String | ✅ | Name of the region for this request. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `access_location` | String | Policy of which locations are allowed to create regional
snapshots. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Access region_disk_setting outputs
region_disk_setting_id = region_disk_setting.id
region_disk_setting_access_location = region_disk_setting.access_location
```

---


### Global_forwarding_rule

Creates a GlobalForwardingRule resource in the specified project using
the data included in the request.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `backend_service` | String |  | Identifies the backend service to which the forwarding rule sends traffic.
Required for internal and external passthrough Network Load Balancers;
must be omitted for all other load balancer types. |
| `allow_global_access` | bool |  | If set to true, clients can access the internal passthrough Network Load
Balancers, the regional internal Application Load Balancer, and the
regional internal proxy Network Load Balancer from all regions.
If false, only allows access from the local region the load balancer is
located at. Note that for INTERNAL_MANAGED forwarding rules, this field
cannot be changed after the forwarding rule is created. |
| `load_balancing_scheme` | String |  | Specifies the forwarding rule type.

For more information about forwarding rules, refer to
Forwarding rule concepts. |
| `ports` | Vec<String> |  | The ports, portRange, and allPorts
fields are mutually exclusive. Only packets addressed to ports in the
specified range will be forwarded to the backends configured with this
forwarding rule.

The ports field has the following limitations:
   
   - It requires that the forwarding rule IPProtocol be TCP,
   UDP, or SCTP, and
   - It's applicable only to the following products: internal passthrough
   Network Load Balancers, backend service-based external passthrough Network
   Load Balancers, and internal protocol forwarding.
   - You can specify a list of up to five ports by number, separated by
   commas. The ports can be contiguous or discontiguous.



For external forwarding rules, two or more forwarding rules cannot use the
same [IPAddress, IPProtocol] pair if they share at least one
port number.

For internal forwarding rules within the same VPC network, two or more
forwarding rules cannot use the same [IPAddress, IPProtocol]
pair if they share at least one port number.

@pattern: \\d+(?:-\\d+)? |
| `is_mirroring_collector` | bool |  | Indicates whether or not this load balancer can be used as a collector for
packet mirroring. To prevent mirroring loops, instances behind this
load balancer will not have their traffic mirrored even if aPacketMirroring rule applies to them.
This can only be set to true for load balancers that have theirloadBalancingScheme set to INTERNAL. |
| `self_link_with_id` | String |  | [Output Only] Server-defined URL for this resource with the resource id. |
| `base_forwarding_rule` | String |  | [Output Only] The URL for the corresponding base forwarding rule. By base
forwarding rule, we mean the forwarding rule that has the same IP address,
protocol, and port settings with the current forwarding rule, but without
sourceIPRanges specified.
Always empty if the current forwarding rule does not have sourceIPRanges
specified. |
| `labels` | HashMap<String, String> |  | Labels for this resource. These can only be added or modified by thesetLabels method. Each label key/value pair must comply withRFC1035.
Label values may be empty. |
| `service_name` | String |  | [Output Only]
The internal fully qualified service name for this forwarding rule.

This field is only used for internal load balancing. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `kind` | String |  | [Output Only] Type of the resource. Alwayscompute#forwardingRule for forwarding rule resources. |
| `region` | String |  | [Output Only] URL of the region where the regional forwarding rule resides.
This field is not applicable to global forwarding rules.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `service_label` | String |  | An optional prefix to the service name for this forwarding rule.
If specified, the prefix is the first label of the fully qualified service
name.

The label must be 1-63 characters long, and comply withRFC1035.
Specifically, the label must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash.

This field is only used for internal load balancing. |
| `subnetwork` | String |  | This field identifies the subnetwork that the load balanced IP should
belong to for this forwarding rule, used with internal load balancers and
external passthrough Network Load Balancers with IPv6.

If the network specified is in auto subnet mode, this field is optional.
However, a subnetwork must be specified if the network is in custom subnet
mode or when creating external forwarding rule with IPv6. |
| `ip_version` | String |  | The IP Version that will be used by this forwarding rule.  Valid options
are IPV4 or IPV6. |
| `ip_collection` | String |  | Resource reference of a PublicDelegatedPrefix. The PDP must
be a sub-PDP in EXTERNAL_IPV6_FORWARDING_RULE_CREATION mode.

Use one of the following formats to specify a sub-PDP when creating an IPv6
NetLB forwarding rule using BYOIP:
Full resource URL, as inhttps://www.googleapis.com/compute/v1/projects/project_id/regions/region/publicDelegatedPrefixes/sub-pdp-name
Partial URL, as in:
   
   - projects/project_id/regions/region/publicDelegatedPrefixes/sub-pdp-name
   - regions/region/publicDelegatedPrefixes/sub-pdp-name |
| `no_automate_dns_zone` | bool |  | This is used in PSC consumer ForwardingRule to control whether it should
try to auto-generate a DNS zone or not. Non-PSC forwarding rules do not use
this field. Once set, this field is not mutable. |
| `external_managed_backend_bucket_migration_state` | String |  | Specifies the canary migration state for the backend buckets attached to
this forwarding rule. Possible values are PREPARE, TEST_BY_PERCENTAGE, and
TEST_ALL_TRAFFIC.

To begin the migration from EXTERNAL to EXTERNAL_MANAGED, the state must be
changed to PREPARE. The state must be changed to TEST_ALL_TRAFFIC before
the loadBalancingScheme can be changed to EXTERNAL_MANAGED. Optionally, the
TEST_BY_PERCENTAGE state can be used to migrate traffic to backend buckets
attached to this forwarding rule by percentage using
externalManagedBackendBucketMigrationTestingPercentage.

Rolling back a migration requires the states to be set in reverse order. So
changing the scheme from EXTERNAL_MANAGED to EXTERNAL requires the state to
be set to TEST_ALL_TRAFFIC at the same time. Optionally, the
TEST_BY_PERCENTAGE state can be used to migrate some traffic back to
EXTERNAL or PREPARE can be used to migrate all traffic back to EXTERNAL. |
| `source_ip_ranges` | Vec<String> |  | If not empty, this forwarding rule will only forward the traffic when the
source IP address matches one of the IP addresses or CIDR ranges set here.
Note that a forwarding rule can only have up to 64 source IP ranges, and
this field can only be used with a regional forwarding rule whose scheme isEXTERNAL.
Each source_ip_range entry should be either an IP address (for
example, 1.2.3.4) or a CIDR range (for example, 1.2.3.0/24). |
| `external_managed_backend_bucket_migration_testing_percentage` | f64 |  | Determines the fraction of requests to backend buckets that should be
processed by the global external Application Load Balancer.

The value of this field must be in the range [0, 100].

This value can only be set if the loadBalancingScheme in the BackendService
is set to EXTERNAL (when using the classic Application Load Balancer) and
the migration state is TEST_BY_PERCENTAGE. |
| `ip_address` | String |  | IP address for which this forwarding rule accepts traffic. When a client
sends traffic to this IP address, the forwarding rule directs the traffic
to the referenced target or backendService.
While creating a forwarding rule, specifying an IPAddress is
required under the following circumstances:

   
   - When the target is set to targetGrpcProxy andvalidateForProxyless is set to true, theIPAddress should be set to 0.0.0.0.
   - When the target is a Private Service Connect Google APIs
   bundle, you must specify an IPAddress.


Otherwise, you can optionally specify an IP address that references an
existing static (reserved) IP address resource. When omitted, Google Cloud
assigns an ephemeral IP address.

Use one of the following formats to specify an IP address while creating a
forwarding rule:

* IP address number, as in `100.1.2.3`
* IPv6 address range, as in `2600:1234::/96`
* Full resource URL, as inhttps://www.googleapis.com/compute/v1/projects/project_id/regions/region/addresses/address-name
* Partial URL or by name, as in:
   
   - projects/project_id/regions/region/addresses/address-name
   - regions/region/addresses/address-name
   - global/addresses/address-name
   - address-name



The forwarding rule's target or backendService,
and in most cases, also the loadBalancingScheme, determine the
type of IP address that you can use. For detailed information, see
[IP address
specifications](https://cloud.google.com/load-balancing/docs/forwarding-rule-concepts#ip_address_specifications).

When reading an IPAddress, the API always returns the IP
address number. |
| `all_ports` | bool |  | The ports, portRange, and allPorts
fields are mutually exclusive. Only packets addressed to ports in the
specified range will be forwarded to the backends configured with this
forwarding rule.

The allPorts field has the following limitations:
   
   - It requires that the forwarding rule IPProtocol be TCP,
   UDP, SCTP, or L3_DEFAULT.
   - It's applicable only to the following products: internal passthrough
   Network Load Balancers, backend service-based external passthrough Network
   Load Balancers, and internal and external protocol forwarding.
   - Set this field to true to allow packets addressed to any port or
   packets lacking destination port information (for example, UDP fragments
   after the first fragment) to be forwarded to the backends configured with
   this forwarding rule. The L3_DEFAULT protocol requiresallPorts be set to true. |
| `psc_connection_id` | String |  | [Output Only] The PSC connection id of the PSC forwarding rule. |
| `network` | String |  | This field is not used for global external load balancing.

For internal passthrough Network Load Balancers, this field identifies the
network that the load balanced IP should belong to for this forwarding
rule.
If the subnetwork is specified, the network of the subnetwork will be used.
If neither subnetwork nor this field is specified, the default network will
be used.

For Private Service Connect forwarding rules that forward traffic to Google
APIs, a network must be provided. |
| `allow_psc_global_access` | bool |  | This is used in PSC consumer ForwardingRule to control whether the PSC
endpoint can be accessed from another region. |
| `ip_protocol` | String |  | The IP protocol to which this rule applies.

For protocol forwarding, valid
options are TCP, UDP, ESP,AH, SCTP, ICMP andL3_DEFAULT.

The valid IP protocols are different for different load balancing products
as described in [Load balancing
features](https://cloud.google.com/load-balancing/docs/features#protocols_from_the_load_balancer_to_the_backends). |
| `network_tier` | String |  | This signifies the networking tier used for configuring
this load balancer and can only take the following values:PREMIUM, STANDARD.

For regional ForwardingRule, the valid values are PREMIUM andSTANDARD. For GlobalForwardingRule, the valid value isPREMIUM.

If this field is not specified, it is assumed to be PREMIUM.
If IPAddress is specified, this value must be equal to the
networkTier of the Address. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `name` | String |  | Name of the resource; provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash.

For Private Service Connect forwarding rules that forward traffic to Google
APIs, the forwarding rule name must be a 1-20 characters string with
lowercase letters and numbers and must start with a letter. |
| `fingerprint` | String |  | Fingerprint of this resource. A hash of the contents stored in this object.
This field is used in optimistic locking. This field will be ignored when
inserting a ForwardingRule. Include the fingerprint in patch request to
ensure that you do not overwrite changes that were applied from another
concurrent request.

To see the latest fingerprint, make a get() request to
retrieve a ForwardingRule. |
| `port_range` | String |  | The ports, portRange, and allPorts
fields are mutually exclusive. Only packets addressed to ports in the
specified range will be forwarded to the backends configured with this
forwarding rule.

The portRange field has the following limitations:
   
   - It requires that the forwarding rule IPProtocol be TCP,
   UDP, or SCTP, and
   - It's applicable only to the following products: external passthrough
   Network Load Balancers, internal and external proxy Network Load Balancers,
   internal and external Application Load Balancers, external protocol
   forwarding, and Classic VPN.
   - Some products have restrictions on what ports can be used. See 
   port specifications for details.



For external forwarding rules, two or more forwarding rules cannot use the
same [IPAddress, IPProtocol] pair, and cannot have overlappingportRanges.

For internal forwarding rules within the same VPC network, two or more
forwarding rules cannot use the same [IPAddress, IPProtocol]
pair, and cannot have overlapping portRanges.

@pattern: \\d+(?:-\\d+)? |
| `psc_connection_status` | String |  |  |
| `service_directory_registrations` | Vec<String> |  | Service Directory resources to register this forwarding rule with.
Currently, only supports a single Service Directory resource. |
| `target` | String |  | The URL of the target resource to receive the matched traffic.  For
regional forwarding rules, this target must be in the same region as the
forwarding rule. For global forwarding rules, this target must be a global
load balancing resource.

The forwarded traffic must be of a type appropriate to the target object.
   
   
     -  For load balancers, see the "Target" column in [Port specifications](https://cloud.google.com/load-balancing/docs/forwarding-rule-concepts#ip_address_specifications).
     -  For Private Service Connect forwarding rules that forward traffic to Google APIs, provide the name of a supported Google API bundle:
       
      
            -  vpc-sc -  APIs that support VPC Service Controls. 
            -  all-apis - All supported Google APIs. 
          
     
     -  For Private Service Connect forwarding rules that forward traffic to managed services, the target must be a service attachment. The target is not mutable once set as a service attachment. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `metadata_filters` | Vec<String> |  | Opaque filter criteria used by load balancer to restrict routing
configuration to a limited set of xDS
compliant clients. In their xDS requests to load balancer, xDS clients
present node
metadata. When there is a match, the relevant configuration
is made available to those proxies. Otherwise, all the resources (e.g.TargetHttpProxy, UrlMap)
referenced by the ForwardingRule are not visible to
those proxies.

For each metadataFilter in this list, if itsfilterMatchCriteria is set to MATCH_ANY, at least one of thefilterLabels must match the corresponding label provided in
the metadata. If its filterMatchCriteria is set to
MATCH_ALL, then all of its filterLabels must match with
corresponding labels provided in the metadata. If multiplemetadataFilters are specified, all of them need to be satisfied
in order to be considered a match.

metadataFilters specified here will be applifed before
those specified in the UrlMap that thisForwardingRule references.

metadataFilters only applies to Loadbalancers that have
their loadBalancingScheme set toINTERNAL_SELF_MANAGED. |
| `allow_psc_packet_injection` | bool |  | This is used in PSC consumer ForwardingRule to control whether the producer
is allowed to inject packets into the consumer's network. If set to true,
the target service attachment must have tunneling enabled and
TunnelingConfig.RoutingMode set to PACKET_INJECTION
Non-PSC forwarding rules should not use this field.

This field was never released to any customers and is deprecated and
will be removed in the future. |
| `label_fingerprint` | String |  | A fingerprint for the labels being applied to this resource, which is
essentially a hash of the labels set used for optimistic locking. The
fingerprint is initially generated by Compute Engine and changes after
every request to modify or update labels. You must always provide an
up-to-date fingerprint hash in order to update or change labels,
otherwise the request will fail with error412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve a ForwardingRule. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `backend_service` | String | Identifies the backend service to which the forwarding rule sends traffic.
Required for internal and external passthrough Network Load Balancers;
must be omitted for all other load balancer types. |
| `allow_global_access` | bool | If set to true, clients can access the internal passthrough Network Load
Balancers, the regional internal Application Load Balancer, and the
regional internal proxy Network Load Balancer from all regions.
If false, only allows access from the local region the load balancer is
located at. Note that for INTERNAL_MANAGED forwarding rules, this field
cannot be changed after the forwarding rule is created. |
| `load_balancing_scheme` | String | Specifies the forwarding rule type.

For more information about forwarding rules, refer to
Forwarding rule concepts. |
| `ports` | Vec<String> | The ports, portRange, and allPorts
fields are mutually exclusive. Only packets addressed to ports in the
specified range will be forwarded to the backends configured with this
forwarding rule.

The ports field has the following limitations:
   
   - It requires that the forwarding rule IPProtocol be TCP,
   UDP, or SCTP, and
   - It's applicable only to the following products: internal passthrough
   Network Load Balancers, backend service-based external passthrough Network
   Load Balancers, and internal protocol forwarding.
   - You can specify a list of up to five ports by number, separated by
   commas. The ports can be contiguous or discontiguous.



For external forwarding rules, two or more forwarding rules cannot use the
same [IPAddress, IPProtocol] pair if they share at least one
port number.

For internal forwarding rules within the same VPC network, two or more
forwarding rules cannot use the same [IPAddress, IPProtocol]
pair if they share at least one port number.

@pattern: \\d+(?:-\\d+)? |
| `is_mirroring_collector` | bool | Indicates whether or not this load balancer can be used as a collector for
packet mirroring. To prevent mirroring loops, instances behind this
load balancer will not have their traffic mirrored even if aPacketMirroring rule applies to them.
This can only be set to true for load balancers that have theirloadBalancingScheme set to INTERNAL. |
| `self_link_with_id` | String | [Output Only] Server-defined URL for this resource with the resource id. |
| `base_forwarding_rule` | String | [Output Only] The URL for the corresponding base forwarding rule. By base
forwarding rule, we mean the forwarding rule that has the same IP address,
protocol, and port settings with the current forwarding rule, but without
sourceIPRanges specified.
Always empty if the current forwarding rule does not have sourceIPRanges
specified. |
| `labels` | HashMap<String, String> | Labels for this resource. These can only be added or modified by thesetLabels method. Each label key/value pair must comply withRFC1035.
Label values may be empty. |
| `service_name` | String | [Output Only]
The internal fully qualified service name for this forwarding rule.

This field is only used for internal load balancing. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `kind` | String | [Output Only] Type of the resource. Alwayscompute#forwardingRule for forwarding rule resources. |
| `region` | String | [Output Only] URL of the region where the regional forwarding rule resides.
This field is not applicable to global forwarding rules.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `service_label` | String | An optional prefix to the service name for this forwarding rule.
If specified, the prefix is the first label of the fully qualified service
name.

The label must be 1-63 characters long, and comply withRFC1035.
Specifically, the label must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash.

This field is only used for internal load balancing. |
| `subnetwork` | String | This field identifies the subnetwork that the load balanced IP should
belong to for this forwarding rule, used with internal load balancers and
external passthrough Network Load Balancers with IPv6.

If the network specified is in auto subnet mode, this field is optional.
However, a subnetwork must be specified if the network is in custom subnet
mode or when creating external forwarding rule with IPv6. |
| `ip_version` | String | The IP Version that will be used by this forwarding rule.  Valid options
are IPV4 or IPV6. |
| `ip_collection` | String | Resource reference of a PublicDelegatedPrefix. The PDP must
be a sub-PDP in EXTERNAL_IPV6_FORWARDING_RULE_CREATION mode.

Use one of the following formats to specify a sub-PDP when creating an IPv6
NetLB forwarding rule using BYOIP:
Full resource URL, as inhttps://www.googleapis.com/compute/v1/projects/project_id/regions/region/publicDelegatedPrefixes/sub-pdp-name
Partial URL, as in:
   
   - projects/project_id/regions/region/publicDelegatedPrefixes/sub-pdp-name
   - regions/region/publicDelegatedPrefixes/sub-pdp-name |
| `no_automate_dns_zone` | bool | This is used in PSC consumer ForwardingRule to control whether it should
try to auto-generate a DNS zone or not. Non-PSC forwarding rules do not use
this field. Once set, this field is not mutable. |
| `external_managed_backend_bucket_migration_state` | String | Specifies the canary migration state for the backend buckets attached to
this forwarding rule. Possible values are PREPARE, TEST_BY_PERCENTAGE, and
TEST_ALL_TRAFFIC.

To begin the migration from EXTERNAL to EXTERNAL_MANAGED, the state must be
changed to PREPARE. The state must be changed to TEST_ALL_TRAFFIC before
the loadBalancingScheme can be changed to EXTERNAL_MANAGED. Optionally, the
TEST_BY_PERCENTAGE state can be used to migrate traffic to backend buckets
attached to this forwarding rule by percentage using
externalManagedBackendBucketMigrationTestingPercentage.

Rolling back a migration requires the states to be set in reverse order. So
changing the scheme from EXTERNAL_MANAGED to EXTERNAL requires the state to
be set to TEST_ALL_TRAFFIC at the same time. Optionally, the
TEST_BY_PERCENTAGE state can be used to migrate some traffic back to
EXTERNAL or PREPARE can be used to migrate all traffic back to EXTERNAL. |
| `source_ip_ranges` | Vec<String> | If not empty, this forwarding rule will only forward the traffic when the
source IP address matches one of the IP addresses or CIDR ranges set here.
Note that a forwarding rule can only have up to 64 source IP ranges, and
this field can only be used with a regional forwarding rule whose scheme isEXTERNAL.
Each source_ip_range entry should be either an IP address (for
example, 1.2.3.4) or a CIDR range (for example, 1.2.3.0/24). |
| `external_managed_backend_bucket_migration_testing_percentage` | f64 | Determines the fraction of requests to backend buckets that should be
processed by the global external Application Load Balancer.

The value of this field must be in the range [0, 100].

This value can only be set if the loadBalancingScheme in the BackendService
is set to EXTERNAL (when using the classic Application Load Balancer) and
the migration state is TEST_BY_PERCENTAGE. |
| `ip_address` | String | IP address for which this forwarding rule accepts traffic. When a client
sends traffic to this IP address, the forwarding rule directs the traffic
to the referenced target or backendService.
While creating a forwarding rule, specifying an IPAddress is
required under the following circumstances:

   
   - When the target is set to targetGrpcProxy andvalidateForProxyless is set to true, theIPAddress should be set to 0.0.0.0.
   - When the target is a Private Service Connect Google APIs
   bundle, you must specify an IPAddress.


Otherwise, you can optionally specify an IP address that references an
existing static (reserved) IP address resource. When omitted, Google Cloud
assigns an ephemeral IP address.

Use one of the following formats to specify an IP address while creating a
forwarding rule:

* IP address number, as in `100.1.2.3`
* IPv6 address range, as in `2600:1234::/96`
* Full resource URL, as inhttps://www.googleapis.com/compute/v1/projects/project_id/regions/region/addresses/address-name
* Partial URL or by name, as in:
   
   - projects/project_id/regions/region/addresses/address-name
   - regions/region/addresses/address-name
   - global/addresses/address-name
   - address-name



The forwarding rule's target or backendService,
and in most cases, also the loadBalancingScheme, determine the
type of IP address that you can use. For detailed information, see
[IP address
specifications](https://cloud.google.com/load-balancing/docs/forwarding-rule-concepts#ip_address_specifications).

When reading an IPAddress, the API always returns the IP
address number. |
| `all_ports` | bool | The ports, portRange, and allPorts
fields are mutually exclusive. Only packets addressed to ports in the
specified range will be forwarded to the backends configured with this
forwarding rule.

The allPorts field has the following limitations:
   
   - It requires that the forwarding rule IPProtocol be TCP,
   UDP, SCTP, or L3_DEFAULT.
   - It's applicable only to the following products: internal passthrough
   Network Load Balancers, backend service-based external passthrough Network
   Load Balancers, and internal and external protocol forwarding.
   - Set this field to true to allow packets addressed to any port or
   packets lacking destination port information (for example, UDP fragments
   after the first fragment) to be forwarded to the backends configured with
   this forwarding rule. The L3_DEFAULT protocol requiresallPorts be set to true. |
| `psc_connection_id` | String | [Output Only] The PSC connection id of the PSC forwarding rule. |
| `network` | String | This field is not used for global external load balancing.

For internal passthrough Network Load Balancers, this field identifies the
network that the load balanced IP should belong to for this forwarding
rule.
If the subnetwork is specified, the network of the subnetwork will be used.
If neither subnetwork nor this field is specified, the default network will
be used.

For Private Service Connect forwarding rules that forward traffic to Google
APIs, a network must be provided. |
| `allow_psc_global_access` | bool | This is used in PSC consumer ForwardingRule to control whether the PSC
endpoint can be accessed from another region. |
| `ip_protocol` | String | The IP protocol to which this rule applies.

For protocol forwarding, valid
options are TCP, UDP, ESP,AH, SCTP, ICMP andL3_DEFAULT.

The valid IP protocols are different for different load balancing products
as described in [Load balancing
features](https://cloud.google.com/load-balancing/docs/features#protocols_from_the_load_balancer_to_the_backends). |
| `network_tier` | String | This signifies the networking tier used for configuring
this load balancer and can only take the following values:PREMIUM, STANDARD.

For regional ForwardingRule, the valid values are PREMIUM andSTANDARD. For GlobalForwardingRule, the valid value isPREMIUM.

If this field is not specified, it is assumed to be PREMIUM.
If IPAddress is specified, this value must be equal to the
networkTier of the Address. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `name` | String | Name of the resource; provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash.

For Private Service Connect forwarding rules that forward traffic to Google
APIs, the forwarding rule name must be a 1-20 characters string with
lowercase letters and numbers and must start with a letter. |
| `fingerprint` | String | Fingerprint of this resource. A hash of the contents stored in this object.
This field is used in optimistic locking. This field will be ignored when
inserting a ForwardingRule. Include the fingerprint in patch request to
ensure that you do not overwrite changes that were applied from another
concurrent request.

To see the latest fingerprint, make a get() request to
retrieve a ForwardingRule. |
| `port_range` | String | The ports, portRange, and allPorts
fields are mutually exclusive. Only packets addressed to ports in the
specified range will be forwarded to the backends configured with this
forwarding rule.

The portRange field has the following limitations:
   
   - It requires that the forwarding rule IPProtocol be TCP,
   UDP, or SCTP, and
   - It's applicable only to the following products: external passthrough
   Network Load Balancers, internal and external proxy Network Load Balancers,
   internal and external Application Load Balancers, external protocol
   forwarding, and Classic VPN.
   - Some products have restrictions on what ports can be used. See 
   port specifications for details.



For external forwarding rules, two or more forwarding rules cannot use the
same [IPAddress, IPProtocol] pair, and cannot have overlappingportRanges.

For internal forwarding rules within the same VPC network, two or more
forwarding rules cannot use the same [IPAddress, IPProtocol]
pair, and cannot have overlapping portRanges.

@pattern: \\d+(?:-\\d+)? |
| `psc_connection_status` | String |  |
| `service_directory_registrations` | Vec<String> | Service Directory resources to register this forwarding rule with.
Currently, only supports a single Service Directory resource. |
| `target` | String | The URL of the target resource to receive the matched traffic.  For
regional forwarding rules, this target must be in the same region as the
forwarding rule. For global forwarding rules, this target must be a global
load balancing resource.

The forwarded traffic must be of a type appropriate to the target object.
   
   
     -  For load balancers, see the "Target" column in [Port specifications](https://cloud.google.com/load-balancing/docs/forwarding-rule-concepts#ip_address_specifications).
     -  For Private Service Connect forwarding rules that forward traffic to Google APIs, provide the name of a supported Google API bundle:
       
      
            -  vpc-sc -  APIs that support VPC Service Controls. 
            -  all-apis - All supported Google APIs. 
          
     
     -  For Private Service Connect forwarding rules that forward traffic to managed services, the target must be a service attachment. The target is not mutable once set as a service attachment. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `metadata_filters` | Vec<String> | Opaque filter criteria used by load balancer to restrict routing
configuration to a limited set of xDS
compliant clients. In their xDS requests to load balancer, xDS clients
present node
metadata. When there is a match, the relevant configuration
is made available to those proxies. Otherwise, all the resources (e.g.TargetHttpProxy, UrlMap)
referenced by the ForwardingRule are not visible to
those proxies.

For each metadataFilter in this list, if itsfilterMatchCriteria is set to MATCH_ANY, at least one of thefilterLabels must match the corresponding label provided in
the metadata. If its filterMatchCriteria is set to
MATCH_ALL, then all of its filterLabels must match with
corresponding labels provided in the metadata. If multiplemetadataFilters are specified, all of them need to be satisfied
in order to be considered a match.

metadataFilters specified here will be applifed before
those specified in the UrlMap that thisForwardingRule references.

metadataFilters only applies to Loadbalancers that have
their loadBalancingScheme set toINTERNAL_SELF_MANAGED. |
| `allow_psc_packet_injection` | bool | This is used in PSC consumer ForwardingRule to control whether the producer
is allowed to inject packets into the consumer's network. If set to true,
the target service attachment must have tunneling enabled and
TunnelingConfig.RoutingMode set to PACKET_INJECTION
Non-PSC forwarding rules should not use this field.

This field was never released to any customers and is deprecated and
will be removed in the future. |
| `label_fingerprint` | String | A fingerprint for the labels being applied to this resource, which is
essentially a hash of the labels set used for optimistic locking. The
fingerprint is initially generated by Compute Engine and changes after
every request to modify or update labels. You must always provide an
up-to-date fingerprint hash in order to update or change labels,
otherwise the request will fail with error412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve a ForwardingRule. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create global_forwarding_rule
global_forwarding_rule = provider.compute_api.Global_forwarding_rule {
    project = "value"  # Project ID for this request.
}

# Access global_forwarding_rule outputs
global_forwarding_rule_id = global_forwarding_rule.id
global_forwarding_rule_backend_service = global_forwarding_rule.backend_service
global_forwarding_rule_allow_global_access = global_forwarding_rule.allow_global_access
global_forwarding_rule_load_balancing_scheme = global_forwarding_rule.load_balancing_scheme
global_forwarding_rule_ports = global_forwarding_rule.ports
global_forwarding_rule_is_mirroring_collector = global_forwarding_rule.is_mirroring_collector
global_forwarding_rule_self_link_with_id = global_forwarding_rule.self_link_with_id
global_forwarding_rule_base_forwarding_rule = global_forwarding_rule.base_forwarding_rule
global_forwarding_rule_labels = global_forwarding_rule.labels
global_forwarding_rule_service_name = global_forwarding_rule.service_name
global_forwarding_rule_id = global_forwarding_rule.id
global_forwarding_rule_kind = global_forwarding_rule.kind
global_forwarding_rule_region = global_forwarding_rule.region
global_forwarding_rule_service_label = global_forwarding_rule.service_label
global_forwarding_rule_subnetwork = global_forwarding_rule.subnetwork
global_forwarding_rule_ip_version = global_forwarding_rule.ip_version
global_forwarding_rule_ip_collection = global_forwarding_rule.ip_collection
global_forwarding_rule_no_automate_dns_zone = global_forwarding_rule.no_automate_dns_zone
global_forwarding_rule_external_managed_backend_bucket_migration_state = global_forwarding_rule.external_managed_backend_bucket_migration_state
global_forwarding_rule_source_ip_ranges = global_forwarding_rule.source_ip_ranges
global_forwarding_rule_external_managed_backend_bucket_migration_testing_percentage = global_forwarding_rule.external_managed_backend_bucket_migration_testing_percentage
global_forwarding_rule_ip_address = global_forwarding_rule.ip_address
global_forwarding_rule_all_ports = global_forwarding_rule.all_ports
global_forwarding_rule_psc_connection_id = global_forwarding_rule.psc_connection_id
global_forwarding_rule_network = global_forwarding_rule.network
global_forwarding_rule_allow_psc_global_access = global_forwarding_rule.allow_psc_global_access
global_forwarding_rule_ip_protocol = global_forwarding_rule.ip_protocol
global_forwarding_rule_network_tier = global_forwarding_rule.network_tier
global_forwarding_rule_self_link = global_forwarding_rule.self_link
global_forwarding_rule_name = global_forwarding_rule.name
global_forwarding_rule_fingerprint = global_forwarding_rule.fingerprint
global_forwarding_rule_port_range = global_forwarding_rule.port_range
global_forwarding_rule_psc_connection_status = global_forwarding_rule.psc_connection_status
global_forwarding_rule_service_directory_registrations = global_forwarding_rule.service_directory_registrations
global_forwarding_rule_target = global_forwarding_rule.target
global_forwarding_rule_description = global_forwarding_rule.description
global_forwarding_rule_creation_timestamp = global_forwarding_rule.creation_timestamp
global_forwarding_rule_metadata_filters = global_forwarding_rule.metadata_filters
global_forwarding_rule_allow_psc_packet_injection = global_forwarding_rule.allow_psc_packet_injection
global_forwarding_rule_label_fingerprint = global_forwarding_rule.label_fingerprint
```

---


### Public_delegated_prefixe

Creates a PublicDelegatedPrefix in the specified project in the given
region using the parameters that are included in the request.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `byoip_api_version` | String |  | [Output Only] The version of BYOIP API. |
| `allocatable_prefix_length` | i64 |  | The allocatable prefix length supported by this public delegated prefix.
This field is optional and cannot be set for prefixes in DELEGATION mode.
It cannot be set for IPv4 prefixes either, and it always defaults to 32. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `ipv6_access_type` | String |  | [Output Only] The internet access type for IPv6 Public Delegated Prefixes.
Inherited from parent prefix. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `enable_enhanced_ipv4_allocation` | bool |  | [Output Only] Whether this PDP supports enhanced IPv4 allocations.
Applicable for IPv4 PDPs only. |
| `mode` | String |  | The public delegated prefix mode for IPv6 only. |
| `public_delegated_sub_prefixs` | Vec<String> |  | The list of sub public delegated prefixes that exist for this public
delegated prefix. |
| `name` | String |  | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `status` | String |  | [Output Only] The status of the public delegated prefix, which can be one
of following values:
   
   
     - `INITIALIZING` The public delegated prefix is being initialized and
     addresses cannot be created yet.
     - `READY_TO_ANNOUNCE` The public delegated prefix is a live migration
     prefix and is active.
     - `ANNOUNCED` The public delegated prefix is announced and ready to
     use.
     - `DELETING` The public delegated prefix is being deprovsioned.
     - `ACTIVE` The public delegated prefix is ready to use. |
| `is_live_migration` | bool |  | If true, the prefix will be live migrated. |
| `region` | String |  | [Output Only] URL of the region where the public delegated prefix resides.
This field applies only to the region resource. You must specify this
field as part of the HTTP request URL. It is not settable as a field in
the request body. |
| `kind` | String |  | [Output Only] Type of the resource. Alwayscompute#publicDelegatedPrefix for public delegated prefixes. |
| `ip_cidr_range` | String |  | The IP address range, in CIDR format, represented by this public
delegated prefix. |
| `fingerprint` | String |  | Fingerprint of this resource. A hash of the contents stored in this object.
This field is used in optimistic locking. This field will be ignored when
inserting a new PublicDelegatedPrefix. An up-to-date
fingerprint must be provided in order to update thePublicDelegatedPrefix, otherwise the request will fail with
error 412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve a PublicDelegatedPrefix. |
| `parent_prefix` | String |  | The URL of parent prefix. Either PublicAdvertisedPrefix or
PublicDelegatedPrefix. |
| `id` | String |  | [Output Only] The unique identifier for the resource type. The server
generates this identifier. |
| `region` | String | ✅ | Name of the region of this request. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `byoip_api_version` | String | [Output Only] The version of BYOIP API. |
| `allocatable_prefix_length` | i64 | The allocatable prefix length supported by this public delegated prefix.
This field is optional and cannot be set for prefixes in DELEGATION mode.
It cannot be set for IPv4 prefixes either, and it always defaults to 32. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `ipv6_access_type` | String | [Output Only] The internet access type for IPv6 Public Delegated Prefixes.
Inherited from parent prefix. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `enable_enhanced_ipv4_allocation` | bool | [Output Only] Whether this PDP supports enhanced IPv4 allocations.
Applicable for IPv4 PDPs only. |
| `mode` | String | The public delegated prefix mode for IPv6 only. |
| `public_delegated_sub_prefixs` | Vec<String> | The list of sub public delegated prefixes that exist for this public
delegated prefix. |
| `name` | String | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `status` | String | [Output Only] The status of the public delegated prefix, which can be one
of following values:
   
   
     - `INITIALIZING` The public delegated prefix is being initialized and
     addresses cannot be created yet.
     - `READY_TO_ANNOUNCE` The public delegated prefix is a live migration
     prefix and is active.
     - `ANNOUNCED` The public delegated prefix is announced and ready to
     use.
     - `DELETING` The public delegated prefix is being deprovsioned.
     - `ACTIVE` The public delegated prefix is ready to use. |
| `is_live_migration` | bool | If true, the prefix will be live migrated. |
| `region` | String | [Output Only] URL of the region where the public delegated prefix resides.
This field applies only to the region resource. You must specify this
field as part of the HTTP request URL. It is not settable as a field in
the request body. |
| `kind` | String | [Output Only] Type of the resource. Alwayscompute#publicDelegatedPrefix for public delegated prefixes. |
| `ip_cidr_range` | String | The IP address range, in CIDR format, represented by this public
delegated prefix. |
| `fingerprint` | String | Fingerprint of this resource. A hash of the contents stored in this object.
This field is used in optimistic locking. This field will be ignored when
inserting a new PublicDelegatedPrefix. An up-to-date
fingerprint must be provided in order to update thePublicDelegatedPrefix, otherwise the request will fail with
error 412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve a PublicDelegatedPrefix. |
| `parent_prefix` | String | The URL of parent prefix. Either PublicAdvertisedPrefix or
PublicDelegatedPrefix. |
| `id` | String | [Output Only] The unique identifier for the resource type. The server
generates this identifier. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create public_delegated_prefixe
public_delegated_prefixe = provider.compute_api.Public_delegated_prefixe {
    region = "value"  # Name of the region of this request.
    project = "value"  # Project ID for this request.
}

# Access public_delegated_prefixe outputs
public_delegated_prefixe_id = public_delegated_prefixe.id
public_delegated_prefixe_byoip_api_version = public_delegated_prefixe.byoip_api_version
public_delegated_prefixe_allocatable_prefix_length = public_delegated_prefixe.allocatable_prefix_length
public_delegated_prefixe_description = public_delegated_prefixe.description
public_delegated_prefixe_ipv6_access_type = public_delegated_prefixe.ipv6_access_type
public_delegated_prefixe_creation_timestamp = public_delegated_prefixe.creation_timestamp
public_delegated_prefixe_enable_enhanced_ipv4_allocation = public_delegated_prefixe.enable_enhanced_ipv4_allocation
public_delegated_prefixe_mode = public_delegated_prefixe.mode
public_delegated_prefixe_public_delegated_sub_prefixs = public_delegated_prefixe.public_delegated_sub_prefixs
public_delegated_prefixe_name = public_delegated_prefixe.name
public_delegated_prefixe_self_link = public_delegated_prefixe.self_link
public_delegated_prefixe_status = public_delegated_prefixe.status
public_delegated_prefixe_is_live_migration = public_delegated_prefixe.is_live_migration
public_delegated_prefixe_region = public_delegated_prefixe.region
public_delegated_prefixe_kind = public_delegated_prefixe.kind
public_delegated_prefixe_ip_cidr_range = public_delegated_prefixe.ip_cidr_range
public_delegated_prefixe_fingerprint = public_delegated_prefixe.fingerprint
public_delegated_prefixe_parent_prefix = public_delegated_prefixe.parent_prefix
public_delegated_prefixe_id = public_delegated_prefixe.id
```

---


### External_vpn_gateway

Creates a ExternalVpnGateway in the specified project using
the data included in the request.

**Operations**: ✅ Create ✅ Read ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `labels` | HashMap<String, String> |  | Labels for this resource. These can only be added or modified by thesetLabels method. Each label key/value pair must comply withRFC1035.
Label values may be empty. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `redundancy_type` | String |  | Indicates the user-supplied redundancy type of this external VPN gateway. |
| `label_fingerprint` | String |  | A fingerprint for the labels being applied to this ExternalVpnGateway,
which is essentially a hash of the labels set used for optimistic locking.
The fingerprint is initially generated by Compute Engine and changes after
every request to modify or update labels. You must always provide an
up-to-date fingerprint hash in order to update or change labels,
otherwise the request will fail with error412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve an ExternalVpnGateway. |
| `name` | String |  | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must be
a dash, lowercase letter, or digit, except the last character, which cannot
be a dash. |
| `kind` | String |  | [Output Only] Type of the resource. Alwayscompute#externalVpnGateway for externalVpnGateways. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `interfaces` | Vec<String> |  | A list of interfaces for this external VPN gateway.

If your peer-side gateway is an on-premises gateway and non-AWS cloud
providers' gateway, at most two interfaces can be provided for an external
VPN gateway. If your peer side is an AWS virtual private gateway, four
interfaces should be provided for an external VPN gateway. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `labels` | HashMap<String, String> | Labels for this resource. These can only be added or modified by thesetLabels method. Each label key/value pair must comply withRFC1035.
Label values may be empty. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `redundancy_type` | String | Indicates the user-supplied redundancy type of this external VPN gateway. |
| `label_fingerprint` | String | A fingerprint for the labels being applied to this ExternalVpnGateway,
which is essentially a hash of the labels set used for optimistic locking.
The fingerprint is initially generated by Compute Engine and changes after
every request to modify or update labels. You must always provide an
up-to-date fingerprint hash in order to update or change labels,
otherwise the request will fail with error412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve an ExternalVpnGateway. |
| `name` | String | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must be
a dash, lowercase letter, or digit, except the last character, which cannot
be a dash. |
| `kind` | String | [Output Only] Type of the resource. Alwayscompute#externalVpnGateway for externalVpnGateways. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `interfaces` | Vec<String> | A list of interfaces for this external VPN gateway.

If your peer-side gateway is an on-premises gateway and non-AWS cloud
providers' gateway, at most two interfaces can be provided for an external
VPN gateway. If your peer side is an AWS virtual private gateway, four
interfaces should be provided for an external VPN gateway. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create external_vpn_gateway
external_vpn_gateway = provider.compute_api.External_vpn_gateway {
    project = "value"  # Project ID for this request.
}

# Access external_vpn_gateway outputs
external_vpn_gateway_id = external_vpn_gateway.id
external_vpn_gateway_id = external_vpn_gateway.id
external_vpn_gateway_labels = external_vpn_gateway.labels
external_vpn_gateway_description = external_vpn_gateway.description
external_vpn_gateway_redundancy_type = external_vpn_gateway.redundancy_type
external_vpn_gateway_label_fingerprint = external_vpn_gateway.label_fingerprint
external_vpn_gateway_name = external_vpn_gateway.name
external_vpn_gateway_kind = external_vpn_gateway.kind
external_vpn_gateway_self_link = external_vpn_gateway.self_link
external_vpn_gateway_interfaces = external_vpn_gateway.interfaces
external_vpn_gateway_creation_timestamp = external_vpn_gateway.creation_timestamp
```

---


### Region_security_policie

Creates a new policy in the specified project using the data included in
the request.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `kind` | String |  | [Output only] Type of the resource. Alwayscompute#securityPolicyfor security policies |
| `region` | String |  | [Output Only] URL of the region where the regional security policy
resides. This field is not applicable to global security policies. |
| `associations` | Vec<String> |  | A list of associations that belong to this policy. |
| `rules` | Vec<String> |  | A list of rules that belong to this policy.
There must always be a default rule which is a rule with priority
2147483647 and match all condition (for the match condition this means
match  "*" for srcIpRanges and for the networkMatch condition every field
must be either match "*" or not set). If no rules are provided when
creating a security policy, a default rule with action "allow" will be
added. |
| `adaptive_protection_config` | String |  |  |
| `user_defined_fields` | Vec<String> |  | Definitions of user-defined fields for CLOUD_ARMOR_NETWORK policies. A
user-defined field consists of up to 4 bytes extracted from a fixed offset
in the packet, relative to the IPv4, IPv6, TCP, or UDP header, with an
optional mask to select certain bits. Rules may then specify matching
values for these fields.

Example:

 userDefinedFields:
 - name: "ipv4_fragment_offset"
   base: IPV4
   offset: 6
   size: 2
   mask: "0x1fff" |
| `name` | String |  | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `recaptcha_options_config` | String |  |  |
| `advanced_options_config` | String |  |  |
| `short_name` | String |  | User-provided name of the organization security policy. The name should be
unique in the organization in which the security policy is created. This
should only be used when SecurityPolicyType is CLOUD_ARMOR.
The name must be 1-63 characters long, and comply with
https://www.ietf.org/rfc/rfc1035.txt. Specifically, the name must be 1-63
characters long and match the regular expression
`[a-z]([-a-z0-9]*[a-z0-9])?` which means the first character must be a
lowercase letter, and all following characters must be a dash, lowercase
letter, or digit, except the last character, which cannot be a dash. |
| `parent` | String |  | [Output Only] The parent of the security policy. |
| `fingerprint` | String |  | Specifies a fingerprint for this resource, which is essentially a hash of
the metadata's contents and used for optimistic locking. The
fingerprint is initially generated by Compute Engine and changes after
every request to modify or update metadata. You must always provide an
up-to-date fingerprint hash in order to update or change metadata,
otherwise the request will fail with error412 conditionNotMet.

To see the latest fingerprint, make get() request to the
security policy. |
| `ddos_protection_config` | String |  |  |
| `label_fingerprint` | String |  | A fingerprint for the labels being applied to this security policy, which
is essentially a hash of the labels set used for optimistic locking. The
fingerprint is initially generated by Compute Engine and changes after
every request to modify or update labels. You must always provide an
up-to-date fingerprint hash in order to update or change labels.

To see the latest fingerprint, make get() request to the
security policy. |
| `display_name` | String |  | User-provided name of the organization security policy. The name should be
unique in the organization in which the security policy is created. This
should only be used when SecurityPolicyType is FIREWALL.
The name must be 1-63 characters long, and comply with
https://www.ietf.org/rfc/rfc1035.txt. Specifically, the name must be 1-63
characters long and match the regular expression
`[a-z]([-a-z0-9]*[a-z0-9])?` which means the first character must be a
lowercase letter, and all following characters must be a dash, lowercase
letter, or digit, except the last character, which cannot be a dash. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `labels` | HashMap<String, String> |  | Labels for this resource. These can only be added or modified by thesetLabels method. Each label key/value pair must comply withRFC1035.
Label values may be empty. |
| `rule_tuple_count` | i64 |  | [Output Only] Total count of all security policy rule tuples. A security
policy can not exceed a set number of tuples. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `self_link_with_id` | String |  | [Output Only] Server-defined URL for this resource with the resource id. |
| `type` | String |  | The type indicates the intended use of the security policy.
   
   - CLOUD_ARMOR: Cloud Armor backend security policies can
   be configured to filter incoming HTTP requests targeting backend services.
   They filter requests before they hit the origin servers.
   - CLOUD_ARMOR_EDGE: Cloud Armor edge security policies can
   be configured to filter incoming HTTP requests targeting backend services
   (including Cloud CDN-enabled) as well as backend buckets (Cloud Storage).
   They filter requests before the request is served from Google's cache.
   - CLOUD_ARMOR_INTERNAL_SERVICE (preview only): Cloud Armor
   internal service policies can be configured to filter HTTP requests
   targeting services managed by Traffic Director in a service mesh. They
   filter requests before the request is served from the application.

- CLOUD_ARMOR_NETWORK: Cloud Armor network policies
can be configured to filter packets targeting network load balancing
resources such as backend services, target pools, target instances, and
instances with external IPs. They filter requests before the request is
served from the application.


This field can be set only at resource creation time. |
| `region` | String | ✅ | Name of the region scoping this request. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `kind` | String | [Output only] Type of the resource. Alwayscompute#securityPolicyfor security policies |
| `region` | String | [Output Only] URL of the region where the regional security policy
resides. This field is not applicable to global security policies. |
| `associations` | Vec<String> | A list of associations that belong to this policy. |
| `rules` | Vec<String> | A list of rules that belong to this policy.
There must always be a default rule which is a rule with priority
2147483647 and match all condition (for the match condition this means
match  "*" for srcIpRanges and for the networkMatch condition every field
must be either match "*" or not set). If no rules are provided when
creating a security policy, a default rule with action "allow" will be
added. |
| `adaptive_protection_config` | String |  |
| `user_defined_fields` | Vec<String> | Definitions of user-defined fields for CLOUD_ARMOR_NETWORK policies. A
user-defined field consists of up to 4 bytes extracted from a fixed offset
in the packet, relative to the IPv4, IPv6, TCP, or UDP header, with an
optional mask to select certain bits. Rules may then specify matching
values for these fields.

Example:

 userDefinedFields:
 - name: "ipv4_fragment_offset"
   base: IPV4
   offset: 6
   size: 2
   mask: "0x1fff" |
| `name` | String | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `recaptcha_options_config` | String |  |
| `advanced_options_config` | String |  |
| `short_name` | String | User-provided name of the organization security policy. The name should be
unique in the organization in which the security policy is created. This
should only be used when SecurityPolicyType is CLOUD_ARMOR.
The name must be 1-63 characters long, and comply with
https://www.ietf.org/rfc/rfc1035.txt. Specifically, the name must be 1-63
characters long and match the regular expression
`[a-z]([-a-z0-9]*[a-z0-9])?` which means the first character must be a
lowercase letter, and all following characters must be a dash, lowercase
letter, or digit, except the last character, which cannot be a dash. |
| `parent` | String | [Output Only] The parent of the security policy. |
| `fingerprint` | String | Specifies a fingerprint for this resource, which is essentially a hash of
the metadata's contents and used for optimistic locking. The
fingerprint is initially generated by Compute Engine and changes after
every request to modify or update metadata. You must always provide an
up-to-date fingerprint hash in order to update or change metadata,
otherwise the request will fail with error412 conditionNotMet.

To see the latest fingerprint, make get() request to the
security policy. |
| `ddos_protection_config` | String |  |
| `label_fingerprint` | String | A fingerprint for the labels being applied to this security policy, which
is essentially a hash of the labels set used for optimistic locking. The
fingerprint is initially generated by Compute Engine and changes after
every request to modify or update labels. You must always provide an
up-to-date fingerprint hash in order to update or change labels.

To see the latest fingerprint, make get() request to the
security policy. |
| `display_name` | String | User-provided name of the organization security policy. The name should be
unique in the organization in which the security policy is created. This
should only be used when SecurityPolicyType is FIREWALL.
The name must be 1-63 characters long, and comply with
https://www.ietf.org/rfc/rfc1035.txt. Specifically, the name must be 1-63
characters long and match the regular expression
`[a-z]([-a-z0-9]*[a-z0-9])?` which means the first character must be a
lowercase letter, and all following characters must be a dash, lowercase
letter, or digit, except the last character, which cannot be a dash. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `labels` | HashMap<String, String> | Labels for this resource. These can only be added or modified by thesetLabels method. Each label key/value pair must comply withRFC1035.
Label values may be empty. |
| `rule_tuple_count` | i64 | [Output Only] Total count of all security policy rule tuples. A security
policy can not exceed a set number of tuples. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `self_link_with_id` | String | [Output Only] Server-defined URL for this resource with the resource id. |
| `type` | String | The type indicates the intended use of the security policy.
   
   - CLOUD_ARMOR: Cloud Armor backend security policies can
   be configured to filter incoming HTTP requests targeting backend services.
   They filter requests before they hit the origin servers.
   - CLOUD_ARMOR_EDGE: Cloud Armor edge security policies can
   be configured to filter incoming HTTP requests targeting backend services
   (including Cloud CDN-enabled) as well as backend buckets (Cloud Storage).
   They filter requests before the request is served from Google's cache.
   - CLOUD_ARMOR_INTERNAL_SERVICE (preview only): Cloud Armor
   internal service policies can be configured to filter HTTP requests
   targeting services managed by Traffic Director in a service mesh. They
   filter requests before the request is served from the application.

- CLOUD_ARMOR_NETWORK: Cloud Armor network policies
can be configured to filter packets targeting network load balancing
resources such as backend services, target pools, target instances, and
instances with external IPs. They filter requests before the request is
served from the application.


This field can be set only at resource creation time. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create region_security_policie
region_security_policie = provider.compute_api.Region_security_policie {
    region = "value"  # Name of the region scoping this request.
    project = "value"  # Project ID for this request.
}

# Access region_security_policie outputs
region_security_policie_id = region_security_policie.id
region_security_policie_kind = region_security_policie.kind
region_security_policie_region = region_security_policie.region
region_security_policie_associations = region_security_policie.associations
region_security_policie_rules = region_security_policie.rules
region_security_policie_adaptive_protection_config = region_security_policie.adaptive_protection_config
region_security_policie_user_defined_fields = region_security_policie.user_defined_fields
region_security_policie_name = region_security_policie.name
region_security_policie_recaptcha_options_config = region_security_policie.recaptcha_options_config
region_security_policie_advanced_options_config = region_security_policie.advanced_options_config
region_security_policie_short_name = region_security_policie.short_name
region_security_policie_parent = region_security_policie.parent
region_security_policie_fingerprint = region_security_policie.fingerprint
region_security_policie_ddos_protection_config = region_security_policie.ddos_protection_config
region_security_policie_label_fingerprint = region_security_policie.label_fingerprint
region_security_policie_display_name = region_security_policie.display_name
region_security_policie_creation_timestamp = region_security_policie.creation_timestamp
region_security_policie_description = region_security_policie.description
region_security_policie_labels = region_security_policie.labels
region_security_policie_rule_tuple_count = region_security_policie.rule_tuple_count
region_security_policie_id = region_security_policie.id
region_security_policie_self_link = region_security_policie.self_link
region_security_policie_self_link_with_id = region_security_policie.self_link_with_id
region_security_policie_type = region_security_policie.type
```

---


### Interconnect_attachment

Creates an InterconnectAttachment in the specified project using the data
included in the request.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `cloud_router_ipv6_address` | String |  | [Output Only] IPv6 address + prefix length to be configured on Cloud
Router Interface for this interconnect attachment. |
| `subnet_length` | i64 |  | Input only. Length of the IPv4 subnet mask.
Allowed values:
   
   
    - 29 (default)
    - 30

The default value is 29, except for Cross-Cloud Interconnect
connections that use an InterconnectRemoteLocation with a
constraints.subnetLengthRange.min equal to 30. For example,
connections that use an Azure remote location fall into this
category. In these cases, the default value is 30, and requesting
29 returns an error.

Where both 29 and 30 are allowed, 29 is preferred, because it gives
Google Cloud Support more debugging visibility. |
| `cloud_router_ipv6_interface_id` | String |  | This field is not available. |
| `labels` | HashMap<String, String> |  | Labels for this resource. These can only be added or modified by thesetLabels method. Each label key/value pair must comply withRFC1035.
Label values may be empty. |
| `dataplane_version` | i64 |  | [Output Only] Dataplane version for this InterconnectAttachment. This
field is only present for Dataplane version 2 and higher. Absence of this
field in the API output indicates that the Dataplane is version 1. |
| `l2_forwarding` | String |  | L2 Interconnect Attachment related config. This field is required if the
type is L2_DEDICATED.

The configuration specifies how VLAN tags (like dot1q, qinq, or dot1ad)
within L2 packets are mapped to the destination appliances IP addresses.
The packet is then encapsulated with the appliance IP address and sent to
the edge appliance. |
| `private_interconnect_info` | String |  | [Output Only] Information specific to an InterconnectAttachment.
This property is populated if the interconnect that
this is attached to is of type DEDICATED. |
| `candidate_subnets` | Vec<String> |  | Input only. Up to 16 candidate prefixes that can be used to restrict the allocation
of cloudRouterIpAddress and customerRouterIpAddress for this attachment.
All prefixes must be within link-local address space (169.254.0.0/16) and
must be /29 or shorter (/28, /27, etc). Google will attempt to select an
unused /29 from the supplied candidate prefix(es). The request will fail if
all possible /29s are in use on Google's edge. If not supplied, Google will
randomly select an unused /29 from all of link-local space. |
| `candidate_ipv6_subnets` | Vec<String> |  | This field is not available. |
| `attachment_group` | String |  | [Output Only] URL of the AttachmentGroup that includes this Attachment. |
| `interconnect` | String |  | URL of the underlying Interconnect object that this attachment's traffic
will traverse through. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `customer_router_ip_address` | String |  | [Output Only] IPv4 address + prefix length to be configured on the customer
router subinterface for this interconnect attachment. |
| `edge_availability_domain` | String |  | Input only. Desired availability domain for the attachment. Only available for type
PARTNER, at creation time, and can take one of the following values:
   
   - AVAILABILITY_DOMAIN_ANY
   - AVAILABILITY_DOMAIN_1
   - AVAILABILITY_DOMAIN_2


For improved reliability, customers should configure a pair of attachments,
one per availability domain. The selected availability domain will be
provided to the Partner via the pairing key, so that the provisioned
circuit will lie in the specified domain. If not specified, the value will
default to AVAILABILITY_DOMAIN_ANY. |
| `ipsec_internal_addresses` | Vec<String> |  | A list of URLs of addresses that have been reserved for the VLAN
attachment. Used only for the VLAN attachment that has the encryption
option as IPSEC. The addresses must be regional internal IP address ranges.
When creating an HA VPN gateway over the VLAN attachment, if the attachment
is configured to use a regional internal IP address, then the VPN gateway's
IP address is allocated from the IP address range specified here. For
example, if the HA VPN gateway's interface 0 is paired to this VLAN
attachment, then a regional internal IP address for the VPN gateway
interface 0 will be allocated from the IP address specified for this
VLAN attachment.
If this field is not specified when creating the VLAN attachment, then
later on when creating an HA VPN gateway on this VLAN attachment, the HA
VPN gateway's IP address is allocated from the regional external IP address
pool. |
| `name` | String |  | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `params` | String |  | Input only. [Input Only] Additional params passed with the request, but not persisted
as part of resource payload. |
| `operational_status` | String |  | [Output Only] The current status of whether or not this interconnect
attachment is functional, which can take one of the following values:
   
   - OS_ACTIVE: The attachment has been turned up and is ready to
   use. 
   - OS_UNPROVISIONED: The attachment is not ready to use yet,
   because turnup is not complete. |
| `candidate_customer_router_ipv6_address` | String |  | Single IPv6 address + prefix length to be configured on the customer router
interface for this interconnect attachment. |
| `pairing_key` | String |  | [Output only for type PARTNER. Input only for PARTNER_PROVIDER. Not
present for DEDICATED].
The opaque identifier of a PARTNER attachment used to initiate
provisioning with a selected partner.
Of the form "XXXXX/region/domain" |
| `region` | String |  | [Output Only] URL of the region where the regional interconnect attachment
resides.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `stack_type` | String |  | The stack type for this interconnect attachment to identify whether the
IPv6 feature is enabled or not. If not specified, IPV4_ONLY
will be used.

This field can be both set at interconnect attachments creation and
update interconnect attachment operations. |
| `state` | String |  | [Output Only] The current state of this attachment's functionality.
Enum values ACTIVE and UNPROVISIONED are shared by DEDICATED/PRIVATE,
PARTNER, and PARTNER_PROVIDER interconnect attachments, while enum values
PENDING_PARTNER, PARTNER_REQUEST_RECEIVED, and PENDING_CUSTOMER are used
for only PARTNER and PARTNER_PROVIDER interconnect attachments.
This state can take one of the following values:
   
   - ACTIVE: The attachment has been turned up and is ready to use.
   - UNPROVISIONED: The attachment is not ready to use yet, because turnup
   is not complete.
   - PENDING_PARTNER: A newly-created PARTNER attachment that has not yet
   been configured on the Partner side.
   - PARTNER_REQUEST_RECEIVED: A PARTNER attachment is in the process of
   provisioning after a PARTNER_PROVIDER attachment was created that
   references it. 
   - PENDING_CUSTOMER: A PARTNER or PARTNER_PROVIDER
   attachment that is waiting for a customer to activate it. 
   - DEFUNCT:
   The attachment was deleted externally and is no longer functional. This
   could be because the associated Interconnect was removed, or because the
   other side of a Partner attachment was deleted. |
| `partner_asn` | String |  | Optional BGP ASN for the router supplied by a Layer 3 Partner if they
configured BGP on behalf of the customer.
Output only for PARTNER type, input only for PARTNER_PROVIDER, not
available for DEDICATED. |
| `configuration_constraints` | String |  | [Output Only] Constraints for this attachment, if any. The attachment does
not work if these constraints are not met. |
| `kind` | String |  | [Output Only] Type of the resource. Alwayscompute#interconnectAttachment for interconnect attachments. |
| `candidate_cloud_router_ipv6_address` | String |  | Single IPv6 address + prefix length to be configured on the cloud router
interface for this interconnect attachment.
   
   - Both candidate_cloud_router_ipv6_address and
   candidate_customer_router_ipv6_address fields must be set or both must be
   unset.
   - Prefix length of both candidate_cloud_router_ipv6_address and
   candidate_customer_router_ipv6_address must be the same.
   - Max prefix length is 126. |
| `partner_metadata` | String |  | Informational metadata about Partner attachments from Partners to display
to customers.
Output only for PARTNER type, mutable for PARTNER_PROVIDER, not
available for DEDICATED. |
| `type` | String |  | The type of interconnect attachment this is, which can take one of the
following values:
   
   - DEDICATED: an attachment to a Dedicated Interconnect.
   - PARTNER: an attachment to a Partner Interconnect, created by the
   customer.
   - PARTNER_PROVIDER: an attachment to a Partner Interconnect, created by
   the partner.

- L2_DEDICATED: a L2 attachment to a Dedicated Interconnect. |
| `google_reference_id` | String |  | [Output Only] Google reference ID, to be used when raising support tickets
with Google or otherwise to debug backend connectivity issues.
[Deprecated] This field is not used. |
| `cloud_router_ip_address` | String |  | [Output Only] IPv4 address + prefix length to be configured on Cloud Router
Interface for this interconnect attachment. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `customer_router_ipv6_interface_id` | String |  | This field is not available. |
| `encryption` | String |  | Indicates the user-supplied encryption option of this VLAN attachment
(interconnectAttachment). Can only be specified at attachment creation
for PARTNER or DEDICATED attachments.
Possible values are:
   
   - NONE - This is the default value, which means that the
   VLAN attachment carries unencrypted traffic. VMs are able to send
   traffic to, or receive traffic from, such a VLAN attachment.
   - IPSEC - The VLAN attachment carries only encrypted
   traffic that is encrypted by an IPsec device, such as an HA VPN gateway or
   third-party IPsec VPN. VMs cannot directly send traffic to, or receive
   traffic from, such a VLAN attachment. To use *HA VPN over Cloud
   Interconnect*, the VLAN attachment must be created with this
   option. |
| `bandwidth` | String |  | Provisioned bandwidth capacity for the interconnect attachment. For
attachments of type DEDICATED, the user can set the bandwidth.
For attachments of type PARTNER, the Google Partner that is operating
the interconnect must set the bandwidth.
Output only for PARTNER type, mutable for PARTNER_PROVIDER and DEDICATED,
and can take one of the following values:
   
   - BPS_50M: 50 Mbit/s
   - BPS_100M: 100 Mbit/s
   - BPS_200M: 200 Mbit/s
   - BPS_300M: 300 Mbit/s
   - BPS_400M: 400 Mbit/s
   - BPS_500M: 500 Mbit/s
   - BPS_1G: 1 Gbit/s
   - BPS_2G: 2 Gbit/s
   - BPS_5G: 5 Gbit/s
   - BPS_10G: 10 Gbit/s
   - BPS_20G: 20 Gbit/s
   - BPS_50G: 50 Gbit/s
   - BPS_100G: 100 Gbit/s |
| `customer_router_ipv6_address` | String |  | [Output Only] IPv6 address + prefix length to be configured on the
customer router subinterface for this interconnect attachment. |
| `candidate_cloud_router_ip_address` | String |  | Single IPv4 address + prefix length to be configured on the cloud router
interface for this interconnect attachment.
   
   - Both candidate_cloud_router_ip_address and
   candidate_customer_router_ip_address fields must be set or both must be
   unset.
   - Prefix length of both candidate_cloud_router_ip_address and
   candidate_customer_router_ip_address must be the same.
   - Max prefix length is 31. |
| `mtu` | i64 |  | Maximum Transmission Unit (MTU), in bytes, of packets passing through this
interconnect attachment.
Valid values are 1440, 1460, 1500, and 8896. If not specified,
the value will default to 1440. |
| `candidate_customer_router_ip_address` | String |  | Single IPv4 address + prefix length to be configured on the customer router
interface for this interconnect attachment. |
| `remote_service` | String |  | [Output Only]
If the attachment is on a Cross-Cloud Interconnect connection, this field
contains the interconnect's remote location service provider. Example
values: "Amazon Web Services" "Microsoft Azure".

The field is set only for attachments on Cross-Cloud Interconnect
connections. Its value is copied from the InterconnectRemoteLocation
remoteService field. |
| `vlan_tag8021q` | i64 |  | The IEEE 802.1Q VLAN tag for this attachment, in the range 2-4093.
Only specified at creation time. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `satisfies_pzs` | bool |  | [Output Only] Reserved for future use. |
| `admin_enabled` | bool |  | Determines whether this Attachment will carry packets.
Not present for PARTNER_PROVIDER. |
| `label_fingerprint` | String |  | A fingerprint for the labels being applied to this InterconnectAttachment,
which is essentially a hash of the labels set used for optimistic locking.
The fingerprint is initially generated by Compute Engine and changes after
every request to modify or update labels. You must always provide an
up-to-date fingerprint hash in order to update or change labels,
otherwise the request will fail with error412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve an InterconnectAttachment. |
| `description` | String |  | An optional description of this resource. |
| `router` | String |  | URL of the Cloud Router to be used for dynamic routing. This router must be
in the same region as this InterconnectAttachment. The
InterconnectAttachment will automatically connect the Interconnect to the
network & region within which the Cloud Router is configured. |
| `region` | String | ✅ | Name of the region for this request. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `cloud_router_ipv6_address` | String | [Output Only] IPv6 address + prefix length to be configured on Cloud
Router Interface for this interconnect attachment. |
| `subnet_length` | i64 | Input only. Length of the IPv4 subnet mask.
Allowed values:
   
   
    - 29 (default)
    - 30

The default value is 29, except for Cross-Cloud Interconnect
connections that use an InterconnectRemoteLocation with a
constraints.subnetLengthRange.min equal to 30. For example,
connections that use an Azure remote location fall into this
category. In these cases, the default value is 30, and requesting
29 returns an error.

Where both 29 and 30 are allowed, 29 is preferred, because it gives
Google Cloud Support more debugging visibility. |
| `cloud_router_ipv6_interface_id` | String | This field is not available. |
| `labels` | HashMap<String, String> | Labels for this resource. These can only be added or modified by thesetLabels method. Each label key/value pair must comply withRFC1035.
Label values may be empty. |
| `dataplane_version` | i64 | [Output Only] Dataplane version for this InterconnectAttachment. This
field is only present for Dataplane version 2 and higher. Absence of this
field in the API output indicates that the Dataplane is version 1. |
| `l2_forwarding` | String | L2 Interconnect Attachment related config. This field is required if the
type is L2_DEDICATED.

The configuration specifies how VLAN tags (like dot1q, qinq, or dot1ad)
within L2 packets are mapped to the destination appliances IP addresses.
The packet is then encapsulated with the appliance IP address and sent to
the edge appliance. |
| `private_interconnect_info` | String | [Output Only] Information specific to an InterconnectAttachment.
This property is populated if the interconnect that
this is attached to is of type DEDICATED. |
| `candidate_subnets` | Vec<String> | Input only. Up to 16 candidate prefixes that can be used to restrict the allocation
of cloudRouterIpAddress and customerRouterIpAddress for this attachment.
All prefixes must be within link-local address space (169.254.0.0/16) and
must be /29 or shorter (/28, /27, etc). Google will attempt to select an
unused /29 from the supplied candidate prefix(es). The request will fail if
all possible /29s are in use on Google's edge. If not supplied, Google will
randomly select an unused /29 from all of link-local space. |
| `candidate_ipv6_subnets` | Vec<String> | This field is not available. |
| `attachment_group` | String | [Output Only] URL of the AttachmentGroup that includes this Attachment. |
| `interconnect` | String | URL of the underlying Interconnect object that this attachment's traffic
will traverse through. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `customer_router_ip_address` | String | [Output Only] IPv4 address + prefix length to be configured on the customer
router subinterface for this interconnect attachment. |
| `edge_availability_domain` | String | Input only. Desired availability domain for the attachment. Only available for type
PARTNER, at creation time, and can take one of the following values:
   
   - AVAILABILITY_DOMAIN_ANY
   - AVAILABILITY_DOMAIN_1
   - AVAILABILITY_DOMAIN_2


For improved reliability, customers should configure a pair of attachments,
one per availability domain. The selected availability domain will be
provided to the Partner via the pairing key, so that the provisioned
circuit will lie in the specified domain. If not specified, the value will
default to AVAILABILITY_DOMAIN_ANY. |
| `ipsec_internal_addresses` | Vec<String> | A list of URLs of addresses that have been reserved for the VLAN
attachment. Used only for the VLAN attachment that has the encryption
option as IPSEC. The addresses must be regional internal IP address ranges.
When creating an HA VPN gateway over the VLAN attachment, if the attachment
is configured to use a regional internal IP address, then the VPN gateway's
IP address is allocated from the IP address range specified here. For
example, if the HA VPN gateway's interface 0 is paired to this VLAN
attachment, then a regional internal IP address for the VPN gateway
interface 0 will be allocated from the IP address specified for this
VLAN attachment.
If this field is not specified when creating the VLAN attachment, then
later on when creating an HA VPN gateway on this VLAN attachment, the HA
VPN gateway's IP address is allocated from the regional external IP address
pool. |
| `name` | String | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `params` | String | Input only. [Input Only] Additional params passed with the request, but not persisted
as part of resource payload. |
| `operational_status` | String | [Output Only] The current status of whether or not this interconnect
attachment is functional, which can take one of the following values:
   
   - OS_ACTIVE: The attachment has been turned up and is ready to
   use. 
   - OS_UNPROVISIONED: The attachment is not ready to use yet,
   because turnup is not complete. |
| `candidate_customer_router_ipv6_address` | String | Single IPv6 address + prefix length to be configured on the customer router
interface for this interconnect attachment. |
| `pairing_key` | String | [Output only for type PARTNER. Input only for PARTNER_PROVIDER. Not
present for DEDICATED].
The opaque identifier of a PARTNER attachment used to initiate
provisioning with a selected partner.
Of the form "XXXXX/region/domain" |
| `region` | String | [Output Only] URL of the region where the regional interconnect attachment
resides.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `stack_type` | String | The stack type for this interconnect attachment to identify whether the
IPv6 feature is enabled or not. If not specified, IPV4_ONLY
will be used.

This field can be both set at interconnect attachments creation and
update interconnect attachment operations. |
| `state` | String | [Output Only] The current state of this attachment's functionality.
Enum values ACTIVE and UNPROVISIONED are shared by DEDICATED/PRIVATE,
PARTNER, and PARTNER_PROVIDER interconnect attachments, while enum values
PENDING_PARTNER, PARTNER_REQUEST_RECEIVED, and PENDING_CUSTOMER are used
for only PARTNER and PARTNER_PROVIDER interconnect attachments.
This state can take one of the following values:
   
   - ACTIVE: The attachment has been turned up and is ready to use.
   - UNPROVISIONED: The attachment is not ready to use yet, because turnup
   is not complete.
   - PENDING_PARTNER: A newly-created PARTNER attachment that has not yet
   been configured on the Partner side.
   - PARTNER_REQUEST_RECEIVED: A PARTNER attachment is in the process of
   provisioning after a PARTNER_PROVIDER attachment was created that
   references it. 
   - PENDING_CUSTOMER: A PARTNER or PARTNER_PROVIDER
   attachment that is waiting for a customer to activate it. 
   - DEFUNCT:
   The attachment was deleted externally and is no longer functional. This
   could be because the associated Interconnect was removed, or because the
   other side of a Partner attachment was deleted. |
| `partner_asn` | String | Optional BGP ASN for the router supplied by a Layer 3 Partner if they
configured BGP on behalf of the customer.
Output only for PARTNER type, input only for PARTNER_PROVIDER, not
available for DEDICATED. |
| `configuration_constraints` | String | [Output Only] Constraints for this attachment, if any. The attachment does
not work if these constraints are not met. |
| `kind` | String | [Output Only] Type of the resource. Alwayscompute#interconnectAttachment for interconnect attachments. |
| `candidate_cloud_router_ipv6_address` | String | Single IPv6 address + prefix length to be configured on the cloud router
interface for this interconnect attachment.
   
   - Both candidate_cloud_router_ipv6_address and
   candidate_customer_router_ipv6_address fields must be set or both must be
   unset.
   - Prefix length of both candidate_cloud_router_ipv6_address and
   candidate_customer_router_ipv6_address must be the same.
   - Max prefix length is 126. |
| `partner_metadata` | String | Informational metadata about Partner attachments from Partners to display
to customers.
Output only for PARTNER type, mutable for PARTNER_PROVIDER, not
available for DEDICATED. |
| `type` | String | The type of interconnect attachment this is, which can take one of the
following values:
   
   - DEDICATED: an attachment to a Dedicated Interconnect.
   - PARTNER: an attachment to a Partner Interconnect, created by the
   customer.
   - PARTNER_PROVIDER: an attachment to a Partner Interconnect, created by
   the partner.

- L2_DEDICATED: a L2 attachment to a Dedicated Interconnect. |
| `google_reference_id` | String | [Output Only] Google reference ID, to be used when raising support tickets
with Google or otherwise to debug backend connectivity issues.
[Deprecated] This field is not used. |
| `cloud_router_ip_address` | String | [Output Only] IPv4 address + prefix length to be configured on Cloud Router
Interface for this interconnect attachment. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `customer_router_ipv6_interface_id` | String | This field is not available. |
| `encryption` | String | Indicates the user-supplied encryption option of this VLAN attachment
(interconnectAttachment). Can only be specified at attachment creation
for PARTNER or DEDICATED attachments.
Possible values are:
   
   - NONE - This is the default value, which means that the
   VLAN attachment carries unencrypted traffic. VMs are able to send
   traffic to, or receive traffic from, such a VLAN attachment.
   - IPSEC - The VLAN attachment carries only encrypted
   traffic that is encrypted by an IPsec device, such as an HA VPN gateway or
   third-party IPsec VPN. VMs cannot directly send traffic to, or receive
   traffic from, such a VLAN attachment. To use *HA VPN over Cloud
   Interconnect*, the VLAN attachment must be created with this
   option. |
| `bandwidth` | String | Provisioned bandwidth capacity for the interconnect attachment. For
attachments of type DEDICATED, the user can set the bandwidth.
For attachments of type PARTNER, the Google Partner that is operating
the interconnect must set the bandwidth.
Output only for PARTNER type, mutable for PARTNER_PROVIDER and DEDICATED,
and can take one of the following values:
   
   - BPS_50M: 50 Mbit/s
   - BPS_100M: 100 Mbit/s
   - BPS_200M: 200 Mbit/s
   - BPS_300M: 300 Mbit/s
   - BPS_400M: 400 Mbit/s
   - BPS_500M: 500 Mbit/s
   - BPS_1G: 1 Gbit/s
   - BPS_2G: 2 Gbit/s
   - BPS_5G: 5 Gbit/s
   - BPS_10G: 10 Gbit/s
   - BPS_20G: 20 Gbit/s
   - BPS_50G: 50 Gbit/s
   - BPS_100G: 100 Gbit/s |
| `customer_router_ipv6_address` | String | [Output Only] IPv6 address + prefix length to be configured on the
customer router subinterface for this interconnect attachment. |
| `candidate_cloud_router_ip_address` | String | Single IPv4 address + prefix length to be configured on the cloud router
interface for this interconnect attachment.
   
   - Both candidate_cloud_router_ip_address and
   candidate_customer_router_ip_address fields must be set or both must be
   unset.
   - Prefix length of both candidate_cloud_router_ip_address and
   candidate_customer_router_ip_address must be the same.
   - Max prefix length is 31. |
| `mtu` | i64 | Maximum Transmission Unit (MTU), in bytes, of packets passing through this
interconnect attachment.
Valid values are 1440, 1460, 1500, and 8896. If not specified,
the value will default to 1440. |
| `candidate_customer_router_ip_address` | String | Single IPv4 address + prefix length to be configured on the customer router
interface for this interconnect attachment. |
| `remote_service` | String | [Output Only]
If the attachment is on a Cross-Cloud Interconnect connection, this field
contains the interconnect's remote location service provider. Example
values: "Amazon Web Services" "Microsoft Azure".

The field is set only for attachments on Cross-Cloud Interconnect
connections. Its value is copied from the InterconnectRemoteLocation
remoteService field. |
| `vlan_tag8021q` | i64 | The IEEE 802.1Q VLAN tag for this attachment, in the range 2-4093.
Only specified at creation time. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `satisfies_pzs` | bool | [Output Only] Reserved for future use. |
| `admin_enabled` | bool | Determines whether this Attachment will carry packets.
Not present for PARTNER_PROVIDER. |
| `label_fingerprint` | String | A fingerprint for the labels being applied to this InterconnectAttachment,
which is essentially a hash of the labels set used for optimistic locking.
The fingerprint is initially generated by Compute Engine and changes after
every request to modify or update labels. You must always provide an
up-to-date fingerprint hash in order to update or change labels,
otherwise the request will fail with error412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve an InterconnectAttachment. |
| `description` | String | An optional description of this resource. |
| `router` | String | URL of the Cloud Router to be used for dynamic routing. This router must be
in the same region as this InterconnectAttachment. The
InterconnectAttachment will automatically connect the Interconnect to the
network & region within which the Cloud Router is configured. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create interconnect_attachment
interconnect_attachment = provider.compute_api.Interconnect_attachment {
    region = "value"  # Name of the region for this request.
    project = "value"  # Project ID for this request.
}

# Access interconnect_attachment outputs
interconnect_attachment_id = interconnect_attachment.id
interconnect_attachment_cloud_router_ipv6_address = interconnect_attachment.cloud_router_ipv6_address
interconnect_attachment_subnet_length = interconnect_attachment.subnet_length
interconnect_attachment_cloud_router_ipv6_interface_id = interconnect_attachment.cloud_router_ipv6_interface_id
interconnect_attachment_labels = interconnect_attachment.labels
interconnect_attachment_dataplane_version = interconnect_attachment.dataplane_version
interconnect_attachment_l2_forwarding = interconnect_attachment.l2_forwarding
interconnect_attachment_private_interconnect_info = interconnect_attachment.private_interconnect_info
interconnect_attachment_candidate_subnets = interconnect_attachment.candidate_subnets
interconnect_attachment_candidate_ipv6_subnets = interconnect_attachment.candidate_ipv6_subnets
interconnect_attachment_attachment_group = interconnect_attachment.attachment_group
interconnect_attachment_interconnect = interconnect_attachment.interconnect
interconnect_attachment_creation_timestamp = interconnect_attachment.creation_timestamp
interconnect_attachment_customer_router_ip_address = interconnect_attachment.customer_router_ip_address
interconnect_attachment_edge_availability_domain = interconnect_attachment.edge_availability_domain
interconnect_attachment_ipsec_internal_addresses = interconnect_attachment.ipsec_internal_addresses
interconnect_attachment_name = interconnect_attachment.name
interconnect_attachment_params = interconnect_attachment.params
interconnect_attachment_operational_status = interconnect_attachment.operational_status
interconnect_attachment_candidate_customer_router_ipv6_address = interconnect_attachment.candidate_customer_router_ipv6_address
interconnect_attachment_pairing_key = interconnect_attachment.pairing_key
interconnect_attachment_region = interconnect_attachment.region
interconnect_attachment_stack_type = interconnect_attachment.stack_type
interconnect_attachment_state = interconnect_attachment.state
interconnect_attachment_partner_asn = interconnect_attachment.partner_asn
interconnect_attachment_configuration_constraints = interconnect_attachment.configuration_constraints
interconnect_attachment_kind = interconnect_attachment.kind
interconnect_attachment_candidate_cloud_router_ipv6_address = interconnect_attachment.candidate_cloud_router_ipv6_address
interconnect_attachment_partner_metadata = interconnect_attachment.partner_metadata
interconnect_attachment_type = interconnect_attachment.type
interconnect_attachment_google_reference_id = interconnect_attachment.google_reference_id
interconnect_attachment_cloud_router_ip_address = interconnect_attachment.cloud_router_ip_address
interconnect_attachment_id = interconnect_attachment.id
interconnect_attachment_customer_router_ipv6_interface_id = interconnect_attachment.customer_router_ipv6_interface_id
interconnect_attachment_encryption = interconnect_attachment.encryption
interconnect_attachment_bandwidth = interconnect_attachment.bandwidth
interconnect_attachment_customer_router_ipv6_address = interconnect_attachment.customer_router_ipv6_address
interconnect_attachment_candidate_cloud_router_ip_address = interconnect_attachment.candidate_cloud_router_ip_address
interconnect_attachment_mtu = interconnect_attachment.mtu
interconnect_attachment_candidate_customer_router_ip_address = interconnect_attachment.candidate_customer_router_ip_address
interconnect_attachment_remote_service = interconnect_attachment.remote_service
interconnect_attachment_vlan_tag8021q = interconnect_attachment.vlan_tag8021q
interconnect_attachment_self_link = interconnect_attachment.self_link
interconnect_attachment_satisfies_pzs = interconnect_attachment.satisfies_pzs
interconnect_attachment_admin_enabled = interconnect_attachment.admin_enabled
interconnect_attachment_label_fingerprint = interconnect_attachment.label_fingerprint
interconnect_attachment_description = interconnect_attachment.description
interconnect_attachment_router = interconnect_attachment.router
```

---


### Global_network_endpoint_group

Creates a network endpoint group in the specified project using the
parameters that are included in the request.

**Operations**: ✅ Create ✅ Read ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `psc_target_service` | String |  | The target service url used to set up private service connection to
a Google API or a PSC Producer Service Attachment.
An example value is: asia-northeast3-cloudkms.googleapis.com.

Optional. Only valid when networkEndpointType isPRIVATE_SERVICE_CONNECT. |
| `serverless_deployment` | String |  | Only valid when networkEndpointType isSERVERLESS. Only one of cloudRun,appEngine, cloudFunction orserverlessDeployment may be set. |
| `kind` | String |  | [Output Only] Type of the resource. Alwayscompute#networkEndpointGroup for network endpoint group. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `app_engine` | String |  | Optional. Only valid when networkEndpointType isSERVERLESS. Only one of cloudRun,appEngine or cloudFunction may be set. |
| `zone` | String |  | [Output Only] The URL of thezone
where the network endpoint group is located. |
| `cloud_function` | String |  | Optional. Only valid when networkEndpointType isSERVERLESS. Only one of cloudRun,appEngine or cloudFunction may be set. |
| `network_endpoint_type` | String |  | Type of network endpoints in this network endpoint group. Can be one ofGCE_VM_IP, GCE_VM_IP_PORT,NON_GCP_PRIVATE_IP_PORT, INTERNET_FQDN_PORT,INTERNET_IP_PORT, SERVERLESS,PRIVATE_SERVICE_CONNECT, GCE_VM_IP_PORTMAP. |
| `load_balancer` | String |  | This field is only valid when the network endpoint group is used for load
balancing.
[Deprecated] This field is deprecated. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `name` | String |  | Name of the resource; provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must be
a dash, lowercase letter, or digit, except the last character, which cannot
be a dash. |
| `psc_data` | String |  | Optional. Only valid when networkEndpointType isPRIVATE_SERVICE_CONNECT. |
| `cloud_run` | String |  | Optional. Only valid when networkEndpointType isSERVERLESS. Only one of cloudRun,appEngine or cloudFunction may be set. |
| `region` | String |  | [Output Only] The URL of theregion
where the network endpoint group is located. |
| `network` | String |  | The URL of the network to which all network endpoints in the NEG belong.
Uses default project network if unspecified. |
| `annotations` | HashMap<String, String> |  | Optional. Metadata defined as annotations on the network endpoint group. |
| `default_port` | i64 |  | The default port used if the port number is not specified in the network
endpoint.

Optional. If the network endpoint type is either GCE_VM_IP,SERVERLESS or PRIVATE_SERVICE_CONNECT, this
field must not be specified. |
| `size` | i64 |  | [Output only] Number of network endpoints in the network endpoint group. |
| `subnetwork` | String |  | Optional URL of the subnetwork to which all network endpoints in the NEG
belong. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `psc_target_service` | String | The target service url used to set up private service connection to
a Google API or a PSC Producer Service Attachment.
An example value is: asia-northeast3-cloudkms.googleapis.com.

Optional. Only valid when networkEndpointType isPRIVATE_SERVICE_CONNECT. |
| `serverless_deployment` | String | Only valid when networkEndpointType isSERVERLESS. Only one of cloudRun,appEngine, cloudFunction orserverlessDeployment may be set. |
| `kind` | String | [Output Only] Type of the resource. Alwayscompute#networkEndpointGroup for network endpoint group. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `app_engine` | String | Optional. Only valid when networkEndpointType isSERVERLESS. Only one of cloudRun,appEngine or cloudFunction may be set. |
| `zone` | String | [Output Only] The URL of thezone
where the network endpoint group is located. |
| `cloud_function` | String | Optional. Only valid when networkEndpointType isSERVERLESS. Only one of cloudRun,appEngine or cloudFunction may be set. |
| `network_endpoint_type` | String | Type of network endpoints in this network endpoint group. Can be one ofGCE_VM_IP, GCE_VM_IP_PORT,NON_GCP_PRIVATE_IP_PORT, INTERNET_FQDN_PORT,INTERNET_IP_PORT, SERVERLESS,PRIVATE_SERVICE_CONNECT, GCE_VM_IP_PORTMAP. |
| `load_balancer` | String | This field is only valid when the network endpoint group is used for load
balancing.
[Deprecated] This field is deprecated. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `name` | String | Name of the resource; provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must be
a dash, lowercase letter, or digit, except the last character, which cannot
be a dash. |
| `psc_data` | String | Optional. Only valid when networkEndpointType isPRIVATE_SERVICE_CONNECT. |
| `cloud_run` | String | Optional. Only valid when networkEndpointType isSERVERLESS. Only one of cloudRun,appEngine or cloudFunction may be set. |
| `region` | String | [Output Only] The URL of theregion
where the network endpoint group is located. |
| `network` | String | The URL of the network to which all network endpoints in the NEG belong.
Uses default project network if unspecified. |
| `annotations` | HashMap<String, String> | Optional. Metadata defined as annotations on the network endpoint group. |
| `default_port` | i64 | The default port used if the port number is not specified in the network
endpoint.

Optional. If the network endpoint type is either GCE_VM_IP,SERVERLESS or PRIVATE_SERVICE_CONNECT, this
field must not be specified. |
| `size` | i64 | [Output only] Number of network endpoints in the network endpoint group. |
| `subnetwork` | String | Optional URL of the subnetwork to which all network endpoints in the NEG
belong. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create global_network_endpoint_group
global_network_endpoint_group = provider.compute_api.Global_network_endpoint_group {
    project = "value"  # Project ID for this request.
}

# Access global_network_endpoint_group outputs
global_network_endpoint_group_id = global_network_endpoint_group.id
global_network_endpoint_group_psc_target_service = global_network_endpoint_group.psc_target_service
global_network_endpoint_group_serverless_deployment = global_network_endpoint_group.serverless_deployment
global_network_endpoint_group_kind = global_network_endpoint_group.kind
global_network_endpoint_group_creation_timestamp = global_network_endpoint_group.creation_timestamp
global_network_endpoint_group_description = global_network_endpoint_group.description
global_network_endpoint_group_app_engine = global_network_endpoint_group.app_engine
global_network_endpoint_group_zone = global_network_endpoint_group.zone
global_network_endpoint_group_cloud_function = global_network_endpoint_group.cloud_function
global_network_endpoint_group_network_endpoint_type = global_network_endpoint_group.network_endpoint_type
global_network_endpoint_group_load_balancer = global_network_endpoint_group.load_balancer
global_network_endpoint_group_id = global_network_endpoint_group.id
global_network_endpoint_group_name = global_network_endpoint_group.name
global_network_endpoint_group_psc_data = global_network_endpoint_group.psc_data
global_network_endpoint_group_cloud_run = global_network_endpoint_group.cloud_run
global_network_endpoint_group_region = global_network_endpoint_group.region
global_network_endpoint_group_network = global_network_endpoint_group.network
global_network_endpoint_group_annotations = global_network_endpoint_group.annotations
global_network_endpoint_group_default_port = global_network_endpoint_group.default_port
global_network_endpoint_group_size = global_network_endpoint_group.size
global_network_endpoint_group_subnetwork = global_network_endpoint_group.subnetwork
global_network_endpoint_group_self_link = global_network_endpoint_group.self_link
```

---


### Global_public_delegated_prefixe

Creates a global PublicDelegatedPrefix in the specified project using the
parameters that are included in the request.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `byoip_api_version` | String |  | [Output Only] The version of BYOIP API. |
| `allocatable_prefix_length` | i64 |  | The allocatable prefix length supported by this public delegated prefix.
This field is optional and cannot be set for prefixes in DELEGATION mode.
It cannot be set for IPv4 prefixes either, and it always defaults to 32. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `ipv6_access_type` | String |  | [Output Only] The internet access type for IPv6 Public Delegated Prefixes.
Inherited from parent prefix. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `enable_enhanced_ipv4_allocation` | bool |  | [Output Only] Whether this PDP supports enhanced IPv4 allocations.
Applicable for IPv4 PDPs only. |
| `mode` | String |  | The public delegated prefix mode for IPv6 only. |
| `public_delegated_sub_prefixs` | Vec<String> |  | The list of sub public delegated prefixes that exist for this public
delegated prefix. |
| `name` | String |  | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `status` | String |  | [Output Only] The status of the public delegated prefix, which can be one
of following values:
   
   
     - `INITIALIZING` The public delegated prefix is being initialized and
     addresses cannot be created yet.
     - `READY_TO_ANNOUNCE` The public delegated prefix is a live migration
     prefix and is active.
     - `ANNOUNCED` The public delegated prefix is announced and ready to
     use.
     - `DELETING` The public delegated prefix is being deprovsioned.
     - `ACTIVE` The public delegated prefix is ready to use. |
| `is_live_migration` | bool |  | If true, the prefix will be live migrated. |
| `region` | String |  | [Output Only] URL of the region where the public delegated prefix resides.
This field applies only to the region resource. You must specify this
field as part of the HTTP request URL. It is not settable as a field in
the request body. |
| `kind` | String |  | [Output Only] Type of the resource. Alwayscompute#publicDelegatedPrefix for public delegated prefixes. |
| `ip_cidr_range` | String |  | The IP address range, in CIDR format, represented by this public
delegated prefix. |
| `fingerprint` | String |  | Fingerprint of this resource. A hash of the contents stored in this object.
This field is used in optimistic locking. This field will be ignored when
inserting a new PublicDelegatedPrefix. An up-to-date
fingerprint must be provided in order to update thePublicDelegatedPrefix, otherwise the request will fail with
error 412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve a PublicDelegatedPrefix. |
| `parent_prefix` | String |  | The URL of parent prefix. Either PublicAdvertisedPrefix or
PublicDelegatedPrefix. |
| `id` | String |  | [Output Only] The unique identifier for the resource type. The server
generates this identifier. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `byoip_api_version` | String | [Output Only] The version of BYOIP API. |
| `allocatable_prefix_length` | i64 | The allocatable prefix length supported by this public delegated prefix.
This field is optional and cannot be set for prefixes in DELEGATION mode.
It cannot be set for IPv4 prefixes either, and it always defaults to 32. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `ipv6_access_type` | String | [Output Only] The internet access type for IPv6 Public Delegated Prefixes.
Inherited from parent prefix. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `enable_enhanced_ipv4_allocation` | bool | [Output Only] Whether this PDP supports enhanced IPv4 allocations.
Applicable for IPv4 PDPs only. |
| `mode` | String | The public delegated prefix mode for IPv6 only. |
| `public_delegated_sub_prefixs` | Vec<String> | The list of sub public delegated prefixes that exist for this public
delegated prefix. |
| `name` | String | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `status` | String | [Output Only] The status of the public delegated prefix, which can be one
of following values:
   
   
     - `INITIALIZING` The public delegated prefix is being initialized and
     addresses cannot be created yet.
     - `READY_TO_ANNOUNCE` The public delegated prefix is a live migration
     prefix and is active.
     - `ANNOUNCED` The public delegated prefix is announced and ready to
     use.
     - `DELETING` The public delegated prefix is being deprovsioned.
     - `ACTIVE` The public delegated prefix is ready to use. |
| `is_live_migration` | bool | If true, the prefix will be live migrated. |
| `region` | String | [Output Only] URL of the region where the public delegated prefix resides.
This field applies only to the region resource. You must specify this
field as part of the HTTP request URL. It is not settable as a field in
the request body. |
| `kind` | String | [Output Only] Type of the resource. Alwayscompute#publicDelegatedPrefix for public delegated prefixes. |
| `ip_cidr_range` | String | The IP address range, in CIDR format, represented by this public
delegated prefix. |
| `fingerprint` | String | Fingerprint of this resource. A hash of the contents stored in this object.
This field is used in optimistic locking. This field will be ignored when
inserting a new PublicDelegatedPrefix. An up-to-date
fingerprint must be provided in order to update thePublicDelegatedPrefix, otherwise the request will fail with
error 412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve a PublicDelegatedPrefix. |
| `parent_prefix` | String | The URL of parent prefix. Either PublicAdvertisedPrefix or
PublicDelegatedPrefix. |
| `id` | String | [Output Only] The unique identifier for the resource type. The server
generates this identifier. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create global_public_delegated_prefixe
global_public_delegated_prefixe = provider.compute_api.Global_public_delegated_prefixe {
    project = "value"  # Project ID for this request.
}

# Access global_public_delegated_prefixe outputs
global_public_delegated_prefixe_id = global_public_delegated_prefixe.id
global_public_delegated_prefixe_byoip_api_version = global_public_delegated_prefixe.byoip_api_version
global_public_delegated_prefixe_allocatable_prefix_length = global_public_delegated_prefixe.allocatable_prefix_length
global_public_delegated_prefixe_description = global_public_delegated_prefixe.description
global_public_delegated_prefixe_ipv6_access_type = global_public_delegated_prefixe.ipv6_access_type
global_public_delegated_prefixe_creation_timestamp = global_public_delegated_prefixe.creation_timestamp
global_public_delegated_prefixe_enable_enhanced_ipv4_allocation = global_public_delegated_prefixe.enable_enhanced_ipv4_allocation
global_public_delegated_prefixe_mode = global_public_delegated_prefixe.mode
global_public_delegated_prefixe_public_delegated_sub_prefixs = global_public_delegated_prefixe.public_delegated_sub_prefixs
global_public_delegated_prefixe_name = global_public_delegated_prefixe.name
global_public_delegated_prefixe_self_link = global_public_delegated_prefixe.self_link
global_public_delegated_prefixe_status = global_public_delegated_prefixe.status
global_public_delegated_prefixe_is_live_migration = global_public_delegated_prefixe.is_live_migration
global_public_delegated_prefixe_region = global_public_delegated_prefixe.region
global_public_delegated_prefixe_kind = global_public_delegated_prefixe.kind
global_public_delegated_prefixe_ip_cidr_range = global_public_delegated_prefixe.ip_cidr_range
global_public_delegated_prefixe_fingerprint = global_public_delegated_prefixe.fingerprint
global_public_delegated_prefixe_parent_prefix = global_public_delegated_prefixe.parent_prefix
global_public_delegated_prefixe_id = global_public_delegated_prefixe.id
```

---


### Target_vpn_gateway

Creates a target VPN gateway in the specified project and region using
the data included in the request.

**Operations**: ✅ Create ✅ Read ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `forwarding_rules` | Vec<String> |  | [Output Only] A list of URLs to the ForwardingRule resources.
ForwardingRules are created usingcompute.forwardingRules.insert and associated with a VPN
gateway. |
| `label_fingerprint` | String |  | A fingerprint for the labels being applied to this TargetVpnGateway, which
is essentially a hash of the labels set used for optimistic locking. The
fingerprint is initially generated by Compute Engine and changes after
every request to modify or update labels. You must always provide an
up-to-date fingerprint hash in order to update or change labels,
otherwise the request will fail with error412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve a TargetVpnGateway. |
| `name` | String |  | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `labels` | HashMap<String, String> |  | Labels for this resource. These can only be added or modified by thesetLabels method. Each label key/value pair must comply withRFC1035.
Label values may be empty. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `tunnels` | Vec<String> |  | [Output Only] A list of URLs to VpnTunnel resources. VpnTunnels are
created using the compute.vpntunnels.insert method and
associated with a VPN gateway. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `kind` | String |  | [Output Only] Type of resource. Alwayscompute#targetVpnGateway for target VPN gateways. |
| `network` | String |  | URL of the network to which this VPN gateway is attached. Provided by the
client when the VPN gateway is created. |
| `status` | String |  | [Output Only] The status of the VPN gateway,
which can be one of the following: CREATING, READY, FAILED, or DELETING. |
| `region` | String |  | [Output Only] URL of the region where the target VPN gateway resides.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `region` | String | ✅ | Name of the region for this request. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `forwarding_rules` | Vec<String> | [Output Only] A list of URLs to the ForwardingRule resources.
ForwardingRules are created usingcompute.forwardingRules.insert and associated with a VPN
gateway. |
| `label_fingerprint` | String | A fingerprint for the labels being applied to this TargetVpnGateway, which
is essentially a hash of the labels set used for optimistic locking. The
fingerprint is initially generated by Compute Engine and changes after
every request to modify or update labels. You must always provide an
up-to-date fingerprint hash in order to update or change labels,
otherwise the request will fail with error412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve a TargetVpnGateway. |
| `name` | String | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `labels` | HashMap<String, String> | Labels for this resource. These can only be added or modified by thesetLabels method. Each label key/value pair must comply withRFC1035.
Label values may be empty. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `tunnels` | Vec<String> | [Output Only] A list of URLs to VpnTunnel resources. VpnTunnels are
created using the compute.vpntunnels.insert method and
associated with a VPN gateway. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `kind` | String | [Output Only] Type of resource. Alwayscompute#targetVpnGateway for target VPN gateways. |
| `network` | String | URL of the network to which this VPN gateway is attached. Provided by the
client when the VPN gateway is created. |
| `status` | String | [Output Only] The status of the VPN gateway,
which can be one of the following: CREATING, READY, FAILED, or DELETING. |
| `region` | String | [Output Only] URL of the region where the target VPN gateway resides.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create target_vpn_gateway
target_vpn_gateway = provider.compute_api.Target_vpn_gateway {
    region = "value"  # Name of the region for this request.
    project = "value"  # Project ID for this request.
}

# Access target_vpn_gateway outputs
target_vpn_gateway_id = target_vpn_gateway.id
target_vpn_gateway_creation_timestamp = target_vpn_gateway.creation_timestamp
target_vpn_gateway_forwarding_rules = target_vpn_gateway.forwarding_rules
target_vpn_gateway_label_fingerprint = target_vpn_gateway.label_fingerprint
target_vpn_gateway_name = target_vpn_gateway.name
target_vpn_gateway_labels = target_vpn_gateway.labels
target_vpn_gateway_self_link = target_vpn_gateway.self_link
target_vpn_gateway_id = target_vpn_gateway.id
target_vpn_gateway_tunnels = target_vpn_gateway.tunnels
target_vpn_gateway_description = target_vpn_gateway.description
target_vpn_gateway_kind = target_vpn_gateway.kind
target_vpn_gateway_network = target_vpn_gateway.network
target_vpn_gateway_status = target_vpn_gateway.status
target_vpn_gateway_region = target_vpn_gateway.region
```

---


### Router

Creates a Router resource in the specified project and region using
the data included in the request.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `params` | String |  | Input only. [Input Only] Additional params passed with the request, but not persisted
as part of resource payload. |
| `name` | String |  | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `md5_authentication_keys` | Vec<String> |  | Keys used for MD5 authentication. |
| `kind` | String |  | [Output Only] Type of resource. Always compute#router for
routers. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `ncc_gateway` | String |  | URI of the ncc_gateway to which this router associated. |
| `interfaces` | Vec<String> |  | Router interfaces.
To create a BGP peer that uses a router interface,
the interface must have one of the following fields specified:
   
   - linkedVpnTunnel
   - linkedInterconnectAttachment
   - subnetwork


You can create a router interface without any of these fields specified.
However, you cannot create a BGP peer that uses that interface. |
| `bgp_peers` | Vec<String> |  | BGP information that must be configured into the routing stack to
establish BGP peering. This information must specify the peer ASN and
either the interface name, IP address, or peer IP address. Please refer toRFC4273. |
| `network` | String |  | URI of the network to which this router belongs. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `bgp` | String |  | BGP information specific to this router. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `encrypted_interconnect_router` | bool |  | Indicates if a router is dedicated for use with encrypted VLAN
attachments (interconnectAttachments). |
| `nats` | Vec<String> |  | A list of NAT services created in this router. |
| `region` | String |  | [Output Only] URI of the region where the router resides.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `project` | String | ✅ | Project ID for this request. |
| `region` | String | ✅ | Name of the region for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `params` | String | Input only. [Input Only] Additional params passed with the request, but not persisted
as part of resource payload. |
| `name` | String | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `md5_authentication_keys` | Vec<String> | Keys used for MD5 authentication. |
| `kind` | String | [Output Only] Type of resource. Always compute#router for
routers. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `ncc_gateway` | String | URI of the ncc_gateway to which this router associated. |
| `interfaces` | Vec<String> | Router interfaces.
To create a BGP peer that uses a router interface,
the interface must have one of the following fields specified:
   
   - linkedVpnTunnel
   - linkedInterconnectAttachment
   - subnetwork


You can create a router interface without any of these fields specified.
However, you cannot create a BGP peer that uses that interface. |
| `bgp_peers` | Vec<String> | BGP information that must be configured into the routing stack to
establish BGP peering. This information must specify the peer ASN and
either the interface name, IP address, or peer IP address. Please refer toRFC4273. |
| `network` | String | URI of the network to which this router belongs. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `bgp` | String | BGP information specific to this router. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `encrypted_interconnect_router` | bool | Indicates if a router is dedicated for use with encrypted VLAN
attachments (interconnectAttachments). |
| `nats` | Vec<String> | A list of NAT services created in this router. |
| `region` | String | [Output Only] URI of the region where the router resides.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create router
router = provider.compute_api.Router {
    project = "value"  # Project ID for this request.
    region = "value"  # Name of the region for this request.
}

# Access router outputs
router_id = router.id
router_params = router.params
router_name = router.name
router_self_link = router.self_link
router_md5_authentication_keys = router.md5_authentication_keys
router_kind = router.kind
router_id = router.id
router_ncc_gateway = router.ncc_gateway
router_interfaces = router.interfaces
router_bgp_peers = router.bgp_peers
router_network = router.network
router_description = router.description
router_bgp = router.bgp
router_creation_timestamp = router.creation_timestamp
router_encrypted_interconnect_router = router.encrypted_interconnect_router
router_nats = router.nats
router_region = router.region
```

---


### License_code

Returns permissions that a caller has on the specified resource.
 *Caution* This resource is intended
for use only by third-party partners who are creatingCloud Marketplace
images.

**Operations**: ✅ Create ✅ Read

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `permissions` | Vec<String> |  | The set of permissions to check for the 'resource'. Permissions with
wildcards (such as '*' or 'storage.*') are not allowed. |
| `resource` | String | ✅ | Name or id of the resource for this request. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `transferable` | bool | [Output Only] If true, the license will remain attached when creating
images or snapshots from disks. Otherwise, the license is not transferred. |
| `description` | String | [Output Only] Description of this License Code. |
| `name` | String | [Output Only] Name of the resource. The name is 1-20 characters long and
must be a valid 64 bit integer. |
| `kind` | String | [Output Only] Type of resource. Always compute#licenseCode for
licenses. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `license_alias` | Vec<String> | [Output Only] URL and description aliases of Licenses with the same
License Code. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `state` | String | [Output Only] Current state of this License Code. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create license_code
license_code = provider.compute_api.License_code {
    resource = "value"  # Name or id of the resource for this request.
    project = "value"  # Project ID for this request.
}

# Access license_code outputs
license_code_id = license_code.id
license_code_transferable = license_code.transferable
license_code_description = license_code.description
license_code_name = license_code.name
license_code_kind = license_code.kind
license_code_creation_timestamp = license_code.creation_timestamp
license_code_license_alias = license_code.license_alias
license_code_self_link = license_code.self_link
license_code_id = license_code.id
license_code_state = license_code.state
```

---


### Security_policie

Creates a new policy in the specified project using the data included in
the request.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `kind` | String |  | [Output only] Type of the resource. Alwayscompute#securityPolicyfor security policies |
| `region` | String |  | [Output Only] URL of the region where the regional security policy
resides. This field is not applicable to global security policies. |
| `associations` | Vec<String> |  | A list of associations that belong to this policy. |
| `rules` | Vec<String> |  | A list of rules that belong to this policy.
There must always be a default rule which is a rule with priority
2147483647 and match all condition (for the match condition this means
match  "*" for srcIpRanges and for the networkMatch condition every field
must be either match "*" or not set). If no rules are provided when
creating a security policy, a default rule with action "allow" will be
added. |
| `adaptive_protection_config` | String |  |  |
| `user_defined_fields` | Vec<String> |  | Definitions of user-defined fields for CLOUD_ARMOR_NETWORK policies. A
user-defined field consists of up to 4 bytes extracted from a fixed offset
in the packet, relative to the IPv4, IPv6, TCP, or UDP header, with an
optional mask to select certain bits. Rules may then specify matching
values for these fields.

Example:

 userDefinedFields:
 - name: "ipv4_fragment_offset"
   base: IPV4
   offset: 6
   size: 2
   mask: "0x1fff" |
| `name` | String |  | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `recaptcha_options_config` | String |  |  |
| `advanced_options_config` | String |  |  |
| `short_name` | String |  | User-provided name of the organization security policy. The name should be
unique in the organization in which the security policy is created. This
should only be used when SecurityPolicyType is CLOUD_ARMOR.
The name must be 1-63 characters long, and comply with
https://www.ietf.org/rfc/rfc1035.txt. Specifically, the name must be 1-63
characters long and match the regular expression
`[a-z]([-a-z0-9]*[a-z0-9])?` which means the first character must be a
lowercase letter, and all following characters must be a dash, lowercase
letter, or digit, except the last character, which cannot be a dash. |
| `parent` | String |  | [Output Only] The parent of the security policy. |
| `fingerprint` | String |  | Specifies a fingerprint for this resource, which is essentially a hash of
the metadata's contents and used for optimistic locking. The
fingerprint is initially generated by Compute Engine and changes after
every request to modify or update metadata. You must always provide an
up-to-date fingerprint hash in order to update or change metadata,
otherwise the request will fail with error412 conditionNotMet.

To see the latest fingerprint, make get() request to the
security policy. |
| `ddos_protection_config` | String |  |  |
| `label_fingerprint` | String |  | A fingerprint for the labels being applied to this security policy, which
is essentially a hash of the labels set used for optimistic locking. The
fingerprint is initially generated by Compute Engine and changes after
every request to modify or update labels. You must always provide an
up-to-date fingerprint hash in order to update or change labels.

To see the latest fingerprint, make get() request to the
security policy. |
| `display_name` | String |  | User-provided name of the organization security policy. The name should be
unique in the organization in which the security policy is created. This
should only be used when SecurityPolicyType is FIREWALL.
The name must be 1-63 characters long, and comply with
https://www.ietf.org/rfc/rfc1035.txt. Specifically, the name must be 1-63
characters long and match the regular expression
`[a-z]([-a-z0-9]*[a-z0-9])?` which means the first character must be a
lowercase letter, and all following characters must be a dash, lowercase
letter, or digit, except the last character, which cannot be a dash. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `labels` | HashMap<String, String> |  | Labels for this resource. These can only be added or modified by thesetLabels method. Each label key/value pair must comply withRFC1035.
Label values may be empty. |
| `rule_tuple_count` | i64 |  | [Output Only] Total count of all security policy rule tuples. A security
policy can not exceed a set number of tuples. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `self_link_with_id` | String |  | [Output Only] Server-defined URL for this resource with the resource id. |
| `type` | String |  | The type indicates the intended use of the security policy.
   
   - CLOUD_ARMOR: Cloud Armor backend security policies can
   be configured to filter incoming HTTP requests targeting backend services.
   They filter requests before they hit the origin servers.
   - CLOUD_ARMOR_EDGE: Cloud Armor edge security policies can
   be configured to filter incoming HTTP requests targeting backend services
   (including Cloud CDN-enabled) as well as backend buckets (Cloud Storage).
   They filter requests before the request is served from Google's cache.
   - CLOUD_ARMOR_INTERNAL_SERVICE (preview only): Cloud Armor
   internal service policies can be configured to filter HTTP requests
   targeting services managed by Traffic Director in a service mesh. They
   filter requests before the request is served from the application.

- CLOUD_ARMOR_NETWORK: Cloud Armor network policies
can be configured to filter packets targeting network load balancing
resources such as backend services, target pools, target instances, and
instances with external IPs. They filter requests before the request is
served from the application.


This field can be set only at resource creation time. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `kind` | String | [Output only] Type of the resource. Alwayscompute#securityPolicyfor security policies |
| `region` | String | [Output Only] URL of the region where the regional security policy
resides. This field is not applicable to global security policies. |
| `associations` | Vec<String> | A list of associations that belong to this policy. |
| `rules` | Vec<String> | A list of rules that belong to this policy.
There must always be a default rule which is a rule with priority
2147483647 and match all condition (for the match condition this means
match  "*" for srcIpRanges and for the networkMatch condition every field
must be either match "*" or not set). If no rules are provided when
creating a security policy, a default rule with action "allow" will be
added. |
| `adaptive_protection_config` | String |  |
| `user_defined_fields` | Vec<String> | Definitions of user-defined fields for CLOUD_ARMOR_NETWORK policies. A
user-defined field consists of up to 4 bytes extracted from a fixed offset
in the packet, relative to the IPv4, IPv6, TCP, or UDP header, with an
optional mask to select certain bits. Rules may then specify matching
values for these fields.

Example:

 userDefinedFields:
 - name: "ipv4_fragment_offset"
   base: IPV4
   offset: 6
   size: 2
   mask: "0x1fff" |
| `name` | String | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `recaptcha_options_config` | String |  |
| `advanced_options_config` | String |  |
| `short_name` | String | User-provided name of the organization security policy. The name should be
unique in the organization in which the security policy is created. This
should only be used when SecurityPolicyType is CLOUD_ARMOR.
The name must be 1-63 characters long, and comply with
https://www.ietf.org/rfc/rfc1035.txt. Specifically, the name must be 1-63
characters long and match the regular expression
`[a-z]([-a-z0-9]*[a-z0-9])?` which means the first character must be a
lowercase letter, and all following characters must be a dash, lowercase
letter, or digit, except the last character, which cannot be a dash. |
| `parent` | String | [Output Only] The parent of the security policy. |
| `fingerprint` | String | Specifies a fingerprint for this resource, which is essentially a hash of
the metadata's contents and used for optimistic locking. The
fingerprint is initially generated by Compute Engine and changes after
every request to modify or update metadata. You must always provide an
up-to-date fingerprint hash in order to update or change metadata,
otherwise the request will fail with error412 conditionNotMet.

To see the latest fingerprint, make get() request to the
security policy. |
| `ddos_protection_config` | String |  |
| `label_fingerprint` | String | A fingerprint for the labels being applied to this security policy, which
is essentially a hash of the labels set used for optimistic locking. The
fingerprint is initially generated by Compute Engine and changes after
every request to modify or update labels. You must always provide an
up-to-date fingerprint hash in order to update or change labels.

To see the latest fingerprint, make get() request to the
security policy. |
| `display_name` | String | User-provided name of the organization security policy. The name should be
unique in the organization in which the security policy is created. This
should only be used when SecurityPolicyType is FIREWALL.
The name must be 1-63 characters long, and comply with
https://www.ietf.org/rfc/rfc1035.txt. Specifically, the name must be 1-63
characters long and match the regular expression
`[a-z]([-a-z0-9]*[a-z0-9])?` which means the first character must be a
lowercase letter, and all following characters must be a dash, lowercase
letter, or digit, except the last character, which cannot be a dash. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `labels` | HashMap<String, String> | Labels for this resource. These can only be added or modified by thesetLabels method. Each label key/value pair must comply withRFC1035.
Label values may be empty. |
| `rule_tuple_count` | i64 | [Output Only] Total count of all security policy rule tuples. A security
policy can not exceed a set number of tuples. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `self_link_with_id` | String | [Output Only] Server-defined URL for this resource with the resource id. |
| `type` | String | The type indicates the intended use of the security policy.
   
   - CLOUD_ARMOR: Cloud Armor backend security policies can
   be configured to filter incoming HTTP requests targeting backend services.
   They filter requests before they hit the origin servers.
   - CLOUD_ARMOR_EDGE: Cloud Armor edge security policies can
   be configured to filter incoming HTTP requests targeting backend services
   (including Cloud CDN-enabled) as well as backend buckets (Cloud Storage).
   They filter requests before the request is served from Google's cache.
   - CLOUD_ARMOR_INTERNAL_SERVICE (preview only): Cloud Armor
   internal service policies can be configured to filter HTTP requests
   targeting services managed by Traffic Director in a service mesh. They
   filter requests before the request is served from the application.

- CLOUD_ARMOR_NETWORK: Cloud Armor network policies
can be configured to filter packets targeting network load balancing
resources such as backend services, target pools, target instances, and
instances with external IPs. They filter requests before the request is
served from the application.


This field can be set only at resource creation time. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create security_policie
security_policie = provider.compute_api.Security_policie {
    project = "value"  # Project ID for this request.
}

# Access security_policie outputs
security_policie_id = security_policie.id
security_policie_kind = security_policie.kind
security_policie_region = security_policie.region
security_policie_associations = security_policie.associations
security_policie_rules = security_policie.rules
security_policie_adaptive_protection_config = security_policie.adaptive_protection_config
security_policie_user_defined_fields = security_policie.user_defined_fields
security_policie_name = security_policie.name
security_policie_recaptcha_options_config = security_policie.recaptcha_options_config
security_policie_advanced_options_config = security_policie.advanced_options_config
security_policie_short_name = security_policie.short_name
security_policie_parent = security_policie.parent
security_policie_fingerprint = security_policie.fingerprint
security_policie_ddos_protection_config = security_policie.ddos_protection_config
security_policie_label_fingerprint = security_policie.label_fingerprint
security_policie_display_name = security_policie.display_name
security_policie_creation_timestamp = security_policie.creation_timestamp
security_policie_description = security_policie.description
security_policie_labels = security_policie.labels
security_policie_rule_tuple_count = security_policie.rule_tuple_count
security_policie_id = security_policie.id
security_policie_self_link = security_policie.self_link
security_policie_self_link_with_id = security_policie.self_link_with_id
security_policie_type = security_policie.type
```

---


### Region_backend_bucket

Creates a RegionBackendBucket in the specified project in the given scope
using the parameters that are included in the request.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `edge_security_policy` | String |  | [Output Only] The resource URL for the edge security policy associated with
this backend bucket. |
| `cdn_policy` | String |  | Cloud CDN configuration for this BackendBucket. |
| `kind` | String |  | Type of the resource. |
| `compression_mode` | String |  | Compress text responses using Brotli or gzip compression, based on
the client's Accept-Encoding header. |
| `name` | String |  | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `params` | String |  | Input only. [Input Only] Additional params passed with the request, but not persisted
as part of resource payload. |
| `bucket_name` | String |  | Cloud Storage bucket name. |
| `used_by` | Vec<String> |  | [Output Only] List of resources referencing that backend bucket. |
| `load_balancing_scheme` | String |  | The value can only be INTERNAL_MANAGED for cross-region internal layer 7
load balancer.

If loadBalancingScheme is not specified, the backend bucket can be used by
classic global external load balancers, or global application external load
balancers, or both. |
| `description` | String |  | An optional textual description of the resource; provided by the client
when the resource is created. |
| `id` | String |  | [Output Only] Unique identifier for the resource; defined by the server. |
| `region` | String |  | [Output Only] URL of the region where the regional backend bucket
resides. This field is not applicable to global backend buckets.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `custom_response_headers` | Vec<String> |  | Headers that the Application Load Balancer should add to proxied responses. |
| `enable_cdn` | bool |  | If true, enable Cloud CDN for this BackendBucket. |
| `region` | String | ✅ | Name of the region of this request. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `edge_security_policy` | String | [Output Only] The resource URL for the edge security policy associated with
this backend bucket. |
| `cdn_policy` | String | Cloud CDN configuration for this BackendBucket. |
| `kind` | String | Type of the resource. |
| `compression_mode` | String | Compress text responses using Brotli or gzip compression, based on
the client's Accept-Encoding header. |
| `name` | String | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `params` | String | Input only. [Input Only] Additional params passed with the request, but not persisted
as part of resource payload. |
| `bucket_name` | String | Cloud Storage bucket name. |
| `used_by` | Vec<String> | [Output Only] List of resources referencing that backend bucket. |
| `load_balancing_scheme` | String | The value can only be INTERNAL_MANAGED for cross-region internal layer 7
load balancer.

If loadBalancingScheme is not specified, the backend bucket can be used by
classic global external load balancers, or global application external load
balancers, or both. |
| `description` | String | An optional textual description of the resource; provided by the client
when the resource is created. |
| `id` | String | [Output Only] Unique identifier for the resource; defined by the server. |
| `region` | String | [Output Only] URL of the region where the regional backend bucket
resides. This field is not applicable to global backend buckets.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `custom_response_headers` | Vec<String> | Headers that the Application Load Balancer should add to proxied responses. |
| `enable_cdn` | bool | If true, enable Cloud CDN for this BackendBucket. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create region_backend_bucket
region_backend_bucket = provider.compute_api.Region_backend_bucket {
    region = "value"  # Name of the region of this request.
    project = "value"  # Project ID for this request.
}

# Access region_backend_bucket outputs
region_backend_bucket_id = region_backend_bucket.id
region_backend_bucket_self_link = region_backend_bucket.self_link
region_backend_bucket_creation_timestamp = region_backend_bucket.creation_timestamp
region_backend_bucket_edge_security_policy = region_backend_bucket.edge_security_policy
region_backend_bucket_cdn_policy = region_backend_bucket.cdn_policy
region_backend_bucket_kind = region_backend_bucket.kind
region_backend_bucket_compression_mode = region_backend_bucket.compression_mode
region_backend_bucket_name = region_backend_bucket.name
region_backend_bucket_params = region_backend_bucket.params
region_backend_bucket_bucket_name = region_backend_bucket.bucket_name
region_backend_bucket_used_by = region_backend_bucket.used_by
region_backend_bucket_load_balancing_scheme = region_backend_bucket.load_balancing_scheme
region_backend_bucket_description = region_backend_bucket.description
region_backend_bucket_id = region_backend_bucket.id
region_backend_bucket_region = region_backend_bucket.region
region_backend_bucket_custom_response_headers = region_backend_bucket.custom_response_headers
region_backend_bucket_enable_cdn = region_backend_bucket.enable_cdn
```

---


### Http_health_check

Creates a HttpHealthCheck resource in the specified project using the data
included in the request.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `healthy_threshold` | i64 |  | A so-far unhealthy instance will be marked healthy after this
many consecutive successes. The default value is 2. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `host` | String |  | The value of the host header in the HTTP health check request. If left
empty (default value), the public IP on behalf of which this health check
is performed will be used. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `port` | i64 |  | The TCP port number for the HTTP health check request. The default value is80. |
| `request_path` | String |  | The request path of the HTTP health check request. The default value is/. This field does not support query
parameters. Must comply withRFC3986. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `timeout_sec` | i64 |  | How long (in seconds) to wait before claiming failure. The default value is5 seconds. It is invalid for timeoutSec to have
greater value than checkIntervalSec. |
| `unhealthy_threshold` | i64 |  | A so-far healthy instance will be marked unhealthy after this
many consecutive failures. The default value is 2. |
| `check_interval_sec` | i64 |  | How often (in seconds) to send a health check. The default value is5 seconds. |
| `name` | String |  | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `kind` | String |  | [Output Only] Type of the resource. Alwayscompute#httpHealthCheck for HTTP health checks. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `healthy_threshold` | i64 | A so-far unhealthy instance will be marked healthy after this
many consecutive successes. The default value is 2. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `host` | String | The value of the host header in the HTTP health check request. If left
empty (default value), the public IP on behalf of which this health check
is performed will be used. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `port` | i64 | The TCP port number for the HTTP health check request. The default value is80. |
| `request_path` | String | The request path of the HTTP health check request. The default value is/. This field does not support query
parameters. Must comply withRFC3986. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `timeout_sec` | i64 | How long (in seconds) to wait before claiming failure. The default value is5 seconds. It is invalid for timeoutSec to have
greater value than checkIntervalSec. |
| `unhealthy_threshold` | i64 | A so-far healthy instance will be marked unhealthy after this
many consecutive failures. The default value is 2. |
| `check_interval_sec` | i64 | How often (in seconds) to send a health check. The default value is5 seconds. |
| `name` | String | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `kind` | String | [Output Only] Type of the resource. Alwayscompute#httpHealthCheck for HTTP health checks. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create http_health_check
http_health_check = provider.compute_api.Http_health_check {
    project = "value"  # Project ID for this request.
}

# Access http_health_check outputs
http_health_check_id = http_health_check.id
http_health_check_creation_timestamp = http_health_check.creation_timestamp
http_health_check_healthy_threshold = http_health_check.healthy_threshold
http_health_check_description = http_health_check.description
http_health_check_host = http_health_check.host
http_health_check_id = http_health_check.id
http_health_check_port = http_health_check.port
http_health_check_request_path = http_health_check.request_path
http_health_check_self_link = http_health_check.self_link
http_health_check_timeout_sec = http_health_check.timeout_sec
http_health_check_unhealthy_threshold = http_health_check.unhealthy_threshold
http_health_check_check_interval_sec = http_health_check.check_interval_sec
http_health_check_name = http_health_check.name
http_health_check_kind = http_health_check.kind
```

---


### Global_operation

Waits for the specified Operation resource to return as `DONE`
or for the request to approach the 2 minute deadline, and retrieves the
specified Operation resource. This method differs from the
`GET` method in that it waits for no more than the default
deadline (2 minutes) and then returns the current state of the operation,
which might be `DONE` or still in progress.

This method is called on a best-effort basis. Specifically:
   
   
    - In uncommon cases, when the server is overloaded, the request might
    return before the default deadline is reached, or might return after zero
    seconds.
   - If the default deadline is reached, there is no guarantee that the
    operation is actually done when the method returns. Be prepared to retry
    if the operation is not `DONE`.

**Operations**: ✅ Create ✅ Read ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `operation` | String | ✅ | Name of the Operations resource to return, or its unique numeric
identifier. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `start_time` | String | [Output Only] The time that this operation was started by the server.
This value is inRFC3339
text format. |
| `http_error_message` | String | [Output Only] If the operation fails, this field contains the HTTP error
message that was returned, such as `NOT FOUND`. |
| `id` | String | [Output Only] The unique identifier for the operation. This identifier is
defined by the server. |
| `kind` | String | [Output Only] Type of the resource. Always `compute#operation` for
Operation resources. |
| `error` | String | [Output Only] If errors are generated during processing of the operation,
this field will be populated. |
| `description` | String | [Output Only] A textual description of the operation, which is
set when the operation is created. |
| `status_message` | String | [Output Only] An optional textual description of the current status of the
operation. |
| `warnings` | Vec<String> | [Output Only] If warning messages are generated during processing of the
operation, this field will be populated. |
| `creation_timestamp` | String | [Deprecated] This field is deprecated. |
| `zone` | String | [Output Only] The URL of the zone where the operation resides. Only
applicable when performing per-zone operations. |
| `user` | String | [Output Only] User who requested the operation, for example:
`user@example.com` or
`alice_smith_identifier (global/workforcePools/example-com-us-employees)`. |
| `name` | String | [Output Only] Name of the operation. |
| `operation_group_id` | String | [Output Only] An ID that represents a group of operations, such as when a
group of operations results from a `bulkInsert` API request. |
| `client_operation_id` | String | [Output Only] The value of `requestId` if you provided it in the request.
Not present otherwise. |
| `http_error_status_code` | i64 | [Output Only] If the operation fails, this field contains the HTTP error
status code that was returned. For example, a `404` means the
resource was not found. |
| `progress` | i64 | [Output Only] An optional progress indicator that ranges from 0 to 100.
There is no requirement that this be linear or support any granularity of
operations. This should not be used to guess when the operation will be
complete. This number should monotonically increase as the operation
progresses. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `status` | String | [Output Only] The status of the operation, which can be one of the
following:
`PENDING`, `RUNNING`, or `DONE`. |
| `target_link` | String | [Output Only] The URL of the resource that the operation modifies. For
operations related to creating a snapshot, this points to the disk
that the snapshot was created from. |
| `set_common_instance_metadata_operation_metadata` | String | [Output Only] If the operation is for projects.setCommonInstanceMetadata,
this field will contain information on all underlying zonal actions and
their state. |
| `target_id` | String | [Output Only] The unique target ID, which identifies a specific incarnation
of the target resource. |
| `instances_bulk_insert_operation_metadata` | String |  |
| `insert_time` | String | [Output Only] The time that this operation was requested.
This value is inRFC3339
text format. |
| `operation_type` | String | [Output Only] The type of operation, such as `insert`,
`update`, or `delete`, and so on. |
| `region` | String | [Output Only] The URL of the region where the operation resides. Only
applicable when performing regional operations. |
| `end_time` | String | [Output Only] The time that this operation was completed. This value is inRFC3339
text format. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create global_operation
global_operation = provider.compute_api.Global_operation {
    operation = "value"  # Name of the Operations resource to return, or its unique numeric
identifier.
    project = "value"  # Project ID for this request.
}

# Access global_operation outputs
global_operation_id = global_operation.id
global_operation_start_time = global_operation.start_time
global_operation_http_error_message = global_operation.http_error_message
global_operation_id = global_operation.id
global_operation_kind = global_operation.kind
global_operation_error = global_operation.error
global_operation_description = global_operation.description
global_operation_status_message = global_operation.status_message
global_operation_warnings = global_operation.warnings
global_operation_creation_timestamp = global_operation.creation_timestamp
global_operation_zone = global_operation.zone
global_operation_user = global_operation.user
global_operation_name = global_operation.name
global_operation_operation_group_id = global_operation.operation_group_id
global_operation_client_operation_id = global_operation.client_operation_id
global_operation_http_error_status_code = global_operation.http_error_status_code
global_operation_progress = global_operation.progress
global_operation_self_link = global_operation.self_link
global_operation_status = global_operation.status
global_operation_target_link = global_operation.target_link
global_operation_set_common_instance_metadata_operation_metadata = global_operation.set_common_instance_metadata_operation_metadata
global_operation_target_id = global_operation.target_id
global_operation_instances_bulk_insert_operation_metadata = global_operation.instances_bulk_insert_operation_metadata
global_operation_insert_time = global_operation.insert_time
global_operation_operation_type = global_operation.operation_type
global_operation_region = global_operation.region
global_operation_end_time = global_operation.end_time
```

---


### Region_target_http_proxie

Creates a TargetHttpProxy resource in the specified project and region
using the data included in the request.

**Operations**: ✅ Create ✅ Read ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `region` | String |  | [Output Only] URL of the region where the regional Target HTTP Proxy
resides. This field is not applicable to global Target HTTP Proxies. |
| `url_map` | String |  | URL to the UrlMap resource that defines the mapping from URL to
the BackendService. |
| `proxy_bind` | bool |  | This field only applies when the forwarding rule that references this
target proxy has a loadBalancingScheme set toINTERNAL_SELF_MANAGED.

When this field is set to true, Envoy proxies set up inbound
traffic interception and bind to the IP address and port specified in the
forwarding rule. This is generally useful when using Traffic Director to
configure Envoy as a gateway or middle proxy (in other words, not a
sidecar proxy). The Envoy proxy listens for inbound requests and handles
requests when it receives them.

The default is false. |
| `kind` | String |  | [Output Only] Type of resource. Always compute#targetHttpProxy
for target HTTP proxies. |
| `http_filters` | Vec<String> |  | URLs to networkservices.HttpFilter resources
enabled for xDS clients using this configuration. For example,https://networkservices.googleapis.com/v1alpha1/projects/project/locations/locationhttpFilters/httpFilter
Only filters that handle outbound connection and
stream events may be specified. These filters work in conjunction with
a default set of HTTP filters that may already be configured by Traffic
Director. Traffic Director will determine the final location of these
filters within xDS configuration based on the name of the HTTP filter. If
Traffic Director positions multiple filters at the same location, those
filters will be in the same order as specified in this list. 

httpFilters only applies for loadbalancers withloadBalancingScheme set to INTERNAL_SELF_MANAGED.
See ForwardingRule for more details. |
| `fingerprint` | String |  | Fingerprint of this resource. A hash of the contents stored in this object.
This field is used in optimistic locking. This field will be ignored when
inserting a TargetHttpProxy. An up-to-date fingerprint must
be provided in order to patch/update the TargetHttpProxy; otherwise, the
request will fail with error 412 conditionNotMet. To see the
latest fingerprint, make a get() request to retrieve the
TargetHttpProxy. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `http_keep_alive_timeout_sec` | i64 |  | Specifies how long to keep a connection open, after completing a response,
while there is no matching traffic (in seconds). If an HTTP keep-alive is
not specified, a default value (610 seconds) will be used.

For global external Application Load Balancers, the minimum allowed value
is 5 seconds and the maximum allowed value is 1200 seconds.

For classic Application Load Balancers, this option is not supported. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `name` | String |  | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `project` | String | ✅ | Project ID for this request. |
| `region` | String | ✅ | Name of the region scoping this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `region` | String | [Output Only] URL of the region where the regional Target HTTP Proxy
resides. This field is not applicable to global Target HTTP Proxies. |
| `url_map` | String | URL to the UrlMap resource that defines the mapping from URL to
the BackendService. |
| `proxy_bind` | bool | This field only applies when the forwarding rule that references this
target proxy has a loadBalancingScheme set toINTERNAL_SELF_MANAGED.

When this field is set to true, Envoy proxies set up inbound
traffic interception and bind to the IP address and port specified in the
forwarding rule. This is generally useful when using Traffic Director to
configure Envoy as a gateway or middle proxy (in other words, not a
sidecar proxy). The Envoy proxy listens for inbound requests and handles
requests when it receives them.

The default is false. |
| `kind` | String | [Output Only] Type of resource. Always compute#targetHttpProxy
for target HTTP proxies. |
| `http_filters` | Vec<String> | URLs to networkservices.HttpFilter resources
enabled for xDS clients using this configuration. For example,https://networkservices.googleapis.com/v1alpha1/projects/project/locations/locationhttpFilters/httpFilter
Only filters that handle outbound connection and
stream events may be specified. These filters work in conjunction with
a default set of HTTP filters that may already be configured by Traffic
Director. Traffic Director will determine the final location of these
filters within xDS configuration based on the name of the HTTP filter. If
Traffic Director positions multiple filters at the same location, those
filters will be in the same order as specified in this list. 

httpFilters only applies for loadbalancers withloadBalancingScheme set to INTERNAL_SELF_MANAGED.
See ForwardingRule for more details. |
| `fingerprint` | String | Fingerprint of this resource. A hash of the contents stored in this object.
This field is used in optimistic locking. This field will be ignored when
inserting a TargetHttpProxy. An up-to-date fingerprint must
be provided in order to patch/update the TargetHttpProxy; otherwise, the
request will fail with error 412 conditionNotMet. To see the
latest fingerprint, make a get() request to retrieve the
TargetHttpProxy. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `http_keep_alive_timeout_sec` | i64 | Specifies how long to keep a connection open, after completing a response,
while there is no matching traffic (in seconds). If an HTTP keep-alive is
not specified, a default value (610 seconds) will be used.

For global external Application Load Balancers, the minimum allowed value
is 5 seconds and the maximum allowed value is 1200 seconds.

For classic Application Load Balancers, this option is not supported. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `name` | String | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create region_target_http_proxie
region_target_http_proxie = provider.compute_api.Region_target_http_proxie {
    project = "value"  # Project ID for this request.
    region = "value"  # Name of the region scoping this request.
}

# Access region_target_http_proxie outputs
region_target_http_proxie_id = region_target_http_proxie.id
region_target_http_proxie_region = region_target_http_proxie.region
region_target_http_proxie_url_map = region_target_http_proxie.url_map
region_target_http_proxie_proxy_bind = region_target_http_proxie.proxy_bind
region_target_http_proxie_kind = region_target_http_proxie.kind
region_target_http_proxie_http_filters = region_target_http_proxie.http_filters
region_target_http_proxie_fingerprint = region_target_http_proxie.fingerprint
region_target_http_proxie_self_link = region_target_http_proxie.self_link
region_target_http_proxie_description = region_target_http_proxie.description
region_target_http_proxie_creation_timestamp = region_target_http_proxie.creation_timestamp
region_target_http_proxie_http_keep_alive_timeout_sec = region_target_http_proxie.http_keep_alive_timeout_sec
region_target_http_proxie_id = region_target_http_proxie.id
region_target_http_proxie_name = region_target_http_proxie.name
```

---


### Region_instance

Creates multiple instances in a given region. Count specifies the number of
instances to create.

**Operations**: ✅ Create

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `min_count` | String |  | The minimum number of instances to create. If no min_count is
specified then count is used as the default value. Ifmin_count instances cannot be created, then no instances will
be created and instances already created will be deleted. |
| `name_pattern` | String |  | The string pattern used for the names of the VMs.
Either name_pattern or per_instance_properties
must be set. The pattern must contain one continuous sequence of
placeholder hash characters (#) with each character corresponding to one
digit of the generated instance name. Example: a name_pattern
of inst-#### generates instance names such asinst-0001 and inst-0002. If existing instances
in the same project and zone have names that match the name pattern
then the generated instance numbers start after the biggest
existing number. For example, if there exists an instance with nameinst-0050, then instance names generated using the patterninst-#### begin with inst-0051. The name pattern
placeholder #...# can contain up to 18 characters. |
| `instance_properties` | String |  | The instance properties defining the VM instances to be created. Required
if sourceInstanceTemplate is not provided. |
| `per_instance_properties` | HashMap<String, String> |  | Per-instance properties to be set on individual instances.
Keys of this map specify requested instance names.
Can be empty if name_pattern is used. |
| `count` | String |  | The maximum number of instances to create. |
| `source_instance_template` | String |  | Specifies the instance template from which to create instances. You may
combine sourceInstanceTemplate withinstanceProperties to override specific values from an
existing instance template. Bulk API follows the semantics of JSON Merge
Patch described by RFC
7396.

It can be a full or partial URL. For example, the following are
all valid URLs to an instance template: 
   
   
      - https://www.googleapis.com/compute/v1/projects/project/global/instanceTemplates/instanceTemplate
      - projects/project/global/instanceTemplates/instanceTemplate
      - global/instanceTemplates/instanceTemplate



This field is optional. |
| `location_policy` | String |  | Policy for choosing target zone. For more information, seeCreate VMs in
bulk. |
| `project` | String | ✅ | Project ID for this request. |
| `region` | String | ✅ | The name of the region for this request. |



#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create region_instance
region_instance = provider.compute_api.Region_instance {
    project = "value"  # Project ID for this request.
    region = "value"  # The name of the region for this request.
}

```

---


### Region_instance_group_manager

Creates a managed instance group using the information that you specify
in the request. After the group is created, instances in the group are
created using the specified instance template.
This operation is marked as DONE when the group is created
even if the instances in the group have not yet been created. You must
separately verify the status of the individual instances with thelistmanagedinstances
method.

A regional managed instance group can contain up to 2000 instances.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `target_pools` | Vec<String> |  | The URLs for all TargetPool resources to which instances in theinstanceGroup field are added. The target pools automatically
apply to all of the instances in the managed instance group. |
| `satisfies_pzi` | bool |  | [Output Only] Reserved for future use. |
| `current_actions` | String |  | [Output Only] The list of instance actions and the number of instances
in this managed instance group that are scheduled for each of those
actions. |
| `target_suspended_size` | i64 |  | The target number of suspended instances for this managed instance group.
This number changes when you: 
   
   - Suspend instance using the suspendInstances
   method or resume instances using the resumeInstances
   method.
   - Manually change the targetSuspendedSize using the update
   method. |
| `zone` | String |  | [Output Only] The URL of azone
where the managed instance group is located (for zonal resources). |
| `base_instance_name` | String |  | The base instance name is a prefix that you want to attach to the names of
all VMs in a MIG. The maximum character length is 58 and the name must
comply with RFC1035 format.

When a VM is created in the group, the MIG appends a hyphen and a random
four-character string to the base instance name. If you want the MIG to
assign sequential numbers instead of a random string, then end the base
instance name with a hyphen followed by one or more hash symbols. The hash
symbols indicate the number of digits. For example, a base instance name of
"vm-###" results in "vm-001" as a VM name.
@pattern
[a-z](([-a-z0-9]{0,57})|([-a-z0-9]{0,51}-#{1,10}(\\[[0-9]{1,10}\\])?)) |
| `status` | String |  | [Output Only] The status of this managed instance group. |
| `target_stopped_size` | i64 |  | The target number of stopped instances for this managed instance group.
This number changes when you: 
   
   - Stop instance using the stopInstances
   method or start instances using the startInstances
   method.
   - Manually change the targetStoppedSize using the update
   method. |
| `all_instances_config` | String |  | Specifies configuration that overrides the instance template configuration
for the group. |
| `description` | String |  | An optional description of this resource. |
| `instance_lifecycle_policy` | String |  | The repair policy for this managed instance group. |
| `self_link` | String |  | [Output Only] The URL for this managed instance group. The server defines
this URL. |
| `satisfies_pzs` | bool |  | [Output Only] Reserved for future use. |
| `standby_policy` | String |  | Standby policy for stopped and suspended instances. |
| `region` | String |  | [Output Only] The URL of theregion
where the managed instance group resides (for regional resources). |
| `instance_template` | String |  | The URL of the instance template that is specified for this managed
instance group. The group uses this template to create all new instances
in the managed instance group. The templates for existing instances in the
group do not change unless you run recreateInstances, runapplyUpdatesToInstances, or set the group'supdatePolicy.type to PROACTIVE. |
| `fingerprint` | String |  | Fingerprint of this resource. This field may be used in optimistic locking.
It will be ignored when inserting an InstanceGroupManager. An up-to-date
fingerprint must be provided in order to update the InstanceGroupManager,
otherwise the request will fail with error412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve an InstanceGroupManager. |
| `creation_timestamp` | String |  | [Output Only] The creation timestamp for this managed instance group inRFC3339
text format. |
| `id` | String |  | [Output Only] A unique identifier for this resource type. The server
generates this identifier. |
| `name` | String |  | The name of the managed instance group. The name must be 1-63 characters
long, and comply withRFC1035. |
| `target_size` | i64 |  | The target number of running instances for this managed instance group.
You can reduce this number by using the instanceGroupManager
deleteInstances or abandonInstances methods. Resizing the group also
changes this number. |
| `named_ports` | Vec<String> |  | [Output Only] Named ports configured on the Instance Groups complementary
to this Instance Group Manager. |
| `multi_mig` | String |  | URL to the multi-MIG that this Managed Instance Group belongs to. |
| `params` | String |  | Input only. Additional params passed with the request, but not persisted
as part of resource payload. |
| `instance_flexibility_policy` | String |  | Instance flexibility allowing MIG to create VMs from multiple
types of machines.
Instance flexibility configuration on MIG overrides instance
template configuration. |
| `failover_action` | String |  | The action to perform in case of zone failure. Only one value is supported,NO_FAILOVER. The default is NO_FAILOVER. |
| `auto_healing_policies` | Vec<String> |  | The autohealing policy for this managed instance group. You can specify
only one value. |
| `distribution_policy` | String |  | Policy specifying the intended distribution of managed instances across
zones in a regional managed instance group. |
| `kind` | String |  | [Output Only] The resource type, which is alwayscompute#instanceGroupManager for managed instance groups. |
| `resource_policies` | String |  | Resource policies for this managed instance group. |
| `service_account` | String |  | The service account to be used as credentials for all operations performed
by the managed instance group on instances. The service accounts needs all
permissions required to create and delete instances.
By default, the service account
{projectNumber}@cloudservices.gserviceaccount.com is used. |
| `instance_group` | String |  | [Output Only] The URL of the Instance Group resource. |
| `target_size_policy` | String |  | The policy that specifies how the MIG creates its VMs to achieve the target
size. |
| `update_policy` | String |  | The update policy for this managed instance group. |
| `list_managed_instances_results` | String |  | Pagination behavior of the listManagedInstances API method for
this managed instance group. |
| `stateful_policy` | String |  | Stateful configuration for this Instanced Group Manager |
| `versions` | Vec<String> |  | Specifies the instance templates used by this managed instance group to
create instances.

Each version is defined by an instanceTemplate and aname. Every version can appear at most once per instance
group. This field overrides the top-level instanceTemplate
field. Read more about therelationships
between these fields. Exactly one version must leave thetargetSize field unset. That version will be applied to all
remaining instances. For more information, read aboutcanary
updates. |
| `region` | String | ✅ | Name of the region scoping this request. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `target_pools` | Vec<String> | The URLs for all TargetPool resources to which instances in theinstanceGroup field are added. The target pools automatically
apply to all of the instances in the managed instance group. |
| `satisfies_pzi` | bool | [Output Only] Reserved for future use. |
| `current_actions` | String | [Output Only] The list of instance actions and the number of instances
in this managed instance group that are scheduled for each of those
actions. |
| `target_suspended_size` | i64 | The target number of suspended instances for this managed instance group.
This number changes when you: 
   
   - Suspend instance using the suspendInstances
   method or resume instances using the resumeInstances
   method.
   - Manually change the targetSuspendedSize using the update
   method. |
| `zone` | String | [Output Only] The URL of azone
where the managed instance group is located (for zonal resources). |
| `base_instance_name` | String | The base instance name is a prefix that you want to attach to the names of
all VMs in a MIG. The maximum character length is 58 and the name must
comply with RFC1035 format.

When a VM is created in the group, the MIG appends a hyphen and a random
four-character string to the base instance name. If you want the MIG to
assign sequential numbers instead of a random string, then end the base
instance name with a hyphen followed by one or more hash symbols. The hash
symbols indicate the number of digits. For example, a base instance name of
"vm-###" results in "vm-001" as a VM name.
@pattern
[a-z](([-a-z0-9]{0,57})|([-a-z0-9]{0,51}-#{1,10}(\\[[0-9]{1,10}\\])?)) |
| `status` | String | [Output Only] The status of this managed instance group. |
| `target_stopped_size` | i64 | The target number of stopped instances for this managed instance group.
This number changes when you: 
   
   - Stop instance using the stopInstances
   method or start instances using the startInstances
   method.
   - Manually change the targetStoppedSize using the update
   method. |
| `all_instances_config` | String | Specifies configuration that overrides the instance template configuration
for the group. |
| `description` | String | An optional description of this resource. |
| `instance_lifecycle_policy` | String | The repair policy for this managed instance group. |
| `self_link` | String | [Output Only] The URL for this managed instance group. The server defines
this URL. |
| `satisfies_pzs` | bool | [Output Only] Reserved for future use. |
| `standby_policy` | String | Standby policy for stopped and suspended instances. |
| `region` | String | [Output Only] The URL of theregion
where the managed instance group resides (for regional resources). |
| `instance_template` | String | The URL of the instance template that is specified for this managed
instance group. The group uses this template to create all new instances
in the managed instance group. The templates for existing instances in the
group do not change unless you run recreateInstances, runapplyUpdatesToInstances, or set the group'supdatePolicy.type to PROACTIVE. |
| `fingerprint` | String | Fingerprint of this resource. This field may be used in optimistic locking.
It will be ignored when inserting an InstanceGroupManager. An up-to-date
fingerprint must be provided in order to update the InstanceGroupManager,
otherwise the request will fail with error412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve an InstanceGroupManager. |
| `creation_timestamp` | String | [Output Only] The creation timestamp for this managed instance group inRFC3339
text format. |
| `id` | String | [Output Only] A unique identifier for this resource type. The server
generates this identifier. |
| `name` | String | The name of the managed instance group. The name must be 1-63 characters
long, and comply withRFC1035. |
| `target_size` | i64 | The target number of running instances for this managed instance group.
You can reduce this number by using the instanceGroupManager
deleteInstances or abandonInstances methods. Resizing the group also
changes this number. |
| `named_ports` | Vec<String> | [Output Only] Named ports configured on the Instance Groups complementary
to this Instance Group Manager. |
| `multi_mig` | String | URL to the multi-MIG that this Managed Instance Group belongs to. |
| `params` | String | Input only. Additional params passed with the request, but not persisted
as part of resource payload. |
| `instance_flexibility_policy` | String | Instance flexibility allowing MIG to create VMs from multiple
types of machines.
Instance flexibility configuration on MIG overrides instance
template configuration. |
| `failover_action` | String | The action to perform in case of zone failure. Only one value is supported,NO_FAILOVER. The default is NO_FAILOVER. |
| `auto_healing_policies` | Vec<String> | The autohealing policy for this managed instance group. You can specify
only one value. |
| `distribution_policy` | String | Policy specifying the intended distribution of managed instances across
zones in a regional managed instance group. |
| `kind` | String | [Output Only] The resource type, which is alwayscompute#instanceGroupManager for managed instance groups. |
| `resource_policies` | String | Resource policies for this managed instance group. |
| `service_account` | String | The service account to be used as credentials for all operations performed
by the managed instance group on instances. The service accounts needs all
permissions required to create and delete instances.
By default, the service account
{projectNumber}@cloudservices.gserviceaccount.com is used. |
| `instance_group` | String | [Output Only] The URL of the Instance Group resource. |
| `target_size_policy` | String | The policy that specifies how the MIG creates its VMs to achieve the target
size. |
| `update_policy` | String | The update policy for this managed instance group. |
| `list_managed_instances_results` | String | Pagination behavior of the listManagedInstances API method for
this managed instance group. |
| `stateful_policy` | String | Stateful configuration for this Instanced Group Manager |
| `versions` | Vec<String> | Specifies the instance templates used by this managed instance group to
create instances.

Each version is defined by an instanceTemplate and aname. Every version can appear at most once per instance
group. This field overrides the top-level instanceTemplate
field. Read more about therelationships
between these fields. Exactly one version must leave thetargetSize field unset. That version will be applied to all
remaining instances. For more information, read aboutcanary
updates. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create region_instance_group_manager
region_instance_group_manager = provider.compute_api.Region_instance_group_manager {
    region = "value"  # Name of the region scoping this request.
    project = "value"  # Project ID for this request.
}

# Access region_instance_group_manager outputs
region_instance_group_manager_id = region_instance_group_manager.id
region_instance_group_manager_target_pools = region_instance_group_manager.target_pools
region_instance_group_manager_satisfies_pzi = region_instance_group_manager.satisfies_pzi
region_instance_group_manager_current_actions = region_instance_group_manager.current_actions
region_instance_group_manager_target_suspended_size = region_instance_group_manager.target_suspended_size
region_instance_group_manager_zone = region_instance_group_manager.zone
region_instance_group_manager_base_instance_name = region_instance_group_manager.base_instance_name
region_instance_group_manager_status = region_instance_group_manager.status
region_instance_group_manager_target_stopped_size = region_instance_group_manager.target_stopped_size
region_instance_group_manager_all_instances_config = region_instance_group_manager.all_instances_config
region_instance_group_manager_description = region_instance_group_manager.description
region_instance_group_manager_instance_lifecycle_policy = region_instance_group_manager.instance_lifecycle_policy
region_instance_group_manager_self_link = region_instance_group_manager.self_link
region_instance_group_manager_satisfies_pzs = region_instance_group_manager.satisfies_pzs
region_instance_group_manager_standby_policy = region_instance_group_manager.standby_policy
region_instance_group_manager_region = region_instance_group_manager.region
region_instance_group_manager_instance_template = region_instance_group_manager.instance_template
region_instance_group_manager_fingerprint = region_instance_group_manager.fingerprint
region_instance_group_manager_creation_timestamp = region_instance_group_manager.creation_timestamp
region_instance_group_manager_id = region_instance_group_manager.id
region_instance_group_manager_name = region_instance_group_manager.name
region_instance_group_manager_target_size = region_instance_group_manager.target_size
region_instance_group_manager_named_ports = region_instance_group_manager.named_ports
region_instance_group_manager_multi_mig = region_instance_group_manager.multi_mig
region_instance_group_manager_params = region_instance_group_manager.params
region_instance_group_manager_instance_flexibility_policy = region_instance_group_manager.instance_flexibility_policy
region_instance_group_manager_failover_action = region_instance_group_manager.failover_action
region_instance_group_manager_auto_healing_policies = region_instance_group_manager.auto_healing_policies
region_instance_group_manager_distribution_policy = region_instance_group_manager.distribution_policy
region_instance_group_manager_kind = region_instance_group_manager.kind
region_instance_group_manager_resource_policies = region_instance_group_manager.resource_policies
region_instance_group_manager_service_account = region_instance_group_manager.service_account
region_instance_group_manager_instance_group = region_instance_group_manager.instance_group
region_instance_group_manager_target_size_policy = region_instance_group_manager.target_size_policy
region_instance_group_manager_update_policy = region_instance_group_manager.update_policy
region_instance_group_manager_list_managed_instances_results = region_instance_group_manager.list_managed_instances_results
region_instance_group_manager_stateful_policy = region_instance_group_manager.stateful_policy
region_instance_group_manager_versions = region_instance_group_manager.versions
```

---


### Instance_group_manager

Creates a managed instance group using the information that you specify
in the request. After the group is created, instances in the group are
created using the specified instance template.
This operation is marked as DONE when the group is created
even if the instances in the group have not yet been created. You
must separately verify the status of the individual instances with thelistmanagedinstances
method.

A managed instance group can have up to 1000 VM instances per group. Please
contact Cloud Support if you need an increase in
this limit.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `target_pools` | Vec<String> |  | The URLs for all TargetPool resources to which instances in theinstanceGroup field are added. The target pools automatically
apply to all of the instances in the managed instance group. |
| `satisfies_pzi` | bool |  | [Output Only] Reserved for future use. |
| `current_actions` | String |  | [Output Only] The list of instance actions and the number of instances
in this managed instance group that are scheduled for each of those
actions. |
| `target_suspended_size` | i64 |  | The target number of suspended instances for this managed instance group.
This number changes when you: 
   
   - Suspend instance using the suspendInstances
   method or resume instances using the resumeInstances
   method.
   - Manually change the targetSuspendedSize using the update
   method. |
| `zone` | String |  | [Output Only] The URL of azone
where the managed instance group is located (for zonal resources). |
| `base_instance_name` | String |  | The base instance name is a prefix that you want to attach to the names of
all VMs in a MIG. The maximum character length is 58 and the name must
comply with RFC1035 format.

When a VM is created in the group, the MIG appends a hyphen and a random
four-character string to the base instance name. If you want the MIG to
assign sequential numbers instead of a random string, then end the base
instance name with a hyphen followed by one or more hash symbols. The hash
symbols indicate the number of digits. For example, a base instance name of
"vm-###" results in "vm-001" as a VM name.
@pattern
[a-z](([-a-z0-9]{0,57})|([-a-z0-9]{0,51}-#{1,10}(\\[[0-9]{1,10}\\])?)) |
| `status` | String |  | [Output Only] The status of this managed instance group. |
| `target_stopped_size` | i64 |  | The target number of stopped instances for this managed instance group.
This number changes when you: 
   
   - Stop instance using the stopInstances
   method or start instances using the startInstances
   method.
   - Manually change the targetStoppedSize using the update
   method. |
| `all_instances_config` | String |  | Specifies configuration that overrides the instance template configuration
for the group. |
| `description` | String |  | An optional description of this resource. |
| `instance_lifecycle_policy` | String |  | The repair policy for this managed instance group. |
| `self_link` | String |  | [Output Only] The URL for this managed instance group. The server defines
this URL. |
| `satisfies_pzs` | bool |  | [Output Only] Reserved for future use. |
| `standby_policy` | String |  | Standby policy for stopped and suspended instances. |
| `region` | String |  | [Output Only] The URL of theregion
where the managed instance group resides (for regional resources). |
| `instance_template` | String |  | The URL of the instance template that is specified for this managed
instance group. The group uses this template to create all new instances
in the managed instance group. The templates for existing instances in the
group do not change unless you run recreateInstances, runapplyUpdatesToInstances, or set the group'supdatePolicy.type to PROACTIVE. |
| `fingerprint` | String |  | Fingerprint of this resource. This field may be used in optimistic locking.
It will be ignored when inserting an InstanceGroupManager. An up-to-date
fingerprint must be provided in order to update the InstanceGroupManager,
otherwise the request will fail with error412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve an InstanceGroupManager. |
| `creation_timestamp` | String |  | [Output Only] The creation timestamp for this managed instance group inRFC3339
text format. |
| `id` | String |  | [Output Only] A unique identifier for this resource type. The server
generates this identifier. |
| `name` | String |  | The name of the managed instance group. The name must be 1-63 characters
long, and comply withRFC1035. |
| `target_size` | i64 |  | The target number of running instances for this managed instance group.
You can reduce this number by using the instanceGroupManager
deleteInstances or abandonInstances methods. Resizing the group also
changes this number. |
| `named_ports` | Vec<String> |  | [Output Only] Named ports configured on the Instance Groups complementary
to this Instance Group Manager. |
| `multi_mig` | String |  | URL to the multi-MIG that this Managed Instance Group belongs to. |
| `params` | String |  | Input only. Additional params passed with the request, but not persisted
as part of resource payload. |
| `instance_flexibility_policy` | String |  | Instance flexibility allowing MIG to create VMs from multiple
types of machines.
Instance flexibility configuration on MIG overrides instance
template configuration. |
| `failover_action` | String |  | The action to perform in case of zone failure. Only one value is supported,NO_FAILOVER. The default is NO_FAILOVER. |
| `auto_healing_policies` | Vec<String> |  | The autohealing policy for this managed instance group. You can specify
only one value. |
| `distribution_policy` | String |  | Policy specifying the intended distribution of managed instances across
zones in a regional managed instance group. |
| `kind` | String |  | [Output Only] The resource type, which is alwayscompute#instanceGroupManager for managed instance groups. |
| `resource_policies` | String |  | Resource policies for this managed instance group. |
| `service_account` | String |  | The service account to be used as credentials for all operations performed
by the managed instance group on instances. The service accounts needs all
permissions required to create and delete instances.
By default, the service account
{projectNumber}@cloudservices.gserviceaccount.com is used. |
| `instance_group` | String |  | [Output Only] The URL of the Instance Group resource. |
| `target_size_policy` | String |  | The policy that specifies how the MIG creates its VMs to achieve the target
size. |
| `update_policy` | String |  | The update policy for this managed instance group. |
| `list_managed_instances_results` | String |  | Pagination behavior of the listManagedInstances API method for
this managed instance group. |
| `stateful_policy` | String |  | Stateful configuration for this Instanced Group Manager |
| `versions` | Vec<String> |  | Specifies the instance templates used by this managed instance group to
create instances.

Each version is defined by an instanceTemplate and aname. Every version can appear at most once per instance
group. This field overrides the top-level instanceTemplate
field. Read more about therelationships
between these fields. Exactly one version must leave thetargetSize field unset. That version will be applied to all
remaining instances. For more information, read aboutcanary
updates. |
| `project` | String | ✅ | Project ID for this request. |
| `zone` | String | ✅ | The name of the zone
where you want to create the managed instance group. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `target_pools` | Vec<String> | The URLs for all TargetPool resources to which instances in theinstanceGroup field are added. The target pools automatically
apply to all of the instances in the managed instance group. |
| `satisfies_pzi` | bool | [Output Only] Reserved for future use. |
| `current_actions` | String | [Output Only] The list of instance actions and the number of instances
in this managed instance group that are scheduled for each of those
actions. |
| `target_suspended_size` | i64 | The target number of suspended instances for this managed instance group.
This number changes when you: 
   
   - Suspend instance using the suspendInstances
   method or resume instances using the resumeInstances
   method.
   - Manually change the targetSuspendedSize using the update
   method. |
| `zone` | String | [Output Only] The URL of azone
where the managed instance group is located (for zonal resources). |
| `base_instance_name` | String | The base instance name is a prefix that you want to attach to the names of
all VMs in a MIG. The maximum character length is 58 and the name must
comply with RFC1035 format.

When a VM is created in the group, the MIG appends a hyphen and a random
four-character string to the base instance name. If you want the MIG to
assign sequential numbers instead of a random string, then end the base
instance name with a hyphen followed by one or more hash symbols. The hash
symbols indicate the number of digits. For example, a base instance name of
"vm-###" results in "vm-001" as a VM name.
@pattern
[a-z](([-a-z0-9]{0,57})|([-a-z0-9]{0,51}-#{1,10}(\\[[0-9]{1,10}\\])?)) |
| `status` | String | [Output Only] The status of this managed instance group. |
| `target_stopped_size` | i64 | The target number of stopped instances for this managed instance group.
This number changes when you: 
   
   - Stop instance using the stopInstances
   method or start instances using the startInstances
   method.
   - Manually change the targetStoppedSize using the update
   method. |
| `all_instances_config` | String | Specifies configuration that overrides the instance template configuration
for the group. |
| `description` | String | An optional description of this resource. |
| `instance_lifecycle_policy` | String | The repair policy for this managed instance group. |
| `self_link` | String | [Output Only] The URL for this managed instance group. The server defines
this URL. |
| `satisfies_pzs` | bool | [Output Only] Reserved for future use. |
| `standby_policy` | String | Standby policy for stopped and suspended instances. |
| `region` | String | [Output Only] The URL of theregion
where the managed instance group resides (for regional resources). |
| `instance_template` | String | The URL of the instance template that is specified for this managed
instance group. The group uses this template to create all new instances
in the managed instance group. The templates for existing instances in the
group do not change unless you run recreateInstances, runapplyUpdatesToInstances, or set the group'supdatePolicy.type to PROACTIVE. |
| `fingerprint` | String | Fingerprint of this resource. This field may be used in optimistic locking.
It will be ignored when inserting an InstanceGroupManager. An up-to-date
fingerprint must be provided in order to update the InstanceGroupManager,
otherwise the request will fail with error412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve an InstanceGroupManager. |
| `creation_timestamp` | String | [Output Only] The creation timestamp for this managed instance group inRFC3339
text format. |
| `id` | String | [Output Only] A unique identifier for this resource type. The server
generates this identifier. |
| `name` | String | The name of the managed instance group. The name must be 1-63 characters
long, and comply withRFC1035. |
| `target_size` | i64 | The target number of running instances for this managed instance group.
You can reduce this number by using the instanceGroupManager
deleteInstances or abandonInstances methods. Resizing the group also
changes this number. |
| `named_ports` | Vec<String> | [Output Only] Named ports configured on the Instance Groups complementary
to this Instance Group Manager. |
| `multi_mig` | String | URL to the multi-MIG that this Managed Instance Group belongs to. |
| `params` | String | Input only. Additional params passed with the request, but not persisted
as part of resource payload. |
| `instance_flexibility_policy` | String | Instance flexibility allowing MIG to create VMs from multiple
types of machines.
Instance flexibility configuration on MIG overrides instance
template configuration. |
| `failover_action` | String | The action to perform in case of zone failure. Only one value is supported,NO_FAILOVER. The default is NO_FAILOVER. |
| `auto_healing_policies` | Vec<String> | The autohealing policy for this managed instance group. You can specify
only one value. |
| `distribution_policy` | String | Policy specifying the intended distribution of managed instances across
zones in a regional managed instance group. |
| `kind` | String | [Output Only] The resource type, which is alwayscompute#instanceGroupManager for managed instance groups. |
| `resource_policies` | String | Resource policies for this managed instance group. |
| `service_account` | String | The service account to be used as credentials for all operations performed
by the managed instance group on instances. The service accounts needs all
permissions required to create and delete instances.
By default, the service account
{projectNumber}@cloudservices.gserviceaccount.com is used. |
| `instance_group` | String | [Output Only] The URL of the Instance Group resource. |
| `target_size_policy` | String | The policy that specifies how the MIG creates its VMs to achieve the target
size. |
| `update_policy` | String | The update policy for this managed instance group. |
| `list_managed_instances_results` | String | Pagination behavior of the listManagedInstances API method for
this managed instance group. |
| `stateful_policy` | String | Stateful configuration for this Instanced Group Manager |
| `versions` | Vec<String> | Specifies the instance templates used by this managed instance group to
create instances.

Each version is defined by an instanceTemplate and aname. Every version can appear at most once per instance
group. This field overrides the top-level instanceTemplate
field. Read more about therelationships
between these fields. Exactly one version must leave thetargetSize field unset. That version will be applied to all
remaining instances. For more information, read aboutcanary
updates. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create instance_group_manager
instance_group_manager = provider.compute_api.Instance_group_manager {
    project = "value"  # Project ID for this request.
    zone = "value"  # The name of the zone
where you want to create the managed instance group.
}

# Access instance_group_manager outputs
instance_group_manager_id = instance_group_manager.id
instance_group_manager_target_pools = instance_group_manager.target_pools
instance_group_manager_satisfies_pzi = instance_group_manager.satisfies_pzi
instance_group_manager_current_actions = instance_group_manager.current_actions
instance_group_manager_target_suspended_size = instance_group_manager.target_suspended_size
instance_group_manager_zone = instance_group_manager.zone
instance_group_manager_base_instance_name = instance_group_manager.base_instance_name
instance_group_manager_status = instance_group_manager.status
instance_group_manager_target_stopped_size = instance_group_manager.target_stopped_size
instance_group_manager_all_instances_config = instance_group_manager.all_instances_config
instance_group_manager_description = instance_group_manager.description
instance_group_manager_instance_lifecycle_policy = instance_group_manager.instance_lifecycle_policy
instance_group_manager_self_link = instance_group_manager.self_link
instance_group_manager_satisfies_pzs = instance_group_manager.satisfies_pzs
instance_group_manager_standby_policy = instance_group_manager.standby_policy
instance_group_manager_region = instance_group_manager.region
instance_group_manager_instance_template = instance_group_manager.instance_template
instance_group_manager_fingerprint = instance_group_manager.fingerprint
instance_group_manager_creation_timestamp = instance_group_manager.creation_timestamp
instance_group_manager_id = instance_group_manager.id
instance_group_manager_name = instance_group_manager.name
instance_group_manager_target_size = instance_group_manager.target_size
instance_group_manager_named_ports = instance_group_manager.named_ports
instance_group_manager_multi_mig = instance_group_manager.multi_mig
instance_group_manager_params = instance_group_manager.params
instance_group_manager_instance_flexibility_policy = instance_group_manager.instance_flexibility_policy
instance_group_manager_failover_action = instance_group_manager.failover_action
instance_group_manager_auto_healing_policies = instance_group_manager.auto_healing_policies
instance_group_manager_distribution_policy = instance_group_manager.distribution_policy
instance_group_manager_kind = instance_group_manager.kind
instance_group_manager_resource_policies = instance_group_manager.resource_policies
instance_group_manager_service_account = instance_group_manager.service_account
instance_group_manager_instance_group = instance_group_manager.instance_group
instance_group_manager_target_size_policy = instance_group_manager.target_size_policy
instance_group_manager_update_policy = instance_group_manager.update_policy
instance_group_manager_list_managed_instances_results = instance_group_manager.list_managed_instances_results
instance_group_manager_stateful_policy = instance_group_manager.stateful_policy
instance_group_manager_versions = instance_group_manager.versions
```

---


### Health_check

Creates a HealthCheck resource in the specified project using the data
included in the request.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `http_health_check` | String |  |  |
| `healthy_threshold` | i64 |  | A so-far unhealthy instance will be marked healthy after this
many consecutive successes. The default value is 2. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp in3339
text format. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `name` | String |  | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
For example, a name that is 1-63 characters long, matches the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?`, and otherwise complies with
RFC1035. This regular expression describes a name where the first
character is a lowercase letter, and all following characters are a dash,
lowercase letter, or digit, except the last character, which isn't a dash. |
| `source_regions` | Vec<String> |  | The list of cloud regions from which health checks are performed. If any
regions are specified, then exactly 3 regions should be specified. The
region names must be valid names of Google Cloud regions.
This can only be set for global health check.
If this list is non-empty, then there are restrictions
on what other health check fields are supported and what other resources
can use this health check:
   
   - SSL, HTTP2, and GRPC protocols are not supported.
   - The TCP request field is not supported.
   - The proxyHeader field for HTTP, HTTPS, and TCP is not
   supported.
   - The checkIntervalSec field must be at least 30.
   - The health check cannot be used with BackendService nor with managed
   instance group auto-healing. |
| `http2_health_check` | String |  |  |
| `kind` | String |  | Type of the resource. |
| `grpc_tls_health_check` | String |  |  |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `check_interval_sec` | i64 |  | How often (in seconds) to send a health check. The default value is 5
seconds. |
| `grpc_health_check` | String |  |  |
| `tcp_health_check` | String |  |  |
| `timeout_sec` | i64 |  | How long (in seconds) to wait before claiming failure. The default value is
5 seconds. It is invalid for timeoutSec to have greater
value than checkIntervalSec. |
| `region` | String |  | [Output Only] Region where the health check resides.  Not applicable to
global health checks. |
| `https_health_check` | String |  |  |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `type` | String |  | Specifies the type of the healthCheck, either TCP,SSL, HTTP, HTTPS,HTTP2 or GRPC. Exactly one of the
protocol-specific health check fields must be specified, which must matchtype field. |
| `ssl_health_check` | String |  |  |
| `unhealthy_threshold` | i64 |  | A so-far healthy instance will be marked unhealthy after this many
consecutive failures. The default value is 2. |
| `log_config` | String |  | Configure logging on this health check. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `http_health_check` | String |  |
| `healthy_threshold` | i64 | A so-far unhealthy instance will be marked healthy after this
many consecutive successes. The default value is 2. |
| `creation_timestamp` | String | [Output Only] Creation timestamp in3339
text format. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `name` | String | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
For example, a name that is 1-63 characters long, matches the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?`, and otherwise complies with
RFC1035. This regular expression describes a name where the first
character is a lowercase letter, and all following characters are a dash,
lowercase letter, or digit, except the last character, which isn't a dash. |
| `source_regions` | Vec<String> | The list of cloud regions from which health checks are performed. If any
regions are specified, then exactly 3 regions should be specified. The
region names must be valid names of Google Cloud regions.
This can only be set for global health check.
If this list is non-empty, then there are restrictions
on what other health check fields are supported and what other resources
can use this health check:
   
   - SSL, HTTP2, and GRPC protocols are not supported.
   - The TCP request field is not supported.
   - The proxyHeader field for HTTP, HTTPS, and TCP is not
   supported.
   - The checkIntervalSec field must be at least 30.
   - The health check cannot be used with BackendService nor with managed
   instance group auto-healing. |
| `http2_health_check` | String |  |
| `kind` | String | Type of the resource. |
| `grpc_tls_health_check` | String |  |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `check_interval_sec` | i64 | How often (in seconds) to send a health check. The default value is 5
seconds. |
| `grpc_health_check` | String |  |
| `tcp_health_check` | String |  |
| `timeout_sec` | i64 | How long (in seconds) to wait before claiming failure. The default value is
5 seconds. It is invalid for timeoutSec to have greater
value than checkIntervalSec. |
| `region` | String | [Output Only] Region where the health check resides.  Not applicable to
global health checks. |
| `https_health_check` | String |  |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `type` | String | Specifies the type of the healthCheck, either TCP,SSL, HTTP, HTTPS,HTTP2 or GRPC. Exactly one of the
protocol-specific health check fields must be specified, which must matchtype field. |
| `ssl_health_check` | String |  |
| `unhealthy_threshold` | i64 | A so-far healthy instance will be marked unhealthy after this many
consecutive failures. The default value is 2. |
| `log_config` | String | Configure logging on this health check. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create health_check
health_check = provider.compute_api.Health_check {
    project = "value"  # Project ID for this request.
}

# Access health_check outputs
health_check_id = health_check.id
health_check_http_health_check = health_check.http_health_check
health_check_healthy_threshold = health_check.healthy_threshold
health_check_creation_timestamp = health_check.creation_timestamp
health_check_id = health_check.id
health_check_name = health_check.name
health_check_source_regions = health_check.source_regions
health_check_http2_health_check = health_check.http2_health_check
health_check_kind = health_check.kind
health_check_grpc_tls_health_check = health_check.grpc_tls_health_check
health_check_description = health_check.description
health_check_check_interval_sec = health_check.check_interval_sec
health_check_grpc_health_check = health_check.grpc_health_check
health_check_tcp_health_check = health_check.tcp_health_check
health_check_timeout_sec = health_check.timeout_sec
health_check_region = health_check.region
health_check_https_health_check = health_check.https_health_check
health_check_self_link = health_check.self_link
health_check_type = health_check.type
health_check_ssl_health_check = health_check.ssl_health_check
health_check_unhealthy_threshold = health_check.unhealthy_threshold
health_check_log_config = health_check.log_config
```

---


### Snapshot_setting

Get snapshot settings.

**Operations**: ✅ Read ✅ Update

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `access_location` | String |  | (Regional snapshots use only)Policy of which location is allowed to access
snapshot. |
| `storage_location` | String |  | Policy of which storage location is going to be resolved, and additional
data that particularizes how the policy is going to be carried out. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `access_location` | String | (Regional snapshots use only)Policy of which location is allowed to access
snapshot. |
| `storage_location` | String | Policy of which storage location is going to be resolved, and additional
data that particularizes how the policy is going to be carried out. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Access snapshot_setting outputs
snapshot_setting_id = snapshot_setting.id
snapshot_setting_access_location = snapshot_setting.access_location
snapshot_setting_storage_location = snapshot_setting.storage_location
```

---


### Snapshot

Creates a snapshot in the specified project using the data included
in the request. For regular snapshot creation, consider using this method
instead of disks.createSnapshot,
as this method supports more features, such as creating snapshots in a
project different from the source disk project.

**Operations**: ✅ Create ✅ Read ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `snapshot_type` | String |  | Indicates the type of the snapshot. |
| `location_hint` | String |  | An opaque location hint used to place the snapshot close to other
resources.
This field is for use by internal tools that use the public API. |
| `region` | String |  | [Output Only] URL of the region where the snapshot resides. Only applicable
for regional snapshots. |
| `source_disk` | String |  | The source disk used to create this snapshot. |
| `source_snapshot_schedule_policy` | String |  | [Output Only] URL of the resource policy which created this
scheduled snapshot. |
| `source_instant_snapshot` | String |  | The source instant snapshot used to create this snapshot. You can provide
this as a partial or full URL to the resource. For example, the following
are valid values:
   
   
     - https://www.googleapis.com/compute/v1/projects/project/zones/zone/instantSnapshots/instantSnapshot
     - projects/project/zones/zone/instantSnapshots/instantSnapshot 
   - zones/zone/instantSnapshots/instantSnapshot |
| `architecture` | String |  | [Output Only] The architecture of the snapshot. Valid values are
ARM64 or X86_64. |
| `source_instant_snapshot_id` | String |  | [Output Only] The unique ID of the instant snapshot used to create this
snapshot. This value identifies the exact instant snapshot that was used to
create this snapshot. For example, if you created the snapshot from an
instant snapshot that was later deleted and recreated under the same name,
the source instant snapshot ID would identify the exact instant snapshot
that was used. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `source_snapshot_schedule_policy_id` | String |  | [Output Only] ID of the resource policy which created this
scheduled snapshot. |
| `enable_confidential_compute` | bool |  | Whether this snapshot is created from a confidential compute mode disk.
[Output Only]: This field is not set by user, but from source disk. |
| `creation_size_bytes` | String |  | [Output Only] Size in bytes of the snapshot at creation time. |
| `chain_name` | String |  | Creates the new snapshot in the snapshot chain labeled with the
specified name. The chain name must be 1-63 characters long and comply
with RFC1035. This is an uncommon option only for advanced service
owners who needs to create separate snapshot chains, for example,
for chargeback tracking. When you describe your snapshot resource, this
field is visible only if it has a non-empty value. |
| `source_disk_id` | String |  | [Output Only] The ID value of the disk used to create this snapshot. This
value may be used to determine whether the snapshot was taken from the
current or a previous instance of a given disk name. |
| `satisfies_pzi` | bool |  | Output only. Reserved for future use. |
| `labels` | HashMap<String, String> |  | Labels to apply to this snapshot. These can be later modified by
the setLabels method.
Label values may be empty. |
| `storage_bytes` | String |  | [Output Only] A size of the storage used by the snapshot. As snapshots
share storage, this number is expected to change with snapshot
creation/deletion. |
| `name` | String |  | Name of the resource; provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must be
a dash, lowercase letter, or digit, except the last character, which cannot
be a dash. |
| `licenses` | Vec<String> |  | [Output Only] A list of public visible licenses that apply to this
snapshot. This can be because the original image had licenses attached
(such as a Windows image). |
| `auto_created` | bool |  | [Output Only] Set to true if snapshots are automatically created by
applying resource policy on the target disk. |
| `source_disk_for_recovery_checkpoint` | String |  | The source disk whose recovery checkpoint will be used to create this
snapshot. |
| `source_disk_encryption_key` | String |  | The customer-supplied
encryption key of the source disk. Required if the source disk is
protected by a customer-supplied encryption key. |
| `snapshot_encryption_key` | String |  | Encrypts the snapshot using acustomer-supplied
encryption key.

After you encrypt a snapshot using a customer-supplied key, you must
provide the same key if you use the snapshot later. For example, you must
provide the encryption key when you create a disk from the encrypted
snapshot in a future request.

Customer-supplied encryption keys do not protect access to metadata
of the snapshot.

If you do not provide an encryption key when creating the snapshot, then
the snapshot will be encrypted using an automatically generated key and you
do not need to provide a key to use the snapshot later. |
| `user_licenses` | Vec<String> |  | [Output Only] A list of user provided licenses represented by a list of
URLs to the license resource. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `disk_size_gb` | String |  | [Output Only] Size of the source disk, specified in GB. |
| `satisfies_pzs` | bool |  | [Output Only] Reserved for future use. |
| `license_codes` | Vec<String> |  | [Output Only] Integer license codes indicating which licenses are attached
to this snapshot. |
| `status` | String |  | [Output Only] The status of the snapshot. This can beCREATING, DELETING, FAILED,READY, or UPLOADING. |
| `storage_bytes_status` | String |  | [Output Only] An indicator whether storageBytes is in a
stable state or it is being adjusted as a result of shared storage
reallocation. This status can either be UPDATING, meaning
the size of the snapshot is being updated, or UP_TO_DATE,
meaning the size of the snapshot is up-to-date. |
| `guest_flush` | bool |  | [Input Only] Whether to attempt an application consistent snapshot by
informing the OS to prepare for the snapshot process. |
| `storage_locations` | Vec<String> |  | Cloud Storage bucket storage location of the snapshot (regional or
multi-regional). |
| `download_bytes` | String |  | [Output Only] Number of bytes downloaded to restore a snapshot to a disk. |
| `label_fingerprint` | String |  | A fingerprint for the labels being applied to this snapshot, which is
essentially a hash of the labels set used for optimistic locking. The
fingerprint is initially generated by Compute Engine and changes after
every request to modify or update labels. You must always provide an
up-to-date fingerprint hash in order to update or change labels,
otherwise the request will fail with error412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve a snapshot. |
| `guest_os_features` | Vec<String> |  | [Output Only] A list of features to enable on the guest operating system.
Applicable only for bootable images. Read 
Enabling guest operating system features to see a list of available
options. |
| `params` | String |  | Input only. [Input Only] Additional params passed with the request, but not persisted
as part of resource payload. |
| `kind` | String |  | [Output Only] Type of the resource. Always compute#snapshot
for Snapshot resources. |
| `source_instant_snapshot_encryption_key` | String |  | Customer provided encryption key when creating Snapshot from Instant
Snapshot. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `snapshot_type` | String | Indicates the type of the snapshot. |
| `location_hint` | String | An opaque location hint used to place the snapshot close to other
resources.
This field is for use by internal tools that use the public API. |
| `region` | String | [Output Only] URL of the region where the snapshot resides. Only applicable
for regional snapshots. |
| `source_disk` | String | The source disk used to create this snapshot. |
| `source_snapshot_schedule_policy` | String | [Output Only] URL of the resource policy which created this
scheduled snapshot. |
| `source_instant_snapshot` | String | The source instant snapshot used to create this snapshot. You can provide
this as a partial or full URL to the resource. For example, the following
are valid values:
   
   
     - https://www.googleapis.com/compute/v1/projects/project/zones/zone/instantSnapshots/instantSnapshot
     - projects/project/zones/zone/instantSnapshots/instantSnapshot 
   - zones/zone/instantSnapshots/instantSnapshot |
| `architecture` | String | [Output Only] The architecture of the snapshot. Valid values are
ARM64 or X86_64. |
| `source_instant_snapshot_id` | String | [Output Only] The unique ID of the instant snapshot used to create this
snapshot. This value identifies the exact instant snapshot that was used to
create this snapshot. For example, if you created the snapshot from an
instant snapshot that was later deleted and recreated under the same name,
the source instant snapshot ID would identify the exact instant snapshot
that was used. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `source_snapshot_schedule_policy_id` | String | [Output Only] ID of the resource policy which created this
scheduled snapshot. |
| `enable_confidential_compute` | bool | Whether this snapshot is created from a confidential compute mode disk.
[Output Only]: This field is not set by user, but from source disk. |
| `creation_size_bytes` | String | [Output Only] Size in bytes of the snapshot at creation time. |
| `chain_name` | String | Creates the new snapshot in the snapshot chain labeled with the
specified name. The chain name must be 1-63 characters long and comply
with RFC1035. This is an uncommon option only for advanced service
owners who needs to create separate snapshot chains, for example,
for chargeback tracking. When you describe your snapshot resource, this
field is visible only if it has a non-empty value. |
| `source_disk_id` | String | [Output Only] The ID value of the disk used to create this snapshot. This
value may be used to determine whether the snapshot was taken from the
current or a previous instance of a given disk name. |
| `satisfies_pzi` | bool | Output only. Reserved for future use. |
| `labels` | HashMap<String, String> | Labels to apply to this snapshot. These can be later modified by
the setLabels method.
Label values may be empty. |
| `storage_bytes` | String | [Output Only] A size of the storage used by the snapshot. As snapshots
share storage, this number is expected to change with snapshot
creation/deletion. |
| `name` | String | Name of the resource; provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must be
a dash, lowercase letter, or digit, except the last character, which cannot
be a dash. |
| `licenses` | Vec<String> | [Output Only] A list of public visible licenses that apply to this
snapshot. This can be because the original image had licenses attached
(such as a Windows image). |
| `auto_created` | bool | [Output Only] Set to true if snapshots are automatically created by
applying resource policy on the target disk. |
| `source_disk_for_recovery_checkpoint` | String | The source disk whose recovery checkpoint will be used to create this
snapshot. |
| `source_disk_encryption_key` | String | The customer-supplied
encryption key of the source disk. Required if the source disk is
protected by a customer-supplied encryption key. |
| `snapshot_encryption_key` | String | Encrypts the snapshot using acustomer-supplied
encryption key.

After you encrypt a snapshot using a customer-supplied key, you must
provide the same key if you use the snapshot later. For example, you must
provide the encryption key when you create a disk from the encrypted
snapshot in a future request.

Customer-supplied encryption keys do not protect access to metadata
of the snapshot.

If you do not provide an encryption key when creating the snapshot, then
the snapshot will be encrypted using an automatically generated key and you
do not need to provide a key to use the snapshot later. |
| `user_licenses` | Vec<String> | [Output Only] A list of user provided licenses represented by a list of
URLs to the license resource. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `disk_size_gb` | String | [Output Only] Size of the source disk, specified in GB. |
| `satisfies_pzs` | bool | [Output Only] Reserved for future use. |
| `license_codes` | Vec<String> | [Output Only] Integer license codes indicating which licenses are attached
to this snapshot. |
| `status` | String | [Output Only] The status of the snapshot. This can beCREATING, DELETING, FAILED,READY, or UPLOADING. |
| `storage_bytes_status` | String | [Output Only] An indicator whether storageBytes is in a
stable state or it is being adjusted as a result of shared storage
reallocation. This status can either be UPDATING, meaning
the size of the snapshot is being updated, or UP_TO_DATE,
meaning the size of the snapshot is up-to-date. |
| `guest_flush` | bool | [Input Only] Whether to attempt an application consistent snapshot by
informing the OS to prepare for the snapshot process. |
| `storage_locations` | Vec<String> | Cloud Storage bucket storage location of the snapshot (regional or
multi-regional). |
| `download_bytes` | String | [Output Only] Number of bytes downloaded to restore a snapshot to a disk. |
| `label_fingerprint` | String | A fingerprint for the labels being applied to this snapshot, which is
essentially a hash of the labels set used for optimistic locking. The
fingerprint is initially generated by Compute Engine and changes after
every request to modify or update labels. You must always provide an
up-to-date fingerprint hash in order to update or change labels,
otherwise the request will fail with error412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve a snapshot. |
| `guest_os_features` | Vec<String> | [Output Only] A list of features to enable on the guest operating system.
Applicable only for bootable images. Read 
Enabling guest operating system features to see a list of available
options. |
| `params` | String | Input only. [Input Only] Additional params passed with the request, but not persisted
as part of resource payload. |
| `kind` | String | [Output Only] Type of the resource. Always compute#snapshot
for Snapshot resources. |
| `source_instant_snapshot_encryption_key` | String | Customer provided encryption key when creating Snapshot from Instant
Snapshot. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create snapshot
snapshot = provider.compute_api.Snapshot {
    project = "value"  # Project ID for this request.
}

# Access snapshot outputs
snapshot_id = snapshot.id
snapshot_snapshot_type = snapshot.snapshot_type
snapshot_location_hint = snapshot.location_hint
snapshot_region = snapshot.region
snapshot_source_disk = snapshot.source_disk
snapshot_source_snapshot_schedule_policy = snapshot.source_snapshot_schedule_policy
snapshot_source_instant_snapshot = snapshot.source_instant_snapshot
snapshot_architecture = snapshot.architecture
snapshot_source_instant_snapshot_id = snapshot.source_instant_snapshot_id
snapshot_self_link = snapshot.self_link
snapshot_source_snapshot_schedule_policy_id = snapshot.source_snapshot_schedule_policy_id
snapshot_enable_confidential_compute = snapshot.enable_confidential_compute
snapshot_creation_size_bytes = snapshot.creation_size_bytes
snapshot_chain_name = snapshot.chain_name
snapshot_source_disk_id = snapshot.source_disk_id
snapshot_satisfies_pzi = snapshot.satisfies_pzi
snapshot_labels = snapshot.labels
snapshot_storage_bytes = snapshot.storage_bytes
snapshot_name = snapshot.name
snapshot_licenses = snapshot.licenses
snapshot_auto_created = snapshot.auto_created
snapshot_source_disk_for_recovery_checkpoint = snapshot.source_disk_for_recovery_checkpoint
snapshot_source_disk_encryption_key = snapshot.source_disk_encryption_key
snapshot_snapshot_encryption_key = snapshot.snapshot_encryption_key
snapshot_user_licenses = snapshot.user_licenses
snapshot_creation_timestamp = snapshot.creation_timestamp
snapshot_description = snapshot.description
snapshot_id = snapshot.id
snapshot_disk_size_gb = snapshot.disk_size_gb
snapshot_satisfies_pzs = snapshot.satisfies_pzs
snapshot_license_codes = snapshot.license_codes
snapshot_status = snapshot.status
snapshot_storage_bytes_status = snapshot.storage_bytes_status
snapshot_guest_flush = snapshot.guest_flush
snapshot_storage_locations = snapshot.storage_locations
snapshot_download_bytes = snapshot.download_bytes
snapshot_label_fingerprint = snapshot.label_fingerprint
snapshot_guest_os_features = snapshot.guest_os_features
snapshot_params = snapshot.params
snapshot_kind = snapshot.kind
snapshot_source_instant_snapshot_encryption_key = snapshot.source_instant_snapshot_encryption_key
```

---


### Region_url_map

Creates a UrlMap resource in the specified project using
the data included in the request.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `default_custom_error_response_policy` | String |  | defaultCustomErrorResponsePolicy specifies how the Load
Balancer returns error responses when BackendServiceorBackendBucket responds with an error. 

This policy takes
effect at the load balancer level and applies only when no policy has been
defined for the error code at lower levels like PathMatcher, RouteRule and
PathRule within this UrlMap. 

For example, consider a UrlMap with the
following configuration:
   
   
     - defaultCustomErrorResponsePolicy containing policies for
     responding to 5xx and 4xx errors
     - A PathMatcher configured for *.example.com has
     defaultCustomErrorResponsePolicy for 4xx.

If a request for http://www.example.com/ encounters a404, the policy inpathMatcher.defaultCustomErrorResponsePolicy will be enforced.
When the request for http://www.example.com/ encounters a502, the policy inUrlMap.defaultCustomErrorResponsePolicy will be enforced. When
a request that does not match any host in *.example.com such
as http://www.myotherexample.com/, encounters a404, UrlMap.defaultCustomErrorResponsePolicy
takes effect. 

When used in conjunction withdefaultRouteAction.retryPolicy, retries take precedence. Only
once all retries are exhausted, thedefaultCustomErrorResponsePolicy is applied. While attempting
a retry, if load balancer is successful in reaching the
service, the defaultCustomErrorResponsePolicy is ignored and
the response from the service is returned to the client.

defaultCustomErrorResponsePolicy is supported only for
global external Application Load Balancers. |
| `default_service` | String |  | The full or partial URL of the defaultService resource to
which traffic is directed if none of the hostRules match.
If defaultRouteAction is also specified, advanced
routing actions, such as URL rewrites, take effect before sending the
request to the backend.


Only one of defaultUrlRedirect, defaultService
or defaultRouteAction.weightedBackendService can be set.


defaultService has no effect when the URL map is bound
to a target gRPC proxy that has the validateForProxyless field
set to true. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `path_matchers` | Vec<String> |  | The list of named PathMatchers to use against the URL. |
| `fingerprint` | String |  | Fingerprint of this resource. A hash of the contents stored in this object.
This field is used in optimistic locking. This field is ignored when
inserting a UrlMap. An up-to-date fingerprint must be provided
in order to update the UrlMap, otherwise the request will
fail with error 412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve a UrlMap. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `header_action` | String |  | Specifies changes to request and response headers that need to take effect
for the selected backendService.

The headerAction specified here take effect afterheaderAction specified under pathMatcher.

headerAction is not supported for load balancers
that have
their loadBalancingScheme set to EXTERNAL.

Not supported when the URL map is bound to a target gRPC proxy that
has validateForProxyless field set to true. |
| `kind` | String |  | [Output Only] Type of the resource. Always compute#urlMaps for
url maps. |
| `name` | String |  | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `tests` | Vec<String> |  | The list of expected URL mapping tests. Request to update theUrlMap succeeds only if all test cases pass. You can specify a
maximum of 100 tests per UrlMap.

Not supported when the URL map is bound to a target gRPC proxy that
has validateForProxyless field set to true. |
| `region` | String |  | [Output Only] URL of the region where the regional URL map resides.
This field is not applicable to global URL maps.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `host_rules` | Vec<String> |  | The list of host rules to use against the URL. |
| `default_route_action` | String |  | defaultRouteAction takes effect when none of the 
hostRules match. The load balancer performs advanced routing
actions, such as URL rewrites and header transformations, before forwarding
the request to the selected backend.


Only one of defaultUrlRedirect, defaultService
or defaultRouteAction.weightedBackendService can be set.


 URL maps for classic Application Load Balancers only support
the urlRewrite action within defaultRouteAction.


defaultRouteAction has no effect when the URL map is bound
to a target gRPC proxy that has the validateForProxyless field
set to true. |
| `default_url_redirect` | String |  | When none of the specified hostRules match, the request
is redirected to a URL specified by defaultUrlRedirect.


Only one of defaultUrlRedirect, defaultService
or defaultRouteAction.weightedBackendService can be set.


Not supported when the URL map is bound to a target gRPC proxy. |
| `project` | String | ✅ | Project ID for this request. |
| `region` | String | ✅ | Name of the region scoping this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `default_custom_error_response_policy` | String | defaultCustomErrorResponsePolicy specifies how the Load
Balancer returns error responses when BackendServiceorBackendBucket responds with an error. 

This policy takes
effect at the load balancer level and applies only when no policy has been
defined for the error code at lower levels like PathMatcher, RouteRule and
PathRule within this UrlMap. 

For example, consider a UrlMap with the
following configuration:
   
   
     - defaultCustomErrorResponsePolicy containing policies for
     responding to 5xx and 4xx errors
     - A PathMatcher configured for *.example.com has
     defaultCustomErrorResponsePolicy for 4xx.

If a request for http://www.example.com/ encounters a404, the policy inpathMatcher.defaultCustomErrorResponsePolicy will be enforced.
When the request for http://www.example.com/ encounters a502, the policy inUrlMap.defaultCustomErrorResponsePolicy will be enforced. When
a request that does not match any host in *.example.com such
as http://www.myotherexample.com/, encounters a404, UrlMap.defaultCustomErrorResponsePolicy
takes effect. 

When used in conjunction withdefaultRouteAction.retryPolicy, retries take precedence. Only
once all retries are exhausted, thedefaultCustomErrorResponsePolicy is applied. While attempting
a retry, if load balancer is successful in reaching the
service, the defaultCustomErrorResponsePolicy is ignored and
the response from the service is returned to the client.

defaultCustomErrorResponsePolicy is supported only for
global external Application Load Balancers. |
| `default_service` | String | The full or partial URL of the defaultService resource to
which traffic is directed if none of the hostRules match.
If defaultRouteAction is also specified, advanced
routing actions, such as URL rewrites, take effect before sending the
request to the backend.


Only one of defaultUrlRedirect, defaultService
or defaultRouteAction.weightedBackendService can be set.


defaultService has no effect when the URL map is bound
to a target gRPC proxy that has the validateForProxyless field
set to true. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `path_matchers` | Vec<String> | The list of named PathMatchers to use against the URL. |
| `fingerprint` | String | Fingerprint of this resource. A hash of the contents stored in this object.
This field is used in optimistic locking. This field is ignored when
inserting a UrlMap. An up-to-date fingerprint must be provided
in order to update the UrlMap, otherwise the request will
fail with error 412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve a UrlMap. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `header_action` | String | Specifies changes to request and response headers that need to take effect
for the selected backendService.

The headerAction specified here take effect afterheaderAction specified under pathMatcher.

headerAction is not supported for load balancers
that have
their loadBalancingScheme set to EXTERNAL.

Not supported when the URL map is bound to a target gRPC proxy that
has validateForProxyless field set to true. |
| `kind` | String | [Output Only] Type of the resource. Always compute#urlMaps for
url maps. |
| `name` | String | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `tests` | Vec<String> | The list of expected URL mapping tests. Request to update theUrlMap succeeds only if all test cases pass. You can specify a
maximum of 100 tests per UrlMap.

Not supported when the URL map is bound to a target gRPC proxy that
has validateForProxyless field set to true. |
| `region` | String | [Output Only] URL of the region where the regional URL map resides.
This field is not applicable to global URL maps.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `host_rules` | Vec<String> | The list of host rules to use against the URL. |
| `default_route_action` | String | defaultRouteAction takes effect when none of the 
hostRules match. The load balancer performs advanced routing
actions, such as URL rewrites and header transformations, before forwarding
the request to the selected backend.


Only one of defaultUrlRedirect, defaultService
or defaultRouteAction.weightedBackendService can be set.


 URL maps for classic Application Load Balancers only support
the urlRewrite action within defaultRouteAction.


defaultRouteAction has no effect when the URL map is bound
to a target gRPC proxy that has the validateForProxyless field
set to true. |
| `default_url_redirect` | String | When none of the specified hostRules match, the request
is redirected to a URL specified by defaultUrlRedirect.


Only one of defaultUrlRedirect, defaultService
or defaultRouteAction.weightedBackendService can be set.


Not supported when the URL map is bound to a target gRPC proxy. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create region_url_map
region_url_map = provider.compute_api.Region_url_map {
    project = "value"  # Project ID for this request.
    region = "value"  # Name of the region scoping this request.
}

# Access region_url_map outputs
region_url_map_id = region_url_map.id
region_url_map_self_link = region_url_map.self_link
region_url_map_default_custom_error_response_policy = region_url_map.default_custom_error_response_policy
region_url_map_default_service = region_url_map.default_service
region_url_map_description = region_url_map.description
region_url_map_path_matchers = region_url_map.path_matchers
region_url_map_fingerprint = region_url_map.fingerprint
region_url_map_creation_timestamp = region_url_map.creation_timestamp
region_url_map_header_action = region_url_map.header_action
region_url_map_kind = region_url_map.kind
region_url_map_name = region_url_map.name
region_url_map_id = region_url_map.id
region_url_map_tests = region_url_map.tests
region_url_map_region = region_url_map.region
region_url_map_host_rules = region_url_map.host_rules
region_url_map_default_route_action = region_url_map.default_route_action
region_url_map_default_url_redirect = region_url_map.default_url_redirect
```

---


### Network

Creates a network in the specified project using the data included
in the request.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `kind` | String |  | [Output Only] Type of the resource. Always compute#network for
networks. |
| `network_firewall_policy_enforcement_order` | String |  | The network firewall policy enforcement order. Can be either
AFTER_CLASSIC_FIREWALL or BEFORE_CLASSIC_FIREWALL. Defaults to
AFTER_CLASSIC_FIREWALL if the field is not specified. |
| `params` | String |  | Input only. [Input Only] Additional params passed with the request, but not persisted
as part of resource payload. |
| `subnetworks` | Vec<String> |  | [Output Only] Server-defined fully-qualified URLs for all subnetworks
in this VPC network. |
| `enable_ula_internal_ipv6` | bool |  | Enable ULA internal ipv6 on this network. Enabling this feature will assign
a /48 from google defined ULA prefix fd20::/20.
. |
| `name` | String |  | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?`. The first character must be a
lowercase letter, and all following characters (except for the last
character) must be a dash, lowercase letter, or digit. The last character
must be a lowercase letter or digit. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `firewall_policy` | String |  | [Output Only] URL of the firewall policy the network is associated with. |
| `internal_ipv6_range` | String |  | When enabling ula internal ipv6, caller optionally can specify the /48
range they want from the google defined ULA prefix fd20::/20. The input
must be a valid /48 ULA IPv6 address and must be within the fd20::/20.
Operation will fail if the speficied /48 is already in used by another
resource. If the field is not speficied, then a /48 range will be randomly
allocated from fd20::/20 and returned via this field.
. |
| `mtu` | i64 |  | Maximum Transmission Unit in bytes.
The minimum value for this field is 1300 and the maximum value is 8896.
The suggested value is 1500, which is the default MTU used on the
Internet, or 8896 if you want to use Jumbo frames. If unspecified, the
value defaults to 1460. |
| `description` | String |  | An optional description of this resource. Provide this field when you
create the resource. |
| `gateway_i_pv4` | String |  | [Output Only] The gateway address for default routing out of the network,
selected by Google Cloud. |
| `i_pv4_range` | String |  | Deprecated in favor of subnet mode networks.
The range of internal addresses that are legal on this network. This
range is aCIDR specification, for example:192.168.0.0/16. Provided by the client when the network is
created. |
| `self_link_with_id` | String |  | [Output Only] Server-defined URL for this resource with the resource id. |
| `peerings` | Vec<String> |  | [Output Only] A list of network peerings for the resource. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `auto_create_subnetworks` | bool |  | Must be set to create a VPC network. If not set, a legacy network is
created.

When set to true, the VPC network is created in auto mode.
When set to false, the VPC network is created in custom mode.

An auto mode VPC network starts with one subnet per region. Each subnet
has a predetermined range as described inAuto mode VPC network IP ranges.

For custom mode VPC networks, you can add subnets using the subnetworksinsert
method. |
| `network_profile` | String |  | A full or partial URL of the network profile to apply to this network.
This field can be set only at resource creation time. For example, the
following are valid URLs: 
   
   - https://www.googleapis.com/compute/{api_version}/projects/{project_id}/global/networkProfiles/{network_profile_name}
   - projects/{project_id}/global/networkProfiles/{network_profile_name} |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `routing_config` | String |  | The network-level routing configuration for this network.  Used by Cloud
Router to determine what type of network-wide routing behavior to enforce. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `kind` | String | [Output Only] Type of the resource. Always compute#network for
networks. |
| `network_firewall_policy_enforcement_order` | String | The network firewall policy enforcement order. Can be either
AFTER_CLASSIC_FIREWALL or BEFORE_CLASSIC_FIREWALL. Defaults to
AFTER_CLASSIC_FIREWALL if the field is not specified. |
| `params` | String | Input only. [Input Only] Additional params passed with the request, but not persisted
as part of resource payload. |
| `subnetworks` | Vec<String> | [Output Only] Server-defined fully-qualified URLs for all subnetworks
in this VPC network. |
| `enable_ula_internal_ipv6` | bool | Enable ULA internal ipv6 on this network. Enabling this feature will assign
a /48 from google defined ULA prefix fd20::/20.
. |
| `name` | String | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?`. The first character must be a
lowercase letter, and all following characters (except for the last
character) must be a dash, lowercase letter, or digit. The last character
must be a lowercase letter or digit. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `firewall_policy` | String | [Output Only] URL of the firewall policy the network is associated with. |
| `internal_ipv6_range` | String | When enabling ula internal ipv6, caller optionally can specify the /48
range they want from the google defined ULA prefix fd20::/20. The input
must be a valid /48 ULA IPv6 address and must be within the fd20::/20.
Operation will fail if the speficied /48 is already in used by another
resource. If the field is not speficied, then a /48 range will be randomly
allocated from fd20::/20 and returned via this field.
. |
| `mtu` | i64 | Maximum Transmission Unit in bytes.
The minimum value for this field is 1300 and the maximum value is 8896.
The suggested value is 1500, which is the default MTU used on the
Internet, or 8896 if you want to use Jumbo frames. If unspecified, the
value defaults to 1460. |
| `description` | String | An optional description of this resource. Provide this field when you
create the resource. |
| `gateway_i_pv4` | String | [Output Only] The gateway address for default routing out of the network,
selected by Google Cloud. |
| `i_pv4_range` | String | Deprecated in favor of subnet mode networks.
The range of internal addresses that are legal on this network. This
range is aCIDR specification, for example:192.168.0.0/16. Provided by the client when the network is
created. |
| `self_link_with_id` | String | [Output Only] Server-defined URL for this resource with the resource id. |
| `peerings` | Vec<String> | [Output Only] A list of network peerings for the resource. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `auto_create_subnetworks` | bool | Must be set to create a VPC network. If not set, a legacy network is
created.

When set to true, the VPC network is created in auto mode.
When set to false, the VPC network is created in custom mode.

An auto mode VPC network starts with one subnet per region. Each subnet
has a predetermined range as described inAuto mode VPC network IP ranges.

For custom mode VPC networks, you can add subnets using the subnetworksinsert
method. |
| `network_profile` | String | A full or partial URL of the network profile to apply to this network.
This field can be set only at resource creation time. For example, the
following are valid URLs: 
   
   - https://www.googleapis.com/compute/{api_version}/projects/{project_id}/global/networkProfiles/{network_profile_name}
   - projects/{project_id}/global/networkProfiles/{network_profile_name} |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `routing_config` | String | The network-level routing configuration for this network.  Used by Cloud
Router to determine what type of network-wide routing behavior to enforce. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create network
network = provider.compute_api.Network {
    project = "value"  # Project ID for this request.
}

# Access network outputs
network_id = network.id
network_kind = network.kind
network_network_firewall_policy_enforcement_order = network.network_firewall_policy_enforcement_order
network_params = network.params
network_subnetworks = network.subnetworks
network_enable_ula_internal_ipv6 = network.enable_ula_internal_ipv6
network_name = network.name
network_creation_timestamp = network.creation_timestamp
network_firewall_policy = network.firewall_policy
network_internal_ipv6_range = network.internal_ipv6_range
network_mtu = network.mtu
network_description = network.description
network_gateway_i_pv4 = network.gateway_i_pv4
network_i_pv4_range = network.i_pv4_range
network_self_link_with_id = network.self_link_with_id
network_peerings = network.peerings
network_self_link = network.self_link
network_auto_create_subnetworks = network.auto_create_subnetworks
network_network_profile = network.network_profile
network_id = network.id
network_routing_config = network.routing_config
```

---


### Subnetwork

Creates a subnetwork in the specified project using the data
included in the request.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `name` | String |  | The name of the resource, provided by the client when initially creating
the resource. The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `resolve_subnet_mask` | String |  | Configures subnet mask resolution for this subnetwork. |
| `stack_type` | String |  | The stack type for the subnet. If set to IPV4_ONLY, new VMs
in the subnet are assigned IPv4 addresses only. If set toIPV4_IPV6, new VMs in the subnet can be assigned both IPv4 and
IPv6 addresses. If not specified, IPV4_ONLY is used.

This field can be both set at resource creation time and updated usingpatch. |
| `purpose` | String |  |  |
| `ip_collection` | String |  | Reference to the source of IP, like a PublicDelegatedPrefix
(PDP) for BYOIP. The PDP must be a sub-PDP in
EXTERNAL_IPV6_SUBNETWORK_CREATION or INTERNAL_IPV6_SUBNETWORK_CREATION
mode.

Use one of the following formats to specify a sub-PDP when creating a dual
stack or IPv6-only subnetwork with external access using BYOIP:
   
   - 
   Full resource URL, as inhttps://www.googleapis.com/compute/v1/projects/projectId/regions/region/publicDelegatedPrefixes/sub-pdp-name
   - 
   Partial URL, as in
     
      
          - projects/projectId/regions/region/publicDelegatedPrefixes/sub-pdp-name
          - regions/region/publicDelegatedPrefixes/sub-pdp-name |
| `reserved_internal_range` | String |  | The URL of the reserved internal range. |
| `ipv6_cidr_range` | String |  | [Output Only] This field is for internal use. |
| `state` | String |  | [Output Only] The state of the subnetwork, which can be one of the
following values:READY: Subnetwork is created and ready to useDRAINING: only applicable to subnetworks that have the
purpose set to INTERNAL_HTTPS_LOAD_BALANCER and indicates that
connections to the load balancer are being drained. A subnetwork that is
draining cannot be used or modified until it reaches a status ofREADY |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `private_ip_google_access` | bool |  | Whether the VMs in this subnet can access Google services without assigned
external IP addresses. This field can be both set at resource creation
time and updated using setPrivateIpGoogleAccess. |
| `system_reserved_external_ipv6_ranges` | Vec<String> |  | Output only. [Output Only] The array of external IPv6 network ranges reserved from
the subnetwork's external IPv6 range for system use. |
| `external_ipv6_prefix` | String |  | The external IPv6 address range that is owned by this
subnetwork. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `network` | String |  | The URL of the network to which this subnetwork belongs, provided by the
client when initially creating the subnetwork. This field can be set only
at resource creation time. |
| `ip_cidr_range` | String |  | The range of internal addresses that are owned by this subnetwork.
Provide this property when you create the subnetwork. For example,10.0.0.0/8 or 100.64.0.0/10. Ranges must
be unique and non-overlapping within a network. Only IPv4 is supported.
This field is set at resource creation time. The range can be any range
listed in theValid
ranges list. The range can be expanded after creation usingexpandIpCidrRange. |
| `params` | String |  | Input only. [Input Only] Additional params passed with the request, but not persisted
as part of resource payload. |
| `secondary_ip_ranges` | Vec<String> |  | An array of configurations for secondary IP ranges for VM instances
contained in this subnetwork. The primary IP of such VM must belong to the
primary ipCidrRange of the subnetwork. The alias IPs may belong to either
primary or secondary ranges. This field can be updated with apatch request. |
| `ipv6_access_type` | String |  | The access type of IPv6 address this subnet holds. It's immutable and can
only be specified during creation or the first time the subnet is updated
into IPV4_IPV6 dual stack. |
| `log_config` | String |  | This field denotes the VPC flow logging options for this subnetwork.
If logging is enabled, logs are exported to Cloud Logging. |
| `internal_ipv6_prefix` | String |  | The internal IPv6 address range that is owned by this
subnetwork. |
| `allow_subnet_cidr_routes_overlap` | bool |  | Whether this subnetwork's ranges can conflict with existing static routes.
Setting this to true allows this subnetwork's primary and secondary ranges
to overlap with (and contain) static routes that have already been
configured on the corresponding network.

For example if a static route has range 10.1.0.0/16, a subnet
range 10.0.0.0/8 could only be created if allow_conflicting_routes=true.

Overlapping is only allowed on subnetwork operations; routes
whose ranges conflict with this subnetwork's ranges won't be allowed unless
route.allow_conflicting_subnetworks is set to true.

Typically packets destined to IPs within the subnetwork (which may contain
private/sensitive data) are prevented from leaving the virtual network.
Setting this field to true will disable this feature.

The default value is false and applies to all existing subnetworks and
automatically created subnetworks.

This field cannot be set to true at resource creation time. |
| `role` | String |  | The role of subnetwork. Currently, this field is only used when
purpose is set to GLOBAL_MANAGED_PROXY orREGIONAL_MANAGED_PROXY. The value can be set toACTIVE or BACKUP. An ACTIVE
subnetwork is one that is currently being used for Envoy-based load
balancers in a region. A BACKUP subnetwork is one that is
ready to be promoted to ACTIVE or is currently draining.
This field can be updated with a patch request. |
| `gateway_address` | String |  | [Output Only] The gateway address for default routes to reach destination
addresses outside this subnetwork. |
| `kind` | String |  | [Output Only] Type of the resource. Always compute#subnetwork
for Subnetwork resources. |
| `ipv6_gce_endpoint` | String |  | [Output Only] Possible endpoints of this subnetwork. It can be one of the
following:
   
   - VM_ONLY: The subnetwork can be used for creating instances and
   IPv6 addresses with VM endpoint type. Such a subnetwork gets external IPv6
   ranges from a public delegated prefix and cannot be used to create NetLb.
   - VM_AND_FR: The subnetwork can be used for creating both VM
   instances and Forwarding Rules. It can also be used to reserve IPv6
   addresses with both VM and FR endpoint types. Such a subnetwork gets its
   IPv6 range from Google IP Pool directly. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. This field can be set only at resource creation time. |
| `utilization_details` | String |  | Output only. [Output Only] The current IP utilization of all subnetwork ranges. Contains
the total number of allocated and free IPs in each range. |
| `region` | String |  | URL of the region where the Subnetwork resides. This
field can be set only at resource creation time. |
| `enable_flow_logs` | bool |  | Whether to enable flow logging for this subnetwork. If this field is not
explicitly set, it will not appear in get listings. If not set
the default behavior is determined by the org policy, if there is no org
policy specified, then it will default to disabled. This field isn't
supported if the subnet purpose field is set toREGIONAL_MANAGED_PROXY. |
| `system_reserved_internal_ipv6_ranges` | Vec<String> |  | Output only. [Output Only] The array of internal IPv6 network ranges reserved from
the subnetwork's internal IPv6 range for system use. |
| `fingerprint` | String |  | Fingerprint of this resource. A hash of the contents stored in this object.
This field is used in optimistic locking. This field will be ignored when
inserting a Subnetwork. An up-to-date fingerprint must be
provided in order to update the Subnetwork, otherwise the
request will fail with error 412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve a Subnetwork. |
| `private_ipv6_google_access` | String |  | This field is for internal use.

This field can be both set at resource creation time and updated usingpatch. |
| `project` | String | ✅ | Project ID for this request. |
| `region` | String | ✅ | Name of the region scoping this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `name` | String | The name of the resource, provided by the client when initially creating
the resource. The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `resolve_subnet_mask` | String | Configures subnet mask resolution for this subnetwork. |
| `stack_type` | String | The stack type for the subnet. If set to IPV4_ONLY, new VMs
in the subnet are assigned IPv4 addresses only. If set toIPV4_IPV6, new VMs in the subnet can be assigned both IPv4 and
IPv6 addresses. If not specified, IPV4_ONLY is used.

This field can be both set at resource creation time and updated usingpatch. |
| `purpose` | String |  |
| `ip_collection` | String | Reference to the source of IP, like a PublicDelegatedPrefix
(PDP) for BYOIP. The PDP must be a sub-PDP in
EXTERNAL_IPV6_SUBNETWORK_CREATION or INTERNAL_IPV6_SUBNETWORK_CREATION
mode.

Use one of the following formats to specify a sub-PDP when creating a dual
stack or IPv6-only subnetwork with external access using BYOIP:
   
   - 
   Full resource URL, as inhttps://www.googleapis.com/compute/v1/projects/projectId/regions/region/publicDelegatedPrefixes/sub-pdp-name
   - 
   Partial URL, as in
     
      
          - projects/projectId/regions/region/publicDelegatedPrefixes/sub-pdp-name
          - regions/region/publicDelegatedPrefixes/sub-pdp-name |
| `reserved_internal_range` | String | The URL of the reserved internal range. |
| `ipv6_cidr_range` | String | [Output Only] This field is for internal use. |
| `state` | String | [Output Only] The state of the subnetwork, which can be one of the
following values:READY: Subnetwork is created and ready to useDRAINING: only applicable to subnetworks that have the
purpose set to INTERNAL_HTTPS_LOAD_BALANCER and indicates that
connections to the load balancer are being drained. A subnetwork that is
draining cannot be used or modified until it reaches a status ofREADY |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `private_ip_google_access` | bool | Whether the VMs in this subnet can access Google services without assigned
external IP addresses. This field can be both set at resource creation
time and updated using setPrivateIpGoogleAccess. |
| `system_reserved_external_ipv6_ranges` | Vec<String> | Output only. [Output Only] The array of external IPv6 network ranges reserved from
the subnetwork's external IPv6 range for system use. |
| `external_ipv6_prefix` | String | The external IPv6 address range that is owned by this
subnetwork. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `network` | String | The URL of the network to which this subnetwork belongs, provided by the
client when initially creating the subnetwork. This field can be set only
at resource creation time. |
| `ip_cidr_range` | String | The range of internal addresses that are owned by this subnetwork.
Provide this property when you create the subnetwork. For example,10.0.0.0/8 or 100.64.0.0/10. Ranges must
be unique and non-overlapping within a network. Only IPv4 is supported.
This field is set at resource creation time. The range can be any range
listed in theValid
ranges list. The range can be expanded after creation usingexpandIpCidrRange. |
| `params` | String | Input only. [Input Only] Additional params passed with the request, but not persisted
as part of resource payload. |
| `secondary_ip_ranges` | Vec<String> | An array of configurations for secondary IP ranges for VM instances
contained in this subnetwork. The primary IP of such VM must belong to the
primary ipCidrRange of the subnetwork. The alias IPs may belong to either
primary or secondary ranges. This field can be updated with apatch request. |
| `ipv6_access_type` | String | The access type of IPv6 address this subnet holds. It's immutable and can
only be specified during creation or the first time the subnet is updated
into IPV4_IPV6 dual stack. |
| `log_config` | String | This field denotes the VPC flow logging options for this subnetwork.
If logging is enabled, logs are exported to Cloud Logging. |
| `internal_ipv6_prefix` | String | The internal IPv6 address range that is owned by this
subnetwork. |
| `allow_subnet_cidr_routes_overlap` | bool | Whether this subnetwork's ranges can conflict with existing static routes.
Setting this to true allows this subnetwork's primary and secondary ranges
to overlap with (and contain) static routes that have already been
configured on the corresponding network.

For example if a static route has range 10.1.0.0/16, a subnet
range 10.0.0.0/8 could only be created if allow_conflicting_routes=true.

Overlapping is only allowed on subnetwork operations; routes
whose ranges conflict with this subnetwork's ranges won't be allowed unless
route.allow_conflicting_subnetworks is set to true.

Typically packets destined to IPs within the subnetwork (which may contain
private/sensitive data) are prevented from leaving the virtual network.
Setting this field to true will disable this feature.

The default value is false and applies to all existing subnetworks and
automatically created subnetworks.

This field cannot be set to true at resource creation time. |
| `role` | String | The role of subnetwork. Currently, this field is only used when
purpose is set to GLOBAL_MANAGED_PROXY orREGIONAL_MANAGED_PROXY. The value can be set toACTIVE or BACKUP. An ACTIVE
subnetwork is one that is currently being used for Envoy-based load
balancers in a region. A BACKUP subnetwork is one that is
ready to be promoted to ACTIVE or is currently draining.
This field can be updated with a patch request. |
| `gateway_address` | String | [Output Only] The gateway address for default routes to reach destination
addresses outside this subnetwork. |
| `kind` | String | [Output Only] Type of the resource. Always compute#subnetwork
for Subnetwork resources. |
| `ipv6_gce_endpoint` | String | [Output Only] Possible endpoints of this subnetwork. It can be one of the
following:
   
   - VM_ONLY: The subnetwork can be used for creating instances and
   IPv6 addresses with VM endpoint type. Such a subnetwork gets external IPv6
   ranges from a public delegated prefix and cannot be used to create NetLb.
   - VM_AND_FR: The subnetwork can be used for creating both VM
   instances and Forwarding Rules. It can also be used to reserve IPv6
   addresses with both VM and FR endpoint types. Such a subnetwork gets its
   IPv6 range from Google IP Pool directly. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. This field can be set only at resource creation time. |
| `utilization_details` | String | Output only. [Output Only] The current IP utilization of all subnetwork ranges. Contains
the total number of allocated and free IPs in each range. |
| `region` | String | URL of the region where the Subnetwork resides. This
field can be set only at resource creation time. |
| `enable_flow_logs` | bool | Whether to enable flow logging for this subnetwork. If this field is not
explicitly set, it will not appear in get listings. If not set
the default behavior is determined by the org policy, if there is no org
policy specified, then it will default to disabled. This field isn't
supported if the subnet purpose field is set toREGIONAL_MANAGED_PROXY. |
| `system_reserved_internal_ipv6_ranges` | Vec<String> | Output only. [Output Only] The array of internal IPv6 network ranges reserved from
the subnetwork's internal IPv6 range for system use. |
| `fingerprint` | String | Fingerprint of this resource. A hash of the contents stored in this object.
This field is used in optimistic locking. This field will be ignored when
inserting a Subnetwork. An up-to-date fingerprint must be
provided in order to update the Subnetwork, otherwise the
request will fail with error 412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve a Subnetwork. |
| `private_ipv6_google_access` | String | This field is for internal use.

This field can be both set at resource creation time and updated usingpatch. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create subnetwork
subnetwork = provider.compute_api.Subnetwork {
    project = "value"  # Project ID for this request.
    region = "value"  # Name of the region scoping this request.
}

# Access subnetwork outputs
subnetwork_id = subnetwork.id
subnetwork_name = subnetwork.name
subnetwork_resolve_subnet_mask = subnetwork.resolve_subnet_mask
subnetwork_stack_type = subnetwork.stack_type
subnetwork_purpose = subnetwork.purpose
subnetwork_ip_collection = subnetwork.ip_collection
subnetwork_reserved_internal_range = subnetwork.reserved_internal_range
subnetwork_ipv6_cidr_range = subnetwork.ipv6_cidr_range
subnetwork_state = subnetwork.state
subnetwork_creation_timestamp = subnetwork.creation_timestamp
subnetwork_private_ip_google_access = subnetwork.private_ip_google_access
subnetwork_system_reserved_external_ipv6_ranges = subnetwork.system_reserved_external_ipv6_ranges
subnetwork_external_ipv6_prefix = subnetwork.external_ipv6_prefix
subnetwork_id = subnetwork.id
subnetwork_network = subnetwork.network
subnetwork_ip_cidr_range = subnetwork.ip_cidr_range
subnetwork_params = subnetwork.params
subnetwork_secondary_ip_ranges = subnetwork.secondary_ip_ranges
subnetwork_ipv6_access_type = subnetwork.ipv6_access_type
subnetwork_log_config = subnetwork.log_config
subnetwork_internal_ipv6_prefix = subnetwork.internal_ipv6_prefix
subnetwork_allow_subnet_cidr_routes_overlap = subnetwork.allow_subnet_cidr_routes_overlap
subnetwork_role = subnetwork.role
subnetwork_gateway_address = subnetwork.gateway_address
subnetwork_kind = subnetwork.kind
subnetwork_ipv6_gce_endpoint = subnetwork.ipv6_gce_endpoint
subnetwork_self_link = subnetwork.self_link
subnetwork_description = subnetwork.description
subnetwork_utilization_details = subnetwork.utilization_details
subnetwork_region = subnetwork.region
subnetwork_enable_flow_logs = subnetwork.enable_flow_logs
subnetwork_system_reserved_internal_ipv6_ranges = subnetwork.system_reserved_internal_ipv6_ranges
subnetwork_fingerprint = subnetwork.fingerprint
subnetwork_private_ipv6_google_access = subnetwork.private_ipv6_google_access
```

---


### Machine_type

Returns the specified machine type.

**Operations**: ✅ Read

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `guest_cpus` | i64 | [Output Only] The number of virtual CPUs that are available to the
instance. |
| `accelerators` | Vec<String> | [Output Only] A list of accelerator configurations assigned to this
machine type. |
| `memory_mb` | i64 | [Output Only] The amount of physical memory available to the instance,
defined in MB. |
| `name` | String | [Output Only] Name of the resource. |
| `architecture` | String | [Output Only] The architecture of the machine type. |
| `is_shared_cpu` | bool | [Output Only] Whether this machine type has a shared CPU. SeeShared-core machine
types for more information. |
| `zone` | String | [Output Only] The name of the zone where the machine type resides,
such as us-central1-a. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `description` | String | [Output Only] An optional textual description of the resource. |
| `deprecated` | String | [Output Only] The deprecation status associated with this machine type.
Only applicable if the machine type is unavailable. |
| `bundled_local_ssds` | String | [Output Only] The configuration of bundled local SSD for the machine type. |
| `kind` | String | [Output Only] The type of the resource. Alwayscompute#machineType for machine types. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `maximum_persistent_disks_size_gb` | String | [Output Only] Maximum total persistent disks size (GB) allowed. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `maximum_persistent_disks` | i64 | [Output Only] Maximum persistent disks allowed. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Access machine_type outputs
machine_type_id = machine_type.id
machine_type_guest_cpus = machine_type.guest_cpus
machine_type_accelerators = machine_type.accelerators
machine_type_memory_mb = machine_type.memory_mb
machine_type_name = machine_type.name
machine_type_architecture = machine_type.architecture
machine_type_is_shared_cpu = machine_type.is_shared_cpu
machine_type_zone = machine_type.zone
machine_type_id = machine_type.id
machine_type_description = machine_type.description
machine_type_deprecated = machine_type.deprecated
machine_type_bundled_local_ssds = machine_type.bundled_local_ssds
machine_type_kind = machine_type.kind
machine_type_self_link = machine_type.self_link
machine_type_maximum_persistent_disks_size_gb = machine_type.maximum_persistent_disks_size_gb
machine_type_creation_timestamp = machine_type.creation_timestamp
machine_type_maximum_persistent_disks = machine_type.maximum_persistent_disks
```

---


### Interconnect_remote_location

Returns the details for the specified interconnect remote location. Gets a
list of available interconnect remote locations by making alist() request.

**Operations**: ✅ Read

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `name` | String | [Output Only] Name of the resource. |
| `permitted_connections` | Vec<String> | [Output Only] Permitted connections. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `max_lag_size100_gbps` | i64 | [Output Only]
The maximum number of 100 Gbps ports supported in a link aggregation group
(LAG). When linkType is 100 Gbps, requestedLinkCount cannot exceed
max_lag_size_100_gbps. |
| `status` | String | [Output Only] The status of this InterconnectRemoteLocation, which can take
one of the following values:
   
   - CLOSED: The InterconnectRemoteLocation is closed and is unavailable
   for provisioning new Cross-Cloud Interconnects. 
   - AVAILABLE: The
   InterconnectRemoteLocation is available for provisioning new
   Cross-Cloud Interconnects. |
| `address` | String | [Output Only] The postal address of the Point of Presence, each line in
the address is separated by a newline character. |
| `lacp` | String | [Output Only] Link Aggregation Control Protocol (LACP) constraints, which
can take one of the following values: LACP_SUPPORTED, LACP_UNSUPPORTED |
| `city` | String | [Output Only] Metropolitan area designator that indicates which city an
interconnect is located.
For example: "Chicago, IL", "Amsterdam, Netherlands". |
| `peeringdb_facility_id` | String | [Output Only] The peeringdb identifier for this facility (corresponding
with a netfac type in peeringdb). |
| `description` | String | [Output Only] An optional description of the resource. |
| `remote_service` | String | [Output Only] Indicates the service provider present at the remote
location. Example values: "Amazon Web Services", "Microsoft Azure". |
| `facility_provider` | String | [Output Only] The name of the provider for this facility (e.g., EQUINIX). |
| `continent` | String | [Output Only] Continent for this location, which can take one of the
following values:
   
   - AFRICA
   - ASIA_PAC
   - EUROPE
   - NORTH_AMERICA
   - SOUTH_AMERICA |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `attachment_configuration_constraints` | String | [Output Only] Subset of fields from InterconnectAttachment's
|configurationConstraints| field that apply to all attachments for this
remote location. |
| `constraints` | String | [Output Only] Constraints on the parameters for creating Cross-Cloud
Interconnect and associated InterconnectAttachments. |
| `kind` | String | [Output Only] Type of the resource. Alwayscompute#interconnectRemoteLocation for interconnect remote
locations. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `facility_provider_facility_id` | String | [Output Only] A provider-assigned Identifier for this facility (e.g.,
Ashburn-DC1). |
| `max_lag_size10_gbps` | i64 | [Output Only]
The maximum number of 10 Gbps ports supported in a link aggregation group
(LAG). When linkType is 10 Gbps, requestedLinkCount cannot exceed
max_lag_size_10_gbps. |
| `max_lag_size400_gbps` | i64 | [Output Only]
The maximum number of 400 Gbps ports supported in a link aggregation group
(LAG). When linkType is 400 Gbps, requestedLinkCount cannot exceed
max_lag_size_400_gbps. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Access interconnect_remote_location outputs
interconnect_remote_location_id = interconnect_remote_location.id
interconnect_remote_location_name = interconnect_remote_location.name
interconnect_remote_location_permitted_connections = interconnect_remote_location.permitted_connections
interconnect_remote_location_self_link = interconnect_remote_location.self_link
interconnect_remote_location_max_lag_size100_gbps = interconnect_remote_location.max_lag_size100_gbps
interconnect_remote_location_status = interconnect_remote_location.status
interconnect_remote_location_address = interconnect_remote_location.address
interconnect_remote_location_lacp = interconnect_remote_location.lacp
interconnect_remote_location_city = interconnect_remote_location.city
interconnect_remote_location_peeringdb_facility_id = interconnect_remote_location.peeringdb_facility_id
interconnect_remote_location_description = interconnect_remote_location.description
interconnect_remote_location_remote_service = interconnect_remote_location.remote_service
interconnect_remote_location_facility_provider = interconnect_remote_location.facility_provider
interconnect_remote_location_continent = interconnect_remote_location.continent
interconnect_remote_location_creation_timestamp = interconnect_remote_location.creation_timestamp
interconnect_remote_location_attachment_configuration_constraints = interconnect_remote_location.attachment_configuration_constraints
interconnect_remote_location_constraints = interconnect_remote_location.constraints
interconnect_remote_location_kind = interconnect_remote_location.kind
interconnect_remote_location_id = interconnect_remote_location.id
interconnect_remote_location_facility_provider_facility_id = interconnect_remote_location.facility_provider_facility_id
interconnect_remote_location_max_lag_size10_gbps = interconnect_remote_location.max_lag_size10_gbps
interconnect_remote_location_max_lag_size400_gbps = interconnect_remote_location.max_lag_size400_gbps
```

---


### Instant_snapshot

Creates an instant snapshot in the specified zone.

**Operations**: ✅ Create ✅ Read ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `name` | String |  | Name of the resource; provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must be
a dash, lowercase letter, or digit, except the last character, which cannot
be a dash. |
| `zone` | String |  | [Output Only] URL of the zone where the instant snapshot resides.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `label_fingerprint` | String |  | A fingerprint for the labels being applied to this InstantSnapshot, which
is essentially a hash of the labels set used for optimistic locking. The
fingerprint is initially generated by Compute Engine and changes after
every request to modify or update labels. You must always provide an
up-to-date fingerprint hash in order to update or change labels,
otherwise the request will fail with error412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve a InstantSnapshot. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `satisfies_pzi` | bool |  | Output only. Reserved for future use. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `source_disk_id` | String |  | [Output Only] The ID value of the disk used to create this InstantSnapshot.
This value may be used to determine whether the InstantSnapshot
was taken from the current or a previous instance of a given disk name. |
| `region` | String |  | [Output Only] URL of the region where the instant snapshot resides.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `self_link_with_id` | String |  | [Output Only] Server-defined URL for this resource's resource id. |
| `source_disk` | String |  | URL of the source disk used to create this instant snapshot.
Note that the source disk must be in the same zone/region as the
instant snapshot to be created. This can be a full or valid partial URL.
For example, the following are valid values:
   
   
     - 
       https://www.googleapis.com/compute/v1/projects/project/zones/zone/disks/disk
     
     - 
       https://www.googleapis.com/compute/v1/projects/project/regions/region/disks/disk
     
     - 
       projects/project/zones/zone/disks/disk
     
     - 
       projects/project/regions/region/disks/disk
     
     - 
       zones/zone/disks/disk
     
     - 
       regions/region/disks/disk |
| `satisfies_pzs` | bool |  | [Output Only] Reserved for future use. |
| `architecture` | String |  | [Output Only] The architecture of the instant snapshot. Valid values are
ARM64 or X86_64. |
| `kind` | String |  | [Output Only] Type of the resource. Alwayscompute#instantSnapshot for InstantSnapshot resources. |
| `resource_status` | String |  | [Output Only] Status information for the instant snapshot resource. |
| `disk_size_gb` | String |  | [Output Only] Size of the source disk, specified in GB. |
| `labels` | HashMap<String, String> |  | Labels to apply to this InstantSnapshot. These can be later modified by
the setLabels method.
Label values may be empty. |
| `status` | String |  | [Output Only] The status of the instantSnapshot. This can beCREATING, DELETING, FAILED, orREADY. |
| `zone` | String | ✅ | Name of the zone for this request. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `name` | String | Name of the resource; provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must be
a dash, lowercase letter, or digit, except the last character, which cannot
be a dash. |
| `zone` | String | [Output Only] URL of the zone where the instant snapshot resides.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `label_fingerprint` | String | A fingerprint for the labels being applied to this InstantSnapshot, which
is essentially a hash of the labels set used for optimistic locking. The
fingerprint is initially generated by Compute Engine and changes after
every request to modify or update labels. You must always provide an
up-to-date fingerprint hash in order to update or change labels,
otherwise the request will fail with error412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve a InstantSnapshot. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `satisfies_pzi` | bool | Output only. Reserved for future use. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `source_disk_id` | String | [Output Only] The ID value of the disk used to create this InstantSnapshot.
This value may be used to determine whether the InstantSnapshot
was taken from the current or a previous instance of a given disk name. |
| `region` | String | [Output Only] URL of the region where the instant snapshot resides.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `self_link_with_id` | String | [Output Only] Server-defined URL for this resource's resource id. |
| `source_disk` | String | URL of the source disk used to create this instant snapshot.
Note that the source disk must be in the same zone/region as the
instant snapshot to be created. This can be a full or valid partial URL.
For example, the following are valid values:
   
   
     - 
       https://www.googleapis.com/compute/v1/projects/project/zones/zone/disks/disk
     
     - 
       https://www.googleapis.com/compute/v1/projects/project/regions/region/disks/disk
     
     - 
       projects/project/zones/zone/disks/disk
     
     - 
       projects/project/regions/region/disks/disk
     
     - 
       zones/zone/disks/disk
     
     - 
       regions/region/disks/disk |
| `satisfies_pzs` | bool | [Output Only] Reserved for future use. |
| `architecture` | String | [Output Only] The architecture of the instant snapshot. Valid values are
ARM64 or X86_64. |
| `kind` | String | [Output Only] Type of the resource. Alwayscompute#instantSnapshot for InstantSnapshot resources. |
| `resource_status` | String | [Output Only] Status information for the instant snapshot resource. |
| `disk_size_gb` | String | [Output Only] Size of the source disk, specified in GB. |
| `labels` | HashMap<String, String> | Labels to apply to this InstantSnapshot. These can be later modified by
the setLabels method.
Label values may be empty. |
| `status` | String | [Output Only] The status of the instantSnapshot. This can beCREATING, DELETING, FAILED, orREADY. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create instant_snapshot
instant_snapshot = provider.compute_api.Instant_snapshot {
    zone = "value"  # Name of the zone for this request.
    project = "value"  # Project ID for this request.
}

# Access instant_snapshot outputs
instant_snapshot_id = instant_snapshot.id
instant_snapshot_name = instant_snapshot.name
instant_snapshot_zone = instant_snapshot.zone
instant_snapshot_label_fingerprint = instant_snapshot.label_fingerprint
instant_snapshot_description = instant_snapshot.description
instant_snapshot_satisfies_pzi = instant_snapshot.satisfies_pzi
instant_snapshot_self_link = instant_snapshot.self_link
instant_snapshot_id = instant_snapshot.id
instant_snapshot_source_disk_id = instant_snapshot.source_disk_id
instant_snapshot_region = instant_snapshot.region
instant_snapshot_creation_timestamp = instant_snapshot.creation_timestamp
instant_snapshot_self_link_with_id = instant_snapshot.self_link_with_id
instant_snapshot_source_disk = instant_snapshot.source_disk
instant_snapshot_satisfies_pzs = instant_snapshot.satisfies_pzs
instant_snapshot_architecture = instant_snapshot.architecture
instant_snapshot_kind = instant_snapshot.kind
instant_snapshot_resource_status = instant_snapshot.resource_status
instant_snapshot_disk_size_gb = instant_snapshot.disk_size_gb
instant_snapshot_labels = instant_snapshot.labels
instant_snapshot_status = instant_snapshot.status
```

---


### Ssl_policie

Returns the specified SSL policy resource.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `name` | String |  | Name of the resource. The name must be 1-63 characters long, and comply
with RFC1035. Specifically, the name must be 1-63 characters
long and match the regular expression
`[a-z]([-a-z0-9]*[a-z0-9])?` which means the first character
must be a lowercase letter, and all following characters must be a dash,
lowercase letter, or digit, except the last character, which cannot be a
dash. |
| `custom_features` | Vec<String> |  | A list of features enabled when the selected profile is CUSTOM. The
 method returns the set of features that can be
specified in this list. This field must be empty if the profile is notCUSTOM. |
| `enabled_features` | Vec<String> |  | [Output Only] The list of features enabled in the SSL policy. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `kind` | String |  | [Output only] Type of the resource. Alwayscompute#sslPolicyfor SSL policies. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `min_tls_version` | String |  | The minimum version of SSL protocol that can be used by the clients to
establish a connection with the load balancer. This can be one ofTLS_1_0, TLS_1_1, TLS_1_2,TLS_1_3. When set to TLS_1_3, the profile field
must be set to RESTRICTED. |
| `profile` | String |  | Profile specifies the set of SSL features that can be used by the load
balancer when negotiating SSL with clients. This can be one ofCOMPATIBLE, MODERN, RESTRICTED, orCUSTOM. If using CUSTOM, the set of SSL features
to enable must be specified in the customFeatures field. |
| `region` | String |  | [Output Only] URL of the region where the regional SSL policy
resides. This field is not applicable to global SSL policies. |
| `fingerprint` | String |  | Fingerprint of this resource. A hash of the contents stored in this object.
This field is used in optimistic locking. This field will be ignored when
inserting a SslPolicy. An up-to-date fingerprint must be
provided in order to update the SslPolicy, otherwise the
request will fail with error 412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve an SslPolicy. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `warnings` | Vec<String> |  | [Output Only] If potential misconfigurations are detected for this
SSL policy, this field will be populated with warning messages. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `name` | String | Name of the resource. The name must be 1-63 characters long, and comply
with RFC1035. Specifically, the name must be 1-63 characters
long and match the regular expression
`[a-z]([-a-z0-9]*[a-z0-9])?` which means the first character
must be a lowercase letter, and all following characters must be a dash,
lowercase letter, or digit, except the last character, which cannot be a
dash. |
| `custom_features` | Vec<String> | A list of features enabled when the selected profile is CUSTOM. The
 method returns the set of features that can be
specified in this list. This field must be empty if the profile is notCUSTOM. |
| `enabled_features` | Vec<String> | [Output Only] The list of features enabled in the SSL policy. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `kind` | String | [Output only] Type of the resource. Alwayscompute#sslPolicyfor SSL policies. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `min_tls_version` | String | The minimum version of SSL protocol that can be used by the clients to
establish a connection with the load balancer. This can be one ofTLS_1_0, TLS_1_1, TLS_1_2,TLS_1_3. When set to TLS_1_3, the profile field
must be set to RESTRICTED. |
| `profile` | String | Profile specifies the set of SSL features that can be used by the load
balancer when negotiating SSL with clients. This can be one ofCOMPATIBLE, MODERN, RESTRICTED, orCUSTOM. If using CUSTOM, the set of SSL features
to enable must be specified in the customFeatures field. |
| `region` | String | [Output Only] URL of the region where the regional SSL policy
resides. This field is not applicable to global SSL policies. |
| `fingerprint` | String | Fingerprint of this resource. A hash of the contents stored in this object.
This field is used in optimistic locking. This field will be ignored when
inserting a SslPolicy. An up-to-date fingerprint must be
provided in order to update the SslPolicy, otherwise the
request will fail with error 412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve an SslPolicy. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `warnings` | Vec<String> | [Output Only] If potential misconfigurations are detected for this
SSL policy, this field will be populated with warning messages. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create ssl_policie
ssl_policie = provider.compute_api.Ssl_policie {
    project = "value"  # Project ID for this request.
}

# Access ssl_policie outputs
ssl_policie_id = ssl_policie.id
ssl_policie_name = ssl_policie.name
ssl_policie_custom_features = ssl_policie.custom_features
ssl_policie_enabled_features = ssl_policie.enabled_features
ssl_policie_creation_timestamp = ssl_policie.creation_timestamp
ssl_policie_kind = ssl_policie.kind
ssl_policie_id = ssl_policie.id
ssl_policie_min_tls_version = ssl_policie.min_tls_version
ssl_policie_profile = ssl_policie.profile
ssl_policie_region = ssl_policie.region
ssl_policie_fingerprint = ssl_policie.fingerprint
ssl_policie_description = ssl_policie.description
ssl_policie_warnings = ssl_policie.warnings
ssl_policie_self_link = ssl_policie.self_link
```

---


### Instance_setting

Get Instance settings.

**Operations**: ✅ Read ✅ Update

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `kind` | String |  | [Output Only] Type of the resource. Alwayscompute#instance_settings for instance settings. |
| `fingerprint` | String |  | Specifies a fingerprint for instance settings, which is essentially a hash
of the instance settings resource's contents and used for optimistic
locking. The fingerprint is initially generated by Compute Engine and
changes after every request to modify or update the instance settings
resource. You must always provide an up-to-date fingerprint hash in order
to update or change the resource, otherwise the request will fail with
error 412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve the resource. |
| `metadata` | String |  | The metadata key/value pairs assigned to all the instances in the
corresponding scope. |
| `zone` | String |  | [Output Only] URL of the zone where the resource resides
You must specify this field as part of the HTTP request URL. It is not
settable as a field in the request body. |
| `project` | String | ✅ | Project ID for this request. |
| `zone` | String | ✅ | The zone scoping this request. It should conform to RFC1035. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `kind` | String | [Output Only] Type of the resource. Alwayscompute#instance_settings for instance settings. |
| `fingerprint` | String | Specifies a fingerprint for instance settings, which is essentially a hash
of the instance settings resource's contents and used for optimistic
locking. The fingerprint is initially generated by Compute Engine and
changes after every request to modify or update the instance settings
resource. You must always provide an up-to-date fingerprint hash in order
to update or change the resource, otherwise the request will fail with
error 412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve the resource. |
| `metadata` | String | The metadata key/value pairs assigned to all the instances in the
corresponding scope. |
| `zone` | String | [Output Only] URL of the zone where the resource resides
You must specify this field as part of the HTTP request URL. It is not
settable as a field in the request body. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Access instance_setting outputs
instance_setting_id = instance_setting.id
instance_setting_kind = instance_setting.kind
instance_setting_fingerprint = instance_setting.fingerprint
instance_setting_metadata = instance_setting.metadata
instance_setting_zone = instance_setting.zone
```

---


### Ssl_certificate

Creates a SslCertificate resource in the specified project using the data
included in the request.

**Operations**: ✅ Create ✅ Read ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `private_key` | String |  | A value read into memory from a write-only private key file. The private
key file must be in PEM format. For security, only insert
requests include this field. |
| `subject_alternative_names` | Vec<String> |  | [Output Only] Domains associated with the certificate via Subject
Alternative Name. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `name` | String |  | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `kind` | String |  | [Output Only] Type of the resource. Alwayscompute#sslCertificate for SSL certificates. |
| `self_link` | String |  | [Output only] Server-defined URL for the resource. |
| `expire_time` | String |  | [Output Only] Expire time of the certificate. RFC3339 |
| `self_managed` | String |  | Configuration and status of a self-managed SSL certificate. |
| `type` | String |  | (Optional) Specifies the type of SSL certificate, either "SELF_MANAGED" or
"MANAGED". If not specified, the certificate is self-managed and the fieldscertificate and private_key are used. |
| `certificate` | String |  | A value read into memory from a certificate file. The certificate file must
be in PEM format. The certificate chain must be no greater than 5 certs
long. The chain must include at least one intermediate cert. |
| `region` | String |  | [Output Only] URL of the region where the regional SSL Certificate
resides. This field is not applicable to global SSL Certificate. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `managed` | String |  | Configuration and status of a managed SSL certificate. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `private_key` | String | A value read into memory from a write-only private key file. The private
key file must be in PEM format. For security, only insert
requests include this field. |
| `subject_alternative_names` | Vec<String> | [Output Only] Domains associated with the certificate via Subject
Alternative Name. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `name` | String | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `kind` | String | [Output Only] Type of the resource. Alwayscompute#sslCertificate for SSL certificates. |
| `self_link` | String | [Output only] Server-defined URL for the resource. |
| `expire_time` | String | [Output Only] Expire time of the certificate. RFC3339 |
| `self_managed` | String | Configuration and status of a self-managed SSL certificate. |
| `type` | String | (Optional) Specifies the type of SSL certificate, either "SELF_MANAGED" or
"MANAGED". If not specified, the certificate is self-managed and the fieldscertificate and private_key are used. |
| `certificate` | String | A value read into memory from a certificate file. The certificate file must
be in PEM format. The certificate chain must be no greater than 5 certs
long. The chain must include at least one intermediate cert. |
| `region` | String | [Output Only] URL of the region where the regional SSL Certificate
resides. This field is not applicable to global SSL Certificate. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `managed` | String | Configuration and status of a managed SSL certificate. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create ssl_certificate
ssl_certificate = provider.compute_api.Ssl_certificate {
    project = "value"  # Project ID for this request.
}

# Access ssl_certificate outputs
ssl_certificate_id = ssl_certificate.id
ssl_certificate_private_key = ssl_certificate.private_key
ssl_certificate_subject_alternative_names = ssl_certificate.subject_alternative_names
ssl_certificate_id = ssl_certificate.id
ssl_certificate_creation_timestamp = ssl_certificate.creation_timestamp
ssl_certificate_name = ssl_certificate.name
ssl_certificate_kind = ssl_certificate.kind
ssl_certificate_self_link = ssl_certificate.self_link
ssl_certificate_expire_time = ssl_certificate.expire_time
ssl_certificate_self_managed = ssl_certificate.self_managed
ssl_certificate_type = ssl_certificate.type
ssl_certificate_certificate = ssl_certificate.certificate
ssl_certificate_region = ssl_certificate.region
ssl_certificate_description = ssl_certificate.description
ssl_certificate_managed = ssl_certificate.managed
```

---


### Disk_type

Returns the specified disk type.

**Operations**: ✅ Read

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `region` | String | [Output Only] URL of the region where the disk type resides. Only
applicable for regional resources.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `kind` | String | [Output Only] Type of the resource. Always compute#diskType
for disk types. |
| `description` | String | [Output Only] An optional description of this resource. |
| `deprecated` | String | [Output Only] The deprecation status associated with this disk type. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `valid_disk_size` | String | [Output Only] An optional textual description of the valid disk size,
such as "10GB-10TB". |
| `zone` | String | [Output Only] URL of the zone where the disk type resides.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `default_disk_size_gb` | String | [Output Only] Server-defined default disk size in GB. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `name` | String | [Output Only] Name of the resource. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Access disk_type outputs
disk_type_id = disk_type.id
disk_type_region = disk_type.region
disk_type_kind = disk_type.kind
disk_type_description = disk_type.description
disk_type_deprecated = disk_type.deprecated
disk_type_self_link = disk_type.self_link
disk_type_creation_timestamp = disk_type.creation_timestamp
disk_type_valid_disk_size = disk_type.valid_disk_size
disk_type_zone = disk_type.zone
disk_type_default_disk_size_gb = disk_type.default_disk_size_gb
disk_type_id = disk_type.id
disk_type_name = disk_type.name
```

---


### Region_network_firewall_policie

Creates a new network firewall policy in the specified project and region.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `kind` | String |  | [Output only] Type of the resource. Alwayscompute#firewallPolicyfor firewall policies |
| `policy_source` | String |  | [Output Only] Source of this Firewall Policy. USER_DEFINED if
created by a Cloud user, or SYSTEM if created by managed
services like GKE. |
| `region` | String |  | [Output Only] URL of the region where the regional firewall policy resides.
This field is not applicable to global firewall policies.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `rules` | Vec<String> |  | A list of rules that belong to this policy.
There must always be a default rule (rule with priority 2147483647 and
match "*"). If no rules are provided when creating a firewall policy, a
default rule with action "allow" will be added. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `parent` | String |  | [Output Only] The parent of the firewall policy.
This field is not applicable to network firewall policies. |
| `rule_tuple_count` | i64 |  | [Output Only] Total count of all firewall policy rule tuples. A firewall
policy can not exceed a set number of tuples. |
| `display_name` | String |  | Deprecated, please use short name instead. User-provided name of the
Organization firewall policy. The name should be unique in the organization
in which the firewall policy is created.
This field is not applicable to network firewall policies.
This name must be set on creation and cannot be changed.
The name must be 1-63 characters long, and comply
with RFC1035. Specifically, the name must be 1-63 characters
long and match the regular expression `[a-z]([-a-z0-9]*[a-z0-9])?` which
means the first character must be a lowercase letter, and all following
characters must be a dash, lowercase letter, or digit, except the last
character, which cannot be a dash. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `associations` | Vec<String> |  | A list of associations that belong to this firewall policy. |
| `fingerprint` | String |  | Specifies a fingerprint for this resource, which is essentially a hash of
the metadata's contents and used for optimistic locking. The
fingerprint is initially generated by Compute Engine and changes after
every request to modify or update metadata. You must always provide an
up-to-date fingerprint hash in order to update or change metadata,
otherwise the request will fail with error412 conditionNotMet.

To see the latest fingerprint, make get() request to the
firewall policy. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `name` | String |  | Name of the resource. For Organization Firewall Policies it's a
[Output Only] numeric ID allocated by Google Cloud which uniquely
identifies the Organization Firewall Policy. |
| `packet_mirroring_rules` | Vec<String> |  | A list of packet mirroring rules that belong to this policy. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `self_link_with_id` | String |  | [Output Only] Server-defined URL for this resource with the resource id. |
| `short_name` | String |  | User-provided name of the Organization firewall policy. The name should be
unique in the organization in which the firewall policy is created.
This field is not applicable to network firewall policies.
This name must be set on creation and cannot be changed. The name must be
1-63 characters long, and comply with RFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `policy_type` | String |  | The type of the firewall policy. This field can be eitherVPC_POLICY or RDMA_ROCE_POLICY.

Note: if not specified then VPC_POLICY will be used. |
| `project` | String | ✅ | Project ID for this request. |
| `region` | String | ✅ | Name of the region scoping this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `kind` | String | [Output only] Type of the resource. Alwayscompute#firewallPolicyfor firewall policies |
| `policy_source` | String | [Output Only] Source of this Firewall Policy. USER_DEFINED if
created by a Cloud user, or SYSTEM if created by managed
services like GKE. |
| `region` | String | [Output Only] URL of the region where the regional firewall policy resides.
This field is not applicable to global firewall policies.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `rules` | Vec<String> | A list of rules that belong to this policy.
There must always be a default rule (rule with priority 2147483647 and
match "*"). If no rules are provided when creating a firewall policy, a
default rule with action "allow" will be added. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `parent` | String | [Output Only] The parent of the firewall policy.
This field is not applicable to network firewall policies. |
| `rule_tuple_count` | i64 | [Output Only] Total count of all firewall policy rule tuples. A firewall
policy can not exceed a set number of tuples. |
| `display_name` | String | Deprecated, please use short name instead. User-provided name of the
Organization firewall policy. The name should be unique in the organization
in which the firewall policy is created.
This field is not applicable to network firewall policies.
This name must be set on creation and cannot be changed.
The name must be 1-63 characters long, and comply
with RFC1035. Specifically, the name must be 1-63 characters
long and match the regular expression `[a-z]([-a-z0-9]*[a-z0-9])?` which
means the first character must be a lowercase letter, and all following
characters must be a dash, lowercase letter, or digit, except the last
character, which cannot be a dash. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `associations` | Vec<String> | A list of associations that belong to this firewall policy. |
| `fingerprint` | String | Specifies a fingerprint for this resource, which is essentially a hash of
the metadata's contents and used for optimistic locking. The
fingerprint is initially generated by Compute Engine and changes after
every request to modify or update metadata. You must always provide an
up-to-date fingerprint hash in order to update or change metadata,
otherwise the request will fail with error412 conditionNotMet.

To see the latest fingerprint, make get() request to the
firewall policy. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `name` | String | Name of the resource. For Organization Firewall Policies it's a
[Output Only] numeric ID allocated by Google Cloud which uniquely
identifies the Organization Firewall Policy. |
| `packet_mirroring_rules` | Vec<String> | A list of packet mirroring rules that belong to this policy. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `self_link_with_id` | String | [Output Only] Server-defined URL for this resource with the resource id. |
| `short_name` | String | User-provided name of the Organization firewall policy. The name should be
unique in the organization in which the firewall policy is created.
This field is not applicable to network firewall policies.
This name must be set on creation and cannot be changed. The name must be
1-63 characters long, and comply with RFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `policy_type` | String | The type of the firewall policy. This field can be eitherVPC_POLICY or RDMA_ROCE_POLICY.

Note: if not specified then VPC_POLICY will be used. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create region_network_firewall_policie
region_network_firewall_policie = provider.compute_api.Region_network_firewall_policie {
    project = "value"  # Project ID for this request.
    region = "value"  # Name of the region scoping this request.
}

# Access region_network_firewall_policie outputs
region_network_firewall_policie_id = region_network_firewall_policie.id
region_network_firewall_policie_kind = region_network_firewall_policie.kind
region_network_firewall_policie_policy_source = region_network_firewall_policie.policy_source
region_network_firewall_policie_region = region_network_firewall_policie.region
region_network_firewall_policie_rules = region_network_firewall_policie.rules
region_network_firewall_policie_description = region_network_firewall_policie.description
region_network_firewall_policie_parent = region_network_firewall_policie.parent
region_network_firewall_policie_rule_tuple_count = region_network_firewall_policie.rule_tuple_count
region_network_firewall_policie_display_name = region_network_firewall_policie.display_name
region_network_firewall_policie_id = region_network_firewall_policie.id
region_network_firewall_policie_associations = region_network_firewall_policie.associations
region_network_firewall_policie_fingerprint = region_network_firewall_policie.fingerprint
region_network_firewall_policie_creation_timestamp = region_network_firewall_policie.creation_timestamp
region_network_firewall_policie_name = region_network_firewall_policie.name
region_network_firewall_policie_packet_mirroring_rules = region_network_firewall_policie.packet_mirroring_rules
region_network_firewall_policie_self_link = region_network_firewall_policie.self_link
region_network_firewall_policie_self_link_with_id = region_network_firewall_policie.self_link_with_id
region_network_firewall_policie_short_name = region_network_firewall_policie.short_name
region_network_firewall_policie_policy_type = region_network_firewall_policie.policy_type
```

---


### Region_target_https_proxie

Creates a TargetHttpsProxy resource in the specified project and region
using the data included in the request.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `quic_override` | String |  | Specifies the QUIC override policy for this TargetHttpsProxy resource. This
setting determines whether the load balancer attempts to negotiate QUIC
with clients.
You can specify NONE, ENABLE, orDISABLE.
   
   - When quic-override is set to NONE,
   Google manages whether QUIC is used.
   - When quic-override is set to ENABLE, the
   load balancer uses QUIC when possible.
   - When quic-override is set to DISABLE, the
   load balancer doesn't use QUIC.
   - If the quic-override flag is not specified,NONE is implied. |
| `tls_early_data` | String |  | Specifies whether TLS 1.3 0-RTT Data ("Early Data") should be accepted
for this service. Early Data allows a TLS resumption handshake to include
the initial application payload (a HTTP request) alongside the handshake,
reducing the effective round trips to "zero". This applies to TLS 1.3
connections over TCP (HTTP/2) as well as over UDP (QUIC/h3).


This can improve application performance, especially on networks where
interruptions may be common, such as on mobile.


Requests with Early Data will have the "Early-Data" HTTP header set on
the request, with a value of "1", to allow the backend to determine whether
Early Data was included.


Note: TLS Early Data may allow requests to be replayed, as the data is
sent to the backend before the handshake has fully completed. Applications
that allow idempotent HTTP methods to make non-idempotent changes, such as
a GET request updating a database, should not accept Early Data on those
requests, and reject requests with the "Early-Data: 1" HTTP header by
returning a HTTP 425 (Too Early) status code, in order to remain RFC
compliant.


The default value is DISABLED. |
| `ssl_certificates` | Vec<String> |  | URLs to SslCertificate resources that are used to authenticate
connections between users and the load balancer. At least one SSL
certificate must be specified. SslCertificates do not apply when the load
balancing scheme is set to INTERNAL_SELF_MANAGED.

The URLs should refer to a SSL Certificate resource or Certificate Manager
Certificate resource. Mixing Classic Certificates and Certificate Manager
Certificates is not allowed. Certificate Manager Certificates must include
the certificatemanager API namespace. Using Certificate Manager
Certificates in this field is not supported by Global external Application
Load Balancer or Classic Application Load Balancer, use certificate_map
instead.

Currently, you may specify up to 15 Classic SSL Certificates or up to 100
Certificate Manager Certificates.

Certificate Manager Certificates accepted formats are:
   
   - //certificatemanager.googleapis.com/projects/{project}/locations/{location}/certificates/{resourceName}.
   - https://certificatemanager.googleapis.com/v1alpha1/projects/{project}/locations/{location}/certificates/{resourceName}. |
| `http_keep_alive_timeout_sec` | i64 |  | Specifies how long to keep a connection open, after completing a response,
while there is no matching traffic (in seconds). If an HTTP keep-alive is
not specified, a default value (610 seconds) will be used.

For global external Application Load Balancers, the minimum allowed value
is 5 seconds and the maximum allowed value is 1200 seconds.

For classic Application Load Balancers, this option is not supported. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `ssl_policy` | String |  | URL of SslPolicy resource that will be associated with the TargetHttpsProxy
resource. If not set, the TargetHttpsProxy resource has no
SSL policy configured. |
| `url_map` | String |  | A fully-qualified or valid partial URL to the UrlMap resource that defines
the mapping from URL to the BackendService. For example, the following are
all valid URLs for specifying a URL map:
   
   - https://www.googleapis.compute/v1/projects/project/global/urlMaps/url-map 
   - projects/project/global/urlMaps/url-map 
   - global/urlMaps/url-map |
| `proxy_bind` | bool |  | This field only applies when the forwarding rule that references this
target proxy has a loadBalancingScheme set toINTERNAL_SELF_MANAGED.

When this field is set to true, Envoy proxies set up inbound
traffic interception and bind to the IP address and port specified in the
forwarding rule. This is generally useful when using Traffic Director to
configure Envoy as a gateway or middle proxy (in other words, not a
sidecar proxy). The Envoy proxy listens for inbound requests and handles
requests when it receives them.

The default is false. |
| `kind` | String |  | [Output Only] Type of resource. Alwayscompute#targetHttpsProxy for target HTTPS proxies. |
| `authorization_policy` | String |  | Optional. A URL referring to a networksecurity.AuthorizationPolicy resource
that describes how the proxy should authorize inbound traffic. If left
blank, access will not be restricted by an authorization policy.

 Refer to the AuthorizationPolicy resource for additional
details.

 authorizationPolicy only applies to a globalTargetHttpsProxy attached toglobalForwardingRules with theloadBalancingScheme set to INTERNAL_SELF_MANAGED.

 Note: This field currently has no impact. |
| `http_filters` | Vec<String> |  | URLs to networkservices.HttpFilter resources
enabled for xDS clients using this configuration. For example,https://networkservices.googleapis.com/beta/projects/project/locations/location/httpFilters/httpFilter
Only filters that handle outbound connection and
stream events may be specified. These filters work in conjunction with
a default set of HTTP filters that may already be configured by Traffic
Director. Traffic Director will determine the final location of these
filters within xDS configuration based on the name of the HTTP filter. If
Traffic Director positions multiple filters at the same location, those
filters will be in the same order as specified in this list. 

httpFilters only applies for loadbalancers withloadBalancingScheme set to INTERNAL_SELF_MANAGED.
See ForwardingRule for more details. |
| `region` | String |  | [Output Only] URL of the region where the regional TargetHttpsProxy
resides. This field is not applicable to global TargetHttpsProxies. |
| `authorization` | String |  | [Deprecated] Use authorizationPolicy instead. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `authentication` | String |  | [Deprecated] Use serverTlsPolicy instead. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `server_tls_policy` | String |  | Optional. A URL referring to a networksecurity.ServerTlsPolicy resource
that describes how the proxy should authenticate inbound traffic.

 serverTlsPolicy only applies to a globalTargetHttpsProxy attached toglobalForwardingRules with theloadBalancingScheme set to INTERNAL_SELF_MANAGED
or EXTERNAL orEXTERNAL_MANAGED or INTERNAL_MANAGED.
It also applies to a regional TargetHttpsProxy attached to
regional forwardingRules with theloadBalancingScheme set to EXTERNAL_MANAGED orINTERNAL_MANAGED. For details whichServerTlsPolicy resources are accepted withINTERNAL_SELF_MANAGED and which with EXTERNAL,INTERNAL_MANAGED, EXTERNAL_MANAGEDloadBalancingScheme consult ServerTlsPolicy
documentation.

  If left blank, communications are not encrypted. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `fingerprint` | String |  | Fingerprint of this resource. A hash of the contents stored in this object.
This field is used in optimistic locking. This field will be ignored when
inserting a TargetHttpsProxy. An up-to-date fingerprint must
be provided in order to patch the TargetHttpsProxy; otherwise, the request
will fail with error 412 conditionNotMet. To see the latest
fingerprint, make a get() request to retrieve the
TargetHttpsProxy. |
| `certificate_map` | String |  | URL of a certificate map that identifies a certificate map associated with
the given target proxy.
This field can only be set for Global external Application Load Balancer or
Classic Application Load Balancer. For other products use Certificate
Manager Certificates instead.

If set, sslCertificates will be ignored.

 Accepted format is//certificatemanager.googleapis.com/projects/{project}/locations/{location}/certificateMaps/{resourceName}. |
| `name` | String |  | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `project` | String | ✅ | Project ID for this request. |
| `region` | String | ✅ | Name of the region scoping this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `quic_override` | String | Specifies the QUIC override policy for this TargetHttpsProxy resource. This
setting determines whether the load balancer attempts to negotiate QUIC
with clients.
You can specify NONE, ENABLE, orDISABLE.
   
   - When quic-override is set to NONE,
   Google manages whether QUIC is used.
   - When quic-override is set to ENABLE, the
   load balancer uses QUIC when possible.
   - When quic-override is set to DISABLE, the
   load balancer doesn't use QUIC.
   - If the quic-override flag is not specified,NONE is implied. |
| `tls_early_data` | String | Specifies whether TLS 1.3 0-RTT Data ("Early Data") should be accepted
for this service. Early Data allows a TLS resumption handshake to include
the initial application payload (a HTTP request) alongside the handshake,
reducing the effective round trips to "zero". This applies to TLS 1.3
connections over TCP (HTTP/2) as well as over UDP (QUIC/h3).


This can improve application performance, especially on networks where
interruptions may be common, such as on mobile.


Requests with Early Data will have the "Early-Data" HTTP header set on
the request, with a value of "1", to allow the backend to determine whether
Early Data was included.


Note: TLS Early Data may allow requests to be replayed, as the data is
sent to the backend before the handshake has fully completed. Applications
that allow idempotent HTTP methods to make non-idempotent changes, such as
a GET request updating a database, should not accept Early Data on those
requests, and reject requests with the "Early-Data: 1" HTTP header by
returning a HTTP 425 (Too Early) status code, in order to remain RFC
compliant.


The default value is DISABLED. |
| `ssl_certificates` | Vec<String> | URLs to SslCertificate resources that are used to authenticate
connections between users and the load balancer. At least one SSL
certificate must be specified. SslCertificates do not apply when the load
balancing scheme is set to INTERNAL_SELF_MANAGED.

The URLs should refer to a SSL Certificate resource or Certificate Manager
Certificate resource. Mixing Classic Certificates and Certificate Manager
Certificates is not allowed. Certificate Manager Certificates must include
the certificatemanager API namespace. Using Certificate Manager
Certificates in this field is not supported by Global external Application
Load Balancer or Classic Application Load Balancer, use certificate_map
instead.

Currently, you may specify up to 15 Classic SSL Certificates or up to 100
Certificate Manager Certificates.

Certificate Manager Certificates accepted formats are:
   
   - //certificatemanager.googleapis.com/projects/{project}/locations/{location}/certificates/{resourceName}.
   - https://certificatemanager.googleapis.com/v1alpha1/projects/{project}/locations/{location}/certificates/{resourceName}. |
| `http_keep_alive_timeout_sec` | i64 | Specifies how long to keep a connection open, after completing a response,
while there is no matching traffic (in seconds). If an HTTP keep-alive is
not specified, a default value (610 seconds) will be used.

For global external Application Load Balancers, the minimum allowed value
is 5 seconds and the maximum allowed value is 1200 seconds.

For classic Application Load Balancers, this option is not supported. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `ssl_policy` | String | URL of SslPolicy resource that will be associated with the TargetHttpsProxy
resource. If not set, the TargetHttpsProxy resource has no
SSL policy configured. |
| `url_map` | String | A fully-qualified or valid partial URL to the UrlMap resource that defines
the mapping from URL to the BackendService. For example, the following are
all valid URLs for specifying a URL map:
   
   - https://www.googleapis.compute/v1/projects/project/global/urlMaps/url-map 
   - projects/project/global/urlMaps/url-map 
   - global/urlMaps/url-map |
| `proxy_bind` | bool | This field only applies when the forwarding rule that references this
target proxy has a loadBalancingScheme set toINTERNAL_SELF_MANAGED.

When this field is set to true, Envoy proxies set up inbound
traffic interception and bind to the IP address and port specified in the
forwarding rule. This is generally useful when using Traffic Director to
configure Envoy as a gateway or middle proxy (in other words, not a
sidecar proxy). The Envoy proxy listens for inbound requests and handles
requests when it receives them.

The default is false. |
| `kind` | String | [Output Only] Type of resource. Alwayscompute#targetHttpsProxy for target HTTPS proxies. |
| `authorization_policy` | String | Optional. A URL referring to a networksecurity.AuthorizationPolicy resource
that describes how the proxy should authorize inbound traffic. If left
blank, access will not be restricted by an authorization policy.

 Refer to the AuthorizationPolicy resource for additional
details.

 authorizationPolicy only applies to a globalTargetHttpsProxy attached toglobalForwardingRules with theloadBalancingScheme set to INTERNAL_SELF_MANAGED.

 Note: This field currently has no impact. |
| `http_filters` | Vec<String> | URLs to networkservices.HttpFilter resources
enabled for xDS clients using this configuration. For example,https://networkservices.googleapis.com/beta/projects/project/locations/location/httpFilters/httpFilter
Only filters that handle outbound connection and
stream events may be specified. These filters work in conjunction with
a default set of HTTP filters that may already be configured by Traffic
Director. Traffic Director will determine the final location of these
filters within xDS configuration based on the name of the HTTP filter. If
Traffic Director positions multiple filters at the same location, those
filters will be in the same order as specified in this list. 

httpFilters only applies for loadbalancers withloadBalancingScheme set to INTERNAL_SELF_MANAGED.
See ForwardingRule for more details. |
| `region` | String | [Output Only] URL of the region where the regional TargetHttpsProxy
resides. This field is not applicable to global TargetHttpsProxies. |
| `authorization` | String | [Deprecated] Use authorizationPolicy instead. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `authentication` | String | [Deprecated] Use serverTlsPolicy instead. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `server_tls_policy` | String | Optional. A URL referring to a networksecurity.ServerTlsPolicy resource
that describes how the proxy should authenticate inbound traffic.

 serverTlsPolicy only applies to a globalTargetHttpsProxy attached toglobalForwardingRules with theloadBalancingScheme set to INTERNAL_SELF_MANAGED
or EXTERNAL orEXTERNAL_MANAGED or INTERNAL_MANAGED.
It also applies to a regional TargetHttpsProxy attached to
regional forwardingRules with theloadBalancingScheme set to EXTERNAL_MANAGED orINTERNAL_MANAGED. For details whichServerTlsPolicy resources are accepted withINTERNAL_SELF_MANAGED and which with EXTERNAL,INTERNAL_MANAGED, EXTERNAL_MANAGEDloadBalancingScheme consult ServerTlsPolicy
documentation.

  If left blank, communications are not encrypted. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `fingerprint` | String | Fingerprint of this resource. A hash of the contents stored in this object.
This field is used in optimistic locking. This field will be ignored when
inserting a TargetHttpsProxy. An up-to-date fingerprint must
be provided in order to patch the TargetHttpsProxy; otherwise, the request
will fail with error 412 conditionNotMet. To see the latest
fingerprint, make a get() request to retrieve the
TargetHttpsProxy. |
| `certificate_map` | String | URL of a certificate map that identifies a certificate map associated with
the given target proxy.
This field can only be set for Global external Application Load Balancer or
Classic Application Load Balancer. For other products use Certificate
Manager Certificates instead.

If set, sslCertificates will be ignored.

 Accepted format is//certificatemanager.googleapis.com/projects/{project}/locations/{location}/certificateMaps/{resourceName}. |
| `name` | String | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create region_target_https_proxie
region_target_https_proxie = provider.compute_api.Region_target_https_proxie {
    project = "value"  # Project ID for this request.
    region = "value"  # Name of the region scoping this request.
}

# Access region_target_https_proxie outputs
region_target_https_proxie_id = region_target_https_proxie.id
region_target_https_proxie_quic_override = region_target_https_proxie.quic_override
region_target_https_proxie_tls_early_data = region_target_https_proxie.tls_early_data
region_target_https_proxie_ssl_certificates = region_target_https_proxie.ssl_certificates
region_target_https_proxie_http_keep_alive_timeout_sec = region_target_https_proxie.http_keep_alive_timeout_sec
region_target_https_proxie_creation_timestamp = region_target_https_proxie.creation_timestamp
region_target_https_proxie_ssl_policy = region_target_https_proxie.ssl_policy
region_target_https_proxie_url_map = region_target_https_proxie.url_map
region_target_https_proxie_proxy_bind = region_target_https_proxie.proxy_bind
region_target_https_proxie_kind = region_target_https_proxie.kind
region_target_https_proxie_authorization_policy = region_target_https_proxie.authorization_policy
region_target_https_proxie_http_filters = region_target_https_proxie.http_filters
region_target_https_proxie_region = region_target_https_proxie.region
region_target_https_proxie_authorization = region_target_https_proxie.authorization
region_target_https_proxie_description = region_target_https_proxie.description
region_target_https_proxie_authentication = region_target_https_proxie.authentication
region_target_https_proxie_id = region_target_https_proxie.id
region_target_https_proxie_server_tls_policy = region_target_https_proxie.server_tls_policy
region_target_https_proxie_self_link = region_target_https_proxie.self_link
region_target_https_proxie_fingerprint = region_target_https_proxie.fingerprint
region_target_https_proxie_certificate_map = region_target_https_proxie.certificate_map
region_target_https_proxie_name = region_target_https_proxie.name
```

---


### Advice

Advise how, where and when to create the requested amount of instances
with specified accelerators, within the specified time and location limits.
The method recommends creating future reservations for the requested
resources.

**Operations**: ✅ Create

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `future_resources_specs` | HashMap<String, String> |  | Specification of resources to create in the future.
The key of the map is an arbitrary string specified by the caller.
Value of the map is a specification of required resources and their
constraints. Currently only one value is allowed in this map. |
| `region` | String | ✅ | Name of the region for this request. |
| `project` | String | ✅ | Project ID for this request. |



#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create advice
advice = provider.compute_api.Advice {
    region = "value"  # Name of the region for this request.
    project = "value"  # Project ID for this request.
}

```

---


### Region_notification_endpoint

Create a NotificationEndpoint in the specified project in the given region
using the parameters that are included in the request.

**Operations**: ✅ Create ✅ Read ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `grpc_settings` | String |  | Settings of the gRPC notification endpoint including the endpoint URL and
the retry duration. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `region` | String |  | [Output Only] URL of the region where the notification endpoint resides.
This field applies only to the regional resource. You must specify this
field as part of the HTTP request URL. It is not settable as a field in
the request body. |
| `name` | String |  | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `kind` | String |  | [Output Only] Type of the resource. Alwayscompute#notificationEndpoint for notification endpoints. |
| `id` | String |  | [Output Only] A unique identifier for this resource type. The server
generates this identifier. |
| `region` | String | ✅ | Name of the region scoping this request. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `grpc_settings` | String | Settings of the gRPC notification endpoint including the endpoint URL and
the retry duration. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `region` | String | [Output Only] URL of the region where the notification endpoint resides.
This field applies only to the regional resource. You must specify this
field as part of the HTTP request URL. It is not settable as a field in
the request body. |
| `name` | String | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `kind` | String | [Output Only] Type of the resource. Alwayscompute#notificationEndpoint for notification endpoints. |
| `id` | String | [Output Only] A unique identifier for this resource type. The server
generates this identifier. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create region_notification_endpoint
region_notification_endpoint = provider.compute_api.Region_notification_endpoint {
    region = "value"  # Name of the region scoping this request.
    project = "value"  # Project ID for this request.
}

# Access region_notification_endpoint outputs
region_notification_endpoint_id = region_notification_endpoint.id
region_notification_endpoint_grpc_settings = region_notification_endpoint.grpc_settings
region_notification_endpoint_self_link = region_notification_endpoint.self_link
region_notification_endpoint_region = region_notification_endpoint.region
region_notification_endpoint_name = region_notification_endpoint.name
region_notification_endpoint_creation_timestamp = region_notification_endpoint.creation_timestamp
region_notification_endpoint_description = region_notification_endpoint.description
region_notification_endpoint_kind = region_notification_endpoint.kind
region_notification_endpoint_id = region_notification_endpoint.id
```

---


### Future_reservation

Creates a new Future Reservation.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `scheduling_type` | String |  | Maintenance information for this reservation |
| `enable_emergent_maintenance` | bool |  | Indicates if this group of VMs have emergent maintenance enabled. |
| `name` | String |  | The name of the resource, provided by the client when initially creating
the resource. The resource name must be 1-63 characters long, and comply
withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must be
a dash, lowercase letter, or digit, except the last character, which cannot
be a dash. |
| `specific_reservation_required` | bool |  | Indicates whether the auto-created reservation can be consumed by VMs with
affinity for "any" reservation. If the field is set, then only VMs that
target the reservation by name can consume from the delivered reservation. |
| `status` | String |  | [Output only] Status of the Future Reservation |
| `deployment_type` | String |  | Type of the deployment requested as part of future reservation. |
| `aggregate_reservation` | String |  | Aggregate reservation details for the future reservation. |
| `commitment_info` | String |  | If not present, then FR will not deliver a new commitment or update an
existing commitment. |
| `share_settings` | String |  | List of Projects/Folders to share with. |
| `auto_delete_auto_created_reservations` | bool |  | Setting for enabling or disabling automatic deletion for auto-created
reservation. If set to true, auto-created reservations will be
deleted at Future Reservation's end time (default) or at user's defined
timestamp if any of the
[auto_created_reservations_delete_time, auto_created_reservations_duration]
values is specified.
For keeping auto-created reservation indefinitely, this value should be set
to false. |
| `creation_timestamp` | String |  | [Output Only] The creation timestamp for this future reservation inRFC3339
text format. |
| `self_link_with_id` | String |  | [Output Only] Server-defined URL for this resource with the resource id. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the future reservation. |
| `name_prefix` | String |  | Name prefix for the reservations to be created at the time of
delivery. The name prefix must comply with RFC1035.
Maximum allowed length for name prefix is 20. Automatically created
reservations name format will be -date-####. |
| `auto_created_reservations_delete_time` | String |  | Future timestamp when the FR auto-created reservations will be deleted by
Compute Engine. Format of this field must be a valid
href="https://www.ietf.org/rfc/rfc3339.txt">RFC3339 value. |
| `planning_status` | String |  | Planning state before being submitted for evaluation |
| `protection_tier` | String |  | Protection tier for the workload. |
| `specific_sku_properties` | String |  | Future Reservation configuration to indicate instance properties and
total count. |
| `auto_created_reservations_duration` | String |  | Specifies the duration of auto-created reservations.
It represents relative time to future reservation start_time when
auto-created reservations will be automatically deleted by Compute
Engine. Duration time unit is represented as a count of seconds
and fractions of seconds at nanosecond resolution. |
| `time_window` | String |  | Time window for this Future Reservation. |
| `zone` | String |  | [Output Only] URL of the Zone where this future reservation resides. |
| `reservation_mode` | String |  | The reservation mode which determines reservation-termination behavior and
expected pricing. |
| `kind` | String |  | [Output Only] Type of the resource. Alwayscompute#futureReservation for future reservations. |
| `reservation_name` | String |  | Name of reservations where the capacity is provisioned at the time of
delivery of  future reservations. If the reservation with the given name
does not exist already, it is created automatically at the time of Approval
with INACTIVE state till specified start-time. Either provide the
reservation_name or a name_prefix. |
| `id` | String |  | [Output Only] A unique identifier for this future reservation. The server
defines this identifier. |
| `self_link` | String |  | [Output Only] Server-defined fully-qualified URL for this resource. |
| `zone` | String | ✅ | Name of the zone for this request. Name should conform to RFC1035. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `scheduling_type` | String | Maintenance information for this reservation |
| `enable_emergent_maintenance` | bool | Indicates if this group of VMs have emergent maintenance enabled. |
| `name` | String | The name of the resource, provided by the client when initially creating
the resource. The resource name must be 1-63 characters long, and comply
withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must be
a dash, lowercase letter, or digit, except the last character, which cannot
be a dash. |
| `specific_reservation_required` | bool | Indicates whether the auto-created reservation can be consumed by VMs with
affinity for "any" reservation. If the field is set, then only VMs that
target the reservation by name can consume from the delivered reservation. |
| `status` | String | [Output only] Status of the Future Reservation |
| `deployment_type` | String | Type of the deployment requested as part of future reservation. |
| `aggregate_reservation` | String | Aggregate reservation details for the future reservation. |
| `commitment_info` | String | If not present, then FR will not deliver a new commitment or update an
existing commitment. |
| `share_settings` | String | List of Projects/Folders to share with. |
| `auto_delete_auto_created_reservations` | bool | Setting for enabling or disabling automatic deletion for auto-created
reservation. If set to true, auto-created reservations will be
deleted at Future Reservation's end time (default) or at user's defined
timestamp if any of the
[auto_created_reservations_delete_time, auto_created_reservations_duration]
values is specified.
For keeping auto-created reservation indefinitely, this value should be set
to false. |
| `creation_timestamp` | String | [Output Only] The creation timestamp for this future reservation inRFC3339
text format. |
| `self_link_with_id` | String | [Output Only] Server-defined URL for this resource with the resource id. |
| `description` | String | An optional description of this resource. Provide this property when you
create the future reservation. |
| `name_prefix` | String | Name prefix for the reservations to be created at the time of
delivery. The name prefix must comply with RFC1035.
Maximum allowed length for name prefix is 20. Automatically created
reservations name format will be -date-####. |
| `auto_created_reservations_delete_time` | String | Future timestamp when the FR auto-created reservations will be deleted by
Compute Engine. Format of this field must be a valid
href="https://www.ietf.org/rfc/rfc3339.txt">RFC3339 value. |
| `planning_status` | String | Planning state before being submitted for evaluation |
| `protection_tier` | String | Protection tier for the workload. |
| `specific_sku_properties` | String | Future Reservation configuration to indicate instance properties and
total count. |
| `auto_created_reservations_duration` | String | Specifies the duration of auto-created reservations.
It represents relative time to future reservation start_time when
auto-created reservations will be automatically deleted by Compute
Engine. Duration time unit is represented as a count of seconds
and fractions of seconds at nanosecond resolution. |
| `time_window` | String | Time window for this Future Reservation. |
| `zone` | String | [Output Only] URL of the Zone where this future reservation resides. |
| `reservation_mode` | String | The reservation mode which determines reservation-termination behavior and
expected pricing. |
| `kind` | String | [Output Only] Type of the resource. Alwayscompute#futureReservation for future reservations. |
| `reservation_name` | String | Name of reservations where the capacity is provisioned at the time of
delivery of  future reservations. If the reservation with the given name
does not exist already, it is created automatically at the time of Approval
with INACTIVE state till specified start-time. Either provide the
reservation_name or a name_prefix. |
| `id` | String | [Output Only] A unique identifier for this future reservation. The server
defines this identifier. |
| `self_link` | String | [Output Only] Server-defined fully-qualified URL for this resource. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create future_reservation
future_reservation = provider.compute_api.Future_reservation {
    zone = "value"  # Name of the zone for this request. Name should conform to RFC1035.
    project = "value"  # Project ID for this request.
}

# Access future_reservation outputs
future_reservation_id = future_reservation.id
future_reservation_scheduling_type = future_reservation.scheduling_type
future_reservation_enable_emergent_maintenance = future_reservation.enable_emergent_maintenance
future_reservation_name = future_reservation.name
future_reservation_specific_reservation_required = future_reservation.specific_reservation_required
future_reservation_status = future_reservation.status
future_reservation_deployment_type = future_reservation.deployment_type
future_reservation_aggregate_reservation = future_reservation.aggregate_reservation
future_reservation_commitment_info = future_reservation.commitment_info
future_reservation_share_settings = future_reservation.share_settings
future_reservation_auto_delete_auto_created_reservations = future_reservation.auto_delete_auto_created_reservations
future_reservation_creation_timestamp = future_reservation.creation_timestamp
future_reservation_self_link_with_id = future_reservation.self_link_with_id
future_reservation_description = future_reservation.description
future_reservation_name_prefix = future_reservation.name_prefix
future_reservation_auto_created_reservations_delete_time = future_reservation.auto_created_reservations_delete_time
future_reservation_planning_status = future_reservation.planning_status
future_reservation_protection_tier = future_reservation.protection_tier
future_reservation_specific_sku_properties = future_reservation.specific_sku_properties
future_reservation_auto_created_reservations_duration = future_reservation.auto_created_reservations_duration
future_reservation_time_window = future_reservation.time_window
future_reservation_zone = future_reservation.zone
future_reservation_reservation_mode = future_reservation.reservation_mode
future_reservation_kind = future_reservation.kind
future_reservation_reservation_name = future_reservation.reservation_name
future_reservation_id = future_reservation.id
future_reservation_self_link = future_reservation.self_link
```

---


### Node_template

Creates a NodeTemplate resource in the specified project using the data
included in the request.

**Operations**: ✅ Create ✅ Read ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `cpu_overcommit_type` | String |  | CPU overcommit. |
| `region` | String |  | [Output Only] The name of the region where the node template resides,
such as us-central1. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `server_binding` | String |  | Sets the binding properties for the physical server. Valid values include:
   
   - *[Default]* RESTART_NODE_ON_ANY_SERVER:
   Restarts VMs on any available
   physical server
   - RESTART_NODE_ON_MINIMAL_SERVER: Restarts VMs on the same
   physical server whenever possible



See Sole-tenant
node options for more information. |
| `accelerators` | Vec<String> |  |  |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `node_affinity_labels` | HashMap<String, String> |  | Labels to use for node affinity, which will be used in instance scheduling. |
| `status_message` | String |  | [Output Only] An optional, human-readable explanation of the status. |
| `kind` | String |  | [Output Only] The type of the resource. Alwayscompute#nodeTemplate for node templates. |
| `status` | String |  | [Output Only] The status of the node template. One of the following values:CREATING, READY, and DELETING. |
| `node_type` | String |  | The node type to use for nodes group that are created from this template. |
| `disks` | Vec<String> |  |  |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `name` | String |  | The name of the resource, provided by the client when initially creating
the resource. The resource name must be 1-63 characters long, and comply
withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must be
a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `node_type_flexibility` | String |  | Do not use. Instead, use the node_type property. |
| `project` | String | ✅ | Project ID for this request. |
| `region` | String | ✅ | The name of the region for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `cpu_overcommit_type` | String | CPU overcommit. |
| `region` | String | [Output Only] The name of the region where the node template resides,
such as us-central1. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `server_binding` | String | Sets the binding properties for the physical server. Valid values include:
   
   - *[Default]* RESTART_NODE_ON_ANY_SERVER:
   Restarts VMs on any available
   physical server
   - RESTART_NODE_ON_MINIMAL_SERVER: Restarts VMs on the same
   physical server whenever possible



See Sole-tenant
node options for more information. |
| `accelerators` | Vec<String> |  |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `node_affinity_labels` | HashMap<String, String> | Labels to use for node affinity, which will be used in instance scheduling. |
| `status_message` | String | [Output Only] An optional, human-readable explanation of the status. |
| `kind` | String | [Output Only] The type of the resource. Alwayscompute#nodeTemplate for node templates. |
| `status` | String | [Output Only] The status of the node template. One of the following values:CREATING, READY, and DELETING. |
| `node_type` | String | The node type to use for nodes group that are created from this template. |
| `disks` | Vec<String> |  |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `name` | String | The name of the resource, provided by the client when initially creating
the resource. The resource name must be 1-63 characters long, and comply
withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must be
a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `node_type_flexibility` | String | Do not use. Instead, use the node_type property. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create node_template
node_template = provider.compute_api.Node_template {
    project = "value"  # Project ID for this request.
    region = "value"  # The name of the region for this request.
}

# Access node_template outputs
node_template_id = node_template.id
node_template_cpu_overcommit_type = node_template.cpu_overcommit_type
node_template_region = node_template.region
node_template_id = node_template.id
node_template_server_binding = node_template.server_binding
node_template_accelerators = node_template.accelerators
node_template_description = node_template.description
node_template_node_affinity_labels = node_template.node_affinity_labels
node_template_status_message = node_template.status_message
node_template_kind = node_template.kind
node_template_status = node_template.status
node_template_node_type = node_template.node_type
node_template_disks = node_template.disks
node_template_creation_timestamp = node_template.creation_timestamp
node_template_self_link = node_template.self_link
node_template_name = node_template.name
node_template_node_type_flexibility = node_template.node_type_flexibility
```

---


### Route

Creates a Route resource in the specified project using the data included
in the request.

**Operations**: ✅ Create ✅ Read ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `next_hop_ilb` | String |  | The URL to a forwarding rule of typeloadBalancingScheme=INTERNAL that should handle matching
packets or the IP address of the forwarding Rule.
For example, the following are all valid URLs:
   
   
      - https://www.googleapis.com/compute/v1/projects/project/regions/region/forwardingRules/forwardingRule 
   - regions/region/forwardingRules/forwardingRule


If an IP address is provided, must specify an IPv4 address in dot-decimal
notation or an IPv6 address in RFC 4291 format. For example, the following
are all valid IP addresses:
   
   
      - 10.128.0.56
      - 2001:db8::2d9:51:0:0
      - 2001:db8:0:0:2d9:51:0:0


IPv6 addresses will be displayed using RFC 5952 compressed format (e.g.
2001:db8::2d9:51:0:0). Should never be an IPv4-mapped IPv6 address. |
| `dest_range` | String |  | The destination range of outgoing packets that this route applies to. Both
IPv4 and IPv6 are supported.
Must specify an IPv4 range (e.g. 192.0.2.0/24) or an IPv6 range in RFC 4291
format (e.g. 2001:db8::/32). IPv6 range will be displayed using RFC 5952
compressed format. |
| `kind` | String |  | [Output Only] Type of this resource. Always compute#routes for
Route resources. |
| `network` | String |  | Fully-qualified URL of the network that this route applies to. |
| `next_hop_inter_region_cost` | i64 |  | [Output only] Internal fixed region-to-region cost that Google Cloud
calculates based on factors such as network performance, distance, and
available bandwidth between regions. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `next_hop_peering` | String |  | [Output Only] The network peering name that should handle matching packets,
which should conform to RFC1035. |
| `as_paths` | Vec<String> |  | [Output Only] AS path. |
| `name` | String |  | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?`. The first character must be a
lowercase letter, and all following characters (except for the last
character) must be a dash, lowercase letter, or digit. The last character
must be a lowercase letter or digit. |
| `next_hop_ip` | String |  | The network IP address of an instance that should handle matching packets.
Both IPv6 address and IPv4 addresses are supported.
Must specify an IPv4 address in dot-decimal notation (e.g. 192.0.2.99) or
an IPv6 address in RFC 4291 format (e.g. 2001:db8::2d9:51:0:0 or
2001:db8:0:0:2d9:51:0:0). IPv6 addresses will be displayed using RFC 5952
compressed format (e.g. 2001:db8::2d9:51:0:0). Should never be an
IPv4-mapped IPv6 address. |
| `params` | String |  | Input only. [Input Only] Additional params passed with the request, but not persisted
as part of resource payload. |
| `next_hop_med` | i64 |  | [Output Only] Multi-Exit Discriminator, a BGP route metric that indicates
the desirability of a particular route in a network. |
| `priority` | i64 |  | The priority of this route. Priority is used to break ties in cases
where there is more than one matching route of equal prefix length. In
cases where multiple routes have equal prefix length, the one with the
lowest-numbered priority value wins. The default value is `1000`. The
priority value must be from `0` to `65535`, inclusive. |
| `next_hop_instance` | String |  | The URL to an instance that should handle matching packets. You can specify
this as a full or partial URL.
For example: 
https://www.googleapis.com/compute/v1/projects/project/zones/zone/instances/ |
| `description` | String |  | An optional description of this resource. Provide this field when you
create the resource. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `next_hop_interconnect_attachment` | String |  | [Output Only] The URL to an InterconnectAttachment which is the next hop
for the route.
This field will only be populated for dynamic routes generated by
Cloud Router with a linked interconnectAttachment or the static route
generated by each L2 Interconnect Attachment. |
| `route_status` | String |  | [Output only] The status of the route. This status only applies to
dynamic routes learned by Cloud Routers. This status is not applicable
to static routes. |
| `route_type` | String |  | [Output Only] The type of this route, which can be one of the following
values:
- 'TRANSIT' for a transit route that this router learned from
another Cloud Router and will readvertise to one of its BGP peers 
- 'SUBNET' for a route from a subnet of the VPC 
- 'BGP' for a route learned from a BGP peer of this router 
- 'STATIC' for a static route |
| `next_hop_origin` | String |  | [Output Only] Indicates the origin of the route. Can be IGP
(Interior Gateway Protocol), EGP (Exterior Gateway Protocol),
or INCOMPLETE. |
| `tags` | Vec<String> |  | A list of instance tags to which this route applies. |
| `warnings` | Vec<String> |  | [Output Only] If potential misconfigurations are detected for this
route, this field will be populated with warning messages. |
| `next_hop_network` | String |  | The URL of the local network if it should handle matching packets. |
| `self_link` | String |  | [Output Only] Server-defined fully-qualified URL for this resource. |
| `next_hop_vpn_tunnel` | String |  | The URL to a VpnTunnel that should handle matching packets. |
| `next_hop_gateway` | String |  | The URL to a gateway that should handle matching packets.
You can only specify the internet gateway using a full or
partial valid URL: projects/project/global/gateways/default-internet-gateway |
| `next_hop_hub` | String |  | [Output Only] The full resource name of the Network Connectivity Center hub
that will handle matching packets. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `next_hop_ilb` | String | The URL to a forwarding rule of typeloadBalancingScheme=INTERNAL that should handle matching
packets or the IP address of the forwarding Rule.
For example, the following are all valid URLs:
   
   
      - https://www.googleapis.com/compute/v1/projects/project/regions/region/forwardingRules/forwardingRule 
   - regions/region/forwardingRules/forwardingRule


If an IP address is provided, must specify an IPv4 address in dot-decimal
notation or an IPv6 address in RFC 4291 format. For example, the following
are all valid IP addresses:
   
   
      - 10.128.0.56
      - 2001:db8::2d9:51:0:0
      - 2001:db8:0:0:2d9:51:0:0


IPv6 addresses will be displayed using RFC 5952 compressed format (e.g.
2001:db8::2d9:51:0:0). Should never be an IPv4-mapped IPv6 address. |
| `dest_range` | String | The destination range of outgoing packets that this route applies to. Both
IPv4 and IPv6 are supported.
Must specify an IPv4 range (e.g. 192.0.2.0/24) or an IPv6 range in RFC 4291
format (e.g. 2001:db8::/32). IPv6 range will be displayed using RFC 5952
compressed format. |
| `kind` | String | [Output Only] Type of this resource. Always compute#routes for
Route resources. |
| `network` | String | Fully-qualified URL of the network that this route applies to. |
| `next_hop_inter_region_cost` | i64 | [Output only] Internal fixed region-to-region cost that Google Cloud
calculates based on factors such as network performance, distance, and
available bandwidth between regions. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `next_hop_peering` | String | [Output Only] The network peering name that should handle matching packets,
which should conform to RFC1035. |
| `as_paths` | Vec<String> | [Output Only] AS path. |
| `name` | String | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?`. The first character must be a
lowercase letter, and all following characters (except for the last
character) must be a dash, lowercase letter, or digit. The last character
must be a lowercase letter or digit. |
| `next_hop_ip` | String | The network IP address of an instance that should handle matching packets.
Both IPv6 address and IPv4 addresses are supported.
Must specify an IPv4 address in dot-decimal notation (e.g. 192.0.2.99) or
an IPv6 address in RFC 4291 format (e.g. 2001:db8::2d9:51:0:0 or
2001:db8:0:0:2d9:51:0:0). IPv6 addresses will be displayed using RFC 5952
compressed format (e.g. 2001:db8::2d9:51:0:0). Should never be an
IPv4-mapped IPv6 address. |
| `params` | String | Input only. [Input Only] Additional params passed with the request, but not persisted
as part of resource payload. |
| `next_hop_med` | i64 | [Output Only] Multi-Exit Discriminator, a BGP route metric that indicates
the desirability of a particular route in a network. |
| `priority` | i64 | The priority of this route. Priority is used to break ties in cases
where there is more than one matching route of equal prefix length. In
cases where multiple routes have equal prefix length, the one with the
lowest-numbered priority value wins. The default value is `1000`. The
priority value must be from `0` to `65535`, inclusive. |
| `next_hop_instance` | String | The URL to an instance that should handle matching packets. You can specify
this as a full or partial URL.
For example: 
https://www.googleapis.com/compute/v1/projects/project/zones/zone/instances/ |
| `description` | String | An optional description of this resource. Provide this field when you
create the resource. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `next_hop_interconnect_attachment` | String | [Output Only] The URL to an InterconnectAttachment which is the next hop
for the route.
This field will only be populated for dynamic routes generated by
Cloud Router with a linked interconnectAttachment or the static route
generated by each L2 Interconnect Attachment. |
| `route_status` | String | [Output only] The status of the route. This status only applies to
dynamic routes learned by Cloud Routers. This status is not applicable
to static routes. |
| `route_type` | String | [Output Only] The type of this route, which can be one of the following
values:
- 'TRANSIT' for a transit route that this router learned from
another Cloud Router and will readvertise to one of its BGP peers 
- 'SUBNET' for a route from a subnet of the VPC 
- 'BGP' for a route learned from a BGP peer of this router 
- 'STATIC' for a static route |
| `next_hop_origin` | String | [Output Only] Indicates the origin of the route. Can be IGP
(Interior Gateway Protocol), EGP (Exterior Gateway Protocol),
or INCOMPLETE. |
| `tags` | Vec<String> | A list of instance tags to which this route applies. |
| `warnings` | Vec<String> | [Output Only] If potential misconfigurations are detected for this
route, this field will be populated with warning messages. |
| `next_hop_network` | String | The URL of the local network if it should handle matching packets. |
| `self_link` | String | [Output Only] Server-defined fully-qualified URL for this resource. |
| `next_hop_vpn_tunnel` | String | The URL to a VpnTunnel that should handle matching packets. |
| `next_hop_gateway` | String | The URL to a gateway that should handle matching packets.
You can only specify the internet gateway using a full or
partial valid URL: projects/project/global/gateways/default-internet-gateway |
| `next_hop_hub` | String | [Output Only] The full resource name of the Network Connectivity Center hub
that will handle matching packets. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create route
route = provider.compute_api.Route {
    project = "value"  # Project ID for this request.
}

# Access route outputs
route_id = route.id
route_next_hop_ilb = route.next_hop_ilb
route_dest_range = route.dest_range
route_kind = route.kind
route_network = route.network
route_next_hop_inter_region_cost = route.next_hop_inter_region_cost
route_id = route.id
route_next_hop_peering = route.next_hop_peering
route_as_paths = route.as_paths
route_name = route.name
route_next_hop_ip = route.next_hop_ip
route_params = route.params
route_next_hop_med = route.next_hop_med
route_priority = route.priority
route_next_hop_instance = route.next_hop_instance
route_description = route.description
route_creation_timestamp = route.creation_timestamp
route_next_hop_interconnect_attachment = route.next_hop_interconnect_attachment
route_route_status = route.route_status
route_route_type = route.route_type
route_next_hop_origin = route.next_hop_origin
route_tags = route.tags
route_warnings = route.warnings
route_next_hop_network = route.next_hop_network
route_self_link = route.self_link
route_next_hop_vpn_tunnel = route.next_hop_vpn_tunnel
route_next_hop_gateway = route.next_hop_gateway
route_next_hop_hub = route.next_hop_hub
```

---


### Target_instance

Creates a TargetInstance resource in the specified project and zone using
the data included in the request.

**Operations**: ✅ Create ✅ Read ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `network` | String |  | The URL of the network this target instance uses to forward traffic.
If not specified, the traffic will be forwarded to the network that
the default network interface belongs to. |
| `name` | String |  | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `zone` | String |  | [Output Only] URL of the zone where the target instance resides.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `security_policy` | String |  | [Output Only] The resource URL for the security policy associated with this
target instance. |
| `nat_policy` | String |  | Must have a value of NO_NAT.
Protocol forwarding delivers packets while preserving the destination IP
address of the forwarding rule referencing the target instance. |
| `kind` | String |  | [Output Only] The type of the resource. Alwayscompute#targetInstance for target instances. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `instance` | String |  | A URL to the virtual machine instance that handles traffic for this target
instance. When creating a target instance, you can provide the
fully-qualified URL or a valid partial URL to the desired virtual machine.
For example, the following are all valid URLs:
   
   - https://www.googleapis.com/compute/v1/projects/project/zones/zone/instances/instance 
   - projects/project/zones/zone/instances/instance 
   - zones/zone/instances/instance |
| `project` | String | ✅ | Project ID for this request. |
| `zone` | String | ✅ | Name of the zone scoping this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `network` | String | The URL of the network this target instance uses to forward traffic.
If not specified, the traffic will be forwarded to the network that
the default network interface belongs to. |
| `name` | String | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `zone` | String | [Output Only] URL of the zone where the target instance resides.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `security_policy` | String | [Output Only] The resource URL for the security policy associated with this
target instance. |
| `nat_policy` | String | Must have a value of NO_NAT.
Protocol forwarding delivers packets while preserving the destination IP
address of the forwarding rule referencing the target instance. |
| `kind` | String | [Output Only] The type of the resource. Alwayscompute#targetInstance for target instances. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `instance` | String | A URL to the virtual machine instance that handles traffic for this target
instance. When creating a target instance, you can provide the
fully-qualified URL or a valid partial URL to the desired virtual machine.
For example, the following are all valid URLs:
   
   - https://www.googleapis.com/compute/v1/projects/project/zones/zone/instances/instance 
   - projects/project/zones/zone/instances/instance 
   - zones/zone/instances/instance |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create target_instance
target_instance = provider.compute_api.Target_instance {
    project = "value"  # Project ID for this request.
    zone = "value"  # Name of the zone scoping this request.
}

# Access target_instance outputs
target_instance_id = target_instance.id
target_instance_id = target_instance.id
target_instance_description = target_instance.description
target_instance_network = target_instance.network
target_instance_name = target_instance.name
target_instance_self_link = target_instance.self_link
target_instance_zone = target_instance.zone
target_instance_security_policy = target_instance.security_policy
target_instance_nat_policy = target_instance.nat_policy
target_instance_kind = target_instance.kind
target_instance_creation_timestamp = target_instance.creation_timestamp
target_instance_instance = target_instance.instance
```

---


### Region_snapshot_setting

Get region snapshot settings.

**Operations**: ✅ Read ✅ Update

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `access_location` | String |  | (Regional snapshots use only)Policy of which location is allowed to access
snapshot. |
| `storage_location` | String |  | Policy of which storage location is going to be resolved, and additional
data that particularizes how the policy is going to be carried out. |
| `region` | String | ✅ | Name of the region for this request. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `access_location` | String | (Regional snapshots use only)Policy of which location is allowed to access
snapshot. |
| `storage_location` | String | Policy of which storage location is going to be resolved, and additional
data that particularizes how the policy is going to be carried out. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Access region_snapshot_setting outputs
region_snapshot_setting_id = region_snapshot_setting.id
region_snapshot_setting_access_location = region_snapshot_setting.access_location
region_snapshot_setting_storage_location = region_snapshot_setting.storage_location
```

---


### Region_snapshot

Creates a snapshot in the specified region using the data included
in the request.

**Operations**: ✅ Create ✅ Read ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `snapshot_type` | String |  | Indicates the type of the snapshot. |
| `location_hint` | String |  | An opaque location hint used to place the snapshot close to other
resources.
This field is for use by internal tools that use the public API. |
| `region` | String |  | [Output Only] URL of the region where the snapshot resides. Only applicable
for regional snapshots. |
| `source_disk` | String |  | The source disk used to create this snapshot. |
| `source_snapshot_schedule_policy` | String |  | [Output Only] URL of the resource policy which created this
scheduled snapshot. |
| `source_instant_snapshot` | String |  | The source instant snapshot used to create this snapshot. You can provide
this as a partial or full URL to the resource. For example, the following
are valid values:
   
   
     - https://www.googleapis.com/compute/v1/projects/project/zones/zone/instantSnapshots/instantSnapshot
     - projects/project/zones/zone/instantSnapshots/instantSnapshot 
   - zones/zone/instantSnapshots/instantSnapshot |
| `architecture` | String |  | [Output Only] The architecture of the snapshot. Valid values are
ARM64 or X86_64. |
| `source_instant_snapshot_id` | String |  | [Output Only] The unique ID of the instant snapshot used to create this
snapshot. This value identifies the exact instant snapshot that was used to
create this snapshot. For example, if you created the snapshot from an
instant snapshot that was later deleted and recreated under the same name,
the source instant snapshot ID would identify the exact instant snapshot
that was used. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `source_snapshot_schedule_policy_id` | String |  | [Output Only] ID of the resource policy which created this
scheduled snapshot. |
| `enable_confidential_compute` | bool |  | Whether this snapshot is created from a confidential compute mode disk.
[Output Only]: This field is not set by user, but from source disk. |
| `creation_size_bytes` | String |  | [Output Only] Size in bytes of the snapshot at creation time. |
| `chain_name` | String |  | Creates the new snapshot in the snapshot chain labeled with the
specified name. The chain name must be 1-63 characters long and comply
with RFC1035. This is an uncommon option only for advanced service
owners who needs to create separate snapshot chains, for example,
for chargeback tracking. When you describe your snapshot resource, this
field is visible only if it has a non-empty value. |
| `source_disk_id` | String |  | [Output Only] The ID value of the disk used to create this snapshot. This
value may be used to determine whether the snapshot was taken from the
current or a previous instance of a given disk name. |
| `satisfies_pzi` | bool |  | Output only. Reserved for future use. |
| `labels` | HashMap<String, String> |  | Labels to apply to this snapshot. These can be later modified by
the setLabels method.
Label values may be empty. |
| `storage_bytes` | String |  | [Output Only] A size of the storage used by the snapshot. As snapshots
share storage, this number is expected to change with snapshot
creation/deletion. |
| `name` | String |  | Name of the resource; provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must be
a dash, lowercase letter, or digit, except the last character, which cannot
be a dash. |
| `licenses` | Vec<String> |  | [Output Only] A list of public visible licenses that apply to this
snapshot. This can be because the original image had licenses attached
(such as a Windows image). |
| `auto_created` | bool |  | [Output Only] Set to true if snapshots are automatically created by
applying resource policy on the target disk. |
| `source_disk_for_recovery_checkpoint` | String |  | The source disk whose recovery checkpoint will be used to create this
snapshot. |
| `source_disk_encryption_key` | String |  | The customer-supplied
encryption key of the source disk. Required if the source disk is
protected by a customer-supplied encryption key. |
| `snapshot_encryption_key` | String |  | Encrypts the snapshot using acustomer-supplied
encryption key.

After you encrypt a snapshot using a customer-supplied key, you must
provide the same key if you use the snapshot later. For example, you must
provide the encryption key when you create a disk from the encrypted
snapshot in a future request.

Customer-supplied encryption keys do not protect access to metadata
of the snapshot.

If you do not provide an encryption key when creating the snapshot, then
the snapshot will be encrypted using an automatically generated key and you
do not need to provide a key to use the snapshot later. |
| `user_licenses` | Vec<String> |  | [Output Only] A list of user provided licenses represented by a list of
URLs to the license resource. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `disk_size_gb` | String |  | [Output Only] Size of the source disk, specified in GB. |
| `satisfies_pzs` | bool |  | [Output Only] Reserved for future use. |
| `license_codes` | Vec<String> |  | [Output Only] Integer license codes indicating which licenses are attached
to this snapshot. |
| `status` | String |  | [Output Only] The status of the snapshot. This can beCREATING, DELETING, FAILED,READY, or UPLOADING. |
| `storage_bytes_status` | String |  | [Output Only] An indicator whether storageBytes is in a
stable state or it is being adjusted as a result of shared storage
reallocation. This status can either be UPDATING, meaning
the size of the snapshot is being updated, or UP_TO_DATE,
meaning the size of the snapshot is up-to-date. |
| `guest_flush` | bool |  | [Input Only] Whether to attempt an application consistent snapshot by
informing the OS to prepare for the snapshot process. |
| `storage_locations` | Vec<String> |  | Cloud Storage bucket storage location of the snapshot (regional or
multi-regional). |
| `download_bytes` | String |  | [Output Only] Number of bytes downloaded to restore a snapshot to a disk. |
| `label_fingerprint` | String |  | A fingerprint for the labels being applied to this snapshot, which is
essentially a hash of the labels set used for optimistic locking. The
fingerprint is initially generated by Compute Engine and changes after
every request to modify or update labels. You must always provide an
up-to-date fingerprint hash in order to update or change labels,
otherwise the request will fail with error412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve a snapshot. |
| `guest_os_features` | Vec<String> |  | [Output Only] A list of features to enable on the guest operating system.
Applicable only for bootable images. Read 
Enabling guest operating system features to see a list of available
options. |
| `params` | String |  | Input only. [Input Only] Additional params passed with the request, but not persisted
as part of resource payload. |
| `kind` | String |  | [Output Only] Type of the resource. Always compute#snapshot
for Snapshot resources. |
| `source_instant_snapshot_encryption_key` | String |  | Customer provided encryption key when creating Snapshot from Instant
Snapshot. |
| `region` | String | ✅ | Name of the region for this request. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `snapshot_type` | String | Indicates the type of the snapshot. |
| `location_hint` | String | An opaque location hint used to place the snapshot close to other
resources.
This field is for use by internal tools that use the public API. |
| `region` | String | [Output Only] URL of the region where the snapshot resides. Only applicable
for regional snapshots. |
| `source_disk` | String | The source disk used to create this snapshot. |
| `source_snapshot_schedule_policy` | String | [Output Only] URL of the resource policy which created this
scheduled snapshot. |
| `source_instant_snapshot` | String | The source instant snapshot used to create this snapshot. You can provide
this as a partial or full URL to the resource. For example, the following
are valid values:
   
   
     - https://www.googleapis.com/compute/v1/projects/project/zones/zone/instantSnapshots/instantSnapshot
     - projects/project/zones/zone/instantSnapshots/instantSnapshot 
   - zones/zone/instantSnapshots/instantSnapshot |
| `architecture` | String | [Output Only] The architecture of the snapshot. Valid values are
ARM64 or X86_64. |
| `source_instant_snapshot_id` | String | [Output Only] The unique ID of the instant snapshot used to create this
snapshot. This value identifies the exact instant snapshot that was used to
create this snapshot. For example, if you created the snapshot from an
instant snapshot that was later deleted and recreated under the same name,
the source instant snapshot ID would identify the exact instant snapshot
that was used. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `source_snapshot_schedule_policy_id` | String | [Output Only] ID of the resource policy which created this
scheduled snapshot. |
| `enable_confidential_compute` | bool | Whether this snapshot is created from a confidential compute mode disk.
[Output Only]: This field is not set by user, but from source disk. |
| `creation_size_bytes` | String | [Output Only] Size in bytes of the snapshot at creation time. |
| `chain_name` | String | Creates the new snapshot in the snapshot chain labeled with the
specified name. The chain name must be 1-63 characters long and comply
with RFC1035. This is an uncommon option only for advanced service
owners who needs to create separate snapshot chains, for example,
for chargeback tracking. When you describe your snapshot resource, this
field is visible only if it has a non-empty value. |
| `source_disk_id` | String | [Output Only] The ID value of the disk used to create this snapshot. This
value may be used to determine whether the snapshot was taken from the
current or a previous instance of a given disk name. |
| `satisfies_pzi` | bool | Output only. Reserved for future use. |
| `labels` | HashMap<String, String> | Labels to apply to this snapshot. These can be later modified by
the setLabels method.
Label values may be empty. |
| `storage_bytes` | String | [Output Only] A size of the storage used by the snapshot. As snapshots
share storage, this number is expected to change with snapshot
creation/deletion. |
| `name` | String | Name of the resource; provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must be
a dash, lowercase letter, or digit, except the last character, which cannot
be a dash. |
| `licenses` | Vec<String> | [Output Only] A list of public visible licenses that apply to this
snapshot. This can be because the original image had licenses attached
(such as a Windows image). |
| `auto_created` | bool | [Output Only] Set to true if snapshots are automatically created by
applying resource policy on the target disk. |
| `source_disk_for_recovery_checkpoint` | String | The source disk whose recovery checkpoint will be used to create this
snapshot. |
| `source_disk_encryption_key` | String | The customer-supplied
encryption key of the source disk. Required if the source disk is
protected by a customer-supplied encryption key. |
| `snapshot_encryption_key` | String | Encrypts the snapshot using acustomer-supplied
encryption key.

After you encrypt a snapshot using a customer-supplied key, you must
provide the same key if you use the snapshot later. For example, you must
provide the encryption key when you create a disk from the encrypted
snapshot in a future request.

Customer-supplied encryption keys do not protect access to metadata
of the snapshot.

If you do not provide an encryption key when creating the snapshot, then
the snapshot will be encrypted using an automatically generated key and you
do not need to provide a key to use the snapshot later. |
| `user_licenses` | Vec<String> | [Output Only] A list of user provided licenses represented by a list of
URLs to the license resource. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `disk_size_gb` | String | [Output Only] Size of the source disk, specified in GB. |
| `satisfies_pzs` | bool | [Output Only] Reserved for future use. |
| `license_codes` | Vec<String> | [Output Only] Integer license codes indicating which licenses are attached
to this snapshot. |
| `status` | String | [Output Only] The status of the snapshot. This can beCREATING, DELETING, FAILED,READY, or UPLOADING. |
| `storage_bytes_status` | String | [Output Only] An indicator whether storageBytes is in a
stable state or it is being adjusted as a result of shared storage
reallocation. This status can either be UPDATING, meaning
the size of the snapshot is being updated, or UP_TO_DATE,
meaning the size of the snapshot is up-to-date. |
| `guest_flush` | bool | [Input Only] Whether to attempt an application consistent snapshot by
informing the OS to prepare for the snapshot process. |
| `storage_locations` | Vec<String> | Cloud Storage bucket storage location of the snapshot (regional or
multi-regional). |
| `download_bytes` | String | [Output Only] Number of bytes downloaded to restore a snapshot to a disk. |
| `label_fingerprint` | String | A fingerprint for the labels being applied to this snapshot, which is
essentially a hash of the labels set used for optimistic locking. The
fingerprint is initially generated by Compute Engine and changes after
every request to modify or update labels. You must always provide an
up-to-date fingerprint hash in order to update or change labels,
otherwise the request will fail with error412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve a snapshot. |
| `guest_os_features` | Vec<String> | [Output Only] A list of features to enable on the guest operating system.
Applicable only for bootable images. Read 
Enabling guest operating system features to see a list of available
options. |
| `params` | String | Input only. [Input Only] Additional params passed with the request, but not persisted
as part of resource payload. |
| `kind` | String | [Output Only] Type of the resource. Always compute#snapshot
for Snapshot resources. |
| `source_instant_snapshot_encryption_key` | String | Customer provided encryption key when creating Snapshot from Instant
Snapshot. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create region_snapshot
region_snapshot = provider.compute_api.Region_snapshot {
    region = "value"  # Name of the region for this request.
    project = "value"  # Project ID for this request.
}

# Access region_snapshot outputs
region_snapshot_id = region_snapshot.id
region_snapshot_snapshot_type = region_snapshot.snapshot_type
region_snapshot_location_hint = region_snapshot.location_hint
region_snapshot_region = region_snapshot.region
region_snapshot_source_disk = region_snapshot.source_disk
region_snapshot_source_snapshot_schedule_policy = region_snapshot.source_snapshot_schedule_policy
region_snapshot_source_instant_snapshot = region_snapshot.source_instant_snapshot
region_snapshot_architecture = region_snapshot.architecture
region_snapshot_source_instant_snapshot_id = region_snapshot.source_instant_snapshot_id
region_snapshot_self_link = region_snapshot.self_link
region_snapshot_source_snapshot_schedule_policy_id = region_snapshot.source_snapshot_schedule_policy_id
region_snapshot_enable_confidential_compute = region_snapshot.enable_confidential_compute
region_snapshot_creation_size_bytes = region_snapshot.creation_size_bytes
region_snapshot_chain_name = region_snapshot.chain_name
region_snapshot_source_disk_id = region_snapshot.source_disk_id
region_snapshot_satisfies_pzi = region_snapshot.satisfies_pzi
region_snapshot_labels = region_snapshot.labels
region_snapshot_storage_bytes = region_snapshot.storage_bytes
region_snapshot_name = region_snapshot.name
region_snapshot_licenses = region_snapshot.licenses
region_snapshot_auto_created = region_snapshot.auto_created
region_snapshot_source_disk_for_recovery_checkpoint = region_snapshot.source_disk_for_recovery_checkpoint
region_snapshot_source_disk_encryption_key = region_snapshot.source_disk_encryption_key
region_snapshot_snapshot_encryption_key = region_snapshot.snapshot_encryption_key
region_snapshot_user_licenses = region_snapshot.user_licenses
region_snapshot_creation_timestamp = region_snapshot.creation_timestamp
region_snapshot_description = region_snapshot.description
region_snapshot_id = region_snapshot.id
region_snapshot_disk_size_gb = region_snapshot.disk_size_gb
region_snapshot_satisfies_pzs = region_snapshot.satisfies_pzs
region_snapshot_license_codes = region_snapshot.license_codes
region_snapshot_status = region_snapshot.status
region_snapshot_storage_bytes_status = region_snapshot.storage_bytes_status
region_snapshot_guest_flush = region_snapshot.guest_flush
region_snapshot_storage_locations = region_snapshot.storage_locations
region_snapshot_download_bytes = region_snapshot.download_bytes
region_snapshot_label_fingerprint = region_snapshot.label_fingerprint
region_snapshot_guest_os_features = region_snapshot.guest_os_features
region_snapshot_params = region_snapshot.params
region_snapshot_kind = region_snapshot.kind
region_snapshot_source_instant_snapshot_encryption_key = region_snapshot.source_instant_snapshot_encryption_key
```

---


### Region

Returns the specified Region resource.

To decrease latency for this method, you can optionally omit any unneeded
information from the response by using a field mask. This practice is
especially recommended for unused quota information (the `quotas` field).
To exclude one or more fields, set your request's `fields` query parameter
to only include the fields you need. For example, to only include the `id`
and `selfLink` fields, add the query parameter `?fields=id,selfLink` to
your request.

This method fails if the quota information is unavailable for the region
and if the organization policy constraint
compute.requireBasicQuotaInResponse is enforced. This
constraint, when enforced, disables the fail-open behaviour when quota
information (the `items.quotas` field) is unavailable for the region.
It is recommended to use the default setting
for the constraint unless your application requires the fail-closed
behaviour for this method.

**Operations**: ✅ Read

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `name` | String | [Output Only] Name of the resource. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `kind` | String | [Output Only] Type of the resource. Always compute#region for
regions. |
| `description` | String | [Output Only] Textual description of the resource. |
| `deprecated` | String | [Output Only] The deprecation status associated with this region. |
| `quotas` | Vec<String> | [Output Only] Quotas assigned to this region. |
| `zones` | Vec<String> | [Output Only] A list of zones available in this region, in the form of
resource URLs. |
| `quota_status_warning` | String | [Output Only] Warning of fetching the `quotas` field for this region. This
field is populated only if fetching of the `quotas` field fails. |
| `supports_pzs` | bool | [Output Only] Reserved for future use. |
| `status` | String | [Output Only] Status of the region, either UP orDOWN. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Access region outputs
region_id = region.id
region_name = region.name
region_self_link = region.self_link
region_creation_timestamp = region.creation_timestamp
region_kind = region.kind
region_description = region.description
region_deprecated = region.deprecated
region_quotas = region.quotas
region_zones = region.zones
region_quota_status_warning = region.quota_status_warning
region_supports_pzs = region.supports_pzs
region_status = region.status
region_id = region.id
```

---


### Global_addresse

Creates an address resource in the specified project by using the data
included in the request.

**Operations**: ✅ Create ✅ Read ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `purpose` | String |  | The purpose of this resource, which can be one of the following values:
   
   
     - GCE_ENDPOINT for addresses that are used by VM
     instances, alias IP ranges, load balancers, and similar resources.
     - DNS_RESOLVER for a DNS resolver address in a subnetwork
       for a Cloud DNS  inbound
       forwarder IP addresses (regional internal IP address in a subnet of
       a VPC network)
     - VPC_PEERING for global internal IP addresses used for
      
          private services access allocated ranges.
     - NAT_AUTO for the regional external IP addresses used by
          Cloud NAT when allocating addresses using
          
          automatic NAT IP address allocation.
     - IPSEC_INTERCONNECT for addresses created from a private
     IP range that are reserved for a VLAN attachment in an
     *HA VPN over Cloud Interconnect* configuration. These addresses
     are regional resources.
     - `SHARED_LOADBALANCER_VIP` for an internal IP address that is assigned
     to multiple internal forwarding rules.
     - `PRIVATE_SERVICE_CONNECT` for a private network address that is
     used to configure Private Service Connect. Only global internal addresses
     can use this purpose. |
| `users` | Vec<String> |  | [Output Only] The URLs of the resources that are using this address. |
| `ip_collection` | String |  | Reference to the source of external IPv4 addresses,
like a PublicDelegatedPrefix (PDP) for BYOIP.
The PDP must support enhanced IPv4 allocations.

Use one of the following formats to specify a PDP when reserving an
external IPv4 address using BYOIP.
   
   - 
   Full resource URL, as inhttps://www.googleapis.com/compute/v1/projects/projectId/regions/region/publicDelegatedPrefixes/pdp-name
   - 
   Partial URL, as in
     
      
          - projects/projectId/regions/region/publicDelegatedPrefixes/pdp-name
          - regions/region/publicDelegatedPrefixes/pdp-name |
| `name` | String |  | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?`. The first character
must be a lowercase letter, and all following characters (except for the
last character) must be a dash, lowercase letter, or digit. The last
character must be a lowercase letter or digit. |
| `label_fingerprint` | String |  | A fingerprint for the labels being applied to this Address, which is
essentially a hash of the labels set used for optimistic locking. The
fingerprint is initially generated by Compute Engine and changes after
every request to modify or update labels. You must always provide an
up-to-date fingerprint hash in order to update or change labels,
otherwise the request will fail with error412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve an Address. |
| `labels` | HashMap<String, String> |  | Labels for this resource. These can only be added or modified by thesetLabels method. Each label key/value pair must comply withRFC1035.
Label values may be empty. |
| `description` | String |  | An optional description of this resource. Provide this field when you
create the resource. |
| `ip_version` | String |  | The IP version that will be used by this address. Valid options areIPV4 or IPV6. |
| `network` | String |  | The URL of the network in which to reserve the address. This field can
only be used with INTERNAL type with theVPC_PEERING purpose. |
| `prefix_length` | i64 |  | The prefix length if the resource represents an IP range. |
| `region` | String |  | [Output Only] The URL of the region where a regional address resides.
For regional addresses, you must specify the region as a path parameter in
the HTTP request URL. *This field is not applicable to global
addresses.* |
| `kind` | String |  | [Output Only] Type of the resource. Always compute#address for
addresses. |
| `address_type` | String |  | The type of address to reserve, either INTERNAL orEXTERNAL. If unspecified, defaults to EXTERNAL. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `ipv6_endpoint_type` | String |  | The endpoint type of this address, which should be VM
or NETLB. This is used for deciding which type of endpoint
this address can be used after the external IPv6 address reservation. |
| `status` | String |  | [Output Only] The status of the address, which can be one ofRESERVING, RESERVED, or IN_USE.
An address that is RESERVING is currently in the process of
being reserved. A RESERVED address is currently reserved and
available to use. An IN_USE address is currently being used
by another resource and is not available. |
| `subnetwork` | String |  | The URL of the subnetwork in which to reserve the address. If an IP address
is specified, it must be within the subnetwork's IP range. This field can
only be used with INTERNAL type with aGCE_ENDPOINT or DNS_RESOLVER purpose. |
| `network_tier` | String |  | This signifies the networking tier used for configuring this address and
can only take the following values: PREMIUM orSTANDARD. Internal IP addresses are always Premium Tier;
global external IP addresses are always Premium Tier; regional external IP
addresses can be either Standard or Premium Tier.

If this field is not specified, it is assumed to be PREMIUM. |
| `address` | String |  | The static IP address represented by this resource. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `purpose` | String | The purpose of this resource, which can be one of the following values:
   
   
     - GCE_ENDPOINT for addresses that are used by VM
     instances, alias IP ranges, load balancers, and similar resources.
     - DNS_RESOLVER for a DNS resolver address in a subnetwork
       for a Cloud DNS  inbound
       forwarder IP addresses (regional internal IP address in a subnet of
       a VPC network)
     - VPC_PEERING for global internal IP addresses used for
      
          private services access allocated ranges.
     - NAT_AUTO for the regional external IP addresses used by
          Cloud NAT when allocating addresses using
          
          automatic NAT IP address allocation.
     - IPSEC_INTERCONNECT for addresses created from a private
     IP range that are reserved for a VLAN attachment in an
     *HA VPN over Cloud Interconnect* configuration. These addresses
     are regional resources.
     - `SHARED_LOADBALANCER_VIP` for an internal IP address that is assigned
     to multiple internal forwarding rules.
     - `PRIVATE_SERVICE_CONNECT` for a private network address that is
     used to configure Private Service Connect. Only global internal addresses
     can use this purpose. |
| `users` | Vec<String> | [Output Only] The URLs of the resources that are using this address. |
| `ip_collection` | String | Reference to the source of external IPv4 addresses,
like a PublicDelegatedPrefix (PDP) for BYOIP.
The PDP must support enhanced IPv4 allocations.

Use one of the following formats to specify a PDP when reserving an
external IPv4 address using BYOIP.
   
   - 
   Full resource URL, as inhttps://www.googleapis.com/compute/v1/projects/projectId/regions/region/publicDelegatedPrefixes/pdp-name
   - 
   Partial URL, as in
     
      
          - projects/projectId/regions/region/publicDelegatedPrefixes/pdp-name
          - regions/region/publicDelegatedPrefixes/pdp-name |
| `name` | String | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?`. The first character
must be a lowercase letter, and all following characters (except for the
last character) must be a dash, lowercase letter, or digit. The last
character must be a lowercase letter or digit. |
| `label_fingerprint` | String | A fingerprint for the labels being applied to this Address, which is
essentially a hash of the labels set used for optimistic locking. The
fingerprint is initially generated by Compute Engine and changes after
every request to modify or update labels. You must always provide an
up-to-date fingerprint hash in order to update or change labels,
otherwise the request will fail with error412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve an Address. |
| `labels` | HashMap<String, String> | Labels for this resource. These can only be added or modified by thesetLabels method. Each label key/value pair must comply withRFC1035.
Label values may be empty. |
| `description` | String | An optional description of this resource. Provide this field when you
create the resource. |
| `ip_version` | String | The IP version that will be used by this address. Valid options areIPV4 or IPV6. |
| `network` | String | The URL of the network in which to reserve the address. This field can
only be used with INTERNAL type with theVPC_PEERING purpose. |
| `prefix_length` | i64 | The prefix length if the resource represents an IP range. |
| `region` | String | [Output Only] The URL of the region where a regional address resides.
For regional addresses, you must specify the region as a path parameter in
the HTTP request URL. *This field is not applicable to global
addresses.* |
| `kind` | String | [Output Only] Type of the resource. Always compute#address for
addresses. |
| `address_type` | String | The type of address to reserve, either INTERNAL orEXTERNAL. If unspecified, defaults to EXTERNAL. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `ipv6_endpoint_type` | String | The endpoint type of this address, which should be VM
or NETLB. This is used for deciding which type of endpoint
this address can be used after the external IPv6 address reservation. |
| `status` | String | [Output Only] The status of the address, which can be one ofRESERVING, RESERVED, or IN_USE.
An address that is RESERVING is currently in the process of
being reserved. A RESERVED address is currently reserved and
available to use. An IN_USE address is currently being used
by another resource and is not available. |
| `subnetwork` | String | The URL of the subnetwork in which to reserve the address. If an IP address
is specified, it must be within the subnetwork's IP range. This field can
only be used with INTERNAL type with aGCE_ENDPOINT or DNS_RESOLVER purpose. |
| `network_tier` | String | This signifies the networking tier used for configuring this address and
can only take the following values: PREMIUM orSTANDARD. Internal IP addresses are always Premium Tier;
global external IP addresses are always Premium Tier; regional external IP
addresses can be either Standard or Premium Tier.

If this field is not specified, it is assumed to be PREMIUM. |
| `address` | String | The static IP address represented by this resource. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create global_addresse
global_addresse = provider.compute_api.Global_addresse {
    project = "value"  # Project ID for this request.
}

# Access global_addresse outputs
global_addresse_id = global_addresse.id
global_addresse_id = global_addresse.id
global_addresse_purpose = global_addresse.purpose
global_addresse_users = global_addresse.users
global_addresse_ip_collection = global_addresse.ip_collection
global_addresse_name = global_addresse.name
global_addresse_label_fingerprint = global_addresse.label_fingerprint
global_addresse_labels = global_addresse.labels
global_addresse_description = global_addresse.description
global_addresse_ip_version = global_addresse.ip_version
global_addresse_network = global_addresse.network
global_addresse_prefix_length = global_addresse.prefix_length
global_addresse_region = global_addresse.region
global_addresse_kind = global_addresse.kind
global_addresse_address_type = global_addresse.address_type
global_addresse_creation_timestamp = global_addresse.creation_timestamp
global_addresse_ipv6_endpoint_type = global_addresse.ipv6_endpoint_type
global_addresse_status = global_addresse.status
global_addresse_subnetwork = global_addresse.subnetwork
global_addresse_network_tier = global_addresse.network_tier
global_addresse_address = global_addresse.address
global_addresse_self_link = global_addresse.self_link
```

---


### Disk_setting

Get Zonal Disk Settings.

**Operations**: ✅ Read ✅ Update

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `access_location` | String |  | Policy of which locations are allowed to create regional
snapshots. |
| `project` | String | ✅ | Project ID for this request. |
| `zone` | String | ✅ | The name of the zone for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `access_location` | String | Policy of which locations are allowed to create regional
snapshots. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Access disk_setting outputs
disk_setting_id = disk_setting.id
disk_setting_access_location = disk_setting.access_location
```

---


### Resource_policie

Creates a new resource policy.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `workload_policy` | String |  | Resource policy for defining instance placement for MIGs. |
| `kind` | String |  | [Output Only] Type of the resource. Alwayscompute#resource_policies for resource policies. |
| `status` | String |  | [Output Only] The status of resource policy creation. |
| `self_link` | String |  | [Output Only] Server-defined fully-qualified URL for this resource. |
| `description` | String |  |  |
| `group_placement_policy` | String |  | Resource policy for instances for placement configuration. |
| `instance_schedule_policy` | String |  | Resource policy for scheduling instance operations. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `disk_consistency_group_policy` | String |  | Resource policy for disk consistency groups. |
| `resource_status` | String |  | [Output Only] The system status of the resource policy. |
| `name` | String |  | The name of the resource, provided by the client when initially creating
the resource. The resource name must be 1-63 characters long, and comply
withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must be
a dash, lowercase letter, or digit, except the last character, which cannot
be a dash. |
| `snapshot_schedule_policy` | String |  | Resource policy for persistent disks for creating snapshots. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `region` | String |  |  |
| `region` | String | ✅ | Name of the region for this request. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `workload_policy` | String | Resource policy for defining instance placement for MIGs. |
| `kind` | String | [Output Only] Type of the resource. Alwayscompute#resource_policies for resource policies. |
| `status` | String | [Output Only] The status of resource policy creation. |
| `self_link` | String | [Output Only] Server-defined fully-qualified URL for this resource. |
| `description` | String |  |
| `group_placement_policy` | String | Resource policy for instances for placement configuration. |
| `instance_schedule_policy` | String | Resource policy for scheduling instance operations. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `disk_consistency_group_policy` | String | Resource policy for disk consistency groups. |
| `resource_status` | String | [Output Only] The system status of the resource policy. |
| `name` | String | The name of the resource, provided by the client when initially creating
the resource. The resource name must be 1-63 characters long, and comply
withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must be
a dash, lowercase letter, or digit, except the last character, which cannot
be a dash. |
| `snapshot_schedule_policy` | String | Resource policy for persistent disks for creating snapshots. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `region` | String |  |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create resource_policie
resource_policie = provider.compute_api.Resource_policie {
    region = "value"  # Name of the region for this request.
    project = "value"  # Project ID for this request.
}

# Access resource_policie outputs
resource_policie_id = resource_policie.id
resource_policie_workload_policy = resource_policie.workload_policy
resource_policie_kind = resource_policie.kind
resource_policie_status = resource_policie.status
resource_policie_self_link = resource_policie.self_link
resource_policie_description = resource_policie.description
resource_policie_group_placement_policy = resource_policie.group_placement_policy
resource_policie_instance_schedule_policy = resource_policie.instance_schedule_policy
resource_policie_creation_timestamp = resource_policie.creation_timestamp
resource_policie_disk_consistency_group_policy = resource_policie.disk_consistency_group_policy
resource_policie_resource_status = resource_policie.resource_status
resource_policie_name = resource_policie.name
resource_policie_snapshot_schedule_policy = resource_policie.snapshot_schedule_policy
resource_policie_id = resource_policie.id
resource_policie_region = resource_policie.region
```

---


### Storage_pool

Creates a storage pool in the specified project using the data
in the request.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `self_link_with_id` | String |  | [Output Only] Server-defined URL for this resource's resource id. |
| `pool_provisioned_iops` | String |  | Provisioned IOPS of the storage pool. Only relevant if the storage pool
type is hyperdisk-balanced. |
| `resource_status` | String |  | [Output Only] Status information for the storage pool resource. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `labels` | HashMap<String, String> |  | Labels to apply to this storage pool. These can be later modified by
the setLabels method. |
| `self_link` | String |  | [Output Only] Server-defined fully-qualified URL for this resource. |
| `name` | String |  | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?`
which means the first character must be a lowercase letter, and all
following characters must be a dash, lowercase letter, or digit, except
the last character, which cannot be a dash. |
| `state` | String |  | [Output Only] The status of storage pool creation.
   
   
     - CREATING: Storage pool is provisioning.
     storagePool.
     - FAILED: Storage pool creation failed.
     - READY: Storage pool is ready for use.
     - DELETING: Storage pool is deleting. |
| `status` | String |  | [Output Only] Status information for the storage pool resource. |
| `storage_pool_type` | String |  | Type of the storage pool. |
| `pool_provisioned_throughput` | String |  | Provisioned throughput of the storage pool in MiB/s. Only relevant if the
storage pool type is hyperdisk-balanced or hyperdisk-throughput. |
| `zone` | String |  | [Output Only] URL of the zone where the storage pool resides.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `capacity_provisioning_type` | String |  | Provisioning type of the byte capacity of the pool. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `pool_provisioned_capacity_gb` | String |  | Size of the storage pool in GiB. For more information about the size
limits, see https://cloud.google.com/compute/docs/disks/storage-pools. |
| `label_fingerprint` | String |  | A fingerprint for the labels being applied to this storage pool, which is
essentially a hash of the labels set used for optimistic locking. The
fingerprint is initially generated by Compute Engine and changes after
every request to modify or update labels. You must always provide an
up-to-date fingerprint hash in order to update or change labels,
otherwise the request will fail with error412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve a storage pool. |
| `kind` | String |  | [Output Only] Type of the resource. Always compute#storagePool
for storage pools. |
| `performance_provisioning_type` | String |  | Provisioning type of the performance-related parameters of the pool,
such as throughput and IOPS. |
| `project` | String | ✅ | Project ID for this request. |
| `zone` | String | ✅ | The name of the zone for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `self_link_with_id` | String | [Output Only] Server-defined URL for this resource's resource id. |
| `pool_provisioned_iops` | String | Provisioned IOPS of the storage pool. Only relevant if the storage pool
type is hyperdisk-balanced. |
| `resource_status` | String | [Output Only] Status information for the storage pool resource. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `labels` | HashMap<String, String> | Labels to apply to this storage pool. These can be later modified by
the setLabels method. |
| `self_link` | String | [Output Only] Server-defined fully-qualified URL for this resource. |
| `name` | String | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?`
which means the first character must be a lowercase letter, and all
following characters must be a dash, lowercase letter, or digit, except
the last character, which cannot be a dash. |
| `state` | String | [Output Only] The status of storage pool creation.
   
   
     - CREATING: Storage pool is provisioning.
     storagePool.
     - FAILED: Storage pool creation failed.
     - READY: Storage pool is ready for use.
     - DELETING: Storage pool is deleting. |
| `status` | String | [Output Only] Status information for the storage pool resource. |
| `storage_pool_type` | String | Type of the storage pool. |
| `pool_provisioned_throughput` | String | Provisioned throughput of the storage pool in MiB/s. Only relevant if the
storage pool type is hyperdisk-balanced or hyperdisk-throughput. |
| `zone` | String | [Output Only] URL of the zone where the storage pool resides.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `capacity_provisioning_type` | String | Provisioning type of the byte capacity of the pool. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `pool_provisioned_capacity_gb` | String | Size of the storage pool in GiB. For more information about the size
limits, see https://cloud.google.com/compute/docs/disks/storage-pools. |
| `label_fingerprint` | String | A fingerprint for the labels being applied to this storage pool, which is
essentially a hash of the labels set used for optimistic locking. The
fingerprint is initially generated by Compute Engine and changes after
every request to modify or update labels. You must always provide an
up-to-date fingerprint hash in order to update or change labels,
otherwise the request will fail with error412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve a storage pool. |
| `kind` | String | [Output Only] Type of the resource. Always compute#storagePool
for storage pools. |
| `performance_provisioning_type` | String | Provisioning type of the performance-related parameters of the pool,
such as throughput and IOPS. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create storage_pool
storage_pool = provider.compute_api.Storage_pool {
    project = "value"  # Project ID for this request.
    zone = "value"  # The name of the zone for this request.
}

# Access storage_pool outputs
storage_pool_id = storage_pool.id
storage_pool_self_link_with_id = storage_pool.self_link_with_id
storage_pool_pool_provisioned_iops = storage_pool.pool_provisioned_iops
storage_pool_resource_status = storage_pool.resource_status
storage_pool_id = storage_pool.id
storage_pool_description = storage_pool.description
storage_pool_labels = storage_pool.labels
storage_pool_self_link = storage_pool.self_link
storage_pool_name = storage_pool.name
storage_pool_state = storage_pool.state
storage_pool_status = storage_pool.status
storage_pool_storage_pool_type = storage_pool.storage_pool_type
storage_pool_pool_provisioned_throughput = storage_pool.pool_provisioned_throughput
storage_pool_zone = storage_pool.zone
storage_pool_capacity_provisioning_type = storage_pool.capacity_provisioning_type
storage_pool_creation_timestamp = storage_pool.creation_timestamp
storage_pool_pool_provisioned_capacity_gb = storage_pool.pool_provisioned_capacity_gb
storage_pool_label_fingerprint = storage_pool.label_fingerprint
storage_pool_kind = storage_pool.kind
storage_pool_performance_provisioning_type = storage_pool.performance_provisioning_type
```

---


### Target_pool

Creates a target pool in the specified project and region using
the data included in the request.

**Operations**: ✅ Create ✅ Read ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `kind` | String |  | [Output Only] Type of the resource. Always compute#targetPool
for target pools. |
| `region` | String |  | [Output Only] URL of the region where the target pool resides. |
| `security_policy` | String |  | [Output Only] The resource URL for the security policy associated with this
target pool. |
| `backup_pool` | String |  | The server-defined URL for the resource. This field is applicable only when
the containing target pool is serving a forwarding rule as the primary
pool, and its failoverRatio field is properly set to a value
between [0, 1].backupPool and failoverRatio together define
the fallback behavior of the primary target pool: if the ratio of the
healthy instances in the primary pool is at or belowfailoverRatio, traffic arriving at the load-balanced
IP will be directed to the backup pool.

In case where failoverRatio and backupPool
are not set, or all the instances in the backup pool are unhealthy,
the traffic will be directed back to the primary pool in the "force"
mode, where traffic will be spread to the healthy instances with the
best effort, or to all instances when no instance is healthy. |
| `instances` | Vec<String> |  | A list of resource URLs to the virtual machine instances serving this pool.
They must live in zones contained in the same region as this pool. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `session_affinity` | String |  | Session affinity option, must be one of the following values: 
NONE: Connections from the same client IP may go to any
    instance in the pool. 
CLIENT_IP: Connections from the same client IP will go
    to the same instance in
    the pool while that instance remains healthy. 
CLIENT_IP_PROTO: Connections from the same client IP
    with the same IP protocol will go to the same instance in the
    pool while that instance remains healthy. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `failover_ratio` | f64 |  | This field is applicable only when the containing target pool is serving a
forwarding rule as the primary pool (i.e., not as a backup pool to some
other target pool). The value of the field must be in [0, 1].

If set, backupPool must also be set. They together define
the fallback behavior of the primary target pool: if the ratio of the
healthy instances in the primary pool is at or below this number,
traffic arriving at the load-balanced IP will be directed to the
backup pool.

In case where failoverRatio is not set or all the
instances in the backup pool are unhealthy, the traffic will be
directed back to the primary pool in the "force" mode, where traffic
will be spread to the healthy instances with the
best effort, or to all instances when no instance is healthy. |
| `name` | String |  | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `health_checks` | Vec<String> |  | The URL of the HttpHealthCheck resource. A member instance in this
pool is considered healthy if and only if the health checks pass.
Only legacy HttpHealthChecks are supported. Only one health check may be
specified. |
| `project` | String | ✅ | Project ID for this request. |
| `region` | String | ✅ | Name of the region scoping this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `kind` | String | [Output Only] Type of the resource. Always compute#targetPool
for target pools. |
| `region` | String | [Output Only] URL of the region where the target pool resides. |
| `security_policy` | String | [Output Only] The resource URL for the security policy associated with this
target pool. |
| `backup_pool` | String | The server-defined URL for the resource. This field is applicable only when
the containing target pool is serving a forwarding rule as the primary
pool, and its failoverRatio field is properly set to a value
between [0, 1].backupPool and failoverRatio together define
the fallback behavior of the primary target pool: if the ratio of the
healthy instances in the primary pool is at or belowfailoverRatio, traffic arriving at the load-balanced
IP will be directed to the backup pool.

In case where failoverRatio and backupPool
are not set, or all the instances in the backup pool are unhealthy,
the traffic will be directed back to the primary pool in the "force"
mode, where traffic will be spread to the healthy instances with the
best effort, or to all instances when no instance is healthy. |
| `instances` | Vec<String> | A list of resource URLs to the virtual machine instances serving this pool.
They must live in zones contained in the same region as this pool. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `session_affinity` | String | Session affinity option, must be one of the following values: 
NONE: Connections from the same client IP may go to any
    instance in the pool. 
CLIENT_IP: Connections from the same client IP will go
    to the same instance in
    the pool while that instance remains healthy. 
CLIENT_IP_PROTO: Connections from the same client IP
    with the same IP protocol will go to the same instance in the
    pool while that instance remains healthy. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `failover_ratio` | f64 | This field is applicable only when the containing target pool is serving a
forwarding rule as the primary pool (i.e., not as a backup pool to some
other target pool). The value of the field must be in [0, 1].

If set, backupPool must also be set. They together define
the fallback behavior of the primary target pool: if the ratio of the
healthy instances in the primary pool is at or below this number,
traffic arriving at the load-balanced IP will be directed to the
backup pool.

In case where failoverRatio is not set or all the
instances in the backup pool are unhealthy, the traffic will be
directed back to the primary pool in the "force" mode, where traffic
will be spread to the healthy instances with the
best effort, or to all instances when no instance is healthy. |
| `name` | String | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `health_checks` | Vec<String> | The URL of the HttpHealthCheck resource. A member instance in this
pool is considered healthy if and only if the health checks pass.
Only legacy HttpHealthChecks are supported. Only one health check may be
specified. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create target_pool
target_pool = provider.compute_api.Target_pool {
    project = "value"  # Project ID for this request.
    region = "value"  # Name of the region scoping this request.
}

# Access target_pool outputs
target_pool_id = target_pool.id
target_pool_id = target_pool.id
target_pool_kind = target_pool.kind
target_pool_region = target_pool.region
target_pool_security_policy = target_pool.security_policy
target_pool_backup_pool = target_pool.backup_pool
target_pool_instances = target_pool.instances
target_pool_creation_timestamp = target_pool.creation_timestamp
target_pool_session_affinity = target_pool.session_affinity
target_pool_description = target_pool.description
target_pool_failover_ratio = target_pool.failover_ratio
target_pool_name = target_pool.name
target_pool_self_link = target_pool.self_link
target_pool_health_checks = target_pool.health_checks
```

---


### Forwarding_rule

Creates a ForwardingRule resource in the specified project and region using
the data included in the request.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `backend_service` | String |  | Identifies the backend service to which the forwarding rule sends traffic.
Required for internal and external passthrough Network Load Balancers;
must be omitted for all other load balancer types. |
| `allow_global_access` | bool |  | If set to true, clients can access the internal passthrough Network Load
Balancers, the regional internal Application Load Balancer, and the
regional internal proxy Network Load Balancer from all regions.
If false, only allows access from the local region the load balancer is
located at. Note that for INTERNAL_MANAGED forwarding rules, this field
cannot be changed after the forwarding rule is created. |
| `load_balancing_scheme` | String |  | Specifies the forwarding rule type.

For more information about forwarding rules, refer to
Forwarding rule concepts. |
| `ports` | Vec<String> |  | The ports, portRange, and allPorts
fields are mutually exclusive. Only packets addressed to ports in the
specified range will be forwarded to the backends configured with this
forwarding rule.

The ports field has the following limitations:
   
   - It requires that the forwarding rule IPProtocol be TCP,
   UDP, or SCTP, and
   - It's applicable only to the following products: internal passthrough
   Network Load Balancers, backend service-based external passthrough Network
   Load Balancers, and internal protocol forwarding.
   - You can specify a list of up to five ports by number, separated by
   commas. The ports can be contiguous or discontiguous.



For external forwarding rules, two or more forwarding rules cannot use the
same [IPAddress, IPProtocol] pair if they share at least one
port number.

For internal forwarding rules within the same VPC network, two or more
forwarding rules cannot use the same [IPAddress, IPProtocol]
pair if they share at least one port number.

@pattern: \\d+(?:-\\d+)? |
| `is_mirroring_collector` | bool |  | Indicates whether or not this load balancer can be used as a collector for
packet mirroring. To prevent mirroring loops, instances behind this
load balancer will not have their traffic mirrored even if aPacketMirroring rule applies to them.
This can only be set to true for load balancers that have theirloadBalancingScheme set to INTERNAL. |
| `self_link_with_id` | String |  | [Output Only] Server-defined URL for this resource with the resource id. |
| `base_forwarding_rule` | String |  | [Output Only] The URL for the corresponding base forwarding rule. By base
forwarding rule, we mean the forwarding rule that has the same IP address,
protocol, and port settings with the current forwarding rule, but without
sourceIPRanges specified.
Always empty if the current forwarding rule does not have sourceIPRanges
specified. |
| `labels` | HashMap<String, String> |  | Labels for this resource. These can only be added or modified by thesetLabels method. Each label key/value pair must comply withRFC1035.
Label values may be empty. |
| `service_name` | String |  | [Output Only]
The internal fully qualified service name for this forwarding rule.

This field is only used for internal load balancing. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `kind` | String |  | [Output Only] Type of the resource. Alwayscompute#forwardingRule for forwarding rule resources. |
| `region` | String |  | [Output Only] URL of the region where the regional forwarding rule resides.
This field is not applicable to global forwarding rules.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `service_label` | String |  | An optional prefix to the service name for this forwarding rule.
If specified, the prefix is the first label of the fully qualified service
name.

The label must be 1-63 characters long, and comply withRFC1035.
Specifically, the label must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash.

This field is only used for internal load balancing. |
| `subnetwork` | String |  | This field identifies the subnetwork that the load balanced IP should
belong to for this forwarding rule, used with internal load balancers and
external passthrough Network Load Balancers with IPv6.

If the network specified is in auto subnet mode, this field is optional.
However, a subnetwork must be specified if the network is in custom subnet
mode or when creating external forwarding rule with IPv6. |
| `ip_version` | String |  | The IP Version that will be used by this forwarding rule.  Valid options
are IPV4 or IPV6. |
| `ip_collection` | String |  | Resource reference of a PublicDelegatedPrefix. The PDP must
be a sub-PDP in EXTERNAL_IPV6_FORWARDING_RULE_CREATION mode.

Use one of the following formats to specify a sub-PDP when creating an IPv6
NetLB forwarding rule using BYOIP:
Full resource URL, as inhttps://www.googleapis.com/compute/v1/projects/project_id/regions/region/publicDelegatedPrefixes/sub-pdp-name
Partial URL, as in:
   
   - projects/project_id/regions/region/publicDelegatedPrefixes/sub-pdp-name
   - regions/region/publicDelegatedPrefixes/sub-pdp-name |
| `no_automate_dns_zone` | bool |  | This is used in PSC consumer ForwardingRule to control whether it should
try to auto-generate a DNS zone or not. Non-PSC forwarding rules do not use
this field. Once set, this field is not mutable. |
| `external_managed_backend_bucket_migration_state` | String |  | Specifies the canary migration state for the backend buckets attached to
this forwarding rule. Possible values are PREPARE, TEST_BY_PERCENTAGE, and
TEST_ALL_TRAFFIC.

To begin the migration from EXTERNAL to EXTERNAL_MANAGED, the state must be
changed to PREPARE. The state must be changed to TEST_ALL_TRAFFIC before
the loadBalancingScheme can be changed to EXTERNAL_MANAGED. Optionally, the
TEST_BY_PERCENTAGE state can be used to migrate traffic to backend buckets
attached to this forwarding rule by percentage using
externalManagedBackendBucketMigrationTestingPercentage.

Rolling back a migration requires the states to be set in reverse order. So
changing the scheme from EXTERNAL_MANAGED to EXTERNAL requires the state to
be set to TEST_ALL_TRAFFIC at the same time. Optionally, the
TEST_BY_PERCENTAGE state can be used to migrate some traffic back to
EXTERNAL or PREPARE can be used to migrate all traffic back to EXTERNAL. |
| `source_ip_ranges` | Vec<String> |  | If not empty, this forwarding rule will only forward the traffic when the
source IP address matches one of the IP addresses or CIDR ranges set here.
Note that a forwarding rule can only have up to 64 source IP ranges, and
this field can only be used with a regional forwarding rule whose scheme isEXTERNAL.
Each source_ip_range entry should be either an IP address (for
example, 1.2.3.4) or a CIDR range (for example, 1.2.3.0/24). |
| `external_managed_backend_bucket_migration_testing_percentage` | f64 |  | Determines the fraction of requests to backend buckets that should be
processed by the global external Application Load Balancer.

The value of this field must be in the range [0, 100].

This value can only be set if the loadBalancingScheme in the BackendService
is set to EXTERNAL (when using the classic Application Load Balancer) and
the migration state is TEST_BY_PERCENTAGE. |
| `ip_address` | String |  | IP address for which this forwarding rule accepts traffic. When a client
sends traffic to this IP address, the forwarding rule directs the traffic
to the referenced target or backendService.
While creating a forwarding rule, specifying an IPAddress is
required under the following circumstances:

   
   - When the target is set to targetGrpcProxy andvalidateForProxyless is set to true, theIPAddress should be set to 0.0.0.0.
   - When the target is a Private Service Connect Google APIs
   bundle, you must specify an IPAddress.


Otherwise, you can optionally specify an IP address that references an
existing static (reserved) IP address resource. When omitted, Google Cloud
assigns an ephemeral IP address.

Use one of the following formats to specify an IP address while creating a
forwarding rule:

* IP address number, as in `100.1.2.3`
* IPv6 address range, as in `2600:1234::/96`
* Full resource URL, as inhttps://www.googleapis.com/compute/v1/projects/project_id/regions/region/addresses/address-name
* Partial URL or by name, as in:
   
   - projects/project_id/regions/region/addresses/address-name
   - regions/region/addresses/address-name
   - global/addresses/address-name
   - address-name



The forwarding rule's target or backendService,
and in most cases, also the loadBalancingScheme, determine the
type of IP address that you can use. For detailed information, see
[IP address
specifications](https://cloud.google.com/load-balancing/docs/forwarding-rule-concepts#ip_address_specifications).

When reading an IPAddress, the API always returns the IP
address number. |
| `all_ports` | bool |  | The ports, portRange, and allPorts
fields are mutually exclusive. Only packets addressed to ports in the
specified range will be forwarded to the backends configured with this
forwarding rule.

The allPorts field has the following limitations:
   
   - It requires that the forwarding rule IPProtocol be TCP,
   UDP, SCTP, or L3_DEFAULT.
   - It's applicable only to the following products: internal passthrough
   Network Load Balancers, backend service-based external passthrough Network
   Load Balancers, and internal and external protocol forwarding.
   - Set this field to true to allow packets addressed to any port or
   packets lacking destination port information (for example, UDP fragments
   after the first fragment) to be forwarded to the backends configured with
   this forwarding rule. The L3_DEFAULT protocol requiresallPorts be set to true. |
| `psc_connection_id` | String |  | [Output Only] The PSC connection id of the PSC forwarding rule. |
| `network` | String |  | This field is not used for global external load balancing.

For internal passthrough Network Load Balancers, this field identifies the
network that the load balanced IP should belong to for this forwarding
rule.
If the subnetwork is specified, the network of the subnetwork will be used.
If neither subnetwork nor this field is specified, the default network will
be used.

For Private Service Connect forwarding rules that forward traffic to Google
APIs, a network must be provided. |
| `allow_psc_global_access` | bool |  | This is used in PSC consumer ForwardingRule to control whether the PSC
endpoint can be accessed from another region. |
| `ip_protocol` | String |  | The IP protocol to which this rule applies.

For protocol forwarding, valid
options are TCP, UDP, ESP,AH, SCTP, ICMP andL3_DEFAULT.

The valid IP protocols are different for different load balancing products
as described in [Load balancing
features](https://cloud.google.com/load-balancing/docs/features#protocols_from_the_load_balancer_to_the_backends). |
| `network_tier` | String |  | This signifies the networking tier used for configuring
this load balancer and can only take the following values:PREMIUM, STANDARD.

For regional ForwardingRule, the valid values are PREMIUM andSTANDARD. For GlobalForwardingRule, the valid value isPREMIUM.

If this field is not specified, it is assumed to be PREMIUM.
If IPAddress is specified, this value must be equal to the
networkTier of the Address. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `name` | String |  | Name of the resource; provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash.

For Private Service Connect forwarding rules that forward traffic to Google
APIs, the forwarding rule name must be a 1-20 characters string with
lowercase letters and numbers and must start with a letter. |
| `fingerprint` | String |  | Fingerprint of this resource. A hash of the contents stored in this object.
This field is used in optimistic locking. This field will be ignored when
inserting a ForwardingRule. Include the fingerprint in patch request to
ensure that you do not overwrite changes that were applied from another
concurrent request.

To see the latest fingerprint, make a get() request to
retrieve a ForwardingRule. |
| `port_range` | String |  | The ports, portRange, and allPorts
fields are mutually exclusive. Only packets addressed to ports in the
specified range will be forwarded to the backends configured with this
forwarding rule.

The portRange field has the following limitations:
   
   - It requires that the forwarding rule IPProtocol be TCP,
   UDP, or SCTP, and
   - It's applicable only to the following products: external passthrough
   Network Load Balancers, internal and external proxy Network Load Balancers,
   internal and external Application Load Balancers, external protocol
   forwarding, and Classic VPN.
   - Some products have restrictions on what ports can be used. See 
   port specifications for details.



For external forwarding rules, two or more forwarding rules cannot use the
same [IPAddress, IPProtocol] pair, and cannot have overlappingportRanges.

For internal forwarding rules within the same VPC network, two or more
forwarding rules cannot use the same [IPAddress, IPProtocol]
pair, and cannot have overlapping portRanges.

@pattern: \\d+(?:-\\d+)? |
| `psc_connection_status` | String |  |  |
| `service_directory_registrations` | Vec<String> |  | Service Directory resources to register this forwarding rule with.
Currently, only supports a single Service Directory resource. |
| `target` | String |  | The URL of the target resource to receive the matched traffic.  For
regional forwarding rules, this target must be in the same region as the
forwarding rule. For global forwarding rules, this target must be a global
load balancing resource.

The forwarded traffic must be of a type appropriate to the target object.
   
   
     -  For load balancers, see the "Target" column in [Port specifications](https://cloud.google.com/load-balancing/docs/forwarding-rule-concepts#ip_address_specifications).
     -  For Private Service Connect forwarding rules that forward traffic to Google APIs, provide the name of a supported Google API bundle:
       
      
            -  vpc-sc -  APIs that support VPC Service Controls. 
            -  all-apis - All supported Google APIs. 
          
     
     -  For Private Service Connect forwarding rules that forward traffic to managed services, the target must be a service attachment. The target is not mutable once set as a service attachment. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `metadata_filters` | Vec<String> |  | Opaque filter criteria used by load balancer to restrict routing
configuration to a limited set of xDS
compliant clients. In their xDS requests to load balancer, xDS clients
present node
metadata. When there is a match, the relevant configuration
is made available to those proxies. Otherwise, all the resources (e.g.TargetHttpProxy, UrlMap)
referenced by the ForwardingRule are not visible to
those proxies.

For each metadataFilter in this list, if itsfilterMatchCriteria is set to MATCH_ANY, at least one of thefilterLabels must match the corresponding label provided in
the metadata. If its filterMatchCriteria is set to
MATCH_ALL, then all of its filterLabels must match with
corresponding labels provided in the metadata. If multiplemetadataFilters are specified, all of them need to be satisfied
in order to be considered a match.

metadataFilters specified here will be applifed before
those specified in the UrlMap that thisForwardingRule references.

metadataFilters only applies to Loadbalancers that have
their loadBalancingScheme set toINTERNAL_SELF_MANAGED. |
| `allow_psc_packet_injection` | bool |  | This is used in PSC consumer ForwardingRule to control whether the producer
is allowed to inject packets into the consumer's network. If set to true,
the target service attachment must have tunneling enabled and
TunnelingConfig.RoutingMode set to PACKET_INJECTION
Non-PSC forwarding rules should not use this field.

This field was never released to any customers and is deprecated and
will be removed in the future. |
| `label_fingerprint` | String |  | A fingerprint for the labels being applied to this resource, which is
essentially a hash of the labels set used for optimistic locking. The
fingerprint is initially generated by Compute Engine and changes after
every request to modify or update labels. You must always provide an
up-to-date fingerprint hash in order to update or change labels,
otherwise the request will fail with error412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve a ForwardingRule. |
| `project` | String | ✅ | Project ID for this request. |
| `region` | String | ✅ | Name of the region scoping this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `backend_service` | String | Identifies the backend service to which the forwarding rule sends traffic.
Required for internal and external passthrough Network Load Balancers;
must be omitted for all other load balancer types. |
| `allow_global_access` | bool | If set to true, clients can access the internal passthrough Network Load
Balancers, the regional internal Application Load Balancer, and the
regional internal proxy Network Load Balancer from all regions.
If false, only allows access from the local region the load balancer is
located at. Note that for INTERNAL_MANAGED forwarding rules, this field
cannot be changed after the forwarding rule is created. |
| `load_balancing_scheme` | String | Specifies the forwarding rule type.

For more information about forwarding rules, refer to
Forwarding rule concepts. |
| `ports` | Vec<String> | The ports, portRange, and allPorts
fields are mutually exclusive. Only packets addressed to ports in the
specified range will be forwarded to the backends configured with this
forwarding rule.

The ports field has the following limitations:
   
   - It requires that the forwarding rule IPProtocol be TCP,
   UDP, or SCTP, and
   - It's applicable only to the following products: internal passthrough
   Network Load Balancers, backend service-based external passthrough Network
   Load Balancers, and internal protocol forwarding.
   - You can specify a list of up to five ports by number, separated by
   commas. The ports can be contiguous or discontiguous.



For external forwarding rules, two or more forwarding rules cannot use the
same [IPAddress, IPProtocol] pair if they share at least one
port number.

For internal forwarding rules within the same VPC network, two or more
forwarding rules cannot use the same [IPAddress, IPProtocol]
pair if they share at least one port number.

@pattern: \\d+(?:-\\d+)? |
| `is_mirroring_collector` | bool | Indicates whether or not this load balancer can be used as a collector for
packet mirroring. To prevent mirroring loops, instances behind this
load balancer will not have their traffic mirrored even if aPacketMirroring rule applies to them.
This can only be set to true for load balancers that have theirloadBalancingScheme set to INTERNAL. |
| `self_link_with_id` | String | [Output Only] Server-defined URL for this resource with the resource id. |
| `base_forwarding_rule` | String | [Output Only] The URL for the corresponding base forwarding rule. By base
forwarding rule, we mean the forwarding rule that has the same IP address,
protocol, and port settings with the current forwarding rule, but without
sourceIPRanges specified.
Always empty if the current forwarding rule does not have sourceIPRanges
specified. |
| `labels` | HashMap<String, String> | Labels for this resource. These can only be added or modified by thesetLabels method. Each label key/value pair must comply withRFC1035.
Label values may be empty. |
| `service_name` | String | [Output Only]
The internal fully qualified service name for this forwarding rule.

This field is only used for internal load balancing. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `kind` | String | [Output Only] Type of the resource. Alwayscompute#forwardingRule for forwarding rule resources. |
| `region` | String | [Output Only] URL of the region where the regional forwarding rule resides.
This field is not applicable to global forwarding rules.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `service_label` | String | An optional prefix to the service name for this forwarding rule.
If specified, the prefix is the first label of the fully qualified service
name.

The label must be 1-63 characters long, and comply withRFC1035.
Specifically, the label must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash.

This field is only used for internal load balancing. |
| `subnetwork` | String | This field identifies the subnetwork that the load balanced IP should
belong to for this forwarding rule, used with internal load balancers and
external passthrough Network Load Balancers with IPv6.

If the network specified is in auto subnet mode, this field is optional.
However, a subnetwork must be specified if the network is in custom subnet
mode or when creating external forwarding rule with IPv6. |
| `ip_version` | String | The IP Version that will be used by this forwarding rule.  Valid options
are IPV4 or IPV6. |
| `ip_collection` | String | Resource reference of a PublicDelegatedPrefix. The PDP must
be a sub-PDP in EXTERNAL_IPV6_FORWARDING_RULE_CREATION mode.

Use one of the following formats to specify a sub-PDP when creating an IPv6
NetLB forwarding rule using BYOIP:
Full resource URL, as inhttps://www.googleapis.com/compute/v1/projects/project_id/regions/region/publicDelegatedPrefixes/sub-pdp-name
Partial URL, as in:
   
   - projects/project_id/regions/region/publicDelegatedPrefixes/sub-pdp-name
   - regions/region/publicDelegatedPrefixes/sub-pdp-name |
| `no_automate_dns_zone` | bool | This is used in PSC consumer ForwardingRule to control whether it should
try to auto-generate a DNS zone or not. Non-PSC forwarding rules do not use
this field. Once set, this field is not mutable. |
| `external_managed_backend_bucket_migration_state` | String | Specifies the canary migration state for the backend buckets attached to
this forwarding rule. Possible values are PREPARE, TEST_BY_PERCENTAGE, and
TEST_ALL_TRAFFIC.

To begin the migration from EXTERNAL to EXTERNAL_MANAGED, the state must be
changed to PREPARE. The state must be changed to TEST_ALL_TRAFFIC before
the loadBalancingScheme can be changed to EXTERNAL_MANAGED. Optionally, the
TEST_BY_PERCENTAGE state can be used to migrate traffic to backend buckets
attached to this forwarding rule by percentage using
externalManagedBackendBucketMigrationTestingPercentage.

Rolling back a migration requires the states to be set in reverse order. So
changing the scheme from EXTERNAL_MANAGED to EXTERNAL requires the state to
be set to TEST_ALL_TRAFFIC at the same time. Optionally, the
TEST_BY_PERCENTAGE state can be used to migrate some traffic back to
EXTERNAL or PREPARE can be used to migrate all traffic back to EXTERNAL. |
| `source_ip_ranges` | Vec<String> | If not empty, this forwarding rule will only forward the traffic when the
source IP address matches one of the IP addresses or CIDR ranges set here.
Note that a forwarding rule can only have up to 64 source IP ranges, and
this field can only be used with a regional forwarding rule whose scheme isEXTERNAL.
Each source_ip_range entry should be either an IP address (for
example, 1.2.3.4) or a CIDR range (for example, 1.2.3.0/24). |
| `external_managed_backend_bucket_migration_testing_percentage` | f64 | Determines the fraction of requests to backend buckets that should be
processed by the global external Application Load Balancer.

The value of this field must be in the range [0, 100].

This value can only be set if the loadBalancingScheme in the BackendService
is set to EXTERNAL (when using the classic Application Load Balancer) and
the migration state is TEST_BY_PERCENTAGE. |
| `ip_address` | String | IP address for which this forwarding rule accepts traffic. When a client
sends traffic to this IP address, the forwarding rule directs the traffic
to the referenced target or backendService.
While creating a forwarding rule, specifying an IPAddress is
required under the following circumstances:

   
   - When the target is set to targetGrpcProxy andvalidateForProxyless is set to true, theIPAddress should be set to 0.0.0.0.
   - When the target is a Private Service Connect Google APIs
   bundle, you must specify an IPAddress.


Otherwise, you can optionally specify an IP address that references an
existing static (reserved) IP address resource. When omitted, Google Cloud
assigns an ephemeral IP address.

Use one of the following formats to specify an IP address while creating a
forwarding rule:

* IP address number, as in `100.1.2.3`
* IPv6 address range, as in `2600:1234::/96`
* Full resource URL, as inhttps://www.googleapis.com/compute/v1/projects/project_id/regions/region/addresses/address-name
* Partial URL or by name, as in:
   
   - projects/project_id/regions/region/addresses/address-name
   - regions/region/addresses/address-name
   - global/addresses/address-name
   - address-name



The forwarding rule's target or backendService,
and in most cases, also the loadBalancingScheme, determine the
type of IP address that you can use. For detailed information, see
[IP address
specifications](https://cloud.google.com/load-balancing/docs/forwarding-rule-concepts#ip_address_specifications).

When reading an IPAddress, the API always returns the IP
address number. |
| `all_ports` | bool | The ports, portRange, and allPorts
fields are mutually exclusive. Only packets addressed to ports in the
specified range will be forwarded to the backends configured with this
forwarding rule.

The allPorts field has the following limitations:
   
   - It requires that the forwarding rule IPProtocol be TCP,
   UDP, SCTP, or L3_DEFAULT.
   - It's applicable only to the following products: internal passthrough
   Network Load Balancers, backend service-based external passthrough Network
   Load Balancers, and internal and external protocol forwarding.
   - Set this field to true to allow packets addressed to any port or
   packets lacking destination port information (for example, UDP fragments
   after the first fragment) to be forwarded to the backends configured with
   this forwarding rule. The L3_DEFAULT protocol requiresallPorts be set to true. |
| `psc_connection_id` | String | [Output Only] The PSC connection id of the PSC forwarding rule. |
| `network` | String | This field is not used for global external load balancing.

For internal passthrough Network Load Balancers, this field identifies the
network that the load balanced IP should belong to for this forwarding
rule.
If the subnetwork is specified, the network of the subnetwork will be used.
If neither subnetwork nor this field is specified, the default network will
be used.

For Private Service Connect forwarding rules that forward traffic to Google
APIs, a network must be provided. |
| `allow_psc_global_access` | bool | This is used in PSC consumer ForwardingRule to control whether the PSC
endpoint can be accessed from another region. |
| `ip_protocol` | String | The IP protocol to which this rule applies.

For protocol forwarding, valid
options are TCP, UDP, ESP,AH, SCTP, ICMP andL3_DEFAULT.

The valid IP protocols are different for different load balancing products
as described in [Load balancing
features](https://cloud.google.com/load-balancing/docs/features#protocols_from_the_load_balancer_to_the_backends). |
| `network_tier` | String | This signifies the networking tier used for configuring
this load balancer and can only take the following values:PREMIUM, STANDARD.

For regional ForwardingRule, the valid values are PREMIUM andSTANDARD. For GlobalForwardingRule, the valid value isPREMIUM.

If this field is not specified, it is assumed to be PREMIUM.
If IPAddress is specified, this value must be equal to the
networkTier of the Address. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `name` | String | Name of the resource; provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash.

For Private Service Connect forwarding rules that forward traffic to Google
APIs, the forwarding rule name must be a 1-20 characters string with
lowercase letters and numbers and must start with a letter. |
| `fingerprint` | String | Fingerprint of this resource. A hash of the contents stored in this object.
This field is used in optimistic locking. This field will be ignored when
inserting a ForwardingRule. Include the fingerprint in patch request to
ensure that you do not overwrite changes that were applied from another
concurrent request.

To see the latest fingerprint, make a get() request to
retrieve a ForwardingRule. |
| `port_range` | String | The ports, portRange, and allPorts
fields are mutually exclusive. Only packets addressed to ports in the
specified range will be forwarded to the backends configured with this
forwarding rule.

The portRange field has the following limitations:
   
   - It requires that the forwarding rule IPProtocol be TCP,
   UDP, or SCTP, and
   - It's applicable only to the following products: external passthrough
   Network Load Balancers, internal and external proxy Network Load Balancers,
   internal and external Application Load Balancers, external protocol
   forwarding, and Classic VPN.
   - Some products have restrictions on what ports can be used. See 
   port specifications for details.



For external forwarding rules, two or more forwarding rules cannot use the
same [IPAddress, IPProtocol] pair, and cannot have overlappingportRanges.

For internal forwarding rules within the same VPC network, two or more
forwarding rules cannot use the same [IPAddress, IPProtocol]
pair, and cannot have overlapping portRanges.

@pattern: \\d+(?:-\\d+)? |
| `psc_connection_status` | String |  |
| `service_directory_registrations` | Vec<String> | Service Directory resources to register this forwarding rule with.
Currently, only supports a single Service Directory resource. |
| `target` | String | The URL of the target resource to receive the matched traffic.  For
regional forwarding rules, this target must be in the same region as the
forwarding rule. For global forwarding rules, this target must be a global
load balancing resource.

The forwarded traffic must be of a type appropriate to the target object.
   
   
     -  For load balancers, see the "Target" column in [Port specifications](https://cloud.google.com/load-balancing/docs/forwarding-rule-concepts#ip_address_specifications).
     -  For Private Service Connect forwarding rules that forward traffic to Google APIs, provide the name of a supported Google API bundle:
       
      
            -  vpc-sc -  APIs that support VPC Service Controls. 
            -  all-apis - All supported Google APIs. 
          
     
     -  For Private Service Connect forwarding rules that forward traffic to managed services, the target must be a service attachment. The target is not mutable once set as a service attachment. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `metadata_filters` | Vec<String> | Opaque filter criteria used by load balancer to restrict routing
configuration to a limited set of xDS
compliant clients. In their xDS requests to load balancer, xDS clients
present node
metadata. When there is a match, the relevant configuration
is made available to those proxies. Otherwise, all the resources (e.g.TargetHttpProxy, UrlMap)
referenced by the ForwardingRule are not visible to
those proxies.

For each metadataFilter in this list, if itsfilterMatchCriteria is set to MATCH_ANY, at least one of thefilterLabels must match the corresponding label provided in
the metadata. If its filterMatchCriteria is set to
MATCH_ALL, then all of its filterLabels must match with
corresponding labels provided in the metadata. If multiplemetadataFilters are specified, all of them need to be satisfied
in order to be considered a match.

metadataFilters specified here will be applifed before
those specified in the UrlMap that thisForwardingRule references.

metadataFilters only applies to Loadbalancers that have
their loadBalancingScheme set toINTERNAL_SELF_MANAGED. |
| `allow_psc_packet_injection` | bool | This is used in PSC consumer ForwardingRule to control whether the producer
is allowed to inject packets into the consumer's network. If set to true,
the target service attachment must have tunneling enabled and
TunnelingConfig.RoutingMode set to PACKET_INJECTION
Non-PSC forwarding rules should not use this field.

This field was never released to any customers and is deprecated and
will be removed in the future. |
| `label_fingerprint` | String | A fingerprint for the labels being applied to this resource, which is
essentially a hash of the labels set used for optimistic locking. The
fingerprint is initially generated by Compute Engine and changes after
every request to modify or update labels. You must always provide an
up-to-date fingerprint hash in order to update or change labels,
otherwise the request will fail with error412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve a ForwardingRule. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create forwarding_rule
forwarding_rule = provider.compute_api.Forwarding_rule {
    project = "value"  # Project ID for this request.
    region = "value"  # Name of the region scoping this request.
}

# Access forwarding_rule outputs
forwarding_rule_id = forwarding_rule.id
forwarding_rule_backend_service = forwarding_rule.backend_service
forwarding_rule_allow_global_access = forwarding_rule.allow_global_access
forwarding_rule_load_balancing_scheme = forwarding_rule.load_balancing_scheme
forwarding_rule_ports = forwarding_rule.ports
forwarding_rule_is_mirroring_collector = forwarding_rule.is_mirroring_collector
forwarding_rule_self_link_with_id = forwarding_rule.self_link_with_id
forwarding_rule_base_forwarding_rule = forwarding_rule.base_forwarding_rule
forwarding_rule_labels = forwarding_rule.labels
forwarding_rule_service_name = forwarding_rule.service_name
forwarding_rule_id = forwarding_rule.id
forwarding_rule_kind = forwarding_rule.kind
forwarding_rule_region = forwarding_rule.region
forwarding_rule_service_label = forwarding_rule.service_label
forwarding_rule_subnetwork = forwarding_rule.subnetwork
forwarding_rule_ip_version = forwarding_rule.ip_version
forwarding_rule_ip_collection = forwarding_rule.ip_collection
forwarding_rule_no_automate_dns_zone = forwarding_rule.no_automate_dns_zone
forwarding_rule_external_managed_backend_bucket_migration_state = forwarding_rule.external_managed_backend_bucket_migration_state
forwarding_rule_source_ip_ranges = forwarding_rule.source_ip_ranges
forwarding_rule_external_managed_backend_bucket_migration_testing_percentage = forwarding_rule.external_managed_backend_bucket_migration_testing_percentage
forwarding_rule_ip_address = forwarding_rule.ip_address
forwarding_rule_all_ports = forwarding_rule.all_ports
forwarding_rule_psc_connection_id = forwarding_rule.psc_connection_id
forwarding_rule_network = forwarding_rule.network
forwarding_rule_allow_psc_global_access = forwarding_rule.allow_psc_global_access
forwarding_rule_ip_protocol = forwarding_rule.ip_protocol
forwarding_rule_network_tier = forwarding_rule.network_tier
forwarding_rule_self_link = forwarding_rule.self_link
forwarding_rule_name = forwarding_rule.name
forwarding_rule_fingerprint = forwarding_rule.fingerprint
forwarding_rule_port_range = forwarding_rule.port_range
forwarding_rule_psc_connection_status = forwarding_rule.psc_connection_status
forwarding_rule_service_directory_registrations = forwarding_rule.service_directory_registrations
forwarding_rule_target = forwarding_rule.target
forwarding_rule_description = forwarding_rule.description
forwarding_rule_creation_timestamp = forwarding_rule.creation_timestamp
forwarding_rule_metadata_filters = forwarding_rule.metadata_filters
forwarding_rule_allow_psc_packet_injection = forwarding_rule.allow_psc_packet_injection
forwarding_rule_label_fingerprint = forwarding_rule.label_fingerprint
```

---


### Node_type

Returns the specified node type.

**Operations**: ✅ Read

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `max_vms` | i64 | [Output Only] Maximum number of VMs that can be created for this node type. |
| `description` | String | [Output Only] An optional textual description of the resource. |
| `memory_mb` | i64 | [Output Only] The amount of physical memory available to the node type,
defined in MB. |
| `name` | String | [Output Only] Name of the resource. |
| `guest_cpus` | i64 | [Output Only] The number of virtual CPUs that are available to the node
type. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `cpu_platform` | String | [Output Only] The CPU platform used by this node type. |
| `zone` | String | [Output Only] The name of the zone where the node type resides,
such as us-central1-a. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `deprecated` | String | [Output Only] The deprecation status associated with this node type. |
| `local_ssd_gb` | i64 | [Output Only] Local SSD available to the node type, defined in GB. |
| `kind` | String | [Output Only] The type of the resource. Alwayscompute#nodeType for node types. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Access node_type outputs
node_type_id = node_type.id
node_type_max_vms = node_type.max_vms
node_type_description = node_type.description
node_type_memory_mb = node_type.memory_mb
node_type_name = node_type.name
node_type_guest_cpus = node_type.guest_cpus
node_type_self_link = node_type.self_link
node_type_cpu_platform = node_type.cpu_platform
node_type_zone = node_type.zone
node_type_id = node_type.id
node_type_creation_timestamp = node_type.creation_timestamp
node_type_deprecated = node_type.deprecated
node_type_local_ssd_gb = node_type.local_ssd_gb
node_type_kind = node_type.kind
```

---


### Packet_mirroring

Creates a PacketMirroring resource in the specified project and region
using the data included in the request.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `collector_ilb` | String |  | The Forwarding Rule resource of typeloadBalancingScheme=INTERNAL that will be used as collector
for mirrored traffic.
The specified forwarding rule must have isMirroringCollector
set to true. |
| `filter` | String |  | Filter for mirrored traffic. If unspecified, all IPv4 traffic is mirrored. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `mirrored_resources` | String |  | PacketMirroring mirroredResourceInfos.
MirroredResourceInfo specifies a set of mirrored VM instances, subnetworks
and/or tags for which traffic from/to all VM instances will be mirrored. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `enable` | String |  | Indicates whether or not this packet mirroring takes effect.
If set to FALSE, this packet mirroring policy will not be enforced on the
network.

The default is TRUE. |
| `network` | String |  | Specifies the mirrored VPC network. Only packets in this network will be
mirrored. All mirrored VMs should have a NIC in the given network.
All mirrored subnetworks should belong to the given network. |
| `name` | String |  | Name of the resource; provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must be
a dash, lowercase letter, or digit, except the last character, which cannot
be a dash. |
| `priority` | i64 |  | The priority of applying this configuration. Priority is used to break ties
in cases where there is more than one matching rule. In the case of two
rules that apply for a given Instance, the one with the lowest-numbered
priority value wins.

Default value is 1000. Valid range is 0 through 65535. |
| `region` | String |  | [Output Only] URI of the region where the packetMirroring resides. |
| `kind` | String |  | [Output Only] Type of the resource. Alwayscompute#packetMirroring for packet mirrorings. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `region` | String | ✅ | Name of the region for this request. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `collector_ilb` | String | The Forwarding Rule resource of typeloadBalancingScheme=INTERNAL that will be used as collector
for mirrored traffic.
The specified forwarding rule must have isMirroringCollector
set to true. |
| `filter` | String | Filter for mirrored traffic. If unspecified, all IPv4 traffic is mirrored. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `mirrored_resources` | String | PacketMirroring mirroredResourceInfos.
MirroredResourceInfo specifies a set of mirrored VM instances, subnetworks
and/or tags for which traffic from/to all VM instances will be mirrored. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `enable` | String | Indicates whether or not this packet mirroring takes effect.
If set to FALSE, this packet mirroring policy will not be enforced on the
network.

The default is TRUE. |
| `network` | String | Specifies the mirrored VPC network. Only packets in this network will be
mirrored. All mirrored VMs should have a NIC in the given network.
All mirrored subnetworks should belong to the given network. |
| `name` | String | Name of the resource; provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must be
a dash, lowercase letter, or digit, except the last character, which cannot
be a dash. |
| `priority` | i64 | The priority of applying this configuration. Priority is used to break ties
in cases where there is more than one matching rule. In the case of two
rules that apply for a given Instance, the one with the lowest-numbered
priority value wins.

Default value is 1000. Valid range is 0 through 65535. |
| `region` | String | [Output Only] URI of the region where the packetMirroring resides. |
| `kind` | String | [Output Only] Type of the resource. Alwayscompute#packetMirroring for packet mirrorings. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create packet_mirroring
packet_mirroring = provider.compute_api.Packet_mirroring {
    region = "value"  # Name of the region for this request.
    project = "value"  # Project ID for this request.
}

# Access packet_mirroring outputs
packet_mirroring_id = packet_mirroring.id
packet_mirroring_collector_ilb = packet_mirroring.collector_ilb
packet_mirroring_filter = packet_mirroring.filter
packet_mirroring_description = packet_mirroring.description
packet_mirroring_id = packet_mirroring.id
packet_mirroring_mirrored_resources = packet_mirroring.mirrored_resources
packet_mirroring_creation_timestamp = packet_mirroring.creation_timestamp
packet_mirroring_enable = packet_mirroring.enable
packet_mirroring_network = packet_mirroring.network
packet_mirroring_name = packet_mirroring.name
packet_mirroring_priority = packet_mirroring.priority
packet_mirroring_region = packet_mirroring.region
packet_mirroring_kind = packet_mirroring.kind
packet_mirroring_self_link = packet_mirroring.self_link
```

---


### Region_health_check

Creates a HealthCheck resource in the specified project using the data
included in the request.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `http_health_check` | String |  |  |
| `healthy_threshold` | i64 |  | A so-far unhealthy instance will be marked healthy after this
many consecutive successes. The default value is 2. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp in3339
text format. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `name` | String |  | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
For example, a name that is 1-63 characters long, matches the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?`, and otherwise complies with
RFC1035. This regular expression describes a name where the first
character is a lowercase letter, and all following characters are a dash,
lowercase letter, or digit, except the last character, which isn't a dash. |
| `source_regions` | Vec<String> |  | The list of cloud regions from which health checks are performed. If any
regions are specified, then exactly 3 regions should be specified. The
region names must be valid names of Google Cloud regions.
This can only be set for global health check.
If this list is non-empty, then there are restrictions
on what other health check fields are supported and what other resources
can use this health check:
   
   - SSL, HTTP2, and GRPC protocols are not supported.
   - The TCP request field is not supported.
   - The proxyHeader field for HTTP, HTTPS, and TCP is not
   supported.
   - The checkIntervalSec field must be at least 30.
   - The health check cannot be used with BackendService nor with managed
   instance group auto-healing. |
| `http2_health_check` | String |  |  |
| `kind` | String |  | Type of the resource. |
| `grpc_tls_health_check` | String |  |  |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `check_interval_sec` | i64 |  | How often (in seconds) to send a health check. The default value is 5
seconds. |
| `grpc_health_check` | String |  |  |
| `tcp_health_check` | String |  |  |
| `timeout_sec` | i64 |  | How long (in seconds) to wait before claiming failure. The default value is
5 seconds. It is invalid for timeoutSec to have greater
value than checkIntervalSec. |
| `region` | String |  | [Output Only] Region where the health check resides.  Not applicable to
global health checks. |
| `https_health_check` | String |  |  |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `type` | String |  | Specifies the type of the healthCheck, either TCP,SSL, HTTP, HTTPS,HTTP2 or GRPC. Exactly one of the
protocol-specific health check fields must be specified, which must matchtype field. |
| `ssl_health_check` | String |  |  |
| `unhealthy_threshold` | i64 |  | A so-far healthy instance will be marked unhealthy after this many
consecutive failures. The default value is 2. |
| `log_config` | String |  | Configure logging on this health check. |
| `project` | String | ✅ | Project ID for this request. |
| `region` | String | ✅ | Name of the region scoping this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `http_health_check` | String |  |
| `healthy_threshold` | i64 | A so-far unhealthy instance will be marked healthy after this
many consecutive successes. The default value is 2. |
| `creation_timestamp` | String | [Output Only] Creation timestamp in3339
text format. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `name` | String | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
For example, a name that is 1-63 characters long, matches the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?`, and otherwise complies with
RFC1035. This regular expression describes a name where the first
character is a lowercase letter, and all following characters are a dash,
lowercase letter, or digit, except the last character, which isn't a dash. |
| `source_regions` | Vec<String> | The list of cloud regions from which health checks are performed. If any
regions are specified, then exactly 3 regions should be specified. The
region names must be valid names of Google Cloud regions.
This can only be set for global health check.
If this list is non-empty, then there are restrictions
on what other health check fields are supported and what other resources
can use this health check:
   
   - SSL, HTTP2, and GRPC protocols are not supported.
   - The TCP request field is not supported.
   - The proxyHeader field for HTTP, HTTPS, and TCP is not
   supported.
   - The checkIntervalSec field must be at least 30.
   - The health check cannot be used with BackendService nor with managed
   instance group auto-healing. |
| `http2_health_check` | String |  |
| `kind` | String | Type of the resource. |
| `grpc_tls_health_check` | String |  |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `check_interval_sec` | i64 | How often (in seconds) to send a health check. The default value is 5
seconds. |
| `grpc_health_check` | String |  |
| `tcp_health_check` | String |  |
| `timeout_sec` | i64 | How long (in seconds) to wait before claiming failure. The default value is
5 seconds. It is invalid for timeoutSec to have greater
value than checkIntervalSec. |
| `region` | String | [Output Only] Region where the health check resides.  Not applicable to
global health checks. |
| `https_health_check` | String |  |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `type` | String | Specifies the type of the healthCheck, either TCP,SSL, HTTP, HTTPS,HTTP2 or GRPC. Exactly one of the
protocol-specific health check fields must be specified, which must matchtype field. |
| `ssl_health_check` | String |  |
| `unhealthy_threshold` | i64 | A so-far healthy instance will be marked unhealthy after this many
consecutive failures. The default value is 2. |
| `log_config` | String | Configure logging on this health check. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create region_health_check
region_health_check = provider.compute_api.Region_health_check {
    project = "value"  # Project ID for this request.
    region = "value"  # Name of the region scoping this request.
}

# Access region_health_check outputs
region_health_check_id = region_health_check.id
region_health_check_http_health_check = region_health_check.http_health_check
region_health_check_healthy_threshold = region_health_check.healthy_threshold
region_health_check_creation_timestamp = region_health_check.creation_timestamp
region_health_check_id = region_health_check.id
region_health_check_name = region_health_check.name
region_health_check_source_regions = region_health_check.source_regions
region_health_check_http2_health_check = region_health_check.http2_health_check
region_health_check_kind = region_health_check.kind
region_health_check_grpc_tls_health_check = region_health_check.grpc_tls_health_check
region_health_check_description = region_health_check.description
region_health_check_check_interval_sec = region_health_check.check_interval_sec
region_health_check_grpc_health_check = region_health_check.grpc_health_check
region_health_check_tcp_health_check = region_health_check.tcp_health_check
region_health_check_timeout_sec = region_health_check.timeout_sec
region_health_check_region = region_health_check.region
region_health_check_https_health_check = region_health_check.https_health_check
region_health_check_self_link = region_health_check.self_link
region_health_check_type = region_health_check.type
region_health_check_ssl_health_check = region_health_check.ssl_health_check
region_health_check_unhealthy_threshold = region_health_check.unhealthy_threshold
region_health_check_log_config = region_health_check.log_config
```

---


### Firewall_policie

Creates a new policy in the specified project using the data included in
the request.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `kind` | String |  | [Output only] Type of the resource. Alwayscompute#firewallPolicyfor firewall policies |
| `policy_source` | String |  | [Output Only] Source of this Firewall Policy. USER_DEFINED if
created by a Cloud user, or SYSTEM if created by managed
services like GKE. |
| `region` | String |  | [Output Only] URL of the region where the regional firewall policy resides.
This field is not applicable to global firewall policies.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `rules` | Vec<String> |  | A list of rules that belong to this policy.
There must always be a default rule (rule with priority 2147483647 and
match "*"). If no rules are provided when creating a firewall policy, a
default rule with action "allow" will be added. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `parent` | String |  | [Output Only] The parent of the firewall policy.
This field is not applicable to network firewall policies. |
| `rule_tuple_count` | i64 |  | [Output Only] Total count of all firewall policy rule tuples. A firewall
policy can not exceed a set number of tuples. |
| `display_name` | String |  | Deprecated, please use short name instead. User-provided name of the
Organization firewall policy. The name should be unique in the organization
in which the firewall policy is created.
This field is not applicable to network firewall policies.
This name must be set on creation and cannot be changed.
The name must be 1-63 characters long, and comply
with RFC1035. Specifically, the name must be 1-63 characters
long and match the regular expression `[a-z]([-a-z0-9]*[a-z0-9])?` which
means the first character must be a lowercase letter, and all following
characters must be a dash, lowercase letter, or digit, except the last
character, which cannot be a dash. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `associations` | Vec<String> |  | A list of associations that belong to this firewall policy. |
| `fingerprint` | String |  | Specifies a fingerprint for this resource, which is essentially a hash of
the metadata's contents and used for optimistic locking. The
fingerprint is initially generated by Compute Engine and changes after
every request to modify or update metadata. You must always provide an
up-to-date fingerprint hash in order to update or change metadata,
otherwise the request will fail with error412 conditionNotMet.

To see the latest fingerprint, make get() request to the
firewall policy. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `name` | String |  | Name of the resource. For Organization Firewall Policies it's a
[Output Only] numeric ID allocated by Google Cloud which uniquely
identifies the Organization Firewall Policy. |
| `packet_mirroring_rules` | Vec<String> |  | A list of packet mirroring rules that belong to this policy. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `self_link_with_id` | String |  | [Output Only] Server-defined URL for this resource with the resource id. |
| `short_name` | String |  | User-provided name of the Organization firewall policy. The name should be
unique in the organization in which the firewall policy is created.
This field is not applicable to network firewall policies.
This name must be set on creation and cannot be changed. The name must be
1-63 characters long, and comply with RFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `policy_type` | String |  | The type of the firewall policy. This field can be eitherVPC_POLICY or RDMA_ROCE_POLICY.

Note: if not specified then VPC_POLICY will be used. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `kind` | String | [Output only] Type of the resource. Alwayscompute#firewallPolicyfor firewall policies |
| `policy_source` | String | [Output Only] Source of this Firewall Policy. USER_DEFINED if
created by a Cloud user, or SYSTEM if created by managed
services like GKE. |
| `region` | String | [Output Only] URL of the region where the regional firewall policy resides.
This field is not applicable to global firewall policies.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `rules` | Vec<String> | A list of rules that belong to this policy.
There must always be a default rule (rule with priority 2147483647 and
match "*"). If no rules are provided when creating a firewall policy, a
default rule with action "allow" will be added. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `parent` | String | [Output Only] The parent of the firewall policy.
This field is not applicable to network firewall policies. |
| `rule_tuple_count` | i64 | [Output Only] Total count of all firewall policy rule tuples. A firewall
policy can not exceed a set number of tuples. |
| `display_name` | String | Deprecated, please use short name instead. User-provided name of the
Organization firewall policy. The name should be unique in the organization
in which the firewall policy is created.
This field is not applicable to network firewall policies.
This name must be set on creation and cannot be changed.
The name must be 1-63 characters long, and comply
with RFC1035. Specifically, the name must be 1-63 characters
long and match the regular expression `[a-z]([-a-z0-9]*[a-z0-9])?` which
means the first character must be a lowercase letter, and all following
characters must be a dash, lowercase letter, or digit, except the last
character, which cannot be a dash. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `associations` | Vec<String> | A list of associations that belong to this firewall policy. |
| `fingerprint` | String | Specifies a fingerprint for this resource, which is essentially a hash of
the metadata's contents and used for optimistic locking. The
fingerprint is initially generated by Compute Engine and changes after
every request to modify or update metadata. You must always provide an
up-to-date fingerprint hash in order to update or change metadata,
otherwise the request will fail with error412 conditionNotMet.

To see the latest fingerprint, make get() request to the
firewall policy. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `name` | String | Name of the resource. For Organization Firewall Policies it's a
[Output Only] numeric ID allocated by Google Cloud which uniquely
identifies the Organization Firewall Policy. |
| `packet_mirroring_rules` | Vec<String> | A list of packet mirroring rules that belong to this policy. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `self_link_with_id` | String | [Output Only] Server-defined URL for this resource with the resource id. |
| `short_name` | String | User-provided name of the Organization firewall policy. The name should be
unique in the organization in which the firewall policy is created.
This field is not applicable to network firewall policies.
This name must be set on creation and cannot be changed. The name must be
1-63 characters long, and comply with RFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `policy_type` | String | The type of the firewall policy. This field can be eitherVPC_POLICY or RDMA_ROCE_POLICY.

Note: if not specified then VPC_POLICY will be used. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create firewall_policie
firewall_policie = provider.compute_api.Firewall_policie {
}

# Access firewall_policie outputs
firewall_policie_id = firewall_policie.id
firewall_policie_kind = firewall_policie.kind
firewall_policie_policy_source = firewall_policie.policy_source
firewall_policie_region = firewall_policie.region
firewall_policie_rules = firewall_policie.rules
firewall_policie_description = firewall_policie.description
firewall_policie_parent = firewall_policie.parent
firewall_policie_rule_tuple_count = firewall_policie.rule_tuple_count
firewall_policie_display_name = firewall_policie.display_name
firewall_policie_id = firewall_policie.id
firewall_policie_associations = firewall_policie.associations
firewall_policie_fingerprint = firewall_policie.fingerprint
firewall_policie_creation_timestamp = firewall_policie.creation_timestamp
firewall_policie_name = firewall_policie.name
firewall_policie_packet_mirroring_rules = firewall_policie.packet_mirroring_rules
firewall_policie_self_link = firewall_policie.self_link
firewall_policie_self_link_with_id = firewall_policie.self_link_with_id
firewall_policie_short_name = firewall_policie.short_name
firewall_policie_policy_type = firewall_policie.policy_type
```

---


### Region_instance_template

Creates an instance template in the specified project and region using the
global instance template whose URL is included in the request.

**Operations**: ✅ Create ✅ Read ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `self_link` | String |  | [Output Only] The URL for this instance template. The server defines this
URL. |
| `source_instance` | String |  | The source instance used to create the template. You can provide this as a
partial or full URL to the resource. For example, the following are valid
values:
   
   
     - https://www.googleapis.com/compute/v1/projects/project/zones/zone/instances/instance 
   - projects/project/zones/zone/instances/instance |
| `name` | String |  | Name of the resource; provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must be
a dash, lowercase letter, or digit, except the last character, which cannot
be a dash. |
| `source_instance_params` | String |  | The source instance params to use to create this instance template. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `id` | String |  | [Output Only] A unique identifier for this instance template. The server
defines this identifier. |
| `kind` | String |  | [Output Only] The resource type, which is alwayscompute#instanceTemplate for instance templates. |
| `creation_timestamp` | String |  | [Output Only] The creation timestamp for this instance template inRFC3339
text format. |
| `properties` | String |  | The instance properties for this instance template. |
| `region` | String |  | [Output Only] URL of the region where the instance template resides. Only
applicable for regional resources. |
| `region` | String | ✅ | The name of the region for this request. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `self_link` | String | [Output Only] The URL for this instance template. The server defines this
URL. |
| `source_instance` | String | The source instance used to create the template. You can provide this as a
partial or full URL to the resource. For example, the following are valid
values:
   
   
     - https://www.googleapis.com/compute/v1/projects/project/zones/zone/instances/instance 
   - projects/project/zones/zone/instances/instance |
| `name` | String | Name of the resource; provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must be
a dash, lowercase letter, or digit, except the last character, which cannot
be a dash. |
| `source_instance_params` | String | The source instance params to use to create this instance template. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `id` | String | [Output Only] A unique identifier for this instance template. The server
defines this identifier. |
| `kind` | String | [Output Only] The resource type, which is alwayscompute#instanceTemplate for instance templates. |
| `creation_timestamp` | String | [Output Only] The creation timestamp for this instance template inRFC3339
text format. |
| `properties` | String | The instance properties for this instance template. |
| `region` | String | [Output Only] URL of the region where the instance template resides. Only
applicable for regional resources. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create region_instance_template
region_instance_template = provider.compute_api.Region_instance_template {
    region = "value"  # The name of the region for this request.
    project = "value"  # Project ID for this request.
}

# Access region_instance_template outputs
region_instance_template_id = region_instance_template.id
region_instance_template_self_link = region_instance_template.self_link
region_instance_template_source_instance = region_instance_template.source_instance
region_instance_template_name = region_instance_template.name
region_instance_template_source_instance_params = region_instance_template.source_instance_params
region_instance_template_description = region_instance_template.description
region_instance_template_id = region_instance_template.id
region_instance_template_kind = region_instance_template.kind
region_instance_template_creation_timestamp = region_instance_template.creation_timestamp
region_instance_template_properties = region_instance_template.properties
region_instance_template_region = region_instance_template.region
```

---


### Backend_service

Creates a BackendService resource in the specified project using
the data included in the request. For more information, see
Backend services overview.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `compression_mode` | String |  | Compress text responses using Brotli or gzip compression, based on
the client's Accept-Encoding header. |
| `dynamic_forwarding` | String |  | Dynamic forwarding configuration. This field is used to configure the
backend service with dynamic forwarding feature which together with Service
Extension allows customized and complex routing logic. |
| `connection_tracking_policy` | String |  | Connection Tracking configuration for this BackendService. Connection
tracking policy settings are only available for external passthrough
Network Load Balancers and internal passthrough Network Load Balancers.

connectionTrackingPolicy cannot be specified with haPolicy. |
| `custom_response_headers` | Vec<String> |  | Headers that the load balancer adds to proxied responses. See [Creating
custom
headers](https://cloud.google.com/load-balancing/docs/custom-headers). |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `iap` | String |  | The configurations for Identity-Aware Proxy on this resource.
Not available for internal passthrough Network Load Balancers and external
passthrough Network Load Balancers. |
| `network_pass_through_lb_traffic_policy` | String |  | Configures traffic steering properties of internal passthrough Network
Load Balancers.

networkPassThroughLbTrafficPolicy cannot be specified with haPolicy. |
| `custom_metrics` | Vec<String> |  | List of custom metrics that are used for theWEIGHTED_ROUND_ROBIN locality_lb_policy. |
| `params` | String |  | Input only. [Input Only] Additional params passed with the request, but not persisted
as part of resource payload. |
| `consistent_hash` | String |  | Consistent Hash-based load balancing can be used to provide soft session
affinity based on HTTP headers, cookies or other properties. This load
balancing policy is applicable only for HTTP connections. The affinity to a
particular destination host will be lost when one or more hosts are
added/removed from the destination service. This field specifies parameters
that control consistent hashing. This field is only applicable whenlocalityLbPolicy is set to MAGLEV orRING_HASH.

This field is applicable to either:
   
   - A regional backend service with the service_protocol set to HTTP,
   HTTPS, HTTP2 or H2C, and load_balancing_scheme set to
   INTERNAL_MANAGED. 
   - A global backend service with the
   load_balancing_scheme set to INTERNAL_SELF_MANAGED. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `enable_cdn` | bool |  | If true, enables Cloud CDN for the backend service of a
global external Application Load Balancer. |
| `connection_draining` | String |  | connectionDraining cannot be specified with haPolicy. |
| `affinity_cookie_ttl_sec` | i64 |  | Lifetime of cookies in seconds. This setting is applicable to Application
Load Balancers and Traffic Director and requires
GENERATED_COOKIE or HTTP_COOKIE session affinity.

If set to 0, the cookie is non-persistent and lasts only until
the end of the browser session (or equivalent). The maximum allowed value
is two weeks (1,209,600).

Not supported when the backend service is referenced by a URL map that is
bound to target gRPC proxy that has validateForProxyless field set to true. |
| `log_config` | String |  | This field denotes the logging options for the load balancer traffic served
by this backend service. If logging is enabled, logs will be exported to
Stackdriver. |
| `protocol` | String |  | The protocol this BackendService uses to communicate
with backends.

Possible values are HTTP, HTTPS, HTTP2, H2C, TCP, SSL, UDP or GRPC.
depending on the chosen load balancer or Traffic Director configuration.
Refer to the documentation for the load balancers or for Traffic Director
for more information.

Must be set to GRPC when the backend service is referenced by a URL map
that is bound to target gRPC proxy. |
| `session_affinity` | String |  | Type of session affinity to use. The default is NONE.

Only NONE and HEADER_FIELD are supported
when the backend service is referenced by a URL map that is bound to
target gRPC proxy that has validateForProxyless field set to true.

For more details, see:
[Session
Affinity](https://cloud.google.com/load-balancing/docs/backend-service#session_affinity).

sessionAffinity cannot be specified with haPolicy. |
| `security_policy` | String |  | [Output Only] The resource URL for the security policy associated with this
backend service. |
| `tls_settings` | String |  | Configuration for Backend Authenticated TLS and mTLS. May only be specified
when the backend protocol is SSL, HTTPS or HTTP2. |
| `ha_policy` | String |  | Configures self-managed High Availability (HA) for External and Internal
Protocol Forwarding.

The backends of this regional backend service must only specify zonal
network endpoint groups (NEGs) of type GCE_VM_IP.

When haPolicy is set for an Internal Passthrough Network Load Balancer, the
regional backend service must set the network field. All zonal NEGs must
belong to the same network. However, individual NEGs can
belong to different subnetworks of that network.

When haPolicy is specified, the set of attached network endpoints across
all backends comprise an High Availability domain from which one endpoint
is selected as the active endpoint (the leader) that receives all
traffic.

haPolicy can be added only at backend service creation time. Once set up,
it cannot be deleted.

Note that haPolicy is not for load balancing, and therefore cannot be
specified with sessionAffinity, connectionTrackingPolicy, and
failoverPolicy.

haPolicy requires customers to be responsible for tracking backend
endpoint health and electing a leader among the healthy endpoints.
Therefore, haPolicy cannot be specified with healthChecks.

haPolicy can only be specified for External Passthrough Network Load
Balancers and Internal Passthrough Network Load Balancers. |
| `strong_session_affinity_cookie` | String |  | Describes the HTTP cookie used for stateful session affinity. This field is
applicable and required if the sessionAffinity is set toSTRONG_COOKIE_AFFINITY. |
| `kind` | String |  | [Output Only] Type of resource. Always compute#backendService
for backend services. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `service_bindings` | Vec<String> |  | URLs of networkservices.ServiceBinding resources.

Can only be set if load balancing scheme is INTERNAL_SELF_MANAGED.
If set, lists of backends and health checks must be both empty. |
| `locality_lb_policies` | Vec<String> |  | A list of locality load-balancing policies to be used in order of
preference. When you use localityLbPolicies, you must set at least one
value for either the localityLbPolicies[].policy or the
localityLbPolicies[].customPolicy field. localityLbPolicies overrides any
value set in the localityLbPolicy field.

For an example of how to use this field, seeDefine
a list of preferred policies.

Caution: This field and its children are intended for use in a service mesh
that includes gRPC clients only. Envoy proxies can't use backend services
that have this configuration. |
| `metadatas` | HashMap<String, String> |  | Deployment metadata associated with the resource to be set by a GKE hub
controller and read by the backend RCTH |
| `network` | String |  | The URL of the network to which this backend service belongs.

This field must be set for Internal Passthrough Network Load Balancers when
the haPolicy is enabled, and for External Passthrough Network Load
Balancers when the haPolicy fastIpMove is enabled.

This field can only be specified when the load balancing scheme is set toINTERNAL, or when the load balancing scheme is set toEXTERNAL and haPolicy fastIpMove is enabled. |
| `port_name` | String |  | A named port on a backend instance group representing the port for
communication to the backend VMs in that group. The
named port must be [defined on each backend instance
group](https://cloud.google.com/load-balancing/docs/backend-service#named_ports).
This parameter has no meaning if the backends are NEGs. For internal
passthrough Network Load Balancers and external passthrough Network Load
Balancers, omit port_name. |
| `backends` | Vec<String> |  | The list of backends that serve this BackendService. |
| `cdn_policy` | String |  | Cloud CDN configuration for this BackendService. Only available for
specified load balancer types. |
| `max_stream_duration` | String |  | Specifies the default maximum duration (timeout) for streams to this
service. Duration is computed from the beginning of the stream until the
response has been completely processed, including all retries. A stream
that does not complete in this duration is closed.

If not specified, there will be no timeout limit, i.e. the maximum
duration is infinite.

This value can be overridden in the PathMatcher configuration of the
UrlMap that references this backend service.

This field is only allowed when the loadBalancingScheme of
the backend service is INTERNAL_SELF_MANAGED. |
| `health_checks` | Vec<String> |  | The list of URLs to the healthChecks, httpHealthChecks (legacy), or
httpsHealthChecks (legacy) resource for health checking this backend
service. Not all backend services support legacy health checks. See
Load balancer guide. Currently, at most one health check can be
specified for each backend service. Backend services with
instance group or zonal NEG backends must have a health check unless
haPolicy is specified. Backend services with internet or serverless NEG
backends must not have a health check.

healthChecks[] cannot be specified with haPolicy. |
| `region` | String |  | [Output Only] URL of the region where the regional backend service
resides. This field is not applicable to global backend services.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `timeout_sec` | i64 |  | The backend service timeout has a different meaning depending on the
type of load balancer. For more information see,
Backend service settings.
The default is 30 seconds.
The full range of timeout values allowed goes from 1
through 2,147,483,647 seconds.

This value can be overridden in the PathMatcher configuration of the
UrlMap that references this backend service.

Not supported when the backend service is referenced by a URL map that is
bound to target gRPC proxy that has validateForProxyless field set to true.
Instead, use maxStreamDuration. |
| `outlier_detection` | String |  | Settings controlling the ejection of unhealthy backend endpoints from the
load balancing pool of each individual proxy instance that processes the
traffic for the given backend service. If not set, this feature is
considered disabled.

Results of the outlier detection algorithm (ejection of endpoints from the
load balancing pool and returning them back to the pool) are executed
independently by each proxy instance of the load balancer. In most cases,
more than one proxy instance handles the traffic received by a backend
service. Thus, it is possible that an unhealthy endpoint is detected and
ejected by only some of the proxies, and while this happens, other proxies
may continue to send requests to the same unhealthy endpoint until they
detect and eject the unhealthy endpoint.

Applicable backend endpoints can be:
   
   - VM instances in an Instance Group
   - Endpoints in a Zonal NEG (GCE_VM_IP, GCE_VM_IP_PORT)
   - Endpoints in a Hybrid Connectivity NEG (NON_GCP_PRIVATE_IP_PORT)
   - Serverless NEGs, that resolve to Cloud Run, App Engine, or Cloud
   Functions Services 
   - Private Service Connect NEGs, that resolve to
   Google-managed regional API endpoints or managed services published using
   Private Service Connect



Applicable backend service types can be:
   
   - A global backend service with the loadBalancingScheme set to
   INTERNAL_SELF_MANAGED or EXTERNAL_MANAGED. 
   - A regional backend
   service with the serviceProtocol set to HTTP, HTTPS, HTTP2 or H2C, and
   loadBalancingScheme set to INTERNAL_MANAGED or EXTERNAL_MANAGED. Not
   supported for Serverless NEGs.



Not supported when the backend service is referenced by a URL map that is
bound to target gRPC proxy that has validateForProxyless field set to true. |
| `security_settings` | String |  | This field specifies the security settings that apply to this backend
service. This field is applicable to a global backend service with the
load_balancing_scheme set to INTERNAL_SELF_MANAGED. |
| `used_by` | Vec<String> |  | [Output Only] List of resources referencing given backend service. |
| `subsetting` | String |  | subsetting cannot be specified with haPolicy. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `edge_security_policy` | String |  | [Output Only] The resource URL for the edge security policy associated with
this backend service. |
| `ip_address_selection_policy` | String |  | Specifies a preference for traffic sent from the proxy to the backend (or
from the client to the backend for proxyless gRPC).
The possible values are:
   
   - IPV4_ONLY: Only send IPv4 traffic to the backends of the
   backend service (Instance Group, Managed Instance Group, Network Endpoint
   Group), regardless of traffic from the client to the proxy. Only IPv4
   health checks are used to check the health of the backends. This is the
   default setting.
   - PREFER_IPV6: Prioritize the connection to the endpoint's
   IPv6 address over its IPv4 address (provided there is a healthy IPv6
   address).
   - IPV6_ONLY: Only send IPv6 traffic to the backends of the
   backend service (Instance Group, Managed Instance Group, Network Endpoint
   Group), regardless of traffic from the client to the proxy. Only IPv6
   health checks are used to check the health of the backends.



This field is applicable to either:
   
   -  Advanced global external Application Load Balancer (load balancing
   scheme EXTERNAL_MANAGED), 
   -  Regional external Application Load
   Balancer, 
   -  Internal proxy Network Load Balancer (load balancing
   scheme INTERNAL_MANAGED), 
   -  Regional internal Application Load
   Balancer (load balancing scheme INTERNAL_MANAGED), 
   -  Traffic
   Director with Envoy proxies and proxyless gRPC (load balancing scheme
   INTERNAL_SELF_MANAGED). |
| `custom_request_headers` | Vec<String> |  | Headers that the load balancer adds to proxied requests. See [Creating
custom
headers](https://cloud.google.com/load-balancing/docs/custom-headers). |
| `locality_lb_policy` | String |  | The load balancing algorithm used within the scope of the locality. The
possible values are:
   
   - ROUND_ROBIN: This is a simple policy in which each healthy
   backend is selected in round robin order. This is the default.
   - LEAST_REQUEST: An O(1) algorithm which
   selects two random healthy hosts and picks the host which has fewer active
   requests.
   - RING_HASH: The ring/modulo hash load balancer implements
   consistent hashing to backends. The algorithm has the property that the
   addition/removal of a host from a set of N hosts only affects 1/N of the
   requests.
   - RANDOM: The load balancer selects a random healthy
   host.
   - ORIGINAL_DESTINATION: Backend host is selected
   based on the client connection metadata, i.e., connections are opened to
   the same address as the destination address of the incoming connection
   before the connection was redirected to the load balancer.
   - MAGLEV: used as a drop in replacement for the ring hash
   load balancer. Maglev is not as stable as ring hash but has faster table
   lookup build times and host selection times. For more information about
   Maglev, see Maglev:
   A Fast and Reliable Software Network Load Balancer.
   - WEIGHTED_ROUND_ROBIN: Per-endpoint Weighted Round Robin
   Load Balancing using weights computed from Backend reported Custom Metrics.
   If set, the Backend Service responses are expected to contain non-standard
   HTTP response header field Endpoint-Load-Metrics. The reported
   metrics to use for computing the weights are specified via thecustomMetrics field.
   
   This field is applicable to either:
      - A regional backend service with the service_protocol set to HTTP,
      HTTPS, HTTP2 or H2C, and load_balancing_scheme set to
      INTERNAL_MANAGED. 
      - A global backend service with the
      load_balancing_scheme set to INTERNAL_SELF_MANAGED, INTERNAL_MANAGED, or
      EXTERNAL_MANAGED.
   
   
   If sessionAffinity is not configured—that is, if session
   affinity remains at the default value of NONE—then the
   default value for localityLbPolicy
   is ROUND_ROBIN. If session affinity is set to a value other
   than NONE,
   then the default value for localityLbPolicy isMAGLEV.
   
   Only ROUND_ROBIN and RING_HASH are supported
   when the backend service is referenced by a URL map that is bound to
   target gRPC proxy that has validateForProxyless field set to true.
   
   localityLbPolicy cannot be specified with haPolicy. |
| `failover_policy` | String |  | Requires at least one backend instance group to be defined
as a backup (failover) backend.
For load balancers that have configurable failover:
[Internal passthrough Network Load
Balancers](https://cloud.google.com/load-balancing/docs/internal/failover-overview)
and [external passthrough Network Load
Balancers](https://cloud.google.com/load-balancing/docs/network/networklb-failover-overview).

failoverPolicy cannot be specified with haPolicy. |
| `external_managed_migration_state` | String |  | Specifies the canary migration state. Possible values are PREPARE,
TEST_BY_PERCENTAGE, and TEST_ALL_TRAFFIC.

To begin the migration from EXTERNAL to EXTERNAL_MANAGED, the state must be
changed to PREPARE. The state must be changed to TEST_ALL_TRAFFIC before
the loadBalancingScheme can be changed to EXTERNAL_MANAGED. Optionally, the
TEST_BY_PERCENTAGE state can be used to migrate traffic by percentage using
externalManagedMigrationTestingPercentage.

Rolling back a migration requires the states to be set in reverse order. So
changing the scheme from EXTERNAL_MANAGED to EXTERNAL requires the state to
be set to TEST_ALL_TRAFFIC at the same time. Optionally, the
TEST_BY_PERCENTAGE state can be used to migrate some traffic back to
EXTERNAL or PREPARE can be used to migrate all traffic back to EXTERNAL. |
| `name` | String |  | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `circuit_breakers` | String |  |  |
| `external_managed_migration_testing_percentage` | f64 |  | Determines the fraction of requests that should be processed by the Global
external Application Load Balancer.

The value of this field must be in the range [0, 100].

Session affinity options will slightly affect this routing behavior, for
more details, see:Session
Affinity.

This value can only be set if the loadBalancingScheme in the BackendService
is set to EXTERNAL (when using the classic Application Load Balancer) and
the migration state is TEST_BY_PERCENTAGE. |
| `load_balancing_scheme` | String |  | Specifies the load balancer type. A backend service
created for one type of load balancer cannot be used with another.
For more information, refer toChoosing
a load balancer. |
| `port` | i64 |  | Deprecated in favor of portName. The TCP port to connect on
the backend. The default value is 80.
For internal passthrough Network Load Balancers and external passthrough
Network Load Balancers, omit port. |
| `service_lb_policy` | String |  | URL to networkservices.ServiceLbPolicy resource.

Can only be set if load balancing scheme is EXTERNAL_MANAGED,
INTERNAL_MANAGED or INTERNAL_SELF_MANAGED and the scope is global. |
| `fingerprint` | String |  | Fingerprint of this resource. A hash of the contents stored in this object.
This field is used in optimistic locking. This field will be ignored when
inserting a BackendService. An up-to-date fingerprint must be provided in
order to update the BackendService, otherwise the request will
fail with error 412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve a BackendService. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `compression_mode` | String | Compress text responses using Brotli or gzip compression, based on
the client's Accept-Encoding header. |
| `dynamic_forwarding` | String | Dynamic forwarding configuration. This field is used to configure the
backend service with dynamic forwarding feature which together with Service
Extension allows customized and complex routing logic. |
| `connection_tracking_policy` | String | Connection Tracking configuration for this BackendService. Connection
tracking policy settings are only available for external passthrough
Network Load Balancers and internal passthrough Network Load Balancers.

connectionTrackingPolicy cannot be specified with haPolicy. |
| `custom_response_headers` | Vec<String> | Headers that the load balancer adds to proxied responses. See [Creating
custom
headers](https://cloud.google.com/load-balancing/docs/custom-headers). |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `iap` | String | The configurations for Identity-Aware Proxy on this resource.
Not available for internal passthrough Network Load Balancers and external
passthrough Network Load Balancers. |
| `network_pass_through_lb_traffic_policy` | String | Configures traffic steering properties of internal passthrough Network
Load Balancers.

networkPassThroughLbTrafficPolicy cannot be specified with haPolicy. |
| `custom_metrics` | Vec<String> | List of custom metrics that are used for theWEIGHTED_ROUND_ROBIN locality_lb_policy. |
| `params` | String | Input only. [Input Only] Additional params passed with the request, but not persisted
as part of resource payload. |
| `consistent_hash` | String | Consistent Hash-based load balancing can be used to provide soft session
affinity based on HTTP headers, cookies or other properties. This load
balancing policy is applicable only for HTTP connections. The affinity to a
particular destination host will be lost when one or more hosts are
added/removed from the destination service. This field specifies parameters
that control consistent hashing. This field is only applicable whenlocalityLbPolicy is set to MAGLEV orRING_HASH.

This field is applicable to either:
   
   - A regional backend service with the service_protocol set to HTTP,
   HTTPS, HTTP2 or H2C, and load_balancing_scheme set to
   INTERNAL_MANAGED. 
   - A global backend service with the
   load_balancing_scheme set to INTERNAL_SELF_MANAGED. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `enable_cdn` | bool | If true, enables Cloud CDN for the backend service of a
global external Application Load Balancer. |
| `connection_draining` | String | connectionDraining cannot be specified with haPolicy. |
| `affinity_cookie_ttl_sec` | i64 | Lifetime of cookies in seconds. This setting is applicable to Application
Load Balancers and Traffic Director and requires
GENERATED_COOKIE or HTTP_COOKIE session affinity.

If set to 0, the cookie is non-persistent and lasts only until
the end of the browser session (or equivalent). The maximum allowed value
is two weeks (1,209,600).

Not supported when the backend service is referenced by a URL map that is
bound to target gRPC proxy that has validateForProxyless field set to true. |
| `log_config` | String | This field denotes the logging options for the load balancer traffic served
by this backend service. If logging is enabled, logs will be exported to
Stackdriver. |
| `protocol` | String | The protocol this BackendService uses to communicate
with backends.

Possible values are HTTP, HTTPS, HTTP2, H2C, TCP, SSL, UDP or GRPC.
depending on the chosen load balancer or Traffic Director configuration.
Refer to the documentation for the load balancers or for Traffic Director
for more information.

Must be set to GRPC when the backend service is referenced by a URL map
that is bound to target gRPC proxy. |
| `session_affinity` | String | Type of session affinity to use. The default is NONE.

Only NONE and HEADER_FIELD are supported
when the backend service is referenced by a URL map that is bound to
target gRPC proxy that has validateForProxyless field set to true.

For more details, see:
[Session
Affinity](https://cloud.google.com/load-balancing/docs/backend-service#session_affinity).

sessionAffinity cannot be specified with haPolicy. |
| `security_policy` | String | [Output Only] The resource URL for the security policy associated with this
backend service. |
| `tls_settings` | String | Configuration for Backend Authenticated TLS and mTLS. May only be specified
when the backend protocol is SSL, HTTPS or HTTP2. |
| `ha_policy` | String | Configures self-managed High Availability (HA) for External and Internal
Protocol Forwarding.

The backends of this regional backend service must only specify zonal
network endpoint groups (NEGs) of type GCE_VM_IP.

When haPolicy is set for an Internal Passthrough Network Load Balancer, the
regional backend service must set the network field. All zonal NEGs must
belong to the same network. However, individual NEGs can
belong to different subnetworks of that network.

When haPolicy is specified, the set of attached network endpoints across
all backends comprise an High Availability domain from which one endpoint
is selected as the active endpoint (the leader) that receives all
traffic.

haPolicy can be added only at backend service creation time. Once set up,
it cannot be deleted.

Note that haPolicy is not for load balancing, and therefore cannot be
specified with sessionAffinity, connectionTrackingPolicy, and
failoverPolicy.

haPolicy requires customers to be responsible for tracking backend
endpoint health and electing a leader among the healthy endpoints.
Therefore, haPolicy cannot be specified with healthChecks.

haPolicy can only be specified for External Passthrough Network Load
Balancers and Internal Passthrough Network Load Balancers. |
| `strong_session_affinity_cookie` | String | Describes the HTTP cookie used for stateful session affinity. This field is
applicable and required if the sessionAffinity is set toSTRONG_COOKIE_AFFINITY. |
| `kind` | String | [Output Only] Type of resource. Always compute#backendService
for backend services. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `service_bindings` | Vec<String> | URLs of networkservices.ServiceBinding resources.

Can only be set if load balancing scheme is INTERNAL_SELF_MANAGED.
If set, lists of backends and health checks must be both empty. |
| `locality_lb_policies` | Vec<String> | A list of locality load-balancing policies to be used in order of
preference. When you use localityLbPolicies, you must set at least one
value for either the localityLbPolicies[].policy or the
localityLbPolicies[].customPolicy field. localityLbPolicies overrides any
value set in the localityLbPolicy field.

For an example of how to use this field, seeDefine
a list of preferred policies.

Caution: This field and its children are intended for use in a service mesh
that includes gRPC clients only. Envoy proxies can't use backend services
that have this configuration. |
| `metadatas` | HashMap<String, String> | Deployment metadata associated with the resource to be set by a GKE hub
controller and read by the backend RCTH |
| `network` | String | The URL of the network to which this backend service belongs.

This field must be set for Internal Passthrough Network Load Balancers when
the haPolicy is enabled, and for External Passthrough Network Load
Balancers when the haPolicy fastIpMove is enabled.

This field can only be specified when the load balancing scheme is set toINTERNAL, or when the load balancing scheme is set toEXTERNAL and haPolicy fastIpMove is enabled. |
| `port_name` | String | A named port on a backend instance group representing the port for
communication to the backend VMs in that group. The
named port must be [defined on each backend instance
group](https://cloud.google.com/load-balancing/docs/backend-service#named_ports).
This parameter has no meaning if the backends are NEGs. For internal
passthrough Network Load Balancers and external passthrough Network Load
Balancers, omit port_name. |
| `backends` | Vec<String> | The list of backends that serve this BackendService. |
| `cdn_policy` | String | Cloud CDN configuration for this BackendService. Only available for
specified load balancer types. |
| `max_stream_duration` | String | Specifies the default maximum duration (timeout) for streams to this
service. Duration is computed from the beginning of the stream until the
response has been completely processed, including all retries. A stream
that does not complete in this duration is closed.

If not specified, there will be no timeout limit, i.e. the maximum
duration is infinite.

This value can be overridden in the PathMatcher configuration of the
UrlMap that references this backend service.

This field is only allowed when the loadBalancingScheme of
the backend service is INTERNAL_SELF_MANAGED. |
| `health_checks` | Vec<String> | The list of URLs to the healthChecks, httpHealthChecks (legacy), or
httpsHealthChecks (legacy) resource for health checking this backend
service. Not all backend services support legacy health checks. See
Load balancer guide. Currently, at most one health check can be
specified for each backend service. Backend services with
instance group or zonal NEG backends must have a health check unless
haPolicy is specified. Backend services with internet or serverless NEG
backends must not have a health check.

healthChecks[] cannot be specified with haPolicy. |
| `region` | String | [Output Only] URL of the region where the regional backend service
resides. This field is not applicable to global backend services.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `timeout_sec` | i64 | The backend service timeout has a different meaning depending on the
type of load balancer. For more information see,
Backend service settings.
The default is 30 seconds.
The full range of timeout values allowed goes from 1
through 2,147,483,647 seconds.

This value can be overridden in the PathMatcher configuration of the
UrlMap that references this backend service.

Not supported when the backend service is referenced by a URL map that is
bound to target gRPC proxy that has validateForProxyless field set to true.
Instead, use maxStreamDuration. |
| `outlier_detection` | String | Settings controlling the ejection of unhealthy backend endpoints from the
load balancing pool of each individual proxy instance that processes the
traffic for the given backend service. If not set, this feature is
considered disabled.

Results of the outlier detection algorithm (ejection of endpoints from the
load balancing pool and returning them back to the pool) are executed
independently by each proxy instance of the load balancer. In most cases,
more than one proxy instance handles the traffic received by a backend
service. Thus, it is possible that an unhealthy endpoint is detected and
ejected by only some of the proxies, and while this happens, other proxies
may continue to send requests to the same unhealthy endpoint until they
detect and eject the unhealthy endpoint.

Applicable backend endpoints can be:
   
   - VM instances in an Instance Group
   - Endpoints in a Zonal NEG (GCE_VM_IP, GCE_VM_IP_PORT)
   - Endpoints in a Hybrid Connectivity NEG (NON_GCP_PRIVATE_IP_PORT)
   - Serverless NEGs, that resolve to Cloud Run, App Engine, or Cloud
   Functions Services 
   - Private Service Connect NEGs, that resolve to
   Google-managed regional API endpoints or managed services published using
   Private Service Connect



Applicable backend service types can be:
   
   - A global backend service with the loadBalancingScheme set to
   INTERNAL_SELF_MANAGED or EXTERNAL_MANAGED. 
   - A regional backend
   service with the serviceProtocol set to HTTP, HTTPS, HTTP2 or H2C, and
   loadBalancingScheme set to INTERNAL_MANAGED or EXTERNAL_MANAGED. Not
   supported for Serverless NEGs.



Not supported when the backend service is referenced by a URL map that is
bound to target gRPC proxy that has validateForProxyless field set to true. |
| `security_settings` | String | This field specifies the security settings that apply to this backend
service. This field is applicable to a global backend service with the
load_balancing_scheme set to INTERNAL_SELF_MANAGED. |
| `used_by` | Vec<String> | [Output Only] List of resources referencing given backend service. |
| `subsetting` | String | subsetting cannot be specified with haPolicy. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `edge_security_policy` | String | [Output Only] The resource URL for the edge security policy associated with
this backend service. |
| `ip_address_selection_policy` | String | Specifies a preference for traffic sent from the proxy to the backend (or
from the client to the backend for proxyless gRPC).
The possible values are:
   
   - IPV4_ONLY: Only send IPv4 traffic to the backends of the
   backend service (Instance Group, Managed Instance Group, Network Endpoint
   Group), regardless of traffic from the client to the proxy. Only IPv4
   health checks are used to check the health of the backends. This is the
   default setting.
   - PREFER_IPV6: Prioritize the connection to the endpoint's
   IPv6 address over its IPv4 address (provided there is a healthy IPv6
   address).
   - IPV6_ONLY: Only send IPv6 traffic to the backends of the
   backend service (Instance Group, Managed Instance Group, Network Endpoint
   Group), regardless of traffic from the client to the proxy. Only IPv6
   health checks are used to check the health of the backends.



This field is applicable to either:
   
   -  Advanced global external Application Load Balancer (load balancing
   scheme EXTERNAL_MANAGED), 
   -  Regional external Application Load
   Balancer, 
   -  Internal proxy Network Load Balancer (load balancing
   scheme INTERNAL_MANAGED), 
   -  Regional internal Application Load
   Balancer (load balancing scheme INTERNAL_MANAGED), 
   -  Traffic
   Director with Envoy proxies and proxyless gRPC (load balancing scheme
   INTERNAL_SELF_MANAGED). |
| `custom_request_headers` | Vec<String> | Headers that the load balancer adds to proxied requests. See [Creating
custom
headers](https://cloud.google.com/load-balancing/docs/custom-headers). |
| `locality_lb_policy` | String | The load balancing algorithm used within the scope of the locality. The
possible values are:
   
   - ROUND_ROBIN: This is a simple policy in which each healthy
   backend is selected in round robin order. This is the default.
   - LEAST_REQUEST: An O(1) algorithm which
   selects two random healthy hosts and picks the host which has fewer active
   requests.
   - RING_HASH: The ring/modulo hash load balancer implements
   consistent hashing to backends. The algorithm has the property that the
   addition/removal of a host from a set of N hosts only affects 1/N of the
   requests.
   - RANDOM: The load balancer selects a random healthy
   host.
   - ORIGINAL_DESTINATION: Backend host is selected
   based on the client connection metadata, i.e., connections are opened to
   the same address as the destination address of the incoming connection
   before the connection was redirected to the load balancer.
   - MAGLEV: used as a drop in replacement for the ring hash
   load balancer. Maglev is not as stable as ring hash but has faster table
   lookup build times and host selection times. For more information about
   Maglev, see Maglev:
   A Fast and Reliable Software Network Load Balancer.
   - WEIGHTED_ROUND_ROBIN: Per-endpoint Weighted Round Robin
   Load Balancing using weights computed from Backend reported Custom Metrics.
   If set, the Backend Service responses are expected to contain non-standard
   HTTP response header field Endpoint-Load-Metrics. The reported
   metrics to use for computing the weights are specified via thecustomMetrics field.
   
   This field is applicable to either:
      - A regional backend service with the service_protocol set to HTTP,
      HTTPS, HTTP2 or H2C, and load_balancing_scheme set to
      INTERNAL_MANAGED. 
      - A global backend service with the
      load_balancing_scheme set to INTERNAL_SELF_MANAGED, INTERNAL_MANAGED, or
      EXTERNAL_MANAGED.
   
   
   If sessionAffinity is not configured—that is, if session
   affinity remains at the default value of NONE—then the
   default value for localityLbPolicy
   is ROUND_ROBIN. If session affinity is set to a value other
   than NONE,
   then the default value for localityLbPolicy isMAGLEV.
   
   Only ROUND_ROBIN and RING_HASH are supported
   when the backend service is referenced by a URL map that is bound to
   target gRPC proxy that has validateForProxyless field set to true.
   
   localityLbPolicy cannot be specified with haPolicy. |
| `failover_policy` | String | Requires at least one backend instance group to be defined
as a backup (failover) backend.
For load balancers that have configurable failover:
[Internal passthrough Network Load
Balancers](https://cloud.google.com/load-balancing/docs/internal/failover-overview)
and [external passthrough Network Load
Balancers](https://cloud.google.com/load-balancing/docs/network/networklb-failover-overview).

failoverPolicy cannot be specified with haPolicy. |
| `external_managed_migration_state` | String | Specifies the canary migration state. Possible values are PREPARE,
TEST_BY_PERCENTAGE, and TEST_ALL_TRAFFIC.

To begin the migration from EXTERNAL to EXTERNAL_MANAGED, the state must be
changed to PREPARE. The state must be changed to TEST_ALL_TRAFFIC before
the loadBalancingScheme can be changed to EXTERNAL_MANAGED. Optionally, the
TEST_BY_PERCENTAGE state can be used to migrate traffic by percentage using
externalManagedMigrationTestingPercentage.

Rolling back a migration requires the states to be set in reverse order. So
changing the scheme from EXTERNAL_MANAGED to EXTERNAL requires the state to
be set to TEST_ALL_TRAFFIC at the same time. Optionally, the
TEST_BY_PERCENTAGE state can be used to migrate some traffic back to
EXTERNAL or PREPARE can be used to migrate all traffic back to EXTERNAL. |
| `name` | String | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `circuit_breakers` | String |  |
| `external_managed_migration_testing_percentage` | f64 | Determines the fraction of requests that should be processed by the Global
external Application Load Balancer.

The value of this field must be in the range [0, 100].

Session affinity options will slightly affect this routing behavior, for
more details, see:Session
Affinity.

This value can only be set if the loadBalancingScheme in the BackendService
is set to EXTERNAL (when using the classic Application Load Balancer) and
the migration state is TEST_BY_PERCENTAGE. |
| `load_balancing_scheme` | String | Specifies the load balancer type. A backend service
created for one type of load balancer cannot be used with another.
For more information, refer toChoosing
a load balancer. |
| `port` | i64 | Deprecated in favor of portName. The TCP port to connect on
the backend. The default value is 80.
For internal passthrough Network Load Balancers and external passthrough
Network Load Balancers, omit port. |
| `service_lb_policy` | String | URL to networkservices.ServiceLbPolicy resource.

Can only be set if load balancing scheme is EXTERNAL_MANAGED,
INTERNAL_MANAGED or INTERNAL_SELF_MANAGED and the scope is global. |
| `fingerprint` | String | Fingerprint of this resource. A hash of the contents stored in this object.
This field is used in optimistic locking. This field will be ignored when
inserting a BackendService. An up-to-date fingerprint must be provided in
order to update the BackendService, otherwise the request will
fail with error 412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve a BackendService. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create backend_service
backend_service = provider.compute_api.Backend_service {
    project = "value"  # Project ID for this request.
}

# Access backend_service outputs
backend_service_id = backend_service.id
backend_service_compression_mode = backend_service.compression_mode
backend_service_dynamic_forwarding = backend_service.dynamic_forwarding
backend_service_connection_tracking_policy = backend_service.connection_tracking_policy
backend_service_custom_response_headers = backend_service.custom_response_headers
backend_service_description = backend_service.description
backend_service_iap = backend_service.iap
backend_service_network_pass_through_lb_traffic_policy = backend_service.network_pass_through_lb_traffic_policy
backend_service_custom_metrics = backend_service.custom_metrics
backend_service_params = backend_service.params
backend_service_consistent_hash = backend_service.consistent_hash
backend_service_creation_timestamp = backend_service.creation_timestamp
backend_service_enable_cdn = backend_service.enable_cdn
backend_service_connection_draining = backend_service.connection_draining
backend_service_affinity_cookie_ttl_sec = backend_service.affinity_cookie_ttl_sec
backend_service_log_config = backend_service.log_config
backend_service_protocol = backend_service.protocol
backend_service_session_affinity = backend_service.session_affinity
backend_service_security_policy = backend_service.security_policy
backend_service_tls_settings = backend_service.tls_settings
backend_service_ha_policy = backend_service.ha_policy
backend_service_strong_session_affinity_cookie = backend_service.strong_session_affinity_cookie
backend_service_kind = backend_service.kind
backend_service_self_link = backend_service.self_link
backend_service_service_bindings = backend_service.service_bindings
backend_service_locality_lb_policies = backend_service.locality_lb_policies
backend_service_metadatas = backend_service.metadatas
backend_service_network = backend_service.network
backend_service_port_name = backend_service.port_name
backend_service_backends = backend_service.backends
backend_service_cdn_policy = backend_service.cdn_policy
backend_service_max_stream_duration = backend_service.max_stream_duration
backend_service_health_checks = backend_service.health_checks
backend_service_region = backend_service.region
backend_service_timeout_sec = backend_service.timeout_sec
backend_service_outlier_detection = backend_service.outlier_detection
backend_service_security_settings = backend_service.security_settings
backend_service_used_by = backend_service.used_by
backend_service_subsetting = backend_service.subsetting
backend_service_id = backend_service.id
backend_service_edge_security_policy = backend_service.edge_security_policy
backend_service_ip_address_selection_policy = backend_service.ip_address_selection_policy
backend_service_custom_request_headers = backend_service.custom_request_headers
backend_service_locality_lb_policy = backend_service.locality_lb_policy
backend_service_failover_policy = backend_service.failover_policy
backend_service_external_managed_migration_state = backend_service.external_managed_migration_state
backend_service_name = backend_service.name
backend_service_circuit_breakers = backend_service.circuit_breakers
backend_service_external_managed_migration_testing_percentage = backend_service.external_managed_migration_testing_percentage
backend_service_load_balancing_scheme = backend_service.load_balancing_scheme
backend_service_port = backend_service.port
backend_service_service_lb_policy = backend_service.service_lb_policy
backend_service_fingerprint = backend_service.fingerprint
```

---


### License

Create a License resource in the specified project.
 *Caution* This resource is intended
for use only by third-party partners who are creatingCloud Marketplace
images.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `removable_from_disk` | bool |  | If true, this license can be removed from a disk's set of licenses, with no
replacement license needed. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `incompatible_licenses` | Vec<String> |  | Specifies licenseCodes of licenses that are incompatible with this license.
If a license is incompatible with this license, it cannot be attached to
the same disk or image. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `description` | String |  | An optional textual description of the resource; provided by the client
when the resource is created. |
| `name` | String |  | Name of the resource. The name must be 1-63 characters long and
comply withRFC1035. |
| `appendable_to_disk` | bool |  | If true, this license can be appended to an existing disk's set of
licenses. |
| `update_timestamp` | String |  | [Output Only] Last update timestamp inRFC3339
text format. |
| `sole_tenant_only` | bool |  | If true, this license can only be used on VMs on sole tenant nodes. |
| `resource_requirements` | String |  | [Input Only] Deprecated. |
| `charges_use_fee` | bool |  | [Output Only] Deprecated. This field no longer reflects whether a license
charges a usage fee. |
| `transferable` | bool |  | If false, licenses will not be copied from the source resource when
creating an image from a disk, disk from snapshot, or snapshot from disk. |
| `kind` | String |  | [Output Only] Type of resource. Always compute#license for
licenses. |
| `self_link_with_id` | String |  | [Output Only] Server-defined URL for this resource with the resource id. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `minimum_retention` | String |  | If set, this license will be unable to be removed or replaced once attached
to a disk until the minimum_retention period has passed. |
| `required_coattached_licenses` | Vec<String> |  | Specifies the set of permissible coattached licenseCodes of licenses that
satisfy the coattachment requirement of this license. At least one license
from the set must be attached to the same disk or image as this license. |
| `multi_tenant_only` | bool |  | If true, this license can only be used on VMs on multi tenant nodes. |
| `allowed_replacement_licenses` | Vec<String> |  | Specifies licenseCodes of licenses that can replace this license. Note:
such replacements are allowed even if removable_from_disk is false. |
| `license_code` | String |  | [Output Only] The unique code used to attach this license to images,
snapshots, and disks. |
| `os_license` | bool |  | If true, indicates this is an OS license. Only one OS license can be
attached to a disk or image at a time. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `removable_from_disk` | bool | If true, this license can be removed from a disk's set of licenses, with no
replacement license needed. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `incompatible_licenses` | Vec<String> | Specifies licenseCodes of licenses that are incompatible with this license.
If a license is incompatible with this license, it cannot be attached to
the same disk or image. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `description` | String | An optional textual description of the resource; provided by the client
when the resource is created. |
| `name` | String | Name of the resource. The name must be 1-63 characters long and
comply withRFC1035. |
| `appendable_to_disk` | bool | If true, this license can be appended to an existing disk's set of
licenses. |
| `update_timestamp` | String | [Output Only] Last update timestamp inRFC3339
text format. |
| `sole_tenant_only` | bool | If true, this license can only be used on VMs on sole tenant nodes. |
| `resource_requirements` | String | [Input Only] Deprecated. |
| `charges_use_fee` | bool | [Output Only] Deprecated. This field no longer reflects whether a license
charges a usage fee. |
| `transferable` | bool | If false, licenses will not be copied from the source resource when
creating an image from a disk, disk from snapshot, or snapshot from disk. |
| `kind` | String | [Output Only] Type of resource. Always compute#license for
licenses. |
| `self_link_with_id` | String | [Output Only] Server-defined URL for this resource with the resource id. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `minimum_retention` | String | If set, this license will be unable to be removed or replaced once attached
to a disk until the minimum_retention period has passed. |
| `required_coattached_licenses` | Vec<String> | Specifies the set of permissible coattached licenseCodes of licenses that
satisfy the coattachment requirement of this license. At least one license
from the set must be attached to the same disk or image as this license. |
| `multi_tenant_only` | bool | If true, this license can only be used on VMs on multi tenant nodes. |
| `allowed_replacement_licenses` | Vec<String> | Specifies licenseCodes of licenses that can replace this license. Note:
such replacements are allowed even if removable_from_disk is false. |
| `license_code` | String | [Output Only] The unique code used to attach this license to images,
snapshots, and disks. |
| `os_license` | bool | If true, indicates this is an OS license. Only one OS license can be
attached to a disk or image at a time. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create license
license = provider.compute_api.License {
    project = "value"  # Project ID for this request.
}

# Access license outputs
license_id = license.id
license_removable_from_disk = license.removable_from_disk
license_id = license.id
license_incompatible_licenses = license.incompatible_licenses
license_creation_timestamp = license.creation_timestamp
license_description = license.description
license_name = license.name
license_appendable_to_disk = license.appendable_to_disk
license_update_timestamp = license.update_timestamp
license_sole_tenant_only = license.sole_tenant_only
license_resource_requirements = license.resource_requirements
license_charges_use_fee = license.charges_use_fee
license_transferable = license.transferable
license_kind = license.kind
license_self_link_with_id = license.self_link_with_id
license_self_link = license.self_link
license_minimum_retention = license.minimum_retention
license_required_coattached_licenses = license.required_coattached_licenses
license_multi_tenant_only = license.multi_tenant_only
license_allowed_replacement_licenses = license.allowed_replacement_licenses
license_license_code = license.license_code
license_os_license = license.os_license
```

---


### Addresse

Creates an address resource in the specified project by using the data
included in the request.

**Operations**: ✅ Create ✅ Read ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `purpose` | String |  | The purpose of this resource, which can be one of the following values:
   
   
     - GCE_ENDPOINT for addresses that are used by VM
     instances, alias IP ranges, load balancers, and similar resources.
     - DNS_RESOLVER for a DNS resolver address in a subnetwork
       for a Cloud DNS  inbound
       forwarder IP addresses (regional internal IP address in a subnet of
       a VPC network)
     - VPC_PEERING for global internal IP addresses used for
      
          private services access allocated ranges.
     - NAT_AUTO for the regional external IP addresses used by
          Cloud NAT when allocating addresses using
          
          automatic NAT IP address allocation.
     - IPSEC_INTERCONNECT for addresses created from a private
     IP range that are reserved for a VLAN attachment in an
     *HA VPN over Cloud Interconnect* configuration. These addresses
     are regional resources.
     - `SHARED_LOADBALANCER_VIP` for an internal IP address that is assigned
     to multiple internal forwarding rules.
     - `PRIVATE_SERVICE_CONNECT` for a private network address that is
     used to configure Private Service Connect. Only global internal addresses
     can use this purpose. |
| `users` | Vec<String> |  | [Output Only] The URLs of the resources that are using this address. |
| `ip_collection` | String |  | Reference to the source of external IPv4 addresses,
like a PublicDelegatedPrefix (PDP) for BYOIP.
The PDP must support enhanced IPv4 allocations.

Use one of the following formats to specify a PDP when reserving an
external IPv4 address using BYOIP.
   
   - 
   Full resource URL, as inhttps://www.googleapis.com/compute/v1/projects/projectId/regions/region/publicDelegatedPrefixes/pdp-name
   - 
   Partial URL, as in
     
      
          - projects/projectId/regions/region/publicDelegatedPrefixes/pdp-name
          - regions/region/publicDelegatedPrefixes/pdp-name |
| `name` | String |  | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?`. The first character
must be a lowercase letter, and all following characters (except for the
last character) must be a dash, lowercase letter, or digit. The last
character must be a lowercase letter or digit. |
| `label_fingerprint` | String |  | A fingerprint for the labels being applied to this Address, which is
essentially a hash of the labels set used for optimistic locking. The
fingerprint is initially generated by Compute Engine and changes after
every request to modify or update labels. You must always provide an
up-to-date fingerprint hash in order to update or change labels,
otherwise the request will fail with error412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve an Address. |
| `labels` | HashMap<String, String> |  | Labels for this resource. These can only be added or modified by thesetLabels method. Each label key/value pair must comply withRFC1035.
Label values may be empty. |
| `description` | String |  | An optional description of this resource. Provide this field when you
create the resource. |
| `ip_version` | String |  | The IP version that will be used by this address. Valid options areIPV4 or IPV6. |
| `network` | String |  | The URL of the network in which to reserve the address. This field can
only be used with INTERNAL type with theVPC_PEERING purpose. |
| `prefix_length` | i64 |  | The prefix length if the resource represents an IP range. |
| `region` | String |  | [Output Only] The URL of the region where a regional address resides.
For regional addresses, you must specify the region as a path parameter in
the HTTP request URL. *This field is not applicable to global
addresses.* |
| `kind` | String |  | [Output Only] Type of the resource. Always compute#address for
addresses. |
| `address_type` | String |  | The type of address to reserve, either INTERNAL orEXTERNAL. If unspecified, defaults to EXTERNAL. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `ipv6_endpoint_type` | String |  | The endpoint type of this address, which should be VM
or NETLB. This is used for deciding which type of endpoint
this address can be used after the external IPv6 address reservation. |
| `status` | String |  | [Output Only] The status of the address, which can be one ofRESERVING, RESERVED, or IN_USE.
An address that is RESERVING is currently in the process of
being reserved. A RESERVED address is currently reserved and
available to use. An IN_USE address is currently being used
by another resource and is not available. |
| `subnetwork` | String |  | The URL of the subnetwork in which to reserve the address. If an IP address
is specified, it must be within the subnetwork's IP range. This field can
only be used with INTERNAL type with aGCE_ENDPOINT or DNS_RESOLVER purpose. |
| `network_tier` | String |  | This signifies the networking tier used for configuring this address and
can only take the following values: PREMIUM orSTANDARD. Internal IP addresses are always Premium Tier;
global external IP addresses are always Premium Tier; regional external IP
addresses can be either Standard or Premium Tier.

If this field is not specified, it is assumed to be PREMIUM. |
| `address` | String |  | The static IP address represented by this resource. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `project` | String | ✅ | Project ID for this request. |
| `region` | String | ✅ | Name of the region for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `purpose` | String | The purpose of this resource, which can be one of the following values:
   
   
     - GCE_ENDPOINT for addresses that are used by VM
     instances, alias IP ranges, load balancers, and similar resources.
     - DNS_RESOLVER for a DNS resolver address in a subnetwork
       for a Cloud DNS  inbound
       forwarder IP addresses (regional internal IP address in a subnet of
       a VPC network)
     - VPC_PEERING for global internal IP addresses used for
      
          private services access allocated ranges.
     - NAT_AUTO for the regional external IP addresses used by
          Cloud NAT when allocating addresses using
          
          automatic NAT IP address allocation.
     - IPSEC_INTERCONNECT for addresses created from a private
     IP range that are reserved for a VLAN attachment in an
     *HA VPN over Cloud Interconnect* configuration. These addresses
     are regional resources.
     - `SHARED_LOADBALANCER_VIP` for an internal IP address that is assigned
     to multiple internal forwarding rules.
     - `PRIVATE_SERVICE_CONNECT` for a private network address that is
     used to configure Private Service Connect. Only global internal addresses
     can use this purpose. |
| `users` | Vec<String> | [Output Only] The URLs of the resources that are using this address. |
| `ip_collection` | String | Reference to the source of external IPv4 addresses,
like a PublicDelegatedPrefix (PDP) for BYOIP.
The PDP must support enhanced IPv4 allocations.

Use one of the following formats to specify a PDP when reserving an
external IPv4 address using BYOIP.
   
   - 
   Full resource URL, as inhttps://www.googleapis.com/compute/v1/projects/projectId/regions/region/publicDelegatedPrefixes/pdp-name
   - 
   Partial URL, as in
     
      
          - projects/projectId/regions/region/publicDelegatedPrefixes/pdp-name
          - regions/region/publicDelegatedPrefixes/pdp-name |
| `name` | String | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?`. The first character
must be a lowercase letter, and all following characters (except for the
last character) must be a dash, lowercase letter, or digit. The last
character must be a lowercase letter or digit. |
| `label_fingerprint` | String | A fingerprint for the labels being applied to this Address, which is
essentially a hash of the labels set used for optimistic locking. The
fingerprint is initially generated by Compute Engine and changes after
every request to modify or update labels. You must always provide an
up-to-date fingerprint hash in order to update or change labels,
otherwise the request will fail with error412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve an Address. |
| `labels` | HashMap<String, String> | Labels for this resource. These can only be added or modified by thesetLabels method. Each label key/value pair must comply withRFC1035.
Label values may be empty. |
| `description` | String | An optional description of this resource. Provide this field when you
create the resource. |
| `ip_version` | String | The IP version that will be used by this address. Valid options areIPV4 or IPV6. |
| `network` | String | The URL of the network in which to reserve the address. This field can
only be used with INTERNAL type with theVPC_PEERING purpose. |
| `prefix_length` | i64 | The prefix length if the resource represents an IP range. |
| `region` | String | [Output Only] The URL of the region where a regional address resides.
For regional addresses, you must specify the region as a path parameter in
the HTTP request URL. *This field is not applicable to global
addresses.* |
| `kind` | String | [Output Only] Type of the resource. Always compute#address for
addresses. |
| `address_type` | String | The type of address to reserve, either INTERNAL orEXTERNAL. If unspecified, defaults to EXTERNAL. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `ipv6_endpoint_type` | String | The endpoint type of this address, which should be VM
or NETLB. This is used for deciding which type of endpoint
this address can be used after the external IPv6 address reservation. |
| `status` | String | [Output Only] The status of the address, which can be one ofRESERVING, RESERVED, or IN_USE.
An address that is RESERVING is currently in the process of
being reserved. A RESERVED address is currently reserved and
available to use. An IN_USE address is currently being used
by another resource and is not available. |
| `subnetwork` | String | The URL of the subnetwork in which to reserve the address. If an IP address
is specified, it must be within the subnetwork's IP range. This field can
only be used with INTERNAL type with aGCE_ENDPOINT or DNS_RESOLVER purpose. |
| `network_tier` | String | This signifies the networking tier used for configuring this address and
can only take the following values: PREMIUM orSTANDARD. Internal IP addresses are always Premium Tier;
global external IP addresses are always Premium Tier; regional external IP
addresses can be either Standard or Premium Tier.

If this field is not specified, it is assumed to be PREMIUM. |
| `address` | String | The static IP address represented by this resource. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create addresse
addresse = provider.compute_api.Addresse {
    project = "value"  # Project ID for this request.
    region = "value"  # Name of the region for this request.
}

# Access addresse outputs
addresse_id = addresse.id
addresse_id = addresse.id
addresse_purpose = addresse.purpose
addresse_users = addresse.users
addresse_ip_collection = addresse.ip_collection
addresse_name = addresse.name
addresse_label_fingerprint = addresse.label_fingerprint
addresse_labels = addresse.labels
addresse_description = addresse.description
addresse_ip_version = addresse.ip_version
addresse_network = addresse.network
addresse_prefix_length = addresse.prefix_length
addresse_region = addresse.region
addresse_kind = addresse.kind
addresse_address_type = addresse.address_type
addresse_creation_timestamp = addresse.creation_timestamp
addresse_ipv6_endpoint_type = addresse.ipv6_endpoint_type
addresse_status = addresse.status
addresse_subnetwork = addresse.subnetwork
addresse_network_tier = addresse.network_tier
addresse_address = addresse.address
addresse_self_link = addresse.self_link
```

---


### Interconnect_group

Creates a InterconnectGroup in the specified project in the given scope
using the parameters that are included in the request.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `etag` | String |  | Opaque system-generated token that uniquely identifies the configuration.
If provided when patching a configuration in update mode, the provided
token must match the current token or the update is rejected. This provides
a reliable means of doing read-modify-write (optimistic locking) as
described by AIP 154. |
| `id` | String |  | [Output Only] The unique identifier for the resource type. The server
generates this identifier. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `intent` | String |  |  |
| `configured` | String |  |  |
| `name` | String |  | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `physical_structure` | String |  |  |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `interconnects` | HashMap<String, String> |  | Interconnects in the InterconnectGroup. Keys are arbitrary user-specified
strings. Users are encouraged, but not required, to use their preferred
format for resource links as keys.
Note that there are add-members and remove-members methods in gcloud.
The size of this map is limited by an "Interconnects per group" quota. |
| `kind` | String |  | [Output Only] Type of the resource. Always compute#InterconnectGroup |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `etag` | String | Opaque system-generated token that uniquely identifies the configuration.
If provided when patching a configuration in update mode, the provided
token must match the current token or the update is rejected. This provides
a reliable means of doing read-modify-write (optimistic locking) as
described by AIP 154. |
| `id` | String | [Output Only] The unique identifier for the resource type. The server
generates this identifier. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `intent` | String |  |
| `configured` | String |  |
| `name` | String | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `physical_structure` | String |  |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `interconnects` | HashMap<String, String> | Interconnects in the InterconnectGroup. Keys are arbitrary user-specified
strings. Users are encouraged, but not required, to use their preferred
format for resource links as keys.
Note that there are add-members and remove-members methods in gcloud.
The size of this map is limited by an "Interconnects per group" quota. |
| `kind` | String | [Output Only] Type of the resource. Always compute#InterconnectGroup |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create interconnect_group
interconnect_group = provider.compute_api.Interconnect_group {
    project = "value"  # Project ID for this request.
}

# Access interconnect_group outputs
interconnect_group_id = interconnect_group.id
interconnect_group_etag = interconnect_group.etag
interconnect_group_id = interconnect_group.id
interconnect_group_description = interconnect_group.description
interconnect_group_intent = interconnect_group.intent
interconnect_group_configured = interconnect_group.configured
interconnect_group_name = interconnect_group.name
interconnect_group_physical_structure = interconnect_group.physical_structure
interconnect_group_self_link = interconnect_group.self_link
interconnect_group_interconnects = interconnect_group.interconnects
interconnect_group_kind = interconnect_group.kind
interconnect_group_creation_timestamp = interconnect_group.creation_timestamp
```

---


### Public_advertised_prefixe

Creates a PublicAdvertisedPrefix in the specified project
using the parameters that are included in the request.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `shared_secret` | String |  | [Output Only] The shared secret to be used for reverse DNS verification. |
| `pdp_scope` | String |  | Specifies how child public delegated prefix will be scoped. It could
be one of following values:
   
   
     - `REGIONAL`: The public delegated prefix is regional only. The
     provisioning will take a few minutes.
     - `GLOBAL`: The public delegated prefix is global only. The
     provisioning will take ~4 weeks.
     - `GLOBAL_AND_REGIONAL` [output only]: The public delegated prefixes is
      BYOIP V1 legacy prefix. This is output only value and no longer
      supported in BYOIP V2. |
| `ip_cidr_range` | String |  | The address range, in CIDR format, represented by this public advertised
prefix. |
| `id` | String |  | [Output Only] The unique identifier for the resource type. The server
generates this identifier. |
| `ipv6_access_type` | String |  | The internet access type for IPv6 Public Advertised Prefixes. |
| `fingerprint` | String |  | Fingerprint of this resource. A hash of the contents stored in this object.
This field is used in optimistic locking. This field will be ignored when
inserting a new PublicAdvertisedPrefix. An up-to-date
fingerprint must be provided in order to update thePublicAdvertisedPrefix, otherwise the request will fail with
error 412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve a PublicAdvertisedPrefix. |
| `name` | String |  | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `dns_verification_ip` | String |  | The address to be used for reverse DNS verification. |
| `public_delegated_prefixs` | Vec<String> |  | [Output Only] The list of public delegated prefixes that exist for this
public advertised prefix. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `status` | String |  | The status of the public advertised prefix. Possible values include:
   
   
     - `INITIAL`: RPKI validation is complete.
     - `PTR_CONFIGURED`: User has configured the PTR.
     - `VALIDATED`: Reverse DNS lookup is successful.
     - `REVERSE_DNS_LOOKUP_FAILED`: Reverse DNS lookup failed.
     - `PREFIX_CONFIGURATION_IN_PROGRESS`: The prefix is being
     configured.
     - `PREFIX_CONFIGURATION_COMPLETE`: The prefix is fully configured.
     - `PREFIX_REMOVAL_IN_PROGRESS`: The prefix is being removed. |
| `byoip_api_version` | String |  | [Output Only] The version of BYOIP API. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `kind` | String |  | [Output Only] Type of the resource. Alwayscompute#publicAdvertisedPrefix for public advertised prefixes. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `shared_secret` | String | [Output Only] The shared secret to be used for reverse DNS verification. |
| `pdp_scope` | String | Specifies how child public delegated prefix will be scoped. It could
be one of following values:
   
   
     - `REGIONAL`: The public delegated prefix is regional only. The
     provisioning will take a few minutes.
     - `GLOBAL`: The public delegated prefix is global only. The
     provisioning will take ~4 weeks.
     - `GLOBAL_AND_REGIONAL` [output only]: The public delegated prefixes is
      BYOIP V1 legacy prefix. This is output only value and no longer
      supported in BYOIP V2. |
| `ip_cidr_range` | String | The address range, in CIDR format, represented by this public advertised
prefix. |
| `id` | String | [Output Only] The unique identifier for the resource type. The server
generates this identifier. |
| `ipv6_access_type` | String | The internet access type for IPv6 Public Advertised Prefixes. |
| `fingerprint` | String | Fingerprint of this resource. A hash of the contents stored in this object.
This field is used in optimistic locking. This field will be ignored when
inserting a new PublicAdvertisedPrefix. An up-to-date
fingerprint must be provided in order to update thePublicAdvertisedPrefix, otherwise the request will fail with
error 412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve a PublicAdvertisedPrefix. |
| `name` | String | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `dns_verification_ip` | String | The address to be used for reverse DNS verification. |
| `public_delegated_prefixs` | Vec<String> | [Output Only] The list of public delegated prefixes that exist for this
public advertised prefix. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `status` | String | The status of the public advertised prefix. Possible values include:
   
   
     - `INITIAL`: RPKI validation is complete.
     - `PTR_CONFIGURED`: User has configured the PTR.
     - `VALIDATED`: Reverse DNS lookup is successful.
     - `REVERSE_DNS_LOOKUP_FAILED`: Reverse DNS lookup failed.
     - `PREFIX_CONFIGURATION_IN_PROGRESS`: The prefix is being
     configured.
     - `PREFIX_CONFIGURATION_COMPLETE`: The prefix is fully configured.
     - `PREFIX_REMOVAL_IN_PROGRESS`: The prefix is being removed. |
| `byoip_api_version` | String | [Output Only] The version of BYOIP API. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `kind` | String | [Output Only] Type of the resource. Alwayscompute#publicAdvertisedPrefix for public advertised prefixes. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create public_advertised_prefixe
public_advertised_prefixe = provider.compute_api.Public_advertised_prefixe {
    project = "value"  # Project ID for this request.
}

# Access public_advertised_prefixe outputs
public_advertised_prefixe_id = public_advertised_prefixe.id
public_advertised_prefixe_shared_secret = public_advertised_prefixe.shared_secret
public_advertised_prefixe_pdp_scope = public_advertised_prefixe.pdp_scope
public_advertised_prefixe_ip_cidr_range = public_advertised_prefixe.ip_cidr_range
public_advertised_prefixe_id = public_advertised_prefixe.id
public_advertised_prefixe_ipv6_access_type = public_advertised_prefixe.ipv6_access_type
public_advertised_prefixe_fingerprint = public_advertised_prefixe.fingerprint
public_advertised_prefixe_name = public_advertised_prefixe.name
public_advertised_prefixe_dns_verification_ip = public_advertised_prefixe.dns_verification_ip
public_advertised_prefixe_public_delegated_prefixs = public_advertised_prefixe.public_delegated_prefixs
public_advertised_prefixe_self_link = public_advertised_prefixe.self_link
public_advertised_prefixe_status = public_advertised_prefixe.status
public_advertised_prefixe_byoip_api_version = public_advertised_prefixe.byoip_api_version
public_advertised_prefixe_creation_timestamp = public_advertised_prefixe.creation_timestamp
public_advertised_prefixe_description = public_advertised_prefixe.description
public_advertised_prefixe_kind = public_advertised_prefixe.kind
```

---


### Autoscaler

Creates an autoscaler in the specified project using the data
included in the request.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `autoscaling_policy` | String |  | The configuration parameters for the autoscaling algorithm. You can define
one or more signals for an autoscaler: cpuUtilization,customMetricUtilizations, andloadBalancingUtilization.

If none of these are specified, the default will be to autoscale based oncpuUtilization to 0.6 or 60%. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `name` | String |  | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `region` | String |  | [Output Only] URL of theregion
where the instance group resides (for autoscalers living in regional
scope). |
| `status` | String |  | [Output Only] The status of the autoscaler configuration. Current set of
possible values:
   
   - PENDING:
     Autoscaler backend hasn't read new/updated configuration.
   - DELETING:
     Configuration is being deleted.
   - ACTIVE:
     Configuration is acknowledged to be effective. Some warnings might
     be present in the statusDetails field.
   - ERROR:
     Configuration has errors. Actionable for users. Details are present in
     the statusDetails field.


New values might be added in the future. |
| `kind` | String |  | [Output Only] Type of the resource. Always compute#autoscaler
for autoscalers. |
| `scaling_schedule_status` | HashMap<String, String> |  | [Output Only] Status information of existing scaling schedules. |
| `status_details` | Vec<String> |  | [Output Only] Human-readable details about the current state of the
autoscaler. Read the documentation forCommonly
returned status messages for examples of status messages you might
encounter. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `zone` | String |  | [Output Only] URL of thezone
where the instance group resides (for autoscalers living in zonal scope). |
| `target` | String |  | URL of the managed instance group that this autoscaler will scale. This
field is required when creating an autoscaler. |
| `recommended_size` | i64 |  | [Output Only] Target recommended MIG size (number of instances) computed by
autoscaler. Autoscaler calculates the recommended MIG size even when the
autoscaling policy mode is different from ON. This field is empty when
autoscaler is not connected to an existing managed instance group or
autoscaler did not generate its prediction. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `project` | String | ✅ | Project ID for this request. |
| `zone` | String | ✅ | Name of the zone for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `autoscaling_policy` | String | The configuration parameters for the autoscaling algorithm. You can define
one or more signals for an autoscaler: cpuUtilization,customMetricUtilizations, andloadBalancingUtilization.

If none of these are specified, the default will be to autoscale based oncpuUtilization to 0.6 or 60%. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `name` | String | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `region` | String | [Output Only] URL of theregion
where the instance group resides (for autoscalers living in regional
scope). |
| `status` | String | [Output Only] The status of the autoscaler configuration. Current set of
possible values:
   
   - PENDING:
     Autoscaler backend hasn't read new/updated configuration.
   - DELETING:
     Configuration is being deleted.
   - ACTIVE:
     Configuration is acknowledged to be effective. Some warnings might
     be present in the statusDetails field.
   - ERROR:
     Configuration has errors. Actionable for users. Details are present in
     the statusDetails field.


New values might be added in the future. |
| `kind` | String | [Output Only] Type of the resource. Always compute#autoscaler
for autoscalers. |
| `scaling_schedule_status` | HashMap<String, String> | [Output Only] Status information of existing scaling schedules. |
| `status_details` | Vec<String> | [Output Only] Human-readable details about the current state of the
autoscaler. Read the documentation forCommonly
returned status messages for examples of status messages you might
encounter. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `zone` | String | [Output Only] URL of thezone
where the instance group resides (for autoscalers living in zonal scope). |
| `target` | String | URL of the managed instance group that this autoscaler will scale. This
field is required when creating an autoscaler. |
| `recommended_size` | i64 | [Output Only] Target recommended MIG size (number of instances) computed by
autoscaler. Autoscaler calculates the recommended MIG size even when the
autoscaling policy mode is different from ON. This field is empty when
autoscaler is not connected to an existing managed instance group or
autoscaler did not generate its prediction. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create autoscaler
autoscaler = provider.compute_api.Autoscaler {
    project = "value"  # Project ID for this request.
    zone = "value"  # Name of the zone for this request.
}

# Access autoscaler outputs
autoscaler_id = autoscaler.id
autoscaler_autoscaling_policy = autoscaler.autoscaling_policy
autoscaler_description = autoscaler.description
autoscaler_id = autoscaler.id
autoscaler_name = autoscaler.name
autoscaler_region = autoscaler.region
autoscaler_status = autoscaler.status
autoscaler_kind = autoscaler.kind
autoscaler_scaling_schedule_status = autoscaler.scaling_schedule_status
autoscaler_status_details = autoscaler.status_details
autoscaler_self_link = autoscaler.self_link
autoscaler_zone = autoscaler.zone
autoscaler_target = autoscaler.target
autoscaler_recommended_size = autoscaler.recommended_size
autoscaler_creation_timestamp = autoscaler.creation_timestamp
```

---


### Instance_group

Creates an instance group in the specified project using the
parameters that are included in the request.

**Operations**: ✅ Create ✅ Read ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `subnetwork` | String |  | [Output Only] The URL of the subnetwork to which all instances in the
instance group belong. If your instance has multiple network interfaces,
then the network and subnetwork fields only refer to the
network and subnet used by your primary interface (nic0). |
| `kind` | String |  | [Output Only] The resource type, which is alwayscompute#instanceGroup for instance groups. |
| `id` | String |  | [Output Only] A unique identifier for this instance group, generated
by the server. |
| `zone` | String |  | [Output Only] The URL of thezone
where the instance group is located (for zonal resources). |
| `network` | String |  | [Output Only] The URL of the network to which all instances in the
instance group belong. If your instance has multiple network interfaces,
then the network and subnetwork fields only refer to the
network and subnet used by your primary interface (nic0). |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `fingerprint` | String |  | [Output Only] The fingerprint of the named ports. The system
uses this fingerprint to detect conflicts when multiple users change the
named ports concurrently. |
| `name` | String |  | The name of the instance group. The name must be 1-63 characters
long, and comply withRFC1035. |
| `creation_timestamp` | String |  | [Output Only] The creation timestamp for this instance group inRFC3339
text format. |
| `region` | String |  | [Output Only] The URL of theregion
where the instance group is located (for regional resources). |
| `self_link` | String |  | [Output Only] The URL for this instance group. The server generates
this URL. |
| `named_ports` | Vec<String> |  | Optional. Assigns a name to a port number. For example:{name: "http", port: 80}

This
allows the system to reference ports by the assigned name instead of a
port number. Named ports can also contain multiple ports. For example:[{name: "app1", port: 8080}, {name:
"app1", port: 8081}, {name: "app2", port:
8082}]

Named ports apply to all instances in this instance group. |
| `size` | i64 |  | [Output Only] The total number of instances in the instance group. |
| `zone` | String | ✅ | The name of the zone
where you want to create the instance group. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `subnetwork` | String | [Output Only] The URL of the subnetwork to which all instances in the
instance group belong. If your instance has multiple network interfaces,
then the network and subnetwork fields only refer to the
network and subnet used by your primary interface (nic0). |
| `kind` | String | [Output Only] The resource type, which is alwayscompute#instanceGroup for instance groups. |
| `id` | String | [Output Only] A unique identifier for this instance group, generated
by the server. |
| `zone` | String | [Output Only] The URL of thezone
where the instance group is located (for zonal resources). |
| `network` | String | [Output Only] The URL of the network to which all instances in the
instance group belong. If your instance has multiple network interfaces,
then the network and subnetwork fields only refer to the
network and subnet used by your primary interface (nic0). |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `fingerprint` | String | [Output Only] The fingerprint of the named ports. The system
uses this fingerprint to detect conflicts when multiple users change the
named ports concurrently. |
| `name` | String | The name of the instance group. The name must be 1-63 characters
long, and comply withRFC1035. |
| `creation_timestamp` | String | [Output Only] The creation timestamp for this instance group inRFC3339
text format. |
| `region` | String | [Output Only] The URL of theregion
where the instance group is located (for regional resources). |
| `self_link` | String | [Output Only] The URL for this instance group. The server generates
this URL. |
| `named_ports` | Vec<String> | Optional. Assigns a name to a port number. For example:{name: "http", port: 80}

This
allows the system to reference ports by the assigned name instead of a
port number. Named ports can also contain multiple ports. For example:[{name: "app1", port: 8080}, {name:
"app1", port: 8081}, {name: "app2", port:
8082}]

Named ports apply to all instances in this instance group. |
| `size` | i64 | [Output Only] The total number of instances in the instance group. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create instance_group
instance_group = provider.compute_api.Instance_group {
    zone = "value"  # The name of the zone
where you want to create the instance group.
    project = "value"  # Project ID for this request.
}

# Access instance_group outputs
instance_group_id = instance_group.id
instance_group_subnetwork = instance_group.subnetwork
instance_group_kind = instance_group.kind
instance_group_id = instance_group.id
instance_group_zone = instance_group.zone
instance_group_network = instance_group.network
instance_group_description = instance_group.description
instance_group_fingerprint = instance_group.fingerprint
instance_group_name = instance_group.name
instance_group_creation_timestamp = instance_group.creation_timestamp
instance_group_region = instance_group.region
instance_group_self_link = instance_group.self_link
instance_group_named_ports = instance_group.named_ports
instance_group_size = instance_group.size
```

---


### Interconnect_attachment_group

Creates a InterconnectAttachmentGroup in the specified project in the given
scope using the parameters that are included in the request.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `kind` | String |  | [Output Only] Type of the resource. Always
compute#interconnectAttachmentGroup. |
| `name` | String |  | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `logical_structure` | String |  |  |
| `configured` | String |  |  |
| `intent` | String |  |  |
| `attachments` | HashMap<String, String> |  | Attachments in the AttachmentGroup. Keys are arbitrary user-specified
strings. Users are encouraged, but not required, to use their preferred
format for resource links as keys.
Note that there are add-members and remove-members methods in gcloud.
The size of this map is limited by an "Attachments per group" quota. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `etag` | String |  | Opaque system-generated token that uniquely identifies the configuration.
If provided when patching a configuration in update mode, the provided
token must match the current token or the update is rejected. This provides
a reliable means of doing read-modify-write (optimistic locking) as
described byAIP 154. |
| `id` | String |  | [Output Only] The unique identifier for the resource type. The server
generates this identifier. |
| `interconnect_group` | String |  | The URL of an InterconnectGroup that groups these Attachments'
Interconnects. Customers do not need to set this unless directed by
Google Support. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `kind` | String | [Output Only] Type of the resource. Always
compute#interconnectAttachmentGroup. |
| `name` | String | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `logical_structure` | String |  |
| `configured` | String |  |
| `intent` | String |  |
| `attachments` | HashMap<String, String> | Attachments in the AttachmentGroup. Keys are arbitrary user-specified
strings. Users are encouraged, but not required, to use their preferred
format for resource links as keys.
Note that there are add-members and remove-members methods in gcloud.
The size of this map is limited by an "Attachments per group" quota. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `etag` | String | Opaque system-generated token that uniquely identifies the configuration.
If provided when patching a configuration in update mode, the provided
token must match the current token or the update is rejected. This provides
a reliable means of doing read-modify-write (optimistic locking) as
described byAIP 154. |
| `id` | String | [Output Only] The unique identifier for the resource type. The server
generates this identifier. |
| `interconnect_group` | String | The URL of an InterconnectGroup that groups these Attachments'
Interconnects. Customers do not need to set this unless directed by
Google Support. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create interconnect_attachment_group
interconnect_attachment_group = provider.compute_api.Interconnect_attachment_group {
    project = "value"  # Project ID for this request.
}

# Access interconnect_attachment_group outputs
interconnect_attachment_group_id = interconnect_attachment_group.id
interconnect_attachment_group_self_link = interconnect_attachment_group.self_link
interconnect_attachment_group_kind = interconnect_attachment_group.kind
interconnect_attachment_group_name = interconnect_attachment_group.name
interconnect_attachment_group_logical_structure = interconnect_attachment_group.logical_structure
interconnect_attachment_group_configured = interconnect_attachment_group.configured
interconnect_attachment_group_intent = interconnect_attachment_group.intent
interconnect_attachment_group_attachments = interconnect_attachment_group.attachments
interconnect_attachment_group_description = interconnect_attachment_group.description
interconnect_attachment_group_creation_timestamp = interconnect_attachment_group.creation_timestamp
interconnect_attachment_group_etag = interconnect_attachment_group.etag
interconnect_attachment_group_id = interconnect_attachment_group.id
interconnect_attachment_group_interconnect_group = interconnect_attachment_group.interconnect_group
```

---


### Accelerator_type

Returns the specified accelerator type.

**Operations**: ✅ Read

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339 text format. |
| `name` | String | [Output Only] Name of the resource. |
| `description` | String | [Output Only] An optional textual description of the resource. |
| `maximum_cards_per_instance` | i64 | [Output Only] Maximum number of accelerator cards allowed per instance. |
| `self_link` | String | [Output Only] Server-defined, fully qualified URL for this resource. |
| `deprecated` | String | [Output Only] The deprecation status associated with this accelerator type. |
| `kind` | String | [Output Only] The type of the resource. Alwayscompute#acceleratorType for accelerator types. |
| `zone` | String | [Output Only] The name of the zone where the accelerator type resides,
such as us-central1-a.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Access accelerator_type outputs
accelerator_type_id = accelerator_type.id
accelerator_type_id = accelerator_type.id
accelerator_type_creation_timestamp = accelerator_type.creation_timestamp
accelerator_type_name = accelerator_type.name
accelerator_type_description = accelerator_type.description
accelerator_type_maximum_cards_per_instance = accelerator_type.maximum_cards_per_instance
accelerator_type_self_link = accelerator_type.self_link
accelerator_type_deprecated = accelerator_type.deprecated
accelerator_type_kind = accelerator_type.kind
accelerator_type_zone = accelerator_type.zone
```

---


### Vpn_tunnel

Creates a VpnTunnel resource in the specified project and region using
the data included in the request.

**Operations**: ✅ Create ✅ Read ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `cipher_suite` | String |  | User specified list of ciphers to use for the phase 1 and phase 2 of the
IKE protocol. |
| `region` | String |  | [Output Only] URL of the region where the VPN tunnel resides.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `router` | String |  | URL of the router resource to be used for dynamic routing. |
| `shared_secret_hash` | String |  | Hash of the shared secret. |
| `remote_traffic_selector` | Vec<String> |  | Remote traffic selectors to use when establishing the VPN tunnel with
the peer VPN gateway. The value should be a CIDR formatted string,
for example: 192.168.0.0/16. The ranges should be disjoint. Only IPv4 is
supported for Classic VPN tunnels. This field is output only for HA VPN
tunnels. |
| `ike_version` | i64 |  | IKE protocol version to use when establishing the VPN tunnel with the peer
VPN gateway. Acceptable IKE versions are 1 or 2.
The default version is 2. |
| `target_vpn_gateway` | String |  | URL of the Target VPN gateway with which this VPN tunnel is associated.
Provided by the client when the VPN tunnel is created.
This field can be set only for Classic VPN tunnels. |
| `label_fingerprint` | String |  | A fingerprint for the labels being applied to this VpnTunnel, which is
essentially a hash of the labels set used for optimistic locking. The
fingerprint is initially generated by Compute Engine and changes after
every request to modify or update labels. You must always provide an
up-to-date fingerprint hash in order to update or change labels,
otherwise the request will fail with error412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve a VpnTunnel. |
| `kind` | String |  | [Output Only] Type of resource. Always compute#vpnTunnel for
VPN tunnels. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `detailed_status` | String |  | [Output Only] Detailed status message for the VPN tunnel. |
| `peer_external_gateway` | String |  | URL of the peer side external VPN gateway to which this VPN tunnel is
connected.
Provided by the client when the VPN tunnel is created.
This field is exclusive with the field peerGcpGateway. |
| `status` | String |  | [Output Only] The status of the VPN tunnel, which can be one of the
following:
   
   - PROVISIONING: Resource is being allocated for the VPN tunnel.
   - WAITING_FOR_FULL_CONFIG: Waiting to receive all VPN-related configs
   from
     the user. Network, TargetVpnGateway, VpnTunnel, ForwardingRule, and Route
     resources are needed to setup the VPN tunnel.
   - FIRST_HANDSHAKE: Successful first handshake with the peer VPN.
   - ESTABLISHED: Secure session is successfully established with the peer
   VPN. 
   - NETWORK_ERROR: Deprecated, replaced by
   NO_INCOMING_PACKETS 
   - AUTHORIZATION_ERROR: Auth error (for example,
   bad shared secret). 
   - NEGOTIATION_FAILURE: Handshake failed.
   - DEPROVISIONING: Resources are being deallocated for the VPN
   tunnel. 
   - FAILED: Tunnel creation has failed and the tunnel is not
   ready to be used. 
   - NO_INCOMING_PACKETS: No incoming packets from
   peer. 
   - REJECTED: Tunnel configuration was rejected, can be result
   of being denied access. 
   - ALLOCATING_RESOURCES: Cloud VPN is in the
   process of allocating all required resources.
   - STOPPED: Tunnel is stopped due to its Forwarding Rules being deleted
   for Classic VPN tunnels or the project is in frozen state.
   - PEER_IDENTITY_MISMATCH: Peer identity does not match peer IP,
   probably behind NAT. 
   - TS_NARROWING_NOT_ALLOWED: Traffic selector
   narrowing not allowed for an HA-VPN tunnel. |
| `name` | String |  | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `labels` | HashMap<String, String> |  | Labels for this resource. These can only be added or modified by thesetLabels method. Each label key/value pair must comply withRFC1035.
Label values may be empty. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `peer_external_gateway_interface` | i64 |  | The interface ID of the external VPN gateway to which this VPN tunnel is
connected. Provided by the client when the VPN tunnel is created.
Possible values are: `0`, `1`, `2`, `3`. The number of IDs in use
depends on the external VPN gateway redundancy type. |
| `peer_ip` | String |  | IP address of the peer VPN gateway. Only IPv4 is supported. This field can
be set only for Classic VPN tunnels. |
| `shared_secret` | String |  | Shared secret used to set the secure session between the Cloud VPN gateway
and the peer VPN gateway. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `local_traffic_selector` | Vec<String> |  | Local traffic selector to use when establishing the VPN tunnel with the
peer VPN gateway. The value should be a CIDR formatted string, for
example: 192.168.0.0/16. The ranges must be disjoint.
Only IPv4 is supported for Classic VPN tunnels. This field is output only
for HA VPN tunnels. |
| `peer_gcp_gateway` | String |  | URL of the peer side HA VPN gateway to which this VPN tunnel
is connected. Provided by the client when the VPN tunnel is created.
This field can be used when creating highly available VPN from VPC network
to VPC network, the field is exclusive with the field peerExternalGateway.
If provided, the VPN tunnel will automatically use the same
vpnGatewayInterface ID in the peer Google Cloud VPN gateway. |
| `vpn_gateway` | String |  | URL of the VPN gateway with which this VPN tunnel is associated.
Provided by the client when the VPN tunnel is created. This must be
used (instead of target_vpn_gateway) if a High Availability VPN gateway
resource is created. |
| `vpn_gateway_interface` | i64 |  | The interface ID of the VPN gateway with which this VPN tunnel is
associated.
Possible values are: `0`, `1`. |
| `region` | String | ✅ | Name of the region for this request. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `cipher_suite` | String | User specified list of ciphers to use for the phase 1 and phase 2 of the
IKE protocol. |
| `region` | String | [Output Only] URL of the region where the VPN tunnel resides.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `router` | String | URL of the router resource to be used for dynamic routing. |
| `shared_secret_hash` | String | Hash of the shared secret. |
| `remote_traffic_selector` | Vec<String> | Remote traffic selectors to use when establishing the VPN tunnel with
the peer VPN gateway. The value should be a CIDR formatted string,
for example: 192.168.0.0/16. The ranges should be disjoint. Only IPv4 is
supported for Classic VPN tunnels. This field is output only for HA VPN
tunnels. |
| `ike_version` | i64 | IKE protocol version to use when establishing the VPN tunnel with the peer
VPN gateway. Acceptable IKE versions are 1 or 2.
The default version is 2. |
| `target_vpn_gateway` | String | URL of the Target VPN gateway with which this VPN tunnel is associated.
Provided by the client when the VPN tunnel is created.
This field can be set only for Classic VPN tunnels. |
| `label_fingerprint` | String | A fingerprint for the labels being applied to this VpnTunnel, which is
essentially a hash of the labels set used for optimistic locking. The
fingerprint is initially generated by Compute Engine and changes after
every request to modify or update labels. You must always provide an
up-to-date fingerprint hash in order to update or change labels,
otherwise the request will fail with error412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve a VpnTunnel. |
| `kind` | String | [Output Only] Type of resource. Always compute#vpnTunnel for
VPN tunnels. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `detailed_status` | String | [Output Only] Detailed status message for the VPN tunnel. |
| `peer_external_gateway` | String | URL of the peer side external VPN gateway to which this VPN tunnel is
connected.
Provided by the client when the VPN tunnel is created.
This field is exclusive with the field peerGcpGateway. |
| `status` | String | [Output Only] The status of the VPN tunnel, which can be one of the
following:
   
   - PROVISIONING: Resource is being allocated for the VPN tunnel.
   - WAITING_FOR_FULL_CONFIG: Waiting to receive all VPN-related configs
   from
     the user. Network, TargetVpnGateway, VpnTunnel, ForwardingRule, and Route
     resources are needed to setup the VPN tunnel.
   - FIRST_HANDSHAKE: Successful first handshake with the peer VPN.
   - ESTABLISHED: Secure session is successfully established with the peer
   VPN. 
   - NETWORK_ERROR: Deprecated, replaced by
   NO_INCOMING_PACKETS 
   - AUTHORIZATION_ERROR: Auth error (for example,
   bad shared secret). 
   - NEGOTIATION_FAILURE: Handshake failed.
   - DEPROVISIONING: Resources are being deallocated for the VPN
   tunnel. 
   - FAILED: Tunnel creation has failed and the tunnel is not
   ready to be used. 
   - NO_INCOMING_PACKETS: No incoming packets from
   peer. 
   - REJECTED: Tunnel configuration was rejected, can be result
   of being denied access. 
   - ALLOCATING_RESOURCES: Cloud VPN is in the
   process of allocating all required resources.
   - STOPPED: Tunnel is stopped due to its Forwarding Rules being deleted
   for Classic VPN tunnels or the project is in frozen state.
   - PEER_IDENTITY_MISMATCH: Peer identity does not match peer IP,
   probably behind NAT. 
   - TS_NARROWING_NOT_ALLOWED: Traffic selector
   narrowing not allowed for an HA-VPN tunnel. |
| `name` | String | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `labels` | HashMap<String, String> | Labels for this resource. These can only be added or modified by thesetLabels method. Each label key/value pair must comply withRFC1035.
Label values may be empty. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `peer_external_gateway_interface` | i64 | The interface ID of the external VPN gateway to which this VPN tunnel is
connected. Provided by the client when the VPN tunnel is created.
Possible values are: `0`, `1`, `2`, `3`. The number of IDs in use
depends on the external VPN gateway redundancy type. |
| `peer_ip` | String | IP address of the peer VPN gateway. Only IPv4 is supported. This field can
be set only for Classic VPN tunnels. |
| `shared_secret` | String | Shared secret used to set the secure session between the Cloud VPN gateway
and the peer VPN gateway. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `local_traffic_selector` | Vec<String> | Local traffic selector to use when establishing the VPN tunnel with the
peer VPN gateway. The value should be a CIDR formatted string, for
example: 192.168.0.0/16. The ranges must be disjoint.
Only IPv4 is supported for Classic VPN tunnels. This field is output only
for HA VPN tunnels. |
| `peer_gcp_gateway` | String | URL of the peer side HA VPN gateway to which this VPN tunnel
is connected. Provided by the client when the VPN tunnel is created.
This field can be used when creating highly available VPN from VPC network
to VPC network, the field is exclusive with the field peerExternalGateway.
If provided, the VPN tunnel will automatically use the same
vpnGatewayInterface ID in the peer Google Cloud VPN gateway. |
| `vpn_gateway` | String | URL of the VPN gateway with which this VPN tunnel is associated.
Provided by the client when the VPN tunnel is created. This must be
used (instead of target_vpn_gateway) if a High Availability VPN gateway
resource is created. |
| `vpn_gateway_interface` | i64 | The interface ID of the VPN gateway with which this VPN tunnel is
associated.
Possible values are: `0`, `1`. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create vpn_tunnel
vpn_tunnel = provider.compute_api.Vpn_tunnel {
    region = "value"  # Name of the region for this request.
    project = "value"  # Project ID for this request.
}

# Access vpn_tunnel outputs
vpn_tunnel_id = vpn_tunnel.id
vpn_tunnel_cipher_suite = vpn_tunnel.cipher_suite
vpn_tunnel_region = vpn_tunnel.region
vpn_tunnel_router = vpn_tunnel.router
vpn_tunnel_shared_secret_hash = vpn_tunnel.shared_secret_hash
vpn_tunnel_remote_traffic_selector = vpn_tunnel.remote_traffic_selector
vpn_tunnel_ike_version = vpn_tunnel.ike_version
vpn_tunnel_target_vpn_gateway = vpn_tunnel.target_vpn_gateway
vpn_tunnel_label_fingerprint = vpn_tunnel.label_fingerprint
vpn_tunnel_kind = vpn_tunnel.kind
vpn_tunnel_creation_timestamp = vpn_tunnel.creation_timestamp
vpn_tunnel_id = vpn_tunnel.id
vpn_tunnel_detailed_status = vpn_tunnel.detailed_status
vpn_tunnel_peer_external_gateway = vpn_tunnel.peer_external_gateway
vpn_tunnel_status = vpn_tunnel.status
vpn_tunnel_name = vpn_tunnel.name
vpn_tunnel_labels = vpn_tunnel.labels
vpn_tunnel_self_link = vpn_tunnel.self_link
vpn_tunnel_peer_external_gateway_interface = vpn_tunnel.peer_external_gateway_interface
vpn_tunnel_peer_ip = vpn_tunnel.peer_ip
vpn_tunnel_shared_secret = vpn_tunnel.shared_secret
vpn_tunnel_description = vpn_tunnel.description
vpn_tunnel_local_traffic_selector = vpn_tunnel.local_traffic_selector
vpn_tunnel_peer_gcp_gateway = vpn_tunnel.peer_gcp_gateway
vpn_tunnel_vpn_gateway = vpn_tunnel.vpn_gateway
vpn_tunnel_vpn_gateway_interface = vpn_tunnel.vpn_gateway_interface
```

---


### Network_edge_security_service

Creates a new service in the specified project using the data included in
the request.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `name` | String |  | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `region` | String |  | [Output Only] URL of the region where the resource resides. You must
specify this field as part of the HTTP request URL. It is not settable as a
field in the request body. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `kind` | String |  | [Output only] Type of the resource. Alwayscompute#networkEdgeSecurityService for
NetworkEdgeSecurityServices |
| `fingerprint` | String |  | Fingerprint of this resource. A hash of the contents stored in this object.
This field is used in optimistic locking. This field will be ignored when
inserting a NetworkEdgeSecurityService. An up-to-date fingerprint must be
provided in order to update the NetworkEdgeSecurityService,
otherwise the request will fail with error412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve a NetworkEdgeSecurityService. |
| `security_policy` | String |  | The resource URL for the network edge security service associated with this
network edge security service. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `self_link_with_id` | String |  | [Output Only] Server-defined URL for this resource with the resource id. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `project` | String | ✅ | Project ID for this request. |
| `region` | String | ✅ | Name of the region scoping this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `name` | String | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `region` | String | [Output Only] URL of the region where the resource resides. You must
specify this field as part of the HTTP request URL. It is not settable as a
field in the request body. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `kind` | String | [Output only] Type of the resource. Alwayscompute#networkEdgeSecurityService for
NetworkEdgeSecurityServices |
| `fingerprint` | String | Fingerprint of this resource. A hash of the contents stored in this object.
This field is used in optimistic locking. This field will be ignored when
inserting a NetworkEdgeSecurityService. An up-to-date fingerprint must be
provided in order to update the NetworkEdgeSecurityService,
otherwise the request will fail with error412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve a NetworkEdgeSecurityService. |
| `security_policy` | String | The resource URL for the network edge security service associated with this
network edge security service. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `self_link_with_id` | String | [Output Only] Server-defined URL for this resource with the resource id. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create network_edge_security_service
network_edge_security_service = provider.compute_api.Network_edge_security_service {
    project = "value"  # Project ID for this request.
    region = "value"  # Name of the region scoping this request.
}

# Access network_edge_security_service outputs
network_edge_security_service_id = network_edge_security_service.id
network_edge_security_service_name = network_edge_security_service.name
network_edge_security_service_region = network_edge_security_service.region
network_edge_security_service_creation_timestamp = network_edge_security_service.creation_timestamp
network_edge_security_service_self_link = network_edge_security_service.self_link
network_edge_security_service_kind = network_edge_security_service.kind
network_edge_security_service_fingerprint = network_edge_security_service.fingerprint
network_edge_security_service_security_policy = network_edge_security_service.security_policy
network_edge_security_service_description = network_edge_security_service.description
network_edge_security_service_self_link_with_id = network_edge_security_service.self_link_with_id
network_edge_security_service_id = network_edge_security_service.id
```

---


### Region_zone

Retrieves the list of Zone resources under the specific region available to
the specified project.

**Operations**: ✅ Read

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `next_page_token` | String | [Output Only] This token allows you to get the next page of results for
list requests. If the number of results is larger thanmaxResults, use the nextPageToken as a value for
the query parameter pageToken in the next list request.
Subsequent list requests will have their own nextPageToken to
continue paging through the results. |
| `warning` | String | [Output Only] Informational warning message. |
| `id` | String | [Output Only] Unique identifier for the resource; defined by the server. |
| `self_link` | String | [Output Only] Server-defined URL for this resource. |
| `kind` | String | Type of resource. |
| `items` | Vec<String> | A list of Zone resources. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Access region_zone outputs
region_zone_id = region_zone.id
region_zone_next_page_token = region_zone.next_page_token
region_zone_warning = region_zone.warning
region_zone_id = region_zone.id
region_zone_self_link = region_zone.self_link
region_zone_kind = region_zone.kind
region_zone_items = region_zone.items
```

---


### Reservation_block

Sets the access control policy on the specified resource.
Replaces any existing policy.

**Operations**: ✅ Create ✅ Read

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `policy` | String |  | REQUIRED: The complete policy to be applied to the 'resource'. The size of
the policy is limited to a few 10s of KB. An empty policy is in general a
valid policy but certain services (like Projects) might reject them. |
| `etag` | String |  | Flatten Policy to create a backward compatible wire-format.
Deprecated. Use 'policy' to specify the etag. |
| `bindings` | Vec<String> |  | Flatten Policy to create a backwacd compatible wire-format.
Deprecated. Use 'policy' to specify bindings. |
| `resource` | String | ✅ | Name or id of the resource for this request. |
| `zone` | String | ✅ | The name of the zone for this request. |
| `parent_resource` | String | ✅ | Name or id of parent resource of the resource for this request. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `resource` | String |  |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create reservation_block
reservation_block = provider.compute_api.Reservation_block {
    resource = "value"  # Name or id of the resource for this request.
    zone = "value"  # The name of the zone for this request.
    parent_resource = "value"  # Name or id of parent resource of the resource for this request.
    project = "value"  # Project ID for this request.
}

# Access reservation_block outputs
reservation_block_id = reservation_block.id
reservation_block_resource = reservation_block.resource
```

---


### Instance_template

Creates an instance template in the specified project using the
data that is included in the request. If you are creating a new template to
update an existing instance group, your new instance template must use the
same network or, if applicable, the same subnetwork as the original
template.

**Operations**: ✅ Create ✅ Read ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `self_link` | String |  | [Output Only] The URL for this instance template. The server defines this
URL. |
| `source_instance` | String |  | The source instance used to create the template. You can provide this as a
partial or full URL to the resource. For example, the following are valid
values:
   
   
     - https://www.googleapis.com/compute/v1/projects/project/zones/zone/instances/instance 
   - projects/project/zones/zone/instances/instance |
| `name` | String |  | Name of the resource; provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must be
a dash, lowercase letter, or digit, except the last character, which cannot
be a dash. |
| `source_instance_params` | String |  | The source instance params to use to create this instance template. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `id` | String |  | [Output Only] A unique identifier for this instance template. The server
defines this identifier. |
| `kind` | String |  | [Output Only] The resource type, which is alwayscompute#instanceTemplate for instance templates. |
| `creation_timestamp` | String |  | [Output Only] The creation timestamp for this instance template inRFC3339
text format. |
| `properties` | String |  | The instance properties for this instance template. |
| `region` | String |  | [Output Only] URL of the region where the instance template resides. Only
applicable for regional resources. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `self_link` | String | [Output Only] The URL for this instance template. The server defines this
URL. |
| `source_instance` | String | The source instance used to create the template. You can provide this as a
partial or full URL to the resource. For example, the following are valid
values:
   
   
     - https://www.googleapis.com/compute/v1/projects/project/zones/zone/instances/instance 
   - projects/project/zones/zone/instances/instance |
| `name` | String | Name of the resource; provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must be
a dash, lowercase letter, or digit, except the last character, which cannot
be a dash. |
| `source_instance_params` | String | The source instance params to use to create this instance template. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `id` | String | [Output Only] A unique identifier for this instance template. The server
defines this identifier. |
| `kind` | String | [Output Only] The resource type, which is alwayscompute#instanceTemplate for instance templates. |
| `creation_timestamp` | String | [Output Only] The creation timestamp for this instance template inRFC3339
text format. |
| `properties` | String | The instance properties for this instance template. |
| `region` | String | [Output Only] URL of the region where the instance template resides. Only
applicable for regional resources. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create instance_template
instance_template = provider.compute_api.Instance_template {
    project = "value"  # Project ID for this request.
}

# Access instance_template outputs
instance_template_id = instance_template.id
instance_template_self_link = instance_template.self_link
instance_template_source_instance = instance_template.source_instance
instance_template_name = instance_template.name
instance_template_source_instance_params = instance_template.source_instance_params
instance_template_description = instance_template.description
instance_template_id = instance_template.id
instance_template_kind = instance_template.kind
instance_template_creation_timestamp = instance_template.creation_timestamp
instance_template_properties = instance_template.properties
instance_template_region = instance_template.region
```

---


### Region_instance_group

Sets the named ports for the specified regional instance group.

**Operations**: ✅ Create ✅ Read

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `fingerprint` | String |  | The fingerprint of the named ports information for this instance group.
Use this optional property to prevent conflicts when multiple users change
the named ports settings concurrently. Obtain the fingerprint with theinstanceGroups.get
method. Then, include the fingerprint in your request to ensure that you
do not overwrite changes that were applied from another
concurrent request. |
| `named_ports` | Vec<String> |  | The list of named ports to set for this instance group. |
| `project` | String | ✅ | Project ID for this request. |
| `instance_group` | String | ✅ | The name of the regional instance group where the named ports are updated. |
| `region` | String | ✅ | Name of the region scoping this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `subnetwork` | String | [Output Only] The URL of the subnetwork to which all instances in the
instance group belong. If your instance has multiple network interfaces,
then the network and subnetwork fields only refer to the
network and subnet used by your primary interface (nic0). |
| `kind` | String | [Output Only] The resource type, which is alwayscompute#instanceGroup for instance groups. |
| `id` | String | [Output Only] A unique identifier for this instance group, generated
by the server. |
| `zone` | String | [Output Only] The URL of thezone
where the instance group is located (for zonal resources). |
| `network` | String | [Output Only] The URL of the network to which all instances in the
instance group belong. If your instance has multiple network interfaces,
then the network and subnetwork fields only refer to the
network and subnet used by your primary interface (nic0). |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `fingerprint` | String | [Output Only] The fingerprint of the named ports. The system
uses this fingerprint to detect conflicts when multiple users change the
named ports concurrently. |
| `name` | String | The name of the instance group. The name must be 1-63 characters
long, and comply withRFC1035. |
| `creation_timestamp` | String | [Output Only] The creation timestamp for this instance group inRFC3339
text format. |
| `region` | String | [Output Only] The URL of theregion
where the instance group is located (for regional resources). |
| `self_link` | String | [Output Only] The URL for this instance group. The server generates
this URL. |
| `named_ports` | Vec<String> | Optional. Assigns a name to a port number. For example:{name: "http", port: 80}

This
allows the system to reference ports by the assigned name instead of a
port number. Named ports can also contain multiple ports. For example:[{name: "app1", port: 8080}, {name:
"app1", port: 8081}, {name: "app2", port:
8082}]

Named ports apply to all instances in this instance group. |
| `size` | i64 | [Output Only] The total number of instances in the instance group. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create region_instance_group
region_instance_group = provider.compute_api.Region_instance_group {
    project = "value"  # Project ID for this request.
    instance_group = "value"  # The name of the regional instance group where the named ports are updated.
    region = "value"  # Name of the region scoping this request.
}

# Access region_instance_group outputs
region_instance_group_id = region_instance_group.id
region_instance_group_subnetwork = region_instance_group.subnetwork
region_instance_group_kind = region_instance_group.kind
region_instance_group_id = region_instance_group.id
region_instance_group_zone = region_instance_group.zone
region_instance_group_network = region_instance_group.network
region_instance_group_description = region_instance_group.description
region_instance_group_fingerprint = region_instance_group.fingerprint
region_instance_group_name = region_instance_group.name
region_instance_group_creation_timestamp = region_instance_group.creation_timestamp
region_instance_group_region = region_instance_group.region
region_instance_group_self_link = region_instance_group.self_link
region_instance_group_named_ports = region_instance_group.named_ports
region_instance_group_size = region_instance_group.size
```

---


### Target_http_proxie

Creates a TargetHttpProxy resource in the specified
project using the data included in the request.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `region` | String |  | [Output Only] URL of the region where the regional Target HTTP Proxy
resides. This field is not applicable to global Target HTTP Proxies. |
| `url_map` | String |  | URL to the UrlMap resource that defines the mapping from URL to
the BackendService. |
| `proxy_bind` | bool |  | This field only applies when the forwarding rule that references this
target proxy has a loadBalancingScheme set toINTERNAL_SELF_MANAGED.

When this field is set to true, Envoy proxies set up inbound
traffic interception and bind to the IP address and port specified in the
forwarding rule. This is generally useful when using Traffic Director to
configure Envoy as a gateway or middle proxy (in other words, not a
sidecar proxy). The Envoy proxy listens for inbound requests and handles
requests when it receives them.

The default is false. |
| `kind` | String |  | [Output Only] Type of resource. Always compute#targetHttpProxy
for target HTTP proxies. |
| `http_filters` | Vec<String> |  | URLs to networkservices.HttpFilter resources
enabled for xDS clients using this configuration. For example,https://networkservices.googleapis.com/v1alpha1/projects/project/locations/locationhttpFilters/httpFilter
Only filters that handle outbound connection and
stream events may be specified. These filters work in conjunction with
a default set of HTTP filters that may already be configured by Traffic
Director. Traffic Director will determine the final location of these
filters within xDS configuration based on the name of the HTTP filter. If
Traffic Director positions multiple filters at the same location, those
filters will be in the same order as specified in this list. 

httpFilters only applies for loadbalancers withloadBalancingScheme set to INTERNAL_SELF_MANAGED.
See ForwardingRule for more details. |
| `fingerprint` | String |  | Fingerprint of this resource. A hash of the contents stored in this object.
This field is used in optimistic locking. This field will be ignored when
inserting a TargetHttpProxy. An up-to-date fingerprint must
be provided in order to patch/update the TargetHttpProxy; otherwise, the
request will fail with error 412 conditionNotMet. To see the
latest fingerprint, make a get() request to retrieve the
TargetHttpProxy. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `http_keep_alive_timeout_sec` | i64 |  | Specifies how long to keep a connection open, after completing a response,
while there is no matching traffic (in seconds). If an HTTP keep-alive is
not specified, a default value (610 seconds) will be used.

For global external Application Load Balancers, the minimum allowed value
is 5 seconds and the maximum allowed value is 1200 seconds.

For classic Application Load Balancers, this option is not supported. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `name` | String |  | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `region` | String | [Output Only] URL of the region where the regional Target HTTP Proxy
resides. This field is not applicable to global Target HTTP Proxies. |
| `url_map` | String | URL to the UrlMap resource that defines the mapping from URL to
the BackendService. |
| `proxy_bind` | bool | This field only applies when the forwarding rule that references this
target proxy has a loadBalancingScheme set toINTERNAL_SELF_MANAGED.

When this field is set to true, Envoy proxies set up inbound
traffic interception and bind to the IP address and port specified in the
forwarding rule. This is generally useful when using Traffic Director to
configure Envoy as a gateway or middle proxy (in other words, not a
sidecar proxy). The Envoy proxy listens for inbound requests and handles
requests when it receives them.

The default is false. |
| `kind` | String | [Output Only] Type of resource. Always compute#targetHttpProxy
for target HTTP proxies. |
| `http_filters` | Vec<String> | URLs to networkservices.HttpFilter resources
enabled for xDS clients using this configuration. For example,https://networkservices.googleapis.com/v1alpha1/projects/project/locations/locationhttpFilters/httpFilter
Only filters that handle outbound connection and
stream events may be specified. These filters work in conjunction with
a default set of HTTP filters that may already be configured by Traffic
Director. Traffic Director will determine the final location of these
filters within xDS configuration based on the name of the HTTP filter. If
Traffic Director positions multiple filters at the same location, those
filters will be in the same order as specified in this list. 

httpFilters only applies for loadbalancers withloadBalancingScheme set to INTERNAL_SELF_MANAGED.
See ForwardingRule for more details. |
| `fingerprint` | String | Fingerprint of this resource. A hash of the contents stored in this object.
This field is used in optimistic locking. This field will be ignored when
inserting a TargetHttpProxy. An up-to-date fingerprint must
be provided in order to patch/update the TargetHttpProxy; otherwise, the
request will fail with error 412 conditionNotMet. To see the
latest fingerprint, make a get() request to retrieve the
TargetHttpProxy. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `http_keep_alive_timeout_sec` | i64 | Specifies how long to keep a connection open, after completing a response,
while there is no matching traffic (in seconds). If an HTTP keep-alive is
not specified, a default value (610 seconds) will be used.

For global external Application Load Balancers, the minimum allowed value
is 5 seconds and the maximum allowed value is 1200 seconds.

For classic Application Load Balancers, this option is not supported. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `name` | String | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create target_http_proxie
target_http_proxie = provider.compute_api.Target_http_proxie {
    project = "value"  # Project ID for this request.
}

# Access target_http_proxie outputs
target_http_proxie_id = target_http_proxie.id
target_http_proxie_region = target_http_proxie.region
target_http_proxie_url_map = target_http_proxie.url_map
target_http_proxie_proxy_bind = target_http_proxie.proxy_bind
target_http_proxie_kind = target_http_proxie.kind
target_http_proxie_http_filters = target_http_proxie.http_filters
target_http_proxie_fingerprint = target_http_proxie.fingerprint
target_http_proxie_self_link = target_http_proxie.self_link
target_http_proxie_description = target_http_proxie.description
target_http_proxie_creation_timestamp = target_http_proxie.creation_timestamp
target_http_proxie_http_keep_alive_timeout_sec = target_http_proxie.http_keep_alive_timeout_sec
target_http_proxie_id = target_http_proxie.id
target_http_proxie_name = target_http_proxie.name
```

---


### Instance

Creates an instance resource in the specified project using the data
included in the request.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `zone` | String |  | [Output Only] URL of the zone where the instance resides.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `resource_status` | String |  | [Output Only] Specifies values set for instance attributes as
compared to the values requested by user in the corresponding input only
field. |
| `service_accounts` | Vec<String> |  | A list of service accounts, with their specified scopes, authorized for
this instance. Only one service account per VM instance is supported.

Service accounts generate access tokens that can be accessed
through the metadata server and used to authenticate applications on the
instance. SeeService Accounts
for more information. |
| `confidential_instance_config` | String |  |  |
| `guest_accelerators` | Vec<String> |  | A list of the type and count of accelerator cards attached to the instance. |
| `network_performance_config` | String |  |  |
| `shielded_instance_config` | String |  |  |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `last_start_timestamp` | String |  | [Output Only] Last start timestamp inRFC3339 text format. |
| `params` | String |  | Input only. [Input Only] Additional params passed with the request, but not persisted
as part of resource payload. |
| `satisfies_pzs` | bool |  | [Output Only] Reserved for future use. |
| `self_link` | String |  | [Output Only] Server-defined URL for this resource. |
| `disks` | Vec<String> |  | Array of disks associated with this instance. Persistent disks must be
created before you can assign them. |
| `network_interfaces` | Vec<String> |  | An array of network configurations for this instance. These specify how
interfaces are configured to interact with other network services, such as
connecting to the internet. Multiple interfaces are supported
per instance. |
| `satisfies_pzi` | bool |  | [Output Only] Reserved for future use. |
| `start_restricted` | bool |  | [Output Only] Whether a VM has been restricted for start because Compute
Engine has detected suspicious activity. |
| `fingerprint` | String |  | Specifies a fingerprint for this resource, which is essentially a hash of
the instance's contents and used for optimistic locking. The
fingerprint is initially generated by Compute Engine and changes after
every request to modify or update the instance. You must always provide an
up-to-date fingerprint hash in order to update the instance.

To see the latest fingerprint, make get() request to the
instance. |
| `source_machine_image_encryption_key` | String |  | Source machine image encryption key when creating an instance from a
machine image. |
| `resource_policies` | Vec<String> |  | Resource policies applied to this instance. |
| `deletion_protection` | bool |  | Whether the resource should be protected against deletion. |
| `can_ip_forward` | bool |  | Allows this instance to send and receive packets with non-matching
destination or source IPs. This is required if you plan to use this
instance to forward routes. For more information, seeEnabling IP Forwarding. |
| `metadata` | String |  | The metadata key/value pairs assigned
to this instance. This includes metadata keys that were explicitly defined
for the instance. |
| `key_revocation_action_type` | String |  | KeyRevocationActionType of the instance. Supported options are "STOP" and
"NONE". The default value is "NONE" if it is not specified. |
| `advanced_machine_features` | String |  | Controls for advanced machine-related behavior features. |
| `name` | String |  | The name of the resource, provided by the client when initially creating
the resource. The resource name must be 1-63 characters long, and comply
withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must be
a dash, lowercase letter, or digit, except the last character, which cannot
be a dash. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `shielded_instance_integrity_policy` | String |  |  |
| `shielded_vm_integrity_policy` | String |  | Deprecating, please use shielded_instance_integrity_policy. |
| `instance_encryption_key` | String |  | Encrypts suspended data for an instance with acustomer-managed
encryption key.

If you are creating a new instance, this field will encrypt the local SSD
and in-memory contents of the instance during the suspend operation.

If you do not provide an encryption key when creating the instance, then
the local SSD and in-memory contents will be encrypted using
an automatically generated key during the suspend operation. |
| `last_suspended_timestamp` | String |  | [Output Only] Last suspended timestamp inRFC3339 text format. |
| `hostname` | String |  | Specifies the hostname of the instance. The specified hostname must be
RFC1035 compliant. If hostname is not specified, the default hostname is
[INSTANCE_NAME].c.[PROJECT_ID].internal when using the global DNS, and
[INSTANCE_NAME].[ZONE].c.[PROJECT_ID].internal when using zonal DNS. |
| `reservation_affinity` | String |  | Specifies the reservations that this instance can consume from. |
| `scheduling` | String |  | Sets the scheduling options for this instance. |
| `private_ipv6_google_access` | String |  | The private IPv6 google access type for the VM.
If not specified, use  INHERIT_FROM_SUBNETWORK as default. |
| `erase_windows_vss_signature` | bool |  | Specifies whether the disks restored from source snapshots or source
machine image should erase Windows specific VSS signature. |
| `kind` | String |  | [Output Only] Type of the resource. Always compute#instance
for instances. |
| `tags` | String |  | Tags to apply to this instance. Tags are used to identify valid
sources or targets for network firewalls and are specified by the client
during instance creation. The tags can be later modified by the setTags
method. Each tag within the list must comply withRFC1035.
Multiple tags can be specified via the 'tags.items' field. |
| `post_key_revocation_action_type` | String |  | PostKeyRevocationActionType of the instance. |
| `machine_type` | String |  | Full or partial URL of the machine type resource to use for this instance,
in the format:zones/zone/machineTypes/machine-type. This is provided by the client
when the instance is created. For example, the following is a valid partial
url to a predefined
machine type:

zones/us-central1-f/machineTypes/n1-standard-1


To create acustom
machine type, provide a URL to a machine type in the following format,
where CPUS is 1 or an even number up to 32 (2,
4, 6, ... 24, etc), and MEMORY is the total
memory for this instance. Memory must be a multiple of 256 MB and must
be supplied in MB (e.g. 5 GB of memory is 5120 MB):

zones/zone/machineTypes/custom-CPUS-MEMORY


For example: zones/us-central1-f/machineTypes/custom-4-5120
For a full list of restrictions, read theSpecifications
for custom machine types. |
| `last_stop_timestamp` | String |  | [Output Only] Last stop timestamp inRFC3339 text format. |
| `source_machine_image` | String |  | Source machine image |
| `status_message` | String |  | [Output Only] An optional, human-readable explanation of the status. |
| `partner_metadata` | HashMap<String, String> |  | Partner Metadata assigned to the instance. A map from a subdomain
(namespace) to entries map. |
| `shielded_vm_config` | String |  | Deprecating, please use shielded_instance_config. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339 text format. |
| `label_fingerprint` | String |  | A fingerprint for this request, which is essentially a hash of
the label's contents and used for optimistic locking. The
fingerprint is initially generated by Compute Engine and changes after
every request to modify or update labels. You must always provide an
up-to-date fingerprint hash in order to update or change labels.

To see the latest fingerprint, make get() request to the
instance. |
| `labels` | HashMap<String, String> |  | Labels to apply to this instance. These can be later modified by
the setLabels method. |
| `cpu_platform` | String |  | [Output Only] The CPU platform used by this instance. |
| `display_device` | String |  | Enables display device for the instance. |
| `min_cpu_platform` | String |  | Specifies aminimum CPU
platform for the VM instance. Applicable values are the friendly names
of CPU platforms, such as minCpuPlatform: "Intel
Haswell" or minCpuPlatform: "Intel Sandy
Bridge". |
| `status` | String |  | [Output Only] The status of the instance. One of the
following values: PROVISIONING, STAGING,RUNNING, STOPPING, SUSPENDING,SUSPENDED, REPAIRING, andTERMINATED. For more information about the status of the
instance, see 
Instance life cycle. |
| `project` | String | ✅ | Project ID for this request. |
| `zone` | String | ✅ | The name of the zone for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `zone` | String | [Output Only] URL of the zone where the instance resides.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `resource_status` | String | [Output Only] Specifies values set for instance attributes as
compared to the values requested by user in the corresponding input only
field. |
| `service_accounts` | Vec<String> | A list of service accounts, with their specified scopes, authorized for
this instance. Only one service account per VM instance is supported.

Service accounts generate access tokens that can be accessed
through the metadata server and used to authenticate applications on the
instance. SeeService Accounts
for more information. |
| `confidential_instance_config` | String |  |
| `guest_accelerators` | Vec<String> | A list of the type and count of accelerator cards attached to the instance. |
| `network_performance_config` | String |  |
| `shielded_instance_config` | String |  |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `last_start_timestamp` | String | [Output Only] Last start timestamp inRFC3339 text format. |
| `params` | String | Input only. [Input Only] Additional params passed with the request, but not persisted
as part of resource payload. |
| `satisfies_pzs` | bool | [Output Only] Reserved for future use. |
| `self_link` | String | [Output Only] Server-defined URL for this resource. |
| `disks` | Vec<String> | Array of disks associated with this instance. Persistent disks must be
created before you can assign them. |
| `network_interfaces` | Vec<String> | An array of network configurations for this instance. These specify how
interfaces are configured to interact with other network services, such as
connecting to the internet. Multiple interfaces are supported
per instance. |
| `satisfies_pzi` | bool | [Output Only] Reserved for future use. |
| `start_restricted` | bool | [Output Only] Whether a VM has been restricted for start because Compute
Engine has detected suspicious activity. |
| `fingerprint` | String | Specifies a fingerprint for this resource, which is essentially a hash of
the instance's contents and used for optimistic locking. The
fingerprint is initially generated by Compute Engine and changes after
every request to modify or update the instance. You must always provide an
up-to-date fingerprint hash in order to update the instance.

To see the latest fingerprint, make get() request to the
instance. |
| `source_machine_image_encryption_key` | String | Source machine image encryption key when creating an instance from a
machine image. |
| `resource_policies` | Vec<String> | Resource policies applied to this instance. |
| `deletion_protection` | bool | Whether the resource should be protected against deletion. |
| `can_ip_forward` | bool | Allows this instance to send and receive packets with non-matching
destination or source IPs. This is required if you plan to use this
instance to forward routes. For more information, seeEnabling IP Forwarding. |
| `metadata` | String | The metadata key/value pairs assigned
to this instance. This includes metadata keys that were explicitly defined
for the instance. |
| `key_revocation_action_type` | String | KeyRevocationActionType of the instance. Supported options are "STOP" and
"NONE". The default value is "NONE" if it is not specified. |
| `advanced_machine_features` | String | Controls for advanced machine-related behavior features. |
| `name` | String | The name of the resource, provided by the client when initially creating
the resource. The resource name must be 1-63 characters long, and comply
withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must be
a dash, lowercase letter, or digit, except the last character, which cannot
be a dash. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `shielded_instance_integrity_policy` | String |  |
| `shielded_vm_integrity_policy` | String | Deprecating, please use shielded_instance_integrity_policy. |
| `instance_encryption_key` | String | Encrypts suspended data for an instance with acustomer-managed
encryption key.

If you are creating a new instance, this field will encrypt the local SSD
and in-memory contents of the instance during the suspend operation.

If you do not provide an encryption key when creating the instance, then
the local SSD and in-memory contents will be encrypted using
an automatically generated key during the suspend operation. |
| `last_suspended_timestamp` | String | [Output Only] Last suspended timestamp inRFC3339 text format. |
| `hostname` | String | Specifies the hostname of the instance. The specified hostname must be
RFC1035 compliant. If hostname is not specified, the default hostname is
[INSTANCE_NAME].c.[PROJECT_ID].internal when using the global DNS, and
[INSTANCE_NAME].[ZONE].c.[PROJECT_ID].internal when using zonal DNS. |
| `reservation_affinity` | String | Specifies the reservations that this instance can consume from. |
| `scheduling` | String | Sets the scheduling options for this instance. |
| `private_ipv6_google_access` | String | The private IPv6 google access type for the VM.
If not specified, use  INHERIT_FROM_SUBNETWORK as default. |
| `erase_windows_vss_signature` | bool | Specifies whether the disks restored from source snapshots or source
machine image should erase Windows specific VSS signature. |
| `kind` | String | [Output Only] Type of the resource. Always compute#instance
for instances. |
| `tags` | String | Tags to apply to this instance. Tags are used to identify valid
sources or targets for network firewalls and are specified by the client
during instance creation. The tags can be later modified by the setTags
method. Each tag within the list must comply withRFC1035.
Multiple tags can be specified via the 'tags.items' field. |
| `post_key_revocation_action_type` | String | PostKeyRevocationActionType of the instance. |
| `machine_type` | String | Full or partial URL of the machine type resource to use for this instance,
in the format:zones/zone/machineTypes/machine-type. This is provided by the client
when the instance is created. For example, the following is a valid partial
url to a predefined
machine type:

zones/us-central1-f/machineTypes/n1-standard-1


To create acustom
machine type, provide a URL to a machine type in the following format,
where CPUS is 1 or an even number up to 32 (2,
4, 6, ... 24, etc), and MEMORY is the total
memory for this instance. Memory must be a multiple of 256 MB and must
be supplied in MB (e.g. 5 GB of memory is 5120 MB):

zones/zone/machineTypes/custom-CPUS-MEMORY


For example: zones/us-central1-f/machineTypes/custom-4-5120
For a full list of restrictions, read theSpecifications
for custom machine types. |
| `last_stop_timestamp` | String | [Output Only] Last stop timestamp inRFC3339 text format. |
| `source_machine_image` | String | Source machine image |
| `status_message` | String | [Output Only] An optional, human-readable explanation of the status. |
| `partner_metadata` | HashMap<String, String> | Partner Metadata assigned to the instance. A map from a subdomain
(namespace) to entries map. |
| `shielded_vm_config` | String | Deprecating, please use shielded_instance_config. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339 text format. |
| `label_fingerprint` | String | A fingerprint for this request, which is essentially a hash of
the label's contents and used for optimistic locking. The
fingerprint is initially generated by Compute Engine and changes after
every request to modify or update labels. You must always provide an
up-to-date fingerprint hash in order to update or change labels.

To see the latest fingerprint, make get() request to the
instance. |
| `labels` | HashMap<String, String> | Labels to apply to this instance. These can be later modified by
the setLabels method. |
| `cpu_platform` | String | [Output Only] The CPU platform used by this instance. |
| `display_device` | String | Enables display device for the instance. |
| `min_cpu_platform` | String | Specifies aminimum CPU
platform for the VM instance. Applicable values are the friendly names
of CPU platforms, such as minCpuPlatform: "Intel
Haswell" or minCpuPlatform: "Intel Sandy
Bridge". |
| `status` | String | [Output Only] The status of the instance. One of the
following values: PROVISIONING, STAGING,RUNNING, STOPPING, SUSPENDING,SUSPENDED, REPAIRING, andTERMINATED. For more information about the status of the
instance, see 
Instance life cycle. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create instance
instance = provider.compute_api.Instance {
    project = "value"  # Project ID for this request.
    zone = "value"  # The name of the zone for this request.
}

# Access instance outputs
instance_id = instance.id
instance_zone = instance.zone
instance_resource_status = instance.resource_status
instance_service_accounts = instance.service_accounts
instance_confidential_instance_config = instance.confidential_instance_config
instance_guest_accelerators = instance.guest_accelerators
instance_network_performance_config = instance.network_performance_config
instance_shielded_instance_config = instance.shielded_instance_config
instance_description = instance.description
instance_last_start_timestamp = instance.last_start_timestamp
instance_params = instance.params
instance_satisfies_pzs = instance.satisfies_pzs
instance_self_link = instance.self_link
instance_disks = instance.disks
instance_network_interfaces = instance.network_interfaces
instance_satisfies_pzi = instance.satisfies_pzi
instance_start_restricted = instance.start_restricted
instance_fingerprint = instance.fingerprint
instance_source_machine_image_encryption_key = instance.source_machine_image_encryption_key
instance_resource_policies = instance.resource_policies
instance_deletion_protection = instance.deletion_protection
instance_can_ip_forward = instance.can_ip_forward
instance_metadata = instance.metadata
instance_key_revocation_action_type = instance.key_revocation_action_type
instance_advanced_machine_features = instance.advanced_machine_features
instance_name = instance.name
instance_id = instance.id
instance_shielded_instance_integrity_policy = instance.shielded_instance_integrity_policy
instance_shielded_vm_integrity_policy = instance.shielded_vm_integrity_policy
instance_instance_encryption_key = instance.instance_encryption_key
instance_last_suspended_timestamp = instance.last_suspended_timestamp
instance_hostname = instance.hostname
instance_reservation_affinity = instance.reservation_affinity
instance_scheduling = instance.scheduling
instance_private_ipv6_google_access = instance.private_ipv6_google_access
instance_erase_windows_vss_signature = instance.erase_windows_vss_signature
instance_kind = instance.kind
instance_tags = instance.tags
instance_post_key_revocation_action_type = instance.post_key_revocation_action_type
instance_machine_type = instance.machine_type
instance_last_stop_timestamp = instance.last_stop_timestamp
instance_source_machine_image = instance.source_machine_image
instance_status_message = instance.status_message
instance_partner_metadata = instance.partner_metadata
instance_shielded_vm_config = instance.shielded_vm_config
instance_creation_timestamp = instance.creation_timestamp
instance_label_fingerprint = instance.label_fingerprint
instance_labels = instance.labels
instance_cpu_platform = instance.cpu_platform
instance_display_device = instance.display_device
instance_min_cpu_platform = instance.min_cpu_platform
instance_status = instance.status
```

---


### Service_attachment

Creates a ServiceAttachment in the specified project in the given scope
using the parameters that are included in the request.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `fingerprint` | String |  | Fingerprint of this resource. A hash of the contents stored in this object.
This field is used in optimistic locking. This field will be ignored when
inserting a ServiceAttachment. An up-to-date fingerprint must
be provided in order to patch/update the ServiceAttachment; otherwise, the
request will fail with error 412 conditionNotMet. To see the
latest fingerprint, make a get() request to retrieve the
ServiceAttachment. |
| `nat_subnets` | Vec<String> |  | An array of URLs where each entry is the URL of a subnet provided
by the service producer to use for NAT in this service attachment. |
| `connection_preference` | String |  | The connection preference of service attachment. The value can be set
to ACCEPT_AUTOMATIC. An ACCEPT_AUTOMATIC
service attachment is one that always accepts the connection from consumer
forwarding rules. |
| `producer_forwarding_rule` | String |  | The URL of a forwarding rule with loadBalancingScheme INTERNAL* that is
serving the endpoint identified by this service attachment. |
| `id` | String |  | [Output Only] The unique identifier for the resource type. The server
generates this identifier. |
| `target_service` | String |  | The URL of a service serving the endpoint identified by this service
attachment. |
| `tunneling_config` | String |  | When a tunneling config is set on this service attachment it will
encapsulate traffic between consumer and producer.

When tunneling is enabled:
   
   
    -  nat_subnets must be unset 
    -  enable_proxy_protocol must be false 
    -  producer_forwarding_rule must be a L4 ILB. 
   - |
| `psc_service_attachment_id` | String |  | [Output Only] An 128-bit global unique ID of the PSC service attachment. |
| `enable_proxy_protocol` | bool |  | If true, enable the proxy protocol which is for supplying client TCP/IP
address data in TCP connections that traverse proxies on their way to
destination servers. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `region` | String |  | [Output Only] URL of the region where the service attachment resides.
This field applies only to the region resource. You must specify this
field as part of the HTTP request URL. It is not settable as a field in
the request body. |
| `metadata` | HashMap<String, String> |  | Metadata of the service attachment. |
| `name` | String |  | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `kind` | String |  | [Output Only] Type of the resource. Alwayscompute#serviceAttachment for service attachments. |
| `consumer_accept_lists` | Vec<String> |  | Specifies which consumer projects or networks are allowed to connect to the
service attachment. Each project or network has a connection limit. A given
service attachment can manage connections at either the project or network
level. Therefore, both the accept and reject lists for a given service
attachment must contain either only projects or only networks or only
endpoints. |
| `domain_names` | Vec<String> |  | If specified, the domain name will be used during the integration between
the PSC connected endpoints and the Cloud DNS. For example, this is a valid
domain name: "p.mycompany.com.". Current max number of domain names
supported is 1. |
| `connected_endpoints` | Vec<String> |  | [Output Only] An array of connections for all the consumers connected to
this service attachment. |
| `consumer_reject_lists` | Vec<String> |  | Specifies a list of projects or networks that are not allowed to connect to
this service attachment. The project can be specified using its project ID
or project number and the network can be specified using its URL. A given
service attachment can manage connections at either the project or network
level. Therefore, both the reject and accept lists for a given service
attachment must contain either only projects or only networks. |
| `propagated_connection_limit` | i64 |  | The number of consumer spokes that connected Private Service Connect
endpoints can be propagated to through Network Connectivity Center. This
limit lets the service producer limit how many propagated Private Service
Connect connections can be established to this service attachment from a
single consumer.

If the connection preference of the service attachment is ACCEPT_MANUAL,
the limit applies to each project or network that is listed in the consumer
accept list. If the connection preference of the service attachment is
ACCEPT_AUTOMATIC, the limit applies to each project that contains a
connected endpoint.

If unspecified, the default propagated connection limit is 250. |
| `reconcile_connections` | bool |  | This flag determines whether a consumer accept/reject list change can
reconcile the statuses of existing ACCEPTED or REJECTED PSC endpoints.
   
   
    -  If false, connection policy update will only affect existing PENDING
    PSC endpoints. Existing ACCEPTED/REJECTED endpoints will remain untouched
    regardless how the connection policy is modified . 
   -  If true,
    update will affect both PENDING and ACCEPTED/REJECTED PSC endpoints. For
    example, an ACCEPTED PSC endpoint will be moved to REJECTED if its project
    is added to the reject list.


For newly created service attachment, this boolean defaults to false. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `project` | String | ✅ | Project ID for this request. |
| `region` | String | ✅ | Name of the region of this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `fingerprint` | String | Fingerprint of this resource. A hash of the contents stored in this object.
This field is used in optimistic locking. This field will be ignored when
inserting a ServiceAttachment. An up-to-date fingerprint must
be provided in order to patch/update the ServiceAttachment; otherwise, the
request will fail with error 412 conditionNotMet. To see the
latest fingerprint, make a get() request to retrieve the
ServiceAttachment. |
| `nat_subnets` | Vec<String> | An array of URLs where each entry is the URL of a subnet provided
by the service producer to use for NAT in this service attachment. |
| `connection_preference` | String | The connection preference of service attachment. The value can be set
to ACCEPT_AUTOMATIC. An ACCEPT_AUTOMATIC
service attachment is one that always accepts the connection from consumer
forwarding rules. |
| `producer_forwarding_rule` | String | The URL of a forwarding rule with loadBalancingScheme INTERNAL* that is
serving the endpoint identified by this service attachment. |
| `id` | String | [Output Only] The unique identifier for the resource type. The server
generates this identifier. |
| `target_service` | String | The URL of a service serving the endpoint identified by this service
attachment. |
| `tunneling_config` | String | When a tunneling config is set on this service attachment it will
encapsulate traffic between consumer and producer.

When tunneling is enabled:
   
   
    -  nat_subnets must be unset 
    -  enable_proxy_protocol must be false 
    -  producer_forwarding_rule must be a L4 ILB. 
   - |
| `psc_service_attachment_id` | String | [Output Only] An 128-bit global unique ID of the PSC service attachment. |
| `enable_proxy_protocol` | bool | If true, enable the proxy protocol which is for supplying client TCP/IP
address data in TCP connections that traverse proxies on their way to
destination servers. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `region` | String | [Output Only] URL of the region where the service attachment resides.
This field applies only to the region resource. You must specify this
field as part of the HTTP request URL. It is not settable as a field in
the request body. |
| `metadata` | HashMap<String, String> | Metadata of the service attachment. |
| `name` | String | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `kind` | String | [Output Only] Type of the resource. Alwayscompute#serviceAttachment for service attachments. |
| `consumer_accept_lists` | Vec<String> | Specifies which consumer projects or networks are allowed to connect to the
service attachment. Each project or network has a connection limit. A given
service attachment can manage connections at either the project or network
level. Therefore, both the accept and reject lists for a given service
attachment must contain either only projects or only networks or only
endpoints. |
| `domain_names` | Vec<String> | If specified, the domain name will be used during the integration between
the PSC connected endpoints and the Cloud DNS. For example, this is a valid
domain name: "p.mycompany.com.". Current max number of domain names
supported is 1. |
| `connected_endpoints` | Vec<String> | [Output Only] An array of connections for all the consumers connected to
this service attachment. |
| `consumer_reject_lists` | Vec<String> | Specifies a list of projects or networks that are not allowed to connect to
this service attachment. The project can be specified using its project ID
or project number and the network can be specified using its URL. A given
service attachment can manage connections at either the project or network
level. Therefore, both the reject and accept lists for a given service
attachment must contain either only projects or only networks. |
| `propagated_connection_limit` | i64 | The number of consumer spokes that connected Private Service Connect
endpoints can be propagated to through Network Connectivity Center. This
limit lets the service producer limit how many propagated Private Service
Connect connections can be established to this service attachment from a
single consumer.

If the connection preference of the service attachment is ACCEPT_MANUAL,
the limit applies to each project or network that is listed in the consumer
accept list. If the connection preference of the service attachment is
ACCEPT_AUTOMATIC, the limit applies to each project that contains a
connected endpoint.

If unspecified, the default propagated connection limit is 250. |
| `reconcile_connections` | bool | This flag determines whether a consumer accept/reject list change can
reconcile the statuses of existing ACCEPTED or REJECTED PSC endpoints.
   
   
    -  If false, connection policy update will only affect existing PENDING
    PSC endpoints. Existing ACCEPTED/REJECTED endpoints will remain untouched
    regardless how the connection policy is modified . 
   -  If true,
    update will affect both PENDING and ACCEPTED/REJECTED PSC endpoints. For
    example, an ACCEPTED PSC endpoint will be moved to REJECTED if its project
    is added to the reject list.


For newly created service attachment, this boolean defaults to false. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create service_attachment
service_attachment = provider.compute_api.Service_attachment {
    project = "value"  # Project ID for this request.
    region = "value"  # Name of the region of this request.
}

# Access service_attachment outputs
service_attachment_id = service_attachment.id
service_attachment_fingerprint = service_attachment.fingerprint
service_attachment_nat_subnets = service_attachment.nat_subnets
service_attachment_connection_preference = service_attachment.connection_preference
service_attachment_producer_forwarding_rule = service_attachment.producer_forwarding_rule
service_attachment_id = service_attachment.id
service_attachment_target_service = service_attachment.target_service
service_attachment_tunneling_config = service_attachment.tunneling_config
service_attachment_psc_service_attachment_id = service_attachment.psc_service_attachment_id
service_attachment_enable_proxy_protocol = service_attachment.enable_proxy_protocol
service_attachment_description = service_attachment.description
service_attachment_region = service_attachment.region
service_attachment_metadata = service_attachment.metadata
service_attachment_name = service_attachment.name
service_attachment_kind = service_attachment.kind
service_attachment_consumer_accept_lists = service_attachment.consumer_accept_lists
service_attachment_domain_names = service_attachment.domain_names
service_attachment_connected_endpoints = service_attachment.connected_endpoints
service_attachment_consumer_reject_lists = service_attachment.consumer_reject_lists
service_attachment_propagated_connection_limit = service_attachment.propagated_connection_limit
service_attachment_reconcile_connections = service_attachment.reconcile_connections
service_attachment_creation_timestamp = service_attachment.creation_timestamp
service_attachment_self_link = service_attachment.self_link
```

---


### Preview_feature

Returns the details of the given PreviewFeature.

**Operations**: ✅ Read ✅ Update

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `description` | String |  | [Output Only] Description of the feature. |
| `status` | String |  | [Output only] Status of the feature. |
| `activation_status` | String |  | Specifies whether the feature is enabled or disabled. |
| `kind` | String |  | [Output only] The type of the feature. Always "compute#previewFeature" for
preview features. |
| `name` | String |  | Name of the feature. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `rollout_operation` | String |  | Rollout operation of the feature. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `project` | String | ✅ | Project ID for this request. |
| `preview_feature` | String | ✅ | Name of the PreviewFeature for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `description` | String | [Output Only] Description of the feature. |
| `status` | String | [Output only] Status of the feature. |
| `activation_status` | String | Specifies whether the feature is enabled or disabled. |
| `kind` | String | [Output only] The type of the feature. Always "compute#previewFeature" for
preview features. |
| `name` | String | Name of the feature. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `rollout_operation` | String | Rollout operation of the feature. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Access preview_feature outputs
preview_feature_id = preview_feature.id
preview_feature_self_link = preview_feature.self_link
preview_feature_description = preview_feature.description
preview_feature_status = preview_feature.status
preview_feature_activation_status = preview_feature.activation_status
preview_feature_kind = preview_feature.kind
preview_feature_name = preview_feature.name
preview_feature_id = preview_feature.id
preview_feature_rollout_operation = preview_feature.rollout_operation
preview_feature_creation_timestamp = preview_feature.creation_timestamp
```

---


### Region_disk

Creates a persistent regional disk in the specified project using the data
included in the request.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `last_detach_timestamp` | String |  | [Output Only] Last detach timestamp inRFC3339
text format. |
| `source_image` | String |  | The source image used to create this disk. If the source image is
deleted, this field will not be set.

To create a disk with one of the public operating system images, specify
the image by its family name. For example, specifyfamily/debian-9 to use the latest Debian 9 image:

projects/debian-cloud/global/images/family/debian-9


Alternatively, use a specific version of a public operating system image:

projects/debian-cloud/global/images/debian-9-stretch-vYYYYMMDD


To create a disk with a custom image that you created, specify the
image name in the following format:

global/images/my-custom-image


You can also specify a custom image by its image family, which returns
the latest version of the image in that family. Replace the image name
with family/family-name:

global/images/family/my-image-family |
| `storage_type` | String |  | [Deprecated] Storage type of the persistent disk. |
| `erase_windows_vss_signature` | bool |  | Specifies whether the disk restored from a source snapshot should erase
Windows specific VSS signature. |
| `type` | String |  | URL of the disk type resource describing which disk type to use to create
the disk. Provide this when creating the disk. For example:projects/project/zones/zone/diskTypes/pd-ssd. See Persistent disk
types. |
| `user_licenses` | Vec<String> |  | A list of publicly visible user-licenses. Unlike regular licenses, user
provided licenses can be modified after the disk is created. This includes
a list of URLs to the license resource. For example, to provide a debian
license:

https://www.googleapis.com/compute/v1/projects/debian-cloud/global/licenses/debian-9-stretch |
| `zone` | String |  | [Output Only] URL of the zone where the disk resides.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `async_secondary_disks` | HashMap<String, String> |  | [Output Only] A list of disks this disk is asynchronously replicated to. |
| `params` | String |  | Input only. [Input Only] Additional params passed with the request, but not persisted
as part of resource payload. |
| `source_snapshot_id` | String |  | [Output Only] The unique ID of the snapshot used to create this disk. This
value identifies the exact snapshot that was used to create this persistent
disk. For example, if you created the persistent disk from a snapshot that
was later deleted and recreated under the same name, the source snapshot ID
would identify the exact version of the snapshot that was used. |
| `interface` | String |  | [Deprecated] Specifies the disk interface to use for attaching this disk,
which is either SCSI or NVME. The default isSCSI. |
| `provisioned_throughput` | String |  | Indicates how much throughput to provision for the disk. This sets the
number of throughput mb per second that the disk can handle. Values must be
greater than or equal to 1. |
| `guest_os_features` | Vec<String> |  | A list of features to enable on the guest operating system. Applicable
only for bootable images. Read
Enabling guest operating system features to see a list of available
options. |
| `last_attach_timestamp` | String |  | [Output Only] Last attach timestamp inRFC3339
text format. |
| `self_link` | String |  | [Output Only] Server-defined fully-qualified URL for this resource. |
| `source_image_encryption_key` | String |  | Thecustomer-supplied
encryption key of the source image. Required if the source image is
protected by a customer-supplied encryption key. |
| `source_instant_snapshot` | String |  | The source instant snapshot used to create this disk. You can provide
this as a partial or full URL to the resource. For example, the following
are valid values:
   
   
     - https://www.googleapis.com/compute/v1/projects/project/zones/zone/instantSnapshots/instantSnapshot
     - projects/project/zones/zone/instantSnapshots/instantSnapshot 
   - zones/zone/instantSnapshots/instantSnapshot |
| `status` | String |  | [Output Only] The status of disk creation.
   
   
     - CREATING: Disk is provisioning.
     - RESTORING: Source data is being copied into the
     disk.
     - FAILED: Disk creation failed.
     - READY: Disk is ready for use.
     - DELETING: Disk is deleting. |
| `multi_writer` | bool |  | Indicates whether or not the disk can be read/write attached to
more than one instance. |
| `source_consistency_group_policy_id` | String |  | [Output Only] ID of the DiskConsistencyGroupPolicy for a secondary disk
that was created using a consistency group. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `licenses` | Vec<String> |  | A list of publicly visible licenses. Reserved for Google's use. |
| `options` | String |  | Internal use only. |
| `provisioned_iops` | String |  | Indicates how many IOPS to provision for the disk. This sets the number
of I/O operations per second that the disk can handle. Values must be
between 10,000 and 120,000. For more details, see theExtreme persistent
disk documentation. |
| `resource_status` | String |  | [Output Only] Status information for the disk resource. |
| `physical_block_size_bytes` | String |  | Physical block size of the persistent disk, in bytes.
If not present in a request, a default value is used.
The currently supported size is 4096, other sizes may be added in
the future.
If an unsupported value is requested, the error message will list
the supported values for the caller's project. |
| `region` | String |  | [Output Only] URL of the region where the disk resides. Only applicable for
regional resources.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `satisfies_pzi` | bool |  | Output only. Reserved for future use. |
| `source_consistency_group_policy` | String |  | [Output Only] URL of the DiskConsistencyGroupPolicy for a secondary disk
that was created using a consistency group. |
| `source_disk` | String |  | The source disk used to create this disk. You can provide this as a
partial or full URL to the resource. For example, the following are valid
values:
   
   
     - 
       https://www.googleapis.com/compute/v1/projects/project/zones/zone/disks/disk
     
     - 
       https://www.googleapis.com/compute/v1/projects/project/regions/region/disks/disk
     
     - 
       projects/project/zones/zone/disks/disk
     
     - 
       projects/project/regions/region/disks/disk
     
     - 
       zones/zone/disks/disk
     
     - 
       regions/region/disks/disk |
| `source_snapshot` | String |  | The source snapshot used to create this disk. You can provide this as a
partial or full URL to the resource. For example, the following are valid
values:
   
   
     - https://www.googleapis.com/compute/v1/projects/project/global/snapshots/snapshot 
   - projects/project/global/snapshots/snapshot
     - global/snapshots/snapshot |
| `name` | String |  | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?`
which means the first character must be a lowercase letter, and all
following characters must be a dash, lowercase letter, or digit, except
the last character, which cannot be a dash. |
| `source_snapshot_encryption_key` | String |  | Thecustomer-supplied
encryption key of the source snapshot. Required if the source snapshot
is protected by a customer-supplied encryption key. |
| `users` | Vec<String> |  | [Output Only] Links to the users of the disk (attached instances)
in form:projects/project/zones/zone/instances/instance |
| `satisfies_pzs` | bool |  | [Output Only] Reserved for future use. |
| `license_codes` | Vec<String> |  | Integer license codes indicating which licenses are attached to this disk. |
| `label_fingerprint` | String |  | A fingerprint for the labels being applied to this disk, which is
essentially a hash of the labels set used for optimistic locking. The
fingerprint is initially generated by Compute Engine and changes after
every request to modify or update labels. You must always provide an
up-to-date fingerprint hash in order to update or change labels,
otherwise the request will fail with error412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve a disk. |
| `enable_confidential_compute` | bool |  | Whether this disk is using confidential compute mode. |
| `resource_policies` | Vec<String> |  | Resource policies applied to this disk for automatic snapshot creations. |
| `disk_encryption_key` | String |  | Encrypts the disk using a
customer-supplied encryption key or a 
customer-managed encryption key.

Encryption keys do not protect access to metadata of the disk.

After you encrypt a disk with a customer-supplied key, you must provide the
same key if you use the disk later. For example, to create a disk snapshot,
to create a disk image, to create a machine image, or to attach the disk to
a virtual machine.

After you encrypt a disk with a customer-managed key, thediskEncryptionKey.kmsKeyName is set to a key *version*
name once the disk is created. The disk is encrypted with this version of
the key. In the response, diskEncryptionKey.kmsKeyName appears
in the following format:

"diskEncryptionKey.kmsKeyName":
"projects/kms_project_id/locations/region/keyRings/
key_region/cryptoKeys/key
/cryptoKeysVersions/version

If you do not provide an encryption key when creating the disk, then the 
disk is encrypted using an automatically generated key and you don't need 
to provide a key to use the disk later. |
| `size_gb` | String |  | Size, in GB, of the persistent disk. You can specify
this field when creating a persistent disk using thesourceImage, sourceSnapshot, orsourceDisk parameter, or specify it alone to create an empty
persistent disk.

If you specify this field along with a source, the value ofsizeGb must not be less than the size of the
source.
Acceptable values are greater than 0. |
| `source_disk_id` | String |  | [Output Only] The unique ID of the disk used to create this disk. This
value identifies the exact disk that was used to create this persistent
disk. For example, if you created the persistent disk from a disk that
was later deleted and recreated under the same name, the source disk ID
would identify the exact version of the disk that was used. |
| `access_mode` | String |  | The access mode of the disk.
   
   
     - READ_WRITE_SINGLE: The default AccessMode, means the
     disk can be attached to single instance in RW mode.
     - READ_WRITE_MANY: The AccessMode means the disk can be
     attached to multiple instances in RW mode.
     - READ_ONLY_MANY: The AccessMode means the disk can be
     attached to multiple instances in RO mode.


The AccessMode is only valid for Hyperdisk disk types. |
| `storage_pool` | String |  | The storage pool in which the new disk is created. You can provide
this as a partial or full URL to the resource. For example, the following
are valid values:
   
   
     - https://www.googleapis.com/compute/v1/projects/project/zones/zone/storagePools/storagePool
     - projects/project/zones/zone/storagePools/storagePool 
   - zones/zone/storagePools/storagePool |
| `architecture` | String |  | The architecture of the disk. Valid values are
ARM64 or X86_64. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `location_hint` | String |  | An opaque location hint used to place the disk close to other resources.
This field is for use by internal tools that use the public API. |
| `source_image_id` | String |  | [Output Only] The ID value of the image used to create this disk. This
value identifies the exact image that was used to create this persistent
disk. For example, if you created the persistent disk from an image that
was later deleted and recreated under the same name, the source image ID
would identify the exact version of the image that was used. |
| `source_storage_object` | String |  | The full Google Cloud Storage URI where the disk image is stored. This file
must be a gzip-compressed tarball whose name ends in .tar.gz or virtual
machine disk whose name ends in vmdk. Valid URIs may start with gs:// or
https://storage.googleapis.com/. This flag is not optimized for creating
multiple disks from a source storage object. To create many disks from a
source storage object, use gcloud compute images
import instead. |
| `async_primary_disk` | String |  | Disk asynchronously replicated into this disk. |
| `replica_zones` | Vec<String> |  | URLs of the zones where the disk should be replicated to. Only applicable
for regional resources. |
| `kind` | String |  | [Output Only] Type of the resource. Always compute#disk for
disks. |
| `source_instant_snapshot_id` | String |  | [Output Only] The unique ID of the instant snapshot used to create this
disk. This value identifies the exact instant snapshot that was used to
create this persistent disk. For example, if you created the persistent
disk from an instant snapshot that was later deleted and recreated under
the same name, the source instant snapshot ID would identify the exact
version of the instant snapshot that was used. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `labels` | HashMap<String, String> |  | Labels to apply to this disk. These can be later modified by
the setLabels method. |
| `locked` | bool |  | [Output Only] The field indicates if the disk is created from a locked
source image. Attachment of a disk created from a locked source image will
cause the following operations to become irreversibly prohibited:
   
   - R/W or R/O disk attachment to any other instance
   - Disk detachment. And the disk can only be deleted when the instance
   is deleted
   - Creation of images or snapshots
   - Disk cloning



Furthermore, the instance with at least one disk with locked flag set to
true will be prohibited from performing the operations below:
   
   - Further attachment of secondary disks.
   - Detachment of any disks
   - Create machine images
   - Create instance template
   - Delete the instance with --keep-disk parameter set to true for locked
   disks
   - Attach a locked disk with --auto-delete parameter set to false |
| `region` | String | ✅ | Name of the region for this request. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `last_detach_timestamp` | String | [Output Only] Last detach timestamp inRFC3339
text format. |
| `source_image` | String | The source image used to create this disk. If the source image is
deleted, this field will not be set.

To create a disk with one of the public operating system images, specify
the image by its family name. For example, specifyfamily/debian-9 to use the latest Debian 9 image:

projects/debian-cloud/global/images/family/debian-9


Alternatively, use a specific version of a public operating system image:

projects/debian-cloud/global/images/debian-9-stretch-vYYYYMMDD


To create a disk with a custom image that you created, specify the
image name in the following format:

global/images/my-custom-image


You can also specify a custom image by its image family, which returns
the latest version of the image in that family. Replace the image name
with family/family-name:

global/images/family/my-image-family |
| `storage_type` | String | [Deprecated] Storage type of the persistent disk. |
| `erase_windows_vss_signature` | bool | Specifies whether the disk restored from a source snapshot should erase
Windows specific VSS signature. |
| `type` | String | URL of the disk type resource describing which disk type to use to create
the disk. Provide this when creating the disk. For example:projects/project/zones/zone/diskTypes/pd-ssd. See Persistent disk
types. |
| `user_licenses` | Vec<String> | A list of publicly visible user-licenses. Unlike regular licenses, user
provided licenses can be modified after the disk is created. This includes
a list of URLs to the license resource. For example, to provide a debian
license:

https://www.googleapis.com/compute/v1/projects/debian-cloud/global/licenses/debian-9-stretch |
| `zone` | String | [Output Only] URL of the zone where the disk resides.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `async_secondary_disks` | HashMap<String, String> | [Output Only] A list of disks this disk is asynchronously replicated to. |
| `params` | String | Input only. [Input Only] Additional params passed with the request, but not persisted
as part of resource payload. |
| `source_snapshot_id` | String | [Output Only] The unique ID of the snapshot used to create this disk. This
value identifies the exact snapshot that was used to create this persistent
disk. For example, if you created the persistent disk from a snapshot that
was later deleted and recreated under the same name, the source snapshot ID
would identify the exact version of the snapshot that was used. |
| `interface` | String | [Deprecated] Specifies the disk interface to use for attaching this disk,
which is either SCSI or NVME. The default isSCSI. |
| `provisioned_throughput` | String | Indicates how much throughput to provision for the disk. This sets the
number of throughput mb per second that the disk can handle. Values must be
greater than or equal to 1. |
| `guest_os_features` | Vec<String> | A list of features to enable on the guest operating system. Applicable
only for bootable images. Read
Enabling guest operating system features to see a list of available
options. |
| `last_attach_timestamp` | String | [Output Only] Last attach timestamp inRFC3339
text format. |
| `self_link` | String | [Output Only] Server-defined fully-qualified URL for this resource. |
| `source_image_encryption_key` | String | Thecustomer-supplied
encryption key of the source image. Required if the source image is
protected by a customer-supplied encryption key. |
| `source_instant_snapshot` | String | The source instant snapshot used to create this disk. You can provide
this as a partial or full URL to the resource. For example, the following
are valid values:
   
   
     - https://www.googleapis.com/compute/v1/projects/project/zones/zone/instantSnapshots/instantSnapshot
     - projects/project/zones/zone/instantSnapshots/instantSnapshot 
   - zones/zone/instantSnapshots/instantSnapshot |
| `status` | String | [Output Only] The status of disk creation.
   
   
     - CREATING: Disk is provisioning.
     - RESTORING: Source data is being copied into the
     disk.
     - FAILED: Disk creation failed.
     - READY: Disk is ready for use.
     - DELETING: Disk is deleting. |
| `multi_writer` | bool | Indicates whether or not the disk can be read/write attached to
more than one instance. |
| `source_consistency_group_policy_id` | String | [Output Only] ID of the DiskConsistencyGroupPolicy for a secondary disk
that was created using a consistency group. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `licenses` | Vec<String> | A list of publicly visible licenses. Reserved for Google's use. |
| `options` | String | Internal use only. |
| `provisioned_iops` | String | Indicates how many IOPS to provision for the disk. This sets the number
of I/O operations per second that the disk can handle. Values must be
between 10,000 and 120,000. For more details, see theExtreme persistent
disk documentation. |
| `resource_status` | String | [Output Only] Status information for the disk resource. |
| `physical_block_size_bytes` | String | Physical block size of the persistent disk, in bytes.
If not present in a request, a default value is used.
The currently supported size is 4096, other sizes may be added in
the future.
If an unsupported value is requested, the error message will list
the supported values for the caller's project. |
| `region` | String | [Output Only] URL of the region where the disk resides. Only applicable for
regional resources.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `satisfies_pzi` | bool | Output only. Reserved for future use. |
| `source_consistency_group_policy` | String | [Output Only] URL of the DiskConsistencyGroupPolicy for a secondary disk
that was created using a consistency group. |
| `source_disk` | String | The source disk used to create this disk. You can provide this as a
partial or full URL to the resource. For example, the following are valid
values:
   
   
     - 
       https://www.googleapis.com/compute/v1/projects/project/zones/zone/disks/disk
     
     - 
       https://www.googleapis.com/compute/v1/projects/project/regions/region/disks/disk
     
     - 
       projects/project/zones/zone/disks/disk
     
     - 
       projects/project/regions/region/disks/disk
     
     - 
       zones/zone/disks/disk
     
     - 
       regions/region/disks/disk |
| `source_snapshot` | String | The source snapshot used to create this disk. You can provide this as a
partial or full URL to the resource. For example, the following are valid
values:
   
   
     - https://www.googleapis.com/compute/v1/projects/project/global/snapshots/snapshot 
   - projects/project/global/snapshots/snapshot
     - global/snapshots/snapshot |
| `name` | String | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?`
which means the first character must be a lowercase letter, and all
following characters must be a dash, lowercase letter, or digit, except
the last character, which cannot be a dash. |
| `source_snapshot_encryption_key` | String | Thecustomer-supplied
encryption key of the source snapshot. Required if the source snapshot
is protected by a customer-supplied encryption key. |
| `users` | Vec<String> | [Output Only] Links to the users of the disk (attached instances)
in form:projects/project/zones/zone/instances/instance |
| `satisfies_pzs` | bool | [Output Only] Reserved for future use. |
| `license_codes` | Vec<String> | Integer license codes indicating which licenses are attached to this disk. |
| `label_fingerprint` | String | A fingerprint for the labels being applied to this disk, which is
essentially a hash of the labels set used for optimistic locking. The
fingerprint is initially generated by Compute Engine and changes after
every request to modify or update labels. You must always provide an
up-to-date fingerprint hash in order to update or change labels,
otherwise the request will fail with error412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve a disk. |
| `enable_confidential_compute` | bool | Whether this disk is using confidential compute mode. |
| `resource_policies` | Vec<String> | Resource policies applied to this disk for automatic snapshot creations. |
| `disk_encryption_key` | String | Encrypts the disk using a
customer-supplied encryption key or a 
customer-managed encryption key.

Encryption keys do not protect access to metadata of the disk.

After you encrypt a disk with a customer-supplied key, you must provide the
same key if you use the disk later. For example, to create a disk snapshot,
to create a disk image, to create a machine image, or to attach the disk to
a virtual machine.

After you encrypt a disk with a customer-managed key, thediskEncryptionKey.kmsKeyName is set to a key *version*
name once the disk is created. The disk is encrypted with this version of
the key. In the response, diskEncryptionKey.kmsKeyName appears
in the following format:

"diskEncryptionKey.kmsKeyName":
"projects/kms_project_id/locations/region/keyRings/
key_region/cryptoKeys/key
/cryptoKeysVersions/version

If you do not provide an encryption key when creating the disk, then the 
disk is encrypted using an automatically generated key and you don't need 
to provide a key to use the disk later. |
| `size_gb` | String | Size, in GB, of the persistent disk. You can specify
this field when creating a persistent disk using thesourceImage, sourceSnapshot, orsourceDisk parameter, or specify it alone to create an empty
persistent disk.

If you specify this field along with a source, the value ofsizeGb must not be less than the size of the
source.
Acceptable values are greater than 0. |
| `source_disk_id` | String | [Output Only] The unique ID of the disk used to create this disk. This
value identifies the exact disk that was used to create this persistent
disk. For example, if you created the persistent disk from a disk that
was later deleted and recreated under the same name, the source disk ID
would identify the exact version of the disk that was used. |
| `access_mode` | String | The access mode of the disk.
   
   
     - READ_WRITE_SINGLE: The default AccessMode, means the
     disk can be attached to single instance in RW mode.
     - READ_WRITE_MANY: The AccessMode means the disk can be
     attached to multiple instances in RW mode.
     - READ_ONLY_MANY: The AccessMode means the disk can be
     attached to multiple instances in RO mode.


The AccessMode is only valid for Hyperdisk disk types. |
| `storage_pool` | String | The storage pool in which the new disk is created. You can provide
this as a partial or full URL to the resource. For example, the following
are valid values:
   
   
     - https://www.googleapis.com/compute/v1/projects/project/zones/zone/storagePools/storagePool
     - projects/project/zones/zone/storagePools/storagePool 
   - zones/zone/storagePools/storagePool |
| `architecture` | String | The architecture of the disk. Valid values are
ARM64 or X86_64. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `location_hint` | String | An opaque location hint used to place the disk close to other resources.
This field is for use by internal tools that use the public API. |
| `source_image_id` | String | [Output Only] The ID value of the image used to create this disk. This
value identifies the exact image that was used to create this persistent
disk. For example, if you created the persistent disk from an image that
was later deleted and recreated under the same name, the source image ID
would identify the exact version of the image that was used. |
| `source_storage_object` | String | The full Google Cloud Storage URI where the disk image is stored. This file
must be a gzip-compressed tarball whose name ends in .tar.gz or virtual
machine disk whose name ends in vmdk. Valid URIs may start with gs:// or
https://storage.googleapis.com/. This flag is not optimized for creating
multiple disks from a source storage object. To create many disks from a
source storage object, use gcloud compute images
import instead. |
| `async_primary_disk` | String | Disk asynchronously replicated into this disk. |
| `replica_zones` | Vec<String> | URLs of the zones where the disk should be replicated to. Only applicable
for regional resources. |
| `kind` | String | [Output Only] Type of the resource. Always compute#disk for
disks. |
| `source_instant_snapshot_id` | String | [Output Only] The unique ID of the instant snapshot used to create this
disk. This value identifies the exact instant snapshot that was used to
create this persistent disk. For example, if you created the persistent
disk from an instant snapshot that was later deleted and recreated under
the same name, the source instant snapshot ID would identify the exact
version of the instant snapshot that was used. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `labels` | HashMap<String, String> | Labels to apply to this disk. These can be later modified by
the setLabels method. |
| `locked` | bool | [Output Only] The field indicates if the disk is created from a locked
source image. Attachment of a disk created from a locked source image will
cause the following operations to become irreversibly prohibited:
   
   - R/W or R/O disk attachment to any other instance
   - Disk detachment. And the disk can only be deleted when the instance
   is deleted
   - Creation of images or snapshots
   - Disk cloning



Furthermore, the instance with at least one disk with locked flag set to
true will be prohibited from performing the operations below:
   
   - Further attachment of secondary disks.
   - Detachment of any disks
   - Create machine images
   - Create instance template
   - Delete the instance with --keep-disk parameter set to true for locked
   disks
   - Attach a locked disk with --auto-delete parameter set to false |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create region_disk
region_disk = provider.compute_api.Region_disk {
    region = "value"  # Name of the region for this request.
    project = "value"  # Project ID for this request.
}

# Access region_disk outputs
region_disk_id = region_disk.id
region_disk_last_detach_timestamp = region_disk.last_detach_timestamp
region_disk_source_image = region_disk.source_image
region_disk_storage_type = region_disk.storage_type
region_disk_erase_windows_vss_signature = region_disk.erase_windows_vss_signature
region_disk_type = region_disk.type
region_disk_user_licenses = region_disk.user_licenses
region_disk_zone = region_disk.zone
region_disk_async_secondary_disks = region_disk.async_secondary_disks
region_disk_params = region_disk.params
region_disk_source_snapshot_id = region_disk.source_snapshot_id
region_disk_interface = region_disk.interface
region_disk_provisioned_throughput = region_disk.provisioned_throughput
region_disk_guest_os_features = region_disk.guest_os_features
region_disk_last_attach_timestamp = region_disk.last_attach_timestamp
region_disk_self_link = region_disk.self_link
region_disk_source_image_encryption_key = region_disk.source_image_encryption_key
region_disk_source_instant_snapshot = region_disk.source_instant_snapshot
region_disk_status = region_disk.status
region_disk_multi_writer = region_disk.multi_writer
region_disk_source_consistency_group_policy_id = region_disk.source_consistency_group_policy_id
region_disk_id = region_disk.id
region_disk_licenses = region_disk.licenses
region_disk_options = region_disk.options
region_disk_provisioned_iops = region_disk.provisioned_iops
region_disk_resource_status = region_disk.resource_status
region_disk_physical_block_size_bytes = region_disk.physical_block_size_bytes
region_disk_region = region_disk.region
region_disk_satisfies_pzi = region_disk.satisfies_pzi
region_disk_source_consistency_group_policy = region_disk.source_consistency_group_policy
region_disk_source_disk = region_disk.source_disk
region_disk_source_snapshot = region_disk.source_snapshot
region_disk_name = region_disk.name
region_disk_source_snapshot_encryption_key = region_disk.source_snapshot_encryption_key
region_disk_users = region_disk.users
region_disk_satisfies_pzs = region_disk.satisfies_pzs
region_disk_license_codes = region_disk.license_codes
region_disk_label_fingerprint = region_disk.label_fingerprint
region_disk_enable_confidential_compute = region_disk.enable_confidential_compute
region_disk_resource_policies = region_disk.resource_policies
region_disk_disk_encryption_key = region_disk.disk_encryption_key
region_disk_size_gb = region_disk.size_gb
region_disk_source_disk_id = region_disk.source_disk_id
region_disk_access_mode = region_disk.access_mode
region_disk_storage_pool = region_disk.storage_pool
region_disk_architecture = region_disk.architecture
region_disk_creation_timestamp = region_disk.creation_timestamp
region_disk_location_hint = region_disk.location_hint
region_disk_source_image_id = region_disk.source_image_id
region_disk_source_storage_object = region_disk.source_storage_object
region_disk_async_primary_disk = region_disk.async_primary_disk
region_disk_replica_zones = region_disk.replica_zones
region_disk_kind = region_disk.kind
region_disk_source_instant_snapshot_id = region_disk.source_instant_snapshot_id
region_disk_description = region_disk.description
region_disk_labels = region_disk.labels
region_disk_locked = region_disk.locked
```

---


### Target_grpc_proxie

Creates a TargetGrpcProxy in the specified project in the given scope
using the parameters that are included in the request.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `self_link_with_id` | String |  | [Output Only] Server-defined URL with id for the resource. |
| `validate_for_proxyless` | bool |  | If true, indicates that the BackendServices referenced by the urlMap may be
accessed by gRPC applications without using a sidecar proxy. This will
enable configuration checks on urlMap and its referenced BackendServices to
not allow unsupported features. A gRPC application must use
"xds:///" scheme in the target URI of the service it is
connecting to.
If false, indicates that the BackendServices referenced by the urlMap will
be accessed by gRPC applications via a sidecar proxy. In this case, a gRPC
application must not use "xds:///" scheme in the target URI of
the service it is connecting to |
| `url_map` | String |  | URL to the UrlMap resource that defines the mapping from URL to the
BackendService. The protocol field in the BackendService must be set to
GRPC. |
| `name` | String |  | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `id` | String |  | [Output Only] The unique identifier for the resource type. The server
generates this identifier. |
| `fingerprint` | String |  | Fingerprint of this resource. A hash of the contents stored in this object.
This field is used in optimistic locking. This field will be ignored when
inserting a TargetGrpcProxy. An up-to-date fingerprint must
be provided in order to patch/update the TargetGrpcProxy; otherwise, the
request will fail with error 412 conditionNotMet. To see the
latest fingerprint, make a get() request to retrieve the
TargetGrpcProxy. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `kind` | String |  | [Output Only] Type of the resource. Alwayscompute#targetGrpcProxy for target grpc proxies. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `self_link_with_id` | String | [Output Only] Server-defined URL with id for the resource. |
| `validate_for_proxyless` | bool | If true, indicates that the BackendServices referenced by the urlMap may be
accessed by gRPC applications without using a sidecar proxy. This will
enable configuration checks on urlMap and its referenced BackendServices to
not allow unsupported features. A gRPC application must use
"xds:///" scheme in the target URI of the service it is
connecting to.
If false, indicates that the BackendServices referenced by the urlMap will
be accessed by gRPC applications via a sidecar proxy. In this case, a gRPC
application must not use "xds:///" scheme in the target URI of
the service it is connecting to |
| `url_map` | String | URL to the UrlMap resource that defines the mapping from URL to the
BackendService. The protocol field in the BackendService must be set to
GRPC. |
| `name` | String | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `id` | String | [Output Only] The unique identifier for the resource type. The server
generates this identifier. |
| `fingerprint` | String | Fingerprint of this resource. A hash of the contents stored in this object.
This field is used in optimistic locking. This field will be ignored when
inserting a TargetGrpcProxy. An up-to-date fingerprint must
be provided in order to patch/update the TargetGrpcProxy; otherwise, the
request will fail with error 412 conditionNotMet. To see the
latest fingerprint, make a get() request to retrieve the
TargetGrpcProxy. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `kind` | String | [Output Only] Type of the resource. Alwayscompute#targetGrpcProxy for target grpc proxies. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create target_grpc_proxie
target_grpc_proxie = provider.compute_api.Target_grpc_proxie {
    project = "value"  # Project ID for this request.
}

# Access target_grpc_proxie outputs
target_grpc_proxie_id = target_grpc_proxie.id
target_grpc_proxie_creation_timestamp = target_grpc_proxie.creation_timestamp
target_grpc_proxie_self_link_with_id = target_grpc_proxie.self_link_with_id
target_grpc_proxie_validate_for_proxyless = target_grpc_proxie.validate_for_proxyless
target_grpc_proxie_url_map = target_grpc_proxie.url_map
target_grpc_proxie_name = target_grpc_proxie.name
target_grpc_proxie_id = target_grpc_proxie.id
target_grpc_proxie_fingerprint = target_grpc_proxie.fingerprint
target_grpc_proxie_self_link = target_grpc_proxie.self_link
target_grpc_proxie_kind = target_grpc_proxie.kind
target_grpc_proxie_description = target_grpc_proxie.description
```

---


### Backend_bucket

Creates a BackendBucket resource in the specified project using
the data included in the request.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `edge_security_policy` | String |  | [Output Only] The resource URL for the edge security policy associated with
this backend bucket. |
| `cdn_policy` | String |  | Cloud CDN configuration for this BackendBucket. |
| `kind` | String |  | Type of the resource. |
| `compression_mode` | String |  | Compress text responses using Brotli or gzip compression, based on
the client's Accept-Encoding header. |
| `name` | String |  | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `params` | String |  | Input only. [Input Only] Additional params passed with the request, but not persisted
as part of resource payload. |
| `bucket_name` | String |  | Cloud Storage bucket name. |
| `used_by` | Vec<String> |  | [Output Only] List of resources referencing that backend bucket. |
| `load_balancing_scheme` | String |  | The value can only be INTERNAL_MANAGED for cross-region internal layer 7
load balancer.

If loadBalancingScheme is not specified, the backend bucket can be used by
classic global external load balancers, or global application external load
balancers, or both. |
| `description` | String |  | An optional textual description of the resource; provided by the client
when the resource is created. |
| `id` | String |  | [Output Only] Unique identifier for the resource; defined by the server. |
| `region` | String |  | [Output Only] URL of the region where the regional backend bucket
resides. This field is not applicable to global backend buckets.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `custom_response_headers` | Vec<String> |  | Headers that the Application Load Balancer should add to proxied responses. |
| `enable_cdn` | bool |  | If true, enable Cloud CDN for this BackendBucket. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `edge_security_policy` | String | [Output Only] The resource URL for the edge security policy associated with
this backend bucket. |
| `cdn_policy` | String | Cloud CDN configuration for this BackendBucket. |
| `kind` | String | Type of the resource. |
| `compression_mode` | String | Compress text responses using Brotli or gzip compression, based on
the client's Accept-Encoding header. |
| `name` | String | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `params` | String | Input only. [Input Only] Additional params passed with the request, but not persisted
as part of resource payload. |
| `bucket_name` | String | Cloud Storage bucket name. |
| `used_by` | Vec<String> | [Output Only] List of resources referencing that backend bucket. |
| `load_balancing_scheme` | String | The value can only be INTERNAL_MANAGED for cross-region internal layer 7
load balancer.

If loadBalancingScheme is not specified, the backend bucket can be used by
classic global external load balancers, or global application external load
balancers, or both. |
| `description` | String | An optional textual description of the resource; provided by the client
when the resource is created. |
| `id` | String | [Output Only] Unique identifier for the resource; defined by the server. |
| `region` | String | [Output Only] URL of the region where the regional backend bucket
resides. This field is not applicable to global backend buckets.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `custom_response_headers` | Vec<String> | Headers that the Application Load Balancer should add to proxied responses. |
| `enable_cdn` | bool | If true, enable Cloud CDN for this BackendBucket. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create backend_bucket
backend_bucket = provider.compute_api.Backend_bucket {
    project = "value"  # Project ID for this request.
}

# Access backend_bucket outputs
backend_bucket_id = backend_bucket.id
backend_bucket_self_link = backend_bucket.self_link
backend_bucket_creation_timestamp = backend_bucket.creation_timestamp
backend_bucket_edge_security_policy = backend_bucket.edge_security_policy
backend_bucket_cdn_policy = backend_bucket.cdn_policy
backend_bucket_kind = backend_bucket.kind
backend_bucket_compression_mode = backend_bucket.compression_mode
backend_bucket_name = backend_bucket.name
backend_bucket_params = backend_bucket.params
backend_bucket_bucket_name = backend_bucket.bucket_name
backend_bucket_used_by = backend_bucket.used_by
backend_bucket_load_balancing_scheme = backend_bucket.load_balancing_scheme
backend_bucket_description = backend_bucket.description
backend_bucket_id = backend_bucket.id
backend_bucket_region = backend_bucket.region
backend_bucket_custom_response_headers = backend_bucket.custom_response_headers
backend_bucket_enable_cdn = backend_bucket.enable_cdn
```

---


### Interconnect

Creates an Interconnect in the specified project using
the data included in the request.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `interconnect_type` | String |  | Type of interconnect, which can take one of the following values:
   
   - PARTNER: A partner-managed interconnection shared between customers
   though a partner.
   - DEDICATED: A dedicated physical interconnection with the
   customer.


Note that a value IT_PRIVATE has been deprecated in favor of DEDICATED. |
| `macsec` | String |  | Configuration that enables Media Access Control security (MACsec) on the
Cloud Interconnect connection between Google and your on-premises router. |
| `application_aware_interconnect` | String |  | Configuration information for application awareness on this Cloud
Interconnect. |
| `google_reference_id` | String |  | [Output Only] Google reference ID to be used when raising support tickets
with Google or otherwise to debug backend connectivity issues. |
| `kind` | String |  | [Output Only] Type of the resource. Alwayscompute#interconnect for interconnects. |
| `available_features` | Vec<String> |  | [Output only] List of features available for this Interconnect connection,
which can take one of the following values:
   
   - IF_MACSEC: If present, then the Interconnect connection is
   provisioned on MACsec capable hardware ports. If not present, then the
   Interconnect connection is provisioned on non-MACsec capable ports. Any
   attempt to enable MACsec will fail.
   - IF_CROSS_SITE_NETWORK: If present, then the Interconnect connection is
   provisioned exclusively for Cross-Site Networking. Any attempt to configure
   VLAN attachments will fail. If not present, then the Interconnect
   connection is not provisioned for Cross-Site Networking. Any attempt to use
   it for Cross-Site Networking will fail. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `labels` | HashMap<String, String> |  | Labels for this resource. These can only be added or modified by thesetLabels method. Each label key/value pair must comply withRFC1035.
Label values may be empty. |
| `satisfies_pzs` | bool |  | [Output Only] Reserved for future use. |
| `aai_enabled` | bool |  | Enable or disable the application awareness feature on this Cloud
Interconnect. |
| `google_ip_address` | String |  | [Output Only] IP address configured on the Google side of the Interconnect
link. This can be used only for ping tests. |
| `admin_enabled` | bool |  | Administrative status of the interconnect. When this is set to true, the
Interconnect is functional and can carry traffic.
When set to false, no packets can be carried over the interconnect and
no BGP routes are exchanged over it. By default, the status is set to true. |
| `peer_ip_address` | String |  | [Output Only] IP address configured on the customer side of the
Interconnect link. The customer should configure this IP address during
turnup when prompted by Google NOC. This can be used only for ping tests. |
| `wire_groups` | Vec<String> |  | [Output Only] A list of the URLs of all CrossSiteNetwork WireGroups
configured to use this Interconnect. The Interconnect cannot be deleted if
this list is non-empty. |
| `link_type` | String |  | Type of link requested, which can take one of the following values:
   
   - LINK_TYPE_ETHERNET_10G_LR: A 10G Ethernet with LR optics
   - LINK_TYPE_ETHERNET_100G_LR: A 100G Ethernet with LR optics.
   - LINK_TYPE_ETHERNET_400G_LR4: A 400G Ethernet with LR4 optics.


 Note that this field indicates the speed of each of
the links in the bundle, not the speed of the entire bundle. |
| `macsec_enabled` | bool |  | Enable or disable MACsec on this Interconnect connection. MACsec enablement
fails if the MACsec object is not specified. |
| `requested_features` | Vec<String> |  | Optional. This parameter can be provided only with Interconnect INSERT. It
isn't valid for Interconnect PATCH. List of features requested for this
Interconnect connection, which can take one of the following values:
   
   - IF_MACSEC: If specified, then the connection is created on MACsec
   capable hardware ports. If not specified, non-MACsec capable ports will
   also be considered.
   - IF_CROSS_SITE_NETWORK: If specified, then the connection is created
   exclusively for Cross-Site Networking. The connection can not be used for
   Cross-Site Networking unless this feature is specified. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `operational_status` | String |  | [Output Only] The current status of this Interconnect's functionality,
which can take one of the following values:
   
   - OS_ACTIVE: A valid Interconnect, which is turned up and is ready to
   use. Attachments may be provisioned on this Interconnect.

- OS_UNPROVISIONED: An Interconnect that has not completed turnup. No
attachments may be provisioned on this Interconnect.
- OS_UNDER_MAINTENANCE: An Interconnect that is undergoing internal
maintenance. No attachments may be provisioned or updated on this
Interconnect. |
| `label_fingerprint` | String |  | A fingerprint for the labels being applied to this Interconnect, which
is essentially a hash of the labels set used for optimistic locking. The
fingerprint is initially generated by Compute Engine and changes after
every request to modify or update labels. You must always provide an
up-to-date fingerprint hash in order to update or change labels,
otherwise the request will fail with error412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve an Interconnect. |
| `params` | String |  | Input only. [Input Only] Additional params passed with the request, but not persisted
as part of resource payload. |
| `provisioned_link_count` | i64 |  | [Output Only] Number of links actually provisioned in this interconnect. |
| `state` | String |  | [Output Only] The current state of Interconnect functionality, which can
take one of the following values:
   
   - ACTIVE: The Interconnect is valid, turned up and ready to use.
   Attachments may be provisioned on this Interconnect.
   - UNPROVISIONED: The Interconnect has not completed turnup. No
   attachments may be provisioned on this Interconnect.
   - UNDER_MAINTENANCE: The Interconnect is undergoing internal maintenance.
   No attachments may be provisioned or updated on this
   Interconnect. |
| `remote_location` | String |  | Indicates that this is a Cross-Cloud Interconnect. This field specifies the
location outside of Google's network that the interconnect is connected to. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `location` | String |  | URL of the InterconnectLocation object that represents where this
connection is to be provisioned. |
| `subzone` | String |  | Specific subzone in the InterconnectLocation that represents where
this connection is to be provisioned. |
| `interconnect_attachments` | Vec<String> |  | [Output Only] A list of the URLs of all InterconnectAttachments configured
to use  this Interconnect. |
| `name` | String |  | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must be
a dash, lowercase letter, or digit, except the last character, which cannot
be a dash. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `requested_link_count` | i64 |  | Target number of physical links in the link bundle, as requested by the
customer. |
| `circuit_infos` | Vec<String> |  | [Output Only] A list of CircuitInfo objects, that describe the individual
circuits in this LAG. |
| `interconnect_groups` | Vec<String> |  | [Output Only] URLs of InterconnectGroups that include this Interconnect.
Order is arbitrary and items are unique. |
| `expected_outages` | Vec<String> |  | [Output Only] A list of outages expected for this Interconnect. |
| `customer_name` | String |  | Customer name, to put in the Letter of Authorization as the party
authorized to request a crossconnect. |
| `noc_contact_email` | String |  | Email address to contact the customer NOC for operations and maintenance
notifications regarding this Interconnect. If specified, this will be used
for notifications in addition to all other forms described, such as
Cloud Monitoring logs alerting and Cloud Notifications. This field is
required for users who sign up for Cloud Interconnect using
workforce identity federation. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `interconnect_type` | String | Type of interconnect, which can take one of the following values:
   
   - PARTNER: A partner-managed interconnection shared between customers
   though a partner.
   - DEDICATED: A dedicated physical interconnection with the
   customer.


Note that a value IT_PRIVATE has been deprecated in favor of DEDICATED. |
| `macsec` | String | Configuration that enables Media Access Control security (MACsec) on the
Cloud Interconnect connection between Google and your on-premises router. |
| `application_aware_interconnect` | String | Configuration information for application awareness on this Cloud
Interconnect. |
| `google_reference_id` | String | [Output Only] Google reference ID to be used when raising support tickets
with Google or otherwise to debug backend connectivity issues. |
| `kind` | String | [Output Only] Type of the resource. Alwayscompute#interconnect for interconnects. |
| `available_features` | Vec<String> | [Output only] List of features available for this Interconnect connection,
which can take one of the following values:
   
   - IF_MACSEC: If present, then the Interconnect connection is
   provisioned on MACsec capable hardware ports. If not present, then the
   Interconnect connection is provisioned on non-MACsec capable ports. Any
   attempt to enable MACsec will fail.
   - IF_CROSS_SITE_NETWORK: If present, then the Interconnect connection is
   provisioned exclusively for Cross-Site Networking. Any attempt to configure
   VLAN attachments will fail. If not present, then the Interconnect
   connection is not provisioned for Cross-Site Networking. Any attempt to use
   it for Cross-Site Networking will fail. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `labels` | HashMap<String, String> | Labels for this resource. These can only be added or modified by thesetLabels method. Each label key/value pair must comply withRFC1035.
Label values may be empty. |
| `satisfies_pzs` | bool | [Output Only] Reserved for future use. |
| `aai_enabled` | bool | Enable or disable the application awareness feature on this Cloud
Interconnect. |
| `google_ip_address` | String | [Output Only] IP address configured on the Google side of the Interconnect
link. This can be used only for ping tests. |
| `admin_enabled` | bool | Administrative status of the interconnect. When this is set to true, the
Interconnect is functional and can carry traffic.
When set to false, no packets can be carried over the interconnect and
no BGP routes are exchanged over it. By default, the status is set to true. |
| `peer_ip_address` | String | [Output Only] IP address configured on the customer side of the
Interconnect link. The customer should configure this IP address during
turnup when prompted by Google NOC. This can be used only for ping tests. |
| `wire_groups` | Vec<String> | [Output Only] A list of the URLs of all CrossSiteNetwork WireGroups
configured to use this Interconnect. The Interconnect cannot be deleted if
this list is non-empty. |
| `link_type` | String | Type of link requested, which can take one of the following values:
   
   - LINK_TYPE_ETHERNET_10G_LR: A 10G Ethernet with LR optics
   - LINK_TYPE_ETHERNET_100G_LR: A 100G Ethernet with LR optics.
   - LINK_TYPE_ETHERNET_400G_LR4: A 400G Ethernet with LR4 optics.


 Note that this field indicates the speed of each of
the links in the bundle, not the speed of the entire bundle. |
| `macsec_enabled` | bool | Enable or disable MACsec on this Interconnect connection. MACsec enablement
fails if the MACsec object is not specified. |
| `requested_features` | Vec<String> | Optional. This parameter can be provided only with Interconnect INSERT. It
isn't valid for Interconnect PATCH. List of features requested for this
Interconnect connection, which can take one of the following values:
   
   - IF_MACSEC: If specified, then the connection is created on MACsec
   capable hardware ports. If not specified, non-MACsec capable ports will
   also be considered.
   - IF_CROSS_SITE_NETWORK: If specified, then the connection is created
   exclusively for Cross-Site Networking. The connection can not be used for
   Cross-Site Networking unless this feature is specified. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `operational_status` | String | [Output Only] The current status of this Interconnect's functionality,
which can take one of the following values:
   
   - OS_ACTIVE: A valid Interconnect, which is turned up and is ready to
   use. Attachments may be provisioned on this Interconnect.

- OS_UNPROVISIONED: An Interconnect that has not completed turnup. No
attachments may be provisioned on this Interconnect.
- OS_UNDER_MAINTENANCE: An Interconnect that is undergoing internal
maintenance. No attachments may be provisioned or updated on this
Interconnect. |
| `label_fingerprint` | String | A fingerprint for the labels being applied to this Interconnect, which
is essentially a hash of the labels set used for optimistic locking. The
fingerprint is initially generated by Compute Engine and changes after
every request to modify or update labels. You must always provide an
up-to-date fingerprint hash in order to update or change labels,
otherwise the request will fail with error412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve an Interconnect. |
| `params` | String | Input only. [Input Only] Additional params passed with the request, but not persisted
as part of resource payload. |
| `provisioned_link_count` | i64 | [Output Only] Number of links actually provisioned in this interconnect. |
| `state` | String | [Output Only] The current state of Interconnect functionality, which can
take one of the following values:
   
   - ACTIVE: The Interconnect is valid, turned up and ready to use.
   Attachments may be provisioned on this Interconnect.
   - UNPROVISIONED: The Interconnect has not completed turnup. No
   attachments may be provisioned on this Interconnect.
   - UNDER_MAINTENANCE: The Interconnect is undergoing internal maintenance.
   No attachments may be provisioned or updated on this
   Interconnect. |
| `remote_location` | String | Indicates that this is a Cross-Cloud Interconnect. This field specifies the
location outside of Google's network that the interconnect is connected to. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `location` | String | URL of the InterconnectLocation object that represents where this
connection is to be provisioned. |
| `subzone` | String | Specific subzone in the InterconnectLocation that represents where
this connection is to be provisioned. |
| `interconnect_attachments` | Vec<String> | [Output Only] A list of the URLs of all InterconnectAttachments configured
to use  this Interconnect. |
| `name` | String | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must be
a dash, lowercase letter, or digit, except the last character, which cannot
be a dash. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `requested_link_count` | i64 | Target number of physical links in the link bundle, as requested by the
customer. |
| `circuit_infos` | Vec<String> | [Output Only] A list of CircuitInfo objects, that describe the individual
circuits in this LAG. |
| `interconnect_groups` | Vec<String> | [Output Only] URLs of InterconnectGroups that include this Interconnect.
Order is arbitrary and items are unique. |
| `expected_outages` | Vec<String> | [Output Only] A list of outages expected for this Interconnect. |
| `customer_name` | String | Customer name, to put in the Letter of Authorization as the party
authorized to request a crossconnect. |
| `noc_contact_email` | String | Email address to contact the customer NOC for operations and maintenance
notifications regarding this Interconnect. If specified, this will be used
for notifications in addition to all other forms described, such as
Cloud Monitoring logs alerting and Cloud Notifications. This field is
required for users who sign up for Cloud Interconnect using
workforce identity federation. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create interconnect
interconnect = provider.compute_api.Interconnect {
    project = "value"  # Project ID for this request.
}

# Access interconnect outputs
interconnect_id = interconnect.id
interconnect_interconnect_type = interconnect.interconnect_type
interconnect_macsec = interconnect.macsec
interconnect_application_aware_interconnect = interconnect.application_aware_interconnect
interconnect_google_reference_id = interconnect.google_reference_id
interconnect_kind = interconnect.kind
interconnect_available_features = interconnect.available_features
interconnect_creation_timestamp = interconnect.creation_timestamp
interconnect_labels = interconnect.labels
interconnect_satisfies_pzs = interconnect.satisfies_pzs
interconnect_aai_enabled = interconnect.aai_enabled
interconnect_google_ip_address = interconnect.google_ip_address
interconnect_admin_enabled = interconnect.admin_enabled
interconnect_peer_ip_address = interconnect.peer_ip_address
interconnect_wire_groups = interconnect.wire_groups
interconnect_link_type = interconnect.link_type
interconnect_macsec_enabled = interconnect.macsec_enabled
interconnect_requested_features = interconnect.requested_features
interconnect_self_link = interconnect.self_link
interconnect_operational_status = interconnect.operational_status
interconnect_label_fingerprint = interconnect.label_fingerprint
interconnect_params = interconnect.params
interconnect_provisioned_link_count = interconnect.provisioned_link_count
interconnect_state = interconnect.state
interconnect_remote_location = interconnect.remote_location
interconnect_id = interconnect.id
interconnect_location = interconnect.location
interconnect_subzone = interconnect.subzone
interconnect_interconnect_attachments = interconnect.interconnect_attachments
interconnect_name = interconnect.name
interconnect_description = interconnect.description
interconnect_requested_link_count = interconnect.requested_link_count
interconnect_circuit_infos = interconnect.circuit_infos
interconnect_interconnect_groups = interconnect.interconnect_groups
interconnect_expected_outages = interconnect.expected_outages
interconnect_customer_name = interconnect.customer_name
interconnect_noc_contact_email = interconnect.noc_contact_email
```

---


### Url_map

Creates a UrlMap resource in the specified project using
the data included in the request.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `default_custom_error_response_policy` | String |  | defaultCustomErrorResponsePolicy specifies how the Load
Balancer returns error responses when BackendServiceorBackendBucket responds with an error. 

This policy takes
effect at the load balancer level and applies only when no policy has been
defined for the error code at lower levels like PathMatcher, RouteRule and
PathRule within this UrlMap. 

For example, consider a UrlMap with the
following configuration:
   
   
     - defaultCustomErrorResponsePolicy containing policies for
     responding to 5xx and 4xx errors
     - A PathMatcher configured for *.example.com has
     defaultCustomErrorResponsePolicy for 4xx.

If a request for http://www.example.com/ encounters a404, the policy inpathMatcher.defaultCustomErrorResponsePolicy will be enforced.
When the request for http://www.example.com/ encounters a502, the policy inUrlMap.defaultCustomErrorResponsePolicy will be enforced. When
a request that does not match any host in *.example.com such
as http://www.myotherexample.com/, encounters a404, UrlMap.defaultCustomErrorResponsePolicy
takes effect. 

When used in conjunction withdefaultRouteAction.retryPolicy, retries take precedence. Only
once all retries are exhausted, thedefaultCustomErrorResponsePolicy is applied. While attempting
a retry, if load balancer is successful in reaching the
service, the defaultCustomErrorResponsePolicy is ignored and
the response from the service is returned to the client.

defaultCustomErrorResponsePolicy is supported only for
global external Application Load Balancers. |
| `default_service` | String |  | The full or partial URL of the defaultService resource to
which traffic is directed if none of the hostRules match.
If defaultRouteAction is also specified, advanced
routing actions, such as URL rewrites, take effect before sending the
request to the backend.


Only one of defaultUrlRedirect, defaultService
or defaultRouteAction.weightedBackendService can be set.


defaultService has no effect when the URL map is bound
to a target gRPC proxy that has the validateForProxyless field
set to true. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `path_matchers` | Vec<String> |  | The list of named PathMatchers to use against the URL. |
| `fingerprint` | String |  | Fingerprint of this resource. A hash of the contents stored in this object.
This field is used in optimistic locking. This field is ignored when
inserting a UrlMap. An up-to-date fingerprint must be provided
in order to update the UrlMap, otherwise the request will
fail with error 412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve a UrlMap. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `header_action` | String |  | Specifies changes to request and response headers that need to take effect
for the selected backendService.

The headerAction specified here take effect afterheaderAction specified under pathMatcher.

headerAction is not supported for load balancers
that have
their loadBalancingScheme set to EXTERNAL.

Not supported when the URL map is bound to a target gRPC proxy that
has validateForProxyless field set to true. |
| `kind` | String |  | [Output Only] Type of the resource. Always compute#urlMaps for
url maps. |
| `name` | String |  | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `tests` | Vec<String> |  | The list of expected URL mapping tests. Request to update theUrlMap succeeds only if all test cases pass. You can specify a
maximum of 100 tests per UrlMap.

Not supported when the URL map is bound to a target gRPC proxy that
has validateForProxyless field set to true. |
| `region` | String |  | [Output Only] URL of the region where the regional URL map resides.
This field is not applicable to global URL maps.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `host_rules` | Vec<String> |  | The list of host rules to use against the URL. |
| `default_route_action` | String |  | defaultRouteAction takes effect when none of the 
hostRules match. The load balancer performs advanced routing
actions, such as URL rewrites and header transformations, before forwarding
the request to the selected backend.


Only one of defaultUrlRedirect, defaultService
or defaultRouteAction.weightedBackendService can be set.


 URL maps for classic Application Load Balancers only support
the urlRewrite action within defaultRouteAction.


defaultRouteAction has no effect when the URL map is bound
to a target gRPC proxy that has the validateForProxyless field
set to true. |
| `default_url_redirect` | String |  | When none of the specified hostRules match, the request
is redirected to a URL specified by defaultUrlRedirect.


Only one of defaultUrlRedirect, defaultService
or defaultRouteAction.weightedBackendService can be set.


Not supported when the URL map is bound to a target gRPC proxy. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `default_custom_error_response_policy` | String | defaultCustomErrorResponsePolicy specifies how the Load
Balancer returns error responses when BackendServiceorBackendBucket responds with an error. 

This policy takes
effect at the load balancer level and applies only when no policy has been
defined for the error code at lower levels like PathMatcher, RouteRule and
PathRule within this UrlMap. 

For example, consider a UrlMap with the
following configuration:
   
   
     - defaultCustomErrorResponsePolicy containing policies for
     responding to 5xx and 4xx errors
     - A PathMatcher configured for *.example.com has
     defaultCustomErrorResponsePolicy for 4xx.

If a request for http://www.example.com/ encounters a404, the policy inpathMatcher.defaultCustomErrorResponsePolicy will be enforced.
When the request for http://www.example.com/ encounters a502, the policy inUrlMap.defaultCustomErrorResponsePolicy will be enforced. When
a request that does not match any host in *.example.com such
as http://www.myotherexample.com/, encounters a404, UrlMap.defaultCustomErrorResponsePolicy
takes effect. 

When used in conjunction withdefaultRouteAction.retryPolicy, retries take precedence. Only
once all retries are exhausted, thedefaultCustomErrorResponsePolicy is applied. While attempting
a retry, if load balancer is successful in reaching the
service, the defaultCustomErrorResponsePolicy is ignored and
the response from the service is returned to the client.

defaultCustomErrorResponsePolicy is supported only for
global external Application Load Balancers. |
| `default_service` | String | The full or partial URL of the defaultService resource to
which traffic is directed if none of the hostRules match.
If defaultRouteAction is also specified, advanced
routing actions, such as URL rewrites, take effect before sending the
request to the backend.


Only one of defaultUrlRedirect, defaultService
or defaultRouteAction.weightedBackendService can be set.


defaultService has no effect when the URL map is bound
to a target gRPC proxy that has the validateForProxyless field
set to true. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `path_matchers` | Vec<String> | The list of named PathMatchers to use against the URL. |
| `fingerprint` | String | Fingerprint of this resource. A hash of the contents stored in this object.
This field is used in optimistic locking. This field is ignored when
inserting a UrlMap. An up-to-date fingerprint must be provided
in order to update the UrlMap, otherwise the request will
fail with error 412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve a UrlMap. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `header_action` | String | Specifies changes to request and response headers that need to take effect
for the selected backendService.

The headerAction specified here take effect afterheaderAction specified under pathMatcher.

headerAction is not supported for load balancers
that have
their loadBalancingScheme set to EXTERNAL.

Not supported when the URL map is bound to a target gRPC proxy that
has validateForProxyless field set to true. |
| `kind` | String | [Output Only] Type of the resource. Always compute#urlMaps for
url maps. |
| `name` | String | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `tests` | Vec<String> | The list of expected URL mapping tests. Request to update theUrlMap succeeds only if all test cases pass. You can specify a
maximum of 100 tests per UrlMap.

Not supported when the URL map is bound to a target gRPC proxy that
has validateForProxyless field set to true. |
| `region` | String | [Output Only] URL of the region where the regional URL map resides.
This field is not applicable to global URL maps.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `host_rules` | Vec<String> | The list of host rules to use against the URL. |
| `default_route_action` | String | defaultRouteAction takes effect when none of the 
hostRules match. The load balancer performs advanced routing
actions, such as URL rewrites and header transformations, before forwarding
the request to the selected backend.


Only one of defaultUrlRedirect, defaultService
or defaultRouteAction.weightedBackendService can be set.


 URL maps for classic Application Load Balancers only support
the urlRewrite action within defaultRouteAction.


defaultRouteAction has no effect when the URL map is bound
to a target gRPC proxy that has the validateForProxyless field
set to true. |
| `default_url_redirect` | String | When none of the specified hostRules match, the request
is redirected to a URL specified by defaultUrlRedirect.


Only one of defaultUrlRedirect, defaultService
or defaultRouteAction.weightedBackendService can be set.


Not supported when the URL map is bound to a target gRPC proxy. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create url_map
url_map = provider.compute_api.Url_map {
    project = "value"  # Project ID for this request.
}

# Access url_map outputs
url_map_id = url_map.id
url_map_self_link = url_map.self_link
url_map_default_custom_error_response_policy = url_map.default_custom_error_response_policy
url_map_default_service = url_map.default_service
url_map_description = url_map.description
url_map_path_matchers = url_map.path_matchers
url_map_fingerprint = url_map.fingerprint
url_map_creation_timestamp = url_map.creation_timestamp
url_map_header_action = url_map.header_action
url_map_kind = url_map.kind
url_map_name = url_map.name
url_map_id = url_map.id
url_map_tests = url_map.tests
url_map_region = url_map.region
url_map_host_rules = url_map.host_rules
url_map_default_route_action = url_map.default_route_action
url_map_default_url_redirect = url_map.default_url_redirect
```

---


### Project

Sets the Cloud Armor tier of the project. To set ENTERPRISE or above the
billing account of the project must be subscribed to Cloud Armor
Enterprise. See Subscribing
to Cloud Armor Enterprise for more information.

**Operations**: ✅ Create ✅ Read

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `cloud_armor_tier` | String |  | Managed protection tier to be set. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `common_instance_metadata` | String | Metadata key/value pairs available to all instances contained in this
project. See Custom
metadata for more information. |
| `default_service_account` | String | [Output Only] Default service account used by VMs running in this project. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. This is *not* the project ID, and is
just a unique ID used by Compute Engine to identify resources. |
| `usage_export_location` | String | An optional naming prefix for daily usage reports and the Google Cloud
Storage bucket where they are stored. |
| `kind` | String | [Output Only] Type of the resource. Always compute#project for
projects. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `managed_protection_tier` | String | [Output Only] The Cloud Armor Managed Protection (CAMP) tier for
this project. It can be one of the following values:CA_STANDARD, CAMP_PLUS_PAYGO.

If this field is not specified, it is assumed to beCA_STANDARD. |
| `quotas` | Vec<String> | [Output Only] Quotas assigned to this project. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `xpn_project_status` | String | [Output Only] The role this project has in a shared VPC configuration.
Currently, only projects with the host role, which is specified by the
value HOST, are differentiated. |
| `vm_dns_setting` | String | [Output Only] Default internal DNS setting used by VMs running in
this project. |
| `default_network_tier` | String | This signifies the default network tier used for configuring resources of
the project and can only take the following values:PREMIUM, STANDARD. Initially the default network
tier is PREMIUM. |
| `description` | String | An optional textual description of the resource. |
| `name` | String | The project ID. For example: my-example-project. Use the
project ID to make requests to Compute Engine. |
| `cloud_armor_tier` | String | [Output Only] The Cloud Armor tier for this project. It can be one of the
following values: CA_STANDARD,CA_ENTERPRISE_PAYGO.

If this field is not specified, it is assumed to beCA_STANDARD. |
| `enabled_features` | Vec<String> | An optional list of restricted features enabled for use on this project. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create project
project = provider.compute_api.Project {
    project = "value"  # Project ID for this request.
}

# Access project outputs
project_id = project.id
project_common_instance_metadata = project.common_instance_metadata
project_default_service_account = project.default_service_account
project_id = project.id
project_usage_export_location = project.usage_export_location
project_kind = project.kind
project_creation_timestamp = project.creation_timestamp
project_managed_protection_tier = project.managed_protection_tier
project_quotas = project.quotas
project_self_link = project.self_link
project_xpn_project_status = project.xpn_project_status
project_vm_dns_setting = project.vm_dns_setting
project_default_network_tier = project.default_network_tier
project_description = project.description
project_name = project.name
project_cloud_armor_tier = project.cloud_armor_tier
project_enabled_features = project.enabled_features
```

---


### Region_target_tcp_proxie

Creates a TargetTcpProxy resource in the specified project and region using
the data included in the request.

**Operations**: ✅ Create ✅ Read ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `service` | String |  | URL to the BackendService resource. |
| `kind` | String |  | [Output Only] Type of the resource. Alwayscompute#targetTcpProxy for target TCP proxies. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `proxy_header` | String |  | Specifies the type of proxy header to append before sending data to the
backend, either NONE or PROXY_V1. The default
is NONE. |
| `name` | String |  | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `proxy_bind` | bool |  | This field only applies when the forwarding rule that references this
target proxy has a loadBalancingScheme set toINTERNAL_SELF_MANAGED.

When this field is set to true, Envoy proxies set up inbound
traffic interception and bind to the IP address and port specified in the
forwarding rule. This is generally useful when using Traffic Director to
configure Envoy as a gateway or middle proxy (in other words, not a
sidecar proxy). The Envoy proxy listens for inbound requests and handles
requests when it receives them.

The default is false. |
| `region` | String |  | [Output Only] URL of the region where the regional TCP proxy resides.
This field is not applicable to global TCP proxy. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `project` | String | ✅ | Project ID for this request. |
| `region` | String | ✅ | Name of the region scoping this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `service` | String | URL to the BackendService resource. |
| `kind` | String | [Output Only] Type of the resource. Alwayscompute#targetTcpProxy for target TCP proxies. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `proxy_header` | String | Specifies the type of proxy header to append before sending data to the
backend, either NONE or PROXY_V1. The default
is NONE. |
| `name` | String | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `proxy_bind` | bool | This field only applies when the forwarding rule that references this
target proxy has a loadBalancingScheme set toINTERNAL_SELF_MANAGED.

When this field is set to true, Envoy proxies set up inbound
traffic interception and bind to the IP address and port specified in the
forwarding rule. This is generally useful when using Traffic Director to
configure Envoy as a gateway or middle proxy (in other words, not a
sidecar proxy). The Envoy proxy listens for inbound requests and handles
requests when it receives them.

The default is false. |
| `region` | String | [Output Only] URL of the region where the regional TCP proxy resides.
This field is not applicable to global TCP proxy. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create region_target_tcp_proxie
region_target_tcp_proxie = provider.compute_api.Region_target_tcp_proxie {
    project = "value"  # Project ID for this request.
    region = "value"  # Name of the region scoping this request.
}

# Access region_target_tcp_proxie outputs
region_target_tcp_proxie_id = region_target_tcp_proxie.id
region_target_tcp_proxie_service = region_target_tcp_proxie.service
region_target_tcp_proxie_kind = region_target_tcp_proxie.kind
region_target_tcp_proxie_self_link = region_target_tcp_proxie.self_link
region_target_tcp_proxie_proxy_header = region_target_tcp_proxie.proxy_header
region_target_tcp_proxie_name = region_target_tcp_proxie.name
region_target_tcp_proxie_id = region_target_tcp_proxie.id
region_target_tcp_proxie_creation_timestamp = region_target_tcp_proxie.creation_timestamp
region_target_tcp_proxie_proxy_bind = region_target_tcp_proxie.proxy_bind
region_target_tcp_proxie_region = region_target_tcp_proxie.region
region_target_tcp_proxie_description = region_target_tcp_proxie.description
```

---


### Region_network_endpoint_group

Creates a network endpoint group in the specified project using the
parameters that are included in the request.

**Operations**: ✅ Create ✅ Read ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `psc_target_service` | String |  | The target service url used to set up private service connection to
a Google API or a PSC Producer Service Attachment.
An example value is: asia-northeast3-cloudkms.googleapis.com.

Optional. Only valid when networkEndpointType isPRIVATE_SERVICE_CONNECT. |
| `serverless_deployment` | String |  | Only valid when networkEndpointType isSERVERLESS. Only one of cloudRun,appEngine, cloudFunction orserverlessDeployment may be set. |
| `kind` | String |  | [Output Only] Type of the resource. Alwayscompute#networkEndpointGroup for network endpoint group. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `app_engine` | String |  | Optional. Only valid when networkEndpointType isSERVERLESS. Only one of cloudRun,appEngine or cloudFunction may be set. |
| `zone` | String |  | [Output Only] The URL of thezone
where the network endpoint group is located. |
| `cloud_function` | String |  | Optional. Only valid when networkEndpointType isSERVERLESS. Only one of cloudRun,appEngine or cloudFunction may be set. |
| `network_endpoint_type` | String |  | Type of network endpoints in this network endpoint group. Can be one ofGCE_VM_IP, GCE_VM_IP_PORT,NON_GCP_PRIVATE_IP_PORT, INTERNET_FQDN_PORT,INTERNET_IP_PORT, SERVERLESS,PRIVATE_SERVICE_CONNECT, GCE_VM_IP_PORTMAP. |
| `load_balancer` | String |  | This field is only valid when the network endpoint group is used for load
balancing.
[Deprecated] This field is deprecated. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `name` | String |  | Name of the resource; provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must be
a dash, lowercase letter, or digit, except the last character, which cannot
be a dash. |
| `psc_data` | String |  | Optional. Only valid when networkEndpointType isPRIVATE_SERVICE_CONNECT. |
| `cloud_run` | String |  | Optional. Only valid when networkEndpointType isSERVERLESS. Only one of cloudRun,appEngine or cloudFunction may be set. |
| `region` | String |  | [Output Only] The URL of theregion
where the network endpoint group is located. |
| `network` | String |  | The URL of the network to which all network endpoints in the NEG belong.
Uses default project network if unspecified. |
| `annotations` | HashMap<String, String> |  | Optional. Metadata defined as annotations on the network endpoint group. |
| `default_port` | i64 |  | The default port used if the port number is not specified in the network
endpoint.

Optional. If the network endpoint type is either GCE_VM_IP,SERVERLESS or PRIVATE_SERVICE_CONNECT, this
field must not be specified. |
| `size` | i64 |  | [Output only] Number of network endpoints in the network endpoint group. |
| `subnetwork` | String |  | Optional URL of the subnetwork to which all network endpoints in the NEG
belong. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `project` | String | ✅ | Project ID for this request. |
| `region` | String | ✅ | The name of the region where
you want to create the network endpoint group. It should comply with
RFC1035. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `psc_target_service` | String | The target service url used to set up private service connection to
a Google API or a PSC Producer Service Attachment.
An example value is: asia-northeast3-cloudkms.googleapis.com.

Optional. Only valid when networkEndpointType isPRIVATE_SERVICE_CONNECT. |
| `serverless_deployment` | String | Only valid when networkEndpointType isSERVERLESS. Only one of cloudRun,appEngine, cloudFunction orserverlessDeployment may be set. |
| `kind` | String | [Output Only] Type of the resource. Alwayscompute#networkEndpointGroup for network endpoint group. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `app_engine` | String | Optional. Only valid when networkEndpointType isSERVERLESS. Only one of cloudRun,appEngine or cloudFunction may be set. |
| `zone` | String | [Output Only] The URL of thezone
where the network endpoint group is located. |
| `cloud_function` | String | Optional. Only valid when networkEndpointType isSERVERLESS. Only one of cloudRun,appEngine or cloudFunction may be set. |
| `network_endpoint_type` | String | Type of network endpoints in this network endpoint group. Can be one ofGCE_VM_IP, GCE_VM_IP_PORT,NON_GCP_PRIVATE_IP_PORT, INTERNET_FQDN_PORT,INTERNET_IP_PORT, SERVERLESS,PRIVATE_SERVICE_CONNECT, GCE_VM_IP_PORTMAP. |
| `load_balancer` | String | This field is only valid when the network endpoint group is used for load
balancing.
[Deprecated] This field is deprecated. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `name` | String | Name of the resource; provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must be
a dash, lowercase letter, or digit, except the last character, which cannot
be a dash. |
| `psc_data` | String | Optional. Only valid when networkEndpointType isPRIVATE_SERVICE_CONNECT. |
| `cloud_run` | String | Optional. Only valid when networkEndpointType isSERVERLESS. Only one of cloudRun,appEngine or cloudFunction may be set. |
| `region` | String | [Output Only] The URL of theregion
where the network endpoint group is located. |
| `network` | String | The URL of the network to which all network endpoints in the NEG belong.
Uses default project network if unspecified. |
| `annotations` | HashMap<String, String> | Optional. Metadata defined as annotations on the network endpoint group. |
| `default_port` | i64 | The default port used if the port number is not specified in the network
endpoint.

Optional. If the network endpoint type is either GCE_VM_IP,SERVERLESS or PRIVATE_SERVICE_CONNECT, this
field must not be specified. |
| `size` | i64 | [Output only] Number of network endpoints in the network endpoint group. |
| `subnetwork` | String | Optional URL of the subnetwork to which all network endpoints in the NEG
belong. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create region_network_endpoint_group
region_network_endpoint_group = provider.compute_api.Region_network_endpoint_group {
    project = "value"  # Project ID for this request.
    region = "value"  # The name of the region where
you want to create the network endpoint group. It should comply with
RFC1035.
}

# Access region_network_endpoint_group outputs
region_network_endpoint_group_id = region_network_endpoint_group.id
region_network_endpoint_group_psc_target_service = region_network_endpoint_group.psc_target_service
region_network_endpoint_group_serverless_deployment = region_network_endpoint_group.serverless_deployment
region_network_endpoint_group_kind = region_network_endpoint_group.kind
region_network_endpoint_group_creation_timestamp = region_network_endpoint_group.creation_timestamp
region_network_endpoint_group_description = region_network_endpoint_group.description
region_network_endpoint_group_app_engine = region_network_endpoint_group.app_engine
region_network_endpoint_group_zone = region_network_endpoint_group.zone
region_network_endpoint_group_cloud_function = region_network_endpoint_group.cloud_function
region_network_endpoint_group_network_endpoint_type = region_network_endpoint_group.network_endpoint_type
region_network_endpoint_group_load_balancer = region_network_endpoint_group.load_balancer
region_network_endpoint_group_id = region_network_endpoint_group.id
region_network_endpoint_group_name = region_network_endpoint_group.name
region_network_endpoint_group_psc_data = region_network_endpoint_group.psc_data
region_network_endpoint_group_cloud_run = region_network_endpoint_group.cloud_run
region_network_endpoint_group_region = region_network_endpoint_group.region
region_network_endpoint_group_network = region_network_endpoint_group.network
region_network_endpoint_group_annotations = region_network_endpoint_group.annotations
region_network_endpoint_group_default_port = region_network_endpoint_group.default_port
region_network_endpoint_group_size = region_network_endpoint_group.size
region_network_endpoint_group_subnetwork = region_network_endpoint_group.subnetwork
region_network_endpoint_group_self_link = region_network_endpoint_group.self_link
```

---


### Region_network_policie

Creates a new policy in the specified project using the data included in
the request.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `name` | String |  | Name of the resource. |
| `kind` | String |  | [Output only] Type of the resource. Alwayscompute#networkPolicy for network policies |
| `region` | String |  | [Output Only] URL of the region where the regional network policy resides.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `self_link_with_id` | String |  | [Output Only] Server-defined URL for this resource with the resource id. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `associations` | Vec<String> |  | [Output Only] A list of associations that belong to this network policy. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `rule_tuple_count` | i64 |  | [Output Only] Total count of all network policy rule tuples. A network
policy can not exceed a set number of tuples. |
| `traffic_classification_rules` | Vec<String> |  | [Output Only] A list of traffic classification rules that belong to this
policy. |
| `region` | String | ✅ | Name of the region of this request. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `name` | String | Name of the resource. |
| `kind` | String | [Output only] Type of the resource. Alwayscompute#networkPolicy for network policies |
| `region` | String | [Output Only] URL of the region where the regional network policy resides.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `self_link_with_id` | String | [Output Only] Server-defined URL for this resource with the resource id. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `associations` | Vec<String> | [Output Only] A list of associations that belong to this network policy. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `rule_tuple_count` | i64 | [Output Only] Total count of all network policy rule tuples. A network
policy can not exceed a set number of tuples. |
| `traffic_classification_rules` | Vec<String> | [Output Only] A list of traffic classification rules that belong to this
policy. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create region_network_policie
region_network_policie = provider.compute_api.Region_network_policie {
    region = "value"  # Name of the region of this request.
    project = "value"  # Project ID for this request.
}

# Access region_network_policie outputs
region_network_policie_id = region_network_policie.id
region_network_policie_name = region_network_policie.name
region_network_policie_kind = region_network_policie.kind
region_network_policie_region = region_network_policie.region
region_network_policie_id = region_network_policie.id
region_network_policie_self_link_with_id = region_network_policie.self_link_with_id
region_network_policie_description = region_network_policie.description
region_network_policie_associations = region_network_policie.associations
region_network_policie_self_link = region_network_policie.self_link
region_network_policie_creation_timestamp = region_network_policie.creation_timestamp
region_network_policie_rule_tuple_count = region_network_policie.rule_tuple_count
region_network_policie_traffic_classification_rules = region_network_policie.traffic_classification_rules
```

---


### Region_health_source

Create a HealthSource in the specified project in the given region
using the parameters that are included in the request.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `fingerprint` | String |  | Fingerprint of this resource. A hash of the contents stored in this object.
This field is used in optimistic locking. This field will be ignored when
inserting a HealthSource. An up-to-date fingerprint
must be provided in order to patch the HealthSource; Otherwise, the request
will fail with error 412 conditionNotMet. To see the latest
fingerprint, make a get() request to retrieve the
HealthSource. |
| `health_aggregation_policy` | String |  | URL to the HealthAggregationPolicy resource. Must be set. Must
be regional and in the same region as the HealthSource. Can be
mutated. |
| `id` | String |  | [Output Only] A unique identifier for this resource type. The server
generates this identifier. |
| `kind` | String |  | [Output Only] Type of the resource. Alwayscompute#healthSource for health sources. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `region` | String |  | [Output Only] URL of the region where the health source resides.
This field applies only to the regional resource. You must specify this
field as part of the HTTP request URL. It is not settable as a field in
the request body. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `name` | String |  | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `source_type` | String |  | Specifies the type of the HealthSource. The only allowed value
is BACKEND_SERVICE. Must be specified when theHealthSource is created, and cannot be mutated. |
| `self_link_with_id` | String |  | [Output Only] Server-defined URL with id for the resource. |
| `sources` | Vec<String> |  | URLs to the source resources. Must be size 1. Must be aBackendService if
the sourceType is BACKEND_SERVICE. TheBackendService must have load balancing schemeINTERNAL or INTERNAL_MANAGED and must be regional
and in the same region as the HealthSource (cross-region
deployment for INTERNAL_MANAGED is not supported). TheBackendService may use only IGs, MIGs, or NEGs of typeGCE_VM_IP or GCE_VM_IP_PORT. TheBackendService may not use haPolicy. Can be
mutated. |
| `project` | String | ✅ | Project ID for this request. |
| `region` | String | ✅ | Name of the region scoping this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `fingerprint` | String | Fingerprint of this resource. A hash of the contents stored in this object.
This field is used in optimistic locking. This field will be ignored when
inserting a HealthSource. An up-to-date fingerprint
must be provided in order to patch the HealthSource; Otherwise, the request
will fail with error 412 conditionNotMet. To see the latest
fingerprint, make a get() request to retrieve the
HealthSource. |
| `health_aggregation_policy` | String | URL to the HealthAggregationPolicy resource. Must be set. Must
be regional and in the same region as the HealthSource. Can be
mutated. |
| `id` | String | [Output Only] A unique identifier for this resource type. The server
generates this identifier. |
| `kind` | String | [Output Only] Type of the resource. Alwayscompute#healthSource for health sources. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `region` | String | [Output Only] URL of the region where the health source resides.
This field applies only to the regional resource. You must specify this
field as part of the HTTP request URL. It is not settable as a field in
the request body. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `name` | String | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `source_type` | String | Specifies the type of the HealthSource. The only allowed value
is BACKEND_SERVICE. Must be specified when theHealthSource is created, and cannot be mutated. |
| `self_link_with_id` | String | [Output Only] Server-defined URL with id for the resource. |
| `sources` | Vec<String> | URLs to the source resources. Must be size 1. Must be aBackendService if
the sourceType is BACKEND_SERVICE. TheBackendService must have load balancing schemeINTERNAL or INTERNAL_MANAGED and must be regional
and in the same region as the HealthSource (cross-region
deployment for INTERNAL_MANAGED is not supported). TheBackendService may use only IGs, MIGs, or NEGs of typeGCE_VM_IP or GCE_VM_IP_PORT. TheBackendService may not use haPolicy. Can be
mutated. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create region_health_source
region_health_source = provider.compute_api.Region_health_source {
    project = "value"  # Project ID for this request.
    region = "value"  # Name of the region scoping this request.
}

# Access region_health_source outputs
region_health_source_id = region_health_source.id
region_health_source_fingerprint = region_health_source.fingerprint
region_health_source_health_aggregation_policy = region_health_source.health_aggregation_policy
region_health_source_id = region_health_source.id
region_health_source_kind = region_health_source.kind
region_health_source_self_link = region_health_source.self_link
region_health_source_region = region_health_source.region
region_health_source_description = region_health_source.description
region_health_source_name = region_health_source.name
region_health_source_creation_timestamp = region_health_source.creation_timestamp
region_health_source_source_type = region_health_source.source_type
region_health_source_self_link_with_id = region_health_source.self_link_with_id
region_health_source_sources = region_health_source.sources
```

---


### Target_tcp_proxie

Creates a TargetTcpProxy resource in the specified project using
the data included in the request.

**Operations**: ✅ Create ✅ Read ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `service` | String |  | URL to the BackendService resource. |
| `kind` | String |  | [Output Only] Type of the resource. Alwayscompute#targetTcpProxy for target TCP proxies. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `proxy_header` | String |  | Specifies the type of proxy header to append before sending data to the
backend, either NONE or PROXY_V1. The default
is NONE. |
| `name` | String |  | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `proxy_bind` | bool |  | This field only applies when the forwarding rule that references this
target proxy has a loadBalancingScheme set toINTERNAL_SELF_MANAGED.

When this field is set to true, Envoy proxies set up inbound
traffic interception and bind to the IP address and port specified in the
forwarding rule. This is generally useful when using Traffic Director to
configure Envoy as a gateway or middle proxy (in other words, not a
sidecar proxy). The Envoy proxy listens for inbound requests and handles
requests when it receives them.

The default is false. |
| `region` | String |  | [Output Only] URL of the region where the regional TCP proxy resides.
This field is not applicable to global TCP proxy. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `service` | String | URL to the BackendService resource. |
| `kind` | String | [Output Only] Type of the resource. Alwayscompute#targetTcpProxy for target TCP proxies. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `proxy_header` | String | Specifies the type of proxy header to append before sending data to the
backend, either NONE or PROXY_V1. The default
is NONE. |
| `name` | String | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `proxy_bind` | bool | This field only applies when the forwarding rule that references this
target proxy has a loadBalancingScheme set toINTERNAL_SELF_MANAGED.

When this field is set to true, Envoy proxies set up inbound
traffic interception and bind to the IP address and port specified in the
forwarding rule. This is generally useful when using Traffic Director to
configure Envoy as a gateway or middle proxy (in other words, not a
sidecar proxy). The Envoy proxy listens for inbound requests and handles
requests when it receives them.

The default is false. |
| `region` | String | [Output Only] URL of the region where the regional TCP proxy resides.
This field is not applicable to global TCP proxy. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create target_tcp_proxie
target_tcp_proxie = provider.compute_api.Target_tcp_proxie {
    project = "value"  # Project ID for this request.
}

# Access target_tcp_proxie outputs
target_tcp_proxie_id = target_tcp_proxie.id
target_tcp_proxie_service = target_tcp_proxie.service
target_tcp_proxie_kind = target_tcp_proxie.kind
target_tcp_proxie_self_link = target_tcp_proxie.self_link
target_tcp_proxie_proxy_header = target_tcp_proxie.proxy_header
target_tcp_proxie_name = target_tcp_proxie.name
target_tcp_proxie_id = target_tcp_proxie.id
target_tcp_proxie_creation_timestamp = target_tcp_proxie.creation_timestamp
target_tcp_proxie_proxy_bind = target_tcp_proxie.proxy_bind
target_tcp_proxie_region = target_tcp_proxie.region
target_tcp_proxie_description = target_tcp_proxie.description
```

---


### Global_organization_operation

Retrieves the specified Operations resource. Gets a list of operations
by making a `list()` request.

**Operations**: ✅ Read ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `start_time` | String | [Output Only] The time that this operation was started by the server.
This value is inRFC3339
text format. |
| `http_error_message` | String | [Output Only] If the operation fails, this field contains the HTTP error
message that was returned, such as `NOT FOUND`. |
| `id` | String | [Output Only] The unique identifier for the operation. This identifier is
defined by the server. |
| `kind` | String | [Output Only] Type of the resource. Always `compute#operation` for
Operation resources. |
| `error` | String | [Output Only] If errors are generated during processing of the operation,
this field will be populated. |
| `description` | String | [Output Only] A textual description of the operation, which is
set when the operation is created. |
| `status_message` | String | [Output Only] An optional textual description of the current status of the
operation. |
| `warnings` | Vec<String> | [Output Only] If warning messages are generated during processing of the
operation, this field will be populated. |
| `creation_timestamp` | String | [Deprecated] This field is deprecated. |
| `zone` | String | [Output Only] The URL of the zone where the operation resides. Only
applicable when performing per-zone operations. |
| `user` | String | [Output Only] User who requested the operation, for example:
`user@example.com` or
`alice_smith_identifier (global/workforcePools/example-com-us-employees)`. |
| `name` | String | [Output Only] Name of the operation. |
| `operation_group_id` | String | [Output Only] An ID that represents a group of operations, such as when a
group of operations results from a `bulkInsert` API request. |
| `client_operation_id` | String | [Output Only] The value of `requestId` if you provided it in the request.
Not present otherwise. |
| `http_error_status_code` | i64 | [Output Only] If the operation fails, this field contains the HTTP error
status code that was returned. For example, a `404` means the
resource was not found. |
| `progress` | i64 | [Output Only] An optional progress indicator that ranges from 0 to 100.
There is no requirement that this be linear or support any granularity of
operations. This should not be used to guess when the operation will be
complete. This number should monotonically increase as the operation
progresses. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `status` | String | [Output Only] The status of the operation, which can be one of the
following:
`PENDING`, `RUNNING`, or `DONE`. |
| `target_link` | String | [Output Only] The URL of the resource that the operation modifies. For
operations related to creating a snapshot, this points to the disk
that the snapshot was created from. |
| `set_common_instance_metadata_operation_metadata` | String | [Output Only] If the operation is for projects.setCommonInstanceMetadata,
this field will contain information on all underlying zonal actions and
their state. |
| `target_id` | String | [Output Only] The unique target ID, which identifies a specific incarnation
of the target resource. |
| `instances_bulk_insert_operation_metadata` | String |  |
| `insert_time` | String | [Output Only] The time that this operation was requested.
This value is inRFC3339
text format. |
| `operation_type` | String | [Output Only] The type of operation, such as `insert`,
`update`, or `delete`, and so on. |
| `region` | String | [Output Only] The URL of the region where the operation resides. Only
applicable when performing regional operations. |
| `end_time` | String | [Output Only] The time that this operation was completed. This value is inRFC3339
text format. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Access global_organization_operation outputs
global_organization_operation_id = global_organization_operation.id
global_organization_operation_start_time = global_organization_operation.start_time
global_organization_operation_http_error_message = global_organization_operation.http_error_message
global_organization_operation_id = global_organization_operation.id
global_organization_operation_kind = global_organization_operation.kind
global_organization_operation_error = global_organization_operation.error
global_organization_operation_description = global_organization_operation.description
global_organization_operation_status_message = global_organization_operation.status_message
global_organization_operation_warnings = global_organization_operation.warnings
global_organization_operation_creation_timestamp = global_organization_operation.creation_timestamp
global_organization_operation_zone = global_organization_operation.zone
global_organization_operation_user = global_organization_operation.user
global_organization_operation_name = global_organization_operation.name
global_organization_operation_operation_group_id = global_organization_operation.operation_group_id
global_organization_operation_client_operation_id = global_organization_operation.client_operation_id
global_organization_operation_http_error_status_code = global_organization_operation.http_error_status_code
global_organization_operation_progress = global_organization_operation.progress
global_organization_operation_self_link = global_organization_operation.self_link
global_organization_operation_status = global_organization_operation.status
global_organization_operation_target_link = global_organization_operation.target_link
global_organization_operation_set_common_instance_metadata_operation_metadata = global_organization_operation.set_common_instance_metadata_operation_metadata
global_organization_operation_target_id = global_organization_operation.target_id
global_organization_operation_instances_bulk_insert_operation_metadata = global_organization_operation.instances_bulk_insert_operation_metadata
global_organization_operation_insert_time = global_organization_operation.insert_time
global_organization_operation_operation_type = global_organization_operation.operation_type
global_organization_operation_region = global_organization_operation.region
global_organization_operation_end_time = global_organization_operation.end_time
```

---


### Disk

Creates a persistent disk in the specified project using the data
in the request. You can create a disk from a source
(sourceImage, sourceSnapshot, orsourceDisk) or create an empty 500 GB data disk by
omitting all properties. You can also create a disk that is larger than
the default size by specifying the sizeGb property.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `last_detach_timestamp` | String |  | [Output Only] Last detach timestamp inRFC3339
text format. |
| `source_image` | String |  | The source image used to create this disk. If the source image is
deleted, this field will not be set.

To create a disk with one of the public operating system images, specify
the image by its family name. For example, specifyfamily/debian-9 to use the latest Debian 9 image:

projects/debian-cloud/global/images/family/debian-9


Alternatively, use a specific version of a public operating system image:

projects/debian-cloud/global/images/debian-9-stretch-vYYYYMMDD


To create a disk with a custom image that you created, specify the
image name in the following format:

global/images/my-custom-image


You can also specify a custom image by its image family, which returns
the latest version of the image in that family. Replace the image name
with family/family-name:

global/images/family/my-image-family |
| `storage_type` | String |  | [Deprecated] Storage type of the persistent disk. |
| `erase_windows_vss_signature` | bool |  | Specifies whether the disk restored from a source snapshot should erase
Windows specific VSS signature. |
| `type` | String |  | URL of the disk type resource describing which disk type to use to create
the disk. Provide this when creating the disk. For example:projects/project/zones/zone/diskTypes/pd-ssd. See Persistent disk
types. |
| `user_licenses` | Vec<String> |  | A list of publicly visible user-licenses. Unlike regular licenses, user
provided licenses can be modified after the disk is created. This includes
a list of URLs to the license resource. For example, to provide a debian
license:

https://www.googleapis.com/compute/v1/projects/debian-cloud/global/licenses/debian-9-stretch |
| `zone` | String |  | [Output Only] URL of the zone where the disk resides.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `async_secondary_disks` | HashMap<String, String> |  | [Output Only] A list of disks this disk is asynchronously replicated to. |
| `params` | String |  | Input only. [Input Only] Additional params passed with the request, but not persisted
as part of resource payload. |
| `source_snapshot_id` | String |  | [Output Only] The unique ID of the snapshot used to create this disk. This
value identifies the exact snapshot that was used to create this persistent
disk. For example, if you created the persistent disk from a snapshot that
was later deleted and recreated under the same name, the source snapshot ID
would identify the exact version of the snapshot that was used. |
| `interface` | String |  | [Deprecated] Specifies the disk interface to use for attaching this disk,
which is either SCSI or NVME. The default isSCSI. |
| `provisioned_throughput` | String |  | Indicates how much throughput to provision for the disk. This sets the
number of throughput mb per second that the disk can handle. Values must be
greater than or equal to 1. |
| `guest_os_features` | Vec<String> |  | A list of features to enable on the guest operating system. Applicable
only for bootable images. Read
Enabling guest operating system features to see a list of available
options. |
| `last_attach_timestamp` | String |  | [Output Only] Last attach timestamp inRFC3339
text format. |
| `self_link` | String |  | [Output Only] Server-defined fully-qualified URL for this resource. |
| `source_image_encryption_key` | String |  | Thecustomer-supplied
encryption key of the source image. Required if the source image is
protected by a customer-supplied encryption key. |
| `source_instant_snapshot` | String |  | The source instant snapshot used to create this disk. You can provide
this as a partial or full URL to the resource. For example, the following
are valid values:
   
   
     - https://www.googleapis.com/compute/v1/projects/project/zones/zone/instantSnapshots/instantSnapshot
     - projects/project/zones/zone/instantSnapshots/instantSnapshot 
   - zones/zone/instantSnapshots/instantSnapshot |
| `status` | String |  | [Output Only] The status of disk creation.
   
   
     - CREATING: Disk is provisioning.
     - RESTORING: Source data is being copied into the
     disk.
     - FAILED: Disk creation failed.
     - READY: Disk is ready for use.
     - DELETING: Disk is deleting. |
| `multi_writer` | bool |  | Indicates whether or not the disk can be read/write attached to
more than one instance. |
| `source_consistency_group_policy_id` | String |  | [Output Only] ID of the DiskConsistencyGroupPolicy for a secondary disk
that was created using a consistency group. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `licenses` | Vec<String> |  | A list of publicly visible licenses. Reserved for Google's use. |
| `options` | String |  | Internal use only. |
| `provisioned_iops` | String |  | Indicates how many IOPS to provision for the disk. This sets the number
of I/O operations per second that the disk can handle. Values must be
between 10,000 and 120,000. For more details, see theExtreme persistent
disk documentation. |
| `resource_status` | String |  | [Output Only] Status information for the disk resource. |
| `physical_block_size_bytes` | String |  | Physical block size of the persistent disk, in bytes.
If not present in a request, a default value is used.
The currently supported size is 4096, other sizes may be added in
the future.
If an unsupported value is requested, the error message will list
the supported values for the caller's project. |
| `region` | String |  | [Output Only] URL of the region where the disk resides. Only applicable for
regional resources.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `satisfies_pzi` | bool |  | Output only. Reserved for future use. |
| `source_consistency_group_policy` | String |  | [Output Only] URL of the DiskConsistencyGroupPolicy for a secondary disk
that was created using a consistency group. |
| `source_disk` | String |  | The source disk used to create this disk. You can provide this as a
partial or full URL to the resource. For example, the following are valid
values:
   
   
     - 
       https://www.googleapis.com/compute/v1/projects/project/zones/zone/disks/disk
     
     - 
       https://www.googleapis.com/compute/v1/projects/project/regions/region/disks/disk
     
     - 
       projects/project/zones/zone/disks/disk
     
     - 
       projects/project/regions/region/disks/disk
     
     - 
       zones/zone/disks/disk
     
     - 
       regions/region/disks/disk |
| `source_snapshot` | String |  | The source snapshot used to create this disk. You can provide this as a
partial or full URL to the resource. For example, the following are valid
values:
   
   
     - https://www.googleapis.com/compute/v1/projects/project/global/snapshots/snapshot 
   - projects/project/global/snapshots/snapshot
     - global/snapshots/snapshot |
| `name` | String |  | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?`
which means the first character must be a lowercase letter, and all
following characters must be a dash, lowercase letter, or digit, except
the last character, which cannot be a dash. |
| `source_snapshot_encryption_key` | String |  | Thecustomer-supplied
encryption key of the source snapshot. Required if the source snapshot
is protected by a customer-supplied encryption key. |
| `users` | Vec<String> |  | [Output Only] Links to the users of the disk (attached instances)
in form:projects/project/zones/zone/instances/instance |
| `satisfies_pzs` | bool |  | [Output Only] Reserved for future use. |
| `license_codes` | Vec<String> |  | Integer license codes indicating which licenses are attached to this disk. |
| `label_fingerprint` | String |  | A fingerprint for the labels being applied to this disk, which is
essentially a hash of the labels set used for optimistic locking. The
fingerprint is initially generated by Compute Engine and changes after
every request to modify or update labels. You must always provide an
up-to-date fingerprint hash in order to update or change labels,
otherwise the request will fail with error412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve a disk. |
| `enable_confidential_compute` | bool |  | Whether this disk is using confidential compute mode. |
| `resource_policies` | Vec<String> |  | Resource policies applied to this disk for automatic snapshot creations. |
| `disk_encryption_key` | String |  | Encrypts the disk using a
customer-supplied encryption key or a 
customer-managed encryption key.

Encryption keys do not protect access to metadata of the disk.

After you encrypt a disk with a customer-supplied key, you must provide the
same key if you use the disk later. For example, to create a disk snapshot,
to create a disk image, to create a machine image, or to attach the disk to
a virtual machine.

After you encrypt a disk with a customer-managed key, thediskEncryptionKey.kmsKeyName is set to a key *version*
name once the disk is created. The disk is encrypted with this version of
the key. In the response, diskEncryptionKey.kmsKeyName appears
in the following format:

"diskEncryptionKey.kmsKeyName":
"projects/kms_project_id/locations/region/keyRings/
key_region/cryptoKeys/key
/cryptoKeysVersions/version

If you do not provide an encryption key when creating the disk, then the 
disk is encrypted using an automatically generated key and you don't need 
to provide a key to use the disk later. |
| `size_gb` | String |  | Size, in GB, of the persistent disk. You can specify
this field when creating a persistent disk using thesourceImage, sourceSnapshot, orsourceDisk parameter, or specify it alone to create an empty
persistent disk.

If you specify this field along with a source, the value ofsizeGb must not be less than the size of the
source.
Acceptable values are greater than 0. |
| `source_disk_id` | String |  | [Output Only] The unique ID of the disk used to create this disk. This
value identifies the exact disk that was used to create this persistent
disk. For example, if you created the persistent disk from a disk that
was later deleted and recreated under the same name, the source disk ID
would identify the exact version of the disk that was used. |
| `access_mode` | String |  | The access mode of the disk.
   
   
     - READ_WRITE_SINGLE: The default AccessMode, means the
     disk can be attached to single instance in RW mode.
     - READ_WRITE_MANY: The AccessMode means the disk can be
     attached to multiple instances in RW mode.
     - READ_ONLY_MANY: The AccessMode means the disk can be
     attached to multiple instances in RO mode.


The AccessMode is only valid for Hyperdisk disk types. |
| `storage_pool` | String |  | The storage pool in which the new disk is created. You can provide
this as a partial or full URL to the resource. For example, the following
are valid values:
   
   
     - https://www.googleapis.com/compute/v1/projects/project/zones/zone/storagePools/storagePool
     - projects/project/zones/zone/storagePools/storagePool 
   - zones/zone/storagePools/storagePool |
| `architecture` | String |  | The architecture of the disk. Valid values are
ARM64 or X86_64. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `location_hint` | String |  | An opaque location hint used to place the disk close to other resources.
This field is for use by internal tools that use the public API. |
| `source_image_id` | String |  | [Output Only] The ID value of the image used to create this disk. This
value identifies the exact image that was used to create this persistent
disk. For example, if you created the persistent disk from an image that
was later deleted and recreated under the same name, the source image ID
would identify the exact version of the image that was used. |
| `source_storage_object` | String |  | The full Google Cloud Storage URI where the disk image is stored. This file
must be a gzip-compressed tarball whose name ends in .tar.gz or virtual
machine disk whose name ends in vmdk. Valid URIs may start with gs:// or
https://storage.googleapis.com/. This flag is not optimized for creating
multiple disks from a source storage object. To create many disks from a
source storage object, use gcloud compute images
import instead. |
| `async_primary_disk` | String |  | Disk asynchronously replicated into this disk. |
| `replica_zones` | Vec<String> |  | URLs of the zones where the disk should be replicated to. Only applicable
for regional resources. |
| `kind` | String |  | [Output Only] Type of the resource. Always compute#disk for
disks. |
| `source_instant_snapshot_id` | String |  | [Output Only] The unique ID of the instant snapshot used to create this
disk. This value identifies the exact instant snapshot that was used to
create this persistent disk. For example, if you created the persistent
disk from an instant snapshot that was later deleted and recreated under
the same name, the source instant snapshot ID would identify the exact
version of the instant snapshot that was used. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `labels` | HashMap<String, String> |  | Labels to apply to this disk. These can be later modified by
the setLabels method. |
| `locked` | bool |  | [Output Only] The field indicates if the disk is created from a locked
source image. Attachment of a disk created from a locked source image will
cause the following operations to become irreversibly prohibited:
   
   - R/W or R/O disk attachment to any other instance
   - Disk detachment. And the disk can only be deleted when the instance
   is deleted
   - Creation of images or snapshots
   - Disk cloning



Furthermore, the instance with at least one disk with locked flag set to
true will be prohibited from performing the operations below:
   
   - Further attachment of secondary disks.
   - Detachment of any disks
   - Create machine images
   - Create instance template
   - Delete the instance with --keep-disk parameter set to true for locked
   disks
   - Attach a locked disk with --auto-delete parameter set to false |
| `project` | String | ✅ | Project ID for this request. |
| `zone` | String | ✅ | The name of the zone for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `last_detach_timestamp` | String | [Output Only] Last detach timestamp inRFC3339
text format. |
| `source_image` | String | The source image used to create this disk. If the source image is
deleted, this field will not be set.

To create a disk with one of the public operating system images, specify
the image by its family name. For example, specifyfamily/debian-9 to use the latest Debian 9 image:

projects/debian-cloud/global/images/family/debian-9


Alternatively, use a specific version of a public operating system image:

projects/debian-cloud/global/images/debian-9-stretch-vYYYYMMDD


To create a disk with a custom image that you created, specify the
image name in the following format:

global/images/my-custom-image


You can also specify a custom image by its image family, which returns
the latest version of the image in that family. Replace the image name
with family/family-name:

global/images/family/my-image-family |
| `storage_type` | String | [Deprecated] Storage type of the persistent disk. |
| `erase_windows_vss_signature` | bool | Specifies whether the disk restored from a source snapshot should erase
Windows specific VSS signature. |
| `type` | String | URL of the disk type resource describing which disk type to use to create
the disk. Provide this when creating the disk. For example:projects/project/zones/zone/diskTypes/pd-ssd. See Persistent disk
types. |
| `user_licenses` | Vec<String> | A list of publicly visible user-licenses. Unlike regular licenses, user
provided licenses can be modified after the disk is created. This includes
a list of URLs to the license resource. For example, to provide a debian
license:

https://www.googleapis.com/compute/v1/projects/debian-cloud/global/licenses/debian-9-stretch |
| `zone` | String | [Output Only] URL of the zone where the disk resides.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `async_secondary_disks` | HashMap<String, String> | [Output Only] A list of disks this disk is asynchronously replicated to. |
| `params` | String | Input only. [Input Only] Additional params passed with the request, but not persisted
as part of resource payload. |
| `source_snapshot_id` | String | [Output Only] The unique ID of the snapshot used to create this disk. This
value identifies the exact snapshot that was used to create this persistent
disk. For example, if you created the persistent disk from a snapshot that
was later deleted and recreated under the same name, the source snapshot ID
would identify the exact version of the snapshot that was used. |
| `interface` | String | [Deprecated] Specifies the disk interface to use for attaching this disk,
which is either SCSI or NVME. The default isSCSI. |
| `provisioned_throughput` | String | Indicates how much throughput to provision for the disk. This sets the
number of throughput mb per second that the disk can handle. Values must be
greater than or equal to 1. |
| `guest_os_features` | Vec<String> | A list of features to enable on the guest operating system. Applicable
only for bootable images. Read
Enabling guest operating system features to see a list of available
options. |
| `last_attach_timestamp` | String | [Output Only] Last attach timestamp inRFC3339
text format. |
| `self_link` | String | [Output Only] Server-defined fully-qualified URL for this resource. |
| `source_image_encryption_key` | String | Thecustomer-supplied
encryption key of the source image. Required if the source image is
protected by a customer-supplied encryption key. |
| `source_instant_snapshot` | String | The source instant snapshot used to create this disk. You can provide
this as a partial or full URL to the resource. For example, the following
are valid values:
   
   
     - https://www.googleapis.com/compute/v1/projects/project/zones/zone/instantSnapshots/instantSnapshot
     - projects/project/zones/zone/instantSnapshots/instantSnapshot 
   - zones/zone/instantSnapshots/instantSnapshot |
| `status` | String | [Output Only] The status of disk creation.
   
   
     - CREATING: Disk is provisioning.
     - RESTORING: Source data is being copied into the
     disk.
     - FAILED: Disk creation failed.
     - READY: Disk is ready for use.
     - DELETING: Disk is deleting. |
| `multi_writer` | bool | Indicates whether or not the disk can be read/write attached to
more than one instance. |
| `source_consistency_group_policy_id` | String | [Output Only] ID of the DiskConsistencyGroupPolicy for a secondary disk
that was created using a consistency group. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `licenses` | Vec<String> | A list of publicly visible licenses. Reserved for Google's use. |
| `options` | String | Internal use only. |
| `provisioned_iops` | String | Indicates how many IOPS to provision for the disk. This sets the number
of I/O operations per second that the disk can handle. Values must be
between 10,000 and 120,000. For more details, see theExtreme persistent
disk documentation. |
| `resource_status` | String | [Output Only] Status information for the disk resource. |
| `physical_block_size_bytes` | String | Physical block size of the persistent disk, in bytes.
If not present in a request, a default value is used.
The currently supported size is 4096, other sizes may be added in
the future.
If an unsupported value is requested, the error message will list
the supported values for the caller's project. |
| `region` | String | [Output Only] URL of the region where the disk resides. Only applicable for
regional resources.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `satisfies_pzi` | bool | Output only. Reserved for future use. |
| `source_consistency_group_policy` | String | [Output Only] URL of the DiskConsistencyGroupPolicy for a secondary disk
that was created using a consistency group. |
| `source_disk` | String | The source disk used to create this disk. You can provide this as a
partial or full URL to the resource. For example, the following are valid
values:
   
   
     - 
       https://www.googleapis.com/compute/v1/projects/project/zones/zone/disks/disk
     
     - 
       https://www.googleapis.com/compute/v1/projects/project/regions/region/disks/disk
     
     - 
       projects/project/zones/zone/disks/disk
     
     - 
       projects/project/regions/region/disks/disk
     
     - 
       zones/zone/disks/disk
     
     - 
       regions/region/disks/disk |
| `source_snapshot` | String | The source snapshot used to create this disk. You can provide this as a
partial or full URL to the resource. For example, the following are valid
values:
   
   
     - https://www.googleapis.com/compute/v1/projects/project/global/snapshots/snapshot 
   - projects/project/global/snapshots/snapshot
     - global/snapshots/snapshot |
| `name` | String | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?`
which means the first character must be a lowercase letter, and all
following characters must be a dash, lowercase letter, or digit, except
the last character, which cannot be a dash. |
| `source_snapshot_encryption_key` | String | Thecustomer-supplied
encryption key of the source snapshot. Required if the source snapshot
is protected by a customer-supplied encryption key. |
| `users` | Vec<String> | [Output Only] Links to the users of the disk (attached instances)
in form:projects/project/zones/zone/instances/instance |
| `satisfies_pzs` | bool | [Output Only] Reserved for future use. |
| `license_codes` | Vec<String> | Integer license codes indicating which licenses are attached to this disk. |
| `label_fingerprint` | String | A fingerprint for the labels being applied to this disk, which is
essentially a hash of the labels set used for optimistic locking. The
fingerprint is initially generated by Compute Engine and changes after
every request to modify or update labels. You must always provide an
up-to-date fingerprint hash in order to update or change labels,
otherwise the request will fail with error412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve a disk. |
| `enable_confidential_compute` | bool | Whether this disk is using confidential compute mode. |
| `resource_policies` | Vec<String> | Resource policies applied to this disk for automatic snapshot creations. |
| `disk_encryption_key` | String | Encrypts the disk using a
customer-supplied encryption key or a 
customer-managed encryption key.

Encryption keys do not protect access to metadata of the disk.

After you encrypt a disk with a customer-supplied key, you must provide the
same key if you use the disk later. For example, to create a disk snapshot,
to create a disk image, to create a machine image, or to attach the disk to
a virtual machine.

After you encrypt a disk with a customer-managed key, thediskEncryptionKey.kmsKeyName is set to a key *version*
name once the disk is created. The disk is encrypted with this version of
the key. In the response, diskEncryptionKey.kmsKeyName appears
in the following format:

"diskEncryptionKey.kmsKeyName":
"projects/kms_project_id/locations/region/keyRings/
key_region/cryptoKeys/key
/cryptoKeysVersions/version

If you do not provide an encryption key when creating the disk, then the 
disk is encrypted using an automatically generated key and you don't need 
to provide a key to use the disk later. |
| `size_gb` | String | Size, in GB, of the persistent disk. You can specify
this field when creating a persistent disk using thesourceImage, sourceSnapshot, orsourceDisk parameter, or specify it alone to create an empty
persistent disk.

If you specify this field along with a source, the value ofsizeGb must not be less than the size of the
source.
Acceptable values are greater than 0. |
| `source_disk_id` | String | [Output Only] The unique ID of the disk used to create this disk. This
value identifies the exact disk that was used to create this persistent
disk. For example, if you created the persistent disk from a disk that
was later deleted and recreated under the same name, the source disk ID
would identify the exact version of the disk that was used. |
| `access_mode` | String | The access mode of the disk.
   
   
     - READ_WRITE_SINGLE: The default AccessMode, means the
     disk can be attached to single instance in RW mode.
     - READ_WRITE_MANY: The AccessMode means the disk can be
     attached to multiple instances in RW mode.
     - READ_ONLY_MANY: The AccessMode means the disk can be
     attached to multiple instances in RO mode.


The AccessMode is only valid for Hyperdisk disk types. |
| `storage_pool` | String | The storage pool in which the new disk is created. You can provide
this as a partial or full URL to the resource. For example, the following
are valid values:
   
   
     - https://www.googleapis.com/compute/v1/projects/project/zones/zone/storagePools/storagePool
     - projects/project/zones/zone/storagePools/storagePool 
   - zones/zone/storagePools/storagePool |
| `architecture` | String | The architecture of the disk. Valid values are
ARM64 or X86_64. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `location_hint` | String | An opaque location hint used to place the disk close to other resources.
This field is for use by internal tools that use the public API. |
| `source_image_id` | String | [Output Only] The ID value of the image used to create this disk. This
value identifies the exact image that was used to create this persistent
disk. For example, if you created the persistent disk from an image that
was later deleted and recreated under the same name, the source image ID
would identify the exact version of the image that was used. |
| `source_storage_object` | String | The full Google Cloud Storage URI where the disk image is stored. This file
must be a gzip-compressed tarball whose name ends in .tar.gz or virtual
machine disk whose name ends in vmdk. Valid URIs may start with gs:// or
https://storage.googleapis.com/. This flag is not optimized for creating
multiple disks from a source storage object. To create many disks from a
source storage object, use gcloud compute images
import instead. |
| `async_primary_disk` | String | Disk asynchronously replicated into this disk. |
| `replica_zones` | Vec<String> | URLs of the zones where the disk should be replicated to. Only applicable
for regional resources. |
| `kind` | String | [Output Only] Type of the resource. Always compute#disk for
disks. |
| `source_instant_snapshot_id` | String | [Output Only] The unique ID of the instant snapshot used to create this
disk. This value identifies the exact instant snapshot that was used to
create this persistent disk. For example, if you created the persistent
disk from an instant snapshot that was later deleted and recreated under
the same name, the source instant snapshot ID would identify the exact
version of the instant snapshot that was used. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `labels` | HashMap<String, String> | Labels to apply to this disk. These can be later modified by
the setLabels method. |
| `locked` | bool | [Output Only] The field indicates if the disk is created from a locked
source image. Attachment of a disk created from a locked source image will
cause the following operations to become irreversibly prohibited:
   
   - R/W or R/O disk attachment to any other instance
   - Disk detachment. And the disk can only be deleted when the instance
   is deleted
   - Creation of images or snapshots
   - Disk cloning



Furthermore, the instance with at least one disk with locked flag set to
true will be prohibited from performing the operations below:
   
   - Further attachment of secondary disks.
   - Detachment of any disks
   - Create machine images
   - Create instance template
   - Delete the instance with --keep-disk parameter set to true for locked
   disks
   - Attach a locked disk with --auto-delete parameter set to false |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create disk
disk = provider.compute_api.Disk {
    project = "value"  # Project ID for this request.
    zone = "value"  # The name of the zone for this request.
}

# Access disk outputs
disk_id = disk.id
disk_last_detach_timestamp = disk.last_detach_timestamp
disk_source_image = disk.source_image
disk_storage_type = disk.storage_type
disk_erase_windows_vss_signature = disk.erase_windows_vss_signature
disk_type = disk.type
disk_user_licenses = disk.user_licenses
disk_zone = disk.zone
disk_async_secondary_disks = disk.async_secondary_disks
disk_params = disk.params
disk_source_snapshot_id = disk.source_snapshot_id
disk_interface = disk.interface
disk_provisioned_throughput = disk.provisioned_throughput
disk_guest_os_features = disk.guest_os_features
disk_last_attach_timestamp = disk.last_attach_timestamp
disk_self_link = disk.self_link
disk_source_image_encryption_key = disk.source_image_encryption_key
disk_source_instant_snapshot = disk.source_instant_snapshot
disk_status = disk.status
disk_multi_writer = disk.multi_writer
disk_source_consistency_group_policy_id = disk.source_consistency_group_policy_id
disk_id = disk.id
disk_licenses = disk.licenses
disk_options = disk.options
disk_provisioned_iops = disk.provisioned_iops
disk_resource_status = disk.resource_status
disk_physical_block_size_bytes = disk.physical_block_size_bytes
disk_region = disk.region
disk_satisfies_pzi = disk.satisfies_pzi
disk_source_consistency_group_policy = disk.source_consistency_group_policy
disk_source_disk = disk.source_disk
disk_source_snapshot = disk.source_snapshot
disk_name = disk.name
disk_source_snapshot_encryption_key = disk.source_snapshot_encryption_key
disk_users = disk.users
disk_satisfies_pzs = disk.satisfies_pzs
disk_license_codes = disk.license_codes
disk_label_fingerprint = disk.label_fingerprint
disk_enable_confidential_compute = disk.enable_confidential_compute
disk_resource_policies = disk.resource_policies
disk_disk_encryption_key = disk.disk_encryption_key
disk_size_gb = disk.size_gb
disk_source_disk_id = disk.source_disk_id
disk_access_mode = disk.access_mode
disk_storage_pool = disk.storage_pool
disk_architecture = disk.architecture
disk_creation_timestamp = disk.creation_timestamp
disk_location_hint = disk.location_hint
disk_source_image_id = disk.source_image_id
disk_source_storage_object = disk.source_storage_object
disk_async_primary_disk = disk.async_primary_disk
disk_replica_zones = disk.replica_zones
disk_kind = disk.kind
disk_source_instant_snapshot_id = disk.source_instant_snapshot_id
disk_description = disk.description
disk_labels = disk.labels
disk_locked = disk.locked
```

---


### Region_instance_group_manager_resize_request

Creates a new Resize Request that starts provisioning VMs immediately
or queues VM creation.

**Operations**: ✅ Create ✅ Read ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `instances` | Vec<String> |  | The names of instances to be created by this resize request. The number of
names specified determines the number of instances to create. The group's
target size will be increased by this number. This field cannot be used
together with 'resize_by'. |
| `self_link_with_id` | String |  | [Output Only] Server-defined URL for this resource with the resource id. |
| `requested_run_duration` | String |  | Requested run duration for instances that will be created by this request.
At the end of the run duration instance will be deleted. |
| `id` | String |  | [Output Only] A unique identifier for this resource type. The server
generates this identifier. |
| `kind` | String |  | [Output Only] The resource type, which is alwayscompute#instanceGroupManagerResizeRequest for
resize requests. |
| `resize_by` | i64 |  | The number of instances to be created by this resize request. The group's
target size will be increased by this number. This field cannot be used
together with 'instances'. |
| `self_link` | String |  | [Output Only] The URL for this resize request. The server defines
this URL. |
| `count` | i64 |  | This field is deprecated, please use resize_by instead.
The count of instances to create as part of this resize request. |
| `status` | String |  | [Output only] Status of the request. |
| `zone` | String |  | [Output Only] The URL of azone
where the resize request is located. Populated only for zonal resize
requests. |
| `region` | String |  | [Output Only] The URL of aregion
where the resize request is located. Populated only for regional resize
requests. |
| `creation_timestamp` | String |  | [Output Only] The creation timestamp for this resize request inRFC3339
text format. |
| `name` | String |  | The name of this resize request. The name must be 1-63 characters
long, and comply withRFC1035. |
| `state` | String |  | [Output only] Current state of the request. |
| `description` | String |  | An optional description of this resource. |
| `project` | String | ✅ | Project ID for this request. |
| `instance_group_manager` | String | ✅ | Name of the managed instance group to which the resize request is scoped.
Name should conform to RFC1035 or be a resource ID. |
| `region` | String | ✅ | Name of the region
scoping this request. Name should conform to RFC1035. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `instances` | Vec<String> | The names of instances to be created by this resize request. The number of
names specified determines the number of instances to create. The group's
target size will be increased by this number. This field cannot be used
together with 'resize_by'. |
| `self_link_with_id` | String | [Output Only] Server-defined URL for this resource with the resource id. |
| `requested_run_duration` | String | Requested run duration for instances that will be created by this request.
At the end of the run duration instance will be deleted. |
| `id` | String | [Output Only] A unique identifier for this resource type. The server
generates this identifier. |
| `kind` | String | [Output Only] The resource type, which is alwayscompute#instanceGroupManagerResizeRequest for
resize requests. |
| `resize_by` | i64 | The number of instances to be created by this resize request. The group's
target size will be increased by this number. This field cannot be used
together with 'instances'. |
| `self_link` | String | [Output Only] The URL for this resize request. The server defines
this URL. |
| `count` | i64 | This field is deprecated, please use resize_by instead.
The count of instances to create as part of this resize request. |
| `status` | String | [Output only] Status of the request. |
| `zone` | String | [Output Only] The URL of azone
where the resize request is located. Populated only for zonal resize
requests. |
| `region` | String | [Output Only] The URL of aregion
where the resize request is located. Populated only for regional resize
requests. |
| `creation_timestamp` | String | [Output Only] The creation timestamp for this resize request inRFC3339
text format. |
| `name` | String | The name of this resize request. The name must be 1-63 characters
long, and comply withRFC1035. |
| `state` | String | [Output only] Current state of the request. |
| `description` | String | An optional description of this resource. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create region_instance_group_manager_resize_request
region_instance_group_manager_resize_request = provider.compute_api.Region_instance_group_manager_resize_request {
    project = "value"  # Project ID for this request.
    instance_group_manager = "value"  # Name of the managed instance group to which the resize request is scoped.
Name should conform to RFC1035 or be a resource ID.
    region = "value"  # Name of the region
scoping this request. Name should conform to RFC1035.
}

# Access region_instance_group_manager_resize_request outputs
region_instance_group_manager_resize_request_id = region_instance_group_manager_resize_request.id
region_instance_group_manager_resize_request_instances = region_instance_group_manager_resize_request.instances
region_instance_group_manager_resize_request_self_link_with_id = region_instance_group_manager_resize_request.self_link_with_id
region_instance_group_manager_resize_request_requested_run_duration = region_instance_group_manager_resize_request.requested_run_duration
region_instance_group_manager_resize_request_id = region_instance_group_manager_resize_request.id
region_instance_group_manager_resize_request_kind = region_instance_group_manager_resize_request.kind
region_instance_group_manager_resize_request_resize_by = region_instance_group_manager_resize_request.resize_by
region_instance_group_manager_resize_request_self_link = region_instance_group_manager_resize_request.self_link
region_instance_group_manager_resize_request_count = region_instance_group_manager_resize_request.count
region_instance_group_manager_resize_request_status = region_instance_group_manager_resize_request.status
region_instance_group_manager_resize_request_zone = region_instance_group_manager_resize_request.zone
region_instance_group_manager_resize_request_region = region_instance_group_manager_resize_request.region
region_instance_group_manager_resize_request_creation_timestamp = region_instance_group_manager_resize_request.creation_timestamp
region_instance_group_manager_resize_request_name = region_instance_group_manager_resize_request.name
region_instance_group_manager_resize_request_state = region_instance_group_manager_resize_request.state
region_instance_group_manager_resize_request_description = region_instance_group_manager_resize_request.description
```

---


### Region_health_check_service

Creates a regional HealthCheckService resource in the
specified project and region using the data included in the request.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `fingerprint` | String |  | Fingerprint of this resource. A hash of the contents stored in this object.
This field is used in optimistic locking. This field will be ignored when
inserting a HealthCheckService. An up-to-date fingerprint must
be provided in order to patch/update the HealthCheckService; Otherwise, the
request will fail with error 412 conditionNotMet. To see the
latest fingerprint, make a get() request to retrieve the
HealthCheckService. |
| `health_checks` | Vec<String> |  | A list of URLs to the HealthCheck resources. Must have
at least one HealthCheck, and not more than 10 for regionalHealthCheckService, and not more than 1 for globalHealthCheckService.HealthCheck resources must haveportSpecification=USE_SERVING_PORT orportSpecification=USE_FIXED_PORT. For
regional HealthCheckService, theHealthCheck must be regional and in the same
region. For global HealthCheckService,HealthCheck must be global. Mix of regional and globalHealthChecks is not supported. Multiple regionalHealthChecks must belong to the same region. RegionalHealthChecks must belong to the same region as zones ofNetworkEndpointGroups. For globalHealthCheckService using globalINTERNET_IP_PORT NetworkEndpointGroups, the
global HealthChecks must specify sourceRegions,
and HealthChecks that specify sourceRegions can
only be used with global INTERNET_IP_PORTNetworkEndpointGroups. |
| `health_status_aggregation_policy` | String |  | Optional. Policy for how the results from multiple health checks
for the same endpoint are aggregated. Defaults to NO_AGGREGATION
if unspecified.
   
   - NO_AGGREGATION. An EndpointHealth message is
   returned for each  pair in the health check
   service.
   - AND. If any health check of an endpoint reportsUNHEALTHY, then UNHEALTHY is theHealthState of the endpoint. If all health checks reportHEALTHY, the HealthState of the endpoint isHEALTHY.

.
This is only allowed with regional HealthCheckService. |
| `network_endpoint_groups` | Vec<String> |  | A list of URLs to the NetworkEndpointGroup
resources. Must not have more than 100.  For regionalHealthCheckService, NEGs must be in
zones in the region of the HealthCheckService. For globalHealthCheckServices, the NetworkEndpointGroups
must be global INTERNET_IP_PORT. |
| `region` | String |  | [Output Only] URL of the region where the health check service
resides. This field is not applicable to global health check services.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `kind` | String |  | [Output only] Type of the resource. Alwayscompute#healthCheckServicefor health check services. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `name` | String |  | Name of the resource. The name must be 1-63 characters long, and comply
with RFC1035. Specifically, the name must be 1-63 characters
long and match the regular expression
`[a-z]([-a-z0-9]*[a-z0-9])?` which means the first character
must be a lowercase letter, and all following characters must be a dash,
lowercase letter, or digit, except the last character, which cannot be a
dash. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `notification_endpoints` | Vec<String> |  | A list of URLs to the NotificationEndpoint
resources. Must not have more than 10.  A list of endpoints for
receiving notifications of change in health status. For regionalHealthCheckService,NotificationEndpoint must be regional and in the
same region. For global HealthCheckService,NotificationEndpoint must be global. |
| `health_status_aggregation_strategy` | String |  | This field is deprecated. Use health_status_aggregation_policy
instead.

Policy for how the results from multiple health checks for the
same endpoint are aggregated.
   
   - NO_AGGREGATION. An EndpointHealth
   message is returned for each backend in the health check
   service.
   - AND.  If any backend's health check reportsUNHEALTHY, then UNHEALTHY is theHealthState of the entire health check service. If
   all backend's are healthy, the HealthState of the
   health check service is HEALTHY.

. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `project` | String | ✅ | Project ID for this request. |
| `region` | String | ✅ | Name of the region scoping this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `fingerprint` | String | Fingerprint of this resource. A hash of the contents stored in this object.
This field is used in optimistic locking. This field will be ignored when
inserting a HealthCheckService. An up-to-date fingerprint must
be provided in order to patch/update the HealthCheckService; Otherwise, the
request will fail with error 412 conditionNotMet. To see the
latest fingerprint, make a get() request to retrieve the
HealthCheckService. |
| `health_checks` | Vec<String> | A list of URLs to the HealthCheck resources. Must have
at least one HealthCheck, and not more than 10 for regionalHealthCheckService, and not more than 1 for globalHealthCheckService.HealthCheck resources must haveportSpecification=USE_SERVING_PORT orportSpecification=USE_FIXED_PORT. For
regional HealthCheckService, theHealthCheck must be regional and in the same
region. For global HealthCheckService,HealthCheck must be global. Mix of regional and globalHealthChecks is not supported. Multiple regionalHealthChecks must belong to the same region. RegionalHealthChecks must belong to the same region as zones ofNetworkEndpointGroups. For globalHealthCheckService using globalINTERNET_IP_PORT NetworkEndpointGroups, the
global HealthChecks must specify sourceRegions,
and HealthChecks that specify sourceRegions can
only be used with global INTERNET_IP_PORTNetworkEndpointGroups. |
| `health_status_aggregation_policy` | String | Optional. Policy for how the results from multiple health checks
for the same endpoint are aggregated. Defaults to NO_AGGREGATION
if unspecified.
   
   - NO_AGGREGATION. An EndpointHealth message is
   returned for each  pair in the health check
   service.
   - AND. If any health check of an endpoint reportsUNHEALTHY, then UNHEALTHY is theHealthState of the endpoint. If all health checks reportHEALTHY, the HealthState of the endpoint isHEALTHY.

.
This is only allowed with regional HealthCheckService. |
| `network_endpoint_groups` | Vec<String> | A list of URLs to the NetworkEndpointGroup
resources. Must not have more than 100.  For regionalHealthCheckService, NEGs must be in
zones in the region of the HealthCheckService. For globalHealthCheckServices, the NetworkEndpointGroups
must be global INTERNET_IP_PORT. |
| `region` | String | [Output Only] URL of the region where the health check service
resides. This field is not applicable to global health check services.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `kind` | String | [Output only] Type of the resource. Alwayscompute#healthCheckServicefor health check services. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `name` | String | Name of the resource. The name must be 1-63 characters long, and comply
with RFC1035. Specifically, the name must be 1-63 characters
long and match the regular expression
`[a-z]([-a-z0-9]*[a-z0-9])?` which means the first character
must be a lowercase letter, and all following characters must be a dash,
lowercase letter, or digit, except the last character, which cannot be a
dash. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `notification_endpoints` | Vec<String> | A list of URLs to the NotificationEndpoint
resources. Must not have more than 10.  A list of endpoints for
receiving notifications of change in health status. For regionalHealthCheckService,NotificationEndpoint must be regional and in the
same region. For global HealthCheckService,NotificationEndpoint must be global. |
| `health_status_aggregation_strategy` | String | This field is deprecated. Use health_status_aggregation_policy
instead.

Policy for how the results from multiple health checks for the
same endpoint are aggregated.
   
   - NO_AGGREGATION. An EndpointHealth
   message is returned for each backend in the health check
   service.
   - AND.  If any backend's health check reportsUNHEALTHY, then UNHEALTHY is theHealthState of the entire health check service. If
   all backend's are healthy, the HealthState of the
   health check service is HEALTHY.

. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create region_health_check_service
region_health_check_service = provider.compute_api.Region_health_check_service {
    project = "value"  # Project ID for this request.
    region = "value"  # Name of the region scoping this request.
}

# Access region_health_check_service outputs
region_health_check_service_id = region_health_check_service.id
region_health_check_service_description = region_health_check_service.description
region_health_check_service_fingerprint = region_health_check_service.fingerprint
region_health_check_service_health_checks = region_health_check_service.health_checks
region_health_check_service_health_status_aggregation_policy = region_health_check_service.health_status_aggregation_policy
region_health_check_service_network_endpoint_groups = region_health_check_service.network_endpoint_groups
region_health_check_service_region = region_health_check_service.region
region_health_check_service_kind = region_health_check_service.kind
region_health_check_service_creation_timestamp = region_health_check_service.creation_timestamp
region_health_check_service_name = region_health_check_service.name
region_health_check_service_id = region_health_check_service.id
region_health_check_service_notification_endpoints = region_health_check_service.notification_endpoints
region_health_check_service_health_status_aggregation_strategy = region_health_check_service.health_status_aggregation_strategy
region_health_check_service_self_link = region_health_check_service.self_link
```

---


### Storage_pool_type

Returns the specified storage pool type.

**Operations**: ✅ Read

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `description` | String | [Output Only] An optional description of this resource. |
| `kind` | String | [Output Only] Type of the resource. Alwayscompute#storagePoolType for storage pool types. |
| `max_pool_provisioned_iops` | String | [Output Only] Maximum provisioned IOPS. |
| `min_pool_provisioned_capacity_gb` | String | [Output Only] Minimum storage pool size in GB. |
| `min_pool_provisioned_throughput` | String | [Output Only] Minimum provisioned throughput. |
| `self_link_with_id` | String | [Output Only] Server-defined URL for this resource with the resource id. |
| `name` | String | [Output Only] Name of the resource. |
| `max_pool_provisioned_throughput` | String | [Output Only] Maximum provisioned throughput. |
| `max_pool_provisioned_capacity_gb` | String | [Output Only] Maximum storage pool size in GB. |
| `zone` | String | [Output Only] URL of the zone where the storage pool type resides.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `deprecated` | String | [Output Only] The deprecation status associated with this storage pool
type. |
| `min_pool_provisioned_iops` | String | [Output Only] Minimum provisioned IOPS. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `min_size_gb` | String | [Deprecated] This field is deprecated. Use minPoolProvisionedCapacityGb
instead. |
| `supported_disk_types` | Vec<String> | [Output Only] The list of disk types supported in this storage pool type. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Access storage_pool_type outputs
storage_pool_type_id = storage_pool_type.id
storage_pool_type_id = storage_pool_type.id
storage_pool_type_description = storage_pool_type.description
storage_pool_type_kind = storage_pool_type.kind
storage_pool_type_max_pool_provisioned_iops = storage_pool_type.max_pool_provisioned_iops
storage_pool_type_min_pool_provisioned_capacity_gb = storage_pool_type.min_pool_provisioned_capacity_gb
storage_pool_type_min_pool_provisioned_throughput = storage_pool_type.min_pool_provisioned_throughput
storage_pool_type_self_link_with_id = storage_pool_type.self_link_with_id
storage_pool_type_name = storage_pool_type.name
storage_pool_type_max_pool_provisioned_throughput = storage_pool_type.max_pool_provisioned_throughput
storage_pool_type_max_pool_provisioned_capacity_gb = storage_pool_type.max_pool_provisioned_capacity_gb
storage_pool_type_zone = storage_pool_type.zone
storage_pool_type_deprecated = storage_pool_type.deprecated
storage_pool_type_min_pool_provisioned_iops = storage_pool_type.min_pool_provisioned_iops
storage_pool_type_self_link = storage_pool_type.self_link
storage_pool_type_min_size_gb = storage_pool_type.min_size_gb
storage_pool_type_supported_disk_types = storage_pool_type.supported_disk_types
storage_pool_type_creation_timestamp = storage_pool_type.creation_timestamp
```

---


### Reservation

Creates a new reservation. For more information, readReserving zonal
resources.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `name` | String |  | The name of the resource, provided by the client when initially creating
the resource. The resource name must be 1-63 characters long, and comply
withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must be
a dash, lowercase letter, or digit, except the last character, which cannot
be a dash. |
| `kind` | String |  | [Output Only] Type of the resource. Alwayscompute#reservations for reservations. |
| `self_link` | String |  | [Output Only] Server-defined fully-qualified URL for this resource. |
| `specific_reservation` | String |  | Reservation for instances with specific machine shapes. |
| `protection_tier` | String |  | Protection tier for the workload which specifies the workload expectations
in the event of infrastructure failures at data center (e.g. power
and/or cooling failures). |
| `aggregate_reservation` | String |  | Reservation for aggregated resources, providing shape flexibility. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `share_settings` | String |  | Specify share-settings to create a shared reservation. This property is
optional. For more information about the syntax and options for this
field and its subfields, see the guide for creating
a shared reservation. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `resource_policies` | HashMap<String, String> |  | Resource policies to be added to this reservation. The key is defined by
user, and the value is resource policy url. This is to define placement
policy with reservation. |
| `linked_commitments` | Vec<String> |  | [Output Only] Full or partial URL to parent commitments. This field
displays for reservations that are tied to multiple commitments. |
| `reservation_mode` | String |  | [Output only] Indicates the reservation mode of the reservation. |
| `resource_status` | String |  | [Output Only] Status information for Reservation resource. |
| `scheduling_type` | String |  | The type of maintenance for the reservation. |
| `commitment` | String |  | [Output Only] Full or partial URL to a parent commitment. This field
displays for reservations that are tied to a commitment. |
| `deployment_type` | String |  | Specifies the deployment strategy for this reservation. |
| `delete_after_duration` | String |  | Duration time relative to reservation creation when Compute Engine will
automatically delete this resource. |
| `satisfies_pzs` | bool |  | [Output Only] Reserved for future use. |
| `status` | String |  | [Output Only] The status of the reservation.
   
   
     - CREATING: Reservation resources are being
       allocated.
     - READY: Reservation resources have been allocated,
       and the reservation is ready for use.
     - DELETING: Reservation deletion is in progress.
     - UPDATING: Reservation update is in progress. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `reservation_sharing_policy` | String |  | Specify the reservation sharing policy. If unspecified, the reservation
will not be shared with Google Cloud managed services. |
| `delete_at_time` | String |  | Absolute time in future when the reservation will be
 auto-deleted by Compute Engine. Timestamp is represented inRFC3339 text format. |
| `enable_emergent_maintenance` | bool |  | Indicates whether Compute Engine allows unplanned maintenance for your VMs;
for example, to fix hardware errors. |
| `advanced_deployment_control` | String |  | Advanced control for cluster management, applicable only to DENSE
deployment type reservations. |
| `specific_reservation_required` | bool |  | Indicates whether the reservation can be consumed by VMs with affinity
for "any" reservation. If the field is set, then only VMs that target
the reservation by name can consume from this reservation. |
| `zone` | String |  | Zone in which the reservation resides. A zone must be provided if the
reservation is created within a commitment. |
| `zone` | String | ✅ | Name of the zone for this request. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `name` | String | The name of the resource, provided by the client when initially creating
the resource. The resource name must be 1-63 characters long, and comply
withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must be
a dash, lowercase letter, or digit, except the last character, which cannot
be a dash. |
| `kind` | String | [Output Only] Type of the resource. Alwayscompute#reservations for reservations. |
| `self_link` | String | [Output Only] Server-defined fully-qualified URL for this resource. |
| `specific_reservation` | String | Reservation for instances with specific machine shapes. |
| `protection_tier` | String | Protection tier for the workload which specifies the workload expectations
in the event of infrastructure failures at data center (e.g. power
and/or cooling failures). |
| `aggregate_reservation` | String | Reservation for aggregated resources, providing shape flexibility. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `share_settings` | String | Specify share-settings to create a shared reservation. This property is
optional. For more information about the syntax and options for this
field and its subfields, see the guide for creating
a shared reservation. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `resource_policies` | HashMap<String, String> | Resource policies to be added to this reservation. The key is defined by
user, and the value is resource policy url. This is to define placement
policy with reservation. |
| `linked_commitments` | Vec<String> | [Output Only] Full or partial URL to parent commitments. This field
displays for reservations that are tied to multiple commitments. |
| `reservation_mode` | String | [Output only] Indicates the reservation mode of the reservation. |
| `resource_status` | String | [Output Only] Status information for Reservation resource. |
| `scheduling_type` | String | The type of maintenance for the reservation. |
| `commitment` | String | [Output Only] Full or partial URL to a parent commitment. This field
displays for reservations that are tied to a commitment. |
| `deployment_type` | String | Specifies the deployment strategy for this reservation. |
| `delete_after_duration` | String | Duration time relative to reservation creation when Compute Engine will
automatically delete this resource. |
| `satisfies_pzs` | bool | [Output Only] Reserved for future use. |
| `status` | String | [Output Only] The status of the reservation.
   
   
     - CREATING: Reservation resources are being
       allocated.
     - READY: Reservation resources have been allocated,
       and the reservation is ready for use.
     - DELETING: Reservation deletion is in progress.
     - UPDATING: Reservation update is in progress. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `reservation_sharing_policy` | String | Specify the reservation sharing policy. If unspecified, the reservation
will not be shared with Google Cloud managed services. |
| `delete_at_time` | String | Absolute time in future when the reservation will be
 auto-deleted by Compute Engine. Timestamp is represented inRFC3339 text format. |
| `enable_emergent_maintenance` | bool | Indicates whether Compute Engine allows unplanned maintenance for your VMs;
for example, to fix hardware errors. |
| `advanced_deployment_control` | String | Advanced control for cluster management, applicable only to DENSE
deployment type reservations. |
| `specific_reservation_required` | bool | Indicates whether the reservation can be consumed by VMs with affinity
for "any" reservation. If the field is set, then only VMs that target
the reservation by name can consume from this reservation. |
| `zone` | String | Zone in which the reservation resides. A zone must be provided if the
reservation is created within a commitment. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create reservation
reservation = provider.compute_api.Reservation {
    zone = "value"  # Name of the zone for this request.
    project = "value"  # Project ID for this request.
}

# Access reservation outputs
reservation_id = reservation.id
reservation_name = reservation.name
reservation_kind = reservation.kind
reservation_self_link = reservation.self_link
reservation_specific_reservation = reservation.specific_reservation
reservation_protection_tier = reservation.protection_tier
reservation_aggregate_reservation = reservation.aggregate_reservation
reservation_id = reservation.id
reservation_share_settings = reservation.share_settings
reservation_creation_timestamp = reservation.creation_timestamp
reservation_resource_policies = reservation.resource_policies
reservation_linked_commitments = reservation.linked_commitments
reservation_reservation_mode = reservation.reservation_mode
reservation_resource_status = reservation.resource_status
reservation_scheduling_type = reservation.scheduling_type
reservation_commitment = reservation.commitment
reservation_deployment_type = reservation.deployment_type
reservation_delete_after_duration = reservation.delete_after_duration
reservation_satisfies_pzs = reservation.satisfies_pzs
reservation_status = reservation.status
reservation_description = reservation.description
reservation_reservation_sharing_policy = reservation.reservation_sharing_policy
reservation_delete_at_time = reservation.delete_at_time
reservation_enable_emergent_maintenance = reservation.enable_emergent_maintenance
reservation_advanced_deployment_control = reservation.advanced_deployment_control
reservation_specific_reservation_required = reservation.specific_reservation_required
reservation_zone = reservation.zone
```

---


### Region_backend_service

Creates a regional BackendService resource in the specified project using
the data included in the request. For more information, see
Backend services overview.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `compression_mode` | String |  | Compress text responses using Brotli or gzip compression, based on
the client's Accept-Encoding header. |
| `dynamic_forwarding` | String |  | Dynamic forwarding configuration. This field is used to configure the
backend service with dynamic forwarding feature which together with Service
Extension allows customized and complex routing logic. |
| `connection_tracking_policy` | String |  | Connection Tracking configuration for this BackendService. Connection
tracking policy settings are only available for external passthrough
Network Load Balancers and internal passthrough Network Load Balancers.

connectionTrackingPolicy cannot be specified with haPolicy. |
| `custom_response_headers` | Vec<String> |  | Headers that the load balancer adds to proxied responses. See [Creating
custom
headers](https://cloud.google.com/load-balancing/docs/custom-headers). |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `iap` | String |  | The configurations for Identity-Aware Proxy on this resource.
Not available for internal passthrough Network Load Balancers and external
passthrough Network Load Balancers. |
| `network_pass_through_lb_traffic_policy` | String |  | Configures traffic steering properties of internal passthrough Network
Load Balancers.

networkPassThroughLbTrafficPolicy cannot be specified with haPolicy. |
| `custom_metrics` | Vec<String> |  | List of custom metrics that are used for theWEIGHTED_ROUND_ROBIN locality_lb_policy. |
| `params` | String |  | Input only. [Input Only] Additional params passed with the request, but not persisted
as part of resource payload. |
| `consistent_hash` | String |  | Consistent Hash-based load balancing can be used to provide soft session
affinity based on HTTP headers, cookies or other properties. This load
balancing policy is applicable only for HTTP connections. The affinity to a
particular destination host will be lost when one or more hosts are
added/removed from the destination service. This field specifies parameters
that control consistent hashing. This field is only applicable whenlocalityLbPolicy is set to MAGLEV orRING_HASH.

This field is applicable to either:
   
   - A regional backend service with the service_protocol set to HTTP,
   HTTPS, HTTP2 or H2C, and load_balancing_scheme set to
   INTERNAL_MANAGED. 
   - A global backend service with the
   load_balancing_scheme set to INTERNAL_SELF_MANAGED. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `enable_cdn` | bool |  | If true, enables Cloud CDN for the backend service of a
global external Application Load Balancer. |
| `connection_draining` | String |  | connectionDraining cannot be specified with haPolicy. |
| `affinity_cookie_ttl_sec` | i64 |  | Lifetime of cookies in seconds. This setting is applicable to Application
Load Balancers and Traffic Director and requires
GENERATED_COOKIE or HTTP_COOKIE session affinity.

If set to 0, the cookie is non-persistent and lasts only until
the end of the browser session (or equivalent). The maximum allowed value
is two weeks (1,209,600).

Not supported when the backend service is referenced by a URL map that is
bound to target gRPC proxy that has validateForProxyless field set to true. |
| `log_config` | String |  | This field denotes the logging options for the load balancer traffic served
by this backend service. If logging is enabled, logs will be exported to
Stackdriver. |
| `protocol` | String |  | The protocol this BackendService uses to communicate
with backends.

Possible values are HTTP, HTTPS, HTTP2, H2C, TCP, SSL, UDP or GRPC.
depending on the chosen load balancer or Traffic Director configuration.
Refer to the documentation for the load balancers or for Traffic Director
for more information.

Must be set to GRPC when the backend service is referenced by a URL map
that is bound to target gRPC proxy. |
| `session_affinity` | String |  | Type of session affinity to use. The default is NONE.

Only NONE and HEADER_FIELD are supported
when the backend service is referenced by a URL map that is bound to
target gRPC proxy that has validateForProxyless field set to true.

For more details, see:
[Session
Affinity](https://cloud.google.com/load-balancing/docs/backend-service#session_affinity).

sessionAffinity cannot be specified with haPolicy. |
| `security_policy` | String |  | [Output Only] The resource URL for the security policy associated with this
backend service. |
| `tls_settings` | String |  | Configuration for Backend Authenticated TLS and mTLS. May only be specified
when the backend protocol is SSL, HTTPS or HTTP2. |
| `ha_policy` | String |  | Configures self-managed High Availability (HA) for External and Internal
Protocol Forwarding.

The backends of this regional backend service must only specify zonal
network endpoint groups (NEGs) of type GCE_VM_IP.

When haPolicy is set for an Internal Passthrough Network Load Balancer, the
regional backend service must set the network field. All zonal NEGs must
belong to the same network. However, individual NEGs can
belong to different subnetworks of that network.

When haPolicy is specified, the set of attached network endpoints across
all backends comprise an High Availability domain from which one endpoint
is selected as the active endpoint (the leader) that receives all
traffic.

haPolicy can be added only at backend service creation time. Once set up,
it cannot be deleted.

Note that haPolicy is not for load balancing, and therefore cannot be
specified with sessionAffinity, connectionTrackingPolicy, and
failoverPolicy.

haPolicy requires customers to be responsible for tracking backend
endpoint health and electing a leader among the healthy endpoints.
Therefore, haPolicy cannot be specified with healthChecks.

haPolicy can only be specified for External Passthrough Network Load
Balancers and Internal Passthrough Network Load Balancers. |
| `strong_session_affinity_cookie` | String |  | Describes the HTTP cookie used for stateful session affinity. This field is
applicable and required if the sessionAffinity is set toSTRONG_COOKIE_AFFINITY. |
| `kind` | String |  | [Output Only] Type of resource. Always compute#backendService
for backend services. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `service_bindings` | Vec<String> |  | URLs of networkservices.ServiceBinding resources.

Can only be set if load balancing scheme is INTERNAL_SELF_MANAGED.
If set, lists of backends and health checks must be both empty. |
| `locality_lb_policies` | Vec<String> |  | A list of locality load-balancing policies to be used in order of
preference. When you use localityLbPolicies, you must set at least one
value for either the localityLbPolicies[].policy or the
localityLbPolicies[].customPolicy field. localityLbPolicies overrides any
value set in the localityLbPolicy field.

For an example of how to use this field, seeDefine
a list of preferred policies.

Caution: This field and its children are intended for use in a service mesh
that includes gRPC clients only. Envoy proxies can't use backend services
that have this configuration. |
| `metadatas` | HashMap<String, String> |  | Deployment metadata associated with the resource to be set by a GKE hub
controller and read by the backend RCTH |
| `network` | String |  | The URL of the network to which this backend service belongs.

This field must be set for Internal Passthrough Network Load Balancers when
the haPolicy is enabled, and for External Passthrough Network Load
Balancers when the haPolicy fastIpMove is enabled.

This field can only be specified when the load balancing scheme is set toINTERNAL, or when the load balancing scheme is set toEXTERNAL and haPolicy fastIpMove is enabled. |
| `port_name` | String |  | A named port on a backend instance group representing the port for
communication to the backend VMs in that group. The
named port must be [defined on each backend instance
group](https://cloud.google.com/load-balancing/docs/backend-service#named_ports).
This parameter has no meaning if the backends are NEGs. For internal
passthrough Network Load Balancers and external passthrough Network Load
Balancers, omit port_name. |
| `backends` | Vec<String> |  | The list of backends that serve this BackendService. |
| `cdn_policy` | String |  | Cloud CDN configuration for this BackendService. Only available for
specified load balancer types. |
| `max_stream_duration` | String |  | Specifies the default maximum duration (timeout) for streams to this
service. Duration is computed from the beginning of the stream until the
response has been completely processed, including all retries. A stream
that does not complete in this duration is closed.

If not specified, there will be no timeout limit, i.e. the maximum
duration is infinite.

This value can be overridden in the PathMatcher configuration of the
UrlMap that references this backend service.

This field is only allowed when the loadBalancingScheme of
the backend service is INTERNAL_SELF_MANAGED. |
| `health_checks` | Vec<String> |  | The list of URLs to the healthChecks, httpHealthChecks (legacy), or
httpsHealthChecks (legacy) resource for health checking this backend
service. Not all backend services support legacy health checks. See
Load balancer guide. Currently, at most one health check can be
specified for each backend service. Backend services with
instance group or zonal NEG backends must have a health check unless
haPolicy is specified. Backend services with internet or serverless NEG
backends must not have a health check.

healthChecks[] cannot be specified with haPolicy. |
| `region` | String |  | [Output Only] URL of the region where the regional backend service
resides. This field is not applicable to global backend services.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `timeout_sec` | i64 |  | The backend service timeout has a different meaning depending on the
type of load balancer. For more information see,
Backend service settings.
The default is 30 seconds.
The full range of timeout values allowed goes from 1
through 2,147,483,647 seconds.

This value can be overridden in the PathMatcher configuration of the
UrlMap that references this backend service.

Not supported when the backend service is referenced by a URL map that is
bound to target gRPC proxy that has validateForProxyless field set to true.
Instead, use maxStreamDuration. |
| `outlier_detection` | String |  | Settings controlling the ejection of unhealthy backend endpoints from the
load balancing pool of each individual proxy instance that processes the
traffic for the given backend service. If not set, this feature is
considered disabled.

Results of the outlier detection algorithm (ejection of endpoints from the
load balancing pool and returning them back to the pool) are executed
independently by each proxy instance of the load balancer. In most cases,
more than one proxy instance handles the traffic received by a backend
service. Thus, it is possible that an unhealthy endpoint is detected and
ejected by only some of the proxies, and while this happens, other proxies
may continue to send requests to the same unhealthy endpoint until they
detect and eject the unhealthy endpoint.

Applicable backend endpoints can be:
   
   - VM instances in an Instance Group
   - Endpoints in a Zonal NEG (GCE_VM_IP, GCE_VM_IP_PORT)
   - Endpoints in a Hybrid Connectivity NEG (NON_GCP_PRIVATE_IP_PORT)
   - Serverless NEGs, that resolve to Cloud Run, App Engine, or Cloud
   Functions Services 
   - Private Service Connect NEGs, that resolve to
   Google-managed regional API endpoints or managed services published using
   Private Service Connect



Applicable backend service types can be:
   
   - A global backend service with the loadBalancingScheme set to
   INTERNAL_SELF_MANAGED or EXTERNAL_MANAGED. 
   - A regional backend
   service with the serviceProtocol set to HTTP, HTTPS, HTTP2 or H2C, and
   loadBalancingScheme set to INTERNAL_MANAGED or EXTERNAL_MANAGED. Not
   supported for Serverless NEGs.



Not supported when the backend service is referenced by a URL map that is
bound to target gRPC proxy that has validateForProxyless field set to true. |
| `security_settings` | String |  | This field specifies the security settings that apply to this backend
service. This field is applicable to a global backend service with the
load_balancing_scheme set to INTERNAL_SELF_MANAGED. |
| `used_by` | Vec<String> |  | [Output Only] List of resources referencing given backend service. |
| `subsetting` | String |  | subsetting cannot be specified with haPolicy. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `edge_security_policy` | String |  | [Output Only] The resource URL for the edge security policy associated with
this backend service. |
| `ip_address_selection_policy` | String |  | Specifies a preference for traffic sent from the proxy to the backend (or
from the client to the backend for proxyless gRPC).
The possible values are:
   
   - IPV4_ONLY: Only send IPv4 traffic to the backends of the
   backend service (Instance Group, Managed Instance Group, Network Endpoint
   Group), regardless of traffic from the client to the proxy. Only IPv4
   health checks are used to check the health of the backends. This is the
   default setting.
   - PREFER_IPV6: Prioritize the connection to the endpoint's
   IPv6 address over its IPv4 address (provided there is a healthy IPv6
   address).
   - IPV6_ONLY: Only send IPv6 traffic to the backends of the
   backend service (Instance Group, Managed Instance Group, Network Endpoint
   Group), regardless of traffic from the client to the proxy. Only IPv6
   health checks are used to check the health of the backends.



This field is applicable to either:
   
   -  Advanced global external Application Load Balancer (load balancing
   scheme EXTERNAL_MANAGED), 
   -  Regional external Application Load
   Balancer, 
   -  Internal proxy Network Load Balancer (load balancing
   scheme INTERNAL_MANAGED), 
   -  Regional internal Application Load
   Balancer (load balancing scheme INTERNAL_MANAGED), 
   -  Traffic
   Director with Envoy proxies and proxyless gRPC (load balancing scheme
   INTERNAL_SELF_MANAGED). |
| `custom_request_headers` | Vec<String> |  | Headers that the load balancer adds to proxied requests. See [Creating
custom
headers](https://cloud.google.com/load-balancing/docs/custom-headers). |
| `locality_lb_policy` | String |  | The load balancing algorithm used within the scope of the locality. The
possible values are:
   
   - ROUND_ROBIN: This is a simple policy in which each healthy
   backend is selected in round robin order. This is the default.
   - LEAST_REQUEST: An O(1) algorithm which
   selects two random healthy hosts and picks the host which has fewer active
   requests.
   - RING_HASH: The ring/modulo hash load balancer implements
   consistent hashing to backends. The algorithm has the property that the
   addition/removal of a host from a set of N hosts only affects 1/N of the
   requests.
   - RANDOM: The load balancer selects a random healthy
   host.
   - ORIGINAL_DESTINATION: Backend host is selected
   based on the client connection metadata, i.e., connections are opened to
   the same address as the destination address of the incoming connection
   before the connection was redirected to the load balancer.
   - MAGLEV: used as a drop in replacement for the ring hash
   load balancer. Maglev is not as stable as ring hash but has faster table
   lookup build times and host selection times. For more information about
   Maglev, see Maglev:
   A Fast and Reliable Software Network Load Balancer.
   - WEIGHTED_ROUND_ROBIN: Per-endpoint Weighted Round Robin
   Load Balancing using weights computed from Backend reported Custom Metrics.
   If set, the Backend Service responses are expected to contain non-standard
   HTTP response header field Endpoint-Load-Metrics. The reported
   metrics to use for computing the weights are specified via thecustomMetrics field.
   
   This field is applicable to either:
      - A regional backend service with the service_protocol set to HTTP,
      HTTPS, HTTP2 or H2C, and load_balancing_scheme set to
      INTERNAL_MANAGED. 
      - A global backend service with the
      load_balancing_scheme set to INTERNAL_SELF_MANAGED, INTERNAL_MANAGED, or
      EXTERNAL_MANAGED.
   
   
   If sessionAffinity is not configured—that is, if session
   affinity remains at the default value of NONE—then the
   default value for localityLbPolicy
   is ROUND_ROBIN. If session affinity is set to a value other
   than NONE,
   then the default value for localityLbPolicy isMAGLEV.
   
   Only ROUND_ROBIN and RING_HASH are supported
   when the backend service is referenced by a URL map that is bound to
   target gRPC proxy that has validateForProxyless field set to true.
   
   localityLbPolicy cannot be specified with haPolicy. |
| `failover_policy` | String |  | Requires at least one backend instance group to be defined
as a backup (failover) backend.
For load balancers that have configurable failover:
[Internal passthrough Network Load
Balancers](https://cloud.google.com/load-balancing/docs/internal/failover-overview)
and [external passthrough Network Load
Balancers](https://cloud.google.com/load-balancing/docs/network/networklb-failover-overview).

failoverPolicy cannot be specified with haPolicy. |
| `external_managed_migration_state` | String |  | Specifies the canary migration state. Possible values are PREPARE,
TEST_BY_PERCENTAGE, and TEST_ALL_TRAFFIC.

To begin the migration from EXTERNAL to EXTERNAL_MANAGED, the state must be
changed to PREPARE. The state must be changed to TEST_ALL_TRAFFIC before
the loadBalancingScheme can be changed to EXTERNAL_MANAGED. Optionally, the
TEST_BY_PERCENTAGE state can be used to migrate traffic by percentage using
externalManagedMigrationTestingPercentage.

Rolling back a migration requires the states to be set in reverse order. So
changing the scheme from EXTERNAL_MANAGED to EXTERNAL requires the state to
be set to TEST_ALL_TRAFFIC at the same time. Optionally, the
TEST_BY_PERCENTAGE state can be used to migrate some traffic back to
EXTERNAL or PREPARE can be used to migrate all traffic back to EXTERNAL. |
| `name` | String |  | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `circuit_breakers` | String |  |  |
| `external_managed_migration_testing_percentage` | f64 |  | Determines the fraction of requests that should be processed by the Global
external Application Load Balancer.

The value of this field must be in the range [0, 100].

Session affinity options will slightly affect this routing behavior, for
more details, see:Session
Affinity.

This value can only be set if the loadBalancingScheme in the BackendService
is set to EXTERNAL (when using the classic Application Load Balancer) and
the migration state is TEST_BY_PERCENTAGE. |
| `load_balancing_scheme` | String |  | Specifies the load balancer type. A backend service
created for one type of load balancer cannot be used with another.
For more information, refer toChoosing
a load balancer. |
| `port` | i64 |  | Deprecated in favor of portName. The TCP port to connect on
the backend. The default value is 80.
For internal passthrough Network Load Balancers and external passthrough
Network Load Balancers, omit port. |
| `service_lb_policy` | String |  | URL to networkservices.ServiceLbPolicy resource.

Can only be set if load balancing scheme is EXTERNAL_MANAGED,
INTERNAL_MANAGED or INTERNAL_SELF_MANAGED and the scope is global. |
| `fingerprint` | String |  | Fingerprint of this resource. A hash of the contents stored in this object.
This field is used in optimistic locking. This field will be ignored when
inserting a BackendService. An up-to-date fingerprint must be provided in
order to update the BackendService, otherwise the request will
fail with error 412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve a BackendService. |
| `region` | String | ✅ | Name of the region scoping this request. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `compression_mode` | String | Compress text responses using Brotli or gzip compression, based on
the client's Accept-Encoding header. |
| `dynamic_forwarding` | String | Dynamic forwarding configuration. This field is used to configure the
backend service with dynamic forwarding feature which together with Service
Extension allows customized and complex routing logic. |
| `connection_tracking_policy` | String | Connection Tracking configuration for this BackendService. Connection
tracking policy settings are only available for external passthrough
Network Load Balancers and internal passthrough Network Load Balancers.

connectionTrackingPolicy cannot be specified with haPolicy. |
| `custom_response_headers` | Vec<String> | Headers that the load balancer adds to proxied responses. See [Creating
custom
headers](https://cloud.google.com/load-balancing/docs/custom-headers). |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `iap` | String | The configurations for Identity-Aware Proxy on this resource.
Not available for internal passthrough Network Load Balancers and external
passthrough Network Load Balancers. |
| `network_pass_through_lb_traffic_policy` | String | Configures traffic steering properties of internal passthrough Network
Load Balancers.

networkPassThroughLbTrafficPolicy cannot be specified with haPolicy. |
| `custom_metrics` | Vec<String> | List of custom metrics that are used for theWEIGHTED_ROUND_ROBIN locality_lb_policy. |
| `params` | String | Input only. [Input Only] Additional params passed with the request, but not persisted
as part of resource payload. |
| `consistent_hash` | String | Consistent Hash-based load balancing can be used to provide soft session
affinity based on HTTP headers, cookies or other properties. This load
balancing policy is applicable only for HTTP connections. The affinity to a
particular destination host will be lost when one or more hosts are
added/removed from the destination service. This field specifies parameters
that control consistent hashing. This field is only applicable whenlocalityLbPolicy is set to MAGLEV orRING_HASH.

This field is applicable to either:
   
   - A regional backend service with the service_protocol set to HTTP,
   HTTPS, HTTP2 or H2C, and load_balancing_scheme set to
   INTERNAL_MANAGED. 
   - A global backend service with the
   load_balancing_scheme set to INTERNAL_SELF_MANAGED. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `enable_cdn` | bool | If true, enables Cloud CDN for the backend service of a
global external Application Load Balancer. |
| `connection_draining` | String | connectionDraining cannot be specified with haPolicy. |
| `affinity_cookie_ttl_sec` | i64 | Lifetime of cookies in seconds. This setting is applicable to Application
Load Balancers and Traffic Director and requires
GENERATED_COOKIE or HTTP_COOKIE session affinity.

If set to 0, the cookie is non-persistent and lasts only until
the end of the browser session (or equivalent). The maximum allowed value
is two weeks (1,209,600).

Not supported when the backend service is referenced by a URL map that is
bound to target gRPC proxy that has validateForProxyless field set to true. |
| `log_config` | String | This field denotes the logging options for the load balancer traffic served
by this backend service. If logging is enabled, logs will be exported to
Stackdriver. |
| `protocol` | String | The protocol this BackendService uses to communicate
with backends.

Possible values are HTTP, HTTPS, HTTP2, H2C, TCP, SSL, UDP or GRPC.
depending on the chosen load balancer or Traffic Director configuration.
Refer to the documentation for the load balancers or for Traffic Director
for more information.

Must be set to GRPC when the backend service is referenced by a URL map
that is bound to target gRPC proxy. |
| `session_affinity` | String | Type of session affinity to use. The default is NONE.

Only NONE and HEADER_FIELD are supported
when the backend service is referenced by a URL map that is bound to
target gRPC proxy that has validateForProxyless field set to true.

For more details, see:
[Session
Affinity](https://cloud.google.com/load-balancing/docs/backend-service#session_affinity).

sessionAffinity cannot be specified with haPolicy. |
| `security_policy` | String | [Output Only] The resource URL for the security policy associated with this
backend service. |
| `tls_settings` | String | Configuration for Backend Authenticated TLS and mTLS. May only be specified
when the backend protocol is SSL, HTTPS or HTTP2. |
| `ha_policy` | String | Configures self-managed High Availability (HA) for External and Internal
Protocol Forwarding.

The backends of this regional backend service must only specify zonal
network endpoint groups (NEGs) of type GCE_VM_IP.

When haPolicy is set for an Internal Passthrough Network Load Balancer, the
regional backend service must set the network field. All zonal NEGs must
belong to the same network. However, individual NEGs can
belong to different subnetworks of that network.

When haPolicy is specified, the set of attached network endpoints across
all backends comprise an High Availability domain from which one endpoint
is selected as the active endpoint (the leader) that receives all
traffic.

haPolicy can be added only at backend service creation time. Once set up,
it cannot be deleted.

Note that haPolicy is not for load balancing, and therefore cannot be
specified with sessionAffinity, connectionTrackingPolicy, and
failoverPolicy.

haPolicy requires customers to be responsible for tracking backend
endpoint health and electing a leader among the healthy endpoints.
Therefore, haPolicy cannot be specified with healthChecks.

haPolicy can only be specified for External Passthrough Network Load
Balancers and Internal Passthrough Network Load Balancers. |
| `strong_session_affinity_cookie` | String | Describes the HTTP cookie used for stateful session affinity. This field is
applicable and required if the sessionAffinity is set toSTRONG_COOKIE_AFFINITY. |
| `kind` | String | [Output Only] Type of resource. Always compute#backendService
for backend services. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `service_bindings` | Vec<String> | URLs of networkservices.ServiceBinding resources.

Can only be set if load balancing scheme is INTERNAL_SELF_MANAGED.
If set, lists of backends and health checks must be both empty. |
| `locality_lb_policies` | Vec<String> | A list of locality load-balancing policies to be used in order of
preference. When you use localityLbPolicies, you must set at least one
value for either the localityLbPolicies[].policy or the
localityLbPolicies[].customPolicy field. localityLbPolicies overrides any
value set in the localityLbPolicy field.

For an example of how to use this field, seeDefine
a list of preferred policies.

Caution: This field and its children are intended for use in a service mesh
that includes gRPC clients only. Envoy proxies can't use backend services
that have this configuration. |
| `metadatas` | HashMap<String, String> | Deployment metadata associated with the resource to be set by a GKE hub
controller and read by the backend RCTH |
| `network` | String | The URL of the network to which this backend service belongs.

This field must be set for Internal Passthrough Network Load Balancers when
the haPolicy is enabled, and for External Passthrough Network Load
Balancers when the haPolicy fastIpMove is enabled.

This field can only be specified when the load balancing scheme is set toINTERNAL, or when the load balancing scheme is set toEXTERNAL and haPolicy fastIpMove is enabled. |
| `port_name` | String | A named port on a backend instance group representing the port for
communication to the backend VMs in that group. The
named port must be [defined on each backend instance
group](https://cloud.google.com/load-balancing/docs/backend-service#named_ports).
This parameter has no meaning if the backends are NEGs. For internal
passthrough Network Load Balancers and external passthrough Network Load
Balancers, omit port_name. |
| `backends` | Vec<String> | The list of backends that serve this BackendService. |
| `cdn_policy` | String | Cloud CDN configuration for this BackendService. Only available for
specified load balancer types. |
| `max_stream_duration` | String | Specifies the default maximum duration (timeout) for streams to this
service. Duration is computed from the beginning of the stream until the
response has been completely processed, including all retries. A stream
that does not complete in this duration is closed.

If not specified, there will be no timeout limit, i.e. the maximum
duration is infinite.

This value can be overridden in the PathMatcher configuration of the
UrlMap that references this backend service.

This field is only allowed when the loadBalancingScheme of
the backend service is INTERNAL_SELF_MANAGED. |
| `health_checks` | Vec<String> | The list of URLs to the healthChecks, httpHealthChecks (legacy), or
httpsHealthChecks (legacy) resource for health checking this backend
service. Not all backend services support legacy health checks. See
Load balancer guide. Currently, at most one health check can be
specified for each backend service. Backend services with
instance group or zonal NEG backends must have a health check unless
haPolicy is specified. Backend services with internet or serverless NEG
backends must not have a health check.

healthChecks[] cannot be specified with haPolicy. |
| `region` | String | [Output Only] URL of the region where the regional backend service
resides. This field is not applicable to global backend services.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `timeout_sec` | i64 | The backend service timeout has a different meaning depending on the
type of load balancer. For more information see,
Backend service settings.
The default is 30 seconds.
The full range of timeout values allowed goes from 1
through 2,147,483,647 seconds.

This value can be overridden in the PathMatcher configuration of the
UrlMap that references this backend service.

Not supported when the backend service is referenced by a URL map that is
bound to target gRPC proxy that has validateForProxyless field set to true.
Instead, use maxStreamDuration. |
| `outlier_detection` | String | Settings controlling the ejection of unhealthy backend endpoints from the
load balancing pool of each individual proxy instance that processes the
traffic for the given backend service. If not set, this feature is
considered disabled.

Results of the outlier detection algorithm (ejection of endpoints from the
load balancing pool and returning them back to the pool) are executed
independently by each proxy instance of the load balancer. In most cases,
more than one proxy instance handles the traffic received by a backend
service. Thus, it is possible that an unhealthy endpoint is detected and
ejected by only some of the proxies, and while this happens, other proxies
may continue to send requests to the same unhealthy endpoint until they
detect and eject the unhealthy endpoint.

Applicable backend endpoints can be:
   
   - VM instances in an Instance Group
   - Endpoints in a Zonal NEG (GCE_VM_IP, GCE_VM_IP_PORT)
   - Endpoints in a Hybrid Connectivity NEG (NON_GCP_PRIVATE_IP_PORT)
   - Serverless NEGs, that resolve to Cloud Run, App Engine, or Cloud
   Functions Services 
   - Private Service Connect NEGs, that resolve to
   Google-managed regional API endpoints or managed services published using
   Private Service Connect



Applicable backend service types can be:
   
   - A global backend service with the loadBalancingScheme set to
   INTERNAL_SELF_MANAGED or EXTERNAL_MANAGED. 
   - A regional backend
   service with the serviceProtocol set to HTTP, HTTPS, HTTP2 or H2C, and
   loadBalancingScheme set to INTERNAL_MANAGED or EXTERNAL_MANAGED. Not
   supported for Serverless NEGs.



Not supported when the backend service is referenced by a URL map that is
bound to target gRPC proxy that has validateForProxyless field set to true. |
| `security_settings` | String | This field specifies the security settings that apply to this backend
service. This field is applicable to a global backend service with the
load_balancing_scheme set to INTERNAL_SELF_MANAGED. |
| `used_by` | Vec<String> | [Output Only] List of resources referencing given backend service. |
| `subsetting` | String | subsetting cannot be specified with haPolicy. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `edge_security_policy` | String | [Output Only] The resource URL for the edge security policy associated with
this backend service. |
| `ip_address_selection_policy` | String | Specifies a preference for traffic sent from the proxy to the backend (or
from the client to the backend for proxyless gRPC).
The possible values are:
   
   - IPV4_ONLY: Only send IPv4 traffic to the backends of the
   backend service (Instance Group, Managed Instance Group, Network Endpoint
   Group), regardless of traffic from the client to the proxy. Only IPv4
   health checks are used to check the health of the backends. This is the
   default setting.
   - PREFER_IPV6: Prioritize the connection to the endpoint's
   IPv6 address over its IPv4 address (provided there is a healthy IPv6
   address).
   - IPV6_ONLY: Only send IPv6 traffic to the backends of the
   backend service (Instance Group, Managed Instance Group, Network Endpoint
   Group), regardless of traffic from the client to the proxy. Only IPv6
   health checks are used to check the health of the backends.



This field is applicable to either:
   
   -  Advanced global external Application Load Balancer (load balancing
   scheme EXTERNAL_MANAGED), 
   -  Regional external Application Load
   Balancer, 
   -  Internal proxy Network Load Balancer (load balancing
   scheme INTERNAL_MANAGED), 
   -  Regional internal Application Load
   Balancer (load balancing scheme INTERNAL_MANAGED), 
   -  Traffic
   Director with Envoy proxies and proxyless gRPC (load balancing scheme
   INTERNAL_SELF_MANAGED). |
| `custom_request_headers` | Vec<String> | Headers that the load balancer adds to proxied requests. See [Creating
custom
headers](https://cloud.google.com/load-balancing/docs/custom-headers). |
| `locality_lb_policy` | String | The load balancing algorithm used within the scope of the locality. The
possible values are:
   
   - ROUND_ROBIN: This is a simple policy in which each healthy
   backend is selected in round robin order. This is the default.
   - LEAST_REQUEST: An O(1) algorithm which
   selects two random healthy hosts and picks the host which has fewer active
   requests.
   - RING_HASH: The ring/modulo hash load balancer implements
   consistent hashing to backends. The algorithm has the property that the
   addition/removal of a host from a set of N hosts only affects 1/N of the
   requests.
   - RANDOM: The load balancer selects a random healthy
   host.
   - ORIGINAL_DESTINATION: Backend host is selected
   based on the client connection metadata, i.e., connections are opened to
   the same address as the destination address of the incoming connection
   before the connection was redirected to the load balancer.
   - MAGLEV: used as a drop in replacement for the ring hash
   load balancer. Maglev is not as stable as ring hash but has faster table
   lookup build times and host selection times. For more information about
   Maglev, see Maglev:
   A Fast and Reliable Software Network Load Balancer.
   - WEIGHTED_ROUND_ROBIN: Per-endpoint Weighted Round Robin
   Load Balancing using weights computed from Backend reported Custom Metrics.
   If set, the Backend Service responses are expected to contain non-standard
   HTTP response header field Endpoint-Load-Metrics. The reported
   metrics to use for computing the weights are specified via thecustomMetrics field.
   
   This field is applicable to either:
      - A regional backend service with the service_protocol set to HTTP,
      HTTPS, HTTP2 or H2C, and load_balancing_scheme set to
      INTERNAL_MANAGED. 
      - A global backend service with the
      load_balancing_scheme set to INTERNAL_SELF_MANAGED, INTERNAL_MANAGED, or
      EXTERNAL_MANAGED.
   
   
   If sessionAffinity is not configured—that is, if session
   affinity remains at the default value of NONE—then the
   default value for localityLbPolicy
   is ROUND_ROBIN. If session affinity is set to a value other
   than NONE,
   then the default value for localityLbPolicy isMAGLEV.
   
   Only ROUND_ROBIN and RING_HASH are supported
   when the backend service is referenced by a URL map that is bound to
   target gRPC proxy that has validateForProxyless field set to true.
   
   localityLbPolicy cannot be specified with haPolicy. |
| `failover_policy` | String | Requires at least one backend instance group to be defined
as a backup (failover) backend.
For load balancers that have configurable failover:
[Internal passthrough Network Load
Balancers](https://cloud.google.com/load-balancing/docs/internal/failover-overview)
and [external passthrough Network Load
Balancers](https://cloud.google.com/load-balancing/docs/network/networklb-failover-overview).

failoverPolicy cannot be specified with haPolicy. |
| `external_managed_migration_state` | String | Specifies the canary migration state. Possible values are PREPARE,
TEST_BY_PERCENTAGE, and TEST_ALL_TRAFFIC.

To begin the migration from EXTERNAL to EXTERNAL_MANAGED, the state must be
changed to PREPARE. The state must be changed to TEST_ALL_TRAFFIC before
the loadBalancingScheme can be changed to EXTERNAL_MANAGED. Optionally, the
TEST_BY_PERCENTAGE state can be used to migrate traffic by percentage using
externalManagedMigrationTestingPercentage.

Rolling back a migration requires the states to be set in reverse order. So
changing the scheme from EXTERNAL_MANAGED to EXTERNAL requires the state to
be set to TEST_ALL_TRAFFIC at the same time. Optionally, the
TEST_BY_PERCENTAGE state can be used to migrate some traffic back to
EXTERNAL or PREPARE can be used to migrate all traffic back to EXTERNAL. |
| `name` | String | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `circuit_breakers` | String |  |
| `external_managed_migration_testing_percentage` | f64 | Determines the fraction of requests that should be processed by the Global
external Application Load Balancer.

The value of this field must be in the range [0, 100].

Session affinity options will slightly affect this routing behavior, for
more details, see:Session
Affinity.

This value can only be set if the loadBalancingScheme in the BackendService
is set to EXTERNAL (when using the classic Application Load Balancer) and
the migration state is TEST_BY_PERCENTAGE. |
| `load_balancing_scheme` | String | Specifies the load balancer type. A backend service
created for one type of load balancer cannot be used with another.
For more information, refer toChoosing
a load balancer. |
| `port` | i64 | Deprecated in favor of portName. The TCP port to connect on
the backend. The default value is 80.
For internal passthrough Network Load Balancers and external passthrough
Network Load Balancers, omit port. |
| `service_lb_policy` | String | URL to networkservices.ServiceLbPolicy resource.

Can only be set if load balancing scheme is EXTERNAL_MANAGED,
INTERNAL_MANAGED or INTERNAL_SELF_MANAGED and the scope is global. |
| `fingerprint` | String | Fingerprint of this resource. A hash of the contents stored in this object.
This field is used in optimistic locking. This field will be ignored when
inserting a BackendService. An up-to-date fingerprint must be provided in
order to update the BackendService, otherwise the request will
fail with error 412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve a BackendService. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create region_backend_service
region_backend_service = provider.compute_api.Region_backend_service {
    region = "value"  # Name of the region scoping this request.
    project = "value"  # Project ID for this request.
}

# Access region_backend_service outputs
region_backend_service_id = region_backend_service.id
region_backend_service_compression_mode = region_backend_service.compression_mode
region_backend_service_dynamic_forwarding = region_backend_service.dynamic_forwarding
region_backend_service_connection_tracking_policy = region_backend_service.connection_tracking_policy
region_backend_service_custom_response_headers = region_backend_service.custom_response_headers
region_backend_service_description = region_backend_service.description
region_backend_service_iap = region_backend_service.iap
region_backend_service_network_pass_through_lb_traffic_policy = region_backend_service.network_pass_through_lb_traffic_policy
region_backend_service_custom_metrics = region_backend_service.custom_metrics
region_backend_service_params = region_backend_service.params
region_backend_service_consistent_hash = region_backend_service.consistent_hash
region_backend_service_creation_timestamp = region_backend_service.creation_timestamp
region_backend_service_enable_cdn = region_backend_service.enable_cdn
region_backend_service_connection_draining = region_backend_service.connection_draining
region_backend_service_affinity_cookie_ttl_sec = region_backend_service.affinity_cookie_ttl_sec
region_backend_service_log_config = region_backend_service.log_config
region_backend_service_protocol = region_backend_service.protocol
region_backend_service_session_affinity = region_backend_service.session_affinity
region_backend_service_security_policy = region_backend_service.security_policy
region_backend_service_tls_settings = region_backend_service.tls_settings
region_backend_service_ha_policy = region_backend_service.ha_policy
region_backend_service_strong_session_affinity_cookie = region_backend_service.strong_session_affinity_cookie
region_backend_service_kind = region_backend_service.kind
region_backend_service_self_link = region_backend_service.self_link
region_backend_service_service_bindings = region_backend_service.service_bindings
region_backend_service_locality_lb_policies = region_backend_service.locality_lb_policies
region_backend_service_metadatas = region_backend_service.metadatas
region_backend_service_network = region_backend_service.network
region_backend_service_port_name = region_backend_service.port_name
region_backend_service_backends = region_backend_service.backends
region_backend_service_cdn_policy = region_backend_service.cdn_policy
region_backend_service_max_stream_duration = region_backend_service.max_stream_duration
region_backend_service_health_checks = region_backend_service.health_checks
region_backend_service_region = region_backend_service.region
region_backend_service_timeout_sec = region_backend_service.timeout_sec
region_backend_service_outlier_detection = region_backend_service.outlier_detection
region_backend_service_security_settings = region_backend_service.security_settings
region_backend_service_used_by = region_backend_service.used_by
region_backend_service_subsetting = region_backend_service.subsetting
region_backend_service_id = region_backend_service.id
region_backend_service_edge_security_policy = region_backend_service.edge_security_policy
region_backend_service_ip_address_selection_policy = region_backend_service.ip_address_selection_policy
region_backend_service_custom_request_headers = region_backend_service.custom_request_headers
region_backend_service_locality_lb_policy = region_backend_service.locality_lb_policy
region_backend_service_failover_policy = region_backend_service.failover_policy
region_backend_service_external_managed_migration_state = region_backend_service.external_managed_migration_state
region_backend_service_name = region_backend_service.name
region_backend_service_circuit_breakers = region_backend_service.circuit_breakers
region_backend_service_external_managed_migration_testing_percentage = region_backend_service.external_managed_migration_testing_percentage
region_backend_service_load_balancing_scheme = region_backend_service.load_balancing_scheme
region_backend_service_port = region_backend_service.port
region_backend_service_service_lb_policy = region_backend_service.service_lb_policy
region_backend_service_fingerprint = region_backend_service.fingerprint
```

---


### Network_endpoint_group

Creates a network endpoint group in the specified project using the
parameters that are included in the request.

**Operations**: ✅ Create ✅ Read ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `psc_target_service` | String |  | The target service url used to set up private service connection to
a Google API or a PSC Producer Service Attachment.
An example value is: asia-northeast3-cloudkms.googleapis.com.

Optional. Only valid when networkEndpointType isPRIVATE_SERVICE_CONNECT. |
| `serverless_deployment` | String |  | Only valid when networkEndpointType isSERVERLESS. Only one of cloudRun,appEngine, cloudFunction orserverlessDeployment may be set. |
| `kind` | String |  | [Output Only] Type of the resource. Alwayscompute#networkEndpointGroup for network endpoint group. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `app_engine` | String |  | Optional. Only valid when networkEndpointType isSERVERLESS. Only one of cloudRun,appEngine or cloudFunction may be set. |
| `zone` | String |  | [Output Only] The URL of thezone
where the network endpoint group is located. |
| `cloud_function` | String |  | Optional. Only valid when networkEndpointType isSERVERLESS. Only one of cloudRun,appEngine or cloudFunction may be set. |
| `network_endpoint_type` | String |  | Type of network endpoints in this network endpoint group. Can be one ofGCE_VM_IP, GCE_VM_IP_PORT,NON_GCP_PRIVATE_IP_PORT, INTERNET_FQDN_PORT,INTERNET_IP_PORT, SERVERLESS,PRIVATE_SERVICE_CONNECT, GCE_VM_IP_PORTMAP. |
| `load_balancer` | String |  | This field is only valid when the network endpoint group is used for load
balancing.
[Deprecated] This field is deprecated. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `name` | String |  | Name of the resource; provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must be
a dash, lowercase letter, or digit, except the last character, which cannot
be a dash. |
| `psc_data` | String |  | Optional. Only valid when networkEndpointType isPRIVATE_SERVICE_CONNECT. |
| `cloud_run` | String |  | Optional. Only valid when networkEndpointType isSERVERLESS. Only one of cloudRun,appEngine or cloudFunction may be set. |
| `region` | String |  | [Output Only] The URL of theregion
where the network endpoint group is located. |
| `network` | String |  | The URL of the network to which all network endpoints in the NEG belong.
Uses default project network if unspecified. |
| `annotations` | HashMap<String, String> |  | Optional. Metadata defined as annotations on the network endpoint group. |
| `default_port` | i64 |  | The default port used if the port number is not specified in the network
endpoint.

Optional. If the network endpoint type is either GCE_VM_IP,SERVERLESS or PRIVATE_SERVICE_CONNECT, this
field must not be specified. |
| `size` | i64 |  | [Output only] Number of network endpoints in the network endpoint group. |
| `subnetwork` | String |  | Optional URL of the subnetwork to which all network endpoints in the NEG
belong. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `project` | String | ✅ | Project ID for this request. |
| `zone` | String | ✅ | The name of the zone where
you want to create the network endpoint group. It should comply with
RFC1035. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `psc_target_service` | String | The target service url used to set up private service connection to
a Google API or a PSC Producer Service Attachment.
An example value is: asia-northeast3-cloudkms.googleapis.com.

Optional. Only valid when networkEndpointType isPRIVATE_SERVICE_CONNECT. |
| `serverless_deployment` | String | Only valid when networkEndpointType isSERVERLESS. Only one of cloudRun,appEngine, cloudFunction orserverlessDeployment may be set. |
| `kind` | String | [Output Only] Type of the resource. Alwayscompute#networkEndpointGroup for network endpoint group. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `app_engine` | String | Optional. Only valid when networkEndpointType isSERVERLESS. Only one of cloudRun,appEngine or cloudFunction may be set. |
| `zone` | String | [Output Only] The URL of thezone
where the network endpoint group is located. |
| `cloud_function` | String | Optional. Only valid when networkEndpointType isSERVERLESS. Only one of cloudRun,appEngine or cloudFunction may be set. |
| `network_endpoint_type` | String | Type of network endpoints in this network endpoint group. Can be one ofGCE_VM_IP, GCE_VM_IP_PORT,NON_GCP_PRIVATE_IP_PORT, INTERNET_FQDN_PORT,INTERNET_IP_PORT, SERVERLESS,PRIVATE_SERVICE_CONNECT, GCE_VM_IP_PORTMAP. |
| `load_balancer` | String | This field is only valid when the network endpoint group is used for load
balancing.
[Deprecated] This field is deprecated. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `name` | String | Name of the resource; provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must be
a dash, lowercase letter, or digit, except the last character, which cannot
be a dash. |
| `psc_data` | String | Optional. Only valid when networkEndpointType isPRIVATE_SERVICE_CONNECT. |
| `cloud_run` | String | Optional. Only valid when networkEndpointType isSERVERLESS. Only one of cloudRun,appEngine or cloudFunction may be set. |
| `region` | String | [Output Only] The URL of theregion
where the network endpoint group is located. |
| `network` | String | The URL of the network to which all network endpoints in the NEG belong.
Uses default project network if unspecified. |
| `annotations` | HashMap<String, String> | Optional. Metadata defined as annotations on the network endpoint group. |
| `default_port` | i64 | The default port used if the port number is not specified in the network
endpoint.

Optional. If the network endpoint type is either GCE_VM_IP,SERVERLESS or PRIVATE_SERVICE_CONNECT, this
field must not be specified. |
| `size` | i64 | [Output only] Number of network endpoints in the network endpoint group. |
| `subnetwork` | String | Optional URL of the subnetwork to which all network endpoints in the NEG
belong. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create network_endpoint_group
network_endpoint_group = provider.compute_api.Network_endpoint_group {
    project = "value"  # Project ID for this request.
    zone = "value"  # The name of the zone where
you want to create the network endpoint group. It should comply with
RFC1035.
}

# Access network_endpoint_group outputs
network_endpoint_group_id = network_endpoint_group.id
network_endpoint_group_psc_target_service = network_endpoint_group.psc_target_service
network_endpoint_group_serverless_deployment = network_endpoint_group.serverless_deployment
network_endpoint_group_kind = network_endpoint_group.kind
network_endpoint_group_creation_timestamp = network_endpoint_group.creation_timestamp
network_endpoint_group_description = network_endpoint_group.description
network_endpoint_group_app_engine = network_endpoint_group.app_engine
network_endpoint_group_zone = network_endpoint_group.zone
network_endpoint_group_cloud_function = network_endpoint_group.cloud_function
network_endpoint_group_network_endpoint_type = network_endpoint_group.network_endpoint_type
network_endpoint_group_load_balancer = network_endpoint_group.load_balancer
network_endpoint_group_id = network_endpoint_group.id
network_endpoint_group_name = network_endpoint_group.name
network_endpoint_group_psc_data = network_endpoint_group.psc_data
network_endpoint_group_cloud_run = network_endpoint_group.cloud_run
network_endpoint_group_region = network_endpoint_group.region
network_endpoint_group_network = network_endpoint_group.network
network_endpoint_group_annotations = network_endpoint_group.annotations
network_endpoint_group_default_port = network_endpoint_group.default_port
network_endpoint_group_size = network_endpoint_group.size
network_endpoint_group_subnetwork = network_endpoint_group.subnetwork
network_endpoint_group_self_link = network_endpoint_group.self_link
```

---


### Wire_group

Creates a wire group in the specified project in the given scope
using the parameters that are included in the request.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `endpoints` | HashMap<String, String> |  | A map that contains the logical endpoints of the wire group. Specify
key-value pairs for the map as follows:
   
   - Key: an RFC1035 user-specified label.
   - Value: an Endpoint object. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `topology` | String |  | Topology details for the wire group configuration. |
| `wire_group_properties` | String |  | Properties of the wire group. |
| `id` | String |  | [Output Only] The unique identifier for the resource type. The server
generates this identifier. |
| `kind` | String |  | [Output Only] Type of the resource. Alwayscompute#wireGroups for wire groups. |
| `reconciling` | bool |  | [Output Only] Indicates whether there are wire changes yet to be processed. |
| `wire_properties` | String |  | Properties for all wires in the wire group. |
| `wires` | Vec<String> |  | The single/redundant wire(s) managed by the wire group. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `admin_enabled` | bool |  | Indicates whether the wires in the wire group are enabled. When false, the
wires in the wire group are disabled. When true and when
there is simultaneously no wire-specific override of `adminEnabled` to
false, a given wire is enabled. Defaults to true. |
| `description` | String |  | An optional description of the wire group. |
| `name` | String |  | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `cross_site_network` | String | ✅ |  |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `endpoints` | HashMap<String, String> | A map that contains the logical endpoints of the wire group. Specify
key-value pairs for the map as follows:
   
   - Key: an RFC1035 user-specified label.
   - Value: an Endpoint object. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `topology` | String | Topology details for the wire group configuration. |
| `wire_group_properties` | String | Properties of the wire group. |
| `id` | String | [Output Only] The unique identifier for the resource type. The server
generates this identifier. |
| `kind` | String | [Output Only] Type of the resource. Alwayscompute#wireGroups for wire groups. |
| `reconciling` | bool | [Output Only] Indicates whether there are wire changes yet to be processed. |
| `wire_properties` | String | Properties for all wires in the wire group. |
| `wires` | Vec<String> | The single/redundant wire(s) managed by the wire group. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `admin_enabled` | bool | Indicates whether the wires in the wire group are enabled. When false, the
wires in the wire group are disabled. When true and when
there is simultaneously no wire-specific override of `adminEnabled` to
false, a given wire is enabled. Defaults to true. |
| `description` | String | An optional description of the wire group. |
| `name` | String | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create wire_group
wire_group = provider.compute_api.Wire_group {
    cross_site_network = "value"  # Required field
    project = "value"  # Project ID for this request.
}

# Access wire_group outputs
wire_group_id = wire_group.id
wire_group_endpoints = wire_group.endpoints
wire_group_creation_timestamp = wire_group.creation_timestamp
wire_group_topology = wire_group.topology
wire_group_wire_group_properties = wire_group.wire_group_properties
wire_group_id = wire_group.id
wire_group_kind = wire_group.kind
wire_group_reconciling = wire_group.reconciling
wire_group_wire_properties = wire_group.wire_properties
wire_group_wires = wire_group.wires
wire_group_self_link = wire_group.self_link
wire_group_admin_enabled = wire_group.admin_enabled
wire_group_description = wire_group.description
wire_group_name = wire_group.name
```

---


### Target_ssl_proxie

Creates a TargetSslProxy resource in the specified project using
the data included in the request.

**Operations**: ✅ Create ✅ Read ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `certificate_map` | String |  | URL of a certificate map that identifies a certificate map associated with
the given target proxy.
This field can only be set for global target proxies.
If set, sslCertificates will be ignored.

 Accepted format is//certificatemanager.googleapis.com/projects/{project}/locations/{location}/certificateMaps/{resourceName}. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `service` | String |  | URL to the BackendService resource. |
| `ssl_certificates` | Vec<String> |  | URLs to SslCertificate resources that are used to
authenticate connections to Backends. At least one SSL certificate
must be specified. Currently, you may specify up to 15 SSL certificates.
sslCertificates do not apply when the load balancing scheme is set to
INTERNAL_SELF_MANAGED. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `name` | String |  | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `ssl_policy` | String |  | URL of SslPolicy resource that will be associated with the TargetSslProxy
resource. If not set, the TargetSslProxy resource will not have any
SSL policy configured. |
| `proxy_header` | String |  | Specifies the type of proxy header to append before sending data to the
backend, either NONE or PROXY_V1. The default
is NONE. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `kind` | String |  | [Output Only] Type of the resource. Alwayscompute#targetSslProxy for target SSL proxies. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `certificate_map` | String | URL of a certificate map that identifies a certificate map associated with
the given target proxy.
This field can only be set for global target proxies.
If set, sslCertificates will be ignored.

 Accepted format is//certificatemanager.googleapis.com/projects/{project}/locations/{location}/certificateMaps/{resourceName}. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `service` | String | URL to the BackendService resource. |
| `ssl_certificates` | Vec<String> | URLs to SslCertificate resources that are used to
authenticate connections to Backends. At least one SSL certificate
must be specified. Currently, you may specify up to 15 SSL certificates.
sslCertificates do not apply when the load balancing scheme is set to
INTERNAL_SELF_MANAGED. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `name` | String | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `ssl_policy` | String | URL of SslPolicy resource that will be associated with the TargetSslProxy
resource. If not set, the TargetSslProxy resource will not have any
SSL policy configured. |
| `proxy_header` | String | Specifies the type of proxy header to append before sending data to the
backend, either NONE or PROXY_V1. The default
is NONE. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `kind` | String | [Output Only] Type of the resource. Alwayscompute#targetSslProxy for target SSL proxies. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create target_ssl_proxie
target_ssl_proxie = provider.compute_api.Target_ssl_proxie {
    project = "value"  # Project ID for this request.
}

# Access target_ssl_proxie outputs
target_ssl_proxie_id = target_ssl_proxie.id
target_ssl_proxie_certificate_map = target_ssl_proxie.certificate_map
target_ssl_proxie_self_link = target_ssl_proxie.self_link
target_ssl_proxie_service = target_ssl_proxie.service
target_ssl_proxie_ssl_certificates = target_ssl_proxie.ssl_certificates
target_ssl_proxie_id = target_ssl_proxie.id
target_ssl_proxie_description = target_ssl_proxie.description
target_ssl_proxie_name = target_ssl_proxie.name
target_ssl_proxie_ssl_policy = target_ssl_proxie.ssl_policy
target_ssl_proxie_proxy_header = target_ssl_proxie.proxy_header
target_ssl_proxie_creation_timestamp = target_ssl_proxie.creation_timestamp
target_ssl_proxie_kind = target_ssl_proxie.kind
```

---


### Zone_vm_extension_policie

Creates a new zone-level VM extension policy within a project.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `managed_by_global` | bool |  | Optional. [Output Only] Indicates if this policy is managed by a global policy. |
| `extension_policies` | HashMap<String, String> |  | Required. A map of extension names (e.g., "cloudops") to their corresponding policy
configurations. |
| `instance_selectors` | Vec<String> |  | Optional. Selectors to target VMs for this policy. VMs are selected if they match
*any* of the provided selectors (logical OR). If this list is empty, the
policy applies to all VMs. |
| `priority` | i64 |  | Optional. Priority of this policy. Used to resolve conflicts when multiple policies
apply to the same extension.
The policy priority is an integer from 0 to 65535, inclusive. Lower
integers indicate higher priorities. If you do not specify a priority when
creating a rule, it is assigned a priority of 1000. If priorities are
equal, the policy with the more recent creation timestamp takes precedence. |
| `name` | String |  | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?`
which means the first character must be a lowercase letter, and all
following characters must be a dash, lowercase letter, or digit, except
the last character, which cannot be a dash. |
| `self_link_with_id` | String |  | [Output Only] Server-defined URL for this resource's resource id. |
| `self_link` | String |  | [Output Only] Server-defined fully-qualified URL for this resource. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `state` | String |  | Optional. [Output Only] Current state of the policy: ACTIVE or DELETING. |
| `update_timestamp` | String |  | [Output Only] Update timestamp inRFC3339
text format. |
| `description` | String |  | An optional description of this resource. |
| `kind` | String |  | [Output Only] Type of the resource. Alwayscompute#vmExtensionPolicy. |
| `global_resource_link` | String |  | Optional. [Output Only] Link to the global policy that manages this zone policy, if
applicable. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `zone` | String | ✅ | Name of the zone for this request. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `managed_by_global` | bool | Optional. [Output Only] Indicates if this policy is managed by a global policy. |
| `extension_policies` | HashMap<String, String> | Required. A map of extension names (e.g., "cloudops") to their corresponding policy
configurations. |
| `instance_selectors` | Vec<String> | Optional. Selectors to target VMs for this policy. VMs are selected if they match
*any* of the provided selectors (logical OR). If this list is empty, the
policy applies to all VMs. |
| `priority` | i64 | Optional. Priority of this policy. Used to resolve conflicts when multiple policies
apply to the same extension.
The policy priority is an integer from 0 to 65535, inclusive. Lower
integers indicate higher priorities. If you do not specify a priority when
creating a rule, it is assigned a priority of 1000. If priorities are
equal, the policy with the more recent creation timestamp takes precedence. |
| `name` | String | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?`
which means the first character must be a lowercase letter, and all
following characters must be a dash, lowercase letter, or digit, except
the last character, which cannot be a dash. |
| `self_link_with_id` | String | [Output Only] Server-defined URL for this resource's resource id. |
| `self_link` | String | [Output Only] Server-defined fully-qualified URL for this resource. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `state` | String | Optional. [Output Only] Current state of the policy: ACTIVE or DELETING. |
| `update_timestamp` | String | [Output Only] Update timestamp inRFC3339
text format. |
| `description` | String | An optional description of this resource. |
| `kind` | String | [Output Only] Type of the resource. Alwayscompute#vmExtensionPolicy. |
| `global_resource_link` | String | Optional. [Output Only] Link to the global policy that manages this zone policy, if
applicable. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create zone_vm_extension_policie
zone_vm_extension_policie = provider.compute_api.Zone_vm_extension_policie {
    zone = "value"  # Name of the zone for this request.
    project = "value"  # Project ID for this request.
}

# Access zone_vm_extension_policie outputs
zone_vm_extension_policie_id = zone_vm_extension_policie.id
zone_vm_extension_policie_managed_by_global = zone_vm_extension_policie.managed_by_global
zone_vm_extension_policie_extension_policies = zone_vm_extension_policie.extension_policies
zone_vm_extension_policie_instance_selectors = zone_vm_extension_policie.instance_selectors
zone_vm_extension_policie_priority = zone_vm_extension_policie.priority
zone_vm_extension_policie_name = zone_vm_extension_policie.name
zone_vm_extension_policie_self_link_with_id = zone_vm_extension_policie.self_link_with_id
zone_vm_extension_policie_self_link = zone_vm_extension_policie.self_link
zone_vm_extension_policie_id = zone_vm_extension_policie.id
zone_vm_extension_policie_state = zone_vm_extension_policie.state
zone_vm_extension_policie_update_timestamp = zone_vm_extension_policie.update_timestamp
zone_vm_extension_policie_description = zone_vm_extension_policie.description
zone_vm_extension_policie_kind = zone_vm_extension_policie.kind
zone_vm_extension_policie_global_resource_link = zone_vm_extension_policie.global_resource_link
zone_vm_extension_policie_creation_timestamp = zone_vm_extension_policie.creation_timestamp
```

---


### Region_operation

Waits for the specified Operation resource to return as `DONE`
or for the request to approach the 2 minute deadline, and retrieves the
specified Operation resource. This method differs from the
`GET` method in that it waits for no more than the default
deadline (2 minutes) and then returns the current state of the operation,
which might be `DONE` or still in progress.

This method is called on a best-effort basis. Specifically:
   
   
    - In uncommon cases, when the server is overloaded, the request might
    return before the default deadline is reached, or might return after zero
    seconds.
   - If the default deadline is reached, there is no guarantee that the
    operation is actually done when the method returns. Be prepared to retry
    if the operation is not `DONE`.

**Operations**: ✅ Create ✅ Read ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `region` | String | ✅ | Name of the region for this request. |
| `project` | String | ✅ | Project ID for this request. |
| `operation` | String | ✅ | Name of the Operations resource to return, or its unique numeric
identifier. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `start_time` | String | [Output Only] The time that this operation was started by the server.
This value is inRFC3339
text format. |
| `http_error_message` | String | [Output Only] If the operation fails, this field contains the HTTP error
message that was returned, such as `NOT FOUND`. |
| `id` | String | [Output Only] The unique identifier for the operation. This identifier is
defined by the server. |
| `kind` | String | [Output Only] Type of the resource. Always `compute#operation` for
Operation resources. |
| `error` | String | [Output Only] If errors are generated during processing of the operation,
this field will be populated. |
| `description` | String | [Output Only] A textual description of the operation, which is
set when the operation is created. |
| `status_message` | String | [Output Only] An optional textual description of the current status of the
operation. |
| `warnings` | Vec<String> | [Output Only] If warning messages are generated during processing of the
operation, this field will be populated. |
| `creation_timestamp` | String | [Deprecated] This field is deprecated. |
| `zone` | String | [Output Only] The URL of the zone where the operation resides. Only
applicable when performing per-zone operations. |
| `user` | String | [Output Only] User who requested the operation, for example:
`user@example.com` or
`alice_smith_identifier (global/workforcePools/example-com-us-employees)`. |
| `name` | String | [Output Only] Name of the operation. |
| `operation_group_id` | String | [Output Only] An ID that represents a group of operations, such as when a
group of operations results from a `bulkInsert` API request. |
| `client_operation_id` | String | [Output Only] The value of `requestId` if you provided it in the request.
Not present otherwise. |
| `http_error_status_code` | i64 | [Output Only] If the operation fails, this field contains the HTTP error
status code that was returned. For example, a `404` means the
resource was not found. |
| `progress` | i64 | [Output Only] An optional progress indicator that ranges from 0 to 100.
There is no requirement that this be linear or support any granularity of
operations. This should not be used to guess when the operation will be
complete. This number should monotonically increase as the operation
progresses. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `status` | String | [Output Only] The status of the operation, which can be one of the
following:
`PENDING`, `RUNNING`, or `DONE`. |
| `target_link` | String | [Output Only] The URL of the resource that the operation modifies. For
operations related to creating a snapshot, this points to the disk
that the snapshot was created from. |
| `set_common_instance_metadata_operation_metadata` | String | [Output Only] If the operation is for projects.setCommonInstanceMetadata,
this field will contain information on all underlying zonal actions and
their state. |
| `target_id` | String | [Output Only] The unique target ID, which identifies a specific incarnation
of the target resource. |
| `instances_bulk_insert_operation_metadata` | String |  |
| `insert_time` | String | [Output Only] The time that this operation was requested.
This value is inRFC3339
text format. |
| `operation_type` | String | [Output Only] The type of operation, such as `insert`,
`update`, or `delete`, and so on. |
| `region` | String | [Output Only] The URL of the region where the operation resides. Only
applicable when performing regional operations. |
| `end_time` | String | [Output Only] The time that this operation was completed. This value is inRFC3339
text format. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create region_operation
region_operation = provider.compute_api.Region_operation {
    region = "value"  # Name of the region for this request.
    project = "value"  # Project ID for this request.
    operation = "value"  # Name of the Operations resource to return, or its unique numeric
identifier.
}

# Access region_operation outputs
region_operation_id = region_operation.id
region_operation_start_time = region_operation.start_time
region_operation_http_error_message = region_operation.http_error_message
region_operation_id = region_operation.id
region_operation_kind = region_operation.kind
region_operation_error = region_operation.error
region_operation_description = region_operation.description
region_operation_status_message = region_operation.status_message
region_operation_warnings = region_operation.warnings
region_operation_creation_timestamp = region_operation.creation_timestamp
region_operation_zone = region_operation.zone
region_operation_user = region_operation.user
region_operation_name = region_operation.name
region_operation_operation_group_id = region_operation.operation_group_id
region_operation_client_operation_id = region_operation.client_operation_id
region_operation_http_error_status_code = region_operation.http_error_status_code
region_operation_progress = region_operation.progress
region_operation_self_link = region_operation.self_link
region_operation_status = region_operation.status
region_operation_target_link = region_operation.target_link
region_operation_set_common_instance_metadata_operation_metadata = region_operation.set_common_instance_metadata_operation_metadata
region_operation_target_id = region_operation.target_id
region_operation_instances_bulk_insert_operation_metadata = region_operation.instances_bulk_insert_operation_metadata
region_operation_insert_time = region_operation.insert_time
region_operation_operation_type = region_operation.operation_type
region_operation_region = region_operation.region
region_operation_end_time = region_operation.end_time
```

---


### Node_group

Creates a NodeGroup resource in the specified project using the data
included in the request.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `share_settings` | String |  | Share-settings for the node group |
| `maintenance_window` | String |  |  |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `status` | String |  |  |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `size` | i64 |  | [Output Only] The total number of nodes in the node group. |
| `kind` | String |  | [Output Only] The type of the resource. Alwayscompute#nodeGroup for node group. |
| `node_template` | String |  | URL of the node template to create the node group from. |
| `autoscaling_policy` | String |  | Specifies how autoscaling should behave. |
| `location_hint` | String |  | An opaque location hint used to place the Node close to other
resources.
This field is for use by internal tools that use the public API.
The location hint here on the NodeGroup overrides any location_hint
present in the NodeTemplate. |
| `zone` | String |  | [Output Only] The name of the zone where the node group resides,
such as us-central1-a. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `maintenance_interval` | String |  | Specifies the frequency of planned maintenance events. The accepted values
are: `AS_NEEDED` and `RECURRENT`. |
| `name` | String |  | The name of the resource, provided by the client when initially creating
the resource. The resource name must be 1-63 characters long, and comply
withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must be
a dash, lowercase letter, or digit, except the last character, which cannot
be a dash. |
| `maintenance_policy` | String |  | Specifies how to handle instances when a node in the group undergoes
maintenance. Set to one of: DEFAULT,RESTART_IN_PLACE, or MIGRATE_WITHIN_NODE_GROUP.
The default value is DEFAULT. For more information, see
Maintenance policies. |
| `fingerprint` | String |  |  |
| `zone` | String | ✅ | The name of the zone for this request. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `share_settings` | String | Share-settings for the node group |
| `maintenance_window` | String |  |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `status` | String |  |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `size` | i64 | [Output Only] The total number of nodes in the node group. |
| `kind` | String | [Output Only] The type of the resource. Alwayscompute#nodeGroup for node group. |
| `node_template` | String | URL of the node template to create the node group from. |
| `autoscaling_policy` | String | Specifies how autoscaling should behave. |
| `location_hint` | String | An opaque location hint used to place the Node close to other
resources.
This field is for use by internal tools that use the public API.
The location hint here on the NodeGroup overrides any location_hint
present in the NodeTemplate. |
| `zone` | String | [Output Only] The name of the zone where the node group resides,
such as us-central1-a. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `maintenance_interval` | String | Specifies the frequency of planned maintenance events. The accepted values
are: `AS_NEEDED` and `RECURRENT`. |
| `name` | String | The name of the resource, provided by the client when initially creating
the resource. The resource name must be 1-63 characters long, and comply
withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must be
a dash, lowercase letter, or digit, except the last character, which cannot
be a dash. |
| `maintenance_policy` | String | Specifies how to handle instances when a node in the group undergoes
maintenance. Set to one of: DEFAULT,RESTART_IN_PLACE, or MIGRATE_WITHIN_NODE_GROUP.
The default value is DEFAULT. For more information, see
Maintenance policies. |
| `fingerprint` | String |  |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create node_group
node_group = provider.compute_api.Node_group {
    zone = "value"  # The name of the zone for this request.
    project = "value"  # Project ID for this request.
}

# Access node_group outputs
node_group_id = node_group.id
node_group_share_settings = node_group.share_settings
node_group_maintenance_window = node_group.maintenance_window
node_group_self_link = node_group.self_link
node_group_status = node_group.status
node_group_description = node_group.description
node_group_size = node_group.size
node_group_kind = node_group.kind
node_group_node_template = node_group.node_template
node_group_autoscaling_policy = node_group.autoscaling_policy
node_group_location_hint = node_group.location_hint
node_group_zone = node_group.zone
node_group_creation_timestamp = node_group.creation_timestamp
node_group_id = node_group.id
node_group_maintenance_interval = node_group.maintenance_interval
node_group_name = node_group.name
node_group_maintenance_policy = node_group.maintenance_policy
node_group_fingerprint = node_group.fingerprint
```

---


### Region_multi_mig

Creates a multi-MIG in the specified project.

**Operations**: ✅ Create ✅ Read ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `description` | String |  | An optional description of this resource. |
| `name` | String |  | The name of the multi-MIG.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first character
must be a lowercase letter, and all following characters must be a dash,
lowercase letter, or digit, except the last character, which cannot be a
dash. |
| `self_link` | String |  | [Output only] Server-defined URL for the resource. |
| `region` | String |  | [Output only] The URL of the region where the resource resides. You must
specify this field as part of the HTTP request URL. You cannot set the
region as a field in the request body. |
| `creation_timestamp` | String |  | [Output only] The creation timestamp of this multi-MIG in RFC3339
text format. |
| `status` | String |  |  |
| `resource_policies` | String |  | Resource policies for this multi-MIG. |
| `kind` | String |  | [Output only] Type of the resource. Alwayscompute#multiMig for multi-MIGs. |
| `id` | String |  | [Output only] The unique identifier for this resource type. The server
generates this identifier. |
| `region` | String | ✅ | Name of the region for this request. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `description` | String | An optional description of this resource. |
| `name` | String | The name of the multi-MIG.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first character
must be a lowercase letter, and all following characters must be a dash,
lowercase letter, or digit, except the last character, which cannot be a
dash. |
| `self_link` | String | [Output only] Server-defined URL for the resource. |
| `region` | String | [Output only] The URL of the region where the resource resides. You must
specify this field as part of the HTTP request URL. You cannot set the
region as a field in the request body. |
| `creation_timestamp` | String | [Output only] The creation timestamp of this multi-MIG in RFC3339
text format. |
| `status` | String |  |
| `resource_policies` | String | Resource policies for this multi-MIG. |
| `kind` | String | [Output only] Type of the resource. Alwayscompute#multiMig for multi-MIGs. |
| `id` | String | [Output only] The unique identifier for this resource type. The server
generates this identifier. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create region_multi_mig
region_multi_mig = provider.compute_api.Region_multi_mig {
    region = "value"  # Name of the region for this request.
    project = "value"  # Project ID for this request.
}

# Access region_multi_mig outputs
region_multi_mig_id = region_multi_mig.id
region_multi_mig_description = region_multi_mig.description
region_multi_mig_name = region_multi_mig.name
region_multi_mig_self_link = region_multi_mig.self_link
region_multi_mig_region = region_multi_mig.region
region_multi_mig_creation_timestamp = region_multi_mig.creation_timestamp
region_multi_mig_status = region_multi_mig.status
region_multi_mig_resource_policies = region_multi_mig.resource_policies
region_multi_mig_kind = region_multi_mig.kind
region_multi_mig_id = region_multi_mig.id
```

---


### Machine_image

Creates a machine image in the specified project using the
data that is included in the request. If you are creating a new machine
image to update an existing instance, your new machine image should use the
same network or, if applicable, the same subnetwork as the original
instance.

**Operations**: ✅ Create ✅ Read ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `id` | String |  | [Output Only] A unique identifier for this machine image. The server
defines this identifier. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `labels` | HashMap<String, String> |  | Labels to apply to this machine image. These can be later modified by
the setLabels method. |
| `source_instance` | String |  | The source instance used to create the machine image. You can provide this
as a partial or full URL to the resource. For example, the following are
valid values: 
   
   
     - https://www.googleapis.com/compute/v1/projects/project/zones/zone/instances/instance 
   - projects/project/zones/zone/instances/instance |
| `status` | String |  | [Output Only] The status of the machine image. One of the following values:INVALID, CREATING, READY,DELETING, and UPLOADING. |
| `instance_properties` | String |  | [Output Only] Properties of source instance |
| `total_storage_bytes` | String |  | [Output Only] Total size of the storage used by the machine image. |
| `label_fingerprint` | String |  | A fingerprint for the labels being applied to this machine image, which is
essentially a hash of the labels set used for optimistic locking. The
fingerprint is initially generated by Compute Engine and changes after
every request to modify or update labels. You must always provide an
up-to-date fingerprint hash in order to update or change labels.

To see the latest fingerprint, make get() request to the
machine image. |
| `source_disk_encryption_keys` | Vec<String> |  | [Input Only] Thecustomer-supplied
encryption key of the disks attached to the source instance. Required
if the source disk is protected by a customer-supplied encryption key. |
| `name` | String |  | Name of the resource; provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must be
a dash, lowercase letter, or digit, except the last character, which cannot
be a dash. |
| `guest_flush` | bool |  | [Input Only] Whether to attempt an application consistent machine image by
informing the OS to prepare for the snapshot process. |
| `self_link` | String |  | [Output Only] The URL for this machine image. The server defines this URL. |
| `saved_disks` | Vec<String> |  | An array of Machine Image specific properties for disks attached to the
source instance |
| `satisfies_pzi` | bool |  | Output only. Reserved for future use. |
| `storage_locations` | Vec<String> |  | The regional or multi-regional Cloud Storage bucket location where themachine image is
stored. |
| `creation_timestamp` | String |  | [Output Only] The creation timestamp for this machine image inRFC3339
text format. |
| `source_instance_properties` | String |  | [Output Only] DEPRECATED: Please use instance_properties
instead for source instance related properties. New properties will not be
added to this field. |
| `kind` | String |  | [Output Only] The resource type, which is alwayscompute#machineImage for machine image. |
| `machine_image_encryption_key` | String |  | Encrypts the machine image using acustomer-supplied
encryption key.

After you encrypt a machine image using a customer-supplied key, you must
provide the same key if you use the machine image later. For example, you
must provide the encryption key when you create an instance from the
encrypted machine image in a future request.

Customer-supplied encryption keys do not protect access to metadata of the
machine image.

If you do not provide an encryption key when creating the machine image,
then the machine image will be encrypted using an automatically generated
key and you do not need to provide a key to use the machine image later. |
| `satisfies_pzs` | bool |  | [Output Only] Reserved for future use. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `id` | String | [Output Only] A unique identifier for this machine image. The server
defines this identifier. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `labels` | HashMap<String, String> | Labels to apply to this machine image. These can be later modified by
the setLabels method. |
| `source_instance` | String | The source instance used to create the machine image. You can provide this
as a partial or full URL to the resource. For example, the following are
valid values: 
   
   
     - https://www.googleapis.com/compute/v1/projects/project/zones/zone/instances/instance 
   - projects/project/zones/zone/instances/instance |
| `status` | String | [Output Only] The status of the machine image. One of the following values:INVALID, CREATING, READY,DELETING, and UPLOADING. |
| `instance_properties` | String | [Output Only] Properties of source instance |
| `total_storage_bytes` | String | [Output Only] Total size of the storage used by the machine image. |
| `label_fingerprint` | String | A fingerprint for the labels being applied to this machine image, which is
essentially a hash of the labels set used for optimistic locking. The
fingerprint is initially generated by Compute Engine and changes after
every request to modify or update labels. You must always provide an
up-to-date fingerprint hash in order to update or change labels.

To see the latest fingerprint, make get() request to the
machine image. |
| `source_disk_encryption_keys` | Vec<String> | [Input Only] Thecustomer-supplied
encryption key of the disks attached to the source instance. Required
if the source disk is protected by a customer-supplied encryption key. |
| `name` | String | Name of the resource; provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must be
a dash, lowercase letter, or digit, except the last character, which cannot
be a dash. |
| `guest_flush` | bool | [Input Only] Whether to attempt an application consistent machine image by
informing the OS to prepare for the snapshot process. |
| `self_link` | String | [Output Only] The URL for this machine image. The server defines this URL. |
| `saved_disks` | Vec<String> | An array of Machine Image specific properties for disks attached to the
source instance |
| `satisfies_pzi` | bool | Output only. Reserved for future use. |
| `storage_locations` | Vec<String> | The regional or multi-regional Cloud Storage bucket location where themachine image is
stored. |
| `creation_timestamp` | String | [Output Only] The creation timestamp for this machine image inRFC3339
text format. |
| `source_instance_properties` | String | [Output Only] DEPRECATED: Please use instance_properties
instead for source instance related properties. New properties will not be
added to this field. |
| `kind` | String | [Output Only] The resource type, which is alwayscompute#machineImage for machine image. |
| `machine_image_encryption_key` | String | Encrypts the machine image using acustomer-supplied
encryption key.

After you encrypt a machine image using a customer-supplied key, you must
provide the same key if you use the machine image later. For example, you
must provide the encryption key when you create an instance from the
encrypted machine image in a future request.

Customer-supplied encryption keys do not protect access to metadata of the
machine image.

If you do not provide an encryption key when creating the machine image,
then the machine image will be encrypted using an automatically generated
key and you do not need to provide a key to use the machine image later. |
| `satisfies_pzs` | bool | [Output Only] Reserved for future use. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create machine_image
machine_image = provider.compute_api.Machine_image {
    project = "value"  # Project ID for this request.
}

# Access machine_image outputs
machine_image_id = machine_image.id
machine_image_id = machine_image.id
machine_image_description = machine_image.description
machine_image_labels = machine_image.labels
machine_image_source_instance = machine_image.source_instance
machine_image_status = machine_image.status
machine_image_instance_properties = machine_image.instance_properties
machine_image_total_storage_bytes = machine_image.total_storage_bytes
machine_image_label_fingerprint = machine_image.label_fingerprint
machine_image_source_disk_encryption_keys = machine_image.source_disk_encryption_keys
machine_image_name = machine_image.name
machine_image_guest_flush = machine_image.guest_flush
machine_image_self_link = machine_image.self_link
machine_image_saved_disks = machine_image.saved_disks
machine_image_satisfies_pzi = machine_image.satisfies_pzi
machine_image_storage_locations = machine_image.storage_locations
machine_image_creation_timestamp = machine_image.creation_timestamp
machine_image_source_instance_properties = machine_image.source_instance_properties
machine_image_kind = machine_image.kind
machine_image_machine_image_encryption_key = machine_image.machine_image_encryption_key
machine_image_satisfies_pzs = machine_image.satisfies_pzs
```

---


### Image

Creates an image in the specified project using the data included
in the request.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `locked` | bool |  | A flag for marketplace VM disk created from the image, which is designed
for marketplace VM disk to prevent the proprietary data on the disk from
being accessed unwantedly. The flag will be inherited by the disk created
from the image.

The disk with locked flag set to true will be prohibited from performing
the operations below:
   
   - R/W or R/O disk attach
   - Disk detach, if disk is created via create-on-create
   - Create images
   - Create snapshots
   - Create disk clone (create disk from the current disk)



The image with the locked field set to true will be prohibited from
performing the operations below:
   
   - Create images from the current image
   - Update the locked field for the current image



The instance with at least one disk with locked flag set to true will be
prohibited from performing the operations below:
   
   - Secondary disk attach
   - Create instant snapshot
   - Create machine images
   - Create instance template
   - Delete the instance with --keep-disk parameter set to true |
| `source_disk_encryption_key` | String |  | Thecustomer-supplied
encryption key of the source disk. Required if the source disk is
protected by a customer-supplied encryption key. |
| `source_snapshot_encryption_key` | String |  | The customer-supplied encryption key of the source snapshot. Required if
the source snapshot is protected by a customer-supplied encryption key. |
| `source_snapshot_id` | String |  | [Output Only]
The ID value of the snapshot used to create this image. This value may be
used to determine whether the snapshot was taken from the current or a
previous instance of a given snapshot name. |
| `guest_os_features` | Vec<String> |  | A list of features to enable on the guest operating system. Applicable
only for bootable images. To see a list of available options, see theguestOSfeatures[].type parameter. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `archive_size_bytes` | String |  | Size of the image tar.gz archive stored in Google Cloud
Storage (in bytes). |
| `kind` | String |  | [Output Only] Type of the resource. Always compute#image for
images. |
| `status` | String |  | [Output Only] The status of the image. An image can be used to create other
resources, such as instances, only after the image has been successfully
created and the status is set to READY. Possible
values are FAILED, PENDING, orREADY. |
| `licenses` | Vec<String> |  | Any applicable license URI. |
| `architecture` | String |  | The architecture of the image. Valid values are
ARM64 or X86_64. |
| `source_image_encryption_key` | String |  | The customer-supplied encryption key of the source image. Required if the
source image is protected by a customer-supplied encryption key. |
| `labels` | HashMap<String, String> |  | Labels to apply to this image. These can be later modified by
the setLabels method. |
| `source_disk_id` | String |  | [Output Only]
The ID value of the disk used to create this image. This value may be used
to determine whether the image was taken from the current or a previous
instance of a given disk name. |
| `rollout_override` | String |  | A rollout policy to apply to this image. When specified, the rollout policy
overrides per-zone references to the image via the associated image family.
The rollout policy restricts the zones where this image is accessible when
using a zonal image family reference. When the rollout policy does not
include the user specified zone, or if the zone is rolled out, this image
is accessible.

The rollout policy for this image is read-only, except for allowlisted
users. This field might not be configured. To view the latest
non-deprecated image in a specific zone, use theimageFamilyViews.get method. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `satisfies_pzs` | bool |  | [Output Only] Reserved for future use. |
| `storage_locations` | Vec<String> |  | Cloud Storage bucket storage location of the image (regional or
multi-regional). |
| `params` | String |  | Input only. [Input Only] Additional params passed with the request, but not persisted
as part of resource payload. |
| `source_image` | String |  | URL of the source image used to create this image.
The following are valid formats for the URL:
   
   - https://www.googleapis.com/compute/v1/projects/project_id/global/
   images/image_name
   - projects/project_id/global/images/image_name



In order to create an image, you must provide the full or partial URL of
one of the following:
   
   - The rawDisk.source URL 
   - The sourceDisk URL 
   - The sourceImage URL 
   - The sourceSnapshot URL |
| `user_licenses` | Vec<String> |  | A list of publicly visible user-licenses. Unlike regular licenses, user
provided licenses can be modified after the disk is created. This includes
a list of URLs to the license resource. For example, to provide a debian
license:

https://www.googleapis.com/compute/v1/projects/debian-cloud/global/licenses/debian-9-stretch |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `source_type` | String |  | The type of the image used to create this disk. The
default and only valid value is RAW. |
| `image_encryption_key` | String |  | Encrypts the image using acustomer-supplied
encryption key.

After you encrypt an image with a customer-supplied key, you must provide
the same key if you use the image later (e.g. to create a disk from
the image).

Customer-supplied encryption keys do not protect access to metadata
of the disk.

If you do not provide an encryption key when creating the image, then the
disk will be encrypted using an automatically generated key and you do not
need to provide a key to use the image later. |
| `label_fingerprint` | String |  | A fingerprint for the labels being applied to this image, which is
essentially a hash of the labels used for optimistic locking. The
fingerprint is initially generated by Compute Engine and changes after
every request to modify or update labels. You must always provide an
up-to-date fingerprint hash in order to update or change labels,
otherwise the request will fail with error412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve an image. |
| `raw_disk` | String |  | The parameters of the raw disk image. |
| `source_image_id` | String |  | [Output Only]
The ID value of the image used to create this image. This value may be used
to determine whether the image was taken from the current or a previous
instance of a given image name. |
| `enable_confidential_compute` | bool |  | Whether this image is created from a confidential compute mode disk.
[Output Only]: This field is not set by user, but from source disk. |
| `disk_size_gb` | String |  | Size of the image when restored onto a persistent disk (in GB). |
| `source_disk` | String |  | URL of the source disk used to create this image.
For example, the following are valid values:
   
   - https://www.googleapis.com/compute/v1/projects/project/zones/zone/disks/disk 
   - projects/project/zones/zone/disks/disk 
   - zones/zone/disks/disk



In order to create an image, you must provide the full or partial URL of
one of the following:
   
   - The rawDisk.source URL 
   - The sourceDisk URL 
   - The sourceImage URL 
   - The sourceSnapshot URL |
| `source_snapshot` | String |  | URL of the source snapshot used to create this image.
The following are valid formats for the URL:
   
   - https://www.googleapis.com/compute/v1/projects/project_id/global/
   snapshots/snapshot_name
   - projects/project_id/global/snapshots/snapshot_name



In order to create an image, you must provide the full or partial URL of
one of the following:
   
   - The rawDisk.source URL 
   - The sourceDisk URL 
   - The sourceImage URL 
   - The sourceSnapshot URL |
| `deprecated` | String |  | The deprecation status associated with this image. |
| `family` | String |  | The name of the image family to which this image belongs. The image
family name can be from a publicly managed image family provided by
Compute Engine, or from a custom image family you create. For example,centos-stream-9 is a publicly available image family.
For more information, see Image
family best practices.

When creating disks, you can specify an image family instead of a specific
image name. The image family always returns its latest image that is not
deprecated. The name of the image family must comply with RFC1035. |
| `license_codes` | Vec<String> |  | Integer license codes indicating which licenses are attached to this image. |
| `satisfies_pzi` | bool |  | Output only. Reserved for future use. |
| `shielded_instance_initial_state` | String |  | Set the secure boot keys of shielded instance. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `name` | String |  | Name of the resource; provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must be
a dash, lowercase letter, or digit, except the last character, which cannot
be a dash. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `locked` | bool | A flag for marketplace VM disk created from the image, which is designed
for marketplace VM disk to prevent the proprietary data on the disk from
being accessed unwantedly. The flag will be inherited by the disk created
from the image.

The disk with locked flag set to true will be prohibited from performing
the operations below:
   
   - R/W or R/O disk attach
   - Disk detach, if disk is created via create-on-create
   - Create images
   - Create snapshots
   - Create disk clone (create disk from the current disk)



The image with the locked field set to true will be prohibited from
performing the operations below:
   
   - Create images from the current image
   - Update the locked field for the current image



The instance with at least one disk with locked flag set to true will be
prohibited from performing the operations below:
   
   - Secondary disk attach
   - Create instant snapshot
   - Create machine images
   - Create instance template
   - Delete the instance with --keep-disk parameter set to true |
| `source_disk_encryption_key` | String | Thecustomer-supplied
encryption key of the source disk. Required if the source disk is
protected by a customer-supplied encryption key. |
| `source_snapshot_encryption_key` | String | The customer-supplied encryption key of the source snapshot. Required if
the source snapshot is protected by a customer-supplied encryption key. |
| `source_snapshot_id` | String | [Output Only]
The ID value of the snapshot used to create this image. This value may be
used to determine whether the snapshot was taken from the current or a
previous instance of a given snapshot name. |
| `guest_os_features` | Vec<String> | A list of features to enable on the guest operating system. Applicable
only for bootable images. To see a list of available options, see theguestOSfeatures[].type parameter. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `archive_size_bytes` | String | Size of the image tar.gz archive stored in Google Cloud
Storage (in bytes). |
| `kind` | String | [Output Only] Type of the resource. Always compute#image for
images. |
| `status` | String | [Output Only] The status of the image. An image can be used to create other
resources, such as instances, only after the image has been successfully
created and the status is set to READY. Possible
values are FAILED, PENDING, orREADY. |
| `licenses` | Vec<String> | Any applicable license URI. |
| `architecture` | String | The architecture of the image. Valid values are
ARM64 or X86_64. |
| `source_image_encryption_key` | String | The customer-supplied encryption key of the source image. Required if the
source image is protected by a customer-supplied encryption key. |
| `labels` | HashMap<String, String> | Labels to apply to this image. These can be later modified by
the setLabels method. |
| `source_disk_id` | String | [Output Only]
The ID value of the disk used to create this image. This value may be used
to determine whether the image was taken from the current or a previous
instance of a given disk name. |
| `rollout_override` | String | A rollout policy to apply to this image. When specified, the rollout policy
overrides per-zone references to the image via the associated image family.
The rollout policy restricts the zones where this image is accessible when
using a zonal image family reference. When the rollout policy does not
include the user specified zone, or if the zone is rolled out, this image
is accessible.

The rollout policy for this image is read-only, except for allowlisted
users. This field might not be configured. To view the latest
non-deprecated image in a specific zone, use theimageFamilyViews.get method. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `satisfies_pzs` | bool | [Output Only] Reserved for future use. |
| `storage_locations` | Vec<String> | Cloud Storage bucket storage location of the image (regional or
multi-regional). |
| `params` | String | Input only. [Input Only] Additional params passed with the request, but not persisted
as part of resource payload. |
| `source_image` | String | URL of the source image used to create this image.
The following are valid formats for the URL:
   
   - https://www.googleapis.com/compute/v1/projects/project_id/global/
   images/image_name
   - projects/project_id/global/images/image_name



In order to create an image, you must provide the full or partial URL of
one of the following:
   
   - The rawDisk.source URL 
   - The sourceDisk URL 
   - The sourceImage URL 
   - The sourceSnapshot URL |
| `user_licenses` | Vec<String> | A list of publicly visible user-licenses. Unlike regular licenses, user
provided licenses can be modified after the disk is created. This includes
a list of URLs to the license resource. For example, to provide a debian
license:

https://www.googleapis.com/compute/v1/projects/debian-cloud/global/licenses/debian-9-stretch |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `source_type` | String | The type of the image used to create this disk. The
default and only valid value is RAW. |
| `image_encryption_key` | String | Encrypts the image using acustomer-supplied
encryption key.

After you encrypt an image with a customer-supplied key, you must provide
the same key if you use the image later (e.g. to create a disk from
the image).

Customer-supplied encryption keys do not protect access to metadata
of the disk.

If you do not provide an encryption key when creating the image, then the
disk will be encrypted using an automatically generated key and you do not
need to provide a key to use the image later. |
| `label_fingerprint` | String | A fingerprint for the labels being applied to this image, which is
essentially a hash of the labels used for optimistic locking. The
fingerprint is initially generated by Compute Engine and changes after
every request to modify or update labels. You must always provide an
up-to-date fingerprint hash in order to update or change labels,
otherwise the request will fail with error412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve an image. |
| `raw_disk` | String | The parameters of the raw disk image. |
| `source_image_id` | String | [Output Only]
The ID value of the image used to create this image. This value may be used
to determine whether the image was taken from the current or a previous
instance of a given image name. |
| `enable_confidential_compute` | bool | Whether this image is created from a confidential compute mode disk.
[Output Only]: This field is not set by user, but from source disk. |
| `disk_size_gb` | String | Size of the image when restored onto a persistent disk (in GB). |
| `source_disk` | String | URL of the source disk used to create this image.
For example, the following are valid values:
   
   - https://www.googleapis.com/compute/v1/projects/project/zones/zone/disks/disk 
   - projects/project/zones/zone/disks/disk 
   - zones/zone/disks/disk



In order to create an image, you must provide the full or partial URL of
one of the following:
   
   - The rawDisk.source URL 
   - The sourceDisk URL 
   - The sourceImage URL 
   - The sourceSnapshot URL |
| `source_snapshot` | String | URL of the source snapshot used to create this image.
The following are valid formats for the URL:
   
   - https://www.googleapis.com/compute/v1/projects/project_id/global/
   snapshots/snapshot_name
   - projects/project_id/global/snapshots/snapshot_name



In order to create an image, you must provide the full or partial URL of
one of the following:
   
   - The rawDisk.source URL 
   - The sourceDisk URL 
   - The sourceImage URL 
   - The sourceSnapshot URL |
| `deprecated` | String | The deprecation status associated with this image. |
| `family` | String | The name of the image family to which this image belongs. The image
family name can be from a publicly managed image family provided by
Compute Engine, or from a custom image family you create. For example,centos-stream-9 is a publicly available image family.
For more information, see Image
family best practices.

When creating disks, you can specify an image family instead of a specific
image name. The image family always returns its latest image that is not
deprecated. The name of the image family must comply with RFC1035. |
| `license_codes` | Vec<String> | Integer license codes indicating which licenses are attached to this image. |
| `satisfies_pzi` | bool | Output only. Reserved for future use. |
| `shielded_instance_initial_state` | String | Set the secure boot keys of shielded instance. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `name` | String | Name of the resource; provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must be
a dash, lowercase letter, or digit, except the last character, which cannot
be a dash. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create image
image = provider.compute_api.Image {
    project = "value"  # Project ID for this request.
}

# Access image outputs
image_id = image.id
image_locked = image.locked
image_source_disk_encryption_key = image.source_disk_encryption_key
image_source_snapshot_encryption_key = image.source_snapshot_encryption_key
image_source_snapshot_id = image.source_snapshot_id
image_guest_os_features = image.guest_os_features
image_description = image.description
image_archive_size_bytes = image.archive_size_bytes
image_kind = image.kind
image_status = image.status
image_licenses = image.licenses
image_architecture = image.architecture
image_source_image_encryption_key = image.source_image_encryption_key
image_labels = image.labels
image_source_disk_id = image.source_disk_id
image_rollout_override = image.rollout_override
image_creation_timestamp = image.creation_timestamp
image_satisfies_pzs = image.satisfies_pzs
image_storage_locations = image.storage_locations
image_params = image.params
image_source_image = image.source_image
image_user_licenses = image.user_licenses
image_self_link = image.self_link
image_source_type = image.source_type
image_image_encryption_key = image.image_encryption_key
image_label_fingerprint = image.label_fingerprint
image_raw_disk = image.raw_disk
image_source_image_id = image.source_image_id
image_enable_confidential_compute = image.enable_confidential_compute
image_disk_size_gb = image.disk_size_gb
image_source_disk = image.source_disk
image_source_snapshot = image.source_snapshot
image_deprecated = image.deprecated
image_family = image.family
image_license_codes = image.license_codes
image_satisfies_pzi = image.satisfies_pzi
image_shielded_instance_initial_state = image.shielded_instance_initial_state
image_id = image.id
image_name = image.name
```

---


### Https_health_check

Creates a HttpsHealthCheck resource in the specified project using the data
included in the request.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `request_path` | String |  | The request path of the HTTPS health check request. The default value is
"/". Must comply withRFC3986. |
| `unhealthy_threshold` | i64 |  | A so-far healthy instance will be marked unhealthy after this
many consecutive failures. The default value is 2. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `kind` | String |  | Type of the resource. |
| `host` | String |  | The value of the host header in the HTTPS health check request. If left
empty (default value), the public IP on behalf of which this health check
is performed will be used. |
| `check_interval_sec` | i64 |  | How often (in seconds) to send a health check. The default value is 5
seconds. |
| `name` | String |  | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `port` | i64 |  | The TCP port number for the HTTPS health check request. The default value
is 443. |
| `healthy_threshold` | i64 |  | A so-far unhealthy instance will be marked healthy after this
many consecutive successes. The default value is 2. |
| `timeout_sec` | i64 |  | How long (in seconds) to wait before claiming failure. The default value is
5 seconds. It is invalid for timeoutSec to have a greater value than
checkIntervalSec. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `request_path` | String | The request path of the HTTPS health check request. The default value is
"/". Must comply withRFC3986. |
| `unhealthy_threshold` | i64 | A so-far healthy instance will be marked unhealthy after this
many consecutive failures. The default value is 2. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `kind` | String | Type of the resource. |
| `host` | String | The value of the host header in the HTTPS health check request. If left
empty (default value), the public IP on behalf of which this health check
is performed will be used. |
| `check_interval_sec` | i64 | How often (in seconds) to send a health check. The default value is 5
seconds. |
| `name` | String | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `port` | i64 | The TCP port number for the HTTPS health check request. The default value
is 443. |
| `healthy_threshold` | i64 | A so-far unhealthy instance will be marked healthy after this
many consecutive successes. The default value is 2. |
| `timeout_sec` | i64 | How long (in seconds) to wait before claiming failure. The default value is
5 seconds. It is invalid for timeoutSec to have a greater value than
checkIntervalSec. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create https_health_check
https_health_check = provider.compute_api.Https_health_check {
    project = "value"  # Project ID for this request.
}

# Access https_health_check outputs
https_health_check_id = https_health_check.id
https_health_check_request_path = https_health_check.request_path
https_health_check_unhealthy_threshold = https_health_check.unhealthy_threshold
https_health_check_description = https_health_check.description
https_health_check_creation_timestamp = https_health_check.creation_timestamp
https_health_check_id = https_health_check.id
https_health_check_kind = https_health_check.kind
https_health_check_host = https_health_check.host
https_health_check_check_interval_sec = https_health_check.check_interval_sec
https_health_check_name = https_health_check.name
https_health_check_port = https_health_check.port
https_health_check_healthy_threshold = https_health_check.healthy_threshold
https_health_check_timeout_sec = https_health_check.timeout_sec
https_health_check_self_link = https_health_check.self_link
```

---


### Region_health_aggregation_policie

Create a HealthAggregationPolicy in the specified project in the given
region using the parameters that are included in the request.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `self_link_with_id` | String |  | [Output Only] Server-defined URL with id for the resource. |
| `policy_type` | String |  | Specifies the type of the healthAggregationPolicy. The only allowed value
for global resources is DNS_PUBLIC_IP_POLICY. The only allowed
value for regional resources is BACKEND_SERVICE_POLICY. Must
be specified when the healthAggregationPolicy is created, and cannot be
mutated. |
| `fingerprint` | String |  | Fingerprint of this resource. A hash of the contents stored in this object.
This field is used in optimistic locking. This field will be ignored when
inserting a HealthAggregationPolicy. An up-to-date fingerprint
must be provided in order to patch the HealthAggregationPolicy; Otherwise,
the request will fail with error 412 conditionNotMet. To see
the latest fingerprint, make a get() request to retrieve the
HealthAggregationPolicy. |
| `healthy_percent_threshold` | i64 |  | Can only be set if the policyType field isBACKEND_SERVICE_POLICY. Specifies the threshold (as a
percentage) of healthy endpoints required in order to consider the
aggregated health result HEALTHY. Defaults to 60. Must be in
range [0, 100]. Not applicable if the policyType field isDNB_PUBLIC_IP_POLICY. Can be mutated. This field is optional,
and will be set to the default if unspecified. Note that both this
threshold and minHealthyThreshold must be satisfied in order
for HEALTHY to be the aggregated result. "Endpoints" refers to network
endpoints within a Network Endpoint Group or instances within an Instance
Group. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `min_healthy_threshold` | i64 |  | Can only be set if the policyType field isBACKEND_SERVICE_POLICY. Specifies the minimum number of
healthy endpoints required in order to consider the aggregated health
result HEALTHY. Defaults to 1. Must be positive. Not
applicable if the policyType field isDNB_PUBLIC_IP_POLICY. Can be mutated. This field is optional,
and will be set to the default if unspecified. Note that both this
threshold and healthyPercentThreshold must be satisfied in
order for HEALTHY to be the aggregated result. "Endpoints" refers to
network endpoints within a Network Endpoint Group or instances within an
Instance Group. |
| `name` | String |  | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `region` | String |  | [Output Only] URL of the region where the health aggregation policy
resides. This field applies only to the regional resource. You must specify
this field as part of the HTTP request URL. It is not settable as a field
in the request body. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `kind` | String |  | [Output Only] Type of the resource. Alwayscompute#healthAggregationPolicy for health aggregation
policies. |
| `project` | String | ✅ | Project ID for this request. |
| `region` | String | ✅ | Name of the region scoping this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `self_link_with_id` | String | [Output Only] Server-defined URL with id for the resource. |
| `policy_type` | String | Specifies the type of the healthAggregationPolicy. The only allowed value
for global resources is DNS_PUBLIC_IP_POLICY. The only allowed
value for regional resources is BACKEND_SERVICE_POLICY. Must
be specified when the healthAggregationPolicy is created, and cannot be
mutated. |
| `fingerprint` | String | Fingerprint of this resource. A hash of the contents stored in this object.
This field is used in optimistic locking. This field will be ignored when
inserting a HealthAggregationPolicy. An up-to-date fingerprint
must be provided in order to patch the HealthAggregationPolicy; Otherwise,
the request will fail with error 412 conditionNotMet. To see
the latest fingerprint, make a get() request to retrieve the
HealthAggregationPolicy. |
| `healthy_percent_threshold` | i64 | Can only be set if the policyType field isBACKEND_SERVICE_POLICY. Specifies the threshold (as a
percentage) of healthy endpoints required in order to consider the
aggregated health result HEALTHY. Defaults to 60. Must be in
range [0, 100]. Not applicable if the policyType field isDNB_PUBLIC_IP_POLICY. Can be mutated. This field is optional,
and will be set to the default if unspecified. Note that both this
threshold and minHealthyThreshold must be satisfied in order
for HEALTHY to be the aggregated result. "Endpoints" refers to network
endpoints within a Network Endpoint Group or instances within an Instance
Group. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `min_healthy_threshold` | i64 | Can only be set if the policyType field isBACKEND_SERVICE_POLICY. Specifies the minimum number of
healthy endpoints required in order to consider the aggregated health
result HEALTHY. Defaults to 1. Must be positive. Not
applicable if the policyType field isDNB_PUBLIC_IP_POLICY. Can be mutated. This field is optional,
and will be set to the default if unspecified. Note that both this
threshold and healthyPercentThreshold must be satisfied in
order for HEALTHY to be the aggregated result. "Endpoints" refers to
network endpoints within a Network Endpoint Group or instances within an
Instance Group. |
| `name` | String | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `region` | String | [Output Only] URL of the region where the health aggregation policy
resides. This field applies only to the regional resource. You must specify
this field as part of the HTTP request URL. It is not settable as a field
in the request body. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `kind` | String | [Output Only] Type of the resource. Alwayscompute#healthAggregationPolicy for health aggregation
policies. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create region_health_aggregation_policie
region_health_aggregation_policie = provider.compute_api.Region_health_aggregation_policie {
    project = "value"  # Project ID for this request.
    region = "value"  # Name of the region scoping this request.
}

# Access region_health_aggregation_policie outputs
region_health_aggregation_policie_id = region_health_aggregation_policie.id
region_health_aggregation_policie_self_link = region_health_aggregation_policie.self_link
region_health_aggregation_policie_creation_timestamp = region_health_aggregation_policie.creation_timestamp
region_health_aggregation_policie_self_link_with_id = region_health_aggregation_policie.self_link_with_id
region_health_aggregation_policie_policy_type = region_health_aggregation_policie.policy_type
region_health_aggregation_policie_fingerprint = region_health_aggregation_policie.fingerprint
region_health_aggregation_policie_healthy_percent_threshold = region_health_aggregation_policie.healthy_percent_threshold
region_health_aggregation_policie_id = region_health_aggregation_policie.id
region_health_aggregation_policie_min_healthy_threshold = region_health_aggregation_policie.min_healthy_threshold
region_health_aggregation_policie_name = region_health_aggregation_policie.name
region_health_aggregation_policie_region = region_health_aggregation_policie.region
region_health_aggregation_policie_description = region_health_aggregation_policie.description
region_health_aggregation_policie_kind = region_health_aggregation_policie.kind
```

---


### Region_disk_type

Returns the specified regional disk type.

**Operations**: ✅ Read

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `region` | String | [Output Only] URL of the region where the disk type resides. Only
applicable for regional resources.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `kind` | String | [Output Only] Type of the resource. Always compute#diskType
for disk types. |
| `description` | String | [Output Only] An optional description of this resource. |
| `deprecated` | String | [Output Only] The deprecation status associated with this disk type. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `valid_disk_size` | String | [Output Only] An optional textual description of the valid disk size,
such as "10GB-10TB". |
| `zone` | String | [Output Only] URL of the zone where the disk type resides.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `default_disk_size_gb` | String | [Output Only] Server-defined default disk size in GB. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `name` | String | [Output Only] Name of the resource. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Access region_disk_type outputs
region_disk_type_id = region_disk_type.id
region_disk_type_region = region_disk_type.region
region_disk_type_kind = region_disk_type.kind
region_disk_type_description = region_disk_type.description
region_disk_type_deprecated = region_disk_type.deprecated
region_disk_type_self_link = region_disk_type.self_link
region_disk_type_creation_timestamp = region_disk_type.creation_timestamp
region_disk_type_valid_disk_size = region_disk_type.valid_disk_size
region_disk_type_zone = region_disk_type.zone
region_disk_type_default_disk_size_gb = region_disk_type.default_disk_size_gb
region_disk_type_id = region_disk_type.id
region_disk_type_name = region_disk_type.name
```

---


### Firewall

Creates a firewall rule in the specified project using the data
included in the request.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `description` | String |  | An optional description of this resource. Provide this field when you
create the resource. |
| `source_ranges` | Vec<String> |  | If source ranges are specified, the firewall rule applies only to traffic
that has a source IP address in these ranges. These ranges must be
expressed inCIDR format. One or both of sourceRanges
and sourceTags may be set.
If both fields are set, the rule applies to traffic that has a
source IP address within sourceRanges OR a source IP
from a resource with a matching tag listed in thesourceTags field. The connection does not need to match
both fields for the rule to
apply. Both IPv4 and IPv6 are supported. |
| `source_service_accounts` | Vec<String> |  | If source service accounts are specified, the firewall rules apply only to
traffic originating from an instance with a service account in this list.
Source service accounts cannot be used to control traffic to an instance's
external IP address because service accounts are associated with an
instance, not an IP address.sourceRanges can be set at the same time assourceServiceAccounts.
If both are set, the firewall applies to traffic that
has a source IP address within the sourceRanges OR a source
IP that belongs to an instance with service account listed insourceServiceAccount. The connection does not need to match
both fields for the firewall to apply.sourceServiceAccounts cannot be used at the same time assourceTags or targetTags. |
| `allowed` | Vec<String> |  | The list of ALLOW rules specified by this firewall. Each rule specifies a
protocol and port-range tuple that describes a permitted connection. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `disabled` | bool |  | Denotes whether the firewall rule is disabled. When set to true, the
firewall rule is not enforced and the network behaves as if it did not
exist. If this is unspecified, the firewall rule will be enabled. |
| `enable_logging` | bool |  | Deprecated in favor of enable in LogConfig.
This field denotes whether to enable logging for a particular
firewall rule. If logging is enabled, logs will be exported t
Cloud Logging. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `denied` | Vec<String> |  | The list of DENY rules specified by this firewall. Each rule specifies a
protocol and port-range tuple that describes a denied connection. |
| `log_config` | String |  | This field denotes the logging options for a particular firewall rule. If
logging is enabled, logs will be exported to Cloud Logging. |
| `network` | String |  | URL of the network resource for this firewall rule. If not
specified when creating a firewall rule, the default network
is used:

global/networks/default

If you choose to specify this field, you can specify the network as a full 
or partial URL. For example, the following are all valid URLs: 
   
   - 
   https://www.googleapis.com/compute/v1/projects/myproject/global/networks/my-network 
   - projects/myproject/global/networks/my-network 
   - global/networks/default |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `priority` | i64 |  | Priority for this rule.
This is an integer between `0` and `65535`, both inclusive.
The default value is `1000`.
Relative priorities determine which rule takes effect if multiple rules
apply. Lower values indicate higher priority. For example, a rule with
priority `0` has higher precedence than a rule with priority `1`.
DENY rules take precedence over ALLOW rules if they have equal priority.
Note that VPC networks have implied
rules with a priority of `65535`. To avoid conflicts with the implied
rules, use a priority number less than `65535`. |
| `source_tags` | Vec<String> |  | If source tags are specified, the firewall rule applies only to traffic
with source IPs that match the primary network interfaces of VM instances
that have the tag and are in the same VPC network.
Source tags cannot be used to control traffic to an instance's external IP
address, it only applies to traffic between instances in the same virtual
network. Because tags are associated with instances, not IP addresses.
One or both of sourceRanges and sourceTags may be
set. If both fields are set, the firewall applies to traffic that has a
source IP address within sourceRanges OR a source IP from a
resource with a matching tag listed in the sourceTags
field. The connection does not need to match both fields for the
firewall to apply. |
| `target_tags` | Vec<String> |  | A list of tags that controls which instances the firewall rule
applies to. If targetTags are specified, then the firewall
rule applies only to instances in the VPC network that have one of those
tags. If no targetTags are specified, the firewall rule
applies to all instances on the specified network. |
| `name` | String |  | Name of the resource; provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?`. The first character
must be a lowercase letter, and all following characters (except for the
last character) must be a dash, lowercase letter, or digit. The last
character must be a lowercase letter or digit. |
| `target_service_accounts` | Vec<String> |  | A list of service accounts indicating sets of instances located in the
network that may make network connections as specified inallowed[].targetServiceAccounts cannot be used at the same time astargetTags or sourceTags.
If neither targetServiceAccounts nor targetTags
are specified, the firewall rule applies to all instances on the specified
network. |
| `params` | String |  | Input only. [Input Only] Additional params passed with the request, but not persisted
as part of resource payload. |
| `destination_ranges` | Vec<String> |  | If destination ranges are specified, the firewall rule applies only to
traffic that has destination IP address in these ranges. These ranges must
be expressed inCIDR format. Both IPv4 and IPv6 are supported. |
| `kind` | String |  | [Output Only] Type of the resource. Always compute#firewall
for firewall rules. |
| `direction` | String |  | Direction of traffic to which this firewall applies, either `INGRESS` or
`EGRESS`. The default is `INGRESS`. For `EGRESS` traffic, you cannot
specify the sourceTags fields. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `description` | String | An optional description of this resource. Provide this field when you
create the resource. |
| `source_ranges` | Vec<String> | If source ranges are specified, the firewall rule applies only to traffic
that has a source IP address in these ranges. These ranges must be
expressed inCIDR format. One or both of sourceRanges
and sourceTags may be set.
If both fields are set, the rule applies to traffic that has a
source IP address within sourceRanges OR a source IP
from a resource with a matching tag listed in thesourceTags field. The connection does not need to match
both fields for the rule to
apply. Both IPv4 and IPv6 are supported. |
| `source_service_accounts` | Vec<String> | If source service accounts are specified, the firewall rules apply only to
traffic originating from an instance with a service account in this list.
Source service accounts cannot be used to control traffic to an instance's
external IP address because service accounts are associated with an
instance, not an IP address.sourceRanges can be set at the same time assourceServiceAccounts.
If both are set, the firewall applies to traffic that
has a source IP address within the sourceRanges OR a source
IP that belongs to an instance with service account listed insourceServiceAccount. The connection does not need to match
both fields for the firewall to apply.sourceServiceAccounts cannot be used at the same time assourceTags or targetTags. |
| `allowed` | Vec<String> | The list of ALLOW rules specified by this firewall. Each rule specifies a
protocol and port-range tuple that describes a permitted connection. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `disabled` | bool | Denotes whether the firewall rule is disabled. When set to true, the
firewall rule is not enforced and the network behaves as if it did not
exist. If this is unspecified, the firewall rule will be enabled. |
| `enable_logging` | bool | Deprecated in favor of enable in LogConfig.
This field denotes whether to enable logging for a particular
firewall rule. If logging is enabled, logs will be exported t
Cloud Logging. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `denied` | Vec<String> | The list of DENY rules specified by this firewall. Each rule specifies a
protocol and port-range tuple that describes a denied connection. |
| `log_config` | String | This field denotes the logging options for a particular firewall rule. If
logging is enabled, logs will be exported to Cloud Logging. |
| `network` | String | URL of the network resource for this firewall rule. If not
specified when creating a firewall rule, the default network
is used:

global/networks/default

If you choose to specify this field, you can specify the network as a full 
or partial URL. For example, the following are all valid URLs: 
   
   - 
   https://www.googleapis.com/compute/v1/projects/myproject/global/networks/my-network 
   - projects/myproject/global/networks/my-network 
   - global/networks/default |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `priority` | i64 | Priority for this rule.
This is an integer between `0` and `65535`, both inclusive.
The default value is `1000`.
Relative priorities determine which rule takes effect if multiple rules
apply. Lower values indicate higher priority. For example, a rule with
priority `0` has higher precedence than a rule with priority `1`.
DENY rules take precedence over ALLOW rules if they have equal priority.
Note that VPC networks have implied
rules with a priority of `65535`. To avoid conflicts with the implied
rules, use a priority number less than `65535`. |
| `source_tags` | Vec<String> | If source tags are specified, the firewall rule applies only to traffic
with source IPs that match the primary network interfaces of VM instances
that have the tag and are in the same VPC network.
Source tags cannot be used to control traffic to an instance's external IP
address, it only applies to traffic between instances in the same virtual
network. Because tags are associated with instances, not IP addresses.
One or both of sourceRanges and sourceTags may be
set. If both fields are set, the firewall applies to traffic that has a
source IP address within sourceRanges OR a source IP from a
resource with a matching tag listed in the sourceTags
field. The connection does not need to match both fields for the
firewall to apply. |
| `target_tags` | Vec<String> | A list of tags that controls which instances the firewall rule
applies to. If targetTags are specified, then the firewall
rule applies only to instances in the VPC network that have one of those
tags. If no targetTags are specified, the firewall rule
applies to all instances on the specified network. |
| `name` | String | Name of the resource; provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?`. The first character
must be a lowercase letter, and all following characters (except for the
last character) must be a dash, lowercase letter, or digit. The last
character must be a lowercase letter or digit. |
| `target_service_accounts` | Vec<String> | A list of service accounts indicating sets of instances located in the
network that may make network connections as specified inallowed[].targetServiceAccounts cannot be used at the same time astargetTags or sourceTags.
If neither targetServiceAccounts nor targetTags
are specified, the firewall rule applies to all instances on the specified
network. |
| `params` | String | Input only. [Input Only] Additional params passed with the request, but not persisted
as part of resource payload. |
| `destination_ranges` | Vec<String> | If destination ranges are specified, the firewall rule applies only to
traffic that has destination IP address in these ranges. These ranges must
be expressed inCIDR format. Both IPv4 and IPv6 are supported. |
| `kind` | String | [Output Only] Type of the resource. Always compute#firewall
for firewall rules. |
| `direction` | String | Direction of traffic to which this firewall applies, either `INGRESS` or
`EGRESS`. The default is `INGRESS`. For `EGRESS` traffic, you cannot
specify the sourceTags fields. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create firewall
firewall = provider.compute_api.Firewall {
    project = "value"  # Project ID for this request.
}

# Access firewall outputs
firewall_id = firewall.id
firewall_description = firewall.description
firewall_source_ranges = firewall.source_ranges
firewall_source_service_accounts = firewall.source_service_accounts
firewall_allowed = firewall.allowed
firewall_creation_timestamp = firewall.creation_timestamp
firewall_disabled = firewall.disabled
firewall_enable_logging = firewall.enable_logging
firewall_id = firewall.id
firewall_denied = firewall.denied
firewall_log_config = firewall.log_config
firewall_network = firewall.network
firewall_self_link = firewall.self_link
firewall_priority = firewall.priority
firewall_source_tags = firewall.source_tags
firewall_target_tags = firewall.target_tags
firewall_name = firewall.name
firewall_target_service_accounts = firewall.target_service_accounts
firewall_params = firewall.params
firewall_destination_ranges = firewall.destination_ranges
firewall_kind = firewall.kind
firewall_direction = firewall.direction
```

---


### Reservation_sub_block

Sets the access control policy on the specified resource.
Replaces any existing policy.

**Operations**: ✅ Create ✅ Read

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `policy` | String |  | REQUIRED: The complete policy to be applied to the 'resource'. The size of
the policy is limited to a few 10s of KB. An empty policy is in general a
valid policy but certain services (like Projects) might reject them. |
| `etag` | String |  | Flatten Policy to create a backward compatible wire-format.
Deprecated. Use 'policy' to specify the etag. |
| `bindings` | Vec<String> |  | Flatten Policy to create a backwacd compatible wire-format.
Deprecated. Use 'policy' to specify bindings. |
| `parent_resource` | String | ✅ | Name or id of parent resource of the resource for this request. |
| `zone` | String | ✅ | The name of the zone for this request. |
| `project` | String | ✅ | Project ID for this request. |
| `resource` | String | ✅ | Name or id of the resource for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `resource` | String |  |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create reservation_sub_block
reservation_sub_block = provider.compute_api.Reservation_sub_block {
    parent_resource = "value"  # Name or id of parent resource of the resource for this request.
    zone = "value"  # The name of the zone for this request.
    project = "value"  # Project ID for this request.
    resource = "value"  # Name or id of the resource for this request.
}

# Access reservation_sub_block outputs
reservation_sub_block_id = reservation_sub_block.id
reservation_sub_block_resource = reservation_sub_block.resource
```

---


### Network_attachment

Creates a NetworkAttachment in the specified project in the given scope
using the parameters that are included in the request.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `region` | String |  | [Output Only] URL of the region where the network attachment resides.
This field applies only to the region resource. You must specify this
field as part of the HTTP request URL. It is not settable as a field in
the request body. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `network` | String |  | [Output Only] The URL of the network which the Network Attachment
belongs to. Practically it is inferred by fetching the network of the first
subnetwork associated. Because it is required that all the subnetworks must
be from the same network, it is assured that the Network Attachment belongs
to the same network as all the subnetworks. |
| `self_link_with_id` | String |  | [Output Only] Server-defined URL for this resource's resource id. |
| `name` | String |  | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `fingerprint` | String |  | Fingerprint of this resource. A hash of the contents stored
in this object. This field is used in optimistic locking. An up-to-date
fingerprint must be provided in order to patch. |
| `connection_preference` | String |  |  |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `connection_endpoints` | Vec<String> |  | [Output Only] An array of connections for all the producers connected
to this network attachment. |
| `kind` | String |  | [Output Only] Type of the resource. |
| `subnetworks` | Vec<String> |  | An array of URLs where each entry is the URL of a subnet
provided by the service consumer to use for
endpoints in the producers that connect to this network attachment. |
| `producer_reject_lists` | Vec<String> |  | Projects that are not allowed to connect to this network attachment.
The project can be specified using its id or number. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `id` | String |  | [Output Only] The unique identifier for the resource type. The server
generates this identifier. |
| `producer_accept_lists` | Vec<String> |  | Projects that are allowed to connect to this network attachment.
The project can be specified using its id or number. |
| `project` | String | ✅ | Project ID for this request. |
| `region` | String | ✅ | Name of the region of this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `region` | String | [Output Only] URL of the region where the network attachment resides.
This field applies only to the region resource. You must specify this
field as part of the HTTP request URL. It is not settable as a field in
the request body. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `network` | String | [Output Only] The URL of the network which the Network Attachment
belongs to. Practically it is inferred by fetching the network of the first
subnetwork associated. Because it is required that all the subnetworks must
be from the same network, it is assured that the Network Attachment belongs
to the same network as all the subnetworks. |
| `self_link_with_id` | String | [Output Only] Server-defined URL for this resource's resource id. |
| `name` | String | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `fingerprint` | String | Fingerprint of this resource. A hash of the contents stored
in this object. This field is used in optimistic locking. An up-to-date
fingerprint must be provided in order to patch. |
| `connection_preference` | String |  |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `connection_endpoints` | Vec<String> | [Output Only] An array of connections for all the producers connected
to this network attachment. |
| `kind` | String | [Output Only] Type of the resource. |
| `subnetworks` | Vec<String> | An array of URLs where each entry is the URL of a subnet
provided by the service consumer to use for
endpoints in the producers that connect to this network attachment. |
| `producer_reject_lists` | Vec<String> | Projects that are not allowed to connect to this network attachment.
The project can be specified using its id or number. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `id` | String | [Output Only] The unique identifier for the resource type. The server
generates this identifier. |
| `producer_accept_lists` | Vec<String> | Projects that are allowed to connect to this network attachment.
The project can be specified using its id or number. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create network_attachment
network_attachment = provider.compute_api.Network_attachment {
    project = "value"  # Project ID for this request.
    region = "value"  # Name of the region of this request.
}

# Access network_attachment outputs
network_attachment_id = network_attachment.id
network_attachment_region = network_attachment.region
network_attachment_description = network_attachment.description
network_attachment_network = network_attachment.network
network_attachment_self_link_with_id = network_attachment.self_link_with_id
network_attachment_name = network_attachment.name
network_attachment_fingerprint = network_attachment.fingerprint
network_attachment_connection_preference = network_attachment.connection_preference
network_attachment_creation_timestamp = network_attachment.creation_timestamp
network_attachment_connection_endpoints = network_attachment.connection_endpoints
network_attachment_kind = network_attachment.kind
network_attachment_subnetworks = network_attachment.subnetworks
network_attachment_producer_reject_lists = network_attachment.producer_reject_lists
network_attachment_self_link = network_attachment.self_link
network_attachment_id = network_attachment.id
network_attachment_producer_accept_lists = network_attachment.producer_accept_lists
```

---


### Region_instant_snapshot

Creates an instant snapshot in the specified region.

**Operations**: ✅ Create ✅ Read ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `name` | String |  | Name of the resource; provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must be
a dash, lowercase letter, or digit, except the last character, which cannot
be a dash. |
| `zone` | String |  | [Output Only] URL of the zone where the instant snapshot resides.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `label_fingerprint` | String |  | A fingerprint for the labels being applied to this InstantSnapshot, which
is essentially a hash of the labels set used for optimistic locking. The
fingerprint is initially generated by Compute Engine and changes after
every request to modify or update labels. You must always provide an
up-to-date fingerprint hash in order to update or change labels,
otherwise the request will fail with error412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve a InstantSnapshot. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `satisfies_pzi` | bool |  | Output only. Reserved for future use. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `source_disk_id` | String |  | [Output Only] The ID value of the disk used to create this InstantSnapshot.
This value may be used to determine whether the InstantSnapshot
was taken from the current or a previous instance of a given disk name. |
| `region` | String |  | [Output Only] URL of the region where the instant snapshot resides.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `self_link_with_id` | String |  | [Output Only] Server-defined URL for this resource's resource id. |
| `source_disk` | String |  | URL of the source disk used to create this instant snapshot.
Note that the source disk must be in the same zone/region as the
instant snapshot to be created. This can be a full or valid partial URL.
For example, the following are valid values:
   
   
     - 
       https://www.googleapis.com/compute/v1/projects/project/zones/zone/disks/disk
     
     - 
       https://www.googleapis.com/compute/v1/projects/project/regions/region/disks/disk
     
     - 
       projects/project/zones/zone/disks/disk
     
     - 
       projects/project/regions/region/disks/disk
     
     - 
       zones/zone/disks/disk
     
     - 
       regions/region/disks/disk |
| `satisfies_pzs` | bool |  | [Output Only] Reserved for future use. |
| `architecture` | String |  | [Output Only] The architecture of the instant snapshot. Valid values are
ARM64 or X86_64. |
| `kind` | String |  | [Output Only] Type of the resource. Alwayscompute#instantSnapshot for InstantSnapshot resources. |
| `resource_status` | String |  | [Output Only] Status information for the instant snapshot resource. |
| `disk_size_gb` | String |  | [Output Only] Size of the source disk, specified in GB. |
| `labels` | HashMap<String, String> |  | Labels to apply to this InstantSnapshot. These can be later modified by
the setLabels method.
Label values may be empty. |
| `status` | String |  | [Output Only] The status of the instantSnapshot. This can beCREATING, DELETING, FAILED, orREADY. |
| `project` | String | ✅ | Project ID for this request. |
| `region` | String | ✅ | Name of the region for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `name` | String | Name of the resource; provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must be
a dash, lowercase letter, or digit, except the last character, which cannot
be a dash. |
| `zone` | String | [Output Only] URL of the zone where the instant snapshot resides.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `label_fingerprint` | String | A fingerprint for the labels being applied to this InstantSnapshot, which
is essentially a hash of the labels set used for optimistic locking. The
fingerprint is initially generated by Compute Engine and changes after
every request to modify or update labels. You must always provide an
up-to-date fingerprint hash in order to update or change labels,
otherwise the request will fail with error412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve a InstantSnapshot. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `satisfies_pzi` | bool | Output only. Reserved for future use. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `source_disk_id` | String | [Output Only] The ID value of the disk used to create this InstantSnapshot.
This value may be used to determine whether the InstantSnapshot
was taken from the current or a previous instance of a given disk name. |
| `region` | String | [Output Only] URL of the region where the instant snapshot resides.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `self_link_with_id` | String | [Output Only] Server-defined URL for this resource's resource id. |
| `source_disk` | String | URL of the source disk used to create this instant snapshot.
Note that the source disk must be in the same zone/region as the
instant snapshot to be created. This can be a full or valid partial URL.
For example, the following are valid values:
   
   
     - 
       https://www.googleapis.com/compute/v1/projects/project/zones/zone/disks/disk
     
     - 
       https://www.googleapis.com/compute/v1/projects/project/regions/region/disks/disk
     
     - 
       projects/project/zones/zone/disks/disk
     
     - 
       projects/project/regions/region/disks/disk
     
     - 
       zones/zone/disks/disk
     
     - 
       regions/region/disks/disk |
| `satisfies_pzs` | bool | [Output Only] Reserved for future use. |
| `architecture` | String | [Output Only] The architecture of the instant snapshot. Valid values are
ARM64 or X86_64. |
| `kind` | String | [Output Only] Type of the resource. Alwayscompute#instantSnapshot for InstantSnapshot resources. |
| `resource_status` | String | [Output Only] Status information for the instant snapshot resource. |
| `disk_size_gb` | String | [Output Only] Size of the source disk, specified in GB. |
| `labels` | HashMap<String, String> | Labels to apply to this InstantSnapshot. These can be later modified by
the setLabels method.
Label values may be empty. |
| `status` | String | [Output Only] The status of the instantSnapshot. This can beCREATING, DELETING, FAILED, orREADY. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create region_instant_snapshot
region_instant_snapshot = provider.compute_api.Region_instant_snapshot {
    project = "value"  # Project ID for this request.
    region = "value"  # Name of the region for this request.
}

# Access region_instant_snapshot outputs
region_instant_snapshot_id = region_instant_snapshot.id
region_instant_snapshot_name = region_instant_snapshot.name
region_instant_snapshot_zone = region_instant_snapshot.zone
region_instant_snapshot_label_fingerprint = region_instant_snapshot.label_fingerprint
region_instant_snapshot_description = region_instant_snapshot.description
region_instant_snapshot_satisfies_pzi = region_instant_snapshot.satisfies_pzi
region_instant_snapshot_self_link = region_instant_snapshot.self_link
region_instant_snapshot_id = region_instant_snapshot.id
region_instant_snapshot_source_disk_id = region_instant_snapshot.source_disk_id
region_instant_snapshot_region = region_instant_snapshot.region
region_instant_snapshot_creation_timestamp = region_instant_snapshot.creation_timestamp
region_instant_snapshot_self_link_with_id = region_instant_snapshot.self_link_with_id
region_instant_snapshot_source_disk = region_instant_snapshot.source_disk
region_instant_snapshot_satisfies_pzs = region_instant_snapshot.satisfies_pzs
region_instant_snapshot_architecture = region_instant_snapshot.architecture
region_instant_snapshot_kind = region_instant_snapshot.kind
region_instant_snapshot_resource_status = region_instant_snapshot.resource_status
region_instant_snapshot_disk_size_gb = region_instant_snapshot.disk_size_gb
region_instant_snapshot_labels = region_instant_snapshot.labels
region_instant_snapshot_status = region_instant_snapshot.status
```

---


### Region_autoscaler

Creates an autoscaler in the specified project using
the data included in the request.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `autoscaling_policy` | String |  | The configuration parameters for the autoscaling algorithm. You can define
one or more signals for an autoscaler: cpuUtilization,customMetricUtilizations, andloadBalancingUtilization.

If none of these are specified, the default will be to autoscale based oncpuUtilization to 0.6 or 60%. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `name` | String |  | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `region` | String |  | [Output Only] URL of theregion
where the instance group resides (for autoscalers living in regional
scope). |
| `status` | String |  | [Output Only] The status of the autoscaler configuration. Current set of
possible values:
   
   - PENDING:
     Autoscaler backend hasn't read new/updated configuration.
   - DELETING:
     Configuration is being deleted.
   - ACTIVE:
     Configuration is acknowledged to be effective. Some warnings might
     be present in the statusDetails field.
   - ERROR:
     Configuration has errors. Actionable for users. Details are present in
     the statusDetails field.


New values might be added in the future. |
| `kind` | String |  | [Output Only] Type of the resource. Always compute#autoscaler
for autoscalers. |
| `scaling_schedule_status` | HashMap<String, String> |  | [Output Only] Status information of existing scaling schedules. |
| `status_details` | Vec<String> |  | [Output Only] Human-readable details about the current state of the
autoscaler. Read the documentation forCommonly
returned status messages for examples of status messages you might
encounter. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `zone` | String |  | [Output Only] URL of thezone
where the instance group resides (for autoscalers living in zonal scope). |
| `target` | String |  | URL of the managed instance group that this autoscaler will scale. This
field is required when creating an autoscaler. |
| `recommended_size` | i64 |  | [Output Only] Target recommended MIG size (number of instances) computed by
autoscaler. Autoscaler calculates the recommended MIG size even when the
autoscaling policy mode is different from ON. This field is empty when
autoscaler is not connected to an existing managed instance group or
autoscaler did not generate its prediction. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `project` | String | ✅ | Project ID for this request. |
| `region` | String | ✅ | Name of the region scoping this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `autoscaling_policy` | String | The configuration parameters for the autoscaling algorithm. You can define
one or more signals for an autoscaler: cpuUtilization,customMetricUtilizations, andloadBalancingUtilization.

If none of these are specified, the default will be to autoscale based oncpuUtilization to 0.6 or 60%. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `name` | String | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `region` | String | [Output Only] URL of theregion
where the instance group resides (for autoscalers living in regional
scope). |
| `status` | String | [Output Only] The status of the autoscaler configuration. Current set of
possible values:
   
   - PENDING:
     Autoscaler backend hasn't read new/updated configuration.
   - DELETING:
     Configuration is being deleted.
   - ACTIVE:
     Configuration is acknowledged to be effective. Some warnings might
     be present in the statusDetails field.
   - ERROR:
     Configuration has errors. Actionable for users. Details are present in
     the statusDetails field.


New values might be added in the future. |
| `kind` | String | [Output Only] Type of the resource. Always compute#autoscaler
for autoscalers. |
| `scaling_schedule_status` | HashMap<String, String> | [Output Only] Status information of existing scaling schedules. |
| `status_details` | Vec<String> | [Output Only] Human-readable details about the current state of the
autoscaler. Read the documentation forCommonly
returned status messages for examples of status messages you might
encounter. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `zone` | String | [Output Only] URL of thezone
where the instance group resides (for autoscalers living in zonal scope). |
| `target` | String | URL of the managed instance group that this autoscaler will scale. This
field is required when creating an autoscaler. |
| `recommended_size` | i64 | [Output Only] Target recommended MIG size (number of instances) computed by
autoscaler. Autoscaler calculates the recommended MIG size even when the
autoscaling policy mode is different from ON. This field is empty when
autoscaler is not connected to an existing managed instance group or
autoscaler did not generate its prediction. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create region_autoscaler
region_autoscaler = provider.compute_api.Region_autoscaler {
    project = "value"  # Project ID for this request.
    region = "value"  # Name of the region scoping this request.
}

# Access region_autoscaler outputs
region_autoscaler_id = region_autoscaler.id
region_autoscaler_autoscaling_policy = region_autoscaler.autoscaling_policy
region_autoscaler_description = region_autoscaler.description
region_autoscaler_id = region_autoscaler.id
region_autoscaler_name = region_autoscaler.name
region_autoscaler_region = region_autoscaler.region
region_autoscaler_status = region_autoscaler.status
region_autoscaler_kind = region_autoscaler.kind
region_autoscaler_scaling_schedule_status = region_autoscaler.scaling_schedule_status
region_autoscaler_status_details = region_autoscaler.status_details
region_autoscaler_self_link = region_autoscaler.self_link
region_autoscaler_zone = region_autoscaler.zone
region_autoscaler_target = region_autoscaler.target
region_autoscaler_recommended_size = region_autoscaler.recommended_size
region_autoscaler_creation_timestamp = region_autoscaler.creation_timestamp
```

---


### Region_ssl_policie

Creates a new policy in the specified project and region using the data
included in the request.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `name` | String |  | Name of the resource. The name must be 1-63 characters long, and comply
with RFC1035. Specifically, the name must be 1-63 characters
long and match the regular expression
`[a-z]([-a-z0-9]*[a-z0-9])?` which means the first character
must be a lowercase letter, and all following characters must be a dash,
lowercase letter, or digit, except the last character, which cannot be a
dash. |
| `custom_features` | Vec<String> |  | A list of features enabled when the selected profile is CUSTOM. The
 method returns the set of features that can be
specified in this list. This field must be empty if the profile is notCUSTOM. |
| `enabled_features` | Vec<String> |  | [Output Only] The list of features enabled in the SSL policy. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `kind` | String |  | [Output only] Type of the resource. Alwayscompute#sslPolicyfor SSL policies. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `min_tls_version` | String |  | The minimum version of SSL protocol that can be used by the clients to
establish a connection with the load balancer. This can be one ofTLS_1_0, TLS_1_1, TLS_1_2,TLS_1_3. When set to TLS_1_3, the profile field
must be set to RESTRICTED. |
| `profile` | String |  | Profile specifies the set of SSL features that can be used by the load
balancer when negotiating SSL with clients. This can be one ofCOMPATIBLE, MODERN, RESTRICTED, orCUSTOM. If using CUSTOM, the set of SSL features
to enable must be specified in the customFeatures field. |
| `region` | String |  | [Output Only] URL of the region where the regional SSL policy
resides. This field is not applicable to global SSL policies. |
| `fingerprint` | String |  | Fingerprint of this resource. A hash of the contents stored in this object.
This field is used in optimistic locking. This field will be ignored when
inserting a SslPolicy. An up-to-date fingerprint must be
provided in order to update the SslPolicy, otherwise the
request will fail with error 412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve an SslPolicy. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `warnings` | Vec<String> |  | [Output Only] If potential misconfigurations are detected for this
SSL policy, this field will be populated with warning messages. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `project` | String | ✅ | Project ID for this request. |
| `region` | String | ✅ | Name of the region scoping this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `name` | String | Name of the resource. The name must be 1-63 characters long, and comply
with RFC1035. Specifically, the name must be 1-63 characters
long and match the regular expression
`[a-z]([-a-z0-9]*[a-z0-9])?` which means the first character
must be a lowercase letter, and all following characters must be a dash,
lowercase letter, or digit, except the last character, which cannot be a
dash. |
| `custom_features` | Vec<String> | A list of features enabled when the selected profile is CUSTOM. The
 method returns the set of features that can be
specified in this list. This field must be empty if the profile is notCUSTOM. |
| `enabled_features` | Vec<String> | [Output Only] The list of features enabled in the SSL policy. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `kind` | String | [Output only] Type of the resource. Alwayscompute#sslPolicyfor SSL policies. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `min_tls_version` | String | The minimum version of SSL protocol that can be used by the clients to
establish a connection with the load balancer. This can be one ofTLS_1_0, TLS_1_1, TLS_1_2,TLS_1_3. When set to TLS_1_3, the profile field
must be set to RESTRICTED. |
| `profile` | String | Profile specifies the set of SSL features that can be used by the load
balancer when negotiating SSL with clients. This can be one ofCOMPATIBLE, MODERN, RESTRICTED, orCUSTOM. If using CUSTOM, the set of SSL features
to enable must be specified in the customFeatures field. |
| `region` | String | [Output Only] URL of the region where the regional SSL policy
resides. This field is not applicable to global SSL policies. |
| `fingerprint` | String | Fingerprint of this resource. A hash of the contents stored in this object.
This field is used in optimistic locking. This field will be ignored when
inserting a SslPolicy. An up-to-date fingerprint must be
provided in order to update the SslPolicy, otherwise the
request will fail with error 412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve an SslPolicy. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `warnings` | Vec<String> | [Output Only] If potential misconfigurations are detected for this
SSL policy, this field will be populated with warning messages. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create region_ssl_policie
region_ssl_policie = provider.compute_api.Region_ssl_policie {
    project = "value"  # Project ID for this request.
    region = "value"  # Name of the region scoping this request.
}

# Access region_ssl_policie outputs
region_ssl_policie_id = region_ssl_policie.id
region_ssl_policie_name = region_ssl_policie.name
region_ssl_policie_custom_features = region_ssl_policie.custom_features
region_ssl_policie_enabled_features = region_ssl_policie.enabled_features
region_ssl_policie_creation_timestamp = region_ssl_policie.creation_timestamp
region_ssl_policie_kind = region_ssl_policie.kind
region_ssl_policie_id = region_ssl_policie.id
region_ssl_policie_min_tls_version = region_ssl_policie.min_tls_version
region_ssl_policie_profile = region_ssl_policie.profile
region_ssl_policie_region = region_ssl_policie.region
region_ssl_policie_fingerprint = region_ssl_policie.fingerprint
region_ssl_policie_description = region_ssl_policie.description
region_ssl_policie_warnings = region_ssl_policie.warnings
region_ssl_policie_self_link = region_ssl_policie.self_link
```

---


### Global_vm_extension_policie

Gets details of a global VM extension policy.

**Operations**: ✅ Read

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `instance_selectors` | Vec<String> | Optional. Selector to target VMs for a policy.
There is a logical "AND" between instance_selectors. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `name` | String | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?`
which means the first character must be a lowercase letter, and all
following characters must be a dash, lowercase letter, or digit, except
the last character, which cannot be a dash. |
| `extension_policies` | HashMap<String, String> | Required. Map from extension (eg: "cloudops") to its policy configuration.
The key is the name of the extension. |
| `rollout_operation` | String | Required. The rollout strategy and status. |
| `scoped_resource_status` | String | [Output Only] The scoped resource status. It's only for tracking the
purging status of the policy. |
| `self_link_with_id` | String | [Output Only] Server-defined URL for this resource's resource id. |
| `self_link` | String | [Output Only] Server-defined fully-qualified URL for this resource. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `kind` | String | [Output Only] Type of the resource. Alwayscompute#globalVmExtensionPolicy for globalVmExtensionPolicies. |
| `priority` | i64 | Optional. Used to resolve conflicts when multiple policies are active for the same
extension. Defaults to 0.

Larger the number, higher the priority. When the priority is the same,
the policy with the newer create time has higher priority. |
| `update_timestamp` | String | [Output Only] Update timestamp inRFC3339
text format. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Access global_vm_extension_policie outputs
global_vm_extension_policie_id = global_vm_extension_policie.id
global_vm_extension_policie_id = global_vm_extension_policie.id
global_vm_extension_policie_instance_selectors = global_vm_extension_policie.instance_selectors
global_vm_extension_policie_creation_timestamp = global_vm_extension_policie.creation_timestamp
global_vm_extension_policie_name = global_vm_extension_policie.name
global_vm_extension_policie_extension_policies = global_vm_extension_policie.extension_policies
global_vm_extension_policie_rollout_operation = global_vm_extension_policie.rollout_operation
global_vm_extension_policie_scoped_resource_status = global_vm_extension_policie.scoped_resource_status
global_vm_extension_policie_self_link_with_id = global_vm_extension_policie.self_link_with_id
global_vm_extension_policie_self_link = global_vm_extension_policie.self_link
global_vm_extension_policie_description = global_vm_extension_policie.description
global_vm_extension_policie_kind = global_vm_extension_policie.kind
global_vm_extension_policie_priority = global_vm_extension_policie.priority
global_vm_extension_policie_update_timestamp = global_vm_extension_policie.update_timestamp
```

---


### Instance_group_manager_resize_request

Creates a new resize request that starts provisioning VMs immediately
or queues VM creation.

**Operations**: ✅ Create ✅ Read ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `instances` | Vec<String> |  | The names of instances to be created by this resize request. The number of
names specified determines the number of instances to create. The group's
target size will be increased by this number. This field cannot be used
together with 'resize_by'. |
| `self_link_with_id` | String |  | [Output Only] Server-defined URL for this resource with the resource id. |
| `requested_run_duration` | String |  | Requested run duration for instances that will be created by this request.
At the end of the run duration instance will be deleted. |
| `id` | String |  | [Output Only] A unique identifier for this resource type. The server
generates this identifier. |
| `kind` | String |  | [Output Only] The resource type, which is alwayscompute#instanceGroupManagerResizeRequest for
resize requests. |
| `resize_by` | i64 |  | The number of instances to be created by this resize request. The group's
target size will be increased by this number. This field cannot be used
together with 'instances'. |
| `self_link` | String |  | [Output Only] The URL for this resize request. The server defines
this URL. |
| `count` | i64 |  | This field is deprecated, please use resize_by instead.
The count of instances to create as part of this resize request. |
| `status` | String |  | [Output only] Status of the request. |
| `zone` | String |  | [Output Only] The URL of azone
where the resize request is located. Populated only for zonal resize
requests. |
| `region` | String |  | [Output Only] The URL of aregion
where the resize request is located. Populated only for regional resize
requests. |
| `creation_timestamp` | String |  | [Output Only] The creation timestamp for this resize request inRFC3339
text format. |
| `name` | String |  | The name of this resize request. The name must be 1-63 characters
long, and comply withRFC1035. |
| `state` | String |  | [Output only] Current state of the request. |
| `description` | String |  | An optional description of this resource. |
| `instance_group_manager` | String | ✅ | The name of the managed instance group to which the resize request will
be added.
Name should conform to RFC1035 or be a resource ID. |
| `project` | String | ✅ | Project ID for this request. |
| `zone` | String | ✅ | The name of thezone where the managed
instance group is located and where the resize request will be created.
Name should conform to RFC1035. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `instances` | Vec<String> | The names of instances to be created by this resize request. The number of
names specified determines the number of instances to create. The group's
target size will be increased by this number. This field cannot be used
together with 'resize_by'. |
| `self_link_with_id` | String | [Output Only] Server-defined URL for this resource with the resource id. |
| `requested_run_duration` | String | Requested run duration for instances that will be created by this request.
At the end of the run duration instance will be deleted. |
| `id` | String | [Output Only] A unique identifier for this resource type. The server
generates this identifier. |
| `kind` | String | [Output Only] The resource type, which is alwayscompute#instanceGroupManagerResizeRequest for
resize requests. |
| `resize_by` | i64 | The number of instances to be created by this resize request. The group's
target size will be increased by this number. This field cannot be used
together with 'instances'. |
| `self_link` | String | [Output Only] The URL for this resize request. The server defines
this URL. |
| `count` | i64 | This field is deprecated, please use resize_by instead.
The count of instances to create as part of this resize request. |
| `status` | String | [Output only] Status of the request. |
| `zone` | String | [Output Only] The URL of azone
where the resize request is located. Populated only for zonal resize
requests. |
| `region` | String | [Output Only] The URL of aregion
where the resize request is located. Populated only for regional resize
requests. |
| `creation_timestamp` | String | [Output Only] The creation timestamp for this resize request inRFC3339
text format. |
| `name` | String | The name of this resize request. The name must be 1-63 characters
long, and comply withRFC1035. |
| `state` | String | [Output only] Current state of the request. |
| `description` | String | An optional description of this resource. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create instance_group_manager_resize_request
instance_group_manager_resize_request = provider.compute_api.Instance_group_manager_resize_request {
    instance_group_manager = "value"  # The name of the managed instance group to which the resize request will
be added.
Name should conform to RFC1035 or be a resource ID.
    project = "value"  # Project ID for this request.
    zone = "value"  # The name of thezone where the managed
instance group is located and where the resize request will be created.
Name should conform to RFC1035.
}

# Access instance_group_manager_resize_request outputs
instance_group_manager_resize_request_id = instance_group_manager_resize_request.id
instance_group_manager_resize_request_instances = instance_group_manager_resize_request.instances
instance_group_manager_resize_request_self_link_with_id = instance_group_manager_resize_request.self_link_with_id
instance_group_manager_resize_request_requested_run_duration = instance_group_manager_resize_request.requested_run_duration
instance_group_manager_resize_request_id = instance_group_manager_resize_request.id
instance_group_manager_resize_request_kind = instance_group_manager_resize_request.kind
instance_group_manager_resize_request_resize_by = instance_group_manager_resize_request.resize_by
instance_group_manager_resize_request_self_link = instance_group_manager_resize_request.self_link
instance_group_manager_resize_request_count = instance_group_manager_resize_request.count
instance_group_manager_resize_request_status = instance_group_manager_resize_request.status
instance_group_manager_resize_request_zone = instance_group_manager_resize_request.zone
instance_group_manager_resize_request_region = instance_group_manager_resize_request.region
instance_group_manager_resize_request_creation_timestamp = instance_group_manager_resize_request.creation_timestamp
instance_group_manager_resize_request_name = instance_group_manager_resize_request.name
instance_group_manager_resize_request_state = instance_group_manager_resize_request.state
instance_group_manager_resize_request_description = instance_group_manager_resize_request.description
```

---


### Target_https_proxie

Creates a TargetHttpsProxy resource in the specified
project using the data included in the request.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `quic_override` | String |  | Specifies the QUIC override policy for this TargetHttpsProxy resource. This
setting determines whether the load balancer attempts to negotiate QUIC
with clients.
You can specify NONE, ENABLE, orDISABLE.
   
   - When quic-override is set to NONE,
   Google manages whether QUIC is used.
   - When quic-override is set to ENABLE, the
   load balancer uses QUIC when possible.
   - When quic-override is set to DISABLE, the
   load balancer doesn't use QUIC.
   - If the quic-override flag is not specified,NONE is implied. |
| `tls_early_data` | String |  | Specifies whether TLS 1.3 0-RTT Data ("Early Data") should be accepted
for this service. Early Data allows a TLS resumption handshake to include
the initial application payload (a HTTP request) alongside the handshake,
reducing the effective round trips to "zero". This applies to TLS 1.3
connections over TCP (HTTP/2) as well as over UDP (QUIC/h3).


This can improve application performance, especially on networks where
interruptions may be common, such as on mobile.


Requests with Early Data will have the "Early-Data" HTTP header set on
the request, with a value of "1", to allow the backend to determine whether
Early Data was included.


Note: TLS Early Data may allow requests to be replayed, as the data is
sent to the backend before the handshake has fully completed. Applications
that allow idempotent HTTP methods to make non-idempotent changes, such as
a GET request updating a database, should not accept Early Data on those
requests, and reject requests with the "Early-Data: 1" HTTP header by
returning a HTTP 425 (Too Early) status code, in order to remain RFC
compliant.


The default value is DISABLED. |
| `ssl_certificates` | Vec<String> |  | URLs to SslCertificate resources that are used to authenticate
connections between users and the load balancer. At least one SSL
certificate must be specified. SslCertificates do not apply when the load
balancing scheme is set to INTERNAL_SELF_MANAGED.

The URLs should refer to a SSL Certificate resource or Certificate Manager
Certificate resource. Mixing Classic Certificates and Certificate Manager
Certificates is not allowed. Certificate Manager Certificates must include
the certificatemanager API namespace. Using Certificate Manager
Certificates in this field is not supported by Global external Application
Load Balancer or Classic Application Load Balancer, use certificate_map
instead.

Currently, you may specify up to 15 Classic SSL Certificates or up to 100
Certificate Manager Certificates.

Certificate Manager Certificates accepted formats are:
   
   - //certificatemanager.googleapis.com/projects/{project}/locations/{location}/certificates/{resourceName}.
   - https://certificatemanager.googleapis.com/v1alpha1/projects/{project}/locations/{location}/certificates/{resourceName}. |
| `http_keep_alive_timeout_sec` | i64 |  | Specifies how long to keep a connection open, after completing a response,
while there is no matching traffic (in seconds). If an HTTP keep-alive is
not specified, a default value (610 seconds) will be used.

For global external Application Load Balancers, the minimum allowed value
is 5 seconds and the maximum allowed value is 1200 seconds.

For classic Application Load Balancers, this option is not supported. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `ssl_policy` | String |  | URL of SslPolicy resource that will be associated with the TargetHttpsProxy
resource. If not set, the TargetHttpsProxy resource has no
SSL policy configured. |
| `url_map` | String |  | A fully-qualified or valid partial URL to the UrlMap resource that defines
the mapping from URL to the BackendService. For example, the following are
all valid URLs for specifying a URL map:
   
   - https://www.googleapis.compute/v1/projects/project/global/urlMaps/url-map 
   - projects/project/global/urlMaps/url-map 
   - global/urlMaps/url-map |
| `proxy_bind` | bool |  | This field only applies when the forwarding rule that references this
target proxy has a loadBalancingScheme set toINTERNAL_SELF_MANAGED.

When this field is set to true, Envoy proxies set up inbound
traffic interception and bind to the IP address and port specified in the
forwarding rule. This is generally useful when using Traffic Director to
configure Envoy as a gateway or middle proxy (in other words, not a
sidecar proxy). The Envoy proxy listens for inbound requests and handles
requests when it receives them.

The default is false. |
| `kind` | String |  | [Output Only] Type of resource. Alwayscompute#targetHttpsProxy for target HTTPS proxies. |
| `authorization_policy` | String |  | Optional. A URL referring to a networksecurity.AuthorizationPolicy resource
that describes how the proxy should authorize inbound traffic. If left
blank, access will not be restricted by an authorization policy.

 Refer to the AuthorizationPolicy resource for additional
details.

 authorizationPolicy only applies to a globalTargetHttpsProxy attached toglobalForwardingRules with theloadBalancingScheme set to INTERNAL_SELF_MANAGED.

 Note: This field currently has no impact. |
| `http_filters` | Vec<String> |  | URLs to networkservices.HttpFilter resources
enabled for xDS clients using this configuration. For example,https://networkservices.googleapis.com/beta/projects/project/locations/location/httpFilters/httpFilter
Only filters that handle outbound connection and
stream events may be specified. These filters work in conjunction with
a default set of HTTP filters that may already be configured by Traffic
Director. Traffic Director will determine the final location of these
filters within xDS configuration based on the name of the HTTP filter. If
Traffic Director positions multiple filters at the same location, those
filters will be in the same order as specified in this list. 

httpFilters only applies for loadbalancers withloadBalancingScheme set to INTERNAL_SELF_MANAGED.
See ForwardingRule for more details. |
| `region` | String |  | [Output Only] URL of the region where the regional TargetHttpsProxy
resides. This field is not applicable to global TargetHttpsProxies. |
| `authorization` | String |  | [Deprecated] Use authorizationPolicy instead. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `authentication` | String |  | [Deprecated] Use serverTlsPolicy instead. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `server_tls_policy` | String |  | Optional. A URL referring to a networksecurity.ServerTlsPolicy resource
that describes how the proxy should authenticate inbound traffic.

 serverTlsPolicy only applies to a globalTargetHttpsProxy attached toglobalForwardingRules with theloadBalancingScheme set to INTERNAL_SELF_MANAGED
or EXTERNAL orEXTERNAL_MANAGED or INTERNAL_MANAGED.
It also applies to a regional TargetHttpsProxy attached to
regional forwardingRules with theloadBalancingScheme set to EXTERNAL_MANAGED orINTERNAL_MANAGED. For details whichServerTlsPolicy resources are accepted withINTERNAL_SELF_MANAGED and which with EXTERNAL,INTERNAL_MANAGED, EXTERNAL_MANAGEDloadBalancingScheme consult ServerTlsPolicy
documentation.

  If left blank, communications are not encrypted. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `fingerprint` | String |  | Fingerprint of this resource. A hash of the contents stored in this object.
This field is used in optimistic locking. This field will be ignored when
inserting a TargetHttpsProxy. An up-to-date fingerprint must
be provided in order to patch the TargetHttpsProxy; otherwise, the request
will fail with error 412 conditionNotMet. To see the latest
fingerprint, make a get() request to retrieve the
TargetHttpsProxy. |
| `certificate_map` | String |  | URL of a certificate map that identifies a certificate map associated with
the given target proxy.
This field can only be set for Global external Application Load Balancer or
Classic Application Load Balancer. For other products use Certificate
Manager Certificates instead.

If set, sslCertificates will be ignored.

 Accepted format is//certificatemanager.googleapis.com/projects/{project}/locations/{location}/certificateMaps/{resourceName}. |
| `name` | String |  | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `quic_override` | String | Specifies the QUIC override policy for this TargetHttpsProxy resource. This
setting determines whether the load balancer attempts to negotiate QUIC
with clients.
You can specify NONE, ENABLE, orDISABLE.
   
   - When quic-override is set to NONE,
   Google manages whether QUIC is used.
   - When quic-override is set to ENABLE, the
   load balancer uses QUIC when possible.
   - When quic-override is set to DISABLE, the
   load balancer doesn't use QUIC.
   - If the quic-override flag is not specified,NONE is implied. |
| `tls_early_data` | String | Specifies whether TLS 1.3 0-RTT Data ("Early Data") should be accepted
for this service. Early Data allows a TLS resumption handshake to include
the initial application payload (a HTTP request) alongside the handshake,
reducing the effective round trips to "zero". This applies to TLS 1.3
connections over TCP (HTTP/2) as well as over UDP (QUIC/h3).


This can improve application performance, especially on networks where
interruptions may be common, such as on mobile.


Requests with Early Data will have the "Early-Data" HTTP header set on
the request, with a value of "1", to allow the backend to determine whether
Early Data was included.


Note: TLS Early Data may allow requests to be replayed, as the data is
sent to the backend before the handshake has fully completed. Applications
that allow idempotent HTTP methods to make non-idempotent changes, such as
a GET request updating a database, should not accept Early Data on those
requests, and reject requests with the "Early-Data: 1" HTTP header by
returning a HTTP 425 (Too Early) status code, in order to remain RFC
compliant.


The default value is DISABLED. |
| `ssl_certificates` | Vec<String> | URLs to SslCertificate resources that are used to authenticate
connections between users and the load balancer. At least one SSL
certificate must be specified. SslCertificates do not apply when the load
balancing scheme is set to INTERNAL_SELF_MANAGED.

The URLs should refer to a SSL Certificate resource or Certificate Manager
Certificate resource. Mixing Classic Certificates and Certificate Manager
Certificates is not allowed. Certificate Manager Certificates must include
the certificatemanager API namespace. Using Certificate Manager
Certificates in this field is not supported by Global external Application
Load Balancer or Classic Application Load Balancer, use certificate_map
instead.

Currently, you may specify up to 15 Classic SSL Certificates or up to 100
Certificate Manager Certificates.

Certificate Manager Certificates accepted formats are:
   
   - //certificatemanager.googleapis.com/projects/{project}/locations/{location}/certificates/{resourceName}.
   - https://certificatemanager.googleapis.com/v1alpha1/projects/{project}/locations/{location}/certificates/{resourceName}. |
| `http_keep_alive_timeout_sec` | i64 | Specifies how long to keep a connection open, after completing a response,
while there is no matching traffic (in seconds). If an HTTP keep-alive is
not specified, a default value (610 seconds) will be used.

For global external Application Load Balancers, the minimum allowed value
is 5 seconds and the maximum allowed value is 1200 seconds.

For classic Application Load Balancers, this option is not supported. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `ssl_policy` | String | URL of SslPolicy resource that will be associated with the TargetHttpsProxy
resource. If not set, the TargetHttpsProxy resource has no
SSL policy configured. |
| `url_map` | String | A fully-qualified or valid partial URL to the UrlMap resource that defines
the mapping from URL to the BackendService. For example, the following are
all valid URLs for specifying a URL map:
   
   - https://www.googleapis.compute/v1/projects/project/global/urlMaps/url-map 
   - projects/project/global/urlMaps/url-map 
   - global/urlMaps/url-map |
| `proxy_bind` | bool | This field only applies when the forwarding rule that references this
target proxy has a loadBalancingScheme set toINTERNAL_SELF_MANAGED.

When this field is set to true, Envoy proxies set up inbound
traffic interception and bind to the IP address and port specified in the
forwarding rule. This is generally useful when using Traffic Director to
configure Envoy as a gateway or middle proxy (in other words, not a
sidecar proxy). The Envoy proxy listens for inbound requests and handles
requests when it receives them.

The default is false. |
| `kind` | String | [Output Only] Type of resource. Alwayscompute#targetHttpsProxy for target HTTPS proxies. |
| `authorization_policy` | String | Optional. A URL referring to a networksecurity.AuthorizationPolicy resource
that describes how the proxy should authorize inbound traffic. If left
blank, access will not be restricted by an authorization policy.

 Refer to the AuthorizationPolicy resource for additional
details.

 authorizationPolicy only applies to a globalTargetHttpsProxy attached toglobalForwardingRules with theloadBalancingScheme set to INTERNAL_SELF_MANAGED.

 Note: This field currently has no impact. |
| `http_filters` | Vec<String> | URLs to networkservices.HttpFilter resources
enabled for xDS clients using this configuration. For example,https://networkservices.googleapis.com/beta/projects/project/locations/location/httpFilters/httpFilter
Only filters that handle outbound connection and
stream events may be specified. These filters work in conjunction with
a default set of HTTP filters that may already be configured by Traffic
Director. Traffic Director will determine the final location of these
filters within xDS configuration based on the name of the HTTP filter. If
Traffic Director positions multiple filters at the same location, those
filters will be in the same order as specified in this list. 

httpFilters only applies for loadbalancers withloadBalancingScheme set to INTERNAL_SELF_MANAGED.
See ForwardingRule for more details. |
| `region` | String | [Output Only] URL of the region where the regional TargetHttpsProxy
resides. This field is not applicable to global TargetHttpsProxies. |
| `authorization` | String | [Deprecated] Use authorizationPolicy instead. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `authentication` | String | [Deprecated] Use serverTlsPolicy instead. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `server_tls_policy` | String | Optional. A URL referring to a networksecurity.ServerTlsPolicy resource
that describes how the proxy should authenticate inbound traffic.

 serverTlsPolicy only applies to a globalTargetHttpsProxy attached toglobalForwardingRules with theloadBalancingScheme set to INTERNAL_SELF_MANAGED
or EXTERNAL orEXTERNAL_MANAGED or INTERNAL_MANAGED.
It also applies to a regional TargetHttpsProxy attached to
regional forwardingRules with theloadBalancingScheme set to EXTERNAL_MANAGED orINTERNAL_MANAGED. For details whichServerTlsPolicy resources are accepted withINTERNAL_SELF_MANAGED and which with EXTERNAL,INTERNAL_MANAGED, EXTERNAL_MANAGEDloadBalancingScheme consult ServerTlsPolicy
documentation.

  If left blank, communications are not encrypted. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `fingerprint` | String | Fingerprint of this resource. A hash of the contents stored in this object.
This field is used in optimistic locking. This field will be ignored when
inserting a TargetHttpsProxy. An up-to-date fingerprint must
be provided in order to patch the TargetHttpsProxy; otherwise, the request
will fail with error 412 conditionNotMet. To see the latest
fingerprint, make a get() request to retrieve the
TargetHttpsProxy. |
| `certificate_map` | String | URL of a certificate map that identifies a certificate map associated with
the given target proxy.
This field can only be set for Global external Application Load Balancer or
Classic Application Load Balancer. For other products use Certificate
Manager Certificates instead.

If set, sslCertificates will be ignored.

 Accepted format is//certificatemanager.googleapis.com/projects/{project}/locations/{location}/certificateMaps/{resourceName}. |
| `name` | String | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create target_https_proxie
target_https_proxie = provider.compute_api.Target_https_proxie {
    project = "value"  # Project ID for this request.
}

# Access target_https_proxie outputs
target_https_proxie_id = target_https_proxie.id
target_https_proxie_quic_override = target_https_proxie.quic_override
target_https_proxie_tls_early_data = target_https_proxie.tls_early_data
target_https_proxie_ssl_certificates = target_https_proxie.ssl_certificates
target_https_proxie_http_keep_alive_timeout_sec = target_https_proxie.http_keep_alive_timeout_sec
target_https_proxie_creation_timestamp = target_https_proxie.creation_timestamp
target_https_proxie_ssl_policy = target_https_proxie.ssl_policy
target_https_proxie_url_map = target_https_proxie.url_map
target_https_proxie_proxy_bind = target_https_proxie.proxy_bind
target_https_proxie_kind = target_https_proxie.kind
target_https_proxie_authorization_policy = target_https_proxie.authorization_policy
target_https_proxie_http_filters = target_https_proxie.http_filters
target_https_proxie_region = target_https_proxie.region
target_https_proxie_authorization = target_https_proxie.authorization
target_https_proxie_description = target_https_proxie.description
target_https_proxie_authentication = target_https_proxie.authentication
target_https_proxie_id = target_https_proxie.id
target_https_proxie_server_tls_policy = target_https_proxie.server_tls_policy
target_https_proxie_self_link = target_https_proxie.self_link
target_https_proxie_fingerprint = target_https_proxie.fingerprint
target_https_proxie_certificate_map = target_https_proxie.certificate_map
target_https_proxie_name = target_https_proxie.name
```

---


### Vpn_gateway

Creates a VPN gateway in the specified project and region using
the data included in the request.

**Operations**: ✅ Create ✅ Read ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `labels` | HashMap<String, String> |  | Labels for this resource. These can only be added or modified by thesetLabels method. Each label key/value pair must comply withRFC1035.
Label values may be empty. |
| `name` | String |  | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `vpn_interfaces` | Vec<String> |  | The list of VPN interfaces associated with this VPN gateway. |
| `label_fingerprint` | String |  | A fingerprint for the labels being applied to this VpnGateway, which
is essentially a hash of the labels set used for optimistic locking. The
fingerprint is initially generated by Compute Engine and changes after
every request to modify or update labels. You must always provide an
up-to-date fingerprint hash in order to update or change labels,
otherwise the request will fail with error412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve a VpnGateway. |
| `region` | String |  | [Output Only] URL of the region where the VPN gateway resides. |
| `gateway_ip_version` | String |  | The IP family of the gateway IPs for the HA-VPN gateway interfaces. If not
specified, IPV4 will be used. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `network` | String |  | URL of the network to which this VPN gateway is attached. Provided by the
client when the VPN gateway is created. |
| `kind` | String |  | [Output Only] Type of resource. Always compute#vpnGateway for
VPN gateways. |
| `stack_type` | String |  | The stack type for this VPN gateway to identify the IP protocols that are
enabled. Possible values are: IPV4_ONLY,IPV4_IPV6, IPV6_ONLY. If not specified,IPV4_ONLY is used if the gateway IP version isIPV4, or IPV4_IPV6 if the gateway IP version isIPV6. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `region` | String | ✅ | Name of the region for this request. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `labels` | HashMap<String, String> | Labels for this resource. These can only be added or modified by thesetLabels method. Each label key/value pair must comply withRFC1035.
Label values may be empty. |
| `name` | String | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `vpn_interfaces` | Vec<String> | The list of VPN interfaces associated with this VPN gateway. |
| `label_fingerprint` | String | A fingerprint for the labels being applied to this VpnGateway, which
is essentially a hash of the labels set used for optimistic locking. The
fingerprint is initially generated by Compute Engine and changes after
every request to modify or update labels. You must always provide an
up-to-date fingerprint hash in order to update or change labels,
otherwise the request will fail with error412 conditionNotMet.

To see the latest fingerprint, make a get() request to
retrieve a VpnGateway. |
| `region` | String | [Output Only] URL of the region where the VPN gateway resides. |
| `gateway_ip_version` | String | The IP family of the gateway IPs for the HA-VPN gateway interfaces. If not
specified, IPV4 will be used. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `network` | String | URL of the network to which this VPN gateway is attached. Provided by the
client when the VPN gateway is created. |
| `kind` | String | [Output Only] Type of resource. Always compute#vpnGateway for
VPN gateways. |
| `stack_type` | String | The stack type for this VPN gateway to identify the IP protocols that are
enabled. Possible values are: IPV4_ONLY,IPV4_IPV6, IPV6_ONLY. If not specified,IPV4_ONLY is used if the gateway IP version isIPV4, or IPV4_IPV6 if the gateway IP version isIPV6. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create vpn_gateway
vpn_gateway = provider.compute_api.Vpn_gateway {
    region = "value"  # Name of the region for this request.
    project = "value"  # Project ID for this request.
}

# Access vpn_gateway outputs
vpn_gateway_id = vpn_gateway.id
vpn_gateway_labels = vpn_gateway.labels
vpn_gateway_name = vpn_gateway.name
vpn_gateway_self_link = vpn_gateway.self_link
vpn_gateway_vpn_interfaces = vpn_gateway.vpn_interfaces
vpn_gateway_label_fingerprint = vpn_gateway.label_fingerprint
vpn_gateway_region = vpn_gateway.region
vpn_gateway_gateway_ip_version = vpn_gateway.gateway_ip_version
vpn_gateway_creation_timestamp = vpn_gateway.creation_timestamp
vpn_gateway_network = vpn_gateway.network
vpn_gateway_kind = vpn_gateway.kind
vpn_gateway_stack_type = vpn_gateway.stack_type
vpn_gateway_description = vpn_gateway.description
vpn_gateway_id = vpn_gateway.id
```

---


### Network_profile

Returns the specified network profile.

**Operations**: ✅ Read

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `location` | String | [Output Only] Location to which the network is restricted. |
| `self_link_with_id` | String | [Output Only] Server-defined URL for this resource with the resource id. |
| `description` | String | [Output Only] An optional description of this resource. |
| `kind` | String | [Output Only] Type of the resource. Alwayscompute#networkProfile for network profiles. |
| `features` | String | [Output Only] Features supported by the network. |
| `name` | String | [Output Only] Name of the resource. |
| `profile_type` | String | [Output Only] Type of the network profile. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Access network_profile outputs
network_profile_id = network_profile.id
network_profile_location = network_profile.location
network_profile_self_link_with_id = network_profile.self_link_with_id
network_profile_description = network_profile.description
network_profile_kind = network_profile.kind
network_profile_features = network_profile.features
network_profile_name = network_profile.name
network_profile_profile_type = network_profile.profile_type
network_profile_self_link = network_profile.self_link
network_profile_id = network_profile.id
network_profile_creation_timestamp = network_profile.creation_timestamp
```

---


### Region_commitment

Creates a commitment in the specified project using the data
included in the request.

**Operations**: ✅ Create ✅ Read ✅ Update

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `category` | String |  | The category of the commitment; specifies whether the commitment is for
hardware or software resources. Category MACHINE specifies
that you are committing to hardware machine resources such asVCPU or MEMORY, listed in resources.
Category LICENSE specifies that you are committing to software
licenses, listed in licenseResources.
Note that if you specify MACHINE commitments, then you must
also specify a type to indicate the machine series of the
hardware resource that you are committing to. |
| `name` | String |  | Name of the commitment. You must specify a name when you purchase the
commitment. The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `custom_end_timestamp` | String |  | [Input Only] Optional, specifies the requested commitment end time inRFC3339 text format. Use this option when the desired
commitment's end date is later than the start date + term duration. |
| `end_timestamp` | String |  | [Output Only] Commitment end time inRFC3339
text format. |
| `type` | String |  | The type of commitment; specifies the
machine series for which you want to commit to purchasing resources.
The choice of machine series affects the discount rate and the eligible
resource types.

 The type must be one of the following:ACCELERATOR_OPTIMIZED, ACCELERATOR_OPTIMIZED_A3,ACCELERATOR_OPTIMIZED_A3_MEGA,COMPUTE_OPTIMIZED, COMPUTE_OPTIMIZED_C2D,
 COMPUTE_OPTIMIZED_C3, COMPUTE_OPTIMIZED_C3D,COMPUTE_OPTIMIZED_H3, GENERAL_PURPOSE,GENERAL_PURPOSE_C4, GENERAL_PURPOSE_E2,GENERAL_PURPOSE_N2, GENERAL_PURPOSE_N2D,GENERAL_PURPOSE_N4, GENERAL_PURPOSE_T2D,GRAPHICS_OPTIMIZED, MEMORY_OPTIMIZED,MEMORY_OPTIMIZED_M3, MEMORY_OPTIMIZED_X4,STORAGE_OPTIMIZED_Z3.
For example, type MEMORY_OPTIMIZED specifies a commitment
that applies only to eligible resources of memory optimized M1 and M2
machine series. Type GENERAL_PURPOSE specifies a commitment
that applies only to eligible resources of general purpose N1 machine
series. |
| `status_message` | String |  | [Output Only] An optional, human-readable explanation of the status. |
| `region` | String |  | [Output Only] URL of the region where the commitment and committed
resources are located. |
| `status` | String |  | [Output Only] Status of the commitment with regards to eventual expiration
(each commitment has an end date defined). Status can be one of the
following values: NOT_YET_ACTIVE, ACTIVE, orEXPIRED. |
| `reservations` | Vec<String> |  | The list of new reservations that you want to create and attach to this
commitment.

You must attach reservations to your commitment if your commitment
specifies any GPUs or Local SSD disks. For more information, see 
Attach reservations to resource-based commitments.

Specify this property only if you want to create new
reservations to attach. To attach existing reservations, specify theexistingReservations property instead. |
| `resources` | Vec<String> |  | The list of all the hardware resources, with their types and amounts, that
you want to commit to. Specify as a separate entry in the list for each
individual resource type. |
| `kind` | String |  | [Output Only] Type of the resource. Always compute#commitment
for commitments. |
| `auto_renew` | bool |  | Specifies whether to automatically renew the commitment at the end of its
current term. The default value is false. If you set the field
to true, each time your commitment reaches the end of its
term, Compute Engine automatically renews it for another term. You can
update this field anytime before the commitment expires. For example, if
the commitment is set to expire at 12 AM UTC-8 on January 3, 2027, you can
update this field until 11:59 PM UTC-8 on January 2, 2027. |
| `split_source_commitment` | String |  | The source commitment from which you are transferring resources to create
the new split commitment. For more information, see 
Split commitments. |
| `existing_reservations` | Vec<String> |  |  |
| `license_resource` | String |  | The license specification required as part of a license commitment. |
| `resource_status` | String |  | [Output Only] Status information for Commitment resource. |
| `description` | String |  | An optional description of the commitment. You can provide this property
when you create the resource. |
| `merge_source_commitments` | Vec<String> |  | The list of source commitments that you are merging to create the new
merged commitment. For more information, see
Merging commitments. |
| `plan` | String |  | The minimum time duration that you commit to purchasing resources.
The plan that you choose determines the preset term length of the
commitment (which is 1 year or 3 years) and affects the discount rate that
you receive for your resources. Committing to a longer time duration
typically gives you a higher discount rate. The supported values for this
field are TWELVE_MONTH (1 year), andTHIRTY_SIX_MONTH (3 years). |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `start_timestamp` | String |  | [Output Only] Commitment start time inRFC3339
text format. |
| `project` | String | ✅ | Project ID for this request. |
| `region` | String | ✅ | Name of the region for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `category` | String | The category of the commitment; specifies whether the commitment is for
hardware or software resources. Category MACHINE specifies
that you are committing to hardware machine resources such asVCPU or MEMORY, listed in resources.
Category LICENSE specifies that you are committing to software
licenses, listed in licenseResources.
Note that if you specify MACHINE commitments, then you must
also specify a type to indicate the machine series of the
hardware resource that you are committing to. |
| `name` | String | Name of the commitment. You must specify a name when you purchase the
commitment. The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `custom_end_timestamp` | String | [Input Only] Optional, specifies the requested commitment end time inRFC3339 text format. Use this option when the desired
commitment's end date is later than the start date + term duration. |
| `end_timestamp` | String | [Output Only] Commitment end time inRFC3339
text format. |
| `type` | String | The type of commitment; specifies the
machine series for which you want to commit to purchasing resources.
The choice of machine series affects the discount rate and the eligible
resource types.

 The type must be one of the following:ACCELERATOR_OPTIMIZED, ACCELERATOR_OPTIMIZED_A3,ACCELERATOR_OPTIMIZED_A3_MEGA,COMPUTE_OPTIMIZED, COMPUTE_OPTIMIZED_C2D,
 COMPUTE_OPTIMIZED_C3, COMPUTE_OPTIMIZED_C3D,COMPUTE_OPTIMIZED_H3, GENERAL_PURPOSE,GENERAL_PURPOSE_C4, GENERAL_PURPOSE_E2,GENERAL_PURPOSE_N2, GENERAL_PURPOSE_N2D,GENERAL_PURPOSE_N4, GENERAL_PURPOSE_T2D,GRAPHICS_OPTIMIZED, MEMORY_OPTIMIZED,MEMORY_OPTIMIZED_M3, MEMORY_OPTIMIZED_X4,STORAGE_OPTIMIZED_Z3.
For example, type MEMORY_OPTIMIZED specifies a commitment
that applies only to eligible resources of memory optimized M1 and M2
machine series. Type GENERAL_PURPOSE specifies a commitment
that applies only to eligible resources of general purpose N1 machine
series. |
| `status_message` | String | [Output Only] An optional, human-readable explanation of the status. |
| `region` | String | [Output Only] URL of the region where the commitment and committed
resources are located. |
| `status` | String | [Output Only] Status of the commitment with regards to eventual expiration
(each commitment has an end date defined). Status can be one of the
following values: NOT_YET_ACTIVE, ACTIVE, orEXPIRED. |
| `reservations` | Vec<String> | The list of new reservations that you want to create and attach to this
commitment.

You must attach reservations to your commitment if your commitment
specifies any GPUs or Local SSD disks. For more information, see 
Attach reservations to resource-based commitments.

Specify this property only if you want to create new
reservations to attach. To attach existing reservations, specify theexistingReservations property instead. |
| `resources` | Vec<String> | The list of all the hardware resources, with their types and amounts, that
you want to commit to. Specify as a separate entry in the list for each
individual resource type. |
| `kind` | String | [Output Only] Type of the resource. Always compute#commitment
for commitments. |
| `auto_renew` | bool | Specifies whether to automatically renew the commitment at the end of its
current term. The default value is false. If you set the field
to true, each time your commitment reaches the end of its
term, Compute Engine automatically renews it for another term. You can
update this field anytime before the commitment expires. For example, if
the commitment is set to expire at 12 AM UTC-8 on January 3, 2027, you can
update this field until 11:59 PM UTC-8 on January 2, 2027. |
| `split_source_commitment` | String | The source commitment from which you are transferring resources to create
the new split commitment. For more information, see 
Split commitments. |
| `existing_reservations` | Vec<String> |  |
| `license_resource` | String | The license specification required as part of a license commitment. |
| `resource_status` | String | [Output Only] Status information for Commitment resource. |
| `description` | String | An optional description of the commitment. You can provide this property
when you create the resource. |
| `merge_source_commitments` | Vec<String> | The list of source commitments that you are merging to create the new
merged commitment. For more information, see
Merging commitments. |
| `plan` | String | The minimum time duration that you commit to purchasing resources.
The plan that you choose determines the preset term length of the
commitment (which is 1 year or 3 years) and affects the discount rate that
you receive for your resources. Committing to a longer time duration
typically gives you a higher discount rate. The supported values for this
field are TWELVE_MONTH (1 year), andTHIRTY_SIX_MONTH (3 years). |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `start_timestamp` | String | [Output Only] Commitment start time inRFC3339
text format. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create region_commitment
region_commitment = provider.compute_api.Region_commitment {
    project = "value"  # Project ID for this request.
    region = "value"  # Name of the region for this request.
}

# Access region_commitment outputs
region_commitment_id = region_commitment.id
region_commitment_creation_timestamp = region_commitment.creation_timestamp
region_commitment_category = region_commitment.category
region_commitment_name = region_commitment.name
region_commitment_custom_end_timestamp = region_commitment.custom_end_timestamp
region_commitment_end_timestamp = region_commitment.end_timestamp
region_commitment_type = region_commitment.type
region_commitment_status_message = region_commitment.status_message
region_commitment_region = region_commitment.region
region_commitment_status = region_commitment.status
region_commitment_reservations = region_commitment.reservations
region_commitment_resources = region_commitment.resources
region_commitment_kind = region_commitment.kind
region_commitment_auto_renew = region_commitment.auto_renew
region_commitment_split_source_commitment = region_commitment.split_source_commitment
region_commitment_existing_reservations = region_commitment.existing_reservations
region_commitment_license_resource = region_commitment.license_resource
region_commitment_resource_status = region_commitment.resource_status
region_commitment_description = region_commitment.description
region_commitment_merge_source_commitments = region_commitment.merge_source_commitments
region_commitment_plan = region_commitment.plan
region_commitment_self_link = region_commitment.self_link
region_commitment_id = region_commitment.id
region_commitment_start_timestamp = region_commitment.start_timestamp
```

---


### Zone

Returns the specified Zone resource.

**Operations**: ✅ Read

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `region` | String | [Output Only] Full URL reference to the region which hosts the zone. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `name` | String | [Output Only] Name of the resource. |
| `deprecated` | String | [Output Only] The deprecation status associated with this zone. |
| `supports_pzs` | bool | [Output Only] Reserved for future use. |
| `status` | String | [Output Only] Status of the zone, either UP orDOWN. |
| `kind` | String | [Output Only] Type of the resource. Always compute#zone for
zones. |
| `description` | String | [Output Only] Textual description of the resource. |
| `available_cpu_platforms` | Vec<String> | [Output Only] Available cpu/platform selections for the zone. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Access zone outputs
zone_id = zone.id
zone_creation_timestamp = zone.creation_timestamp
zone_region = zone.region
zone_self_link = zone.self_link
zone_id = zone.id
zone_name = zone.name
zone_deprecated = zone.deprecated
zone_supports_pzs = zone.supports_pzs
zone_status = zone.status
zone_kind = zone.kind
zone_description = zone.description
zone_available_cpu_platforms = zone.available_cpu_platforms
```

---


### Network_firewall_policie

Creates a new policy in the specified project using the data included in
the request.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `kind` | String |  | [Output only] Type of the resource. Alwayscompute#firewallPolicyfor firewall policies |
| `policy_source` | String |  | [Output Only] Source of this Firewall Policy. USER_DEFINED if
created by a Cloud user, or SYSTEM if created by managed
services like GKE. |
| `region` | String |  | [Output Only] URL of the region where the regional firewall policy resides.
This field is not applicable to global firewall policies.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `rules` | Vec<String> |  | A list of rules that belong to this policy.
There must always be a default rule (rule with priority 2147483647 and
match "*"). If no rules are provided when creating a firewall policy, a
default rule with action "allow" will be added. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `parent` | String |  | [Output Only] The parent of the firewall policy.
This field is not applicable to network firewall policies. |
| `rule_tuple_count` | i64 |  | [Output Only] Total count of all firewall policy rule tuples. A firewall
policy can not exceed a set number of tuples. |
| `display_name` | String |  | Deprecated, please use short name instead. User-provided name of the
Organization firewall policy. The name should be unique in the organization
in which the firewall policy is created.
This field is not applicable to network firewall policies.
This name must be set on creation and cannot be changed.
The name must be 1-63 characters long, and comply
with RFC1035. Specifically, the name must be 1-63 characters
long and match the regular expression `[a-z]([-a-z0-9]*[a-z0-9])?` which
means the first character must be a lowercase letter, and all following
characters must be a dash, lowercase letter, or digit, except the last
character, which cannot be a dash. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `associations` | Vec<String> |  | A list of associations that belong to this firewall policy. |
| `fingerprint` | String |  | Specifies a fingerprint for this resource, which is essentially a hash of
the metadata's contents and used for optimistic locking. The
fingerprint is initially generated by Compute Engine and changes after
every request to modify or update metadata. You must always provide an
up-to-date fingerprint hash in order to update or change metadata,
otherwise the request will fail with error412 conditionNotMet.

To see the latest fingerprint, make get() request to the
firewall policy. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `name` | String |  | Name of the resource. For Organization Firewall Policies it's a
[Output Only] numeric ID allocated by Google Cloud which uniquely
identifies the Organization Firewall Policy. |
| `packet_mirroring_rules` | Vec<String> |  | A list of packet mirroring rules that belong to this policy. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `self_link_with_id` | String |  | [Output Only] Server-defined URL for this resource with the resource id. |
| `short_name` | String |  | User-provided name of the Organization firewall policy. The name should be
unique in the organization in which the firewall policy is created.
This field is not applicable to network firewall policies.
This name must be set on creation and cannot be changed. The name must be
1-63 characters long, and comply with RFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `policy_type` | String |  | The type of the firewall policy. This field can be eitherVPC_POLICY or RDMA_ROCE_POLICY.

Note: if not specified then VPC_POLICY will be used. |
| `project` | String | ✅ | Project ID for this request. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `kind` | String | [Output only] Type of the resource. Alwayscompute#firewallPolicyfor firewall policies |
| `policy_source` | String | [Output Only] Source of this Firewall Policy. USER_DEFINED if
created by a Cloud user, or SYSTEM if created by managed
services like GKE. |
| `region` | String | [Output Only] URL of the region where the regional firewall policy resides.
This field is not applicable to global firewall policies.
You must specify this field as part of the HTTP request URL. It is
not settable as a field in the request body. |
| `rules` | Vec<String> | A list of rules that belong to this policy.
There must always be a default rule (rule with priority 2147483647 and
match "*"). If no rules are provided when creating a firewall policy, a
default rule with action "allow" will be added. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `parent` | String | [Output Only] The parent of the firewall policy.
This field is not applicable to network firewall policies. |
| `rule_tuple_count` | i64 | [Output Only] Total count of all firewall policy rule tuples. A firewall
policy can not exceed a set number of tuples. |
| `display_name` | String | Deprecated, please use short name instead. User-provided name of the
Organization firewall policy. The name should be unique in the organization
in which the firewall policy is created.
This field is not applicable to network firewall policies.
This name must be set on creation and cannot be changed.
The name must be 1-63 characters long, and comply
with RFC1035. Specifically, the name must be 1-63 characters
long and match the regular expression `[a-z]([-a-z0-9]*[a-z0-9])?` which
means the first character must be a lowercase letter, and all following
characters must be a dash, lowercase letter, or digit, except the last
character, which cannot be a dash. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `associations` | Vec<String> | A list of associations that belong to this firewall policy. |
| `fingerprint` | String | Specifies a fingerprint for this resource, which is essentially a hash of
the metadata's contents and used for optimistic locking. The
fingerprint is initially generated by Compute Engine and changes after
every request to modify or update metadata. You must always provide an
up-to-date fingerprint hash in order to update or change metadata,
otherwise the request will fail with error412 conditionNotMet.

To see the latest fingerprint, make get() request to the
firewall policy. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `name` | String | Name of the resource. For Organization Firewall Policies it's a
[Output Only] numeric ID allocated by Google Cloud which uniquely
identifies the Organization Firewall Policy. |
| `packet_mirroring_rules` | Vec<String> | A list of packet mirroring rules that belong to this policy. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `self_link_with_id` | String | [Output Only] Server-defined URL for this resource with the resource id. |
| `short_name` | String | User-provided name of the Organization firewall policy. The name should be
unique in the organization in which the firewall policy is created.
This field is not applicable to network firewall policies.
This name must be set on creation and cannot be changed. The name must be
1-63 characters long, and comply with RFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `policy_type` | String | The type of the firewall policy. This field can be eitherVPC_POLICY or RDMA_ROCE_POLICY.

Note: if not specified then VPC_POLICY will be used. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create network_firewall_policie
network_firewall_policie = provider.compute_api.Network_firewall_policie {
    project = "value"  # Project ID for this request.
}

# Access network_firewall_policie outputs
network_firewall_policie_id = network_firewall_policie.id
network_firewall_policie_kind = network_firewall_policie.kind
network_firewall_policie_policy_source = network_firewall_policie.policy_source
network_firewall_policie_region = network_firewall_policie.region
network_firewall_policie_rules = network_firewall_policie.rules
network_firewall_policie_description = network_firewall_policie.description
network_firewall_policie_parent = network_firewall_policie.parent
network_firewall_policie_rule_tuple_count = network_firewall_policie.rule_tuple_count
network_firewall_policie_display_name = network_firewall_policie.display_name
network_firewall_policie_id = network_firewall_policie.id
network_firewall_policie_associations = network_firewall_policie.associations
network_firewall_policie_fingerprint = network_firewall_policie.fingerprint
network_firewall_policie_creation_timestamp = network_firewall_policie.creation_timestamp
network_firewall_policie_name = network_firewall_policie.name
network_firewall_policie_packet_mirroring_rules = network_firewall_policie.packet_mirroring_rules
network_firewall_policie_self_link = network_firewall_policie.self_link
network_firewall_policie_self_link_with_id = network_firewall_policie.self_link_with_id
network_firewall_policie_short_name = network_firewall_policie.short_name
network_firewall_policie_policy_type = network_firewall_policie.policy_type
```

---


### Organization_security_policie

Creates a new policy in the specified organization using the data included
in the request.

Use of this API to insert firewall policies is deprecated. Use
firewallPolicies.insert instead.

**Operations**: ✅ Create ✅ Read ✅ Update ✅ Delete

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `kind` | String |  | [Output only] Type of the resource. Alwayscompute#securityPolicyfor security policies |
| `region` | String |  | [Output Only] URL of the region where the regional security policy
resides. This field is not applicable to global security policies. |
| `associations` | Vec<String> |  | A list of associations that belong to this policy. |
| `rules` | Vec<String> |  | A list of rules that belong to this policy.
There must always be a default rule which is a rule with priority
2147483647 and match all condition (for the match condition this means
match  "*" for srcIpRanges and for the networkMatch condition every field
must be either match "*" or not set). If no rules are provided when
creating a security policy, a default rule with action "allow" will be
added. |
| `adaptive_protection_config` | String |  |  |
| `user_defined_fields` | Vec<String> |  | Definitions of user-defined fields for CLOUD_ARMOR_NETWORK policies. A
user-defined field consists of up to 4 bytes extracted from a fixed offset
in the packet, relative to the IPv4, IPv6, TCP, or UDP header, with an
optional mask to select certain bits. Rules may then specify matching
values for these fields.

Example:

 userDefinedFields:
 - name: "ipv4_fragment_offset"
   base: IPV4
   offset: 6
   size: 2
   mask: "0x1fff" |
| `name` | String |  | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `recaptcha_options_config` | String |  |  |
| `advanced_options_config` | String |  |  |
| `short_name` | String |  | User-provided name of the organization security policy. The name should be
unique in the organization in which the security policy is created. This
should only be used when SecurityPolicyType is CLOUD_ARMOR.
The name must be 1-63 characters long, and comply with
https://www.ietf.org/rfc/rfc1035.txt. Specifically, the name must be 1-63
characters long and match the regular expression
`[a-z]([-a-z0-9]*[a-z0-9])?` which means the first character must be a
lowercase letter, and all following characters must be a dash, lowercase
letter, or digit, except the last character, which cannot be a dash. |
| `parent` | String |  | [Output Only] The parent of the security policy. |
| `fingerprint` | String |  | Specifies a fingerprint for this resource, which is essentially a hash of
the metadata's contents and used for optimistic locking. The
fingerprint is initially generated by Compute Engine and changes after
every request to modify or update metadata. You must always provide an
up-to-date fingerprint hash in order to update or change metadata,
otherwise the request will fail with error412 conditionNotMet.

To see the latest fingerprint, make get() request to the
security policy. |
| `ddos_protection_config` | String |  |  |
| `label_fingerprint` | String |  | A fingerprint for the labels being applied to this security policy, which
is essentially a hash of the labels set used for optimistic locking. The
fingerprint is initially generated by Compute Engine and changes after
every request to modify or update labels. You must always provide an
up-to-date fingerprint hash in order to update or change labels.

To see the latest fingerprint, make get() request to the
security policy. |
| `display_name` | String |  | User-provided name of the organization security policy. The name should be
unique in the organization in which the security policy is created. This
should only be used when SecurityPolicyType is FIREWALL.
The name must be 1-63 characters long, and comply with
https://www.ietf.org/rfc/rfc1035.txt. Specifically, the name must be 1-63
characters long and match the regular expression
`[a-z]([-a-z0-9]*[a-z0-9])?` which means the first character must be a
lowercase letter, and all following characters must be a dash, lowercase
letter, or digit, except the last character, which cannot be a dash. |
| `creation_timestamp` | String |  | [Output Only] Creation timestamp inRFC3339
text format. |
| `description` | String |  | An optional description of this resource. Provide this property when you
create the resource. |
| `labels` | HashMap<String, String> |  | Labels for this resource. These can only be added or modified by thesetLabels method. Each label key/value pair must comply withRFC1035.
Label values may be empty. |
| `rule_tuple_count` | i64 |  | [Output Only] Total count of all security policy rule tuples. A security
policy can not exceed a set number of tuples. |
| `id` | String |  | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `self_link` | String |  | [Output Only] Server-defined URL for the resource. |
| `self_link_with_id` | String |  | [Output Only] Server-defined URL for this resource with the resource id. |
| `type` | String |  | The type indicates the intended use of the security policy.
   
   - CLOUD_ARMOR: Cloud Armor backend security policies can
   be configured to filter incoming HTTP requests targeting backend services.
   They filter requests before they hit the origin servers.
   - CLOUD_ARMOR_EDGE: Cloud Armor edge security policies can
   be configured to filter incoming HTTP requests targeting backend services
   (including Cloud CDN-enabled) as well as backend buckets (Cloud Storage).
   They filter requests before the request is served from Google's cache.
   - CLOUD_ARMOR_INTERNAL_SERVICE (preview only): Cloud Armor
   internal service policies can be configured to filter HTTP requests
   targeting services managed by Traffic Director in a service mesh. They
   filter requests before the request is served from the application.

- CLOUD_ARMOR_NETWORK: Cloud Armor network policies
can be configured to filter packets targeting network load balancing
resources such as backend services, target pools, target instances, and
instances with external IPs. They filter requests before the request is
served from the application.


This field can be set only at resource creation time. |


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `kind` | String | [Output only] Type of the resource. Alwayscompute#securityPolicyfor security policies |
| `region` | String | [Output Only] URL of the region where the regional security policy
resides. This field is not applicable to global security policies. |
| `associations` | Vec<String> | A list of associations that belong to this policy. |
| `rules` | Vec<String> | A list of rules that belong to this policy.
There must always be a default rule which is a rule with priority
2147483647 and match all condition (for the match condition this means
match  "*" for srcIpRanges and for the networkMatch condition every field
must be either match "*" or not set). If no rules are provided when
creating a security policy, a default rule with action "allow" will be
added. |
| `adaptive_protection_config` | String |  |
| `user_defined_fields` | Vec<String> | Definitions of user-defined fields for CLOUD_ARMOR_NETWORK policies. A
user-defined field consists of up to 4 bytes extracted from a fixed offset
in the packet, relative to the IPv4, IPv6, TCP, or UDP header, with an
optional mask to select certain bits. Rules may then specify matching
values for these fields.

Example:

 userDefinedFields:
 - name: "ipv4_fragment_offset"
   base: IPV4
   offset: 6
   size: 2
   mask: "0x1fff" |
| `name` | String | Name of the resource. Provided by the client when the resource is created.
The name must be 1-63 characters long, and comply withRFC1035.
Specifically, the name must be 1-63 characters long and match the regular
expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first
character must be a lowercase letter, and all following characters must
be a dash, lowercase letter, or digit, except the last character, which
cannot be a dash. |
| `recaptcha_options_config` | String |  |
| `advanced_options_config` | String |  |
| `short_name` | String | User-provided name of the organization security policy. The name should be
unique in the organization in which the security policy is created. This
should only be used when SecurityPolicyType is CLOUD_ARMOR.
The name must be 1-63 characters long, and comply with
https://www.ietf.org/rfc/rfc1035.txt. Specifically, the name must be 1-63
characters long and match the regular expression
`[a-z]([-a-z0-9]*[a-z0-9])?` which means the first character must be a
lowercase letter, and all following characters must be a dash, lowercase
letter, or digit, except the last character, which cannot be a dash. |
| `parent` | String | [Output Only] The parent of the security policy. |
| `fingerprint` | String | Specifies a fingerprint for this resource, which is essentially a hash of
the metadata's contents and used for optimistic locking. The
fingerprint is initially generated by Compute Engine and changes after
every request to modify or update metadata. You must always provide an
up-to-date fingerprint hash in order to update or change metadata,
otherwise the request will fail with error412 conditionNotMet.

To see the latest fingerprint, make get() request to the
security policy. |
| `ddos_protection_config` | String |  |
| `label_fingerprint` | String | A fingerprint for the labels being applied to this security policy, which
is essentially a hash of the labels set used for optimistic locking. The
fingerprint is initially generated by Compute Engine and changes after
every request to modify or update labels. You must always provide an
up-to-date fingerprint hash in order to update or change labels.

To see the latest fingerprint, make get() request to the
security policy. |
| `display_name` | String | User-provided name of the organization security policy. The name should be
unique in the organization in which the security policy is created. This
should only be used when SecurityPolicyType is FIREWALL.
The name must be 1-63 characters long, and comply with
https://www.ietf.org/rfc/rfc1035.txt. Specifically, the name must be 1-63
characters long and match the regular expression
`[a-z]([-a-z0-9]*[a-z0-9])?` which means the first character must be a
lowercase letter, and all following characters must be a dash, lowercase
letter, or digit, except the last character, which cannot be a dash. |
| `creation_timestamp` | String | [Output Only] Creation timestamp inRFC3339
text format. |
| `description` | String | An optional description of this resource. Provide this property when you
create the resource. |
| `labels` | HashMap<String, String> | Labels for this resource. These can only be added or modified by thesetLabels method. Each label key/value pair must comply withRFC1035.
Label values may be empty. |
| `rule_tuple_count` | i64 | [Output Only] Total count of all security policy rule tuples. A security
policy can not exceed a set number of tuples. |
| `id` | String | [Output Only] The unique identifier for the resource. This identifier is
defined by the server. |
| `self_link` | String | [Output Only] Server-defined URL for the resource. |
| `self_link_with_id` | String | [Output Only] Server-defined URL for this resource with the resource id. |
| `type` | String | The type indicates the intended use of the security policy.
   
   - CLOUD_ARMOR: Cloud Armor backend security policies can
   be configured to filter incoming HTTP requests targeting backend services.
   They filter requests before they hit the origin servers.
   - CLOUD_ARMOR_EDGE: Cloud Armor edge security policies can
   be configured to filter incoming HTTP requests targeting backend services
   (including Cloud CDN-enabled) as well as backend buckets (Cloud Storage).
   They filter requests before the request is served from Google's cache.
   - CLOUD_ARMOR_INTERNAL_SERVICE (preview only): Cloud Armor
   internal service policies can be configured to filter HTTP requests
   targeting services managed by Traffic Director in a service mesh. They
   filter requests before the request is served from the application.

- CLOUD_ARMOR_NETWORK: Cloud Armor network policies
can be configured to filter packets targeting network load balancing
resources such as backend services, target pools, target instances, and
instances with external IPs. They filter requests before the request is
served from the application.


This field can be set only at resource creation time. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create organization_security_policie
organization_security_policie = provider.compute_api.Organization_security_policie {
}

# Access organization_security_policie outputs
organization_security_policie_id = organization_security_policie.id
organization_security_policie_kind = organization_security_policie.kind
organization_security_policie_region = organization_security_policie.region
organization_security_policie_associations = organization_security_policie.associations
organization_security_policie_rules = organization_security_policie.rules
organization_security_policie_adaptive_protection_config = organization_security_policie.adaptive_protection_config
organization_security_policie_user_defined_fields = organization_security_policie.user_defined_fields
organization_security_policie_name = organization_security_policie.name
organization_security_policie_recaptcha_options_config = organization_security_policie.recaptcha_options_config
organization_security_policie_advanced_options_config = organization_security_policie.advanced_options_config
organization_security_policie_short_name = organization_security_policie.short_name
organization_security_policie_parent = organization_security_policie.parent
organization_security_policie_fingerprint = organization_security_policie.fingerprint
organization_security_policie_ddos_protection_config = organization_security_policie.ddos_protection_config
organization_security_policie_label_fingerprint = organization_security_policie.label_fingerprint
organization_security_policie_display_name = organization_security_policie.display_name
organization_security_policie_creation_timestamp = organization_security_policie.creation_timestamp
organization_security_policie_description = organization_security_policie.description
organization_security_policie_labels = organization_security_policie.labels
organization_security_policie_rule_tuple_count = organization_security_policie.rule_tuple_count
organization_security_policie_id = organization_security_policie.id
organization_security_policie_self_link = organization_security_policie.self_link
organization_security_policie_self_link_with_id = organization_security_policie.self_link_with_id
organization_security_policie_type = organization_security_policie.type
```

---


### Image_family_view

Returns the latest image that is part of an image family, is not
deprecated and is rolled out in the specified zone.

**Operations**: ✅ Read

#### Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|


#### Outputs

| Output | Type | Description |
|--------|------|-------------|
| `image` | String | The latest image that is part of the specified image family in the
requested location, and that is not deprecated. |


#### Usage Example

```kcl
# main.k
import gcp

# Initialize provider
provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Access image_family_view outputs
image_family_view_id = image_family_view.id
image_family_view_image = image_family_view.image
```

---



## Common Operations

### Creating Multiple Resources

```kcl
import gcp

provider = gcp.GcpProvider {
    project = "my-project-id"
}

# Create multiple instant_snapshot resources
instant_snapshot_0 = provider.compute_api.Instant_snapshot {
    project = "value-0"
    zone = "value-0"
}
instant_snapshot_1 = provider.compute_api.Instant_snapshot {
    project = "value-1"
    zone = "value-1"
}
instant_snapshot_2 = provider.compute_api.Instant_snapshot {
    project = "value-2"
    zone = "value-2"
}
```

### Conditional Creation

```kcl
# Only create in production
if environment == "production":
    instant_snapshot = provider.compute_api.Instant_snapshot {
        project = "production-value"
        zone = "production-value"
    }
```

---

## Related Documentation

- [GCP Compute_api Documentation](https://cloud.google.com/compute_api/docs)
- [Getting Started Guide](../getting-started.md)
- [Installation Guide](../installation.md)
- ⬅️ [Back to README](../../README.md)
